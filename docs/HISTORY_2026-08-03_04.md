# HISTORY 2026-08-03 ~ 08-04 (아카이브)

CLAUDE.md가 150,000자 한도에 닿아 2026-08-07에 분리했다.
**아무것도 지우지 않았다** — 원문 그대로다.

이 시기에 결정된 것 중 **지금도 유효한 것**:
- 익절 구조: 조기확정 폐지 -> 본전스톱 + 캡 상향(1A 4.0% / 개장초반 2.5% / 상향 6.0%)
- 강도 하락 판정 기준선을 `entry_strength`(진입 스파이크) -> `_strength_baseline`(워밍업 후 리앵커)
- 손절을 워밍업 게이트 **위**로 (매수 직후 60초 무방비 구멍 수정)
- 시간대 계수 전 구간 1.00 (오후 완화 제거)
- 지수 하락 가드(-5%, 11:00~) / 되돌림 대기 분할매수 / 분할매도
- 버스트 문턱 4천만원
- 테스트 로그 분리(`AUTOTRADER_TEST_LOG=1` -> `logs/test_run.log`)

⚠️ 이 시기 값 중 **나중에 바뀐 것**(현행은 CLAUDE.md 본문을 볼 것):
- 무장 3.0 -> 1.5초로 완화됐다가 **08-07에 3.0초로 복원**
- 매수금액 200만 -> (08-05 실전전환 50만) -> 200만 -> **08-06 100만**
- 상승 이탈(추격매수)은 **08-06에 OFF**
- 눌림목(Pullback)은 **08-06 [E]로 슬롯 0 = 매매 중단**

---

- 2026-08-03 (월, **틱 구동 첫 실거래일**) — 오전 관측 + 오후 **익절 로직 수술**.

  [기동·인프라 — 전부 정상]
  - 08:59:03 자동 기동, 조건검색식 4개 정상 등록(주도주상위=1 / 돌파자동매매용=2 /
    눌림목자동=3 / **종가베팅=4는 매매 라우팅 제외**로 분리). 어제 만든 분리 구독이
    설계대로 동작했다. 참고로 seq는 **4**였다 — 08-02에 HTS 화면 [005]를 보고
    "seq=6"으로 추정했던 건 틀렸고, 코드가 번호가 아니라 **이름으로 찾기 때문에**
    아무 문제가 없었다(이름 기반 설계가 옳았음이 증명된 셈).
  - **FID 228 수신 정상**(09:05 워치독, 체결틱 5,181건). 이번 개편의 유일한 미검증
    가정이었고 틀렸으면 하루 매수 0건이었다. **매도 시장가도 정상 체결**(폴백 로그 없음).
  - **실시간 편입이 장중 내내 살아있었다** — 08-01에 고친 `type='02'` 파싱 효과.
    seq별 편입: 주도주상위 42 / 눌림목자동 92 / 종가베팅 254 / **돌파자동매매용 0**.
    돌파는 구독은 정상인데 HTS 조건식이 하루 종일 한 종목도 못 잡았다(코드 무관,
    사용자가 검색식이 과하게 빡빡하다고 판단 — 추가 조치 없음).
  - 14:50 종가베팅 스캔 정상(유니버스 4종목 -> 적격 0종목 -> "후보 없음").
    분봉 캐시 48파일/5.9MB가 실제로 쓰였다.
  - 429는 5건뿐(07-30 하루 2,469건 대비). 틱 구동이 REST를 거의 안 쓰는 효과.

  [눌림목이 안 사지는 이유 — 대금이 아니라 무장이 문제]
  - 눌림목 단독 3종목(039980/365660/059120)은 **대금 판정까지 가지도 못했다**.
    게이트가 `무장 -> 시간창 -> 대금` 순인데 첫 관문(강도 100·3초)에서 멈췄다.
    039980은 10회 무장했지만 **전부 09:01~09:03**이라 Pullback 창(09:25) 밖이었고,
    창이 열린 뒤엔 무장 0회. 365660/059120은 하루 종일 무장 0회.
  - **`PULLBACK_START=09:25`의 근거가 이미 소멸했다.** 09:25로 늦춘 이유는
    "개장 직후엔 당일 고가가 없어 눌림 판정이 성립 안 함"이었는데, 08-02에
    그 판정(분봉 재검증)을 코드에서 **통째로 없앴다**. 지금 눌림 판정은 전부
    HTS(3분봉 30이평·20봉내 25% 급등)가 하므로 09:00에도 성립한다.
    -> 시간창을 앞당기는 것을 검토할 것(이번엔 미적용, 익절 수술과 섞지 않기 위해).
  - **강도 100 자체는 내리지 말 것.** 100은 "매수체결 = 매도체결"이라 그 아래는
    매도 우위에서 사는 것이다. 07-30에 1B(하락 시 진입)가 실데이터 37건에서
    우위 없음으로 판명된 것과 같은 함정.

  [실적 — 이기는 매매를 하고도 수수료에 먹혔다]
  - 청산 16건, 가격기준 합계 +2.58%(평균 +0.16%). **왕복수수료 0.23%×16=3.68%를
    빼면 순 -1.10%.** 회전이 수익을 다 먹은 하루였다.
  - 유형별: 동적캡 즉시매도 **6건**(평균 +0.35%, 보유 **2.4분**) / 슬롯교체 3건
    (-1.76%) / 조기확정 2건(+1.56%) / 정체정리 2건(+0.60%) / 지수급락 1(+1.76%) /
    익절캡 1(+2.76%) / 손절 1(-3.11%).

  [🔴 발견한 로직 결함 3가지 — 파라미터가 아니라 버그]
  - **① `cap_exit`가 모든 1A에서 항상 참이었다.** 조건이
    `cap >= TP_CAP_UPGRADED`인데 1A 기본캡(0.025)과 TP_CAP_UPGRADED(0.025)가
    **같은 값**이라, 원래 의도("상향된 종목만")와 달리 1A는 **매수 직후
    (워밍업 60초 종료 시점)부터** 동적캡 즉시매도 대상이었다. 실제로 6건이
    75~250초 만에 잘렸다. -> `pos["tp_cap_upgraded"]` 플래그 기반으로 교체.
  - **② `entry_strength`가 진입 순간의 스파이크였다.** 진입은 대량체결 버스트가
    터지는 그 순간이라, 그때 캡처한 `compute_strength`(최근 10초 창)에는 버스트가
    통째로 들어가 거의 항상 국소 최고점이다. 오늘 **042700/090710/010170 세 건이
    정확히 300**(compute_strength 상한)으로 포화돼 있었다. 포화된 최고점을 기준으로
    `현재 < 기준×0.8`을 재면 **정상으로 돌아오기만 해도 '하락'**이 된다 —
    하락 판정이 구조적으로 필연이었다. **08-02에 고친 `avg_trade_value`의
    자기모순과 정확히 같은 종류**(그때도 지금 터지는 버스트가 분모에 섞였다).
    -> `_strength_baseline` / `_maybe_anchor_strength_baseline` 신규: 워밍업 종료
    후 처음 관측되는 안정화된 강도로 **리앵커**. 리앵커 실패 시 옛 스파이크로
    폴백하지 않고 **판단 보류**(폴백하면 수정이 무의미해진다).
  - **③ '익절' 로직이 손실 구간에서 발동했다** — -0.99% / -0.48% / -0.43%를
    "동적캡 즉시매도"라는 이름으로 실현시켰다. -> cap_exit 경로에 순이익>0 가드.
    손실 구간은 손절(-3%)과 손실반등(경로 B)이 담당한다.

  [🔧 익절 구조 변경 — '조기확정' -> '본전스톱' + 캡 상향]
  - **조기확정 폐지.** 순 +1.0% 도달 시 강도 미상승이면 즉시 매도하던 분기를
    제거했다. 오늘 2건이 **전부 매도 후 더 올랐다**(037070은 매도가 대비 **+12.0%**,
    439960은 **+3.4%** — 후자는 우리 봇이 17,040에 되사서 17,350에 판 실제 체결가로 확인).
  - **본전스톱 신설**(`BREAKEVEN_STOP_ENABLED/TRIGGER/FLOOR`): 같은 지점에서 팔지 않고
    **손절선을 본전으로 끌어올린다**. 하방을 먼저 닫으므로 캡을 올려도 리스크가
    늘지 않는다(최악 = 왕복수수료 0.23%).
  - **캡 상향**: 1A 2.5% -> **4.0%** / 눌림·개장초반 1.5% -> **2.5%** /
    상향목표 2.5% -> **6.0%**(`TP_CAP_UPGRADED_MAX` 신규). 기존 `TP_CAP_UPGRADED`는
    **[미사용]** 배너 — 이 값이 기본캡과 같았던 것이 결함 ①의 뿌리다.
  - **왜 트레일링이 아닌가**: 037070 실제 틱 재생 결과 고점 대비 **-0.5~1.0%
    트레일링은 현행보다 나빴다**(+0.72% vs +0.86%). 매수 직후 본격 상승 전에
    **-2.72%까지 밀리는 흔들림**이 있어 좁은 트레일은 전부 털린다. 의미 있는
    개선은 -3.0% 트레일(+9.44%)부터인데 하루치 1종목으로 정하기엔 폭이 커서,
    결과가 예측 가능한 **캡 + 본전스톱** 조합을 먼저 쓴다.
  - 037070 틱 재생 요약(매수 7,340 / 09:15까지 고점 8,310 = +13.22%):
    현행 조기확정 순 **+0.86%** / 본전스톱+캡4% **+3.86%** / 캡6% **+5.90%** /
    본전스톱+4%부터 트레일-3% **+9.44%**.
  - `daily_backtest._exit_signal`에도 본전스톱을 같이 넣어 라이브와 동기화.

  [정정 — 과잉 일반화했던 것]
  - "조기확정이 추세를 세 조각으로 잘라 손해"라고 했으나 **전수로는 사실이 아니었다**.
    회전 손익: 439960 **-1.13%** / 222800 **-1.64%** / 010170 **+5.19%**(더 싸게
    되사서 이득) -> 합계 **+2.42%로 오늘은 회전이 도움**이 됐다. 문제는 회전 자체가
    아니라 **큰 추세를 조기에 놓치는 것**이다.

  [한계 — 반드시 같이 기억할 것]
  - 틱 재생은 **037070 한 종목의 09:00~09:15**가 전부다(다른 종목은 아카이브에 없거나
    매매 시각이 아카이브 범위 밖). **숫자를 기대값으로 보면 안 되고**, "좁은 트레일은
    역효과" / "본전스톱이 하방을 닫는다"는 **구조적 결론만** 유효하다.
  - 틱 아카이브 유니버스가 09:20 시점 감시 종목 기준이라 **정작 눌림목 단독 종목이
    빠졌다**. 검증하고 싶은 대상이 수집되지 않는 구조라 선정 기준 손볼 것.
  - [검증] `test_patch_20260803.py` **32건 신설**(캡 상향/본전스톱/결함 ①②③/청산
    우선순위/백테스트 동기화). 5개 스위트 **524건 전원 통과**.
  - ⚠️ **이 변경은 08-04 08:59 재기동부터 적용된다** — 오늘 돌고 있는 프로세스는
    이미 메모리에 로드된 옛 코드를 계속 쓴다.

  [🔴 손절 구멍 — 워밍업 60초 동안 무방비였다]
  - `on_price_update`의 **워밍업 return이 손절 판정보다 위**에 있어서, 매수 직후
    60초는 얼마가 빠지든 아무 대응이 없었다. 가격 폴백 태스크(30초 주기)도 결국
    같은 함수를 부르므로 **우회 경로도 없었다**. 워밍업의 목적은 '체결강도·거래량
    처럼 매수 직후 흔들리는 지표로 성급히 판단하지 않는 것'이지 **가격 기반 하드
    손절까지 멈추는 것이 아니다** — 급락은 매수 직후일수록 위험한데 정확히 그
    구간이 비어 있었다. -> 손절 판정을 워밍업 게이트 **위**로 올렸다.
    본전스톱·익절캡·정체정리 등 나머지는 워밍업을 그대로 존중한다.
  - 손절 경로 전수 점검 결과 나머지는 정상: 주 경로는 `on_trade`가 보유 종목마다
    매 틱 `on_price_update`를 부르고, 틱이 끊겨도 30초 폴백이 받는다.
    `sell_blocked`는 매도 **연속 실패** 시에만 설정되며 그때는 포지션을 holdings
    에서 빼고 알림까지 보내는 종착 상태라 손절 누락이 아니다.

  [🔴 시간대 계수 — 완화 방향이 거꾸로였다 (우선순위 1 적용)]
  - 08-03 청산 16건을 계수별로 갈라보니 **완화 폭이 클수록 성과가 나빴다**:
    계수 1.00(7건) **+6.56%** / 0.85(3건) -0.60% / **0.65(3건) -5.29%** / 0.80(3건) +1.91%.
    그날 손실 청산 4건이 전부 11:23~12:56에 몰렸고, 오전에 번 +6.56%를 점심
    -5.29%가 거의 다 지웠다.
  - 원래 의도("안 낮추면 오후가 통째로 죽는다")는 거래를 유지하려던 것인데, 실제로는
    **유동성이 가장 마른 시간에 가장 낮은 문턱으로 들어가는** 구조였다. 점심에
    신호가 없는 건 큰 손이 없다는 뜻이지 문턱을 깎을 이유가 아니다.
    -> **점심 계수 0.65 -> 1.00**(완화 제거). 나머지 0.85/0.80/0.95는 오늘 표본으로
    판단이 갈려(0.85 손실 / 0.80 이익) 그대로 뒀다. 며칠 더 쌓아 재검증할 것.
  - 손실 4건 진입 사유 원문(참고): 059120 상대경로·강도 144.7초·비중x3.00(당일 최대
    금액 603만원)인데 -3.11% 손절 / 319400 절대경로 **1,950만원**(=3,000만x0.65) /
    090710 상대경로·평균1틱 41만원(저유동) / 336260 단일 1.00억.
    이 중 **319400은 계수 완화가 없었다면 진입 자체가 안 됐다.**

  [자동종료 — 고정 시각 -> 할 일 끝나면 즉시]
  - 마무리 3종(14:50 종가베팅 / 15:10 강제청산 / 15:30 백테스트) 완료 플래그를
    두고 **셋 다 끝나면 즉시 종료**한다. 08-03엔 15:10 청산 후에도 테마 재계산과
    조건검색 수신이 계속 돌았다.
  - ⚠️ **종가베팅(14:50)만으로 내리면 안 된다** — 뒤에 강제청산(15:10)이 있어서
    먼저 종료하면 보유가 오버나이트로 넘어간다. 그래서 조건이 AND다.
    보유가 남아 있으면 완료로 치지 않고 15:40 하드 폴백까지 살려 둔다.
    강제청산 완료 판정은 트리거 블록 **밖**에 둬야 한다 — 안에 두면 하루 1회만
    평가돼, 그 순간 매도가 pending이면 플래그를 영영 못 세운다.
  - 종가베팅·백테스트는 **실패해도 완료 처리**한다(`finally`). 부가 기능 하나가
    실패했다고 봇이 밤새 도는 옛 문제로 되돌아가면 안 된다.

  [🟡 테스트가 실거래 로그를 오염시켰다 — 재발 방지 완료]
  - 장중에 테스트를 돌렸더니 `logs/autotrader.log`에 가짜 매매가 섞였다
    (`BUY [S03]`, `SELL [000006] 손절 -4.00%` 등 56줄+). 봇 동작과 DB에는 영향이
    없지만(테스트는 Repository를 스텁 처리) **그날을 로그로 분석할 때 실거래와
    구분이 안 된다** — 실제로 "마지막 매수는?"에 테스트 종목이 나왔다.
    오염량이 커서 로그 로테이션까지 유발됐다(autotrader.log.1 생성).
  - -> `utils/logger.py`가 `AUTOTRADER_TEST_LOG=1`이면 **`logs/test_run.log`**에
    쓰도록 분리하고, 5개 테스트 스위트 최상단(core/main 임포트보다 **먼저**)에서
    이 환경변수를 세팅한다. 검증: 테스트 전수 실행 후 `autotrader.log` 크기 불변.
  - **교훈: 장중에 테스트를 돌리려면 로그부터 분리할 것.** DB는 스텁이라 안전했지만
    로그는 실제 파일 핸들을 공유하고 있었다.

  [08-03 종료 경위 — 백테스트는 실행되지 않았다]
  - 15:29:40에 `STOP_SIGNAL` 감지로 정상 종료(15:29:42 "안녕히"). 15:30 일일
    백테스트는 **20초 차이로 실행되지 못했다.** 수동 종료는 모든 것에 우선하는
    설계라 오작동이 아니다. 다만 그날 리포트가 없다는 점은 기억할 것.

  [🟡 종목명이 전부 "조건검색"으로 찍히던 문제 — 표시 전용, 매매 무관]
  - 08-03 텔레그램 '매수 체결' 3건 모두 `종목: 조건검색 (037070)`처럼 나왔다.
    원인은 `main._extract_stock_name`의 후보 키에 `'name'`이 들어 있던 것.
    키움 **실시간 편입 push(type='02')의 최상위 'name'은 종목명이 아니라
    실시간 타입 라벨("조건검색")**이고, 그 페이로드엔 종목명이 아예 없다:
    `{'type':'02','name':'조건검색','item':'079650','values':{...}}`
    그런데 'name'을 후보로 두면 "찾았다"고 판단해버려, 호출부의
    `if stock_name == stock_code:` **REST 폴백이 영영 실행되지 않았다.**
    기동 스냅샷(CNSRREQ)은 '302'에 진짜 이름이 있어 정상이었고, 08-01에 실시간
    편입 파싱을 되살리면서 이 경로가 처음 살아나 드러난 부작용이다.
  - -> 후보 키에서 `'name'`만 제거. 그러면 stock_code가 반환되고 기존 REST
    폴백이 살아난다. **REST 부담 없음** — `order_manager.get_stock_name()`이
    `_name_cache`로 캐싱해 종목당 평생 1콜이다(039980이 42회 편입돼도 1콜).
  - 매매 판정·주문·손익에는 전혀 영향이 없던 **표시 전용** 문제다.

  - [검증] `test_patch_20260803.py` 32 -> **57건**(손절 워밍업/시간계수/자동종료/
    종목명 추출 섹션 추가). 시간계수 변경으로 옛 사양을 검증하던 5건이 정확히 실패했고 전부
    새 사양으로 교체했다 — 테스트가 사양 변경을 잡아낸 정상 동작이다.
    5개 스위트 **552건 전원 통과**.

- 2026-08-03 (계속, 장마감 후) — **2차 수술: 진입 완화 + 청산 실험 + 전면 재검증**.
  오전 수술(익절/손절/시간계수)에 이어 사용자 지정으로 4건을 더 바꿨다.
  **모의투자이므로 리스크를 실험해본다**는 방침이 배경이다.

  [변경 4건]
  - **Pullback 시간창 09:25 -> 09:00** (1A와 완전 동일). 09:25의 근거였던
    "개장 직후엔 당일 고가가 없어 눌림 판정 불가"는 **08-02에 분봉 재검증을
    폐지하면서 이미 소멸**했다 — 지금 눌림 판정은 전부 HTS(3분봉 30이평 +
    20봉내 25% 급등)가 하고 여러 날 봉을 보므로 09:00에도 성립한다.
    ※ 사용자는 09:05를 제안했으나 **실측으로 09:00을 권고**했다: 눌림목 단독
      3종목 중 유일하게 무장한 039980의 무장 10회가 전부 **09:01:47~09:03:16**
      이라 09:05로는 잡히는 게 0건이었다. 나머지 둘은 무장 0회(365660) /
      10:34부터라 기존 창 안(059120)이었다.
    ※ 슬롯 경합 없음 확인 — 08-03 동시보유 최대 2종목(상한 6).
  - **무장 3.0초 -> 2.0초**. ⚠️ **백테스트로 검증한 값이 아니다** — FID228
    이력이 남지 않아 과거 재현이 원리적으로 불가능하다(틱 아카이브엔 시각·
    가격·거래량만 있음). 간접 근거는 08-03 16건의 '매수 시점 지속시간 vs 성과'
    (3~5초 1건 +1.76% / 5~20초 4건 +0.13% / 20초+ 11건 +0.03%)뿐인데 가장 짧은
    구간이 1건이라 약하다. **실험적 완화**임을 명확히 인지할 것.
  - **본전스톱 OFF**(`BREAKEVEN_STOP_ENABLED=False`). 도입 당일 저녁에 08-03
    실제 틱으로 재생해보니 개장 구간(09:00~09:10, 진입 16건)에서 오히려
    손해였다: **끔 -0.177% / +1.0% -0.325% / +1.5% -0.436% / +2.0% -0.253%**.
    변동이 커서 +1% 찍고 무장한 뒤 되돌려 본전에 털리는 일이 잦고, 캡까지
    갈 수 있었던 포지션을 0%로 만든다. 단 이 시뮬레이션의 진입은 **버스트만으로
    생성**한 것이라 무장 게이트가 빠져 실제보다 품질이 낮다 — 수치를 그대로
    믿을 수는 없다. **로직은 보존**했고 플래그만 False다.
  - **개장초반 익절캡 2.5%는 유지**(백테스트가 지지). 같은 재생에서
    1.5% -0.339 / **2.5% -0.325** / 4.0% -0.638 / 6.0% -0.447 / 캡없음 -0.973
    으로 **캡을 풀수록 나빴다**. 개장 구간은 16건 중 손절이 6건일 만큼 험하다.

  [슬롯 — 유지 결정, 근거 정정 포함]
  - ⚠️ **자본 오인 정정**: 분석 도중 "자본 1,000만원"으로 봤던 값은 **테스트
    스텁 값**이었다. 실제는 09:00:06 기록 기준 **497,017,161원**. 6슬롯을 꽉
    채워도 자본의 5.4%라 **자본은 병목이 아니고 슬롯이 진짜 상한**이다.
  - 그럼에도 **유지**를 권고했다: ① 08-03 동시보유 최대 2/6, "슬롯 없음" 탈락
    0건 ② 내일 진입 증가 요인 4개(창 확대·무장 완화·돌파검색식 가동·본전스톱
    OFF로 보유 연장)를 다 합쳐도 3배가 돼야 6 ③ **전략이 아직 순 -1.10%**라
    노출 확대는 손실 확대 ④ 이미 변경이 6개 겹쳐 슬롯까지 바꾸면 원인 분리 불가.
  - 늘릴 근거는 **진단 알림의 `1A 슬롯 없음`이 뜨기 시작할 때** 생긴다.

  [전면 재검증 — "작은 오류도 없게"]
  - **daily_backtest 실제 실행 검증**(가장 위험했던 미검증 경로). 08-03 15:30
    리포트는 STOP_SIGNAL 종료로 **한 번도 안 돌았다** — 새 상수(BREAKEVEN_*,
    캡 4.0/2.5/6.0)를 임포트하는데 검증된 적이 없었다. 봇이 꺼진 상태에서
    수동 실행 -> **11건 재현, 예외 0**.
  - **상수 정합성 불변식 30개를 테스트로 고정**(dryrun 섹션 20). 08-03 결함 ①
    (1A 기본캡 == TP_CAP_UPGRADED라 cap_exit이 항상 참)이 정확히 "값 하나를
    고치고 짝을 안 고친" 부류라, 시간창/캡/틱/슬롯/백테스트동기 5축의 모순을
    상수 수준에서 잡는다. 예: `기본캡 != 상향캡`, `무장시간 < 무장TTL`,
    `시간계수 전부 <= 1.0`, `모든 캡 > 왕복수수료`.
  - **설정 조합 통합 시뮬레이션 신설**(dryrun 섹션 21) — 바뀐 값들을 개별이
    아니라 **동시에** 태운다: 09:02 눌림목이 2.0초 무장+버스트로 매수 ->
    개장초반 캡 2.5% 적용 -> 본전스톱 OFF라 +1% 후 되돌려도 보유 유지 ->
    워밍업 중 손절 작동 -> 점심 문턱 3천만원 -> 11:00 매수는 기본캡 4.0%.
  - 배선 감사: 태스크 정의 20 = gather 등록 20, 전 태스크 루프 내 try/except,
    지연 import 심볼 10개 실존, 91개 .py 구문오류 0.
  - 낡은 주석 정리: 라이브·테스트의 "3초" 하드코딩 표기를 상수 참조 표현으로
    교체(`_legacy`/DEPRECATED 파일은 당시 기록이므로 보존).

  [🛡 지수 하락 가드 신규 (-3%, 11:00~) — 사용자 지정]
  - 기존 SEVERE_CRASH(-5%)보다 **한 단계 얕은** 방어선. 둘은 공존한다.
    판정은 **코스피/코스닥 중 나쁜 쪽**(둘 중 하나라도 -3% 이하면 발동).
  - 발동 조건: **11:00 이후** + 지수 -3% 이하. 개장 직후의 일시적 급락에
    과민 반응하지 않기 위해 11시부터 본다(11시까지 회복 못 한 하락은 그날
    추세일 가능성이 높다는 판단).
  - 효과 3단:
    ① **신규매수 즉시 중단** (`can_buy_more` + `_execute_buy` 이중 가드)
    ② **11:30까지** 순 0%(본전) 이상인 보유분은 청산. 손실 중인 건 안 판다 —
       지수 급락 중 저점 매도가 되므로 회복 기회를 준다.
    ③ **14:50** 남은 보유분(본전 미만)은 손익 무관 강제청산. 정규 강제청산
       (15:10)보다 앞당겨 약세장 오버나이트 리스크를 줄인다.
  - 청산 우선순위: **손절(-3%) > 지수 가드 > 익절캡/정체정리**. 워밍업보다도
    앞이다(가드는 가격 기반이라 강도·거래량처럼 흔들리는 지표를 안 쓴다).
  - REST 추가 부담 **0** — `_refresh_market_rates()`의 60초 캐시를 재사용한다
    (이미 `_is_severe_crash`가 매 가격갱신마다 부르고 있다). 지수 조회 실패 시
    캐시 직전값이 쓰이고 초기값이 0.0이라 **데이터가 없으면 발동하지 않는다**
    (보수적 = 매매를 막지 않음).
  - 진단 분류 `지수 하락 가드(-3%)` 신설 — 없으면 "기타"로 뭉개져 장중에
    "매수 0건"의 원인을 판단할 수 없다(08-02 '버스트 계산 실패'와 같은 교훈).
  - ⚠️ **해석 하나 확인 필요**: "11:30까지 본전 이상이면 청산"을 문자 그대로
    구현해서, **11:30 이후에 본전을 회복한 종목은 팔지 않고 14:50까지 들고
    간다**. 회복 즉시 파는 게 나으면 `INDEX_GUARD_BREAKEVEN_UNTIL`을
    14:50으로 두면 된다(상수 한 줄).

  [죽은 코드 배너 — 이름 때문에 생기는 오해 차단]
  - `PHASE1A_LEADING_SUSTAIN_SEC = 3`과 `evaluate_1a_leading_strength()`에
    **[미사용] / [DEPRECATED] 배너**를 달았다. "1A 매수도 2초로 바뀌었나"를
    확인하다 발견 — 이름이 `PHASE1A_...`라 1A만 3초로 묶는 것처럼 보이지만,
    그 함수는 08-02 틱 구동 전환 때 호출부가 교체되어 **라이브 호출 0건**이다
    (테스트만 호출). 실제 무장은 `TICK_STRENGTH_SUSTAIN_SEC` 하나가 담당하고,
    그 게이트는 **전략 라우팅보다 앞**이라 1A/Pullback에 공통 적용된다.
  - 실측 확인: 주도주상위/돌파자동매매용/눌림목자동 **세 경로 모두** 2.1초에
    무장 -> 매수 성립. 상수를 3.0으로 되돌리면 같은 입력에서 매수가 안 되는
    **반증**까지 확인했다.

  [지수 가드 -5%로 조정 + 시간정리 조건 변경 (사용자 지정, 같은 날 저녁)]
  - **무장 2.0 -> 1.5초.** (사용자가 "2초가 하한이냐"고 물었는데 **2초는 하한이
    아니라 그냥 당시 설정값**이었다 — 1.5초도 문제없이 가능하다.)
    ⚠️ 저유동 종목 주의: 08-03 실측 틱 밀도가 037070 23.2틱/초 vs
    **000400 0.4틱/초**로 60배 차이 난다. 후자는 틱이 2.5초에 하나라 요구시간을
    낮춰도 체감 완화가 거의 없고, 대신 **증거가 2틱뿐**이라는 위험만 남는다.
  - **지수 가드 임계 -3% -> -5%.** 이로써 기존 `SEVERE_CRASH`(-5%, 11:00)와
    **완전히 같은 조건**이 됐다 — 지수가 -5% 이하면 두 규칙이 동시에 켜진다.
    역할은 다르다: SEVERE_CRASH는 익절캡을 1.5%로 축소, INDEX_GUARD는
    매수중단 + 11:30 본전청산 + 14:50 강제청산. **한쪽만 고치면 어긋나므로**
    두 상수가 같은지 검사하는 정합성 테스트를 넣었다.
  - **시간정리 30분 / 정체정리 15분 -> '슬롯 만석'일 때만 발동.**
    존재 이유가 슬롯 기회비용인데 자리가 남으면 비울 이유가 없다. 08-03 실측은
    동시보유 최대 **2/6**인데도 이 규칙으로 4건이 나갔다.

  [🔴 이 변경들이 만든 충돌 3건 — 전부 잡음]
  - **① 시간정리가 지수 가드 사양을 무력화하고 있었다.** "본전 이하는 손절선이나
    14:50까지 가져간다"고 했는데, 시뮬레이션해보니 **보유 31분 종목이
    '시간정리 30분'으로 먼저 매도**됐다. 가드 발동 중엔 신규매수가 막혀 슬롯을
    비워도 쓸 곳이 없으므로, 가드 중에는 시간정리·정체정리를 억제하도록 했다.
  - **② 시간정리 경로가 두 곳이었다.** `on_price_update`와 **`check_timeouts()`**
    (틱이 안 들어오는 종목용 별도 루프). 앞쪽만 고쳤으면 뒤쪽으로 그대로
    잘려나가 조건을 건 의미가 사라졌을 것이다 — 같은 규칙이 두 곳에 있는 걸
    놓쳐 한쪽만 고치는 전형적인 사고다.
  - **③ 슬롯교체가 가드 중에 '매도만' 하고 있었다.** `try_slot_replacement`는
    정체 종목을 팔아 자리만 비우고 실제 매수는 일반 진입 경로가 하는데, 가드
    중엔 그 매수가 막혀 있다 -> **매도만 일어나고 대체는 안 되는 반쪽 동작**.
    손실만 확정된다. 가드 중 교체 중단을 추가했다.

  [시나리오 8종 실측 검증]
  | 상황 | 결과 |
  |---|---|
  | 일반장 · 슬롯 여유 · 45분 · -1.0% | 보유유지 (구버전은 매도) |
  | 일반장 · 슬롯 만석 · 45분 · -1.0% | 시간정리 30분 (슬롯 만석) |
  | 가드 · 11:10 · 슬롯 만석 · -1.0% | **보유유지** (30분 컷 억제) |
  | 가드 · 11:10 · +0.5% | 지수 가드 본전청산 |
  | 가드 · 13:00 · -1.0% | 보유유지 |
  | 가드 · 13:00 · -3.5% | 손절 (가드보다 우선) |
  | 가드 · 14:50 · -1.0% | 지수 가드 강제청산 |
  | 지수 -4.0%(임계 미달) | 일반장과 동일 |

  [백테스트 한계 명시]
  - `daily_backtest`는 **종목별 독립 시뮬레이션이라 슬롯 개념이 없어** 이
    '슬롯 만석' 조건을 재현할 수 없다. 그래서 조건 없이 30분 컷을 적용하며,
    결과적으로 **시간정리가 라이브보다 과다 집계**된다. 청산 정책을 비교할 때
    반드시 감안할 것(코드에 주석으로 박아둠). 실행 검증은 완료(11건 재현).

  [🔴 슬롯 교체 효과성 실측 — 오늘 3건 전부 순손실이었다]
  - 08-03 슬롯 교체 3건: 090710 -70,560원 / 319400 -76,500원 / 336260 -88,800원
    = **합계 -235,860원**. 그런데 효과가 나빴던 게 아니라 **구조적으로 성립할
    수 없는 상태**였다. 세 겹의 문제가 겹쳤다.
  - **① 슬롯이 만석이 아니었다.** 교체 시점 동시보유가 각각 2/6, 1/6, 2/6으로
    **4~5칸이 비어 있었다.** 대체후보가 좋았다면 아무것도 팔지 않고 빈 슬롯에
    사면 됐다 — 교체의 존재 이유(슬롯 기회비용)가 애초에 0이었다.
  - **② 대체후보(950160)는 하루 종일 한 번도 매수되지 않았다.** 3건 모두 같은
    후보를 근거로 팔았는데 DB에 매매 기록 0건 — **판 자리가 그냥 비어 있었다.**
  - **③ 근본 원인: 후보 선정 기준이 08-02 개편 때 같이 안 바뀌었다.**
    `find_replacement_candidate`는 **저장된 과거 점수**(`_watch_scores`)로 후보를
    고르는데, 08-02에 진입이 **무장(FID228) + 버스트**로 바뀌면서 "점수 높은
    후보"가 더 이상 "지금 살 수 있는 종목"을 뜻하지 않게 됐다.
    950160이 증거다 — 무장은 **9회** 했지만 버스트가 없어 못 샀고, 그 무장마저
    **11:06~11:23**인데 교체는 **12:00·12:02·13:07**이라 시각조차 안 겹쳤다.
    즉 40분~2시간 지난 죽은 신호를 근거로 보유분을 팔았다.

  [적용 — 2순위(대체후보 무장 요구)]
  - `StrategyManager.is_armed_now()` 신규 + `REQUIRE_CANDIDATE_ARMED=True`.
    이제 대체후보는 **점수 + 지금 무장 중**을 둘 다 만족해야 자격이 생긴다.
  - ⚠️ **`_armed_at`에 키가 있는지만 보면 안 된다.** 이 dict는 그 종목의 **다음
    틱이 와야** 만료 정리된다(`_maybe_tick_entry`의 TTL 분기) — 틱이 끊기면 낡은
    무장이 그대로 남는다. 08-02에 청산 후 강도 타이머가 잔류해 무장 단계를
    통째로 우회하던 사고와 **같은 구조**다. 그래서 ① TTL(120초) 경과 여부와
    ② **최근 틱이 실제로 들어오는지**를 함께 본다.
  - 판정 실패(예외)는 **자격 없음**으로 처리한다 — 관대하게 넘기면 "팔고 못 사는"
    원래 문제가 그대로 남기 때문이다.
  - 롤백: `core/slot_replacement.py`의 `REQUIRE_CANDIDATE_ARMED = False`
    (구버전 = 점수만으로 선정). 테스트로 이 경로도 검증해 둔다.
  - **1순위(슬롯 만석일 때만 교체)는 아직 미적용** — 사용자 지시로 2순위를 먼저
    넣었다. 오늘 3건은 2순위만으로도 전부 차단된다(무장 시각 불일치).
    다만 "자리가 남는데 파는" 구조 자체는 남아 있으므로 다음 차례로 검토할 것.

  [🔴 재적용 — 처음에 2순위만 넣어 사용자 의도를 절반만 반영했다]
  - 사용자 의도는 **"슬롯 만석 + 대체후보 무장 + 버스트까지 완벽히 준비"**
    였는데 처음엔 '무장'만 넣었다. ①슬롯 만석과 ③버스트를 추가로 적용.
  - 이제 교체는 셋 다 충족해야 발동한다:
    ① `occupied_slots() >= MAX_HOLDINGS` — 자리가 남으면 팔 이유가 없다
    ② `is_armed_now(후보)` — 지금 무장 중
    ③ `check_burst(후보)[0]` — 버스트도 지금 성립 = "슬롯만 비면 즉시 매수될 자리"
  - ⚠️ **부작용 인지**: 버스트 창은 5초인데 교체 태스크는 **60초 주기**라 폴링이
    버스트와 겹칠 확률이 낮다. 즉 이 조건을 켜면 교체가 **사실상 거의 안 돈다.**
    08-03처럼 근거 없이 파는 것보다는 낫지만, 교체를 제대로 살리려면 60초
    폴링이 아니라 **틱 진입 경로에서 '완전히 준비됐는데 슬롯만 없음'을 감지한
    순간** 교체하는 구조가 맞다 — 이월 과제.
  - 롤백: `REQUIRE_CANDIDATE_ARMED` / `REQUIRE_CANDIDATE_BURST`를 False로.

  [🔴 교체 '대상' 선정에도 같은 결함 ②가 남아 있었다 — 사용자가 잡아냄]
  - `find_stagnant_holding`이 아직 **`entry_strength`(진입 스파이크)**로 하락을
    재고 있었다. 오늘 저녁 `_update_dynamic_caps`와
    `_is_strength_rising_vs_entry` 두 곳은 고쳤는데 **여기만 빠졌다** — 같은
    규칙이 **세 곳**에 흩어져 있어 두 곳만 고친 전형적인 사고다.
  - 08-03 실측이 그대로 증거다. 교체된 3종목의 진입강도가
    **090710 300**(compute_strength 상한 포화) / 336260 100 / 319400 70이고
    현재강도는 5 / 9 / 45였다. **300에서 시작하면 `현재 < 진입 x 0.8` 판정을
    피할 수 없다** — 교체 대상 선정이 구조적으로 남발되고 있었다.
  - -> `_strength_baseline`(워밍업 후 리앵커)으로 교체. 기준선을 못 잡았으면
    비교 자체를 포기한다(옛 스파이크로 폴백하면 수정이 무의미해진다).

  [검증] 5개 스위트 **669건 전원 통과**
  (287 / 86 / 117->127 / 145 / 24). 테스트 로그 분리가 작동해
  전수 실행 후에도 `autotrader.log` 크기 불변(799,833바이트) 확인.

- 2026-08-03 (심야, **08-04 기동 직전 교차감사**) — 12건이 하루에 겹치는 날이라
  "각 변경이 스펙대로 도는가"(기존 5개 스위트가 보는 것)가 아니라
  **"변경 A가 변경 B의 사양을 무력화하지 않는가"**만 따로 훑었다.
  기존 669건이 전부 통과하는 상태에서 **결함 3건**이 나왔다.

  [🔴 ① 전면차단 중 우선순위 교체가 '매도만' 하고 있었다 — 08-03 교훈의 재발]
  - `_maybe_tick_entry`와 `_evaluate_1a_pullback_entry`는 `can_buy_*`가 False면
    "슬롯이 꽉 찼다"로 해석해 `_try_1a_priority_upgrade`로 보유분을 **판다.**
    그런데 그 False가 슬롯이 아니라 **지수 가드 / MDD 일손실 차단 / 지수 HALT /
    WS 재연결 격리** 때문일 수 있다. 그 경우 자리를 비워도 매수가 막혀 있어
    **손실만 확정되고 자리는 그냥 빈다.**
  - 08-03에 `slot_replacement`에서 정확히 같은 문제를 고쳤는데(가드 중 교체 중단),
    **이 경로엔 같은 방어가 없었다.** 오늘 신설한 지수 가드가 이 구멍을 처음
    도달 가능하게 만들었다 — 가드가 켜지는 날은 정의상 급락일이라, 하필 그날
    보유분을 근거 없이 팔아치우는 동작이 된다.
  - -> **`_entry_block_reason()` 신설**: "슬롯과 무관하게 신규매수를 막는 사유"를
    한 함수로 모으고, `can_buy_more` / `_try_1a_priority_upgrade` /
    두 진입 경로의 탈락사유 기록 / 진단 알림이 **전부 이 하나만** 본다.
    규칙을 복제하지 않는 것이 이 결함 클래스의 유일한 근본 해결이다.
  - 대조군으로 **과잉차단이 아님도 못박았다** — 차단 사유가 없으면 우선순위
    교체는 예전과 똑같이 동작한다(테스트 [22] 대조군).

  [🟡 ② 진단 알림이 원인을 정반대로 안내하고 있었다]
  - 가드로 하루 종일 못 사는 날에도 탈락 사유가 `슬롯 부족`으로 기록되고,
    요약 첫 줄은 **보유 0/6인데도** `🔒 1A 슬롯 없음`이라고 떴다.
    `build_entry_diagnostics`의 '전면 차단' 목록이 MDD/HALT/격리만 나열하고
    **지수 가드가 빠져** 있었기 때문(복제된 규칙 중 한 곳이 뒤처진 사례).
  - -> 여섯 상태(가드 / MDD / 격리 / 진짜 슬롯만석 / 정상 / 15:10 이후)를 전부
    렌더링해 문구가 원인과 일치하는지 확인했다. 이제 가드 날엔
    `⛔ 전면 차단: 지수 하락 가드(-5%)`가 첫 줄에 뜬다.

  [🟡 ③ 진단 라벨이 상수와 어긋나 거짓말을 하고 있었다]
  - `강도 미무장(3초 미달)` — 실제 요구는 **1.5초**(08-03 저녁 변경).
    `지수 하락 가드(-3%)` — 실제 임계는 **-5%**(08-03 저녁 변경).
    둘 다 텔레그램으로 다윤님이 읽는 문구라, 장중에 원인을 잘못 판단하게 된다.
  - -> 라벨에서 수치를 **제거**하고(`강도 미무장(요구시간 미달)` /
    `지수 하락 가드`), **라벨에 초 수·% 수치를 박지 못하게 하는 테스트**를 넣었다.
    같은 이유로 코드 주석 12곳의 "3초/2초/-3%/상한(2.5%)" 표기도 상수 참조로 정리.

  [확인만 하고 둔 것 — 정상]
  - 손절은 워밍업 중에도, 가드 중에도, 둘 다일 때도 최우선으로 발동.
  - 가드 14:50 강제청산이 **틱이 안 들어오는 종목에도** 닿는다
    (`check_timeouts`는 가드 중 정지가 정상이고, 30초 가격 폴백이 받는다).
  - 시간정리 두 경로(`on_price_update` / `check_timeouts`)가 슬롯 만석 조건에서
    **완전히 같은 결과**를 낸다(여유/만석 양쪽 대조).
  - 무장 1.5초 경계(1.4 탈락 / 1.5·1.6 통과), 09:02 눌림목 매수 + 개장초반 캡,
    슬롯교체 3조건, 청산 후 틱 상태 초기화 — 전부 실측 통과.
  - **핫패스 성능 무변화**: 평상시 1.7us/틱, 무장 상태 전체 게이트 17.2us/틱,
    REST 호출 0. 91개 .py 구문오류 0, 라이브 모듈 임포트 실패 0,
    태스크 정의 20 = gather 등록 20, 지연 import 심볼 누락 0.
  - **`daily_backtest` 실제 실행 검증**: 11건 재현, 예외 0(리포트 정상 생성).
    ⚠️ 리포트의 종목명이 전부 "조건검색"인 것은 **08-03 DB에 그렇게 저장된
    과거 데이터** 때문이다(그날의 표시 버그). 08-04부터는 정상 기록된다.

  [검증] 5개 스위트 **683건 전원 통과**(287 / 86 / 127 / 145->**159** / 24).
  통합 스위트에 **섹션 22(전면차단 교차감사)** 신설 — 가드/MDD/격리 각각에 대해
  "자리를 비우지 않는가 + 사유가 정확히 기록되는가"와 대조군(정상 시 교체 동작),
  그리고 가드 임계 경계(10:59/11:00/-4.9%/-5.0%/회복)를 못박는다.

- 2026-08-04 (화, **08-04 오전 실거래 대조 분석 + 본전스톱 재활성화**) —
  09:00~09:36 사이 1A 9건 청산(2승 7패, 순 -427,200원)이 나온 직후 사용자가
  "무장 1.5초 변경과 체결강도 하락 익절 변경 탓인지" 질문. DB 실거래 +
  코드(`_update_dynamic_caps`)를 직접 대조해 답을 찾았다.

  [무장 1.5초 — 기각] 오늘 손절 5건 중 4건이 8.8~98.1초로 옛 3.0초 기준을
  훨씬 넘겨 무장했다. 3초로 되돌려도 이 거래들은 그대로 발생했을 것 — 무장
  시간 단축과 손실 폭증 사이 상관관계 없음.

  [체결강도 하락시 익절(동적캡) — 관련 있으나 반대 방향의 인과]
  같은 09:00~09:40 창을 08-03과 정면 대조: 08-03은 5거래 4승1패 +165,345원
  (손절 0건), 08-04는 9거래 2승7패 -427,200원(손절 5건). 코드를 열어 확인한
  근본원인:
  - `_update_dynamic_caps`의 `cap_exit` 조건은 08-03 낮 동안(순이익>0 게이트가
    저녁에야 추가됨) **손실 중인 포지션도** 강도·거래량이 같이 꺾이면 잘라줬다
    — 실증: 08-03 전체에 058610(-0.76%)/006360(-0.20%)/042700(-0.25%)가
    "동적캡 즉시매도"라는 이름으로 손실 중 조기 청산됨. **버그가 우연히
    손절(-3%) 대타 역할**을 하며 손실을 -0.2~-0.8%에서 끊어준 셈이었다.
  - 08-04는 이 버그가 고쳐져 `cap_exit = pos["tp_cap_upgraded"]`(강도상승
    확인된 포지션만) **AND 순이익>0**일 때만 발동한다. 대체 경로인
    "손실반등 매도"도 "저점 대비 +1% 반등"이 반드시 있어야 해서, 한 방향으로
    꾸준히 밀리기만 하는 포지션은 어느 경로로도 못 빠져나간다.
  - **결과: 중간 탈출구가 사라져 약해지는 포지션이 그대로 -3% 손절까지
    밀린다.** 오늘 손절 5건이 전부 -3.02~-3.18%에 몰린 것이 그 증거.

  [사용자가 직접 지목하진 않았지만 같은 뿌리 — 조기확정 폐지 + 본전스톱 OFF]
  08-03 09:00대 승리 2건(037070 +1.37%, 439960 +1.76%)은 지금은 삭제된
  "익절 조기확정"(순+1.0% 도달 시 강도 미상승이면 즉시 확정) 덕분이었다.
  이 로직은 08-03 저녁 본전스톱으로 대체됐는데 **본전스톱이 OFF 상태로
  남아 있어서**, 08-04는 포지션이 살짝 플러스로 갔다가 되돌려도 잡아줄
  장치가 캡(4.0%/2.5%) 도달 전까지 전무했다.

  [조치 — 사용자 지정] `BREAKEVEN_STOP_ENABLED = False -> True`로 재활성화
  (`core/strategy_manager.py`). 로직 자체는 08-03에 이미 구현·보존돼 있었고
  플래그만 껐다 켠 것. **주의: 봇이 이미 장중(10:03) 실행 중인 상태에서
  수정** — 파이썬은 상수를 프로세스 시작 시 메모리에 로드하므로, 이 변경은
  **다음 재시작(수동 재시작 또는 08-05 08:59 자동기동)부터 반영**되고 지금
  돌고 있는 프로세스에는 적용 안 된다.
  - 테스트 갱신: `test_patch_20260803.py`의 "본전스톱 기본값 OFF" 계열
    단언(4건 실패로 잡힘)을 ON 기준으로 재작성 — 리던던트했던 검사 1건은
    제거(OFF/ON 케이스 각각 명시적으로 재확인하는 구조로 정리), 126건 전원
    통과. `test_live_dryrun_20260803.py`의 섹션 21 "본전스톱 OFF" 단언 2건도
    ON 기준으로 교체, 160건 전원 통과. **테스트가 사양 변경을 잡아낸 정상
    동작** — 5개 스위트 합계는 683건으로 그대로(126+160으로 재배분됐을 뿐).
  - ⚠️ **이 재적용은 아직 실거래로 검증 전이다.** 08-03 저녁에 켰다 끈 이유
    (무장 게이트 없는 약식 시뮬레이션에서 개장 구간 역효과)가 이번엔 배경이
    아니라, 오늘 실거래에서 드러난 "중간 안전판 부재" 문제를 메우기 위한
    조치다. 다음 거래일 본전스톱발 청산이 실제로 이익을 지키는지, 아니면
    다시 조기 확정처럼 추세를 일찍 자르는지 지켜볼 것.

- 2026-08-04 (계속, 장중 휴지기 11:56 정지 후 수술) — **되돌림 대기 분할매수 +
  분할매도 도입**. 앞선 익절 분석에 이어 "진입 품질부터 손보자"는 사용자 판단으로
  틱 아카이브를 재생해 진입 타이밍을 실측한 결과가 근거다.

  [실측 — 트리거 시점이 국소 고점이다]
  - 08-03/08-04 틱 아카이브(09:00~09:15)로 진입 6건을 재생: **6건 전부**가
    트리거 직후 60초 안에 되돌림을 겪었다 — 매드업 -0.45% / 대원전선 -1.78% /
    빛과전자 -1.59% / 원익홀딩스 -0.95% / 매드업#2 -0.62% / 037070 -1.37%.
    버스트는 정의상 "그 순간 열기가 가장 뜨거운 지점"이라 구조적으로 그렇다
    (08-03에 진입강도가 300 상한으로 포화됐던 것과 같은 현상).
  - 되돌림 대기 시뮬(실제 청산 규칙이 아닌 +5분/+10분 대용 지표):
    즉시 -0.44%/+0.97% · **-0.5% 대기 +0.03%/+1.64%** · -1.0% 대기 +0.38%/+2.05%.
  - **-0.5%를 1차로 쓰는 이유는 6건이 전부 체결(스킵 0)되기 때문**이다 —
    표본이 전혀 안 바뀌는데 진입가만 평균 +0.74% 개선되므로 **선택 편향이 없는
    순수 개선**이다. -1.0%는 성과가 더 좋지만 1건을 놓쳐(표본이 바뀌어) 근거가
    약하다. 그래서 절반씩 2단으로 나눴다(사용자 지정) — 얕게 되돌려도 절반은
    확보하고, 깊이 밀리면 나머지가 평단가를 낮춘다.
  - 손실 거래도 개선된다: 빛과전자 -3.41%->-1.74%, 원익홀딩스 -3.33%->-2.63%.
    진입가가 낮아지면 -3% 손절선까지의 거리가 그만큼 늘어나기 때문이다.

  [실측 — 강도하락 신호는 1분짜리다]
  - 신호(기준선x0.8 이탈) 이후 실제 가격: **+1분 6건 전부 음수 평균 -0.91%**,
    +3분 +0.24%, +5분 +0.22%(037070은 +6.5%). 즉 **일시적 눌림을 잡는 신호이지
    추세 종료 신호가 아니다.** 그런데 기존 코드는 이걸로 100% 영구청산했다.
  - 손해 실측: 매드업 +1.95%에 전량청산했으나 6분 뒤 +3.62%(MFE +4.39%),
    037070 +1.14%에 청산했으나 MFE +13.29% — **가용 수익의 44%/9%만** 챙겼다.
  - -> 신호의 1분 정확도는 **절반을 좋은 가격에 빼는 데** 쓰고, 3~5분 불확실성은
    **절반을 남겨** 회복을 탄다. 어느 집단인지 미리 맞힐 필요가 없어진다.
  - 잔량 트레일 3%인 근거: '고점 도달 전 되돌림'이 매드업 2.03% / 037070 3.90%.
    이보다 좁으면 본 상승 전에 털린다 — **08-03에 -0.5~1.0% 트레일이 현행보다
    나빴던 이유가 이것으로 설명된다.**

  [구현 — 안전에 민감한 지점들]
  - `_execute_sell(..., sell_qty=None)`: 부분매도 지원. **DB는 완전 청산 시
    1회만** `update_sell`을 부르고 수량은 총량, 가격은 **수량가중 평균 매도가**를
    넘긴다. 부분매도마다 부르면 status가 'closed'가 되어 잔량이 어느 행에도
    안 남는다. 이 방식이면 CLAUDE.md 이월과제 18번(부분매도 시 buy_quantity
    미반영)도 애초에 발생하지 않는다(buy_quantity를 건드리지 않으므로).
    MDD 누적은 **이번 체결분만** 더한다(부분매도분은 그때 이미 더했다).
  - `_execute_buy(..., size_frac, add_to_position)`: 2차 트랜치는 새 행을 만들지
    않고 기존 행의 수량·평단가를 갱신한다(행 하나 = 한 왕복 규약 유지).
    이미 보유 중인데 `add_to_position=False`로 오면 **덮어쓰지 않고 skip** —
    예전 구조였다면 holdings[code]를 통째로 덮어써 1차 매수분이 조용히 사라졌다.
  - `_entry_plans`: 대기 중인 종목은 **`occupied_slots()`가 슬롯으로 센다.**
    안 그러면 대기하는 동안 다른 종목이 자리를 가져가 되돌림이 와도 못 산다.
  - **살 수 없는 종목엔 계획을 걸지 않는다** — 등락률 상한 검사를 계획 생성 전에
    한다. 안 하면 진입 불가 종목이 슬롯을 120초 묶는다(테스트가 실제로 잡아냄).
    그 외 사유로 1차가 거부되면 계획을 즉시 회수하는 안전망도 뒀다.
  - `on_trade`가 보유 종목이면 return 하던 지점에 **2차 트랜치 처리**를 추가했다.
    1차 체결 순간 종목이 holdings에 들어가므로, 그대로 두면 2차가 영영 안 된다
    (테스트가 잡아낸 실제 결함).
  - `reset_tick_entry_state`가 계획도 지운다 — 안 지우면 청산된 종목의 계획이
    슬롯을 점유하고, 나중에 되돌림이 닿으면 재매수차단을 우회해 매수가 나간다.
  - 진단 분류 `되돌림 미도달(대기 만료)` 신설. 없으면 "기타"로 뭉개져 장중에
    "왜 안 사는가"를 판단할 수 없다(08-02 '버스트 계산 실패'와 같은 교훈).
  - **손실반등 매도는 전량 유지** — 분할은 '익절을 놓치지 않기' 위한 장치인데
    이 경로는 -3% 손절 전에 손실을 줄이려는 리스크 장치다. 성격이 반대다.
  - **손절은 분할과 무관하게 항상 전량** (최후 방어선 불변, 테스트로 못박음).

  [한계 — 반드시 같이 기억할 것]
  - ⚠️ 근거 표본이 **틱 아카이브 6건**(09:00~09:15)뿐이다. 되돌림 폭·트레일 폭은
    실측에서 나온 **출발점이지 검증된 최적값이 아니다.**
  - ⚠️ **되돌림 없이 그대로 솟는 진짜 폭등을 놓칠 위험**은 이 표본(6/6 체결)으로는
    측정되지 않았다. 이게 유일한 실질 리스크다 — `되돌림 미도달` 진단 건수를
    보고 판단할 것.
  - [검증] `test_patch_20260804.py` **58건 신설**(상수/대기/1·2차 체결/타임아웃
    회수/진입불가 종목/분할매도/잔량 트레일/손실반등 전량/손절 전량/계획 정리).
    기존 4개 스위트의 "즉시 매수" 단언은 새 사양(되돌림 후 체결)으로 갱신했다 —
    테스트가 사양 변경을 잡아낸 정상 동작이다. **6개 스위트 752건 전원 통과.**

- 2026-08-04 (계속) — **버스트 문턱 3천만 -> 4천만 상향** (커밋 `f78e6d6`).
  08-03~04 실거래 32건에서 **버스트 규모와 성과가 단조 관계**로 나왔다
  (추정 거래대금 = 문턱 x 건수):
  `0.8억 미만 19건 승률 37% 평균 -0.64%` / `0.8~1.5억 8건 62% +0.10%` /
  `1.5~3.0억 4건 50% +0.89%` / `3.0억+ 1건 100% +1.76%`.
  특히 **문턱에 딱 걸린 신호가 가장 나빴다** — 19건 중 다수가 정확히
  0.60억(=3천만x2건)에 몰려 있었다. 간신히 넘긴 버스트는 신호가 아니라 노이즈다.
  - 강건성(과최적화 판별): **양일 각각** 개선(08-03 -0.13%->+0.54% /
    08-04 -1.10%->+0.40%), **양 전략 각각** 개선(1A -1.21%->+0.44% /
    눌림 +0.14%->+0.51%). 손절 비중 21.9% -> **7.7%**.
  - 대가: 하루 진입 16.0건 -> 6.5건. 슬롯은 남아돌았으므로(최대 2/6) 배제한
    자리를 다른 종목이 채우지 않는다 — 그냥 거래가 준다.
  - **변경 범위는 절대 경로뿐**이다. 단일(1억)/상대(max(1천만, 평균1틱x20))는
    별도 상수라 영향 없다. ⚠️ 실측상 **상대 경로가 가장 나빴다**(8건 평균
    -0.67%, 단일 +0.52% / 절대 -0.30%) — 별도 판단이 남아 있다.
  - **테스트 픽스처 35곳 + 헬퍼 기본값을 상수 참조로 교체**했다. 3천만원을
    하드코딩하고 있어서 문턱을 올리자 무더기로 실패했다. 앞으로 문턱을 바꿔도
    테스트가 따라온다.

- 2026-08-04 (계속) — **[버그] 폴링 진입 경로가 되돌림 대기를 무력화** (커밋 `e8cf3fe`).
  당일 실거래로 발견. 되돌림 대기를 `_maybe_tick_entry`(틱 경로)에만 붙였는데,
  `on_condition_hit`/`watchlist_reentry`가 쓰는 `_evaluate_1a_pullback_entry`는
  여전히 `_execute_buy`를 직접 불렀다. 계획이 열린 직후 폴링이 즉시 매수해
  대기가 통째로 무력화됐다.
  - 실측: 036930은 트리거 133,100원에 대기(목표 132,434)를 걸고 **46초 뒤
    폴링이 133,600원에 매수** — 기다린 것보다 **더 비싸게** 샀고 계획은 0/2로
    시간초과. 004710도 동일. 333430만 정상(2/2 체결, 평단 4,313원).
    **되돌림 대기가 3건 중 1건만 제대로 돌았다.**
  - 수정: 폴링 경로도 (a) 계획이 있으면 양보하고 (b) 없으면 스스로 계획을 연다.
    **경로에 따라 진입 방식이 달라지면 사후 분석에서 원인 분리가 불가능**하므로
    두 경로를 같은 절차로 통일했다.
  - 회귀 테스트 섹션 10-b 신설(5건). 58 -> 63건.
  - ⚠️ **이 클래스의 버그가 이 코드베이스의 반복 패턴이다** — 같은 규칙이
    여러 경로에 흩어져 있다. 진입 관련을 고칠 땐 `_execute_buy` **호출부 3곳**
    (`_evaluate_1a_pullback_entry` / `_try_fill_entry_plan` / 직접호출)을
    전부 확인할 것.

- 2026-08-04 (장마감 후) — **오후 시간대 계수 완화 전면 제거** (커밋 `055e155`).
  `0.85/0.80/0.95 -> 전부 1.00`. 오후 실효 문턱 3,200만 -> 4,000만원.
  - 08-03+08-04 36건 시간대 분해:
    `09:00~10:30 21건 승률48% 평균-0.11% 최선+6.32% +2%이상4건 정체청산 0%`
    `10:30~11:30  6건 승률50% 평균-0.05% 최선+2.76% +2%이상1건 정체청산33%`
    `11:30~13:00  3건 승률 0% 평균-1.76% 최선-1.17% +2%이상0건 정체청산100%`
    `13:00~14:50  6건 승률83% 평균+0.13% 최선+0.70% +2%이상0건 정체청산67%`
  - **오후엔 큰 수익이 구조적으로 없다** — 11:30 이후 9건 중 +2% 이상 **0건**,
    최선 +0.70%인데 손실은 -1.4~2.4%. 순 기준 승자 3건 합 +1.22% vs 패자 합
    **-7.78%**. 작게 이기고 크게 지는 음수 기대값 구조.
  - **결정적 근거: 무장 시간이 오후가 2배 길다**(71.7초 vs 32.8초). 신호가
    약해서 못 사는 게 아니라 **강도 100이 오래 유지되는데도 가격이 안 움직인다.**
    오전의 버스트는 '신규 매수세 유입'이지만 오후의 버스트는 '잔여 유동성 소화'에
    가깝다. 오후 청산의 **77%가 정체성 청산**인 것이 그 증거(오전 0%).
  - 유동성 실측(08-04, 매매 유니버스 24종목 분봉):
    `09:00~10:30 분당 109.7억(100%)` / `10:30~11:30 39.9억(36%)` /
    `11:30~13:00 20.6억(19%)` / `13:00~14:50 28.1억(26%)`
    -> **오후 거래대금은 오전의 약 1/4**이다.
  - 그런데 코드는 정반대로 오후에 문턱을 20% 낮추고 있었고, **오후 9건 중 6건이
    완화 계수(0.80~0.65)로 진입**했다. 08-03에 점심 0.65를 없앤 것과 같은 결함이
    13:00~ 구간에 남아 있었던 것.

- 2026-08-04 (장마감 후) — **분석만 수행, 코드 변경 없음. 결론이 중요하니 반드시 읽을 것.**

  [A] **2차 매수(물타기) 후 -3% -> -0.5% 회복 분석** (트리거 36건 분봉 전수추적,
  손절선은 평단 -0.75% 대비 -3% = 트리거 대비 -3.73%)
  | 구분 | n | 1차(-0.5%) | 2차(-1.0%) | 2차후 손절선 | **손절후 -0.5% 회복** |
  |---|---|---|---|---|---|
  | 오전 | 27 | 21(78%) | 20(74%) | 10(50%) | **7(70%)** |
  | 오후 |  9 |  8(89%) |  7(78%) |  2(29%) | **0(0%)** |
  - **오후는 밀리면 안 돌아온다.** 다만 오후 n=2라 얇다.
  - ⚠️ **오전 70% 회복도 실제로는 못 쓴다** — 회복까지 4/15/31/33/68/69/109분
    (중앙값 33분)이 걸리는데 손절은 -3%에서 **즉시** 실행된다. 이 데이터는
    "손절을 안 했다면 돌아왔을 것"이지 "2차 매수가 유효했다"가 아니다.
  - **2차 매수의 실질 효과는 손절선을 트리거 대비 -3.49% -> -3.73%로 0.24%p
    낮추는 것뿐**이다. 그 대가로 하락 중인 종목에 물량을 두 배로 싣는다.
    2차까지 체결된 27건 중 **12건(44%)이 결국 손절선까지 갔다.**

  [B] **체결강도 vs 거래대금 — 둘 다 역지표** (96,702틱 / 1,483샘플 / 14종목,
  09:00~09:15). ⚠️ **강도는 FID228이 아니라 틱룰 기반 `compute_strength` 근사치**다.
  - 순위상관(+5분): 체결강도 **-0.052** / 거래대금 **-0.119**
  - 4분위 +5분(평균%/상승확률%): 강도 Q1 `+1.353/59.2` -> Q4 `+0.692/55.2`,
    거래대금 Q1 `+1.284/58.9` -> Q4 `+0.538/47.7` (**Q4만 50% 미만**)
  - **종목내 정규화 시 강도는 사실상 무관**(중앙값 초과 +1.017% vs 이하 +1.021%),
    **거래대금만 뚜렷한 역방향**(+0.575%/51.2% vs +1.450%/60.2%)
  - 2차원 교차(+5분 평균/상승확률): `강도低+대금低 +1.958/65.1` (최고) vs
    **`강도高+대금高 +0.737/53.0` (현재 진입조건, 최악에 가까움)**
  - 해석: **지표가 틀린 게 아니라 타이밍이 틀렸다.** 강도·대금은 상승을
    예측하는 게 아니라 **이미 일어난 상승을 확인**한다. 대량체결이 터지는 순간이
    그 상승의 정점이고 5분 뒤 평균회귀가 온다. 되돌림 대기의 방향이 옳았다는
    독립적 재확인(이번엔 96,702틱 규모).
  - 유형 비교: **A 점증형(80->90->100) 89건 +5분 50.0%/+0.551%** vs
    **B 유지형(100+ 3초) 170건 53.0%/+0.800%** vs **기준선(무작위) 55.8%/+1.019%**.
    B가 A보다 낫지만 **둘 다 무작위 진입보다 못하다.**

  [C] **장초반 고가대비 낙폭별 승률** (2,282샘플, 익절캡 2.5%/손절 -3% 시뮬,
  손익분기 승률 58.7%)
  - **낙폭 3% 이상 63.7% vs 3% 미만 63.0% — 판별력 0.7%p로 사실상 없음**
  - ⚠️ **표본 오염 심각**: 낙폭 3%+ 구간은 11/14 종목에만 있고, **340810(시선AI)
    하나가 167샘플(26%)에 승률 11.8%**. 340810을 빼면 63.7% -> **95.2%**로
    폭등한다(생존편향). **두 숫자 어느 쪽도 믿으면 안 되고, 진실은
    "낙폭만으로는 못 가른다"이다.**
  - **반등 확증(최근 30초 저가 대비 +0.3%↑)을 더하면 갈린다:**
    `낙폭3%+ AND 반등중 324건 69.6% (+0.76%)` vs
    `낙폭3%+ AND 하락중 305건 54.5% (-0.10%)` -> **15.1%p 차이**.
    하락 중 매수는 손익분기(58.7%) 아래라 기대값이 마이너스다.
  - 반등 확증 단독으로도 유효: 반등중 64.9% vs 하락중 58.6%.

  [분석 전반의 한계 — 결론을 그대로 적용하지 말 것]
  - 전부 **하루치(08-04) / 09:00~09:15 / 14종목**이다.
  - 이 15분은 기준선이 **+1.019%/5분**인 극단적 상승 구간이라, 어떤 신호든
    "이미 오른 뒤"에 걸려 구조적으로 역지표가 된다. 횡보·하락장에선 다를 수 있다.
  - 5초 그리드 샘플이라 **자기상관이 크다** — 2,282샘플은 독립 관측이 아니다.
  - **강도가 실물 FID228이 아니다.** 이 차이가 [B] 결론을 뒤집을 수 있다.
    -> **다음 세션 우선 과제: FID228을 틱 아카이브에 함께 저장**(관측만, 매매
    로직 무관). 지금은 `on_trade`에서 받아 쓰고 버린다. 며칠 쌓이면 [B]를
    실물 강도로 재검증할 수 있다.
