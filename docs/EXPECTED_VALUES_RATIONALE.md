# 기대값 블록 — 각 값의 근거 · 실측 · 롤백 (CLAUDE.md에서 분리)

> 2026-08-17 분리. **내용 무변경.** CLAUDE.md 「PC 검증 항목」의 기대값 코드펜스에서
> **들여쓴 근거 주석만** 옮겼다. **값 줄은 CLAUDE.md에 그대로 있다** —
> `audit_20260805.py [9]`와 `audit_deep_20260808.py [6]`이 그 값 문자열을 대조하기
> 때문이고, 실제로 분리 후에도 두 감사가 각각 182건/103건 전원 통과했다.
>
> 🔴 **값을 바꾸려면 반드시 여기를 먼저 읽을 것.** 이 프로젝트가 반복해서 밟은 사고가
> "근거를 모른 채 값을 되돌리는 것"이다(예: 본전스톱 무장 문턱을 낮추면 4개 바닥 열
> 전부에서 나빠진다 — 08-13 격자). 값만 보고 판단하지 말 것.

---


## `1A 09:00:00 ~ 14:50:00        PB 09:00:00 ~ 14:50:00        중복전환 10:30:00`

```
1A 09:00:00 ~ 14:50:00        PB 09:00:00 ~ 14:50:00        중복전환 10:30:00
```


## `청산 15:10                    슬롯 8 0 6 8`

```
청산 15:10                    슬롯 8 0 6 8   <- [E] 눌림 0(매매중단)/1A 8   무장 100.0 3.0
```


## `버스트 40000000 2 100000000   시간계수 [1.0]`

```
버스트 40000000 2 100000000   시간계수 [1.0]   <- 값이 1.0 하나뿐이어야 정상
```


## `버스트방향 True`

```
버스트방향 True   <- [B] 매수 체결만 버스트로 센다. False면 투매도 세던 구버전
```


## `VWAP깊은검사 True 0`

```
VWAP깊은검사 True 0   <- 🆕 08-08. 계획 생성 시 **가장 깊은 트랜치가(-0.7%)**로
```

                      VWAP 검사(검사가 != 체결가라 '계획만 열리고 체결 안 되는'
                      죽은 구간이 있었다). 뒤의 0은 **우선순위 교체 일일 한도**
                      — 🔴 08-10 장중에 6 -> **0(완전 OFF)**. 115초에 6회를
                      소진하고 3회를 유령 포지션에 낭비했다(그날 히스토리 참고)

## `초반캡 True 09:05:00 4`

```
초반캡 True 09:05:00 4   <- 🆕 08-08. 09:05 이전엔 슬롯을 4칸까지만 쓴다
```

                      (발사 면제의 짝. 남는 2칸은 09:05 이후 가속 구간용)

## `발사분리 True 09:05:00 2.5 30.0 120.0 20`

```
발사분리 True 09:05:00 2.5 30.0 120.0 20   <- 🆕 08-08. **발사 조건이 시간대로 갈린다**
```

                      09:00~09:05 게이트 면제 / 09:05~ 버스트 대신 거래대금 가속 2.5배.
                      마지막 20은 **최소 틱수**(금액 하한이 아니다 — 틱이 적으면
                      가속도가 수학적 최대 4.0으로 튀는 것을 막는다)

## `주가계수 10000.0 0.55 0.3 3.0`

```
주가계수 10000.0 0.55 0.3 3.0  <- 2,000원 x0.41 · 10,000원 x1.00 · 50,000원 x2.42
```


## `되돌림 True ((0.003, 0.5), (0.007, 0.5)) 120.0`

```
되돌림 True ((0.003, 0.5), (0.007, 0.5)) 120.0   <- -0.3% / -0.7%
```


## `상승이탈 False 0.003`

```
상승이탈 False 0.003   <- [D] 08-06에 OFF. 폭 상수는 보존(되살릴 때 재사용)
```


## `등락률하한 0.0`

```
등락률하한 0.0        <- [C] 전일종가 대비 이 값 '초과'여야 매수 (= +일 때만)
```


## `주가상한 100000`

```
주가상한 100000       <- 🆕 08-13 사용자 지정. 이 가격 **이상**은 신규 매수 안 함.
```

                      근거: 08-05 이후 청산 105건(수동매도 34건 제외)을 가격대로
                      가르니 **10만+만 유일하게 중앙값 마이너스**다 —
                        1만미만 n=59 평균 +0.16% 중앙 +0.23% 손절 36% -2%이하 22%
                        1~2만  n=22  -0.57%  -0.49%  45%  27%
                        2~5만  n=16  -0.14%  +0.20%  44%  25%
                        **10만+ n=8  -1.87%  -2.08%  62%  50%**
                      🔴 **날짜 효과가 아니다** — 산 4일 전부 그날 평균보다 열위
                      (08-06 -3.06 vs -0.98 / 08-07 -0.74 vs -0.44 /
                       08-10 -1.15 vs -0.07 / 08-11 -3.93 vs -0.91). 4/4일.
                      🔴 기전: **비쌀수록 발사 게이트가 헐거워진다.** 진입 로그
                      전수 파싱 결과 '게이트 무효' 비율이 가격과 단조 증가 —
                      1만미만 26% / 1~2만 28% / 2~5만 32% / **10만+ 50%**.
                      내역: 주가계수 절단 3(상한 3.00에 걸려 문턱이 의도보다
                      15~43% 헐거움, 73,704원부터) / 가속도 4.00 수학적상한 2 /
                      개장초반 발사면제 1.
                      ⚠️ **표본 8건**이라 프로젝트 기준(30건+3일)에 못 미친다.
                      ⚠️ **경계 10만원은 데이터로 고른 값이 아니다** — 5~10만
                         구간 표본이 **0건**이라 어디부터 나빠지는지 미확정.
                      ⚠️ 물타기는 bypass라 **이 상한을 안 받는다**(기존 보유분은
                         -3%에서 수량이 는다). rescue-add는 받아 손절로 수렴.
                      롤백: 0 = 상한 없음

## `진입숙성 60.0`

```
진입숙성 60.0        <- [F] 편입 후 이 초가 지나야 매수(기본값)
```


## `숙성조건식별 {'돌파전': 30.0}`

```
숙성조건식별 {'돌파전': 30.0}   <- 🆕 08-11 조건식 개편으로 키 변경(돌파자동매매용
```

                      -> 돌파전). 30초는 **유지**(사용자 지정 "돌파전후 비교
                      전략으로 바꿨으니 두고보자"). ⚠️ 08-11 5일 실측은
                      "숙성이 길수록 낫다"(0초 -0.804%p / 60초 -0.421 /
                      180초 +0.012, 단조)였지만 그건 **옛 조건식** 데이터다

## `확인전용 ('돌파후',)`

```
확인전용 ('돌파후',)   <- 🆕 08-11. `돌파후` 단독 편입은 **매수하지 않는다**
```

                      (다른 검색식과 겹칠 때만). 🔴 resolve_strategy가 미분류를
                      "1A"로 폴백하므로 source_flags에서 빼는 것만으론 안 막힌다
                      — `_confirm_only_reject`가 유일한 차단점이다

## `파동상한 3`

```
파동상한 3            <- [H] 그날 그 종목의 4번째 이후 거래대금 폭발은 매수 안 함
```


## `파동카운트 True`

```
파동카운트 True        <- 🆕 08-09 결함수정. **파동은 '폭발 횟수'지 '게이트 통과
```

                      횟수'가 아니다.** 08-08 발사 면제(09:00~09:05)가 무조건
                      True를 돌려주는 바람에 거래대금 0원인 종목도 60초마다
                      파동이 1개씩 쌓였다 — 실측 **158종목 중 106개(67%)가
                      09:15까지 상한을 넘겨 하루 종일 매수 불가**.
                      True면 면제 구간에서 check_burst로 **진짜 폭발일 때만**
                      센다(발사 판정은 여전히 면제). 롤백: False = 08-08 사양

## `분할매도 True 0.5 0.03`

```
분할매도 True 0.5 0.03
```


## `지수가드 -5.0 11:00:00 11:30:00 14:50:00`

```
지수가드 -5.0 11:00:00 11:30:00 14:50:00
```


## `캡 0.06 0.06 0.06 0.08        본전스톱 True   1B잔재 False   틱구동 True`

```
캡 0.06 0.06 0.06 0.08        본전스톱 True   1B잔재 False   틱구동 True
```

                      🆕 08-10 캡 전면 상향(4.0/2.5/2.5/6.0 -> 6.0/6.0/6.0/8.0).
                      매도 후 당일 고가까지 평균 +5.45% 더 갔다(41건). 반사실 —
                      '캡 6% + 미달분 종가청산'이면 -44,016 -> **+486,480원**.
                      천장은 **VI 상단**이 맡는다(그날 종가보유를 이긴 유일한 규칙).
                      ⚠️ **본전스톱과 세트**다 — 캡만 올리면 08-05의
                      '캡 도달분 77% 본전 회귀'가 재현될 수 있다

## `본전스톱무장 0.025`

```
본전스톱무장 0.025      <- ⚠️ **08-15부터 비활성**(확정익절이 대체). 값은 롤백용으로
```

                      보존돼 있고 감사가 계속 대조한다. 아래는 도입 당시 근거다.
                      🆕 08-10. 순 +1.0% -> **+2.5%**. 허용 되돌림이 0.8%p뿐이라
                      상승 초입 흔들림에 털렸다(6건 평균 +0.46% 실현, 그 뒤 +6.9% 더 감).
                      🔴 08-13 격자 재확인 — **낮추면 4개 바닥 열 전부 나빠진다**
                      (2.5 > 2.0 > 1.5 > 1.0). 낮추면 더 많이 무장하고 바닥이
                      **승자를 자른다**(조건검색 +6.05->+1.00 / 서산 +4.39->+0.77).
                      -> 문턱은 2.5% 유지가 맞다

## `확정익절 True 0.02`

```
확정익절 True 0.02      <- 🆕🆕 08-15 사용자 지정. **본전스톱을 대체한다**(상호 배타).
```

                      순 **+2.0% 도달 즉시 50% 분할**, 잔량은 3% 트레일.
                      되돌아오길 기다리지 않는다 — 본전스톱은 '무장(+2.5%) 후
                      바닥(+1.0%) 이탈'이라 **경로**를 판정했는데, 실측상 그게
                      **곧 크게 갈 종목을 정확히 골라 +1%에 파는** 형태였다.
                      근거(실매수 150건 / 8일 종일분봉, 비관·낙관 + 슬리피지 0.2%):
                        현행 본전스톱 2.5/1.0  비관 0.326 / 낙관 -0.240 / 승률 55%
                        **이 안 (+2% 50%분할) 비관 0.360 / 낙관 -0.046 / 승률 71%**
                      날짜별 비관 5승3패 / 낙관 6승2패 — **두 가정 모두 우세**.
                      발동 건만 떼면 격차가 더 크다(현행 +0.72% vs OFF +1.35%) —
                      되돌아온 종목 상당수가 그 뒤 익절캡(6%)까지 갔다
                      (대한광통신 -1.32 -> +7.62 / 알트 +4.52 -> +9.22).
                      ⚠️ 같은 구조에서 **문턱만 올리면 더 낫다**(+2.5%는 0.486/0.148).
                      ⚠️ **전량이면 더 낫다**(+2% 전량 0.657/0.719). 50% 분할은
                         성과 일부를 포기하고 상승 참여를 남기는 선택이다(사용자 지정).
                      🔴 `partial_exited` 가드 필수 — 없으면 동적캡·VI·반등소진이
                         남긴 **잔량도 +2%에서 전량 정리**되어 08-12에 고친
                         "분할 잔량이 다음 틱에 털린다"와 같은 결과가 된다.
                      🔴 불변식: 0 < 확정익절 < 익절캡, 본전스톱과 elif로 배타.
                      롤백: `FLAT_TP_ENABLED = False` -> 본전스톱(2.5/1.0) 복귀

## `본전스톱분할 True`

```
본전스톱분할 True       <- 🆕 08-14 사용자 지정. ⚠️ **08-15부터 확정익절이 이 스위치를
```

                      공용으로 쓴다**(False면 확정익절도 전량 청산).
                      아래는 08-14 도입 당시 근거다 — 바닥에서 **전량이 아니라 50%만** 팔고
                      잔량은 고점대비 3% 트레일(PARTIAL_EXIT_TRAIL)에 맡긴다.
                      근거: 08-14 본전스톱 5건 종일 분봉 반사실 —
                        현행(전량) +30,968 / 옛바닥0.2% +34,837(비관)·+48,253(낙관)
                        **분할 +58,155(비관)·+37,844(낙관)** <- 두 가정 모두 최고
                      구조: VI상단·동적캡·반등소진이 이미 50% 분할이고
                      **본전스톱만 전량**이었다(정합성 논거).
                      🔴 `be_partial_done` 가드 필수 — 없으면 분할 후에도 순이익이
                        바닥 아래라 **다음 틱에 잔량 전량청산**되어 기능이 무효가 된다
                        (08-12 VI 상단에서 같은 결함을 실측으로 잡았다).
                      ⚠️ 표본 5건·하루치. 근거는 정합성이지 성과 입증이 아니다.
                      롤백: False

## `본전스톱바닥 0.01`

```
본전스톱바닥 0.01       <- 🆕 08-13 사용자 지정. 0.002 -> **0.010** ('본전'이 아니라 +1% 확보).
```

                      근거: 본전스톱 발동 **12건 전수 실측** —
                        고점 순 평균 +1.85% -> 실현 순 평균 **+0.07%** = **1.78%p 반납**
                        실현 +1.0% 미만 **12/12건** / 마이너스 **4/12건**
                        (제주반도체 +2.09->-0.08 / 포톤 +2.86->-0.04)
                      격자(실매수 59건 x 실틱 x 실물 on_price_update, 결정론 확인):
                        무장2.5 기준 바닥 0.2%=+0.643 / 0.5%=+0.651 /
                        **1.0%=+0.698(최고)** / 1.5%=+0.650. 날짜별 3승1패4무.
                      허용 되돌림 2.3%p -> **1.5%p**.
                      ⚠️ **당장 효과는 거의 0이다** — 08-10에 문턱을 2.5%로 올린 뒤
                        본전스톱은 **한 번도 발동하지 않았다**(08-11~13 청산 57건 중 0건).
                        '발동할 때 더 좋은 가격에 나가게' 하는 보험이다.
                      🔴 불변식: 바닥 < 무장 (같거나 크면 무장 즉시 청산)
                      롤백: 0.002

## `정체시간정리 False`

```
정체시간정리 False     <- 🆕 08-10 **OFF**. 8건 -20,560원 vs 종가보유 +25,715원.
```

                      15~30분은 '방향이 안 났다'고 단정하기엔 너무 짧다
                      (코칩은 시간정리 -1.32%에 잘리고 그 뒤 +12.7%까지 갔다)

## `변동성손절 True 120.0 20 1.5 -0.045 -0.055`

```
변동성손절 True 120.0 20 1.5 -0.045 -0.055
```


## `고정손절 -0.045`

```
고정손절 -0.045       <- 🔴 08-11 **손절 -3% -> -4.5%** (사용자 지정).
```

                      근거: 08-11 손절 9건 중 **6건이 5분 이내**(중앙 2.6분,
                      최단 30초)였고, 손절된 5종목이 **전부 22~26분 안에 재무장**
                      했다 = 노이즈에 털리고 되돌아온 것. 그날 -4.5%를 넘긴 건
                      STX그린로지스(-5.65%) 하나뿐이라 8건이 살아남았다.
                      ⚠️ 실측상 **74%가 하한(-4.5%)에 붙는다** — 종목별 차등이
                      대부분 사라지고 '고정 -4.5% + 변동성 큰 종목만 -5.5%'에
                      가까워진다(평균 손절폭 -3.54% -> -4.71%)

## `추가매수관찰 15.0 0.055 0.06`

```
추가매수관찰 15.0 0.055 0.06   <- 🔴 관찰바닥을 0.045 -> **0.055**로 같이 내렸다.
```

                      **안 내리면 손절선(-4.5%)과 같아져 관찰 창이 0이 되고
                      '손절 대신 추가매수'가 원리적으로 죽는다.**
                      대가: 미구조분 최대손실이 -4.5%가 아니라 **-5.5%**.
                      불변식: 손절 4.5% < 관찰바닥 5.5% < 최종선 6.0%

## `물타기 True 0.03 2000000 True`

```
물타기 True 0.03 2000000 True   <- 🆕 08-11 **-3% 물타기**(사용자 지정).
```

                      원가 대비 -3%면 **조건 없이 동일 주식수 추가매수**.
                      포지션당 1회 / 종목당 총 200만원 캡 / 진입 게이트
                      (등락률·VWAP·VI·숙성·확인전용) 우회.
                      🔴 **MDD 일손실 차단과 지수 가드는 존중**(마지막 True).
                      🔴 **당일 매수분에만** — 재시작 복원분은 제외한다.
                      ⚠️ **백테스트는 판정 불가다**: 실전 5일 금액 기준으로
                      비관 +29,206원 / 낙관 -93,569원으로 **부호가 뒤집힌다**.
                      상위 3건을 빼면 -249,832원이고 날짜별 3승 2패다.
                      -> `[물타기]` 태그와 카운터로 **실거래 채점**할 것

## `물타기컷오프 11:00:00 3 5.0`

```
물타기컷오프 11:00:00 3 5.0   <- 🆕 08-12 사용자 지정 + 결함수정.
```

                      · **11:00까지만** 물타기한다. 08-12 실측으로 컷오프가
                        없어 **15:09:59에도 발동**하는 것을 확인했다 —
                        그 시점 물타기는 반등할 시간이 원리적으로 없다.
                      · 뒤의 `3 5.0`은 **주문 거부 시 재시도 상한 3회 /
                        쿨다운 5초**다. 🔴 상한이 없어서 50틱에 주문을
                        **50회** 냈다(실측). 예수금 부족처럼 지속되는
                        거부에서 REST가 429로 막히면 **매도 주문까지 밀린다**.
                      · 3회 실패하면 포기하고 손절·최종선이 그대로 받는다

## `강제청산 False 15:10`

```
강제청산 False 15:10   <- 🔴🔴 08-12 사용자 지정 **시간 기반 자동청산 전면 폐지**.
```

                      "15:10 강제청산은 없는걸로 — 내가 수동매도하던지
                      오버나이트 할 수 있게". 시각 상수(15:10)는 롤백용 보존.
                      => **남은 청산은 전부 가격 기반**이다(손절/익절캡/
                         본전스톱/VI상단/반등소진/물타기 후 최종선).
                      ⚠️ 신규매수 하드컷오프(15:10)와 진입종료(14:50)는 **유지**

## `지수가드강제청산 False`

```
지수가드강제청산 False   <- 🆕 08-12. 지수 -5% 날의 14:50 강제청산도 같이 껐다.
```

                      ⚠️ 11:30까지의 **본전 이상 청산(1단계)은 생존**한다 —
                      이익 구간에서만 도는 익절 계열이라 손실을 확정하지 않는다

## `진입유효창 1800.0`

```
진입유효창 1800.0      <- 🆕 08-12 장마감 후. **편입 후 30분 지나면 매수 포기**.
```

                      숙성(하한 30/60초)의 반대편 경계 = 진입 유효창 [30초~30분].
                      근거: 8일 104건을 편입→매수 지연으로 분해 —
                      1~3분 MFE **+7.00%**(최고) / 10~30분 +2.59% /
                      **30분~ +1.79%(절반이 1% 미만 = 사실상 못 먹음)**.
                      corr(log10 지연, MFE) **-0.291**, 30분 컷 **5/5일 우세**.
                      대표: 08-12 JW신약 편입 09:01 -> 매수 09:39(38분),
                      MFE -0.12%로 사자마자 밀렸다.
                      🔴 기준은 `_first_seen`(그날 **첫 편입**, setdefault라
                      재편입해도 불변) — 검증 전제와 코드가 일치한다.
                      ⚠️ 조건식으로 풀 문제가 아니다(편입 직후가 가장 좋다).
                      롤백: 0 = 상한 없음

## `VI분할 True`

```
VI분할 True            <- 🆕 08-12 장마감 후. VI 상단 확정매도를 **50% 분할**로.
```

                      근거: 8일 9건 실현 +2.37%인데 **판 뒤 30분 +5.67% /
                      종가까지 +8.85%, 78%가 +3% 이상** 더 갔다(전 청산
                      사유 중 가장 일찍 판다). 목적이 익절이 아니라 VI 발동
                      회피이므로 절반만 빼서 리스크는 줄이고 상승은 절반 참여.
                      잔량은 고점대비 3% 트레일. 롤백: False = 전량 매도

## `분할잔량보호 True`

```
분할잔량보호 True       <- 🆕 08-12 **결함수정**. 분할 1차 후 동적캡이 재발동하면
```

                      잔량이 전량 청산되고 있었다(`partial_exited`면 분할
                      분기를 못 넘어 전량 경로로 떨어짐).
                      실측: 일성건설 45초 / 마녀공장 44초 / 한켐 54초 만에
                      잔량이 털려 "절반은 추세를 남긴다"는 설계가 무력화됐다.
                      -> 잔량은 **트레일에만** 맡긴다(손절·최종선은 그대로).
                      롤백: False

## `편입가앵커 False`

```
편입가앵커 False        <- 🔴 08-12 저녁 **OFF로 되돌림(검증이 틀렸다)**.
```

                      · 켰던 근거: 편입→매수 사이 평균 +1.71% 양보 /
                        08-05~11 90건에서 "체결 90/90, 순 +1.60->+1.68%p, 4/5일".
                      · 🔴 **그 시뮬레이션에 결함이 있었다** — 2차가 미체결일 때
                        `현행 실제 매수가`를 그대로 쓴다고 가정했는데, 실제로는
                        **1차만 체결되어 평단이 올라간다.** 가드는 2차 '가격'만
                        보장하지 '평단'을 보장하지 못한다.
                      · 실측(08-12 틱, 09:00~09:10 n=7): 트랜치 13->**11개**,
                        평단 +0.008%, 순 현행 +8.55%p vs C안 +8.53%p(**현행 우위**).
                        닿으면 싸지고(동양파일 -8.5) 안 닿으면 2차가 통째로
                        날아가 비싸진다(남화토건 **+12.5**).
                      · 09:10 이후(분봉 n=9)는 반대로 C안 +0.18%p 우위 —
                        **구간마다 방향이 뒤집혀 판정 불가**. 그래서 껐다.
                      ⚠️ `_cond_hit_prices` 캐시는 **계속 쌓인다**(REST 0콜) —
                      재검증 재료를 잃지 않기 위해서다.
                      되살리려면 True (단 미체결 효과를 반영해 재검증 후)

## `오버나이트격리 True`

```
오버나이트격리 True     <- 🆕 08-12. 장을 넘긴 보유분은 **다음 기동 때
```

                      status='manual'로 격리**해 봇 관리에서 뺀다(사용자 지정).
                      🔴 판별은 `buy_time`의 **날짜** — 장중 재시작이면 당일분은
                      종전대로 정상 복원된다(08-10 같은 상황 보호).
                      ⚠️ 격리분은 손절·익절·슬롯 어디에도 안 걸린다.
                      사용자가 정리하지 않으면 **매일 쌓여 예수금이 잠긴다**

## `잔고지연가드 45 300 120`

```
잔고지연가드 45 300 120  <- `main.py`의 세 유예. 앞의 둘은 08-07·08-12에 만든
```

                      **'매수가 아직 잔고에 안 뜬다'** 쪽이고, 세 번째
                      `RECONCILE_SELL_GRACE_SECONDS`(🆕 08-15)는 **정반대 방향**이다 —
                      봇이 50% 분할매도한 뒤 서버가 한동안 **팔기 전 수량**을
                      그대로 주는 창을 '수동 추가매수'로 오인하던 것을 막는다.
                      🔴 오인하면 기록만 틀리는 게 아니라 `_maybe_average_down`이
                      `pos["qty"]`로 주문하므로 **물타기가 2배를 산다**(A/B 재현).
                      보류 조건은 ⓐ판 지 120초 이내 **그리고** ⓑ서버 수량이
                      `tracked + _realized_qty`(팔기 전 수량) 이하 — 둘을 **동시에**
                      만족할 때만이라 진짜 추가매수는 즉시 감지된다.
                      불변식: 세 번째 > SYNC_INTERVAL(15) x 2 = 30초이고,
                              실측 잔고지연 99초를 덮어야 한다.
                      롤백: `RECONCILE_SELL_GRACE_SECONDS = 0`

## `반등소진매도 True 0.0`

```
반등소진매도 True 0.0   <- 🆕 08-10. **본전 이상에서만** 발동(구 사양은 손실 구간 전용).
```

                      08-10 오전 8건이 전부 손실 -50,636원 = 그날 손실의 82%

## `우선순위교체 0`

```
우선순위교체 0         <- 🆕 08-10 **완전 OFF**. 115초에 일일한도 6회를 소진하고
```

                      그중 3회를 유령 포지션에 낭비했다

## `VI True 0.1 0.005 2           VI매수차단 False 0.03`

```
VI True 0.1 0.005 2           VI매수차단 False 0.03   <- 08-06 사용자 지정으로 OFF
```


## `VWAP진입 True 0.5 09:05:00 1000000.0`

```
VWAP진입 True 0.5 09:05:00 1000000.0   <- 08-07 장마감 후 **ON**. 단위는
```

                      백만원 확정. 현재가가 VWAP보다 +0.5% 초과 위여야 매수

## `일봉눌림태그 True 3.0 0.6 20 5 2`

```
일봉눌림태그 True 3.0 0.6 20 5 2   <- 08-07 신설. **관측 전용**(매매 무영향).
```

                      entry_reason에 `[일봉눌림 D-N 돌파선 P]`만 남긴다

## `시가대비 8.0   급등강화 6.0 1.5`

```
시가대비 8.0   급등강화 6.0 1.5   <- 6~8%는 버스트 1.5배, 8%~는 매수보류
```

