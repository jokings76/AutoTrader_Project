# AutoTrader_Project 히스토리 — 2026-08-01 ~ 08-02

CLAUDE.md가 150,000자 한도에 닿아 분리했다(2026-08-07).
**당시 경위가 궁금할 때만** 찾아볼 것 — 지금 살아있는 사양은 CLAUDE.md의
"현재 전략 요약"과 "다음 할 일"에 전부 반영돼 있다.

이 기간의 핵심: 틱 구동 전환(08-02) / 1B·1L 코드 완전 삭제 /
치명 버그 9건 수정(08-01) / 실전 드라이런 검증 체계 구축.

---

- 2026-08-01 (토, 장 없음 — 정적 분석 + 전면 수정 세션) — **"이관 후 실행에
  문제없는지 꼼꼼히 확인"** 요청에서 출발. 발단은 "어제도 문제없다고 했는데
  실제 장중엔 전략이 뭔가 빼먹거나 그냥 통과하거나 조건검색과 연동이 안 됐다".
  정적 분석 + 실거래 로그 대조 + 격리 재현으로 **9건 발견, 그중 3건은
  08-03에 하루 종일 매매 0건이 될 수준**이었다.

  [치명 3건]
  - **실시간 조건검색 편입(type='02')이 100% 유실되고 있었음** — 키움은 조건검색
    실시간을 두 형태로 보낸다: (1) 조건식 등록(CNSRREQ search_type=1) 응답은
    플랫 구조(`{'9001','302','10',...}`) (2) 실시간 편입/이탈 push는 `type='02'`
    + **values 중첩**(`{'841','9001','843','20'}`). `_dispatch_condition_item`이
    item 최상위에서만 9001/843/841을 찾아서 (2)가 전부 `stock_code=''` -> 조용히
    return 됐다. 07-31 로그 실측: WS 레벨 편입/이탈 로그 **0건**, 그날의
    "편입 신호" 21건은 전부 기동 스냅샷이었다. `task_condition_snapshot_poll`도
    비활성이라 **기동 1회분 외에는 하루 종일 신규 종목이 안 들어오는 상태**였다.
  - **08:59 기동 시 초기 스냅샷 전량 폐기** — `on_condition_hit`이
    `get_current_phase()`가 None(09:00 이전)이면 이름/조건명 기록 **전에**
    return 했다. 07-31 실측 setup 소요 38초라 08:59 기동이면 스냅샷 처리가
    09:00 전에 끝나 통째로 사라진다. 위 항목과 겹치면 조건검색 종목 0건.
  - **10:30 이후 1A 사실상 사망** — `tick()`이 PULLBACK_END 이후 미보유
    감시종목을 10초마다 `stop_watching`(-> `trade_flow.reset`)했다. 원래
    1B(10:30 종료)용 정리인데 새 1A가 같은 버퍼를 쓰게 되면서 자기 데이터를
    계속 지우는 구조가 됐다. 재현: 동일 틱 흐름에서 60초 거래대금 6천만 ->
    1천만(1/6)으로 떨어져 유동성 필터 탈락.
    **1A 창(09:00~14:50)의 70%가 죽어 있었다.**

  [높음 2건]
  - **당일 시가 계산 오류** — `candles[-1]["open"]`은 내림차순상 '가장 오래된 봉'
    이고, 개장 직후엔 키움이 요청 개수를 채우려 전일 봉을 붙여주므로 사실상
    **전일 시가**였다(07-30 09:01 로그: `candles[-1]` = 20260729 15:09봉).
    그 값으로 "주도주상위 시가대비 +5% 매수보류"를 재던 탓에 실제로는
    '전일종가 대비 +5%'가 되어 개장 초반 주도주 대부분이 잘렸다.
    `_today_open()` 신규(time_str로 당일 봉만 걸러 가장 이른 봉의 시가).
  - **보유 종목에 체결틱이 안 쌓임** — `on_trade`가 보유 종목이면
    `on_price_update` 후 즉시 return 해서 `trade_flow.add_tick`에 도달 못 했다.
    매수 10초 후부터 `compute_strength`가 중립값만 반환 -> 동적 익절캡 상향/
    조기확정, 동적캡 즉시매도, 손실반등 하이브리드 매도가 전부 '판단 불가'로
    **영구 미발동**. (07-31에 매수 66초 만에 발동했던 건 매수 전에 쌓아둔 틱이
    남아있던 구간뿐이었다.)

  [중간 4건]
  - `slot_replacement`에 중립값 가드 누락 — 07-31 조기청산 사고와 같은 원인인데
    `_update_dynamic_caps`에만 넣고 여기는 빠져 있었다.
  - 1A가 `score`/`score_threshold`를 안 실어 확장슬롯·슬롯교체 대상에서
    제외됐다. 겸사겸사 전략 간 점수 스케일 불일치(1A 강도 0~300 / Pullback 0~9)도
    해결 — 이제 **컷라인 대비 비율**로 정규화해 비교한다.
  - `_record_watch_list`가 `watch_list_today` 하나로 "재평가 후보"와 "DB 기록"을
    겸해서, 지연평가 경로가 먼저 후보에 넣으면 DB 행이 영영 안 남았다(일일
    백테스트/틱아카이브 유니버스에서 누락). `_watch_db_written`으로 분리.
  - **pending 오버부킹** — `can_buy_more`가 `len(holdings)`만 세는데, 주문 REST가
    도는 동안(429 재시도 2초 blocking 포함) 종목은 pending에만 있다.
    `on_condition_hit`과 `watchlist_reentry`는 별도 `to_thread`라 급등이
    동시다발하면 같은 마지막 칸을 둘이 통과할 수 있었다. `occupied_slots()` 신규.

  [전략 변경 — 사용자 지정]
  - **1B 전면 비활성화** (`PHASE1B_ENABLED=False`). 1A와 트리거 방향이 정반대
    (1A=매수세 폭발 시 진입 / 1B=60초 -2% 급락 시 진입)라 같은 후보 풀에 동시
    적용하면 진입 타이밍이 오락가락하고 어느 로직이 그 자리를 만들었는지 사후
    분석도 불가능하다. 코드는 플래그로 보존. **phase1b 객체 자체는 유지** —
    이제 1B 전략이 아니라 **1A의 체결틱·호가 데이터 파이프라인**
    (TradeFlowTracker/OrderbookTracker)이라 없애면 1A가 눈이 먼다.
  - **1A 진입조건 재설계**: 체결강도 100 이상 60초 -> **3초** + 대량체결 버스트를
    AND로 (3천만원+ 단일체결 **3건 이상** OR 단일 **1억원+ 1건**). 틱 부족은
    명시적 탈락 — 기존 `is_intensity_sustained`는 틱이 없는 구간에 중립값 100을
    채워 임계값 100을 **그냥 통과**시켰다(데이터가 없을수록 쉽게 뚫리는 구조).
  - **호가 기반 하이브리드 주문**: 매도 1~3호가 잔량 '금액'이 5천만원 이상이면
    시장가(`trde_tp="3"`), 미만(빈 호가창)이면 지정가. 호가 스냅샷 없음/길이
    불일치/예외는 **전부 지정가로 수렴**하고, **시장가 거부 시 지정가 1회 자동
    폴백**. 이 계정은 지금까지 지정가만 내봐서 모의서버의 시장가 지원이 미검증.
    `on_orderbook`도 재활성화(07-31 WallDetector 제거 때 통째로 `return`이 되어
    OrderbookTracker가 한 번도 갱신되지 않고 있었음 — 적재만 하고 FSM은 안 함).
  - **전략 라우팅 배타화**: cond_name에 "눌림목자동" 포함 -> Pullback만,
    그 외 -> 1A만. 주문 직전(`_execute_buy`)에도 하드가드를 둬서 다른 경로가
    생겨도 눌림목 종목이 1A로 새지 않는다.
  - **매수 등락률 상한 전략별 분리**: 1A 12% -> **16%**, 눌림목 -> **10%**.
    1A는 추세추종이라 이미 오른 종목이 더 가는 경우를 12%가 잘라내고 있었고,
    Pullback은 되돌림을 사는 전략이라 많이 오른 종목일수록 되돌림 폭도 커서
    위험하다. `_entry_change_cap`이 sub_strategy와 cond_name 둘 다 보고
    보수적인 쪽으로 수렴시킨다.
  - 1L은 07-31부터 계속 주석 처리 상태(변경 없음).

  [슬롯 효율 — 실거래 73건 실측]
  - 07-28~31 청산 73건 분석: 슬롯 용량 8,400 슬롯·분 중 실사용 **834분 = 9.9%**.
    **슬롯은 부족하지 않고 후보/신호가 부족한 것**이 병목이다(슬롯 개수를
    늘리는 건 의미 없음).
  - 슬롯·분 배분: 손절 31% / 시간정리 25% / 슬롯교체 24% / 익절 14% /
    트레일링 5% -> **돈을 번 청산은 슬롯·분의 19%만 사용**. 분당 손익은
    익절 +0.385%, 손절 -0.326%, 시간정리 -0.026%.
  - 조치 (1) 10:30 이후 1A 캡 3 -> 5 (Pullback 창은 하루의 26%뿐인데 3칸을 상시
    예약해 이후 영구히 놀았음) (2) 이로써 **확장 슬롯(7~8) 구조적 도달 불가
    해소** — 1B/1L을 끄면서 1A(3)+눌림(3)=6=MAX_HOLDINGS가 되어 7번째를
    요청할 주체가 아무도 없는 데드코드였다 (3) **정체 포지션 15분 조기 정리**
    (순손익 ±0.5% 밴드, 30분 컷은 최후 방어선으로 유지).

  [종목 우선순위 — 조건검색식 우선순위 대체]
  - 사용자 요구: "조건검색식 우선순위보다 종목 우선순위가 낫다. 다만 surge는
    빠른 포착이 생명인데 순위를 매기면 딜레이가 생길 것 같다."
  - 설계 원칙 = **비교는 미리 해두고 트리거는 즉시 실행**:
    (1) 슬롯에 여유가 있으면 tier를 **조회조차 안 한다**(딜레이 0. 실측 슬롯
    사용률 10%라 대부분의 트리거가 이 경로) (2) 만석일 때만 tier를 보되 후보를
    모아 정렬하지 않고 이미 틱 버퍼에 있는 값을 조회만 한다(REST 0콜)
    (3) 지표를 **자기 대비 비율**로 만들어 조건검색식별 우선순위 개념 자체를
    제거 (4) tier를 모르면(틱 부족) 남의 슬롯만 못 뺏고 빈 슬롯 매수는 정상 진행.
  - `candidate_tier = 거래대금 가속도(최근30초 / 120초평균) x 체결강도배수 x
    신호배수`. 검증: 절대 거래대금이 5배 큰 BIG(균일·중립) tier 0.83 vs 규모
    1/10인 SMALL(최근 집중·매수우세) tier 10.0 — **소형 급등주가 대형주에
    안 밀린다.**
  - 교체 대상은 "아직 결과가 안 난 자리"(순손익 ±0.5%)로 제한 — 오르고 있는
    포지션을 순간 tier 스파이크로 빼앗지 않는다. 기존 09:00~09:10 시간제한은
    해제(슬롯 만석 자체가 이미 충분한 게이트).

  [제안 A/B/C 적용]
  - **A 신호 계층화**: tier에 단일 대량체결 가중(1억+ x1.5 / 3천만+ x1.2).
    진입 트리거는 두 조건을 동등 취급하되 **슬롯 경합 시에만** 차등을 둔다.
    이산·유한 배수라 tier 폭주 없음.
  - **B 조건검색식별 성과 보정**: `strategy_performance`의 축소추정을
    `cond:주도주상위` 등 **조건식 축**으로도 적용. 병합 조건명은 대표 키 하나로
    접어 표본이 쪼개지지 않게 한다. **100을 바닥 고정**(성과가 좋아도 진입이
    원래보다 헐거워지지 않음). 우선순위가 아니라 문턱 조정이라 **지연 0**.
  - **C tier 기반 금액 가중**: tier 1.0 -> 1.0배, 1.5 이상 -> 1.5배(상한).
    **위쪽으로만** 움직인다 — 축소 방향까지 열면 평범한 후보의 매수 수량이
    0주가 되어 조용히 매매를 잃는다. 최대 노출 8슬롯 x 300만 = 2,400만원.

  [진입 진단 알림 — 신규]
  - "조건검색엔 계속 포착되는데 매수가 안 된다"를 장중에 원인별로 판단하기 위한
    관측 인프라 — `StrategyManager.build_entry_diagnostics()` +
    `main.task_entry_diagnostics`. 텔레그램 '주식 따릉이'(signal) 채널.
  - **정상 필터링과 코드/인프라 이상을 분리해서 보여주는 것**이 핵심:
    사유별 종목 수 집계 + 경고(감시중인데 체결틱 0 -> 0B 구독 이상 / 후보인데
    감시 미시작 -> 라우팅 이상 / 최근 10분 평가 기록 없음 -> 재평가 루프 정지 /
    실시간 편입 수신 0건 -> WS 조건검색 구독 이상) + 전면차단 표시
    (MDD/HALT/WS격리/15:10 컷오프).
  - 알림 피로 방지: 평상시 30분 주기, **경고가 잡히면 10분** 주기.
  - 사유 분류는 `_REJECT_RULES`(문자열 -> 카테고리 매핑)로 하므로, 새 탈락
    사유 문자열을 만들면 여기 규칙도 같이 추가할 것(안 하면 "기타"로 뭉개짐).

  [REST 호출 예산]
  - 실시간 편입이 살아나면 후보 수가 급증해서 `watchlist_reentry`(15초 주기)가
    후보마다 분봉을 긁어 429가 폭증할 위험이 생겼다(이미 하루 2,469건 포화).
    새 1A는 분봉이 전혀 필요 없으므로 **REST 0콜 고속경로** 추가 — 실시간
    체결가 + 캐시된 당일 시가가 있으면 REST를 한 번도 안 부른다. 시가 캐시가
    없으면 1회만 조회하고 캐싱(자가 치유). 후보 40종목 기준 한 사이클
    40콜 -> 0콜. **Pullback은 분봉이 필요하므로 기존 경로 유지.**

  [세션 후반 추가 지시 반영 — 시간창/라우팅/주문]
  - (1차 결정, 아래 "최종 정정" 항목으로 대체됨) **Pullback 시간창
    09:00~10:30 -> 09:20~15:10**으로 처음 늘렸다. 시작을 늦춘 이유는 개장
    직후엔 당일 고가가 아직 형성되지 않아 "눌림(고가 대비 되돌림)" 판정 자체가
    성립하기 어렵기 때문이고, 종료를 늘린 이유는 눌림목자동 조건검색이 하루
    종일 도는데 오후 눌림을 통째로 버릴 이유가 없어서였다.
  - **`ENTRY_WINDOW_END`(두 전략의 진입 평가가 살아있는 마지막 시각) 신설**.
    on_condition_hit / _cleanup_watched / watchlist_reentry가 전부
    PHASE1A_END(14:50)를 직접 박아 쓰고 있어서, Pullback 시간창만 늘려도
    재평가·감시가 14:50에 먼저 끊겨 그 뒤 구간이 조용히 죽는 함정이 있었다.
    세 곳을 `max(PHASE1A_END, PULLBACK_END)`로 자동 산출되는 이 상수 하나로
    통일 — 이후 어느 쪽 시간창을 고쳐도 세 곳이 항상 같이 따라온다.
  - 1A 분기에도 시간창 확인 추가 — 예전엔 `can_buy_phase1a`에만 있어서 자기
    창이 끝난 뒤에도 평가·워치리스트 기록이 계속 돌고 매수만 막혔다(Pullback
    분기와 대칭이 안 맞았음).
  - **조건검색식별 09:20 지연 게이트 제거**(OTHER_COND_START / IMMEDIATE_COND_NAMES
    상수째 삭제, 이 항목은 최종 상태에서도 유지됨). 07-29에 돌파자동매매용을
    지연시킨 근거는 "그때의 1A는 대형주에 덜 급하다"였는데, 지금 1A는 3초 틱
    기반이라 09:00부터 판정 가능하고 급등이 가장 많은 개장 20분을 버리는
    손실이 더 컸다. 1B 폐지로 돌파자동매매용은 1A 외에 갈 곳도 없어졌다.
  - **슬롯 캡 1A/눌림 각 3 -> 4**(이 값은 최종 상태에서도 유지), 시간대별
    상향(`PHASE1A_MAX_SLOTS_LATE`)은 폐지. 캡 합(8)이 공유 상한(6)을 넘게
    잡아 (a) 한쪽이 후보를 못 찾으면 다른 쪽이 그 자리를 흡수하고
    (b) 확장 슬롯(7~8) 도달 가능 상태를 유지한다. 한 전략이 6칸을 독식하는
    일은 여전히 불가(캡 4).
  - **시가대비 +5% 매수보류를 1A 전 경로로 확대**(주도주상위 전용 -> 주도주상위
    +돌파자동매매용, 사용자 지정, 최종 상태에서도 유지). 조건명으로 분기하지
    않는 이유: cond_name이 "기타"로 뭉개진 경우에도 필터가 조용히 꺼지지
    않게 하기 위함.

  [세션 최종 정정 — Pullback 09:25~14:50 + 중복종목 10:30 전략전환 + 강제청산 15:10]
  - 위 1차 결정(Pullback 09:20~15:10)을 사용자가 같은 세션에서 다시 조정.
    **Pullback 시작 09:20 -> 09:25**(기존 눌림목 로직 그대로 유지),
    **종료 15:10 -> 14:50**(=PHASE1A_END와 정확히 동일). 두 전략의 진입
    중단 시각을 한 점으로 묶어 "한쪽만 살아있는 구간"을 아예 없앴다.
  - **강제청산 15:15 -> 15:10** — 진입 종료(14:50) < 청산(15:10)이라 청산
    처리 중 신규매수가 겹치지 않고, 신규매수 하드컷오프(ENTRY_HARD_CUTOFF)와도
    같은 시각이 됐다.
  - **중복 편입 종목의 시간대별 전략 전환** 신규(`DUAL_SOURCE_PULLBACK_FROM
    =10:30`) — 단독 소스는 시각과 무관하게 완전 분리(눌림목자동 단독은 항상
    Pullback, 주도주상위/돌파 단독은 항상 1A)를 유지하되, **중복 편입** 종목만
    10:30 이전엔 1A(오전 모멘텀), 10:30 이후엔 Pullback(오후 되돌림)으로
    갈아탄다. `resolve_strategy()` 한 함수로 규칙을 모아 라우팅·주문직전
    가드·daily_backtest 세 곳이 전부 이 함수를 호출하게 했다 — 그렇지 않으면
    규칙이 갈라져 어긋난다.
  - 이 정정으로 주문 직전 가드도 다시 손봤다: 예전 가드("눌림목자동이 cond_name에
    있으면 1A 매수 전부 차단")를 그대로 두면 중복 종목의 정상적인 오전 1A
    매수까지 막히므로, **단독 소스만** 막도록(반대 방향인 "1A 단독을 Pullback
    으로 사려는 시도"도 함께) 변경했다. 등락률 상한도 cond_name 병용 판정에서
    **실제 매수 전략(sub_strategy) 단독 판정**으로 바꿨다 — 중복 종목이 오전에
    1A로 사졌는데 눌림목 상한(10%)이 걸리는 모순을 없앴다.

  [제안 1 — 매도 시장가 (구멍 메우기, 이번 세션 최대 위험 해소)]
  - 기존엔 **모든 매도**가 `현재가 -1틱` 지정가였다. 그런데 키움 응답의
    `return_code == 0`은 "주문 접수"이지 "체결"이 아닌데 `_execute_sell`은
    접수를 체결로 간주해 holdings에서 지우고 DB에 매도가를 기록한다.
    미체결이면 그 포지션은 **봇의 관리 대상에서 영구 이탈**한다 —
    `_reconcile_manual_sells`는 holdings->서버 방향만 보고, 서버엔 있는데
    holdings엔 없는 종목을 되살리는 로직이 없었기 때문이다. 손절도 익절도
    안 되고 15:15 강제청산(같은 지정가 경로)마저 미체결이면 오버나이트로 넘어간다.
  - 미체결 확률 자체는 낮지만 **위험 구간이 하필 급락 손절**이다:
    get_current_price(REST) 조회와 주문 도달 사이에 가격이 더 떨어지면 지정가가
    시장보다 위에 남는데, 429 스로틀 시 그 사이에 2초 blocking sleep이 낀다.
    1틱을 아끼려다 -3% 손절이 -6%가 되는 거래였다.
  - `order_manager.sell` 기본값을 **시장가**로 바꾸고, 거부 시 지정가 1회 폴백.
    (사용자가 모의투자 시장가 지원을 직접 확인함 — 폴백은 보험으로만 유지)
  - 안전망 `_detect_orphan_positions` 신규: 서버 잔고엔 있는데 holdings엔 없는
    종목을 감지해 텔레그램 알림(종목당 1회). **자동 복구는 하지 않는다** —
    사용자가 직접 산 종목까지 봇이 관리·청산하면 더 큰 사고이므로 판단은 사람에게.

  [제안 2 — 빈 호가창 지정가를 매도N호가로]
  - 호가창이 비었다는 건 매도1호가가 현재가보다 여러 틱 위일 가능성이 크다는
    뜻이라 '현재가+1틱' 지정가는 **아예 안 걸릴 수** 있었다. 미체결이면 봇은
    산 줄 알고 슬롯을 점유한 채 잘못된 매수가로 손익을 계산하다가 45초 뒤
    reconcile이 정리한다(매수 쪽은 자가 치유되지만 그동안 낭비).
  - 이제 매도3호가로 걸어 **슬리피지 상한이 정해진 즉시체결**로 만든다.
    호가 정보가 없으면 기존 현재가+1틱 폴백 유지.
    `OrderbookTracker.get_ask_price()` 신규(요청 레벨이 없으면 가장 깊은 호가로 대체).

  [daily_backtest 동기화 — 조용히 죽을 뻔한 것]
  - 삭제된 상수(IMMEDIATE_COND_NAMES / OTHER_COND_START)를 그대로 임포트하고
    있어서 **매일 15:30 백테스트가 ImportError로 조용히 실패**할 상태였다
    (task_daily_backtest가 lazy import + try/except라 봇은 안 죽고 리포트만 사라짐).
    제거하고, Pullback 시간창을 문자열 하드코딩("1030") 대신 라이브 상수에서
    직접 가져오도록 바꿔 앞으로 라이브를 바꾸면 백테스트가 자동으로 따라온다.

  [검증]
  - `test_patch_20260801.py` **245건 전원 통과**(32개 섹션). 07-31 실제 로그의
    편입 이벤트 원본을 그대로 투입해 재현했고, 구버전 버그도 함께 테스트로
    못박았다(예: "구버전은 여기서 리셋", "구버전은 0건").
  - 기존 `test_phase1b_controller.py`/`test_microstructure.py`는 실패하지만
    **HEAD에서도 동일하게 실패** — 5월 작성 스테일 테스트(WallDetector FSM +
    STRENGTH_MIN_TICKS 도입 이전 가정)라 회귀가 아님. 확인 방법은
    `git stash` 후 재실행.
  - 세션 후반 변경(시간창/라우팅/매도 시장가)으로 옛 사양을 검증하던 테스트
    9건이 정확히 실패했고, 전부 새 사양 단언으로 교체했다 — 테스트가
    사양 변경을 잡아낸 정상 동작이다.
  - 커밋: `96abe57`(치명 3건+2군+1B 비활성화+1A 재설계+하이브리드 주문),
    `423e6a2`(슬롯 효율+종목 우선순위), `a052c8a`(제안 A/B/C+진단 알림),
    `b845d26`(등락률 상한 분리+CLAUDE.md), `242ab76`(시간창/라우팅/매도 시장가),
    `6e54404`(Pullback 09:25~14:50 + 중복종목 10:30 전환 + 강제청산 15:10).



  [모바일 원격 제어 구축 — 2026-08-01 세션 마지막]
  - 이 PC에 Claude Code CLI가 아예 없던 상태에서 처음부터 구축했다:
    Node.js LTS(winget) -> claude CLI(npm 전역) -> Claude Pro 계정 로그인 ->
    프로젝트 폴더 신뢰 승인 -> `claude --remote-control`로 원격 세션 개설.
    상세 버전/경로/함정은 위 "개발/운영 환경" 섹션 참고.
  - 목적: PC에서 작업하던 것을 **폰에서 이어서** 확인·조작하기 위함.
    특히 장중(09:00~15:10)에 PC 앞에 없을 때 로그/진단 알림을 보고
    바로 상태를 물어볼 수 있게 하는 용도.
  - 원격 세션은 **이 대화의 맥락을 그대로 갖고 있지 않다.** 대신 프로젝트
    폴더에서 시작하므로 이 CLAUDE.md를 읽어 맥락을 복원한다 —
    **그래서 이 문서의 정확도가 곧 이관 품질**이다.

- 2026-08-02 (일, 장 없음) — **진입 로직 틱 구동 전면 전환**. 사용자가 눌림목자동
  조건검색식(3분봉 30이평 근접 + 고점대비 -3% + 20봉내 25% 급등 + 5일 평균
  거래대금 100억+)을 공유하며 "지연 없이 확실한 종목만 매수"를 요구한 데서 출발.

  [핵심 발견 — 3초 조건이 원리상 작동 불가였음]
  - `on_trade`(체결 틱 콜백)가 **틱을 버퍼에 쌓기만 하고 진입 평가를 전혀
    하지 않고 있었다.** 1L 블록은 `if False:`로 죽어 있었고, 평가가 실행되는
    경로는 `on_condition_hit`(편입 순간 1회 — 이때는 틱이 0개라 "체결틱 부족"
    으로 무조건 탈락) + `task_watchlist_reentry`(**15초 주기 폴링**)뿐이었다.
  - 진입 조건은 "체결강도 100 이상 3초 유지"라는 **3초짜리 신호**다. 그걸
    15초 간격으로 샘플링하고 있었으니 평균 7.5초/최대 15초가 늦고, 강도가
    3~5초만 100을 넘었다 꺼지는 종목은 **관측조차 되지 않았다.**
  - 원인: 1B/1L을 비활성화할 때 그 둘이 갖고 있던 "실시간 틱 콜백 진입"
    경로가 통째로 사라졌는데 새 1A가 그걸 물려받지 못했다.
  - -> `on_trade`에 `_maybe_tick_entry()` 배선. 판정은 전부 메모리 연산이라
    REST 0콜이고, 조건이 차는 그 틱에서 발화한다.

  [설계 — 무장 -> 발사 2단]
  - **무장**: 키움 공식 체결강도(**FID 228**)가 100 이상 **3초 연속**.
    자체 `compute_strength`를 안 쓴 이유: 틱 부족 시 정확히 100.0을 반환하는데
    임계값도 100이라 **데이터가 없을수록 쉽게 뚫린다**. 228은 이미 매 틱에
    실려오는데(`parsed["strength"]`) 1L 주석처리 이후 아무도 안 쓰고 있었다.
    윈도우 평균이 아니라 `_strength_since` 타이머로 진짜 '연속'을 잰다.
  - **발사**: 대량체결 버스트 3경로 OR (창 5초).
    사용자 원안의 "1초 이내 2회"는 **판정이 물리적으로 불가능**해서 5초로 바꿨다
    — 키움 FID 20이 `HHMMSS` 초 단위라 같은 초의 두 체결은 타임스탬프가 동일하고,
    WS는 여러 틱을 배치로 몰아 보낸다.
  - **순서가 핵심**: 느린 조건(강도 3초)을 무장으로, 순간 이벤트(버스트)를
    발사로 뒀다. 반대로 하면 버스트를 본 뒤 3초를 기다려 그만큼 늦는다.

  [사용자 질문 — "주가대비 가중평균거래대금"]
  - **주가로 나누는 건 틀린 축**이라고 답했다. 3천만원은 1,500원 주식이면
    2만주, 15만원 주식이면 200주인데 결국 *주식 수*가 나오고, 주식 수는
    유동성과 무관하다(같은 7만원짜리도 유동성이 100배 다르다).
  - 옳은 축은 **그 종목의 평균 1틱 체결금액 대비 배수**다. 조건식 C가 보장하는
    건 일 거래대금 100억+뿐이라 그 안에서도 평균 1틱은 30만~300만원으로 10배
    벌어진다. `avg_trade_value()` 신규 + 경로 ③(평균 × 20배)로 반영.
  - **부수 효과가 더 크다**: 자기 정규화라 **시간대 보정이 자동**이다. 점심에
    시장이 마르면 분모도 같이 줄어 '평균의 20배'라는 난이도가 시각과 무관하게
    유지된다.

  [시간대 계수 — 사용자 지정 "시간이 지날수록 느슨해지니 반영"]
  - 절대 경로(①②)에만 적용: 09:00~ 1.00 / 10:30~ 0.85 / 11:30~ 0.65(점심 최저)
    / 13:00~ 0.80 / 14:00~ 0.95. 안 낮추면 오후가 통째로 죽는다.
  - 경로 ③에는 적용하지 않는다(자기 정규화라 중복 완화가 된다).

  [격리 테스트가 잡은 실제 설계 결함]
  - 상대 경로의 분모(평균 1틱)에 **지금 터지는 버스트가 같이 들어가** 있었다.
    분자와 분모가 함께 커져서 **버스트가 클수록 문턱도 올라가는 자기모순** —
    "평균의 40배짜리 체결 2건"이 실제로 탈락했다. `exclude_recent_sec`(최근
    5초 제외)로 '버스트 직전까지의 평상시 체결 크기'만 재도록 수정.
  - `_maybe_tick_entry`가 pending을 미리 넣는데 `_execute_buy`에는 **pending
    등록 전에 early-return 하는 경로가 여럿**(하드컷오프/등락률 상한/전략
    불일치/수량 0) 있어서, 그 경우 pending이 영구 잔류해 `occupied_slots()`가
    그 자리를 계속 점유로 세고 그 종목은 다시는 매수되지 않았다. finally로 회수.

  [함께 바뀐 것]
  - **Pullback의 분봉·점수·VWAP 경로 폐지** — 눌림 판정은 HTS가 이미 하는데
    코드가 1분봉으로 재검증하느라 (a) 시간축이 안 맞아 26종목 전부 탈락하고
    (b) 1분봉 종가를 기다리느라 구조적으로 최대 60초 늦었다. 이제 1A와 완전히
    같은 트리거를 쓴다(차이는 유니버스/슬롯/상한/익절캡/시간창에만).
  - **주문 우선 레인**: 주문(kt10000/kt10001)이 조회와 같은 큐에 서서, 가장
    급한 순간에 방금 나간 조회 때문에 최대 0.6초(+429면 2초)를 기다렸다.
    `_throttle(priority=True)` + `ORDER_MIN_INTERVAL=0.2`로 분리(초당 5회 제한
    안이라 안전). 매도도 같이 적용 — 손절이 늦으면 그만큼 더 밀린 가격에 나간다.
  - **pre-arm**(`prearm_candidate`): 편입 즉시 감시 시작 + 전일종가/시가 캐시
    예열. 트리거 시점에 REST를 부르지 않는다.
  - `watchlist_reentry`는 **主 경로에서 안전망으로 강등** — 두 전략 모두
    REST 0콜이 됐다(구버전은 Pullback 후보마다 분봉 2콜).
  - `entry_reason`에 틱 경로 분기를 **최우선**으로 추가. 안 그러면 sub_strategy
    분기에 걸려 실제로는 쓰지 않는 옛 설명(MA5 눌림목/거래량증가지속)이 DB에
    기록돼 사후 분석이 통째로 어긋난다.
  - 진단 알림 `_REJECT_RULES`에 `강도 미무장(3초 미달)` 분류 추가.
  - `daily_backtest`는 **이제 라이브 진입을 재현하지 못한다**(분봉만으로 재현
    가능한 진입 로직이 하나도 안 남음). 숫자는 그럴듯하게 나오므로 리포트
    상단에 경고를 넣었다 — 청산 정책 비교용으로만 볼 것.

  [검증] `test_patch_20260802.py` 신규 83건 전원 통과(10개 섹션) +
  기존 `test_patch_20260801.py` 250건 전원 통과(구 사양을 검증하던 9건은
  새 사양 단언으로 교체 — 테스트가 사양 변경을 잡아낸 정상 동작).

- 2026-08-02 (계속) — **실전 드라이런 정밀 검증: 08-03에 터졌을 버그 6건 발견·수정**.
  "장중에 오류 나서 고치는 상황을 없애달라"는 요구로, 08:59 기동부터 15:10
  강제청산까지 하루 전 구간을 재현하는 통합 테스트(`test_live_dryrun_20260803.py`,
  71건/16섹션)를 새로 만들어 전수 점검했다. **단위 테스트 333건이 전부 통과하는
  상태에서도 아래가 전부 살아있었다** — 유닛은 부품을, 이건 배선을 본다.

  [치명 — 그대로 뒀으면 08-03에 매매가 반쪽 났을 것]
  - **청산 후 강도 타이머 잔류 -> 무장 단계 우회**: `_strength_since`/`_armed_at`을
    어디서도 안 지웠다. 매도 후 그 종목이 재매수 가능해지면 첫 틱에서
    `now - since`가 수 분~수십 분으로 계산돼 **"3초 연속"이 이미 충족된 것으로
    처리**된다. 그 상태에서 버스트 하나만 오면 진입 조건의 절반이 없는 채로
    매수가 나간다. 손실청산·쿨다운은 재매수차단이 막아주지만 **익절 후 3분이
    지난 종목은 그 그물을 정상 통과**하므로 실제로 뚫린다.
    -> `reset_tick_entry_state()` 신설, 매도(`_execute_sell`)와 감시해제
    (`_cleanup_watched`) 두 지점에 연결.
  - **08:59 스냅샷 종목이 체결틱을 못 쌓음**: 장 시작 전이면 `watch_list_today`에만
    넣고 `return` 해서 `start_watching`이 안 됐다. 무장은 틱이 쌓여야 시작되므로
    09:00 이후 첫 `watchlist_reentry`(최대 15초)까지 대기 — **하루 중 거래가 가장
    폭발하는 개장 직후 15초를 통째로 날리는** 구조였다. 장 시작 전에도 pre-arm은
    하도록 수정(매수는 여전히 09:00부터).
  - **cond_name 병합이 한 방향뿐**: "주도주상위가 **나중에** 온 경우"에만 병합해서,
    주도주상위가 먼저 걸리고 눌림목자동이 나중에 온 종목은 영영 단독으로 남았다.
    -> 중복 편입 종목의 10:30 전략 전환이 **절반만 작동**하고 있었다.
    방향 무관 병합으로 수정.

  [높음]
  - **분봉 부족 종목은 감시조차 시작 안 됨**: `on_condition_hit`이 분봉 15개 미만이면
    `return` 했는데, 그 앞에 pre-arm이 없어서 신규 상장주·거래 뜸한 종목은
    **무장이 원천적으로 불가능**했다. 틱 구동은 분봉이 전혀 필요 없으므로
    pre-arm을 분봉 조회보다 앞으로 옮기고, 분봉 실패 시엔 실시간 체결가로 진행.
  - **15:10 강제청산이 REST 실패 시 오버나이트**: `get_minute_candles` 하나만 보고
    `if candles:`에 걸리면 `_execute_sell`이 아예 안 불렸다. 이 시각은 429가 가장
    심한 구간이고 장 마감 후엔 재시도할 태스크도 없다. 매도는 시장가라 기준가는
    기록용이므로 **실시간 체결가 -> 분봉 -> 매수가** 순으로 폴백하도록 수정.
  - **감시 누락 시 무장만 되고 발사는 영원히 안 되는 상태**: 무장은 FID 228만 보는데
    버스트 판정은 `trade_flow` 버퍼를 읽는다. pre-arm이 어떤 이유로든 누락되면
    그 종목은 조용히 매매에서 빠진다. `_maybe_tick_entry`에 자가 치유 가드 추가.

  [중간 — 붕괴 패턴 예방]
  - `task_subscribe_flush`(1초 주기, WS 전송)와 `task_auto_shutdown`(텔레그램)에
    try/except가 없었다. 여기서 예외가 새면 `asyncio.gather()`가 무너져 봇이
    통째로 종료된다(2026-07-27 실제 장애와 같은 경로). 18개 태스크 전수 점검 후
    이 둘만 보강.

  [함께 확인한 것 — 정상]
  - 프로젝트 242개 .py 전수 구문검사: 오류 8건 전부 미사용 덤프/worktree 사본
    (`db_all.py`, `backtest_simulation.py` 등), 라이브 경로는 이상 없음.
  - 루트 `phase_settings.py`는 어디서도 import되지 않는 스테일 덤프
    (라이브는 `config/phase_settings.py`).
  - `_get_market_defense_mode`는 `MARKET_DEFENSE_ENABLED=False`라 즉시 반환 —
    핫패스에서 REST가 나가지 않는다(켜더라도 60초 캐시).
  - 핫패스 성능 실측: 30종목 x 3,000틱에 **틱당 1us**. 실시간 유입을 못 따라갈
    위험 없음. 무장 후에도 쿨다운(0.25초)이 200틱당 8회로 게이트 폭주를 막는다.
  - 슬롯 회계: 상한 초과 매수 없음, pending 누수 없음, `occupied_slots()`가
    실제 보유수와 일치.
  - 이상 입력 내성: 강도 None/음수/무한대, 종목코드 빈값, 호가 없음,
    phase1b 없음, DB 다운 — 전부 예외 없이 통과하고 매매는 계속된다.

  [검증] 3개 스위트 **404건 전원 통과** (단위 250 + 83, 통합 71).
  통합 테스트는 앞으로도 전략을 바꿀 때마다 돌릴 것 —
  **유닛이 다 통과해도 배선이 끊어져 있을 수 있다는 게 이번의 교훈**이다.

- 2026-08-02 (계속) — **프로젝트 파일 정리**: 추적 파일 139개 -> 99개,
  저장소 27.6MB -> 1.1MB. 용량이 목적이 아니라 **옛 덤프에 삭제된 전략 코드가
  통째로 남아 grep·검색·AI 컨텍스트를 오염시키는 것**이 문제였다(이 세션에서만
  두 번 걸렸다 — 스테일 `phase_settings.py`의 `evaluate_new_intensity_strategy`
  호출을 라이브로 착각할 뻔했다).
  - 판단 근거: `main.py`에서 시작한 **import 도달성 분석**(상대 임포트 포함).
    라이브 모듈은 34개뿐이다. 초안에서 상대 임포트를 빼먹어 `core/theme_manager.py`가
    고아로 잘못 잡혔던 걸 발견·수정했다 — **이 분석을 다시 할 일이 있으면
    반드시 상대 임포트를 처리할 것.**
  - **삭제**: 루트 `phase_settings.py`(진짜는 `config/phase_settings.py`, 이름이
    같아 가장 위험했음) / 코드 덤프 txt 9개(`combined_strategy_code.txt` 25.4MB
    포함, `merge_code.py`로 재생성 가능) / 일회성 패치 `fix_*.py` 3개 /
    0바이트 파일 15개 / **스테일 테스트 8개**(삭제된 Phase2·Phase3·WallDetector
    FSM을 검증하고 있었음) / 구문오류로 실행 불가한 `backtest_simulation.py`·
    `db_all.py` / `utilslogger.py`(ThemeManager 중복 정의 + `utils/logger.py`와
    이름 혼동).
  - **보존 + `[삭제 예정 / DEPRECATED]` 배너**(15개): `*_legacy.py` 12개 +
    `phase3_controller.py`/`order_queue.py`/`api/kiwoom_api.py`.
    CLAUDE.md 규칙 2("파일 삭제 금지")와 충돌해서 지우지 않되, 읽는 사람이
    현재 코드로 착각하지 않도록 최상단에 명시했다. 정리하려면
    `git rm $(git ls-files "*_legacy.py")`.
  - **절대 삭제 금지로 확인된 것**: `notify_scheduler_start.py`(start_trader.bat이
    매일 08:59 호출), `__init__.py` 3개(패키지 마커), 수동 도구 8개
    (`analyze_program_flow.py`/`probe_tick_chart_api.py`/`fetch_themes.py`/
    `create_tables.py` 등).
  - 이전 세션이 남긴 `.claude/worktrees/` 2개도 정리(3.3MB, git 미추적인데
    grep에는 계속 걸렸음). `git worktree remove`가 OneDrive 잠금으로 실패해
    등록 해제 후 PowerShell로 폴더를 지웠다.
  - **유효한 테스트는 이제 3개뿐**: `test_patch_20260801.py`(250 -> 08-02에 281) /
    `test_patch_20260802.py`(83) / `test_live_dryrun_20260803.py`(71).
    정리 후 재검증 404건 전원 통과 + `import main` 정상.

- 2026-08-02 (계속) — **중복/충돌 재감사 + 관측 보강**. 정리 후 라이브 34개
  모듈을 다시 훑었다. 클래스/함수 이름 충돌은 사실상 없었고(`_cache_path`/
  `_save`는 서로 다른 파일의 private 헬퍼), **정합성이 깨진 5건**을 정리했다.

  [🔴 관측 보강 — 08-03 최대 리스크 대응]
  - **FID 228 수신 감시 신설**(`task_fid228_watchdog`, 09:05 1회).
    이번 개편에서 **유일하게 실거래로 검증되지 않은 가정**이 "체결 틱에 228이
    실려 온다"는 것이다(파싱 코드는 예전부터 있었지만 1L 주석처리 후 아무도
    안 씀). 228이 안 오면 무장이 영원히 성립하지 않아 **하루 종일 매수 0건**이
    되는데, 겉으로는 아무 에러도 안 나서 로그를 뒤지기 전엔 알 수 없다.
    - `_trade_tick_total`(0B 자체가 오는가) / `_fid228_seen`(228이 실려온 종목)을
      `on_trade`에서 집계 — set.add라 핫패스 비용 없음(실측 틱당 2us 유지).
    - **"0B가 안 옴"과 "0B는 오는데 228만 빔"을 구분해서 경고**한다. 전자는
      WS 구독 문제, 후자는 FID 매핑 문제로 대응이 완전히 다르기 때문이다.
    - 진단 알림(30분 주기)에도 같은 경고가 들어가지만, 워치독은 09:05에
      **즉시 한 번** 알려 장 초반에 손쓸 수 있게 한다.
  - **진단 알림에 틱 단계별 통과 수 노출**: `⚡ 틱 12,340 → 무장 8 → 버스트 3
    → 매수 2`. `_tick_entry_stats`를 수집만 하고 안 보여주고 있었다 — 08-03에
    가장 알고 싶은 숫자인데 어느 단계가 병목인지 알 방법이 없었다.

  [🟡 정합성 정리]
  - **주석이 코드와 어긋난 3곳 수정** — `strategy_manager.py`의 Pullback 시간창
    주석과 `can_buy_pullback` docstring이 전부 "09:20~15:10"이라고 하는데
    **실제 상수는 09:25~14:50**이었다(세션 중간안이 주석에만 남음). 다음 세션이
    잘못된 시간창을 근거로 작업할 위험이 있어 고쳤고, 테스트로 못박았다.
  - `_REJECT_RULES`에서 `"조건식 지연(09:20)"` 분류 제거 — 그 게이트는 08-01에
    상수째 폐지돼 해당 사유 문자열이 더 이상 생성되지 않는다.
  - 미사용 상수 3개(`OBV_LOOKBACK`/`MAX_WATCH_SLOTS`/`WATCH_TIMEOUT`)에
    "[미사용]" 표시 — 지우지 않은 이유는 전자는 Pullback 분봉 경로를 되살릴 때
    필요하고, 후자 둘은 삭제된 1N 전략의 잔재라 맥락 기록이 유용해서다.
  - **`Phase1BController` docstring 전면 교체** — 이름은 '1B 컨트롤러'인데
    실제로는 **1A/Pullback의 데이터 파이프라인**이다. 라이브에서 쓰는 것
    (`trade_flow`/`orderbook`/`watched`)과 **한 번도 안 불리는 것**
    (`on_trade`/`on_orderbook`/`get_state`, `wall_detector`, `evaluator`)을
    명시했다. StrategyManager가 이 래퍼를 거치지 않고 트래커를 직접 갱신하기
    때문인데, 모르면 "1B가 아직 도나?"로 오해하기 딱 좋다. 리네임은 위험이
    커서 문서화로 대신했다.

  [확인만 하고 둔 것]
  - **수수료 상수 이원화**: `config/settings.py`의 `COMMISSION_RATE`+`TAX_RATE`는
    **아무도 안 읽고** 실제로는 `ROUND_TRIP_COST=0.23%`만 쓴다. settings.py가
    .gitignore 대상(로컬 전용)이라 손대지 않았지만, **거기서 수수료를 고쳐도
    반영되지 않는다**는 점은 기억할 것.
  - 핫패스는 깨끗하다 — `WallDetector.on_orderbook`은 주석 처리돼 있어 호가
    틱마다 낭비되지 않는다(확인함).

  [검증] `test_live_dryrun_20260803.py` 71 -> **84건**(섹션 17 FID228 감시,
  18 정합성 신설) 전원 통과 + 기존 333건 유지 = **총 417건**.

- 2026-08-02 (계속) — **1B/1L 전략 코드 완전 삭제** (순 -419줄).
  발단은 "1B/1L 때문에 충돌이나 헷갈릴 수 있으면 삭제해도 된다"는 지시.
  먼저 조사한 결론은 **실제 충돌은 없다**였다 — 1L은 `if False:` + 문자열
  리터럴이라 파이썬이 파싱조차 안 했고, 1B는 `PHASE1B_ENABLED=False`로 전부
  가드돼 테스트로 못박혀 있었다. 비용은 순수하게 "헷갈림"이었는데, **그
  헷갈림으로 이미 실거래 사고가 두 번 났다**(08-01 `_cleanup_watched`가 1A
  틱 버퍼를 지워 1A 창의 70%가 죽음 / 07-31 1L 특별케이스 누락으로 매수
  66초 만에 조기청산). 그래서 "지금 안 터진다"가 아니라 "다음에 터진다"를
  근거로 삭제를 택했다.
  - **삭제한 것**: 1L 진입 블록·진단 함수·상수 5개·인스턴스 상태 6개 /
    1B 매수 함수 2개(`_try_phase1b_buy`/`_check_1b_confirmations`)·
    `can_buy_phase1b`/`can_buy_leading`·`_1b_confirm`·상수 8개·
    `TAKE_PROFIT_CAP_1B`·on_trade 하락 트리거·on_orderbook FSM 주석 /
    `Phase1BController`의 WallDetector·ChemulEvaluator 배선과
    `on_trade`/`on_orderbook`/`get_state` 래퍼(전부 라이브 호출 0회).
  - **트레일링 익절도 함께 제거**(사용자 지정) — `sub_strategy == "1L"`이
    유일한 진입점이라 1L 삭제 시점에 이미 도달 불가였다. 이제 익절은 예외
    없이 flat 캡이라 **라이브와 daily_backtest의 청산 규칙이 정확히 일치**한다
    (예전엔 백테스트가 "트레일링은 재현 안 함"이라고 적어두고 넘어갔음).
  - **유지한 것**: `phase1b` 컨트롤러 객체(trade_flow/orderbook/watched) —
    전략이 아니라 1A/Pullback의 데이터 파이프라인이라 없애면 두 전략이 눈이
    먼다. `theme_mgr`도 유지 — 1L과 함께 죽은 게 아니라 **매수 후 동적 익절캡
    가산점**으로 계속 쓰인다.
  - `config/phase_settings.py`의 `PHASE_1B`와 `EXIT_POLICY`의 `trail_*`는
    이제 **아무도 안 읽는다** — 삭제 대신 "[미사용]" 배너를 달았다(CLAUDE.md
    규칙 2). 거기서 값을 고쳐도 반영되지 않는다는 점 주의.
  - **리네임은 미룸**: `phase1b` -> `TickDataPipeline`이 근본 해결이지만
    호출부가 ~40곳이고 **08-03이 틱 구동 신규 구조 첫 실거래일**이라,
    지금 얹으면 내일 문제가 났을 때 "새 전략 탓인지 리팩터링 탓인지"를
    구분할 수 없게 된다. 실거래 검증 후로 이월.
  - [검증] `test_patch_20260801.py` 250 -> **281건**(섹션 15를 "비활성화 확인"
    에서 "코드 완전 제거 확인"으로 교체 — 상수 16개·메서드 5개·인스턴스 상태
    7개가 **존재하지 않는지**를 단언하고, 반대로 데이터 파이프라인과 theme_mgr은
    **살아있는지**를 단언). 3개 스위트 **448건 전원 통과**.

- 2026-08-02 (계속) — **`.history/` 삭제 + 신규 고아 모듈 배너**.
  - **`.history/` 삭제** (1,050 파일 / 28.4MB). VSCode 로컬 히스토리 확장이
    05-18~07-26에 쌓아둔 타임스탬프 사본으로, `.gitignore:15`에 등재돼 저장소엔
    영향이 없었지만 **grep·검색·AI 컨텍스트를 심하게 오염**시켰다(이 세션의
    1B/1L 조사에서 grep 결과 225KB가 통째로 이 쓰레기였다). 삭제 전 확인한 것:
    전 파일이 `이름_YYYYMMDDHHMMSS.확장자` 자동 사본(수동 생성 0개), git 추적
    0건, 코드 참조 0건(`core.history_fetcher`/`self.history` 히트는 전부 무관).
    프로젝트가 OneDrive 안이라 휴지통에서 30일 복구 가능.
    효과: 삭제된 옛 전략 심볼 검색 히트가 수백 건 -> `WallDetector` 17 /
    `Phase3Controller` 3 / `evaluate_phase2` 1건으로 줄어 눈으로 훑을 수 있게 됨.
  - **1B 삭제로 새로 고아가 된 2개에 DEPRECATED 배너**:
    `core/strategy/wall_detector.py`, `core/strategy/chemul_evaluator.py`.
    `phase1b_controller.py`의 배선이 사라지면서 이제 서로만 참조한다
    (chemul_evaluator -> wall_detector). 08-02 앞선 정리에서 쓴 것과 같은
    `[삭제 예정 / DEPRECATED]` 배너 형식을 그대로 따랐다(파일 삭제는 규칙 2로 보류).
  - **도달성 분석 재실행** (main.py 기준, 상대 임포트 포함):
    라이브 모듈 **34 -> 32개**. 정확히 위 2개만 줄어 앞선 세션의 분석과 일치.
    나머지 고아는 전부 `_legacy` / 배너 기부착(`api/kiwoom_api.py`,
    `order_queue.py`, `phase3_controller.py`) / 수동 도구 / `__init__.py`다.
    **미조치로 남긴 것 3개**(전부 오늘 변경과 무관한 기존 고아):
    `api/merge.py`(일회성 코드 병합 스크립트), `core/migrate_themes.py`
    (일회성 DB 마이그레이션), `models/account_info.py`(9줄짜리 미사용 클래스 —
    이름 때문에 '계좌 모델'로 오해할 여지는 있음).

- 2026-08-02 (계속) — **08-03 기동 전 동기화 감사** (1B/1L 삭제 후 '꼬임' 점검).
  기존 3개 스위트가 안 보는 축만 따로 감사했다 — 결과 **구멍 1건 발견·수정**.
  - **[발견·수정] `버스트 계산 실패`가 진단에서 "기타"로 뭉개지고 있었다.**
    `check_burst`의 예외 핸들러가 내는 사유인데 `_REJECT_RULES`에 규칙이 없어
    미분류였다. 이건 '시장에 신호가 없다'가 아니라 **우리 코드가 터졌다**는
    뜻이고, 발사 조건이 죽으면 매수가 0건이 된다. 진단 인프라의 목적(정상
    필터링 ↔ 코드 이상 분리)과 정면으로 어긋나는 지점이라 전용 분류
    `버스트 계산 실패(코드 이상)` 신설 + `_REJECT_INFRA`에 추가(인프라 경고로 승격).
    회귀 테스트로 못박음(dryrun 84 -> **86건**).
  - **[확인] 옛 전략 포지션 내성**: sub_strategy가 `1B`/`1L`/`알수없음`/`None`인
    포지션이 DB 복원·수동매수로 들어와도 익절캡 산출과 손절 청산이 정상 동작.
    1L도 이제 트레일링이 아니라 flat 캡(기본 2.5%)으로 청산된다.
  - **[확인] 지연 임포트 10개 심볼 전수 실존** — 봇은 안 죽고 기능만 조용히
    사라지는 부류(07-27 실제 장애 클래스). `daily_backtest`가 strategy_manager에서
    가져오는 15개 심볼도 전부 살아있다.
  - **[확인] 라이브 ↔ daily_backtest 상수 5종 동기화**, 삭제 심볼의 실행코드
    잔존 0건, 라이브 모듈 31개 개별 임포트 전부 성공, 전체 89개 .py 구문오류 0.
  - **[확인] 진입점**: `.bat` 비ASCII 0바이트 / `.ps1` UTF-8 BOM 있음 /
    junction 경로 정상 / 스케줄러 `AutoTrader_Start` 내일 08:59 Enabled /
    **배치가 쓰는 python(3.14.3)과 테스트에 쓴 python이 동일 실행파일**.
  - **[주의] 중복 실행 방지 장치가 없다** — `main.py`에 싱글톤 가드가 없어서
    이전 인스턴스가 살아있는 채로 08:59가 되면 두 개가 뜬다(토큰 충돌).
    지금은 `task_auto_shutdown`(15:40, 요일 무관)이 매일 스스로 꺼주므로
    실무상 문제가 없지만, **수동으로 봇을 띄웠다면 STOP_SIGNAL로 먼저 끌 것**.
    실제로 08-02 08:59에 뜬 인스턴스가 오후까지 살아있는 것을 이 감사에서 확인했다.

- 2026-08-02 (야간, 08-03 기동 전 최종 점검) — **원격제어 연속성 + 기동 인프라
  회귀 테스트 신설**. "중간중간 확인해도 막상 장 시작하면 오류로 재수정하는 일이
  대부분이었다"는 문제의식에서, 전략이 아니라 **기동 계층**을 처음으로 테스트로
  못박았다. 이 계층은 무인(비대화형) 실행이라 실패해도 화면에 아무도 없다 —
  대화형 수동 확인으로는 원리상 못 잡는 부류다(2026-07-28 교훈과 같은 뿌리).
  - **원격제어 세션이 매일 리셋되던 문제** — 08:59마다 새 대화로 시작해 전날
    맥락이 사라졌다. `start_remote_control.ps1`이 기본으로 `--continue`를 붙이도록
    변경(상세는 위 "세션 연속성" 항목). 한도가 걱정될 때만
    `NEW_SESSION_REQUESTED` 플래그로 1회 새로 시작한다.
  - **모바일 '종료' 클릭 후 방치되던 문제** — 세션이 끝나면 런처 창이 프롬프트에
    멈춰, PC 앞에 사람이 없으면 그날 내내 폰 접속이 끊겼다. 자동 재시작 루프
    추가(최대 30회, 5초 미만 즉시종료 3연속이면 설정 문제로 보고 중단).
    **매매 봇과는 완전히 별도 프로세스라 이 창에서 무슨 일이 나든 매매엔 영향 없음**
    (`start_trader.bat`이 `start`로 띄우고 `main.py`는 그 뒤 독립 실행).
  - `main.task_remote_control_watchdog` 신규(09:06 1회) — 프로세스 부재 시 텔레그램
    알림. 블로킹 확인은 `asyncio.to_thread`로 분리(실측 0.67초), 확인 자체가
    실패하면 오탐 방지를 위해 '정상'으로 간주한다.
  - **[발견·조치] 고아 `STOP_SIGNAL`이 실제로 남아 있었다**(당일 20:57 생성).
    이중 방어(배치파일 `del` + `setup()`의 낡은 신호 정리)가 이미 있어 기동은
    막히지 않았겠지만, 봇 미실행을 확인하고 삭제해 삼중으로 안전하게 만들었다.
    **수동으로 봇을 끈 날은 이 파일이 남는다는 것을 기억할 것.**
  - **[확인] 내일 중복 실행 위험 없음** — 점검 시점에 python 프로세스 0개
    (08-02와 달리 봇이 살아있지 않음).
  - **[확인] `config.ini` ↔ 코드 정합성** — 조건검색식 4개가 올바르게 분리
    등록돼 있다(`CONDITION_NAMES` 3개 + `CLOSING_BET_CONDITION_NAME` 분리).
    이게 어긋나면 에러 없이 조용히 구독이 빠지는 부류라 매번 확인할 것.
  - **[확인] 배선 감사** — 정의된 태스크 20개 = gather 등록 20개(불일치 0),
    모든 태스크가 루프 안에 try/except 보유, 지연 import 심볼 10개 전부 실존,
    전체 90개 .py 구문오류 0건.
  - **[인지, 미조치] 5분 이상 긴 WS 단절 시 `_process_initial_snapshot`이 장중에
    호출된다**(main.py 재연결 복구 경로). `_wait_for`가 소켓을 직접 recv 하므로
    이론상 체결틱을 흘릴 수 있으나, 이미 끊겼다 붙은 상황에서 유니버스를 복원하는
    트레이드오프가 명확한 기존 동작이라 손대지 않았다.
  - [검증] `test_live_dryrun_20260803.py`에 **섹션 19(기동 인프라)** 신설,
    86 -> **104건**. 원격제어 배선/비차단/폴백, `--continue` 기본값, 플래그
    1회성, `.ps1` BOM, `.bat` 순수 ASCII, junction 참조, 고아 신호 부재까지
    검사한다. 4개 스위트 **492건 전원 통과**.

