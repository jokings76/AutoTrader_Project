# (구) 다음 할 일 아카이브 — 2026-08-02 / 08-03 기준

> 2026-08-06에 CLAUDE.md 분량 문제로 분리. **전부 그 시점의 계획이라 이미
> 지나갔다.** 아직 유효한 이월 항목은 CLAUDE.md의 "🐛 알려진 결함"과
> "아직 판단 보류 중인 것"에 옮겨져 있으므로, 여기는 경위 확인용이다.

\## (구) 다음 할 일 (2026-08-03 마감 기준 — 08-04에 변경 12건이 한꺼번에 처음 적용)

\### ⚠️ 먼저 읽을 것 — 08-04는 "변경이 겹친 날"이다
하루에 **12가지**가 동시에 바뀐다. 결과가 좋든 나쁘든 **무엇 때문인지 분리하기
어렵다.** 그래서 슬롯 개수·비중(tier)은 일부러 **안 건드렸다**(변수를 더 늘리지
않기 위해). 되돌릴 때도 **반드시 한 번에 하나씩** 할 것.

\### 오늘 바뀐 것 전체 (커밋 8개)

| # | 변경 | 방향 | 근거의 무게 |
|---|---|---|---|
| 1 | '익절 조기확정' 폐지 -> **본전스톱**(당시 최종 **OFF**, ⚠️**08-04 오전에 다시 ON으로 재활성화** — 아래 히스토리 "2026-08-04" 항목 참고) | 익절 확대 | 037070 틱 재생 1건 |
| 2 | 익절캡 1A **4.0%** / 눌림·개장초반 **2.5%** / 상향 **6.0%** | 익절 확대 | 개장구간 캡 비교 실측 |
| 3 | 동적캡 결함 3건 수정(①cap_exit 항상참 ②스파이크 기준선 ③손실구간 발동) | 조기청산 축소 | **로직 결함**(표본 무관) |
| 4 | **손절이 워밍업 중에도 작동** | 손실 축소 | **로직 결함**(표본 무관) |
| 5 | 점심 시간계수 0.65 -> **1.00** | 손실 축소 | 08-03 계수별 실측 |
| 6 | Pullback 창 09:25 -> **09:00** (1A와 동일) | 진입 증가 | 근거 소멸 + 039980 실측 |
| 7 | 무장 3.0 -> 2.0 -> **1.5초** | 진입 증가 | ⚠️ **미검증(실험)** |
| 8 | **지수 하락 가드** 신규(-5%, 11:00~) | 리스크 차단 | 사용자 지정 |
| 9 | 시간정리30분·정체정리15분 -> **슬롯 만석일 때만** | 무의미 청산 제거 | 08-03 실측(최대 2/6) |
| 10 | 슬롯 교체 -> **만석+무장+버스트** 3조건 | 무의미 교체 제거 | 08-03 실측(-235,860원) |
| 11 | 자동종료 고정 15:40 -> **마무리 3종 완료 즉시** | 유휴 제거 | — |
| 12 | 종목명 "조건검색" 표시 버그 수정 / 테스트 로그 분리 | 표시·분석 정확도 | 재현 테스트 |

\### 09:00 최우선 확인 (중요도순)

1. **종목명이 실제 이름으로 찍히는지** — `종목: 삼성전자 (005930)` 형태.
   코드만 나오면 REST 폴백 실패이므로 `raw에 종목명 없음 ... REST 조회 ->` 확인.
2. **09:00~09:25에 눌림목(`1A_눌림`) 매수가 나오는지** — 이 구간은 어제까지
   구조적으로 0건이었다. 나오면 시간창 확대(#6)가 작동한 것.
3. **매수 건수가 급증하는지** — #6 + #7이 겹쳐 어제(16건)보다 크게 늘 수 있다.
   **늘면서 손실이 커지면 #7(무장 1.5초)을 가장 먼저 3.0초로 되돌릴 것**
   (유일하게 검증 근거가 없는 변경이다).
4. **(2026-08-04 10:03 갱신)** `🛡 본전스톱 무장` 로그가 **뜨는 게 정상**이다 —
   같은 날 오전 09:00~09:40 실거래 대조 분석(순손익 +165,345원(08-03) ->
   -427,200원(08-04), 손절 0건 -> 5건) 결과 사용자 지정으로 **재활성화**했다.
   원인은 08-03 저녁에 고친 동적캡 버그(순이익 가드 추가)가 부작용으로
   "약해지는 손실 포지션을 조기에 잘라주던" 안전판을 없앴고, 조기확정 폐지 후
   그 자리를 대신할 게 본전스톱뿐인데 그게 꺼져 있었던 것 — 상세는
   아래 "2026-08-04" 히스토리 항목 참고. **단, 이 재적용은 아직 실거래로
   검증 전**(코드 수정 시점엔 봇이 이미 장중이라 재시작 전까진 미반영)이니
   다음 기동부터 본전스톱발 청산이 실제로 이익을 지키는지 지켜볼 것.
5. **점심(11:30~13:00) 매수가 줄어드는지** — #5의 직접 효과. 어제 이 구간
   3건이 전부 손실(합계 -5.29%)이었다.
6. **`시간정리`/`정체 정리`가 거의 안 뜨는 게 정상**(#9). 뜬다면 사유 끝에
   `슬롯 만석`이 붙어 있어야 한다. 슬롯이 실제로 6개 찼다는 뜻.
7. **`슬롯 교체` 로그가 거의 안 뜨는 게 정상**(#10). 만석+무장+버스트가 동시에
   맞아야 하는데 교체 태스크가 60초 주기라 확률이 낮다.
8. **`🛡 지수 하락 가드 발동`은 안 오는 게 정상**(#8). 지수 -5%는 드물다.
   오면 그날은 신규매수가 멈추고 11:30 본전청산 / 14:50 강제청산이 돈다.
   가드가 켜지면 **진단 알림 첫 줄이 `⛔ 전면 차단: 지수 하락 가드(-5%)`**여야
   한다. 보유가 0~1개인데 `🔒 1A 슬롯 없음`이 뜨면 08-03 심야에 고친 오분류가
   되살아난 것이므로 즉시 확인할 것. **가드 중엔 `슬롯 교체`도 `1A 우선순위
   교체`도 뜨면 안 된다**(둘 다 매도만 하는 반쪽 동작이라 막아뒀다).
9. **15:30 백테스트 리포트 도착 + 그 직후 자동종료**(#11). 텔레그램 문구로 구분:
   `장마감 마무리 완료(...)` = 정상 / `설정된 시간(15:40) 도달 ... (미완료: ...)`
   = 폴백이 걸린 것이므로 괄호 안 항목을 확인.

\### 즉시 되돌리는 법 (한 번에 하나씩!)

| 대상 | 방법 |
|---|---|
| **손실반등 전량청산 복귀** | `PARTIAL_EXIT_ENABLED = False` (동적캡 분할도 같이 꺼짐)<br>손실반등만 되돌리려면 on_price_update의 손실반등 블록에서 분할 분기 제거 |
| **재매수 완화 끄기** | `REBUY_AFTER_LOSS_ENABLED = False` (구버전 = 손실청산 당일 영구차단)<br>문턱만 조절하려면 `REBUY_BURST_VALUE_MULT`(2.0) / `REBUY_AFTER_LOSS_WAIT_SEC`(3600) |
| **손절 대신 추가매수 끄기** | `RESCUE_ADD_ENABLED = False` (구버전 = -3% 즉시 손절)<br>관찰 창만 줄이려면 `RESCUE_ADD_OBSERVE_SEC`(15초) / 하한 `RESCUE_ADD_OBSERVE_FLOOR`(0.045) |
| **상승 이탈 끄기** | `ENTRY_BREAKOUT_ENABLED = False` (되돌림만 기다림) |
| **조건검색 0종목** | HTS [0156]의 '대상'(KRX/통합)과 `kiwoom_ws.CONDITION_STEX_TP`가<br>**반드시 일치**해야 한다. 통합=`"A"` / KRX=`"K"`.<br>어긋나면 **에러 없이 0종목**이라 매수가 하루 종일 0건이 된다 |
| **실전 -> 모의 복귀** | `config.ini`의 `IS_MOCK = true` + **모의 APP_KEY/SECRET_KEY로 교체**<br>(실전 키로는 모의 서버 토큰이 안 나온다). 백업: `AutoTrader_Project_backups/config.ini.mock.bak` |
| **매수금액** | **3곳을 같이** 고칠 것 — `portfolio_optimizer.DEFAULT_BASE_AMOUNT`(실지배값) /<br>`config/phase_settings.py: position_amount` / `order_manager.BUY_AMOUNT_PER_STOCK`.<br>모의 원값은 `2_000_000` |
| 예수금 필드 | `kiwoom_rest.get_orderable_amount`의 폴백 체인 첫 항목.<br>⚠️ `20/30/40/50/60stk_ord_alow_amt`는 **미수**라 절대 쓰지 말 것 |
| 무장 시간 | `TICK_STRENGTH_SUSTAIN_SEC = 3.0` (또는 2.0) |
| 본전스톱 끄기 | `BREAKEVEN_STOP_ENABLED = False` (2026-08-04부터 기본값 True로 변경됨) |
| 되돌림 대기 끄기(=즉시매수 복귀) | `ENTRY_PULLBACK_ENABLED = False` |
| 되돌림 폭/분할 비중 | `ENTRY_PULLBACK_TRANCHES` (기본 `((0.005,0.5),(0.010,0.5))`)<br>단일 진입으로 되돌리려면 `((0.005,1.0),)` |
| 되돌림 대기 시간 | `ENTRY_PULLBACK_TIMEOUT_SEC` (기본 120초) |
| 분할매도 끄기(=전량청산 복귀) | `PARTIAL_EXIT_ENABLED = False` |
| 분할매도 비중/잔량 트레일 | `PARTIAL_EXIT_FRACTION`(0.5) / `PARTIAL_EXIT_TRAIL`(0.03) |
| **버스트 문턱** | `PHASE1A_BURST_TRADE_VALUE = 30_000_000` (08-04에 4천만으로 상향) |
| **시간대 계수** | `TICK_BURST_TIME_MULT`를 옛 값으로:<br>`((10,30),0.85) / ((13,0),0.80) / ((14,0),0.95)`<br>08-04에 전부 1.00으로 만들었다(오후 완화 제거) |
| **오후장 접기** | `PHASE1A_END`/`PULLBACK_END`를 `time(13, 0)`으로<br>(차단 플래그보다 이 방식이 깔끔 — 상수 2개) |
| Pullback 창 | `PULLBACK_START = time(9, 25)` |
| 점심 계수 | `TICK_BURST_TIME_MULT`의 `((11,30), ...)` -> `0.65` |
| 익절캡 | `config/phase_settings.py`의 `take_profit_cap` 0.040 -> 0.025,<br>`TAKE_PROFIT_CAP_PULLBACK/EARLY` 0.025 -> 0.015 |
| 지수 가드 | `INDEX_GUARD_ENABLED = False` (임계는 SEVERE_CRASH와 같은 값 유지할 것) |
| 시간정리 슬롯조건 | `on_price_update`의 `if slots_full:`와 `check_timeouts()` 앞 가드 제거<br>**두 곳 다 고쳐야 함** |
| 슬롯 교체 조건 | `REQUIRE_CANDIDATE_ARMED` / `REQUIRE_CANDIDATE_BURST = False`<br>슬롯 만석 요구는 `try_slot_replacement`에서 제거 |

\### ⚠️ 이 코드베이스의 반복 사고 패턴 (다음 세션이 꼭 알아야 할 것)

**같은 규칙이 여러 곳에 흩어져 있어, 한 곳만 고치고 끝냈다가 조용히 어긋난다.**
오늘 하루에만 이 패턴으로 3번 걸렸다.

- **체결강도 하락 판정이 3곳**: `_update_dynamic_caps` / `_is_strength_rising_vs_entry`
  / **`find_stagnant_holding`**(slot_replacement.py). 앞의 둘만 고치고 세 번째를
  놓쳐, 교체 대상 선정이 계속 진입 스파이크(300 포화)를 기준으로 돌고 있었다.
- **시간정리가 2곳**: `on_price_update` / **`check_timeouts()`**. 앞만 고쳤으면
  틱이 안 들어오는 종목은 뒤쪽 경로로 그대로 잘려나갔을 것이다.
- **-5% 판정이 2곳**: `SEVERE_CRASH` / `INDEX_GUARD`. 임계를 하나만 바꾸면
  동작이 어긋난다(정합성 테스트로 못박아 둠).
- **전면차단 판정이 4곳**(08-03 심야 교차감사에서 발견): `can_buy_more` /
  `_try_1a_priority_upgrade` / 진입 경로의 탈락사유 기록 / `build_entry_diagnostics`.
  가드를 `can_buy_more`에만 넣었더니, 나머지 셋은 "슬롯이 꽉 찼다"로 오해해
  **보유분을 팔고 사지는 못하는** 동작이 됐다.
  -> 이제 전부 **`_entry_block_reason()` 하나**만 본다. 새 차단 조건을 추가할
  일이 생기면 **그 함수에만** 넣을 것(호출부를 늘리지 말 것).
- **진단 라벨에 수치를 박지 말 것**: `강도 미무장(3초 미달)`이 상수를 1.5초로
  바꾼 뒤에도 남아 텔레그램이 거짓 안내를 했다. 라벨은 의미만 담고 수치는
  런타임에 포맷한다(테스트로 금지해 둠).

-> **강도/시간정리/지수/차단 관련을 건드릴 땐 반드시 `grep`으로 전 경로를 먼저 세어볼 것.**

\### 표본이 쌓이면 판단할 것 (며칠~2주)

- **#7 무장 1.5초의 실효** — FID228 이력이 없어 **사후 백테스트가 원리적으로
  불가능**하다. 실거래 관찰이 유일한 검증 수단이다. 진입 건수 증가분이 수익으로
  이어지는지, 약한 신호만 늘어 손실이 커지는지.
- **#1 본전스톱 — 08-04 오전에 ON으로 재전환됨**(08-03 밤엔 OFF가 최종이었으나
  당일 실거래 대조로 재활성화, 상세는 히스토리 "2026-08-04" 항목). 도입 당시
  시뮬레이션은 개장 구간에서 OFF가 우세하다고 나왔었는데 무장 게이트가 빠진
  약식 검증이라 신뢰도가 낮았다 — 이제 ON 상태의 실거래 성과를 며칠 지켜보고
  판단할 것. 다시 끄려면 위 "즉시 되돌리는 법" 표의 "본전스톱 끄기" 참고.
- **#10 슬롯 교체가 아예 안 도는지** — 조건이 셋이라 사실상 멈출 가능성이 크다.
  **며칠 0건이면 그게 정상인지(교체가 원래 불필요했는지) 아니면 조건이 과한지**
  판단해야 한다. 그때 아래 이월 과제를 검토할 것.
- **비중(tier) 예측력** — 08-03엔 최대 비중(x3.00)이 최대 손실(-3.11%)이었다.
  `entry_tier`가 포지션에 기록되므로 상관을 볼 것. 슬롯보다 이쪽이 먼저 검증돼야
  할 축이다.
- **슬롯 확대 시점** — 진단 알림에 `1A 슬롯 없음`이 뜨기 시작하면 그때가 근거가
  생기는 시점(08-03엔 0건, 동시보유 최대 2/6). **자본은 병목이 아니다**
  (497,017,161원, 6슬롯 만석이어도 5.4%).

\### 이월 과제 (오늘 판단만 하고 코드는 안 건드린 것)

- **슬롯 교체를 틱 경로로 이관** — 지금은 60초 폴링인데 버스트 창은 5초라
  둘이 겹칠 확률이 낮다. 제대로 살리려면 `_maybe_tick_entry`에서 **"완전히
  준비됐는데 슬롯만 없음"을 감지한 순간** 교체하는 구조가 맞다. 지금 구조는
  "먼저 팔고, 사지는 못할 수도 있다"라 원자적이지 않다.
- **`phase1b` -> `TickDataPipeline` 리네임** — 이름이 실거래 사고를 두 번 냈다.
  호출부가 ~40곳이라 미뤄둠(08-02부터 이월 중).
- **틱 아카이브 유니버스** — 09:20 시점 감시 종목 기준이라 **정작 검증하고 싶은
  눌림목 단독 종목이 빠진다**(08-03에 039980/365660/059120 전부 미수집).
- 아래 "(구) 다음 할 일"의 이월 항목들(0g 프로그램매매 FID, 429 재측정,
  CandleCache.log_stats 미호출, condition_snapshot_poll 재설계 등)은 여전히 유효.

\## (구) 다음 할 일 (2026-08-02 세션 마무리 기준 — 08-03이 신규 구조 첫 실거래일)

\### 08-03 아침에 반드시 인지할 것 (두 가지)

**(1) 조건검색 실시간 편입 유실** — 08-01 세션에서 이게 그동안 100%
유실되고 있었다는 사실이 확인됐다(07-31 이전 로그도 마찬가지). 즉 지금까지의
모든 실거래 성과는 "기동 시점 스냅샷 종목만으로 낸 결과"다. 08-03부터는 장중
내내 신규 종목이 들어오므로 **매수 건수와 후보 수가 크게 늘어날 수 있다.**

**(2) 진입이 15초 폴링 -> 틱 구동으로 바뀜 (08-02)** — 이게 이번 개편의 본체다.
`on_trade`가 체결틱을 쌓기만 하고 **진입 평가를 전혀 하지 않고 있었다.**
평가 경로는 편입 순간 1회(이때는 틱이 0개라 무조건 탈락) + `watchlist_reentry`
15초 폴링뿐이었다. 진입 조건이 "강도 100 이상 3초 유지"라는 3초짜리 신호인데
15초 간격으로 들여다보고 있었으니 **평균 7.5초가 늦고, 짧게 스치는 신호는
아예 관측조차 안 됐다.** 1B/1L을 끄면서 그 둘이 갖고 있던 실시간 틱 진입
경로가 사라졌는데 새 1A가 물려받지 못한 것이 원인이었다.
-> **08-03에는 매수 건수가 크게 늘 수 있다.** (1)과 겹치면 슬롯 경합이
처음 겪는 수준이 될 수 있으니 슬롯 부족 로그를 특히 볼 것.

\### 09:00 최우선 확인 (중요도순)
1. **08:59 기동 -> `장 시작 전 편입 — 09:00부터 평가 대기` 로그**. 스냅샷
   종목이 폐기되지 않고 후보로 남는지.
2. **09:00 이후 `편입 신호`가 장중 내내 계속 뜨는지** — 이번 수정의 핵심
   지표. 07-31엔 기동 시점 21건 이후 0건이었다. 안 뜨면 `type='02'` 파싱을
   다시 확인할 것(`신규 item_type='02' 최초 관측` 로그로 원본 구조 확인).
3. **첫 `[진입 진단]` 알림 도착**(09:05~09:35). 이후 경고 유무에 따라
   10분/30분 주기. 판단표는 아래 "진단 알림 읽는 법" 참고.
4. **`주문방식 판정: 매도1~3호가 잔량 ...` 로그** — 시장가/지정가가 실제로
   갈리는지. **`시장가 주문 실패 -> 지정가로 1회 폴백`이 뜨면 모의서버가
   시장가 미지원**이라는 뜻이므로 하이브리드를 지정가 전용으로 되돌릴지 판단.
5. **틱 구동이 실제로 도는지 — 이번 개편의 핵심 지표.**
   - **09:05에 FID 228 워치독이 자동 판정한다**(`task_fid228_watchdog`).
     정상이면 로그에 `✅ FID 228 수신 정상`만 남고 조용하다.
     **텔레그램으로 `🚨 [긴급]`이 오면 그게 오늘의 최우선 문제**다:
       · `체결강도(FID 228)가 한 번도 안 옴` -> `🔑 0B 체결 raw 키` 로그에
         `'228'`이 있는지 확인. 없으면 `kiwoom_ws.py` FID 매핑 수정 필요.
       · `체결틱(0B) 수신 0건` -> WS 실시간 구독 문제(`실시간 등록:` 로그 확인).
       둘은 원인이 완전히 달라서 메시지가 구분해서 나온다.
   - 진단 알림의 `⚡ 틱 N → 무장 N → 버스트 N → 매수 N` 한 줄로 **어느 단계가
     병목인지** 바로 판단할 것. 무장이 0이면 228 문제, 무장은 많은데 버스트가
     0이면 대량체결 임계값이 높은 것이다.
   - `🔫 무장 — 체결강도 100 이상 N초 연속` 로그가 뜨는지.
   - `⚡ 틱 진입 트리거 [1A] — ...` 로그로 매수가 발생하는지.
   - 매수 사유 문자열: `1A 틱즉시진입 (강도 100+ N초 연속, ...) | 경로=절대
     | 시간계수 x1.00` 형태. `경로=` 값이 절대/단일/상대 중 무엇인지 세어두면
     임계값 조정 근거가 된다.
   - **진단 알림에 `강도 미무장(3초 미달)`이 대부분이면 정상**(신호 대기 중).
     반대로 무장은 많은데 매수가 0이면 버스트 임계값이 너무 높은 것.
6. **10:30 이후, 그리고 11시~14:50 사이에도 1A 매수가 계속 나오는지** —
   1A 시간창은 09:00~14:50 전체이지 10:30에 끊기지 않는다(이번 개편의
   성공 판정 기준. 표로 볼 때 10:30/11:00 열이 비어있으면 회귀, 14:55 이후
   열이 비어있는 건 정상 — 1A/Pullback 둘 다 14:50에 진입을 멈추기 때문).
6-1. **09:05~09:20 사이 돌파자동매매용 종목이 즉시 1A 평가되는지**
   (09:20 지연 게이트 제거 효과). 예전엔 이 구간이 통째로 비어 있었다.
6-2. **눌림목자동은 09:25 전엔 평가 안 되는 게 정상**, 14:50까지는
   계속 Pullback 매수가 나오는지.
6-3. **중복 편입 종목(주도주상위+눌림목자동 등)이 10:30을 경계로 전략을
   갈아타는지** — 같은 종목이 오전엔 `[1A]`, 오후엔 `[1A_눌림]`으로
   기록되면 정상(`resolve_strategy()` 규칙 실거래 첫 검증).
6-4. **14:50 이후 신규 진입 로그가 완전히 멈추는지, 15:10에 보유 종목이
   전량 강제청산되는지**(기존 15:15에서 변경). 15:10 이후 보유 0종목이어야
   한다.
6-5. **매도 로그에 시장가가 찍히는지** — `시장가 매도 실패 -> 지정가로
   1회 폴백`이 뜨면 매도 시장가가 거부된 것이므로 즉시 확인.
6-6. **`⚠️ 미관리 잔고 감지` 알림이 뜨는지** — 뜨면 매도 미체결이 실제로
   발생했다는 뜻이므로 해당 종목을 수동 확인할 것.
7. **1B/1L 관련 로그가 전혀 안 뜨는 게 정상**(2026-08-02에 코드째 삭제).
   뜨면 회귀 — 옛 커밋이 섞여 들어왔는지 확인할 것.

\### 진단 알림 읽는 법 (원인 판단표)
- `대량체결 부족`이 대부분 -> **코드 정상**, 필터가 제대로 거르는 중(시장에
  큰 손이 안 들어옴). 며칠 관찰 후 3천만/1억 임계값 조정 검토.
- `체결 데이터 없음` / `체결틱 부족` 다수 -> **0B 실시간 미수신**.
  WS 구독 로그(`실시간 등록:`) 확인.
- `감시중인데 체결틱 0` + 종목코드 -> 그 종목만 0B 미수신.
- `후보인데 감시 미시작` -> **라우팅 이상**. 1A 평가에 도달조차 못 함.
- `최근 10분간 평가 기록 없음` -> **`task_watchlist_reentry` 정지**.
- `실시간 편입 수신 0건` -> **조건검색 구독 이상**(오늘 고친 문제의 재발).
- `1A 슬롯 없음` -> 정상. 슬롯 회전 문제로 접근.
- `MDD/HALT/격리/컷오프` -> 정상(리스크 장치 작동 중).

\### 표본 쌓이는 대로 검증 (며칠~2주)
8. **`candidate_tier` 공식의 예측력** — 매수 시 `entry_tier`를 포지션에
   기록해 두었으므로, tier가 높았던 종목이 실제로 더 벌었는지 확인.
   상관이 없으면 공식을 바꾸거나 `PHASE1A_PRIORITY_MARGIN`을 크게 올려
   교체 로직을 사실상 끌 것.
9. **제안 A/B/C 파라미터 검증** — A: 1.5/1.2 배수, B: 조건식별 표본이
   최소 3건에 도달하는지(하루 검색식당 2~5건이라 대부분 중립일 것으로 예상),
   C: 1.5배 확대가 손실도 1.5배로 키운 건 아닌지.
10. **정체 정리 15분 / ±0.5% 밴드** — 너무 일찍 자르는지, 그 자리에 실제로
    더 나은 종목이 들어갔는지.
11. **1A 대량체결 임계값(3천만x3 / 단일 1억)** — 하루 매수 건수가 극단적으로
    적거나 많으면 조정. 진단 알림의 `대량체결 부족` 종목 수가 1차 지표.
12. **등락률 상한 16%/10%** — 16%로 완화한 뒤 고가 추격매수가 늘지 않았는지,
    눌림목 10%가 너무 타이트하지 않은지.
13. **틱 아카이브 데이터로 1A 백테스트 착수** — 08-03부터 `cache/tick_history/`에
    쌓이기 시작. 1~2주 후 `TradeFlowTracker.add_tick()`에 먹여 3초 강도 +
    대량체결 버스트 조건을 과거 데이터로 처음 검증.

\### 이월 (여전히 유효)
14. **0g 프로그램매매 FID 불일치** — 로그로 확정됨: `type='0g'`는 프로그램매매가
    아니라 **'주식종목정보'**(VI 관련, FID 297/305/306/307/370/382/592~594/689).
    기대한 118~124와 전혀 안 겹친다. `program_flow`가 하루 종일 0을 기록 중.
    매매 판단에 미연결이라 무해하지만, 전 종목 `0g` 구독은 낭비 -> 구독 목록에서
    빼거나 진짜 타입 코드를 찾을 것.
15. **429 포화 재측정** — 07-30 실측(하루 2,469건) 이후 재측정 안 함. 이번에
    실시간 편입이 살아나고 틱 아카이빙이 추가된 게 새 변수(반대로 reentry
    고속경로는 크게 줄인다). 08-03 실측 필요.
16. **`CandleCache.log_stats()` 미호출** — 여전히 어디서도 안 부름.
17. **`task_condition_snapshot_poll` 비활성 유지** — `_wait_for`가 소켓에서
    직접 recv 하는 구조라 `listen()`과 경합한다. Future/큐 방식으로 재설계해야
    안전하게 켤 수 있음. **다만 실시간 편입이 정상화됐으므로 우선순위 하락.**
18. **부분 매도 시 DB `buy_quantity` 미반영**(심각도 낮음, 미착수).
19. **`theme_manager.save_themes_to_db()` DB 저장 실패**("more than one '%s'
    placeholder"). 매매엔 영향 없음.
20. **WS 재연결 핸들러 진단** — 계속 관찰.
21. **지수 방어 임시 비활성화**(`MARKET_DEFENSE_ENABLED=False`, 07-28부터).
    새 전략 검증이 끝나면 재활성화 여부 판단할 것.

\### 즉시 끄는 법 (실전에서 이상 징후 발견 시)
- **틱 구동 진입 끄기(폴링으로 복귀)**: `TICK_ENTRY_ENABLED = False`
  — 되돌리면 15초 폴링만 남으므로 매수가 급감한다는 점 인지할 것.
- 무장 시간 조절: `TICK_STRENGTH_SUSTAIN_SEC` (3.0초)
- 버스트 완화/강화: `PHASE1A_BURST_TRADE_COUNT`(2건),
  `TICK_BURST_WINDOW_SEC`(5초), `TICK_BURST_REL_MULT`(20배)
- 시간대 계수 끄기: `TICK_BURST_TIME_MULT`의 계수를 전부 1.00으로
- 상대 버스트 경로만 끄기: `TICK_BURST_REL_MULT = 10**9`
- 주문 우선 레인 끄기: `KiwoomREST.ORDER_MIN_INTERVAL = 0.6` (조회와 동일)
- ~~1B 되살리기: `PHASE1B_ENABLED = True`~~ — **2026-08-02에 코드째 삭제됨.**
  되살리려면 커밋 `900757c`에서 되가져와야 한다(플래그만으로는 안 됨).
- 제안 A 끄기: `PHASE1A_TIER_SINGLE_MULT = 1.0`, `PHASE1A_TIER_BURST_MULT = 1.0`
- 제안 B 끄기: `core/strategy_performance.py`의 `ENABLED = False`
- 제안 C 끄기: `PHASE1A_SIZE_MAX_MULT = 1.0`
- 종목 우선순위 교체 끄기: `PHASE1A_PRIORITY_MARGIN = 999`
- 하이브리드 주문 -> 지정가 전용: `PHASE1A_ASK_DEPTH_MIN = 10**12`
- 정체 정리 끄기: `DEAD_POSITION_MIN = 999`
