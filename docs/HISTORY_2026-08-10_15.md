# 히스토리 2026-08-10 ~ 08-15 (CLAUDE.md에서 분리)

> 2026-08-17에 CLAUDE.md 본문에서 이 파일로 옮겼다. **내용은 한 글자도 바꾸지
> 않았다** — 매 요청마다 로드되는 CLAUDE.md의 고정 토큰 비용을 줄이는 것이
> 목적이고, 7월/08-01~09분을 `docs/HISTORY_*.md`로 뺀 것과 같은 방식이다.
>
> 🔴 **지금 도는 청산 사양이 전부 이 6일에 만들어졌다.** 무엇이 왜 그렇게
> 됐는지 궁금할 때 해당 날짜만 찾아볼 것. **통독하지 말 것.**

---

- 2026-08-10 (월, **🔴 실전 재개일 — 근본결함 2건 수정 + 청산 정책 5건 개편**) —
  **이 프로젝트에서 가장 많은 것이 바뀐 날이다.** 장중 긴급수술 2건 + 재시작,
  장마감 후 근본결함 수정과 청산 정책 전면 개편. 아래 순서대로 읽을 것.

  [결산] 청산 41건 / 실현 **-44,016원** / 승률 49% / 평균 -0.06%.
  ⚠️ **장은 아주 좋았다** — 우리가 산 종목들 종가가 대한광통신 +22.5% /
  저스템 +18.1% / 광전자 +14.1% / 주성엔지니어링 +13.3% / GS건설 +10.3%였다.

  [🔴 이 날의 한 줄 — 앞으로 모든 판단의 출발점]
  **매도한 자리에서 그 종목들이 당일 고가까지 평균 +5.45%, 종가까지 +1.70%
  더 갔다**(41건 전수, 일봉 대조). 반사실:
  | 시나리오 | 손익 |
  |---|---|
  | 현행(실제) | **-44,016원** |
  | 전부 종가까지 보유 | **+338,087원** |
  | 익절캡 6% + 미달분 종가청산 | **+486,480원** |
  ⚠️ 유령분 약 +80,000원이 섞여 있다(실제로는 0주). 빼도 종가보유 +26만원.
  => **종목 선정은 맞았고 청산이 전부 일렀다.** 41건 평균 보유 **25분**.

  [🔴 장중 긴급 ①] 손실반등 하이브리드 매도가 그날 손실의 82%를 만들었다.
  09:50까지 **8건 전부 손실, 합계 -50,636원**(-0.25%~-1.55%, 평균 -0.81%).
  08-05에 "6건 중 5건이 이후 상승"을 확인하고도 50% 분할로만 완화했는데
  부족했다. -> `LOSS_REBOUND_REQUIRE_BREAKEVEN = True` — **본전 이상에서만**
  발동한다. 손실 구간은 손절이 전담. ⚠️ '본전'은 수수료 차감 후라 **가격
  +0.23%부터**다. 사유 문구도 `반등소진 매도`로 정정(예전 문구는 사실이 아니게 됐다).

  [🔴 장중 긴급 ②] 우선순위 교체가 **115초에 일일한도 6회를 전부 소진**했다
  (09:01:41~09:03:36). 초반 슬롯 캡(4칸)이 09:00~09:05에 can_buy_more를 상시
  False로 만들어 후보가 쏟아지는 5분 내내 이 경로가 열린 것이 원인이다.
  그중 **3회는 유령 포지션 매도 실패에 낭비**됐고, 실제로 팔린 3건은 전부
  팔 이유가 없던 정체 종목이다(-1,605원 + 왕복수수료 3회).
  -> `PHASE1A_PRIORITY_MAX_PER_DAY = 0`(완전 OFF). 되살릴 땐 6이 아니라
     **2~3** 또는 '09:05 이전 금지'를 먼저 검토할 것.

  [장중 재시작] 상수는 프로세스 기동 시 로드되므로 09:59:53 정지 -> **10:02:07
  재기동**. ✅ 유령 포지션은 **손댈 필요가 없었다** — 잔고 대조가 자동으로
  잡았다(`[413630] 수동 매도 감지 -> 슬롯 즉시 해제`). DB 직접 수정이 권한으로
  막힌 게 결과적으로 옳았다.

  [🔴 근본결함 — 유령 포지션이 DB에 남아 재시작 때 부활한다]
  · 체인: 지정가 매수 미체결(호가창이 얇아 3,875 지정가인데 주가가 3,900으로
    이탈) -> 봇은 '주문 접수 성공'을 체결로 보고 349주 기록 -> 34초 뒤 매도
    시도 -> `800033: 0주 매도가능` -> 3회 실패 -> `holdings.pop()`으로
    **메모리에서만** 제거 -> 15초 주기 잔고 대조는 메모리를 보므로 아무 일도
    안 함 -> **DB 행이 'holding'으로 하루 종일 방치** -> 재시작 시 부활.
  · ⚠️ **잔고 대조 자체는 정상이었다** — 같은 날 PS일렉(09:56)·대원전선(09:57)을
    제대로 잡아냈다. **매도 실패 경로가 그걸 우회**한 것이 결함이다.
  · 수정: `_release_ghost_position()` 신설 — holdings/pending/plans 정리 +
    **DB까지 닫는다**. 매도 실패가 '수량 부족'(800033)이면 재시도하지 않고
    서버 잔고와 즉시 대조: 0주면 유령 확정 / 부분체결이면 수량 보정 /
    조회 실패면 판단 보류(기존 3회 경로). 지정가 폴백도 생략(주문 방식 문제가 아니다).

  [청산 정책 5건 개편 — 사용자 지정]
  | # | 변경 | 근거 | 롤백 |
  |---|---|---|---|
  | ① | 본전스톱 무장 **+1.0% -> +2.5%** | 6건이 +0.46%에 나가고 그 뒤 **+6.9%** 더 감(기회손실 +67,504원). 허용 되돌림이 0.8%p뿐이라 상승 초입에 털린다 | `BREAKEVEN_TRIGGER = 0.010` |
  | ② | 익절캡 **4.0/2.5/2.5/6.0 -> 6.0/6.0/6.0/8.0** | 아래 격자 참고 | 각 상수 원복 |
  | ③ | 09:00~09:05 발사 게이트 **변경 없음** | 검증이 현행을 지지 | — |
  | ④ | 정체정리 15분·시간정리 30분 **OFF** | 8건 -20,560 vs 종가보유 +25,715. 코칩은 -1.32%에 잘리고 +12.7% 갔다 | `STAGNANT_EXIT_ENABLED = True` |
  | ⑤ | 손절 **고정 -3% -> 장중 고저폭 x1.5** | 틱 4일 순 +0.086 -> +0.153%, 4일 전부 개선 | `STOP_LOSS_VOL_ENABLED = False` |
  ⚠️ **①과 ②는 반드시 세트다.** 캡만 올리면 08-05의 "캡 도달 종목 77%가
  본전까지 되돌아온다"가 재현된다.

  [🔴 문턱 x 캡 격자 — 이번 세션 최대 발견]
  틱 아카이브 4일(08-05·06·07·10), 09:05~, **손절·캡을 틱 경로에 실제로 태워** 측정:
  | 문턱 | 캡 2.5 | 캡 4.0 | 캡 6.0 | 캡 무제한 | 하루 종목 |
  |---|---|---|---|---|---|
  | 2.5(현행) | -0.064 | **+0.086** | -0.012 | **+0.175** | 18.8 |
  | 3.0 | -0.088 | +0.116 | +0.079 | **+0.558**(4/4) | 7.8 |
  | 3.5 | -0.083 | +0.354 | +0.300 | **+1.384**(4/4) | 5.2 |
  **가로(캡)가 세로(문턱)보다 훨씬 크다.** 문턱을 3.5까지 올려도 캡 2.5면
  여전히 마이너스다. => **문턱 상향은 채택하지 않고 캡만 풀었다.**
  · 문턱 상향을 버린 추가 이유: 하루 후보가 18.8 -> 5.2종목으로 줄어 **슬롯
    6개를 못 채운다.** 그리고 3.0의 **중앙값이 -0.133%, 승률 39%**로 소수
    대박(+14.01%, +11.65%) 의존이다(08-07 '꼬리가 두꺼운 분포' 함정 그대로).
  · 천장은 **VI 상단**이 맡는다 — 08-10에 **종가보유를 이긴 규칙은 VI 상단
    확정매도뿐**이었다(2건 +50,162원, 종가보유 대비 **+53,720 우위**).
    일승은 VI에서 판 자리가 당일 고가에서 **+0.1%**였다.

  [③을 바꾸지 않은 이유 — 검증이 아이디어를 기각했다]
  | 규칙 | n | 순평균% | 승률 |
  |---|---|---|---|
  | **면제(현행)** | 154 | **+0.247** | **58%** |
  | 가속 3.0 | 147 | +0.210 | 52% |
  문턱을 3.0까지 올려도 **건수가 154 -> 147로 거의 안 줄어든다.** 120초 창인데
  09:00 이전 데이터가 없어 `accel = 4*min(30,t)/t`라 **t<=48초면 무조건 2.5 초과**다
  (문서에 이미 적혀 있던 워밍업 아티팩트). 문턱이 물리적으로 작동하지 않는다.

  [🔴 ⑤는 처음 만든 걸 버리고 다시 만들었다 — 시간 척도 불일치]
  · 일봉 ATR(14)로 구현했는데 **실측이 반증했다**: 금강철강은 일봉 ATR이
    표본 최저(6.9%)라 손절이 **오히려 좁아지는데**, 정작 그날 -3.04%에 잘리고
    당일 고가까지 **+24.3%** 간 종목이다. 이 시장의 일봉 ATR 중앙값은 12.3%로
    손절폭 척도로 쓰기엔 너무 크다(배수 1.2면 전 종목이 상한에 붙는다).
  · 원인: **보유가 평균 25분인데 일봉은 며칠 척도**다.
  · -> **최근 120초 고저폭**으로 교체. 4일 146 종목·일 실측 중앙값 2.04%라
    배수 1.5면 중앙이 -3.06%로 현행과 같으면서 **146개 중 73개가 실제 차등**을 받는다.
  · 값은 **매수 시점에 1회 계산해 `pos["stop_rate"]`에 박는다**(핫패스 O(1)).

  [🔴 하마터면 무효인 채로 나갈 뻔했다 — 다음 세션이 반드시 기억할 것]
  `compute_stop_rate`가 `time.time()` 기준으로 창을 잘라서, 리플레이에서 창이
  통째로 비어 **조용히 고정값으로 퇴화**했다. ON/OFF **A/B 결과가 완전히 동일**해서
  발견했다. -> 기준 시각을 **버퍼의 마지막 틱**으로 바꿨다. 이제 갈린다:
      OFF: 손절 11건(전부 -3.0x%, 하나는 **-8.40% 갭관통**)
      ON : 손절  7건(**-2.59% ~ -5.64%**, 종목별 차등)
  **교훈: 새 규칙을 넣으면 반드시 ON/OFF A/B로 '실제로 달라지는가'를 확인할 것.**
  스위트가 전원 통과해도 규칙이 무효일 수 있다.

  [교훈 — 스텁 시그니처가 감사를 거짓말시킨다 (하루에 세 번 밟았다)]
  `TradeRepository.update_sell`은 실물이 **trade_id를 위치 인자**로 받는데
  테스트 스텁이 `(cls, **kw)`라 TypeError가 났고, 호출부의 except가 그걸 삼켜
  **'DB 종료 0건'**으로 나왔다. 리플레이의 `_OrderMgr.buy`도 시장가에 1원을
  돌려줘 `buy_price=1`을 만들어 **매도 0건**이라는 가짜 결과를 냈다.
  -> audit_deep과 리플레이 하네스 양쪽 스텁을 실물과 맞췄다.

  [부수 정리]
  · 장전 스캐너 OFF(`AutoTrader_PremarketScan` = `Disabled`, 사용자 지정) —
    일봉 눌림 후보가 조건검색식에 거의 안 잡혀 Phase 1/2 근거가 안 쌓인다.
  · CLAUDE.md 128k -> 108k (08-06 히스토리를 `docs/HISTORY_2026-08-06.md`로 분리).

  [검증] 13개 스위트 **1,555건 전원 통과 / 실패 0**
  (297/89/130/70/165/24/142/187/63/148/50/91/99).
  문턱 스윕은 실물 `value_acceleration`과 **240개 지점 등가검증** 후 수행했고,
  독립표본(종목·일당 첫 신호) + 시간균등 기준선 + 날짜별 분해 원칙을 지켰다.
  정책으로 끈 기능(정체정리·우선순위교체)의 배선 테스트는 **해당 블록에서만
  상수를 되살려** 유지했다 — 되살릴 때 검증이 없으면 그대로 사고가 난다.
  독립 리플레이(아카이브 15일): 매수 127건 전부 게이트 만족, 캡 위반 0, 예외 0.

- 2026-08-11 (화, **손절 -4.5% / -3% 물타기 / 조건식 개편 — 전부 사용자 지정**) —
  결산 **청산 29건 / -86,912원**. 장중엔 코드를 건드리지 않고(REST도 0콜) 관찰만
  했고, 15:33 자동종료 후 한 번에 적용했다.

  [🔴 이 날의 진단 — 폴라리스AI 한 건이 전부를 보여줬다]
  ```
  12:46:30  최초 편입 (돌파자동매매용)   ← 그날 고점 6,840이 12:46
  12:46:38  무장 + 트리거 (가속도 4.00배 = 수학적 상한)
  12:47:02  매수 6,776원 = 고점의 99.1%
  12:51:56  손절 -2.60%
  ```
  · **봇이 늦은 게 아니다 — 정보가 고점에서 도착했다.** 되돌림·숙성·가속도는
    전부 편입 **이후**에 작동하므로 이 구조로는 못 고친다.
  · 한켐도 동일: 편입 시점 조건검색 응답이 `현재가 7,830 = 당일 고가 7,830`.
  · 손절 9건 중 **6건이 5분 이내**(중앙 2.6분, KBI메탈은 **30초**).
    그리고 손절된 5종목이 **전부 22~26분 안에 재무장**했다.
  · 🔴 **가속도가 편입 직후 ~90초간 무조건 상한(4.00)이다** — 120초 버퍼가
    비어 있어서다. 즉 신규 편입 종목에는 **발사 게이트가 사실상 없다**.
    폴라리스AI·한켐·서산 3건 모두 4.00이었다(문서엔 09:00~09:02 워밍업
    아티팩트만 적혀 있었는데, **편입 직후에도 같은 일이 일어난다**).

  [적용 — 5건]
  | # | 변경 | 근거 |
  |---|---|---|
  | ① | **손절 -3% -> -4.5%**, 변동성 하한도 -4.5%, 관찰바닥 0.045 -> **0.055** | 위 손절 6/9건 5분 이내 + 전원 재무장 |
  | ② | **-3% 물타기** — 조건 없이 동일수량 1회, 종목당 200만 캡 | 사용자 지정 |
  | ③ | 조건식 `돌파자동매매용` -> **`돌파전`** + **`돌파후`(확인 전용)** | 사용자가 HTS에서 분리 |
  | ④ | **수동 추가매수 합산** — 서버 평단·수량을 봇이 받아들인다 | 사용자 지정 |
  | ⑤ | **수동매도 vs 미체결 구분** | 08-11에 사용자 매도 6건이 전부 "미체결"로 기록돼 통계 오염 |

  [🔴 ①이 만든 연쇄 — 하마터면 기능 하나를 조용히 죽일 뻔했다]
  손절선(-4.5%)과 `RESCUE_ADD_OBSERVE_FLOOR`(0.045)가 **같아진다**.
  그러면 손절선에 닿는 순간이 곧 관찰 바닥이라 `_rescue_gate`가 시작하자마자
  "sell"로 떨어져 **'손절 대신 추가매수'가 원리적으로 성립 불가**가 된다
  (상수 주석이 경고하던 바로 그 실패 모드). -> 0.055로 같이 내렸다.
  대가는 미구조분 최대손실이 -4.5%가 아니라 **-5.5%**가 되는 것이고,
  사용자가 이를 알고 선택했다("리스크는 수동으로 관리").

  [🔴 ②가 만든 두 번째 연쇄 — 최종 방어선이 무력화됐다]
  물타기 후 평단 9,850 -> 평단 기준 손절선 9,407 = **원가 -5.93%**.
  그런데 `RESCUE_ADD_FINAL_STOP`(원가 -6% = 9,400) 검사가
  `if gross_rate <= stop_rate:` **안**에 있어서, 평단 손절선이 원가 -6%보다
  아래면 **최종 방어선에 영영 도달하지 않는다**(수량이 2배인 채로 더 흘러내린다).
  -> 검사를 **밖으로 꺼내** 원가 -6%를 진짜 하드 백스톱으로 만들었다.
  실측 확인: -5.90% 보유 / **-6.00% 청산**(사유 `구조 후 최종손절`).

  [🔴 ②는 백테스트가 판정 불가로 나왔다. 사용자 지시로 적용했다]
  실전 5일 133건(수동매도 제외) **금액 기준**:
  | 가정 | 물타기 OFF | 물타기 ON | 차이 |
  |---|---|---|---|
  | 비관(분 안 저가 우선) | +25,313원 | +54,519원 | **+29,206원** |
  | 낙관(분 안 고가 우선) | -203,306원 | -296,875원 | **-93,569원** |
  **분 안의 고가/저가 순서 가정만 바꿔도 부호가 뒤집힌다.** 그 외에도
  · 이익이 상위 3건에 의존 — **그 3건 빼면 -249,832원**
  · 날짜별 자본효율 우세 ON 3일 / OFF 2일(동전던지기)
  · 08-06형 하락일엔 손실이 정확히 2배(-84,284원)
  · -3% 도달률이 **38%**(155건 중 59건)로 상시 작동한다
  ⚠️ 7일(08-03/04 추가)로 넓히면 부호가 같아지지만, **그 이틀은 종목당
  매수금액이 360~450만원으로 실전의 7~18배**(모의투자 시절)라 금액 합계를
  지배한다 — 확장이 도움이 안 됐다.
  -> `[물타기]` 태그 + `_avg_down_count_today`로 **실거래 채점**할 것.

  [🔴 VI 여유 가설은 실측으로 기각됐다 — 정반대였다]
  "VI 상단까지 여유가 작으면 최대이익이 막혀 불리하다"는 가설을 5일 전수로 검증:
  | VI 여유 | n | 평균% | 승률 | 손절률 | -4.5% 도달률 | 실현 |
  |---|---|---|---|---|---|---|
  | **0~2%** | 7 | **+0.58** | **57%** | 43% | **29%** | **+18,269원** |
  | 2~3% | 7 | -0.87 | 43% | 57% | 57% | -36,565원 |
  | 4~6% | 35 | -0.33 | 46% | 49% | 34% | -97,585원 |
  | 6%+ | 38 | -0.29 | 47% | 39% | 39% | -157,079원 |
  **여유가 가장 작은 구간이 유일하게 플러스**이고 -4.5% 도달률도 가장 낮다
  (VI 근처 종목은 강해서 잘 안 밀린다). 진입 필터는 **넣지 않았다**.

  [🔴 VI 상단 확정매도는 재검토 대상이 됐다 — 08-10 결론이 뒤집혔다]
  7건 전수 반사실: 실제 +18.41% vs **종가보유 +36.93%**(차이 -18.52%p).
  6/7이 "더 갔다" — 서산은 +1.68%에 팔고 종가 **+18.66%**, 혜인은 +2.90%에
  팔고 당일 고가 +17.56%였다. 08-10에 "종가보유를 이긴 유일한 규칙"이라 적은 건
  **2건 표본**이었다. ⚠️ 다만 종가보유는 슬롯을 하루 종일 묶고, VI 발동 시
  2분 단일가로 못 파는 위험이 있다. **이번엔 안 건드렸다** — 별도 결정 사항.

  [테스트 — 옛 사양을 단언하던 58건을 전부 갱신했다]
  · 손절 -3% 전제로 `9_600`/`9_690` 같은 **수치를 박아둔 픽스처**를 전부
    `int(10_000 * (1 + SM.STOP_LOSS_RATE)) - 1` 같은 **상수 참조**로 바꿨다.
  · 🔴 **물타기가 손절을 가로챈다**(-3%가 -4.5%보다 먼저 온다). 손절 경로만
    보는 블록에서는 `AVG_DOWN_ENABLED = False`로 끄고 보게 했다 — 안 그러면
    손절 테스트가 전부 "보유유지"로 실패한다.
  · `test_live_20260805`에서 **가려져 있던 픽스처 결함**이 드러났다 —
    `warmup_until`에 float(timestamp)를 넣어뒀는데, 손절이 항상 먼저 발동해
    그 줄에 도달하지 않아 숨어 있었다. 손절선이 깊어지자 TypeError로 터졌다.
  · `_execute_buy` 호출부 **4곳 -> 5곳**(`_maybe_average_down` 추가). audit_deep이
    수를 기계적으로 세므로 자동으로 잡혔다.
  · 🆕 `test_patch_20260811.py` **62건 신설**.
  · 🔴 감사 [9]에 **새 상수 6종 대조를 추가**했다 — 손절·물타기를 바꿨는데도
    감사가 통과했던 이유가 "그 항목이 대조 목록에 없어서"였다.
    ⚠️ 그 과정에서 함정 하나를 또 밟았다: 감사가 자기가 꺼둔
    `AVG_DOWN_ENABLED=False`를 문서와 대조해 거짓 실패를 냈다 —
    기본값(`_AVGDOWN_DEFAULT`)과 비교하도록 고쳤다.
  · 🔴 리플레이 로더 결함 수정 — 파일의 **첫 틱 날짜만** 채택해 나머지 날짜를
    통째로 버리고 있었다(417500은 파일이 07-13부터라 08-11 틱이 전부 사라졌다).
    모든 날짜를 추출하도록 고쳐 **매수 표본 95 -> 169건**.

  [검증] **15개 스위트 1,660건 전원 통과 / 실패 0**
  (297/89/131/70/165/24/143/187/68/157/51/91/103/22/62).
  `import main` OK / .py 91개 구문오류 0 / 태스크 정의 21 == gather 21 /
  DB 미청산 `holding` 0건(`manual` 3건은 설계대로 제외).

- 2026-08-12 (**장중 09:41 긴급수술 — 잔고 반영 지연을 '수동 매도'로 오판**) —
  개장 40분 만에 **10건**이 잘못 처리됐다. 09:44 재기동으로 적용.

  [🔴 증상 — 봇이 자기가 산 주식을 '사용자가 팔았다'고 판정했다]
  ```
  [09:01:06] 알트(459550) 246주 매수
  [09:02:07] 알트 수동 매도 감지 -> 슬롯 즉시 해제       (61초)
  [09:02:45] 알트 유령 포지션 감지 — 서버 잔고 246주      (99초, 계좌엔 그대로!)
  ```
  · `RECONCILE_GRACE_SECONDS = 45`가 짧았다. **개장 직후 키움 잔고 반영이
    99초까지 걸린다.** 그 사이를 '사라졌다'로 읽었다.
  · 시장가로 산 코칩도 같은 일을 겪었다 — **미체결이 아니라 반영 지연**이다
    (시장가는 즉시 체결된다). 매수 후 52~100초에 7건이 이렇게 처리됐다.

  [피해 — 손실 자체보다 '조용히 틀린 기록'이 문제다]
  ① DB가 **손익 0(`수동 매도 감지, 매수가로 대체`)**으로 닫혀 통계가 오염된다.
     08-11에 `seen_on_server`를 만들어 고치려던 바로 그 문제인데, 그 표식이
     `_release_ghost_position`에만 쓰이고 **이 경로엔 안 걸려 있었다.**
  ② 슬롯이 풀렸다 다시 잡히며 **같은 종목을 재매수**한다 — 동양파일·코칩이
     두 번씩 매수됐다. 왕복 수수료(0.23%)를 그만큼 반복 지출한다.
  · 오전 실현 -8,032원. **실계좌 자체는 정합했다**(방치 종목 0) — 잔고 대조가
    뒤늦게 자기교정을 하기 때문이다. 그래서 눈에 잘 안 띈다.

  [수정 — 시간만 보지 않고 `seen_on_server`와 묶었다]
  · `_in_grace()`가 이제 2단이다: 45초는 무조건 유예 / 그 뒤에도 **서버에서
    한 번도 확인된 적 없으면** `RECONCILE_UNSEEN_GRACE_SECONDS`(300초)까지 기다린다.
  · ⚠️ **무한정 기다리지는 않는다** — 진짜 미체결이면 holdings에 영영 남아
    슬롯을 점유한다. 상한을 넘으면 종전대로 정리하고, 그 뒤엔 매도 시 800033 ->
    `_release_ghost_position`(08-10 신설)이 받는다.
  · 롤백: `RECONCILE_UNSEEN_GRACE_SECONDS = 0`이면 08-12 오전 동작으로 복귀.

  [재기동 검증] 09:41:26 STOP_SIGNAL -> 09:44:19 정지(48초, 틱 아카이브가 물고
  있었다) -> 09:44:28 재기동. **DB 복원 보유 3 (1A=3)** 이 실계좌와 정확히 일치
  (JW신약/자이에스앤디/알트 + 개인보유 2종목, 방치 0).
  🟢 **오버나이트 격리는 발동하지 않았다 — 당일 매수분이라 정상 복원됐다.**
  새벽에 넣은 "판별은 buy_time의 날짜" 규칙이 장중 재시작을 정확히 보호했다
  (08-10에 실제로 겪은 상황을 예상하고 넣은 것이 그대로 맞았다).

- 2026-08-15 (토, **주말 자동기동 규명 + 스케줄러 3건 정비. 매매 코드 변경 0줄**) —
  사용자가 "주말인데 PC 켜니 프로젝트가 실행됐다"며 원인 규명과 전면 점검을 요청.
  **코드 결함은 0건이었다.** 스위트 **1,849건 전원 통과 / 실패 0**(18개, 실측).

  [🔴 원인 — 08-13 수정의 의도된 부작용이었다]
  · 두 개가 따로 떴다: **대시보드**(19:30:25, 시작프로그램 `AutoTrader 대시보드.lnk`
    — 08-13에 사용자가 만든 것)와 **매매봇**(19:38:20).
  · 봇은 **9초 만에 정상 종료**했다(`buy_attempted 0` / 보유 0). 자동종료 시각을
    한참 지나 떴기 때문에 기동 즉시 종료 절차로 들어갔다.
  · 기전: 08-13에 켠 **`StartWhenAvailable=True`**가 놓친 08:59을 따라잡은 것이다
    (`NumberOfMissedRuns=0`이 증거). 즉 **PC 켜는 시각 = 봇 기동 시각**이 됐다.
    08-13에 "절전으로 개장 25분을 놓친" 문제를 고친 대가다.

  [적용 3건 — 전부 사용자 지정]
  | # | 변경 | 이유 | 롤백 |
  |---|---|---|---|
  | ① | `AutoTrader_Start` 트리거 **매일 -> 주간 월~금**(마스크 62) | 주말 기동 차단 | XML 백업본으로 복원 |
  | ② | `AutoTrader_DailyBackup`·`YouTube_Transcript_Bot`에 **`StartWhenAvailable=True`** + 배터리 제한 2개 해제 | 아래 결함 | 각 값 되돌리기 |
  | ③ | 08-15 백업 **수동 1회 실행**(549파일 238.9MB) | 누락분 복구 | — |
  · ⚠️ YouTube는 **`WakeToRun`만 의도적으로 제외**했다 — 1시간 주기라 켜면
    하루 24번 PC를 깨운다. 오늘 실패 원인은 '절전'이 아니라 '전원 off'였고
    그건 `StartWhenAvailable`이 해결하므로 `WakeToRun`은 불필요하다.

  [🔴 점검 중 발견 — 오늘 백업이 통째로 누락됐다]
  · `AutoTrader_DailyBackup` 결과 **0x800710E0(요청 거부)**, 백업 폴더에 08-15 없음.
  · 원인: 18:00에 PC가 꺼져 있었는데 이 작업은 **`StartWhenAvailable=False`**라
    따라잡지 못했다. **08-13에 `AutoTrader_Start`만 고치고 나머지 작업은 옛 설정
    그대로였다** — 같은 계열 결함이 다른 작업에 남아 있던 것이다.
  · 📌 교훈: **스케줄러 설정을 고칠 땐 같은 함정을 가진 다른 작업도 같이 훑을 것.**
    이 프로젝트의 "같은 규칙이 여러 경로에 흩어져 있다"가 코드 밖에서도 성립했다.

  [문서 드리프트 1건 — `test_replay_archive` 실행 시간]
  "약 40초"로 적혀 있었으나 실측 **204초**다(아카이브가 계속 쌓인 결과).
  멈춘 줄 알고 중단할 만해서 "3~4분 / 진행은 CPU 시간으로 확인"으로 고쳤다.

  [검증] 스위트 18개 **1,849건**(297/89/137/70/165/24/145/187/68/176/51/91/103/22/
  62/47/61/54) 전원 통과 · 실패 0 / `import main` OK / 상수 20종 문서 기대값 전부
  일치 / DB 미청산 **`manual` 5건 · `holding` 0건**(문서와 일치) / .py 112개 구문
  오류 0 / 태스크 정의 21 == gather 21 / `ui/` -> core·main·api import 0건 /
  `.bat` 4개 비ASCII 0바이트 / `STOP_SIGNAL`·`NEW_SESSION_REQUESTED` 없음.
  ⚠️ 검증 하네스에서 자기 결함 2건을 밟았다(둘 다 내 스크립트 문제, 코드 무관):
  ⓐ 자식 출력을 cp949로 디코드해 UnicodeDecodeError — `encoding="utf-8"` 명시로 해결.
  ⓑ 요약을 정규식으로 긁다 로그의 `WARNING ... 일봉 예열 실패` 줄을 잡아
     `test_patch_20260801`을 "실패 15건"으로 **오보**했다(실제 297/0).
     📌 **stdout(요약)과 stderr(로그)를 합쳐서 파싱하지 말 것.**

- 2026-08-15 (토 저녁, **08-17 기동 전 전면 검증 — 🔴 매수 결함 1건 + 문서 결함 다수**) —
  "수정변경된 부분이 꽤 있으니 매수매도에 오류가 절대 없게 촘촘히 검증" 요청.
  **스위트 1,849건이 전원 통과하는 상태에서 매수 수량을 틀리게 만드는 결함이
  나왔다.** 상수 20종은 전 항목이 문서 기대값과 일치했고 인프라도 정상이었다.

  [🔴 결함 — 봇이 **자기 분할매도**를 '사용자의 수동 추가매수'로 오인한다]
  · 체인: 봇이 50% 분할매도 -> `holdings.qty`는 **즉시** 절반 -> 그런데 서버
    잔고는 한동안 **팔기 전 수량**을 그대로 준다(08-12 실측 **99초**) ->
    `server_qty > tracked_qty` -> 08-11에 만든 **'수동 추가매수 합산'**이 발동 ->
    **판 주식이 되살아난다.**
  · 기존 가드 둘 다 못 막는다: ①되돌림 계획은 분할 시점엔 이미 사라졌고
    ②'2회 연속 같은 값'은 `SYNC_INTERVAL`(15초) x2 = **30초면 채워져** 99초
    지연 창 안에 통째로 들어간다.
  · 🔴 **피해가 기록에서 끝나지 않는다.** `_maybe_average_down`이 `pos["qty"]`로
    주문 수량을 정하므로(`exact_quantity`) **물타기가 50주가 아니라 100주를 산다.**
    실물 A/B로 재현: 가드 OFF -> `{'side':'buy','qty':100}` / ON -> `qty 50`.
    그 외 DB `buy_quantity`가 2배로 덮이고, `breakeven_armed`가 False로 리셋돼
    **잔량의 본전스톱 보호가 사라지며**, 사용자에게 거짓 텔레그램이 간다.
  · **왜 지금까지 안 터졌나** — 로그 전수 확인 결과 `💰 수동 추가매수 감지`는
    08-13 삼화전자 2건뿐이고 **전부 진짜 수동매수**였다. 분할매도(08-04 동적캡 /
    08-05 반등소진 / 08-12 VI)가 있었지만 잔량이 대개 **30초 안에** 정리돼
    2회 연속 창을 못 채웠다(08-14 다날은 분할 26초 뒤 트레일 청산).
    🔴 **그런데 08-14에 신설된 본전스톱 50% 분할이 이 확률을 크게 올린다** —
    08-14 하루에만 본전스톱이 **5건** 발동했고, 그 잔량은 3% 트레일이 받으므로
    **훨씬 오래 산다.** 즉 08-17부터 처음으로 위험 구간에 들어간다.
  · 수정: `_reconcile_manual_sells`의 증가 분기 **한 곳**에만 세 번째 가드.
    ⓐ 봇이 판 지 `RECONCILE_SELL_GRACE_SECONDS`(**120초**, 실측 99초를 덮는다)
    이내이고 ⓑ 서버 수량이 `tracked + _realized_qty`(= 팔기 전 수량) 이하면
    **판단을 보류**한다. 두 조건을 **동시에** 만족할 때만이라
    - 진짜 추가매수(팔기 전 수량 초과)는 **즉시 그대로 감지**되고(실측 130주)
    - 지연이 걷히면 종전 동작으로 복귀한다(**영구 억제가 아니다**).
    연속 카운터(`_pending_qty_up`)도 같이 지운다 — 안 지우면 지연이 걷힌 직후
    남아 있던 옛 관측 하나가 '2회 연속'을 채워 그대로 오인이 성립한다.
  · 📌 **이 프로젝트 반복 패턴의 정확한 재발이다** — 08-12에 고친 것이
    "잔고 반영 지연을 **수동 매도**로 오판"이었고, 이번은 **정반대 방향**의
    같은 함정이다. 한쪽을 고칠 때 반대 방향을 같이 보지 않았다.

  [🔎 같이 확인하고 '문제 없음'으로 판정한 것 — 다시 파헤치지 않도록]
  · **본전스톱 50% 분할(08-17 첫 가동)은 전 경로가 정상이다.** 실물
    `on_price_update`로 전수 확인 — 같은 가격 **10틱**에도 잔량 50주 생존
    (08-12 VI 결함 부류 회귀 없음) / 트레일·익절캡·손절로 각각 정상 청산 /
    재상승·재하락해도 2차 분할 없음(`be_partial_done`) / 이미 분할된 포지션은
    종전대로 전량 정리(의도 보존) / **1주는 안 쪼개고 전량, 3주는 1주만**.
  · **경계값이 정확하다** — 실물 `_net_rate`로 경계 가격을 역산해 확인:
    무장은 순 **2.50%에서** True(2.49% False), 바닥은 순 **1.01% 보유 /
    0.99% 분할**. ⚠️ 경계 검증에 `int(가격*(1+net+0.0023))`을 쓰면 부동소수
    오차로 **멀쩡한 코드가 실패로 보인다**(08-13에 이미 겪은 것을 또 밟았다).
    **반드시 실물 `_net_rate`로 역산할 것.**
  · `exit_reason` 대입 6곳이 **전부 `is None` 가드 안**이라, `_be_partial`
    플래그가 다른 청산 사유에 잘못 붙어 50%만 파는 경로는 없다.
  · 부분매도 시 `pos["qty"]`가 정확히 갱신된다(`remaining - quantity`).
  · DB 미청산 **`manual` 5건 · `holding` 0건**(문서와 일치). 08-14 보유분은
    전부 청산돼 오버나이트 격리 대상이 없다.

  [⚠️ 결함은 아니지만 08-17부터 새로 생기는 조합 — 사용자 판단 필요]
  **물타기가 '분할 잔량'에 발동하면 같은 틱에 트레일이 전량을 되판다.**
  실물 재현: 분할(50주 매도) -> 원가 -3% 도달 -> 물타기 50주 매수 ->
  **같은 틱**에 `분할 잔량 트레일`이 100주 매도. 물타기는 순위 0, 트레일은 5-b라
  한 번의 `on_price_update` 안에서 둘 다 돈다.
  · 구조상 **거의 항상** 그렇게 된다 — `trail_peak`는 분할 시점 고점이고
    물타기 지점(원가 -3%)은 그로부터 3% 넘게 아래다.
  · 즉 **왕복 수수료(0.23%)만 확정하는 자기취소 주문**이 된다.
  · 실거래 12건의 물타기는 **전부 분할 이전 포지션**이라 아직 발생한 적이 없다.
    08-14식으로 본전스톱이 5건 돌면 08-17부터 나올 수 있다.
  · 🔴 **고치지 않았다** — 물타기는 사용자가 "**조건 없이** 동일수량 1회"로
    지정한 규칙이라, 조건을 붙이는 것은 사양 변경이다. 원하면 한 줄이다:
    `_maybe_average_down` 앞에 `if pos.get("partial_exited"): return`.
    (근거: 봇이 방금 '줄이겠다'고 판단한 포지션을 같은 틱에 되늘리는 것)

  [🆕 같은 날 밤 — 본전스톱을 **확정익절 2%**로 교체 (사용자 지정)]
  · 사용자 질문("본전스톱을 좀더 늦춰서 1% 익절로 바꾸는 게 나은지")에서 시작해
    **실매수 150건 / 8일 종일 1분봉**(09:00~15:20)으로 정책 격자를 돌렸다.
    비관·낙관 두 가정 + 슬리피지 0~0.3%까지 태웠다.
  · 🔴 **"조금 늦추기"는 역효과였다** — 무장 스윕(바닥 1.0% 고정, 비관/낙관):
    2.5%(당시) 0.551/-0.063 → 3.0% 0.481/-0.119 → 3.5% 0.515/0.052 →
    4.0% 0.654/0.458 → 6.0% 0.807/0.874. **3.0%에서 오히려 나빠지고**
    3.5%부터 단조 상승하는데 6.0%는 익절캡과 같아 **사실상 끈 것**이다.
    => 데이터가 말한 건 "늦춰라"가 아니라 **"이 규칙이 순손해다"**였다.
  · 🔴 **왜 나쁜가 — 전수표가 이유를 보여줬다.** 무장을 찍고 바닥까지 되돌아온
    종목 상당수가 그 뒤 **익절캡(6%)까지** 갔다: 대한광통신 -1.32% vs OFF **+7.62%**,
    알트 +4.52 vs +9.22, 레몬헬스케어 -0.23 vs +6.64, 마키나락스 +3.74 vs +6.78.
    **본전스톱은 곧 크게 갈 종목을 정확히 골라 +1%에 파는** 형태였다 —
    08-10에 기록한 근본 문제("판 자리에서 더 간다")와 같은 계열이다.
  · 평평한 익절 스윕(비관): 1.0% 0.687 / 1.5% 0.734 / 2.0% 0.837 /
    **2.5% 0.932(정점)** / 3.0% 0.738 / 3.5% 0.607. **1%는 최적이 아니다.**
    ⚠️ 곡선이 울퉁불퉁해 "2.5%가 정확히 최적"으로 읽으면 과적합이다.
  · 사용자 지정 = **+2% 도달 즉시 50% 분할, 잔량은 3% 트레일**. 그 정확한 조합을
    따로 재니 현행 대비 비관 0.360 vs 0.326 / 낙관 -0.046 vs -0.240,
    승률 55% -> **71%**, 날짜별 비관 5승3패 / 낙관 6승2패 — **두 가정 모두 우세**.
  · 🔴 **하네스 결함을 하나 밟았다** — 실물은 분할 시
    `trail_peak = max(highest_price, 현재가)`인데 시뮬은 분할 시점 가격만 썼다.
    본전스톱은 **내려오는 길에** 분할하므로 `highest_price >> 현재가`라,
    고치기 전엔 **현행이 실제보다 좋게** 나왔다(0.383 -> 0.326).
    📌 하네스는 실물 코드와 **한 줄씩 대조**할 것 — 특히 상태를 세팅하는 줄.
  · 🔴 **구현에서 잔량 보호 가드를 빠뜨릴 뻔했다.** 처음 배선은 동적캡·VI·
    반등소진이 남긴 **잔량도 순 +2%를 넘으면 전량 정리**했다 — 08-12에 결함으로
    고친 "분할 잔량이 다음 틱에 털린다"와 **같은 결과**(경로만 다르다)이고,
    채택 근거가 된 백테스트도 '잔량은 트레일이 받는다'로 모델링했다.
    `not pos.get("partial_exited")` 가드를 넣어 **검증한 것과 실거래를 일치**시켰다.
    (기존 스위트 `test_patch_20260813`이 이 결함을 잡아냈다.)
  · 🔴 **6개 스위트가 한꺼번에 실패했다** — 전부 옛 본전스톱 사양을 단언하던
    것들이다. 지우지 않고 **`FLAT_TP_ENABLED = False` 롤백 컨텍스트로 감쌌다**
    (끈 기능의 배선 테스트를 지우면 되살릴 때 검증이 없다 — 08-10 교훈).
  · ⚠️ 감사에 소스 문자열 단언을 넣었다가 **또 오탐**을 냈다. 실물
    `on_price_update`에 잔량 포지션을 태우는 **동작 검사**로 교체했다.

  [검증] **19개 스위트 1,912건 전원 통과 / 실패 0** — 🆕 `test_patch_20260815.py`
  **55건 신설**(결함 재현 + **A/B로 물타기 50주 vs 100주가 갈리는지** +
  08-11 기능 보존 + 유예 만료 복귀 + 롤백 / 본전스톱 분할 잔량 전 경로(롤백 검증) /
  **확정익절 경계·즉시분할·10틱 생존·상호배타·롤백 2종**).
  `import main` OK / .py **100개 구문오류 0** / 태스크 정의 **21 == gather 21** /
  `ui/` -> core·main·api import **0건** / `.bat` 비ASCII 0바이트(`shutdown_pc.bat`
  165바이트는 **참조 0건 고아 파일**) / `_execute_buy` 호출 함수 **5곳** 불변 /
  `STOP_SIGNAL`·`NEW_SESSION_REQUESTED` 없음 / 스케줄러 `AutoTrader_Start`
  **Ready, 요일마스크 62, NextRun 08-17 08:59**(주말 차단 확인) /
  `PremarketScan` Disabled.
  ⚠️ `test_replay_archive` 실행시간은 **콜드 204초 / 웜 51초**다(08-15 오전
  204초는 콜드 측정이었다). 100MB 아카이브라 디스크 캐시 영향이 크다.

- 2026-08-14 (금, **정상 매매 + 장마감 후 본전스톱 50% 분할 적용**) —
  ⚠️ **이 날짜의 히스토리 항목이 08-15까지 통째로 비어 있었다**(2026-08-15 검증에서
  발견·보강). 변경은 `되돌리는 법`·`기대값` 블록·PC 검증 표에만 적혀 있었고,
  **그날 무슨 일이 있었는지가 어디에도 없었다.**

  [결산] 청산 **8건 / DB 실현 +48,947원 / 평균 +0.71%**. 스케줄러 08:59 정상 기동.
  ⚠️ 그중 **2건이 '수동 매도 감지'**(삼기·지투파워, 손익 0 가짜 기록)다 —
  08-13에 이어 또 나왔다. **결산은 실계좌 체결내역으로 볼 것.**

  [🟢 08-13 적용분 ⓐⓑ가 둘 다 실거래로 확인됐다]
  · **ⓑ 본전스톱 바닥 +1.0%가 정확히 작동했다** — 발동 **5건**이 전부
    실현 순 **+0.85 ~ +1.00%**에서 나갔다(아이로보틱스 +0.85 / 금호타이어 +0.94 /
    삼기에너지 +0.91 / 삼기 +1.00 / 세아메카닉스 +0.91).
    🔴 08-13에 "**당장 효과는 거의 0**(08-11~13 발동 0건)"이라 적어둔 예측이
    **하루 만에 빗나갔다** — 0건이 아니라 **그날 청산 8건 중 5건**이었다.
    📌 교훈: '최근 N일 발동 0건'은 '앞으로도 안 돈다'가 아니다. 발동 빈도는
    장세에 달렸고, 08-14는 +2.5%를 찍고 되돌아오는 종목이 많은 날이었다.
  · **ⓐ 매수 주가 상한 10만원**: 10만원+ 매수 **0건**(상한 정상). 다만 그날
    후보 자체에 10만원+가 없어 **차단 로그로 확인된 건 아니다** — 판정 보류.
  · 진입 8건이 전부 1A / 개장초반 발사면제 5건 + 가속 경로 3건.
    물타기 3건(지투파워·삼기에너지·세아메카닉스) 정상 발동.

  [🟢 08-12 적용분도 실거래로 재확인] 다날이 **동적캡 50% 분할(52/104주)
  -> 26초 뒤 잔량 트레일(-3.12%)**로 정상 청산됐다. `PARTIAL_EXIT_REMAINDER_HOLD`가
  동적캡 재발동을 막고 트레일이 받은 것으로, 08-12 설계가 그대로 돌았다.
  ⚠️ DB는 가중평균으로 기록된다 — 52주 @5,380 + 52주 @5,280 = `sell_price 5,330`,
  `sell_quantity 104`. **부분매도 건은 DB 한 행에 합산되므로 분할 횟수를
  `trades`로 셀 수 없다**(로그를 봐야 한다 — 08-11 `entry_reason` 함정과 같은 계열).

  [적용 — 본전스톱 50% 분할 (커밋 `e139b0c`, **19:38 장마감 후**)]
  · 바닥에서 전량 대신 **절반만** 팔고 잔량은 3% 트레일에 맡긴다.
  · 근거는 위 5건의 종일 분봉 반사실(비관·낙관 두 가정 모두에서 최고).
  · 구조 논거: VI상단·동적캡·반등소진이 **이미 전부 50% 분할**인데
    **본전스톱만 전량**이었다 — 정합성 복원이다.
  · 🔴 `be_partial_done` 가드가 핵심 — 없으면 분할 후에도 순이익이 여전히
    바닥 아래라 **다음 틱에 잔량이 전량청산**되어 기능이 무효가 된다
    (08-12에 VI 상단에서 정확히 같은 결함을 실측으로 잡았다).
  · ⚠️ **적용이 19:38이라 08-14 그날은 구(舊) 전량 사양으로 돌았다.**
    실제로 위 5건 전부 `buy_quantity == sell_quantity`(전량)다.
    => **50% 분할이 처음 도는 날은 08-17(월)이다.**

- 2026-08-13 (목, **장중 재기동 + 매수 주가 상한 + 본전스톱 바닥 — 전부 사용자 지정**) —
  ⚠️ **스케줄러가 08:59에 안 돌아 개장 25분을 놓쳤다.** 09:14 수동 기동.

  [🔴 스케줄러가 하루를 통째로 건너뛴다 — 코드가 아니라 작업 설정 문제]
  · `LastRunTime` 08-12 / `NextRunTime` **08-14**로 08-13이 통째로 사라졌다.
    원인은 세 설정이다 — `StartWhenAvailable=False`(놓친 실행을 안 따라잡음) /
    `WakeToRun=False`(절전이면 안 깨움) / `DisallowStartIfOnBatteries=True`.
    사용자 확인: **자동업데이트 후 로그인 잠김 -> 절전** 상태였다.
  · -> 네 값을 전부 뒤집었다(앞의 셋 + `StopIfGoingOnBatteries=False`).
  · ⚠️ **완전히 해결된 건 아니다.** `LogonType`이 **Interactive**라 자동업데이트로
    **로그아웃/재부팅되어 로그인 화면**에 머물면 여전히 안 뜬다(화면만 잠긴
    상태는 정상 동작). 그 경우 `StartWhenAvailable=True` 덕에 **로그인하는 즉시**
    기동한다. S4U(로그온 없이 실행)로 바꾸면 무인화되지만 **원격제어 창이
    안 뜨고**(데스크톱 없음) claude 폴더 신뢰 프롬프트에서 멈춘다 — 안 건드렸다.
  · 🟢 **오버나이트 격리가 실거래에서 처음 정상 동작했다** —
    `🌙 오버나이트 보유 2종목을 manual로 격리`(자이에스앤디 80주 / JW신약 515주).

  [🔴 매수 주가 상한 100,000원 — 08-05 이후 105건 가격대 분해]
  ⚠️ '수동 매도 감지'(매수가로 대체된 손익 0)는 **가짜값이라 34건 제외**했다.
  | 가격대 | n | 평균 | 중앙값 | 손절률 | -2%이하 | 최악 |
  |---|---|---|---|---|---|---|
  | 1만미만 | 59 | +0.16% | +0.23% | 36% | 22% | -5.65% |
  | 1~2만 | 22 | -0.57% | -0.49% | 45% | 27% | -3.03% |
  | 2~5만 | 16 | -0.14% | +0.20% | 44% | 25% | -3.07% |
  | **10만+** | **8** | **-1.87%** | **-2.08%** | **62%** | **50%** | -3.93% |
  · **10만+만 중앙값이 마이너스**이고 -2% 이하 청산이 절반이다. 플러스는 1건뿐.
  · 🔴 **날짜 효과가 아니다** — 산 4일 **전부** 그날 평균보다 열위(4/4일).
  · 🔴 **기전: 비쌀수록 발사 게이트가 헐거워진다.** 진입 로그 전수 파싱 —
    '게이트가 사실상 없던 매수' 비율이 **가격과 단조 증가**:
    1만미만 26% / 1~2만 28% / 2~5만 32% / **10만+ 50%**.
    내역 — **주가계수 절단 3건**(clamp 상한 3.00이 **73,704원부터** 걸려
    139,700원은 이론계수 4.26이 3.00으로 잘려 문턱이 **30% 헐겁다**) /
    **가속도 4.00 수학적 상한 2건**(편입 직후 버퍼가 비는 아티팩트) / 면제 1건.
  · 구현: `_entry_change_reject` **단일 창구**에만 추가(그 함수가 "새 조건은
    호출부를 늘리지 말고 여기에만"이라 명시한 지점). 🔴 **전일종가 판정보다
    앞**에 뒀다 — 뒤면 전일종가를 모르는 종목에서 상한이 조용히 같이 죽는다
    (같은 주에 `_entry_delay_reject`에서 정확히 그 결합을 결함으로 잡았다).
  · ⚠️ 표본 8건 / 경계 10만원은 **데이터로 고른 값이 아니다**(5~10만 표본 0건).
  · ⚠️ **물타기는 bypass라 상한을 안 받는다** — 기존 10만+ 보유분은 -3%에서
    오히려 수량이 는다(2주->4주, 테스트로 기록). rescue-add는 받아 손절로 수렴.
  · 부수 관측(미조치): 트랜치 40만원이 양자화로 21~49% 미집행된다.

  [🔴 본전스톱 바닥 0.2% -> 1.0% — '본전'이 아니라 +1% 확보]
  · **실거래 발동 12건 전수**(시뮬 아님): 고점 순 평균 **+1.85%** -> 실현 순
    평균 **+0.07%** = **1.78%p 반납**. 실현 +1.0% 미만 **12/12건**, 마이너스 4건.
    (제주반도체 +2.09 -> **-0.08** / 포톤 +2.86 -> **-0.04**)
    => 바닥 +0.2%는 '본전'이 아니라 **본전 언저리 아무 데나**였다.
  · 🔴 **사용자가 처음 고른 조합(무장 2.0 + 바닥 1.0)은 검증이 기각했다.**
    실매수 59건 x 실제 틱 x **실물 on_price_update** 2차원 격자
    (결정론 확보 — 같은 설정 2회 실행 결과 동일 확인):
    | 무장\바닥 | 0.2% | 0.5% | 1.0% | 1.5% |
    |---|---|---|---|---|
    | 1.0% | +0.534 | +0.587 | — | — |
    | 1.5% | +0.589 | +0.556 | +0.191 | — |
    | 2.0% | +0.627 | +0.579 | +0.574 | +0.376 |
    | **2.5%(현행)** | +0.643 | +0.651 | **+0.698** | +0.650 |
    **무장을 낮추면 4개 열 전부에서 나빠진다**(2.5>2.0>1.5>1.0). 낮추면 더 많이
    무장하고 바닥이 **승자를 자른다** — 조건검색 +6.05->+1.00 / 서산 +4.39->+0.77 /
    다스코 +2.83->+0.64. 08-10에 문턱을 올린 근거가 1.0%p에서도 재현됐다.
    -> 사용자에게 보고 후 **문턱은 2.5% 유지, 바닥만 1.0%**로 확정.
  · 날짜별 3승 1패 4무 / 59건 중 **6건만** 달라진다.
  · ⚠️ **당장 효과는 거의 0이다** — 08-10에 문턱을 2.5%로 올린 뒤 본전스톱은
    **한 번도 발동하지 않았다**(08-11~13 청산 57건 중 **0건**, 순 +2.5% 도달은
    32건 중 2건). '발동할 때 더 좋은 가격에 나가게' 하는 보험이다.

  [🔎 검증 방법론 — 이번에 새로 확인한 것 3가지]
  · 🔴 **`test_replay_archive`는 실행마다 결과가 다르다**(하네스가 실시간 시계를
    쓴다). 동일 코드 2회에 익절캡 28/9/7 vs 26/13/8. **스윕에는 못 쓴다.**
    -> 별도 하네스를 만들되 **시계를 틱 타임스탬프에서 뽑고** stop_rate를
    고정해 결정론을 확보했다(2회 실행 결과 동일을 먼저 단언).
  · ⚠️ 그 하네스의 **실거래 재현율은 48%**다 — 틱 아카이브가 **09:00~09:15만**
    담아 대부분 창에서 잘리고, 결정론을 위해 동적캡·반등소진을 껐다.
    **A/B 비교로만 읽을 것.** 절대 수치는 현실이 아니다.
  · 🔴 **내 날짜별 집계가 한 번 거짓말했다** — 동점을 첫 항목 승리로 세어
    "현행이 5일 최고"가 나왔다. 실제로는 4일이 **완전 동일**이었다.
    **동점 처리를 명시하지 않은 승패 집계는 쓰지 말 것.**

  [🔎 문제 없음으로 확인한 것 — 다시 파헤치지 않도록]
  · VI 분할 잔량은 **본전스톱이 아니라 3% 트레일이 지킨다** — VI 발동 시점
    순이익이 실측 평균 +2.37%로 **무장 문턱(2.5%) 바로 아래**라 무장이 안 된다.
    설계대로이고, 트레일이 정상 청산함을 실물로 확인했다.
  · 청산 우선순위 교차: 무장 상태에서 손절선 관통 -> **손절이 이긴다**(전량) /
    순 +6.1% -> 익절캡 / 순 +1.0% -> 바닥. 충돌 없음.
  · 경계 실측: 무장은 순 **2.50%에서** True(2.49% False), 바닥은 순 **1.00%에서**
    청산(1.01% 보유). 부동소수 반올림으로 한 번 오탐이 났으나 코드는 정확했다.

  [검증] **18개 스위트 1,836건 전원 통과 / 실패 0**
  (297/89/133/70/165/24/145/187/68/176/51/91/103/62/47/61/45/22).
  · `test_patch_20260814.py` **45건 신설**(주가 상한 31 + 본전스톱 바닥 14).
  · 옛 사양(바닥 +0.2%)을 단언하던 **2건을 새 사양으로 교체**하고 롤백 검증을
    같이 넣었다(#4 132->133 / #8 144->145).
  · 감사 168 -> **176**: `주가상한`/`본전스톱무장`/`본전스톱바닥` 문서대조 +
    불변식 5건(바닥<무장<익절캡 / 바닥>0 / 상한>클램프시작가 / 상한<=트랜치1회분).
  · `import main` OK / .py **112개 구문오류 0** / 태스크 정의 21 == gather 21 /
    `ui/` -> core·main·api import **0건** / `_execute_buy` 호출 함수 **5곳** 불변 /
    매수금액 3곳 일치 / `IS_MOCK False` / 조건검색 `'K'`.

  [🔴 장마감 후 MAE 전수 측정 — 주가 상한의 '기전' 설명을 정정했다]
  분봉 REST로 매수 후 최저/최고를 전수 측정(봇 정지 후 실행, 토큰 충돌 없음):
  | 구분 | n | MAE평균 | MAE중앙 | MFE평균 | **MFE/MAE** | MFE 3%+ |
  |---|---|---|---|---|---|---|
  | **10만원+** | 12 | **-5.03%** | -5.00% | **+2.28%** | **0.45** | 25% |
  | 2만원 미만(무작위) | 25 | **-4.87%** | -4.51% | **+6.74%** | **1.38** | 64% |
  · 🔴 **하락폭은 거의 같다**(차이 0.16%p). "비싼 종목이 더 떨어진다"는 **사실이
    아니었다** — 저가주도 76%가 -2% 이하로 밀린다(10만+는 83%).
    사용자의 최초 관찰("10만원+는 2% 이상 떨어진다")은 맞지만 그건 **우리가 사는
    거의 모든 종목의 특성**이지 가격대 특성이 아니다.
  · 🔴 **진짜 차이는 상승폭이다 — 3배**(+2.28% vs +6.74%). 같은 만큼 밀리는데
    **되돌아오지 못한다.** MFE 3%+ 도달이 25% vs 64%, 최대 MFE가 +7.34% vs +20.88%.
  · => 상한의 근거는 '낙폭'이 아니라 **손익비(0.45 vs 1.38)**다. 손절을 넓히거나
    보유를 늘리는 방향으로는 해결되지 않는다(올라오질 않는다).
  · ⚠️ `KiwoomREST(token)`은 `is_mock` 기본값이 **True**다 — 실전 토큰으로 모의
    서버를 때린다. 반드시 `is_mock=settings.IS_MOCK`을 명시할 것(문서화된 지뢰).

  [🔴 로그 로테이션 함정 — 하마터면 틀린 결론을 낼 뻔했다]
  `logs/autotrader.log`만 grep해서 "VI 분할 0건 / 수동 추가매수 0건"이라는
  **틀린 결론**을 냈다. 실제로는 그날 11:49에 로테이션돼 오전 기록이 `.log.1`에
  있었고, 전부 훑으니 **VI 분할 3종목 / 수동 추가매수 1종목**이 정상 동작했다.
  **-> 장중 검증은 반드시 `logs/autotrader.log*`(로테이션 포함)로 볼 것.**

- 2026-08-12 (**심야, 적용분 재검증 — 🔴 결함 5건 + 공허한 검사 1건**) —
  "오늘 수정이 꽤 있으니 매수매도에 오류가 절대 없게 촘촘히 검증" 요청.
  **스위트 1,762건이 전원 통과하는 상태에서 결함 5건이 나왔다.** 전부 그날
  장마감 후에 넣은 ⑫⑬⑭에 딸린 것이고, **3건은 매수·매도 동작 자체**다.

  [🔴 결함 ① VI 50% 분할이 **다음 틱에 잔량을 전량 청산**했다 — 기능이 무효]
  ```
  if (VI_UPPER_EXIT_PARTIAL and ... and not pos.get("partial_exited") ...):
      ... 절반 매도 ...; return
  self._execute_sell(stock_code, current_price, _reason)   # ← 전량
  ```
  · 절반을 팔면 `partial_exited`가 True가 되므로, **바로 다음 틱**에 가드가
    풀려 전량 매도 분기로 떨어진다. VI 밴드는 **0.5% 또는 2호가**라 상승 중
    종목은 그 안에 여러 틱 머문다 -> 실질적으로 **1틱짜리 기능**이었다.
  · 실물 재현(매수 10,700 / 시가 10,000 / VI 11,000 / 현재가 10,970):
        틱1 매도 50주·잔량 50  ->  **틱2 매도 50주·청산 완료**
  · ⚠️ 처음엔 매수가를 시가와 같게 놓고 재현해 **익절캡(6%)이 먼저 잡아** 원인을
    잘못 볼 뻔했다. VI 상단은 시가 x1.10이라, 시가 근처에서 사면 VI 도달 시
    순 +9.5%로 캡을 한참 넘는다. **실전형은 시가 +7% 부근 매수**다(그때 VI에서
    순 +2.4%로 캡 아래 = 실측 평균 실현 +2.37%와 일치).
  · 수정: 분할이 **실제로 나갔을 때만** `vi_partial_done`을 찍고, 그 표식이
    있으면 VI 분기에서 팔지 않고 **아래 사다리(본전스톱/익절캡/트레일/손절)로
    넘긴다.** 🔴 주문이 실패하면 표식을 안 찍으므로 다음 틱에 정상 재시도된다.
  · **의도된 동작은 보존했다** — 동적캡·반등소진이 **이미** 분할한 포지션은
    종전대로 VI에서 전량 정리한다(상수 주석이 명시한 설계).
  · 검증: 같은 가격 4틱에도 잔량 50주 유지 / 트레일(-3%)로 정상 청산 /
    손절 정상 발동 / 롤백(False)이면 연속 틱에서도 전량 1회.

  [🔴 결함 ② 진입 유효창이 **되돌림 2차 트랜치를 막아 반쪽 포지션**을 만든다]
  · 계획이 29분 50초에 열리고 1차가 체결된 뒤, 2차가 30분을 넘겨 도달하면
    `_execute_buy`의 하드가드가 `진입 유효창 만료`로 **2차만 차단**한다.
    실물 재현: 트랜치 `[50]` 1건만 체결 = **의도 금액의 절반만** 산다.
  · 🔴 08-08 「VWAP 사각지대」와 **정확히 같은 계열**이다 — 계획은 열렸는데
    체결 시점 판정이 달라 반쪽이 된다. 그때 원칙으로 승격해 둔
    *"게이트를 사전 검사할 땐 검사 시점과 체결 시점이 같은지 볼 것"*이
    시간 축에서 재발한 것이다.

  [🔴 결함 ③ 진입 유효창이 **rescue-add를 조용히 무력화**했다]
  · `_do_rescue_add`는 `bypass_entry_gates`를 쓰지 않으므로 유효창을 그대로
    받는다. 실물 호출 결과 — 편입 후 **10분: 매수 1건(보유 100->204)** /
    **40분: 매수 0건**. 즉 편입 30분 뒤엔 '손절 대신 추가매수'가 **원리적으로
    발동 불가**가 된다.
  · 🔴 08-07 `[C] 등락률 하한 x rescue`, 08-08 `VWAP x rescue`에 이어 **세 번째**
    같은 실패 모드다. 안전 방향(손절로 수렴)이라 사고는 아니지만, 이번엔
    **문서에 기록조차 없었다**.
  · ②③ 공통 수정: `_entry_delay_reject(..., initiating=)` 신설.
    `_execute_buy`가 **`initiating=not add_to_position`**을 넘긴다 —
    유효창 **상한**은 '새 포지션을 여는 매수'에만 건다.
    🔴 근거와 범위가 일치한다: 상한의 검증(8일 104건)은 **편입->진입 지연**을
    잰 것이지 '보유분에 더 사는 시점'을 잰 것이 아니다.
    숙성(하한)은 `initiating`과 무관하게 그대로 본다(시간은 늘기만 하므로
    추가매수를 막지 않고 하드가드 성격은 유지된다).
  · 회귀 확인: **새 진입은 여전히 30분에 차단**된다(계획 생성 0건).

  [🔴 결함 ④ 숙성을 롤백하면 유효창 상한이 **같이 죽는다**]
  · 유효창 검사가 `need <= 0`(숙성 미사용)의 **조기 return 뒤**에 있었다.
    즉 `MIN_ENTRY_DELAY_SEC = 0`으로 되돌리면 유효창까지 조용히 무효가 된다.
    롤백표는 두 항목을 **별개**로 적고 있으므로 문서와 코드가 어긋나는 결합이다.
  · 수정: 상한 검사를 `need` 판정 **밖으로** 꺼냈다. 실측 확인 — 숙성 0초 +
    편입 31분에서 `진입 유효창 만료`가 정상 발생한다.

  [🔴 결함 ⑤ `진입 유효창 만료`가 진단에서 **"기타"로 뭉개졌다**]
  · 사유 **문자열**은 숙성과 분리했는데 `_REJECT_RULES`에 규칙을 안 넣어
    분류가 `기타`였다. 그러면 장중 체크리스트 **N1**("유효창 만료가 하루
    2~3건인가")을 **원리적으로 판정할 수 없다.**
  · 수정: `("진입 유효창 만료(지연 진입)", ("진입 유효창",))` 추가
    (`매수 컷오프`보다 앞 — 성격이 완전히 다르다).

  [🔴 그리고 검사 하나가 **공허했다** — 이 프로젝트 반복 사고 1위]
  · `test_patch_20260813` [4]의 핵심 단언("분할 후 잔량은 동적캡으로 청산되지
    않는다")이 **틱·거래량을 하나도 안 준 채 `check_timeouts()`만** 불렀다.
    그러면 `_update_dynamic_caps`가 강도 중립값에서 곧바로 `continue` 해
    **동적캡 경로에 도달조차 하지 않는다.**
  · **A/B로 증명**: `PARTIAL_EXIT_REMAINDER_HOLD`를 False로 되돌려도 결과가
    똑같았다(둘 다 매도 0건) — 즉 **수정을 통째로 지워도 통과하는 검사**였다.
  · 🟢 다만 **기능 자체는 정상**이었다. 실동작 시나리오로 다시 재니
    ON이면 잔량 보존 / OFF면 잔량 청산으로 **정확히 갈린다**.
  · 원인이 스텁이었다 — 이 스위트의 `_Rest.get_minute_candles`가 `[]`를
    돌려줘 `vol_ratio`가 None이 되고 거기서 끊긴다. 08-10의
    `update_sell` 스텁 사고와 같은 계열(**스텁이 실물과 달라 감사가 거짓말**).
  · -> 대조군("이 시나리오에서 동적캡이 **실제로** 발동하는가")과
    **A/B가 갈리는지**를 같이 단언하도록 다시 짰다.

  [🔎 같이 확인하고 '문제 없음'으로 판정한 것 — 다시 파헤치지 않도록]
  · **`elif` -> `if` 수정(⑪)은 정확하다** — 세 분기(`server_qty>0` 표식 /
    `>tracked` 추가매수 / `0<server_qty<tracked` 일부매도)가 구조상 배타적이라
    이중 처리가 없다. JW신약 시나리오(515->1032주)가 실물 함수로 재현된다.
  · **물타기(`_maybe_average_down`)는 안전하다** — `bypass_entry_gates=True`라
    유효창 상한을 애초에 안 받는다(rescue-add와 갈린 지점).
  · **`test_replay_archive`는 실행마다 결과가 다르다** — 동일 코드로 두 번
    돌려 익절캡 `28/9/7` vs `26/13/8`이 나왔다(하네스가 실시간 시계를 쓰므로
    실행 시간에 따라 '최근 창'이 달라진다). 🔴 **#15는 건수의 절대값이 아니라
    불변식 위반 여부만 읽을 것** — 문서가 이미 그렇게 적고 있고, 실제로
    불변식은 두 번 다 22/22 통과했다.
  · 상수 전 항목이 CLAUDE.md '기대값' 블록과 **일치**(유효창 1800 / VI분할 True /
    잔량보호 True / 앵커 False / 캡 0.06·0.06·0.06·0.08 / 손절 -0.045 /
    물타기 11:00·3회·5초 / 강제청산 False / 오버나이트격리 True).
  · 🔴 코드 주석의 `_execute_buy` 호출부가 아직 **"4곳"**이었다(물타기 신설 후
    문서만 5곳으로 고쳐졌다) — **두 번째 뒤처짐**이라 같이 정정했다.

  [검증] **17개 스위트 1,781건 전원 통과 / 실패 0**
  (297/89/132/70/165/24/144/187/68/168/51/91/103/62/47/**61**/22).
  · `test_patch_20260813` **42 -> 61건** — 결함 5건의 회귀방지 + 공허했던 검사
    재작성 + **롤백 경로**를 전부 넣었다.
  · `import main` OK / .py **111개 구문오류 0** / 태스크 정의 **21 == gather 21** /
    `ui/`가 core·main·api를 import **0건**(격리 유지) /
    스케줄러 `AutoTrader_Start` **Ready NextRun 08-13 08:59** /
    `PremarketScan` Disabled / `STOP_SIGNAL`·`NEW_SESSION_REQUESTED` 없음.
  · DB 미청산 = **`holding` 2건 + `manual` 3건**으로 문서와 정확히 일치
    (자이에스앤디 80주 @8,968 / JW신약 515주 @2,413 — 둘 다 `buy_time`이
     08-12라 08-13 기동 시 정상 격리된다).

- 2026-08-12 (장마감 후, **진입/청산 5건 개편 — 🔴 결함 1건 + 8일치 분해분석**) —
  "분봉 고점에서 매수하는 걸 막을 방법"을 찾다가 **진입 쪽 세 갈래가 전부
  막히고**, 청산 쪽에서 더 큰 개선 여지가 나왔다. 실거래 8일(08-05~08-12)을
  편입 로그 + 분봉으로 재구성해 분해했다.

  [🔴 결함 — 08-11에 만든 '수동 추가매수 합산'이 한 번도 안 돌았다]
  ```python
  if server_qty > 0:
      pos["seen_on_server"] = True
  elif server_qty > tracked_qty:      # ← 도달 불가
  ```
  `server_qty > 0`이면 첫 분기에서 끝나고, `0`이면 `0 > tracked`도 거짓이다.
  **어느 쪽으로도 실행되지 않는 죽은 코드**였다(`elif` → `if` 한 글자).
  · 실측: 08-12 JW신약 — 봇 515주 vs 실계좌 1,032주. 사용자가 09:44/09:54에
    직접 산 517주를 봇이 모른 채 방치했다(봇이 팔면 515주만 팔린다).
  · ⚠️ test_patch_20260811 [5]가 이걸 통과시켰다 — 로직을 **직접 호출로**
    검증하지 않았기 때문이다. 이번엔 실물 `_reconcile_manual_sells`에
    JW신약 시나리오를 그대로 태워 못박았다.

  [🔴 진입 쪽 세 갈래가 전부 막혔다 — 데이터가 게이트를 옹호한다]
  | 시도 | 결과 |
  |---|---|
  | 되돌림을 깊게(rally 비례) | ❌ 체결 17→8건. **놓친 8건 MFE +7.26% > 잡은 건 +5.88%** |
  | 숙성 단축 | ❌ **08-11↔08-12 방향이 정반대**(짧을수록/길수록). 판정 불가 |
  | 게이트 완화 | ❌ 진짜 놓친 9종목이 **+2.36% / -3.07%**로 손익비가 나쁘다 |
  · 🔴 **탈락 ≠ 놓침**이다. 게이트에 걸린 21종목 중 **12개(57%)는 나중에
    결국 샀다**(숙성은 '그 트리거를 버린다'이지 종목 배제가 아니다).
    이 둘을 섞으면 게이트의 기회비용이 과대평가된다.
  · VWAP이 가장 잘 거른다 — VWAP 탈락 4건 최고평균 +1.60%, 최저 -3.96~-5.67%.

  [🔴 대신 찾은 것 — '언제/무엇을'이 아니라 '얼마나 늦게'가 문제였다]
  편입→매수 지연으로 104건을 갈랐더니 단조 관계가 나왔다:
  ```
  ~1분  n=23 MFE +4.50%   1~3분 n=27 MFE +7.00%(최고)
  10~30분 n=23 +2.59%     30분~ n=14 **+1.79%, 절반이 1% 미만**
  corr(log10 지연, MFE) = -0.291 / 30분 컷 **5/5일 우세**
  ```
  · 대표: JW신약 편입 09:01 → 매수 09:39(**38분**), MFE -0.12%로 사자마자
    밀려 사용자가 수동 추가매수까지 하게 됐다. **예외가 아니라 패턴**이다.
  · => **조건검색식을 고칠 문제가 아니다.** 편입 직후 1~3분이 가장 좋다 =
    조건식은 좋은 자리를 잡아준다. 봇이 늦게 사는 쪽이 문제다.
    숙성(하한)의 반대편에 **상한**을 두는 것으로 끝난다(`MAX_ENTRY_AGE_SEC`).

  [🔴 청산이 진짜 문제라는 것이 8일치로 재확인됐다]
  > **판 자리에서 종가까지 평균 +5.96% 더 갔다**(전체 실현 -0.23%, n=100)

  | 사유 | n | 실현 | 판 뒤 30분 | 종가까지 | +3%↑ |
  |---|---|---|---|---|---|
  | **VI 상단** | 9 | +2.37% | **+5.67%** | +8.85% | **78%** |
  | 동적캡 | 12 | +1.67% | +2.93% | +5.24% | 50% |
  | 손절 | 43 | -2.18% | +2.65% | +6.11% | 35% |
  | 정체/시간정리/트레일 | 11 | — | +0.5~1.7% | — | **0%** |
  · 아래 세 규칙은 **잘 판다**(판 뒤 안 오른다). 문제는 VI·동적캡이다.
  · 🔴 **분할 잔량이 44~55초 만에 털리고 있었다** — 분할 1차 후
    `partial_exited`가 True면 분할 분기를 못 넘어 전량 경로로 떨어진다.
    08-04에 "절반은 추세를 남긴다"고 만든 설계가 무력화된 상태였다.

  [적용 5건]
  | # | 변경 | 근거 강도 |
  |---|---|---|
  | ⑪ | 수동 추가매수 `elif`→`if` | **결함** |
  | ⑫ | 진입 유효창 30분(`MAX_ENTRY_AGE_SEC`) | **강** (5/5일) |
  | ⑬ | VI 상단 50% 분할 | 중 (n=9, 78%) |
  | ⑭ | 분할 잔량 보호 | 정합성(설계 복원) |
  | ⑮ | 편입가 앵커(2차만) | **약** (+0.08%p) |

  [🔴 하네스 함정을 또 밟았다 — 이번엔 '반대쪽 경계']
  `test_replay_archive`가 `_first_seen`을 **-999초**로 밀어뒀는데(숙성 통과
  목적), 유효창 상한이 생기자 **전 종목이 '유효창 만료'로 차단돼 매수 0건**이
  됐다. 08-08에 "가상 epoch를 쓰면 숙성 미달로 매수 0건"을 겪고 적어둔 것과
  **정확히 대칭**인 실패다. -> 종목마다 **자기 첫 틱 120초 전**으로 잡아
  실거래와 같은 형태로 고쳤다.
  **교훈: 시간 게이트에 한쪽 경계를 추가하면, 반대 경계를 흉내내던 하네스가
  전부 깨진다. 리플레이는 '충분히 과거'가 아니라 '실거래와 같은 위치'로.**

  [검증] **17개 스위트 1,762건 전원 통과 / 실패 0**.
  · 🆕 `test_patch_20260813.py` **41건** — 5건의 경계·회귀방지·**롤백 5종**.
    특히 ⑫는 `prearm_candidate`를 두 번 불러 `_first_seen`이 갱신되지 않음
    (첫 편입 기준)을 못박았다 — **분석 전제와 코드 기준이 같아야** 결론이 유효하다.
  · 옛 사양(VI 전량매도)을 단언하던 **7건을 새 사양으로 교체**하고 롤백 검증을
    같이 넣었다(`test_patch_20260805` 143→144, `audit` 162→168).
    ⚠️ VI가 분할이면 `update_sell`을 안 불러 사유가 DB에 안 남는다 —
    '우선순위 매트릭스'처럼 **사유로 판정하는 곳은 분할을 끄고** 재게 했다.

- 2026-08-12 (새벽, **기동 전 전면 검증 — 🔴 결함 2건 + 당일청산 전제 폐지**) —
  "수정변경된 부분이 꽤 있으니 매수매도에 오류가 절대 없게 촘촘히 검증"
  요청. **스위트 1,660건이 전원 통과하는 상태에서 결함 2건이 나왔다.**
  둘 다 **물타기(08-11 신설)가 만든 새 매수 경로**에 있었다.

  [🔴 결함 ① 물타기 주문이 거부되면 매 체결틱마다 재시도한다]
  · `_maybe_average_down`은 미체결이면 `avg_down_done`을 안 찍고 return한다.
    되돌림 계획엔 120초 타임아웃이, rescue-add엔 '실패 시 손절로 수렴'이
    있는데 **물타기만 빠져나갈 구멍이 없었다.**
  · **실측: 50틱을 흘리면 주문을 50회 낸다.** 실전 체결틱은 초당 수십 건이라
    예수금 부족처럼 **지속되는 거부**에서는 키움 REST가 429로 막히고,
    그러면 **매도 주문까지 밀린다** — 매수 실패가 매도 사고로 번지는 형태다.
  · ⚠️ 문서는 이미 "6슬롯이 다 물리면 예수금의 82%… 추가 매수는 주문 거부가
    난다"고 적어 두고 있었다. **거부가 난다는 건 알았는데 그 거부가 폭주를
    유발한다는 걸 몰랐다.**
  · -> `AVG_DOWN_MAX_RETRY = 3` + `AVG_DOWN_RETRY_COOLDOWN_SEC = 5.0`.
    3회 실패하면 포기하고 **손절·최종선이 그대로 받는다**(포기해도 손절이
    정상 발동함을 테스트로 못박았다). 실측 50틱 -> **3회**.

  [🔴 결함 ② 물타기에 시간 컷오프가 없어 15:09:59에도 발동한다]
  · 실측: 10:00 / 14:55 / **15:09 전부 발동**, 15:10(하드컷오프)에서만 차단.
  · 그 시점의 물타기는 **반등할 시간이 원리적으로 없다.** 15:10 강제청산이
    살아 있던 시절엔 87만원어치를 사고 1분 뒤 시장가로 팔았다 —
    왕복 수수료(0.23%)만 확정하는 거래다.
  · -> 사용자 지정으로 **`AVG_DOWN_CUTOFF = 11:00`**.

  [🔴 사용자 지정 — 당일청산 폐지. 이 봇의 전제가 바뀌었다]
  "물타기는 11시까지만, **15:10 강제청산은 없는걸로 — 내가 수동매도하던지
  오버나이트 할 수 있게**". 이어서 "복원분은 manual 격리 / 지수가드 14:50도
  같이 끔 — 어떤 경우도 자동청산 없음".
  | 상수 | 값 | 효과 |
  |---|---|---|
  | `FORCE_CLOSE_ENABLED` | **False** | 15:10 전량 청산 폐지 |
  | `INDEX_GUARD_FORCE_CLOSE_ENABLED` | **False** | 지수 -5%날 14:50 청산도 폐지 |
  | `OVERNIGHT_RESTORE_AS_MANUAL` | **True** | 넘어간 보유분을 다음 기동 때 격리 |
  => **시간 기반 자동청산이 하나도 없다.** 남은 청산은 전부 가격 기반이다.

  [🔴 그냥 끄면 안 되는 것이 3가지 있었다 — 전부 같이 고쳤다]
  ① **봇이 안 내려간다.** `task_auto_shutdown`이 `_force_close_done`을 종료
     조건으로 보는데, 워처가 그냥 return하면 이 값이 영영 False라 15:40
     하드 폴백까지 정기보고·WS재연결이 계속 돈다.
     -> 끌 때 **즉시 `_force_close_done = True`**로 세운다.
  ② **장중 재시작이 깨진다.** 복원분을 날짜 구분 없이 격리하면 08-10처럼
     장중에 재기동했을 때 **당일 보유분까지 봇 관리에서 빠진다**(그날은
     09:59 정지 -> 10:02 재기동을 실제로 했다).
     -> 판별을 **`buy_time`의 날짜**로 했다. 전일 이전만 격리한다.
  ③ **1단계 본전청산까지 죽이면 안 된다.** 지수가드 11:30 이전 규칙은
     '본전 이상이면 정리'라 **익절 계열이고 손실을 확정하지 않는다.**
     -> 2단계(14:50 강제청산)만 껐다. 1단계 생존을 테스트로 못박았다.

  [⚠️ 이 변경이 여는 새 리스크 — 반드시 같이 기억할 것]
  · **갭 하락은 손절로 못 막는다.** 손절선(-4.5%)은 장중 틱에서만 판정된다.
    전날 -4%로 넘긴 종목이 다음날 -10% 갭이면 그 가격이 그대로 손실이다.
  · **격리분이 매일 쌓인다.** 6슬롯 x 90만 = 540만원(물타기 시 1,080만)이
    하루치다. 사용자가 정리하지 않으면 예수금이 잠겨 **신규 매수가 막히는
    것이 첫 증상**이다. `python manual_position_tool.py list`로 매일 볼 것.
  · **MDD 일손실 차단이 오버나이트 손실을 못 본다** — `_daily_realized`는
    당일 **실현**손익 기준이라 미실현은 안 잡힌다.
  · **격리분은 손절·익절 대상이 아니다**(사용자 지정). 봇은 쳐다보지 않는다.

  [검증 — 전부 실행해서 맞춘 값이다]
  · **16개 스위트 1,713건 전원 통과 / 실패 0**
    (297/89/132/70/165/24/143/187/68/162/51/91/103/22/62/**47**).
  · 🆕 `test_patch_20260812.py` **47건 신설** — 컷오프 경계 7종 / **15:09
    회귀방지** / 50틱->3회 / 쿨다운 / **포기해도 손절 정상** / 자동종료
    비차단 / 15:30에도 손절·익절 생존 / 1단계 본전청산 생존 / 당일분 복원 /
    격리분 슬롯 미점유 / **롤백 경로 4종**.
  · `test_patch_20260803.py` 131 -> **132**: 옛 사양(14:50 강제청산)을 단언하던
    3건을 새 사양으로 교체하고 **롤백 검증을 같이 넣었다** — 끈 기능의 배선
    테스트를 지우면 되살릴 때 검증이 없어 그대로 사고가 난다(08-10 교훈).
  · 감사 157 -> **162**: [9] 문서↔코드 대조에 `물타기컷오프` / `강제청산` /
    `지수가드강제청산` / `오버나이트격리` + **시간청산 전멸 불변식**을 추가했다.
    ⚠️ 08-11에 "새 상수를 넣으면 이 블록에도 반드시 추가할 것"이라 적어둔
    바로 그 자리다.
  · 인프라: `import main` OK / .py 97개 구문오류 **0** / 태스크 정의 21 ==
    gather 21 / DB 미청산 `holding` **0건**(manual 3건은 설계대로) /
    스케줄러 `AutoTrader_Start` Ready **NextRun 08-12 08:59** / junction 정상 /
    `.bat` 비ASCII 0바이트 / `ui/`가 core·main·api를 import **0건**(격리 유지).
  · `daily_backtest` **실계정 실행**(텔레그램 미전송): 새벽이라 유니버스
    0종목 — 토큰 정상, `is_mock=False` 명시, 예외 0.

  [🟢 결함이 아니라고 확인한 것들 — 다음 세션이 다시 파헤치지 않도록]
  · 물타기 -> 최종선(원가 -6%) 체인: 변동성 손절이 깊을 때(-5.5%) 평단
    손절선이 원가 -6.92%라 **최종선이 먼저 온다**. 실측으로 정확히 그렇게
    돌았다(청산 수량 180주, 사유 `구조 후 최종손절`).
  · 물타기 후 rescue-add **이중 발동 없음**: 평단 손절선(원가 -5.93%)이
    관찰바닥(-5.5%)보다 아래라 `_rescue_gate`가 즉시 'sell'로 수렴한다.
  · 종목당 200만 캡: 190만 보유분에 물타기하면 **10주만** 사서 총 1,997,000원.
  · 물타기 x MDD/지수가드: 존중하고 `avg_down_done`도 소모하지 않는다(회복 후 재시도).
  · 잔고 대조가 물타기 직후를 "일부 수동 매도"로 볼 수 있으나 **수량을 서버에
    맞추는 자기교정 방향**이라 안전하다(유령 방지).

- 2026-08-11 (저녁, **대시보드 Phase 0 신설 — 매매 코드 변경 0줄**) —
  "UI를 쓰면서 전략 수정도 가능하게" 라는 요청에서 출발해, **관측 전용
  대시보드만** 먼저 만들었다. 전략 변경(Phase 1)은 설계만 하고 넣지 않았다.

  [🔴 가장 중요한 발견 — 이 코드베이스는 '실행 중 전략 변경'이 거의 공짜다]
  `strategy_manager.py`가 상수를 **메서드 안에서 모듈 전역으로** 읽는다
  (`if AVG_DOWN_ENABLED and ...`:6232 / `return STOP_LOSS_RATE`:2222).
  파이썬 전역 조회는 **호출 시점에 동적**이므로 `setattr(module, name, value)`가
  **재시작 없이 다음 판정부터 먹는다.** `self.X = X`로 `__init__`에 박아둔 게
  하나도 없어서 가능하다.
  -> 즉 08-10에 "상수는 기동 시 로드되므로"라며 09:59 정지 → 10:02 재기동한
     그 작업은 **구조적으로는 재시작이 필요 없던 일**이었다.
  ⚠️ 단 예외 2가지가 Phase 1 설계를 지배한다:
    ① **파생 상수** — `TAKE_PROFIT_CAP = EXIT_POLICY["default"][...]`(:1076)라
       딕셔너리를 고쳐도 안 바뀐다. 화이트리스트는 *실제로 읽히는 잎*을 겨냥할 것.
    ② **포지션에 박히는 값** — `pos["stop_rate"]`는 매수 시점 1회 계산이라
       손절 상수를 바꿔도 **보유 중인 종목엔 안 먹는다**(신규 매수분부터).

  [구조 — UI는 봇 메모리를 절대 직접 만지지 않는다]
  · `ui/`가 **`core/`·`main.py`를 import하지 않는다.** 그래서 UI를 아무리 고쳐도
    스위트 1,660건에 영향이 없고 **장중에도 UI만은 안전하게 수정할 수 있다.**
  · 봇과 **별도 프로세스** + DB 커넥션 `readonly=True` + **루프백 전용 바인딩**.
  · Phase 1을 할 때도 UI가 직접 `setattr` 하면 안 된다 — `strategy_overrides`
    테이블에 **요청만** 쓰고, 봇의 `task_config_watch`가 `apply_overrides()`
    **단일 관문**으로 적용한다(이 코드베이스가 이미 쓰는 `_entry_block_reason` /
    `_entry_change_reject` 단일 창구 패턴과 같은 이유). 관문의 불변식은
    **새로 짜지 말고** 감사 파일에 있는 것을 재사용할 것.

  [🔴 데이터 함정 3가지 — 대시보드 만들다 발견. 분석할 때도 그대로 적용된다]
  ① **타임존이 테이블마다 다르다.** `system_events.timestamp`는 **UTC**,
     `trades.buy_time/sell_time`은 **KST**다(DB 세션 TZ가 UTC).
     실측: 뉴엔AI 손절이 trades엔 `13:51:44`, system_events엔 `04:51:44`.
     **한 화면·한 쿼리에 섞으면 9시간 어긋난다.** 날짜 필터도 UTC 경계로
     걸어야 KST 하루가 온전히 잡힌다(그냥 자르면 오전 9시 이전이 전날로 샌다).
  ② **추가매수는 `entry_reason`을 덮지 않는다.** 실측: 뉴엔AI가 17주 매수 ->
     rescue-add 17주 -> 총 34주가 됐는데 `entry_reason`엔 최초 진입 문구만 있고
     `entry_note`("손절 대신 추가매수")는 안 들어간다.
     -> **물타기 발동 건수를 `trades`로 셀 수 없다.** 로그(`💧 물타기`,
        `strategy_manager.py:6638`)나 `system_events.BUY_ADD`를 봐야 한다.
        08-12에 물타기를 채점할 때 이걸 모르면 "0건"이라는 가짜 결론이 난다.
  ③ **`watch_list_log`의 가격 컬럼이 전부 NULL이다** — `surge_rate` /
     `open_price` / `today_open` / `high_pct` 전부(08-11 실측).
     -> 「장중에 볼 것」 0번의 "돌파전 편입 종목 등락률 8% 이내"는 **원리적으로
        계산할 수 없다.** 대신 **조건식 이름 자체**(`cond_name`에 `돌파전`이
        나타나고 `돌파자동매매용`이 사라졌는가)를 본다 — 옛 이름이 보이는 것이
        곧 "새 조건식이 구독에 안 붙었다"는 증거라 오히려 더 직접적이다.

  [기능] 「🔬 장중 관찰 체크리스트」가 이 문서의 순위 0~10을 자동 판정한다
  (`ok/warn/bad/**na**`). 그 외 오늘 요약(실현손익·승률·**평균 보유시간**) /
  보유 현황(`manual`은 '봇 밖'으로 구분) / 청산 사유 분포 / 매매 내역 /
  조건식별 편입·매수 / 미매수 사유 / 시스템 이벤트(KST 변환) / 로그 tail.
  · 🔴 **`na`(판정 불가)를 성실히 쓰는 것이 핵심이다.** 데이터가 없는데 초록불을
    켜면 "괜찮다"고 거짓말을 하고, 그건 표가 없는 것보다 나쁘다. 08-09·08-10에
    스텁이 실물과 달라 감사가 '통과'로 거짓말한 사고가 연달아 있었다.
  · 승률 분모는 `청산 건수`가 아니라 **wins+losses**다 — 손익 0원 건(수동매도
    체결가 미상·미체결 정리)이 08-11에만 10건이라 청산 건수로 나누면 27.6%가
    나와 실제 42.1%를 크게 왜곡한다. 그 건수는 `무손익 N`으로 같이 표시한다.

  [🔴 같은 날 발견·수정한 결함 — 매매일지로 쓰려다 드러났다]
  체크리스트의 **로그 기반 항목(1·2·6번)이 날짜를 무시**하고 있었다.
  `log_count`가 조회 날짜와 무관하게 **'현재 로그 꼬리'**를 셌다 — 08-06을 열어도
  오늘 숫자가 그날 것처럼 표시된다. 지금은 물타기가 0건이라 티가 안 났지만,
  **08-12에 물타기가 5건 돌면 08-06을 열어도 "5건"으로 뜬다.** 매매일지로
  쓰는 순간 정확히 틀린 정보를 주는 종류의 결함이다(이 프로젝트가 반복 경고해 온
  "구간별 집계는 날짜로 쪼개 볼 것"과 같은 계열).
  -> 날짜 접두사(`[YYYY-MM-DD `)로 필터하고 **로테이션분(`.log.1`, `.log.2`…)까지**
     훑도록 고쳤다. 그 날짜 줄이 하나도 없으면 **`None`을 돌려 `na`로 흘린다** —
     0으로 뭉개면 "물타기 0건"이라는 **거짓 초록불**이 켜진다.

  [매매일지 자동저장 — `daily_journal.py`, 별도 프로세스]
  · DB는 매매 기록을 영구 보관하지만 **로그에만 남는 것**(물타기 발동·수동
    추가매수 감지)이 있고 로그는 10MB에서 로테이션된다. -> 장 마감 후 굳힌다.
  · 백필 11일 실행 확인. 과거로 갈수록 `판정 불가`가 는다(08-11 1건 -> 07-30 5건)
    — 로그가 사라진 것이고 **그게 정확한 상태다.**
  · 대시보드는 과거 날짜면 **스냅샷 우선**, 오늘은 항상 실시간.
  · ⚠️ **스케줄러 등록은 안 했다**(사용자 확인 대기). 명령은 `ui/README.md`.

  [검증] 08-11 실데이터로 전 패널 동작 확인(청산 29건 / -86,912원 / 승률 42.1% /
  평균 보유 9.53분). 체크리스트가 **0번·3번을 `이상`으로 정확히 잡았다** —
  그날은 옛 조건식·손절 -3%로 돌았으므로 맞는 판정이다(**08-12부터 정상이어야
  한다**). 서버 에러 0 / 콘솔 에러 0.
  스위트 영향 없음 확인(165 / 103 / 157 재실행, 전원 통과).
  `run_dashboard.bat` 비ASCII **0바이트** + junction 경로 사용(CLAUDE.md 규칙 준수).

- 2026-08-10 (저녁, **08-10 변경분 전면 재검증 — 🔴 검증 인프라 결함 2건, 매매 결함 0건**) —
  "오늘 수정이 많으니 매수매도에 오류가 절대 없게 촘촘히 검증해달라"는 요청.
  **매매 로직 결함은 0건이었다.** 상수 전 항목이 문서 기대값과 일치했고, 경계값을
  실물 청산 함수에 태워도 전부 정확했다. 나온 결함은 **둘 다 '검증이 거짓말할 수
  있는 구조'**다 — 이 프로젝트가 반복해서 당하는 부류다.

  [🔴 결함 ① 스텁 시그니처 불일치가 11개 스위트에 남아 있었다]
  · 08-10 낮에 이 함정을 밟고 **audit_deep과 리플레이 하네스만** 고쳤는데,
    나머지 **11개 스위트에는 옛 스텁 `update_sell(cls, **kw)`가 그대로** 있었다.
  · 실물은 `update_sell(trade_id, sell_price, sell_quantity, ...)`로 **trade_id가
    위치 인자**다. 실제 호출부 3곳 중 **2곳이 위치 인자**로 부른다:
      `_release_ghost_position`(strategy_manager:1947) / `main.py:637` 수동매도 감지.
    (`_execute_sell`만 키워드다.)
  · 🟢 **프로덕션은 안전하다** — 실물 시그니처에 3곳 전부 정상 바인딩됨을
    `inspect.signature().bind()`로 기계 검증했다. 위험은 **테스트 충실도**다:
    저 스텁을 쓰는 스위트가 유령 정리 경로를 타면 TypeError가 나고 호출부의
    except가 삼켜 **'DB 종료 0건'이라는 가짜 통과**가 된다.
  · -> 11개 전부 실물과 맞췄다(위치·키워드 양쪽 수용). **회귀 0** — 고친 뒤
    14개 스위트 재실행에서 건수가 하나도 안 바뀌었다.
  · 🔴 **재발 방지를 기계화했다** — 리플레이 [7]이 모든 스위트의 스텁이 위치
    인자를 받는지 정규식으로 센다. 사람이 기억해서 지킬 규칙이 아니다.

  [🔴 결함 ② 아카이브 폴더명 != 실제 틱 날짜 — 지금까지의 틱 분석이 오염됐다]
  · 아카이버(`core/tick_archive.py`)는 ka10079로 소급 수집하는데, **당일 거래가
    없던 종목은 직전 거래일 틱이 그대로 돌아와 오늘 폴더에 저장된다.**
    실측: `20260810/` 37개 파일 중 실제 08-10 틱은 **12개뿐**이고, 나머지는
    08-04·05·06·07 심지어 **07-14·07-20**짜리다.
  · 폴더는 6개인데 실제 날짜로 다시 묶으면 **11일**이다.
  · ⚠️ **이전 세션들의 "4일치"·"5일치"·"15일 561k틱"은 폴더명으로 센 값**이라
    실제로는 여러 날짜가 섞여 있었다. 08-10 문턱x캡 격자, 08-08 발사 게이트
    독립표본이 여기 해당한다 — **결론을 뒤집을 정도인지는 재측정해야 안다.**
    (날짜별 분해 원칙을 지켰다면 영향이 작지만, 표본 수는 부풀려져 있었다.)
  · -> **아카이브로 분석할 땐 반드시 `time_str`로 날짜를 다시 묶을 것.**
    이 프로젝트의 "구간별 평균은 날짜로 쪼개 볼 것"과 같은 계열의 함정이다.

  [🆕 `test_replay_archive.py` 22건 신설 — 이월과제 #3 종료(세 번 만에)]
  · 08-08·08-09·08-10 세 세션이 매번 스크래치패드에 만들었다 잃어버린 것을
    커밋했다. 아카이브 **11일 558,479틱**을 실물 `StrategyManager`에 관통.
  · 결과: **예외 0 / 종목별 REST 0콜 / 초반 점유 정확히 4에서 멈춤(전 11일) /
    전체 최대 6 / 매수 95건 전부 체결단가·수량 정상 / 매도 32건 전부 정당 사유 /
    정체정리 0건 / 우선순위 교체 0건**(정책 OFF가 실제로 도는지 동작으로 확인).
  · 🔴 **A/B를 내장했다** — 변동성 손절 ON/OFF를 각각 돌려 결과가 **실제로
    갈리는지** 본다. 08-10 낮에 이 규칙이 무효인 걸 정확히 이 방법으로 찾았다.
    실측 ON: 종목별 -2.96%/-3.59%/-5.39%/-5.5%로 **6종 차등** vs OFF: 전부 -3.0%.
  · ⚠️ `_ensure_base_capital`(MDD 기준자본)은 **세션 1회** REST라 '핫패스 0콜'
    단정에서 분리했다 — 뭉개면 진짜 위반(종목마다 부르는 것)이 이 1콜에 묻힌다.
    실측 144,905틱에 1콜로 캐시 수렴 확인.

  [수치 경계 — 실물 `on_price_update`에 직접 태워 확인. 전부 정확했다]
  | 규칙 | 경계 | 결과 |
  |---|---|---|
  | 손절(고정, DB복원분) | 가격 -3.00% | -2.99 통과 / **-3.00 발동** |
  | 손절(변동성) | `stop_rate` -5.5% | -5.49 통과 / **-5.51 발동** |
  | 익절캡 | 순 +6.00%(가격 +6.23%) | +5.99 통과 / **+6.00 발동** |
  | 본전스톱 무장 | 순 +2.50% | +2.49 미무장 / **+2.50 무장** |
  | 본전스톱 발동 | 순 <= +0.20% | +0.21 통과 / **+0.20 발동** |
  | 정체·시간정리 | 슬롯 만석·45분 보유 | **발동 0 (OFF 정상)** |

  [부수 — 문서/주석 정정]
  · `strategy_manager.py`의 본전스톱 주석이 아직 **"순 +1.0%"**였다(상수는 0.025).
    상수명 참조로 바꾸고 "익절캡과 세트"라는 근거를 같이 적었다.
  · `docs/PRODUCT_PLAN_2026-08-08.md`의 **라인 참조 11개 중 10개가 무효**다.
    08-09·08-10에 `strategy_manager.py`가 **5,593 -> 7,111줄**(+1,518)로 늘었다.
    특히 그 문서가 9번 설계 논의의 근거로 인용한
    `:4649 if gross_rate <= STOP_LOSS_RATE:`는 **코드 자체가 없어졌다**
    (08-10에 변동성 손절로 바뀌어 `:6076`, 값은 포지션별). 경고를 달아 뒀다.
  · 모듈레벨 상수 123 -> **166개**(2일 만에 +43). 항목 M 견적이 그만큼 커진다.
  · 소스 문자열 단언 테스트: 항목 M의 "4곳" -> 08-08 8곳 -> **08-10 11곳**.

  [검증] **14개 스위트 1,577건 전원 통과 / 실패 0**
  (297/89/130/70/165/24/142/187/63/148/50/91/99/**22**).
  `import main` OK / .py 90개 구문오류 0 / 태스크 정의 21 == gather 21 /
  스케줄러 `AutoTrader_Start` Ready·`PremarketScan` Disabled / junction 정상 /
  `start_trader.bat` 비ASCII 0 / `.ps1` UTF-8 BOM / STOP_SIGNAL 없음 /
  python 프로세스 0 / DB 미청산 `holding` **0건**(`manual` 3건은 설계대로 제외).
  `daily_backtest` **실계정 실행**(텔레그램 미전송): 유니버스 56종목,
  분봉 조회 실패 **0**, 예외 0.
  ⚠️ `shutdown_pc.bat`에 비ASCII 165바이트가 있다 — 다만 **어디서도 참조되지
  않는 고아 파일**이라 스케줄러 함정(한글 .bat)에 걸리지 않는다. 쓰게 되면 정리할 것.
