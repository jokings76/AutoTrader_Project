# 2026-08-05 히스토리 (아카이브)

CLAUDE.md 150k 한도 때문에 2026-08-09에 분리했다. **경위 확인용**이고,
지금 도는 사양은 CLAUDE.md의 `## 현재 전략 요약`을 볼 것.

⚠️ 이 문서의 값 중 이후 바뀐 것:
- 매수금액 50만 -> 200만(08-05 마감) -> **100만**(08-06)
- 버스트 주가계수 상한 2.00 -> 2.50 -> **3.00**(08-06)
- 조건검색 `stex_tp` "A"(통합) -> **"K"(KRX)**(08-07)
- 무장 1.5초 -> **3.0초**(08-07 실측 복원)
- 상승이탈 ON -> **OFF**(08-06 [D])
- VI 상단 **매수차단**은 OFF(08-06). **확정매도**는 ON 유지
- 대량체결 버스트는 08-08부터 **신규 진입 발사 조건에서 빠졌다**
  (09:00~09:05 면제 / 09:05~ 거래대금 가속 2.5). `check_burst` 자체는
  재매수 판정·슬롯 교체·**파동 카운트**에 그대로 쓰인다

---

- 2026-08-05 (화, **모의 -> 실전투자 전환**) — 사용자 지정으로 실전 위탁계좌에
  연결했다. **전략 로직은 하나도 안 바꿨다** — 08-04 마감 설정 그대로다.
  바꾼 것은 접속 대상과 자금 규모뿐이다.

  [전환 내용]
  - `config.ini`(git 미추적): 실전 APP_KEY/SECRET_KEY, 실전 ACCOUNT_NO, `IS_MOCK=false`.
  - **매수금액 200만 -> 50만원**(모의 대비 1/4, 사용자 지정). 슬롯(4/4/6/8),
    tier 배수(1.5), MDD(-3%), 시간창, 캡은 **전부 모의와 동일**하게 유지 —
    변수를 하나만 바꿔야 실전/모의 결과를 같은 축에서 비교할 수 있다.

  [🔴 치명 — 이것 없이는 08:59에 기동 즉시 죽었다]
  - **`api/auth.py`의 토큰 발급 URL이 `mockapi.kiwoom.com`으로 하드코딩**돼
    있었다. `IS_MOCK`을 보지 않으므로 실전 앱키로 모의 서버에 요청 ->
    발급 실패 -> `main.setup`이 `RuntimeError("토큰 발급 실패")`로 종료.
    무인 기동이라 아무도 모른 채 하루가 사라진다. `IS_MOCK` 분기로 수정.
    전 `.py`를 훑어 다른 하드코딩은 없음을 확인했다(REST/WS/tick_archive는
    원래 분기가 있었다).

  [🔴 예수금 필드가 틀려 MDD가 1/100로 잡히고 있었다]
  - `get_orderable_amount()`가 `ord_alow_amt`를 썼는데 이건 **D+0 현금**이다.
    한국 주식은 매도대금이 D+2 결제라도 그 대금으로 **당일 재매수가 되므로**,
    매도 직후엔 매수여력을 심하게 과소평가한다. 실측:
    ```
    ord_alow_amt          =    100,818   <- 옛 값
    d2_entra              = 10,835,698   (HTS '추정예수금 D+2')
    100stk_ord_alow_amt   = 10,835,694   <- 정답 (증거금100% = 미수 없음)
    20stk_ord_alow_amt    = 62,578,465   <- 미수. 절대 쓰면 안 된다
    repl_amt(대용금)      =  1,681,620   <- 포함하면 안 된다
    ```
  - 매수 자체보다 **MDD 기준자본**에서 치명적이었다. `_ensure_base_capital`이
    이 값을 기준자본으로 삼는데 100,818원이면 일손실 한도 -3%가 **-3,025원** —
    거래 한 건에 그날 매수가 전면 차단된다. 수정 후 **-325,071원**으로 정상화.
  - 폴백 체인 `100stk -> d2_pymn_alow_amt -> ord_alow_amt` (모의서버 호환).
    ⚠️ **20/30/40/50/60stk 계열은 어떤 경우에도 쓰지 말 것** — 미수(반대매매)다.

  [🔴 매수금액이 3곳에 흩어져 있었다 — 처음 고친 곳은 라이브 경로가 아니었다]
  - `order_manager.BUY_AMOUNT_PER_STOCK`만 고쳤는데 라이브는 그걸 안 쓴다.
    실제 지배값은 **`portfolio_optimizer.DEFAULT_BASE_AMOUNT`** 다
    (main.py가 `PortfolioOptimizer(rest_api=self.rest)`로만 생성해 기본값 사용).
    `config/phase_settings.py`의 `position_amount`는 optimizer 실패 시 fallback.
    셋 다 50만원으로 맞추고 각 파일에 "3곳이 같이 움직여야 한다" 주석을 박았다.
    **이 코드베이스의 반복 사고 패턴(같은 규칙 분산)이 또 나온 사례.**

  [실측 — 실제로 얼마씩 사는가]
  - `final_weight`는 **0.90**이다(Kelly가 손실 기대라 하한 0.3 x 변동성 상한 3.0).
    -> 종목당 **449,000원**, 되돌림 1차 트랜치 **224,500원**.
  - `target_volatility=0.02`(일일 2%)를 **1분봉 ATR**(실측 0.22~0.59%)과 비교하는
    구조라 변동성 배수는 사실상 항상 상한 3.0에 붙는다. 즉 비중을 실질적으로
    결정하는 건 Kelly 쪽이다.
  - **0주 스킵 위험은 실무상 없다**: 조건검색식 주가 상한이 150,000원인데
    트랜치가 224,500원이라 최고가 종목도 1주 이상(심텍 97,100원 -> 2주).
    이론상 `final_weight`가 하한 0.3까지 내려가면 트랜치 75,000원이 되어
    75,000원 초과 종목이 0주가 되지만, 그러려면 **1분봉 ATR >= 6.67%**가
    필요해 도달하지 않는다.
  - 노출: 평상시 슬롯 6 만석 = 269만원(**자본의 24.9%**). 최대비중 x 확장슬롯 8은
    이론상 1,200만원(111%)으로 자본을 넘지만, 초과분은 키움이 거부할 뿐
    오주문이 아니다(실측 동시보유 최대 2/6이라 도달 가능성도 낮다).

  [🟢 실전 계좌의 기존 보유 종목 — 봇이 건드리지 않는다 (코드로 확인)]
  - 계좌에 **우리기술(032820) 271주 -30.36%** / **엑스게이트(356680) 2주 -23.02%**
    가 있다. 봇이 이걸 자기 포지션으로 잡으면 -3% 손절로 **즉시 전량 시장가
    매도**되는 사고가 난다. 확인 결과 **안전하다**:
    - `strategy_mgr.holdings`는 서버 잔고가 아니라 **DB(`TradeRepository.find_holdings()`)**
      에서 복원된다. 현재 DB의 open 포지션은 0건.
    - 손절/익절/정체정리/**15:10 강제청산**이 전부 `holdings`를 순회하므로 대상 밖.
      (`task_force_close_watcher`는 `self.strategy_mgr.holdings.keys()`를 돈다)
    - `_detect_orphan_positions`는 **감지해서 알리기만** 하고 자동 편입하지 않는다.
  - -> **08-05 아침에 "⚠️ 미관리 잔고 감지" 알림 2건이 오는 것이 정상이다.**
    놀라지 말 것. 봇 성과와 계좌 잔고가 다른 이유도 이 2종목 때문이다.

  [확인만 하고 둔 것 — 정상]
  - 조건검색식 4개가 실전 계정에서도 **같은 이름·같은 seq**로 조회됨
    (주도주상위=1 / 돌파자동매매용=2 / 눌림목자동=3 / 종가베팅=4). 유사 이름
    오탐도 없음. 종가베팅은 매매 라우팅과 분리 유지.
  - `ACCOUNT_NO`는 **코드에서 한 번도 쓰이지 않는다**(settings.py에서 읽기만 함).
    실제 주문 계좌는 앱키에 묶인 계좌로 결정된다 — 기록용이다.
  - 수수료: `ROUND_TRIP_COST=0.23%`는 실전 기준(편도 0.015% + 세금)에 가깝다.
    모의는 편도 0.3%라 **지금까지의 성과 분석은 오히려 비용을 과소평가**하고
    있었다 — 실전에서 이 가정이 정확해진다.

  [검증]
  - `test_live_20260805.py` **58건 신설** 전원 통과(10개 섹션: 모의 하드코딩 /
    예수금 필드·폴백 / **기존 보유 불가침** / 매수금액 3곳 / 노출한도 /
    매수경로 2곳 되돌림 / 손절 우선순위 / 상수 불변식 / 백테스트 동기 / 배선).
  - 기존 6개 스위트 **757건 중 756건 통과**(1건은 `NEW_SESSION_REQUESTED`
    존재 검사로 이관 직후 정상). **총 815건.**
  - `daily_backtest` 실제 실행 검증: 08-04 유니버스 47종목 -> 5건 재현, 예외 0.
  - 구문오류 0 / `import main` OK / 태스크 정의 20 = gather 등록 20 /
    STOP_SIGNAL 없음 / 스케줄러 08-05 08:59 Ready.

- 2026-08-05 (08:30, **기동 29분 전 — 조건검색 거래소구분 치명 버그**) —
  사용자가 HTS [0156] 조건검색식 **'대상'을 KRX -> 통합으로 변경**한 뒤
  "문제 없는지 봐달라"고 요청. 그대로 기동했으면 **하루 종일 매수 0건**이었다.

  - **원인**: `api/kiwoom_ws.py`의 조건검색 요청이 `stex_tp="K"`(KRX)로
    하드코딩돼 있었다(`subscribe_condition` / `fetch_condition_snapshot` 2곳).
    HTS에서 대상을 통합으로 바꾸면 조건식 자체가 통합 기준이 되는데,
    KRX로 요청하면 **에러 없이 `return_code=0` + 0종목**이 돌아온다.
    로그에 아무 오류도 안 남아 원인을 찾기가 매우 어려운 부류다.
  - **실측 (08:33, 장 시작 전, HTS 대상=통합)**:
    ```
    seq=1 주도주상위   K -> 0종목 / N -> 0종목 / A -> 1종목 ['218410']  <- HTS 일치
    seq=3 눌림목자동   K -> 0종목 / N -> 0종목 / A -> 1종목 ['336260']  <- HTS 일치
    seq=4 종가베팅     K -> 0종목 / N -> 0종목 / A -> 2종목
    ("0"/"1"/"2"/"3" 숫자값은 전부 0종목 — 이 API는 문자 코드를 쓴다)
    ```
  - **수정**: 모듈 상수 `CONDITION_STEX_TP = "A"` 신설, 두 함수의 기본값을
    `None`으로 바꿔 이 상수를 참조하게 했다(한쪽만 고치는 사고 방지).
    **HTS 대상을 KRX로 되돌리면 이 값도 `"K"`로 같이 되돌릴 것.**
  - **주문/잔고는 건드리지 않았다** — 조건검색의 `stex_tp`와 주문의
    `dmst_stex_tp`는 독립이다. 실측(08:35) `kt00018` 잔고조회는
    `KRX`/`NXT` 둘 다 동일한 2종목을 반환했고 `SOR`은 rc=20(거래소구분 오류)이라
    **`"KRX"` 유지가 안전**하다. 주문 4곳(`매수/매도/취소/잔고`)도 그대로 둔다.
    ※ NXT까지 최선호가로 라우팅하려면 `SOR`을 검토할 수 있으나,
      **실전 개장 직전에 주문 경로를 바꾸는 건 금지** — 검증 후로 이월.
  - **[발견, 미수정] `kiwoom_ws.py:229`가 `item["jmcode"]`를 쓰는데 실제
    응답 키는 `'9001'`이라 `ConditionManager.update_snapshot()`이 한 번도
    호출된 적이 없다**(로그의 `스냅샷 매핑 처리 실패: 'jmcode'`가 그것).
    try/except 안이라 스냅샷 자체는 정상 반환되고, `ConditionManager`의
    상태를 읽는 코드가 **어디에도 없어** 사실상 죽은 코드다. 고치면 한 번도
    실행된 적 없는 경로가 처음 돌게 되므로 개장 직전 수정은 보류했다.
  - [검증] `test_live_20260805.py`에 **섹션 11 신설**(58 -> 63건) —
    상수값/두 경로 공통 참조/주문측 KRX 유지를 못박는다.
    7개 스위트 **814건 통과**(1건은 이관 플래그, 정상).

- 2026-08-05 (계속, **13:33 장중 정지 -> 수술 7건 -> 13:54 재가동**) —
  10:30 이후 3시간 매수 0건이 관측돼 사용자 지정으로 장중에 멈추고 고쳤다.
  DB 미청산 0건을 확인한 뒤 STOP_SIGNAL로 정지했다(보유가 있으면 절대 하지 말 것).

  [진단 — 조건이 타이트해서가 아니었다]
  10:30~13:33 구간 퍼널: 편입 3,176 / 무장 1,006 / **트리거 2,166** / 매수 **0**.
  신호는 계속 나는데 두 곳에서 막혔다:
  - **등락률 상한 2,222회** — 대한광통신 혼자 2,110회(최고 +18.5%), 빛과전자 57,
    기가레인 36. 전부 눌림목 상한 10%.
  - **재매수 차단 7,071회** — 이미 손절/청산된 종목이 계속 신호를 냄.
  - 슬롯 부족은 **0회**. 슬롯은 병목이 아니다.

  [조건 완화 시 수익 가능한가 — 사용자 질문에 대한 실측]
  익절캡 2.5% / 손절 -3% 적용해 역산(손익분기 승률 58.7%):
  - 등락률 상한 완화: 6건 중 **익절 4 / 손절 2** (승률 67%, 건당 +0.67% 세전)
  - 재매수 차단 완화: 12건 중 **익절 8 / 손절 4** (승률 67%, 동일)
  둘 다 플러스 기대값이지만 **마진이 8.3%p로 얇고 하루치 표본**이다.
  재매수 차단은 07-29에 실손실(-199,290원)로 만든 규칙이라 더 위험하다.
  -> **둘 다 완화하지 않았다.** 며칠 더 관찰 후 판단할 것.

  [적용한 7건]
  | # | 변경 | 근거 |
  |---|---|---|
  | 1 | `PHASE1A_ASK_DEPTH_MIN` 5,000만 -> **1,000만** | 미체결 3건 기회손실 +11.33%/+15.06%/+5.71% |
  | 2 | `BREAKEVEN_FLOOR` 0.0 -> **0.002** | 포톤이 순 -0.04%로 청산됨(시장가 슬리피지) |
  | 3 | **손절 대신 추가매수** 신규 | 사용자 지정 |
  | 4 | 1A 등락률 16% -> **13%** | ~10% 구간이 이후최고/최저/변동폭 전 지표 1위 |
  | 5 | 되돌림 -0.5/-1.0% -> **-0.3/-0.7%** | 미도달 9건의 실제 되돌림 -0.16~-0.96% |
  | 6 | **'놓친 기회' 알림** 신규 | 수동 판단 지원 |
  | 7 | **되돌림 중 +0.3% 상승 이탈** 신규 | 미도달 9건 중 7건 발동, 못 사던 4건 전부 커버 |

  [#3 손절 대신 추가매수 — 가장 위험한 변경]
  - 발동: -3% 도달 시 ①거래대금 가속도 >= 3.0배 ②체결강도 >= 100 AND 기준선
    대비 상승 이 **둘 다** 성립하면 매도를 **15초 보류**하고 관찰. 그 안에서
    ③저점 대비 **+0.3% 반등**이 확인되면 같은 금액 추가매수.
  - ⚠️ **관찰 창이 없으면 이 기능은 원리적으로 발동 불가**다. 손절은 -3%에
    **최초로 닿는 순간** 발동하는데 그때는 현재가 = 최근저점이라 반등이 정의상
    0%다. 테스트를 쓰다가 발견했다 — 사양만 보고 구현했으면 "켰는데 한 번도
    안 도는" 기능이 됐을 것이다.
  - **추가되는 리스크는 정확히 이만큼**: 구조받지 못한 포지션의 손절이 -3%가
    아니라 최대 **-4.5%**(`RESCUE_ADD_OBSERVE_FLOOR`)에서 난다.
  - 추가매수 후에는 **원가 -6%**(`RESCUE_ADD_FINAL_STOP`)가 무조건 청산선.
    `origin_price`를 포지션에 심어 평단이 덮여도 원가 기준을 유지한다.
  - 종목당 1회 / 하루 2회. 조건 불충족·한도초과·MDD/가드 차단·**예외**는
    전부 손절로 수렴한다.
  - 08-05 역산(조건①만 검증 가능): KBI메탈 4.59배 -> **-6% 최종손절**(09:10에
    최저 4,025 찍고 09:24에 익절선 도달 — 바닥 먼저 찍는 최악 경로),
    씨이랩 3.93배 -> 익절. **1승 1패, -20,700원.** ②③은 틱·호가 이력이 없어
    검증 불가다.

  [#7 상승 이탈 — 예측이 아니라 관측]
  트리거 시점 지표로 되돌림 깊이를 예측하는 건 **불가능**했다(경로별 평균
  되돌림 절대 -0.75% / 단일 -1.30% / 상대 -1.69%인데, 같은 절대 경로에서
  씨이랩 -4.50%도 나온다). 대신 "기다리는데 오히려 오른다"는 관측을 쓴다.
  되돌림 1차(-0.3%)와 대칭이라 **±0.3% 브레이크아웃 밴드**가 된다.
  같은 틱에 둘 다 성립할 수는 없지만 **하락(되돌림) 우선**으로 코드 순서를 박았다.

  [부수] 등락률 상한을 '매수 컷오프'에서 떼어 전용 분류 신설 — 08-05 탈락의
  대부분이 이것인데 15:10 하드컷오프와 뭉개져 원인 판단이 어려웠다.
  `TradeFlowTracker.get_low()` 추가.

  [검증] `test_patch_20260805.py` **45건 신설**, 8개 스위트 **865건 전원 통과**.
  daily_backtest 실행 검증(3건 재현, 예외 0). 커밋 `dc8dc40`.
  재가동 후 DB 복원 정상(손절차단 7종목 / 쿨다운 15종목 유지).

- 2026-08-05 (계속, 14:00~14:35 **매도시점·시가필터·재매수 3건**) — 사용자가
  "JW신약/금호타이어/PS일렉/일승/알트/뉴엔AI 매도시점이 아쉽다"고 지적한 데서 출발.

  [지목된 6종목의 범인은 동적캡이 아니라 **손실반등 하이브리드 매도**였다]
  6건 전부 이 경로였고, **5건이 이후 상승 / 4건은 익절캡까지** 갔다:
    금호타이어 -1.32% -> +6.04% / 뉴엔AI -0.12% -> **+16.49%**
    일승 -0.11% -> +5.69% / JW신약 -0.63% -> +5.97% / PS일렉 -0.66% -> +3.10%
    서산 -0.45% -> +0.76% (유일한 실패)
  **실제 실현 -6,967원 vs 홀딩 시 +28,690원 (차이 +35,657원)** — 그날 최대 손실 요인.
  - 원인은 판정 기준이다. 급락 직후 반등 초기엔 거래량·강도가 **직전 급락
    구간보다 낮은 게 당연**한데 그걸 '가짜 반등'의 근거로 쓰니 조건이 거의
    항상 참이 된다. 08-03에 진입강도 스파이크를 기준선으로 삼아 동적캡이
    오발동한 것과 **같은 종류의 오류**다.
  - -> **전량 청산 -> 50% 분할 + 잔량 3% 트레일**(동적캡과 동일 구조).
    08-04에 "리스크 장치라 성격이 반대"라며 전량을 유지했던 판단을 뒤집었다.

  [시가대비 매수보류 필터가 **하루 종일 죽어 있었다**]
  - 08-05 로그에 이 필터의 발동이 **0건**, PS일렉트로닉스가 시가대비
    **+9.94%**인데 그대로 체결됐다(상한 5%).
  - 원인: `prearm_candidate`의 docstring은 "③ 당일 시가 캐시 예열"을 한다고
    써놨는데 **실제 코드는 분봉이 넘어올 때만** 채웠다. 틱 편입 경로는 분봉을
    안 받으므로 캐시가 비고, 캐시가 비면 `open_price=0`이라 필터가 통째로 스킵된다.
  - -> 전일종가를 가져오는 **ka10001 응답에 `open_pric`이 이미 있다**(실측
    `open_pric=+4195`). `_get_prev_close`가 같은 1콜에서 시가까지 캐시한다.
    **REST 추가 0콜.**
  - ⚠️ 중간에 `_get_opening_price`(분봉 400개)를 진입 핫패스에 넣었다가
    dryrun 테스트가 잡아냈다 — 08-02에 429 예산 때문에 없앤 REST 호출이
    되살아났던 것. **진입 핫패스는 캐시만 읽는다는 원칙을 지킬 것.**
  - ⚠️ 시가대비와 성과는 **무관**했다(최고수익 삼기 +6.25%가 시가대비 1.71%,
    4%대 4건은 0.00/+2.75/-1.09/-0.22%). 5%가 적정한지는 **아직 모른다** —
    측정된 적이 없기 때문이다. 이제부터 데이터가 쌓인다.

  [손실청산 종목의 조건부 재진입 — KBI메탈 상한가 대응]
  - KBI메탈이 손절(09:01) 후 **상한가(매수가 +15.93%)**에 안착했다.
    **손절 파라미터로는 못 잡는다** — 손절 후 9분간 -6.40%(4,025, 09:10)까지
    더 빠져서 어떤 손절선을 써도 털리고, 상승은 3시간 45분 뒤(12:46)다.
  - ⚠️ 첫 아이디어였던 "손절가 +5% 회복 후 재진입"은 실측 **1승 3패**로
    떨어졌다 — 회복을 확인하고 들어가면 이미 **추격매수**다.
  - -> 가격이 아니라 **대금** 조건으로 간다:
    ① 청산 후 60분 경과 ② 종목당 1회 ③ **절대 경로 문턱 2배**
    (8천만 x 2건 = **1.6억**) ④ **상대 경로 금지**
  - 근거: 08-03~04 32건의 버스트 규모별 성과에서 **0.8억을 경계로 승률
    37% -> 62%**로 갈린다(현재 일반 문턱이 0.8억). 재매수는 이미 한 번
    실패한 종목이라 2배 안전마진을 더한다. 상대 경로는 08-04 실측에서
    3경로 중 최악(-0.67% / 절대 -0.30% / 단일 +0.52%)이라 뺐다.
    ⚠️ 1.6억 이상 구간 자체의 표본은 5건뿐 — **최적화값이 아니라 안전마진**이다.
  - `check_burst`에 `value_mult` / `allow_relative` 인자 추가(기본값은 기존 동작).

  [검증] `test_patch_20260805.py` 45 -> **59건**. 8개 스위트 **882건 전원 통과**.
  커밋 `9a7e7e4`. ⚠️ **이 3건은 08-06 08:59 재기동부터 적용된다** — 적용 시점에
  봇이 이미 돌고 있었다(13:54 기동분은 옛 코드).

- 2026-08-05 (마감) — **실전 첫날 결산: 청산 20건 / 실현 +9,796원 / 평균 +0.41%**
  (미청산 0건). 하루에 실전 전환 + 수술 10건이 겹쳤는데 플러스로 끝났다.
  다만 **오늘 성과를 전략 검증으로 읽으면 안 된다** — 변경이 장중에 두 번
  반영됐고(13:54 재가동), 마지막 3건(손실반등 분할/시가필터/재매수 완화)은
  아예 미반영 상태로 끝났다. 08-06이 온전한 첫 관찰일이다.
  - 최대 손실 요인이었던 손실반등 전량청산은 고쳤다(-6,967원 -> 홀딩 시 +28,690원).
  - 최대 미체결 손실이던 지정가 문제도 시장가 문턱 인하로 고쳤다.
  - 08-05 커밋: `591bf3f`(실전전환) `6017c4d`(관찰기록) `bf94f5e`(stex_tp)
    `dc8dc40`(수술 7건) `9a7e7e4`(매도시점 3건) + CLAUDE.md 3건.

- 2026-08-05 (장마감 후, 봇 종료 상태) — **버스트 주가 스케일 + 매수금액 200만 환원
  + 진단 3건**. 사용자가 "천원부터 15만원까지 있는데 전부 같은 버스트 거래대금은
  이상하다"고 지적한 데서 출발했다. **지적이 정확했고, 실측은 예상보다 심했다.**

  [🔴 진단 — 주가가 사실상 '진입 경로'를 결정하고 있었다]
  - 틱 아카이브 107 종목·일(09:00~09:15)에 현행 규칙을 그대로 태운 실측:
    | 주가대 | 절대(4천만x2건) 발생 | 양 경로 모두 0인 종목 |
    |---|---|---|
    | ~2,500원 | **0.05회**/15분 | 67% |
    | 2,500~5천 | **0.04회** | 74% |
    | 5천~1만 | 0.45회 | 59% |
    | 1만~2.5만 | **4.42회** | 16% |
    | 2.5만~ | **6.18회** | 18% |
    **124배 차이**. 5천원 미만에서 절대 경로는 죽어 있었다.
  - 실거래 56건(08-03~05)이 그대로 확인해 준다 — **2,500원 미만 진입 5건은
    경로가 100% '상대'**, 1만~2.5만은 72%가 '절대'. 서로 다른 규칙으로 사고 있었다.
  - 원인: 틱 체결금액 자체가 주가를 따라 커진다(log-log 회귀 **평균1틱 ~ 주가^0.36,
    r=0.77** / '같은 발생빈도' 역산 ~ 주가^0.72). 그리고 **상대 경로가 저가주에
    한해 이 스케일링을 의도치 않게 대신 수행**하고 있었다(실효 문턱 저가 1,175만 /
    고가 3,741만 = 주가^0.36). 다만 4천만에서 잘리고, 분모가 실시간으로 흔들려
    재현·백테스트가 불가능한 형태였다.
  - ⚠️ **부수 정정**: "상대 경로가 3경로 중 최악(-0.67%)"이라던 08-04 판정은
    **저가주 성과와 교란돼 있다** — 저가주가 쓸 수 있는 유일한 경로였기 때문이다.
    미적용 항목 G는 이 근거로 재평가할 것.

  [적용 — 계수 하나를 곱한다]
  - `burst_price_scale(price) = clamp((price/10,000)^0.55, 0.30, 2.50)`.
    `check_burst`에서 시간계수(tmul)와 **똑같은 방식으로** burst_min/single_min에
    곱한다. 가격은 `TradeFlowTracker.get_latest_price()` — 이미 읽고 있는 틱
    버퍼라 **REST 0콜 / 지연 0ms / 결정적(로그로 재현 가능)**.
    | 주가 | 계수 | 절대(2건) | 단일 |
    |---|---|---|---|
    | 1,000원 | 0.30(하한) | 1,200만 | 0.30억 |
    | 2,000원 | 0.41 | 1,650만 | 0.41억 |
    | 5,000원 | 0.68 | 2,730만 | 0.68억 |
    | **10,000원** | **1.00** | **4,000만** | **1.00억** |
    | 30,000원 | 1.83 | 7,320만 | 1.83억 |
    | 53,000원~ | 2.50(상한) | 1.00억 | 2.50억 |
  - **지수 0.55**: 실측 두 값(0.36/0.72)의 중간. 0.40으로 낮추면 저가주가 여전히
    아무것도 못 잡아 밴드 편차가 9.8배로 **오히려 나빠진다**(시뮬 확인).
    0.55에서 편차 **9.3배 -> 3.5배**.
  - **상대 경로 하한을 `max(burst_min, avg x20)`으로** 변경(옛 상수 1천만원 폐기).
    ⚠️ **이걸 안 하면 이번 변경이 저가주에서 통째로 무효다** — 2,000원 기준
    새 절대문턱 1,650만 vs 옛 상대 실효 1,175만이라 여전히 상대가 더 헐겁다.
    이제 상대는 절대보다 헐거워질 수 없어 **사실상 무력화**된다(= 미적용 항목 G
    "상대 경로 폐지"의 근거가 완성됨. 코드 삭제는 다음 단계).
  - **재매수 배수 2.0 -> 2.5** (사용자 지정 "권장보다 좀더 엄격하게").
    스케일 도입으로 배수의 의미가 바뀌었다 — 2.0을 두면 저가주 재매수 문턱이
    1.6억 -> **0.66억으로 완화**되는 부작용이 있었다. 2.5는 그걸 되돌리고도 남는다.

  [🔴 백테스트 — 성과 우위는 입증되지 않았다. 반드시 읽을 것]
  틱 아카이브 3일치로 신·구를 대조했다(진입: 버스트 -> 되돌림/상승이탈 -> 등락률
  상한 / 청산: 1분봉으로 손절 -3%·익절캡·15:10):
  | | 08-03 | 08-04 | 08-05 | 3일 합계 |
  |---|---|---|---|---|
  | 현행 | 4건 +2.27% | 12건 **+0.90%** | 21건 +1.11% | 37건 승률 78% **평균 +1.17%** |
  | 신규 | 4건 +2.27% | 12건 **+0.44%** | 23건 +1.38% | 39건 승률 79% **평균 +1.18%** |
  - **08-05만 개선, 08-04는 악화, 08-03은 동일.** 이 프로젝트가 08-04 버스트
    상향 때 요구했던 "양일 각각 개선"이라는 강건성 기준을 **충족하지 못한다.**
  - 파라미터 그리드(a=0.40/0.55/0.70 x max=1.8/2.5)도 전부 평균 +1.12~+1.21%로
    **차이가 표본 노이즈 수준**이다. max는 이 표본에서 아무 영향이 없었다.
  - 08-04 악화의 정체는 정확히 2종목이다: ①049080(7,650원)이 **더 늦게** 트리거돼
    등락률 상한에 걸려 승리 1건을 잃음 ②340810(3,520원, 08-04 분석의 생존편향
    이상치)이 새로 들어와 손절. 즉 **저가주 문턱을 낮추면 저가주 손실도 같이 들어온다.**
  - ⚠️ **예상 못 했던 기전**: 5천~2.5만 구간은 옛 상대 경로가 새 절대 문턱보다
    헐거웠어서, 이번 변경이 그 구간을 **조인다**. 조이면 트리거가 늦어지고 →
    진입가가 높아지고 → 등락률 상한에 걸리거나 손절 거리가 줄어든다.
    실제로 08-05 317400, 08-04 049080 두 건이 이 경로로 나빠졌다.
  - 시뮬 한계: **무장(FID228)이 아카이브에 없어 재현 불가**라 진입이 실제보다
    많이 잡히고, 결과의 28/39가 익절캡 2.5%라 해상도가 낮다. 3일 39건이다.
  - **결론: 이 변경의 근거는 성과가 아니라 구조다**(주가에 따라 규칙이 갈리던 것을
    없애고, 문턱을 결정적·재현가능하게 만든 것). 성과는 이 표본에서 무해했을 뿐이다.
    며칠 실거래로 관찰하고, 나빠지면 `BURST_PRICE_ALPHA = 0.0` 한 줄로 완전 복귀.

  [매수금액 50만 -> 200만원 환원 (사용자 지정)]
  - **3곳 동시** 변경: `portfolio_optimizer.DEFAULT_BASE_AMOUNT`(실지배값) /
    `phase_settings.position_amount` / `order_manager.BUY_AMOUNT_PER_STOCK`.
  - ⚠️ **노출 성격이 달라졌다**: weight 0.90 실측 기준 종목당 약 180만원,
    슬롯 6 만석이면 약 1,080만원으로 **예수금(약 1,080만)과 거의 같아진다.**
    50만원 때는 자본이 여유였지만 이제 **예수금이 실질 슬롯 상한으로 작동**한다.
    초과분은 키움이 거부할 뿐 오주문은 아니다(실측 동시보유 최대 2/6).

  [🔎 이랜시스(264850) 미진입 원인 — 정상 동작]
  - 사용자는 '돌파자동매매용'으로 기억했으나 실제로는 **주도주상위(13회) +
    눌림목자동(6회) 중복 편입**이었다(돌파 편입 0건).
  - 13:54 첫 편입은 10:30 이후라 `DUAL_SOURCE_PULLBACK_FROM` 규칙에 따라
    **Pullback으로 라우팅 -> 등락률 상한 10%** 적용.
  - 실제 등락률은 **+17.7% ~ +29.8%**로 **143회 전부 상한 초과 탈락**.
    무장은 14:06부터 여러 번 성립했지만 되돌림 계획 자체가 안 걸렸다.
  - **1A(13%)로 라우팅됐어도 결과는 같다**(17.7% > 13%). 즉 중복편입 규칙 문제가
    아니라 등락률 상한 자체가 막은 것 — 필터의 설계 의도대로다.
    다만 이 종목은 그 뒤로도 계속 올라 주도테마 대장주가 됐다. 상한을 다시 볼지는
    "미적용 B(시가대비 상한 적정값)"와 같이 며칠 데이터를 모아 판단할 것.

  [🔎 종가베팅 미수신 원인 — 스캔은 정상, **알림이 안 가게 돼 있었다**]
  - 14:50:12 스캔 시작 -> 유니버스 16종목 -> **적격 0종목** -> `후보 없음`.
    오프라인 재평가로 확인: 점수 통과 1/13, 양봉비 통과 4/13, **둘 다 통과 0**.
    마감 15분 구간(14:36~14:48)에 거래대금 급증이 거의 없었다(대부분 평소 미만).
    가장 근접한 028050이 점수 26.6(통과)인데 양봉비 40%로 1봉 부족했다.
    즉 **정상 필터링이지 코드 이상이 아니다.**
  - **다만 후보 0건이면 텔레그램이 아예 안 갔다**(`if not ranked: continue`).
    그래서 "스캔이 안 돌았다"와 "돌았는데 후보가 없다"를 구분할 수 없었다 —
    이 프로젝트가 진단 알림에서 지켜온 원칙과 정면으로 어긋나는 지점이다.
    -> **후보 0건도 퍼널을 실어 발송**하도록 수정.

  [🔬 아진엑스텍(059120) 정밀 검증 — 무장·버스트 정상 + 신버전도 동일 매수]
  사용자 요청으로 ka10079를 소급 페이징해 14:29~14:40 실체결 **7,646틱**을 받아
  구/신을 직접 대조했다(장마감 후라 28페이지로 저렴했다).
  - **무장 정상**: 14:32:27 무장(강도 100+ 2.3초) -> 트리거 시점 `103.0초 연속`.
    역산 14:32:25로 로그와 정확히 일치한다.
  - **버스트 정상**: 트리거 직전 5초에 **61,321,000원** 단일 체결(평균 1틱
    96.9만원의 **63배**) + 1,321만 + 1,117만. 진짜 대량 유입이었고, 결과도
    그날 두 번째로 좋은 +4.01%였다.
  - **신버전에서도 매수된다**(주가 6,870원 -> 계수 x0.813):
    | | 발화 | 경로 | 트리거가 | 평단 | 수익 |
    |---|---|---|---|---|---|
    | 구버전 | 14:34:11 | **상대**(하한 1,939만) | 6,880 | 6,880 | +4.01% |
    | 신버전 | 14:34:12 | **절대**(문턱 3,262만) | 6,900 | 6,860 | +4.31% |
    **경로가 상대 -> 절대로 바뀌는 게 핵심**이다. 상대 하한이 1,939만 -> 3,262만
    으로 올라 뒷문이 닫혔지만, 절대 문턱이 4,000만 -> 3,262만으로 내려와 그 자리를
    대신 받았다. 설계 의도대로다. (신버전 트리거가 6,900원 / 목표 6,879·6,852로
    **라이브 로그와 정확히 일치**한다 — REST 틱과 WS 틱의 1초 오차 범위.)
  - 확대 검증(09:00~09:15 아카이브가 있는 실매수 10종목):
    **동일 7 / 발화 지연 2 / 신규 1**.
    · 신규 = 일승(4,300원, x0.63) — 저가주가 새로 잡힘
    · 지연 = 마키나락스(25,900원, x1.69) **+70초**, GS건설(29,750원, x1.82) **+14초**
  - ⚠️ **고가주 발화 지연이 이 변경의 실측된 유일한 비용이다.** 문턱이 올라가
    트리거가 늦어지고 -> 진입가가 높아진다(마키나락스 28,200 -> 28,400,
    GS건설 30,050 -> 30,650). 08-04 백테스트 악화(049080)와 **같은 기전**이다.
  - 비대칭 계수(저 0.55 / 고 0.25 또는 0.00)를 3일 백테스트로 시험했으나
    **결과가 완전히 동일**했다(39건 +1.18%). 복잡도만 늘어 도입하지 않는다.
    이 표본에서 성과를 가르는 건 저가 쪽 지수뿐이다.

  [🔎 일일 백테스트 리포트 오해 — 헤더 강화]
  - 08-05에 다윤님이 이 리포트를 실거래로 오해했다("매매하지도 않은 종목이 나오고
    실계좌는 +1.00%인데 -1.04%로 나온다"). **둘 다 정상이고 서로 다른 모집단이다** —
    실계좌 20건(틱 구동 실체결) vs 리포트 4건(분봉 눌림목 가정 매매).
  - 근본 원인: `_entry_signal`이 재현하는 건 **08-02에 폐지된 분봉 Pullback 경로**
    (`score_pullback`+VWAP)다. 라이브의 틱 구동 진입은 **단 1건도 재현하지 못한다.**
    즉 이 리포트의 진입 성과는 지금 존재하지 않는 규칙의 성과다.
  - 기존 헤더에도 "실제 진입과 다릅니다"가 있었지만 안 통했다. 문구가 약해서가
    아니라 **대조군이 없어서**였다 -> 헤더에 **오늘 실제 체결 건수를 같이 실어**
    두 숫자가 다른 모집단임을 눈으로 보이게 했다(`_today_live_trade_count()`,
    DB 실패 시 조용히 '?'). 제목도 `🧪 ... (모의 시뮬레이션)`으로 바꿨다.
  - ⚠️ 사용자 아이디어("조건만족으로 매매했다고 가정한 내역이면 유의미하다")는
    옳다. 다만 그러려면 **현행 진입 규칙**을 재현해야 하고, 그건 분봉으로는
    불가능하다. 그 역할은 이번에 만든 **틱 아카이브 리플레이**가 한다.
    daily_backtest는 청산 정책 비교용으로만 남긴다.

  [검증] 8개 스위트 **902건 전원 통과**(294/87/126/66/78/163/24/63, 실패 1건은
  문서화된 이관 플래그). 옛 사양을 검증하던 6건이 정확히 실패했고 전부 새 사양으로
  교체했다 — 테스트가 사양 변경을 잡아낸 정상 동작이다.
  `daily_backtest` 실제 실행 검증(4건 재현, 예외 0, 새 헤더 렌더링 확인). `import main` OK.

- 2026-08-05 (심야, **08-06 실거래 전 전면 감사**) — **VI 상단 확정매도 신설 +
  버스트 상한 2.0 + 결함 3건 수정 + 감사 스위트(`audit_20260805.py`) 신설**.
  "매일 오류가 나니 특히 매수/매도가 절대 잘못 나가지 않게 촘촘히 검증해달라"는
  요구로, 기능별 테스트가 아니라 **경로별 오작동**만 따로 훑는 감사를 만들었다.
  그 과정에서 **기존 902건이 전부 통과하는 상태에서 결함 3건**이 나왔다.

  [🔴 결함 ① 비정상 가격에 즉시 매도가 나갔다 — 가장 위험]
  - `on_price_update`에 가격 위생검사가 **없었다.** 음수가 들어오면
    `gross_rate`가 -100%로 계산돼 **그 자리에서 시장가 손절**이 나간다.
  - 키움 응답은 등락부호가 붙은 `'+78800'`/`'-78800'` 형태다. 지금은 라이브 두
    경로가 다 막고 있다(WS 0B는 `abs(price)`, REST 분봉은 `_safe_price`가 부호
    제거) — 즉 **현재는 도달 불가**다. 그런데 **모든 매도가 이 함수 하나를
    지나므로**, 파서 한 곳만 바뀌면 전 종목이 동시에 잘못 팔린다.
  - -> 판정 **전에** 검증: 숫자 변환 실패 / 0 이하 / **매수가의 0.2~5배 밖**이면
    청산 판정을 통째로 건너뛴다(매매를 만들지 않는다). KRX 일일 변동은 ±30%이고
    이 봇은 당일 청산이라 5배 밴드는 정상 거래를 절대 막지 않는다 —
    **상한가(+30%)/하한가(-30%) 대조군을 테스트로 못박았다.**
  - 이상 입력 9종(0/None/-1/''/'abc'/-9,800/1/1천만/NaN) 전부 예외 0·매도 0 확인.

  [🔴 결함 ② 지수 조회가 None을 돌려주면 손절이 죽는다]
  - `_refresh_market_rates`가 반환값을 검사 없이 대입했다. 여기에 None이 들어가면
    `_is_severe_crash`/`_is_index_guard_active`의 `min(kospi, kosdaq)`가
    TypeError를 던지는데, **그 둘은 `on_price_update` 최상단에서 불린다** —
    그 종목의 손절·익절이 통째로 멈춘다.
  - 지금 `get_index_change_rate`는 항상 float를 돌려주지만(실패 시 0.0), 함수
    하나가 바뀌면 매매가 조용히 멈추는 구조다. -> **숫자일 때만 대입**하고
    아니면 기존 캐시값 유지.
  - 감사 스텁이 None을 돌려주다가 실제로 터져서 발견했다.

  [🔴 결함 ③ 가격 폴백이 한 종목에서 막히면 나머지가 통째로 스킵]
  - `task_holdings_price_fallback`의 try/except가 **루프 바깥**이라, 한 종목에서
    예외가 나면 거기서 루프가 끊겨 **뒤에 남은 보유 종목들이 그 사이클을
    통째로 건너뛰었다.** 이 태스크는 틱이 끊긴 종목의 손절을 받아주는 마지막
    안전망이라, 한 종목 문제가 다른 종목 손절을 막으면 안 된다.
  - -> try/except를 루프 **안**으로 이동, 사유에 종목코드를 남긴다.

  [🆕 정적VI 상단 근접 확정매도 (사용자 지정)]
  - VI 발동 시 **2분간 단일가매매로 전환돼 시장가 매도가 불가**하다. 익절 캡
    미달 상태로 갇히면 해제 후 밀려 나오는 위험을 그대로 떠안는다.
  - 규칙: **정적VI 상단(시가 x1.10)까지 0.5% 이내 또는 2호가 이내**면 확정매도.
    `VI_UPPER_EXIT_ENABLED / VI_STATIC_RATIO / VI_UPPER_MARGIN_PCT /
    VI_UPPER_MARGIN_TICKS`.
  - **데이터는 이미 공짜로 온다** — `0g` 실시간에 `305`(상한가)/`306`(하한가)/
    `307`(기준가)가 실려 있고 REST `ka10001`의 `upl_pric`/`lst_pric`/`base_pric`와
    정확히 일치함을 확인했다. 시가는 `_opening_prices` 캐시라 **REST 0콜**.
  - ⚠️ **전일종가로 폴백하지 않는다** — 갭상승한 날 전일종가 기준이면 VI선이
    실제보다 낮게 잡혀 **멀쩡한 포지션을 조기 매도**한다. 시가를 모르면 쉰다.
  - ⚠️ **이미 VI선을 넘은 가격에선 발동하지 않는다** — VI가 한 번 걸리면 거래소
    기준가가 갱신돼 우리 계산이 낡는다. 놓치는 쪽이 잘못 파는 쪽보다 안전하다.
  - ⚠️ **순이익 > 0에서만** 판다(08-03 '익절 로직이 손실 구간에서 발동' 결함과
    같은 원칙). 손실 구간은 손절/본전스톱이 담당한다.
  - ⚠️ **2호가는 전 가격대에서 0.5%보다 좁다**(0.133~0.400%) — OR 조건이라
    실질 문턱은 항상 0.5%이고 호가 조건은 중복이다. 버그가 아니라 설계 결과이며,
    pct를 0.2% 아래로 조이면 그때 호가 조건이 살아난다(테스트로 문서화).
  - 우선순위: **손절 > 지수가드 > VI > (워밍업) > 본전스톱 > 익절캡**.
    워밍업보다 앞인 이유는 순수 가격 판정이라서다(08-05 마키나락스는 09:01
    매수분이 VI 0.1% 앞까지 갔다).
  - 실측(08-05 20건 적용): **1건만 발동**(마키나락스, VI까지 0.11%). 승자였던
    삼기(+6.25%, VI까지 0.65%)는 **건드리지 않았다.** 하단 VI는 20건 중 0건이
    근접(최근접 5.8%) — 손절이 언제나 먼저라 **하단 로직은 만들지 않았다.**

  [버스트 주가 스케일 상한 2.5 -> 2.0 (사용자 지정)]
  - ⚠️ **상한은 `10,000 x MAX^(1/ALPHA)` 지점부터만 걸린다**:
    MAX=1.4 -> 18,437원~ / 1.6 -> 23,503원~ / 1.8 -> 29,116원~ /
    **2.0 -> 35,264원~** / 2.5 -> 52,909원~
  - 즉 08-05에 실제로 발화가 지연된 **마키나락스(25,900 x1.69)·GS건설
    (29,750 x1.82)은 둘 다 이 아래라 영향을 받지 않는다.** 그 구간까지 덮으려면
    MAX<=1.6이어야 하는데 2.4만원 이상이 한 값으로 뭉개진다 —
    근본 해결은 MAX가 아니라 ALPHA다(다만 ALPHA 0.40에선 밴드 편차가 9.8배로
    오히려 나빠졌다). 이 사실을 테스트로 못박아 "MAX 낮췄으니 해결됐다"는
    오해를 막았다.

  [분석 — 재버스트 기반 추가매수/캡 상향은 **하지 않기로**]
  사용자 질문("기존 burst 대비 대량으로 들어오면 추가매수하거나 동적캡을
  높일 수 있지 않나")에 대해 틱 아카이브 3일로 실측한 결과 **전부 근거가 없었다**:
  - **재버스트는 예측력 0.** 캡(+2.5%) 도달 시점 직전 90초의 재버스트 유무로
    갈라도 이후 +4% 도달률 **52% vs 50%**, 평균 최대상승 **7.05% vs 7.05%**로
    완전히 같다(n=23/8).
  - **캡을 높이면 오히려 크게 나빠진다**(본전스톱 ON 반영, 31건):
    | 정책 | 평균순익 | 중앙값 |
    |---|---|---|
    | **즉시 전량 2.5%(현행)** | **+2.27%** | +2.27% |
    | 상향캡 4.0% | +1.01% | **+0.20%** |
    | 상향캡 6.0% | +1.18% | **+0.20%** |
    | 50% 익절 + 3% 트레일 | +1.58% | +1.24% |
    중앙값이 +0.20%인 건 **과반이 본전스톱에 걸린다**는 뜻이다 — 캡 도달 종목의
    **77%가 본전까지 되돌아온다.** 즉 캡 상향과 본전스톱은 **서로를 무력화**한다.
    ⚠️ 처음 계산에서 본전스톱을 빼먹어 상향캡이 우세하게(+2.56%) 나왔었다.
    넣으니 뒤집혔다 — **정책 비교 시 본전스톱을 반드시 켜고 볼 것.**
  - **즉시 추가매수도 안 된다.** 재버스트 x2.0 이후 +5분 **-0.67%**(평균 최저
    -2.38%) / +10분 +1.07%로 부호가 지평에 따라 뒤집힌다 — 진입 트리거가 국소
    고점이라 되돌림 대기를 넣은 것과 **똑같은 문제**다. 한다면 즉시 매수가 아니라
    **새 진입 트리거로 취급해 되돌림 대기를 다시 거치게** 해야 한다.

  [🆕 `audit_20260805.py` 신설 — 기능이 아니라 **경로**를 본다]
  8개 섹션 59건: 임포트·구문·배선 / 오늘 바뀐 수치 정확성 / 상수 불변식 /
  **잘못된 매도가 나가지 않는가**(이상 가격 9종 + 상하한가 대조군) /
  **잘못된 매수가 나가지 않는가** / 청산 우선순위 실제 실행 / 백테스트 동기화 /
  핫패스 성능. 실행: `AUTOTRADER_TEST_LOG=1 python audit_20260805.py`
  - ⚠️ **스텁 반환 형식을 실물과 맞출 것.** 처음에 `__getattr__`로 전부 None을
    돌려주는 스텁을 썼다가 매도가 하나도 안 나가는 **가짜 통과/실패**를 만들었다
    (`get_index_change_rate`가 None -> `min()` TypeError). 스텁이 실물과 다르면
    감사 자체가 거짓말을 한다. 지금은 `test_patch_20260805.py`의 검증된 스텁과
    동일한 형식을 쓴다.
  - ⚠️ 감사 파일에서 **테스트 파일을 import하지 말 것** — 그 파일이 통째로
    실행되고 마지막 `sys.exit()`에서 감사가 끊긴다(실제로 겪었다).
