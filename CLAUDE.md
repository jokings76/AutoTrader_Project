\# AutoTrader\_Project 작업 규칙



\## 기본 정보

\- 환경: ⚠️ **실전투자 (2026-08-05부터, `IS_MOCK=false`)**. 왕복수수료 0.23%
&#x20; — 실전 위탁계좌(번호는 `config.ini` 참조, git 미추적), 종목당 매수 **100만원**
&#x20; (08-05 전환 시 50만원 -> 같은 날 장마감 후 200만원 환원. 실지배값은
&#x20; `portfolio_optimizer.DEFAULT_BASE_AMOUNT` — **3곳이 같이 움직여야 한다**).
&#x20; **이 계좌에는 봇과 무관한 사용자 보유 종목이 있다**(우리기술/엑스게이트) —
&#x20; 봇은 DB 기반으로만 청산하므로 건드리지 않지만, 잔고를 볼 때 헷갈리지 말 것.
&#x20; 상세는 히스토리 "2026-08-05 실전투자 전환" 참고.

\- 구조: asyncio + PostgreSQL (Docker `analytics\_pg` 컨테이너, 자동재시작 설정됨)

\- 사용자: 다윤, 한국어로 소통

\- 텔레그램 채널: 오토트레이더(order, 매수/매도 체결·실패), 주식 따릉이(signal, 시스템 로그/정기보고),

&#x20; 종가베팅 포착방(closing\_bet, 종가베팅 TOP10)



\## 개발/운영 환경 (2026-08-01 구축)

\### 실행 환경
- 프로젝트 경로: `C:\Users\rober\OneDrive\문서\vscode\AutoTrader_Project`
- 스케줄러용 junction 경로: `C:\AutoTrader_Bot\ProjectRoot`
  (**비대화형/S4U 컨텍스트에서 실행되는 배치파일은 반드시 이 경로를 쓸 것** —
   실제 경로의 "문서"(한글) 폴더명이 cmd.exe 파싱을 깨뜨린다. 2026-07-28 참고)
- 자동 기동: Windows 작업 스케줄러 `AutoTrader_Start`, 매일 **08:59**
- DB: Docker `analytics_pg` 컨테이너 (PostgreSQL, 자동재시작)

\### Claude Code CLI (2026-08-01 신규 설치)
| 항목 | 버전/경로 |
|---|---|
| Node.js | v24.18.1 (winget `OpenJS.NodeJS.LTS`, `--scope user`) |
| npm | 11.16.0 |
| claude CLI | 2.1.220 (`npm install -g @anthropic-ai/claude-code`) |
| claude 실행파일 | `...\WinGet\Packages\OpenJS.NodeJS.LTS_...\node-v24.18.1-win-x64\claude.ps1` |
| 로그인 방식 | Claude account with subscription (Claude Pro) |

설치 중 실제로 걸렸던 것 (다시 설치할 일 있으면 참고):
- `winget search`가 msstore 원본 약관 동의 프롬프트에서 멈춘다 →
  `--accept-source-agreements` 플래그 필수.
- npm이 `allow-scripts` 정책으로 postinstall(`node install.cjs`)을 건너뛴다는
  경고를 낸다. **core 기능에는 영향 없음**(`claude --version`/`--help` 정상 확인).
  `npm approve-scripts`는 전역 설치에 안 먹히므로(EGLOBAL) 무시하면 된다.
- 설치 직후 PATH는 **새 터미널을 열어야** 반영된다.

\### 08:59 자동 기동 시 원격제어 세션 동시 실행 (2026-08-02 신규)
작업 스케줄러가 봇을 띄울 때 **별도 창으로 `claude --remote-control` 세션도
같이 시작**한다. 장중에 PC 앞에 없어도 폰에서 상태를 물어볼 수 있게 하는 용도.

- `start_trader.bat`이 `main.py`보다 **먼저** `start`로
  `start_remote_control.ps1`을 띄운다. `start`는 즉시 반환하므로 봇 기동을
  막지 않고, 봇이 죽어도 원격제어는 살아있어 원인 진단이 가능하다.
- 세션 이름을 `autotrader`로 고정했다 — 자동 생성 이름(`remote-control-keen-thimble`
  같은 것)은 매번 바뀌어 폰 앱 목록에서 찾기 번거롭다.

**이 조합에서 실제로 밟은 함정 4가지 (다시 건드릴 때 반드시 확인):**
1. `.ps1`을 **BOM 없는 UTF-8로 저장하면 안 된다.** PowerShell 5.1이 cp949로
   읽어 한글이 깨지고 `string is missing the terminator` 구문 오류가 난다.
   UTF-8 **BOM 필수**. (편집 도구가 BOM을 떼는 경우가 있으니 저장 후 재확인)
2. `.bat`에는 **주석이라도 한글을 넣지 말 것** — 2026-07-28에 겪은 것과 같은
   원인으로 깨진다. 비ASCII 0바이트인지 확인하고 커밋할 것.
   한글 경로 처리는 전부 `.ps1` 안으로 몰아넣었다.
3. claude의 **폴더 신뢰 승인은 '실제 경로' 기준**으로 저장돼 있다. junction
   경로(`C:\AutoTrader_Bot\ProjectRoot`)로 시작하면 다른 폴더로 보고 신뢰
   프롬프트를 다시 띄울 수 있는데, 무인 기동에서 프롬프트가 뜨면 거기서 멈춘다.
   -> `.bat`은 ASCII junction만 참조하고, `.ps1`이 **실제 경로로 Set-Location**.
4. `scheduler_debug.log`는 cmd.exe가 ASCII로 쓰는 파일이라 한글(UTF-8)을 섞으면
   한 파일에 두 인코딩이 공존해 전부 깨져 보인다.
   -> 파일에 남기는 로그는 ASCII, 화면 출력만 한글로 분리했다.

수동으로 원격제어만 띄우고 싶을 때:
```
powershell -NoProfile -ExecutionPolicy Bypass -File "C:\AutoTrader_Bot\ProjectRoot\start_remote_control.ps1"
```

\### 모바일 원격 제어 (PC 세션을 폰에서 조작)
```
cd "C:\Users\rober\OneDrive\문서\vscode\AutoTrader_Project"
claude --remote-control
```
- ⚠️ **`claude remote-control`(하위명령)이 아니라 `--remote-control`(플래그)이다.**
  공식 문서 표기와 다르니 주의. 세션 중이면 `/rc`로도 가능.
- 실행하면 `https://claude.ai/code/session_XXXXXXXX` 형태의 URL이 뜬다.
  폰 Claude 앱 또는 폰 브라우저로 그 URL을 열면 연결된다.
- 폰 앱에서는 **[코드] 탭 → 세션 카드(예: `remote-control-keen-thimble`,
  `🖥 연결됨 · jokings76/AutoTrader_Project`)를 탭**해야 채팅창으로 들어간다.
  목록 화면만 보고 "연결이 안 됐다"고 오해하기 쉬움.
- **코드 실행·파일 접근은 전부 PC에서** 일어난다(클라우드 실행 아님).
  따라서 PC가 켜져 있고 **그 터미널 창이 열려 있어야** 하며, 창을 닫으면 끊긴다.
- 모바일에서는 **새 MCP 서버를 추가할 수 없다** — 필요한 건 PC에서 미리 세팅.
- 원격 세션은 기본 Sonnet 5로 시작한다. 전략 로직처럼 복잡한 작업은
  세션에서 `/model` 입력해 **Opus 5로 바꾸고 진행할 것**.
- ⚠️ **실거래 봇이 돌고 있는 장중에 모바일에서 매매 로직 파일을 고치지 말 것.**
  고칠 거면 이 문서 "작업 방식"의 surgical 패치 + import 검증 규칙을 그대로 지킨다.

\#### 세션 연속성 — 매일 전날 대화를 이어간다 (2026-08-02 변경)
`start_remote_control.ps1`이 기본으로 **`--continue`**를 붙인다. 08:59 자동기동도,
장중 자동재시작도 전부 직전 대화를 그대로 이어받는다. 폰 앱에 뜨는 URL은 매번
새로 생기지만 **대화 내용은 이어진다**(세션 이름 `autotrader` 고정).

- **대화가 길어져 한도가 걱정될 때**: 세션 안에서 "이관작업 해줘"라고 요청해
  CLAUDE.md에 맥락을 정리시킨 뒤, 프로젝트 루트에 빈 파일
  **`NEW_SESSION_REQUESTED`**를 만들어 둔다. 다음 기동 **딱 1회만** 새 대화로
  시작하고 플래그는 즉시 삭제되어, 그 다음부터는 다시 자동으로 이어간다.
  (`STOP_SIGNAL`과 완전히 같은 방식. 둘 다 `.gitignore` 대상 — 커밋되면
  체크아웃마다 되살아나 매번 대화가 끊긴다.)
  ```
  New-Item "NEW_SESSION_REQUESTED" -ItemType File
  ```
- **대화 내용은 PC를 꺼도 사라지지 않는다.** 메시지마다
  `~/.claude/projects/<프로젝트>/<세션ID>.jsonl`에 즉시 기록된다(OneDrive 폴더
  **밖**이라 동기화 충돌과도 무관). PC를 끄면 "일시 중단"이지 "소실"이 아니다.
- **자동 재시작**: 모바일에서 세션 '종료'를 눌러도(실수로 두 번 눌러도) 런처가
  5초 뒤 새 세션을 자동으로 띄운다(최대 30회). 예전엔 여기서 창이 프롬프트에
  멈춰, PC 앞에 사람이 없으면 그날 내내 폰 접속이 끊긴 채 방치됐다.
  **완전히 끄려면 PowerShell 창 자체를 닫을 것** — 모바일 '종료' 버튼은 세션만
  끝낼 뿐 창을 못 닫는다. 5초 미만 즉시종료가 3회 연속이면 클릭 실수가 아니라
  설정/인증 문제로 보고 자동재시작을 멈춘다.
- **감시**: `main.task_remote_control_watchdog`가 09:06에 1회 프로세스 존재를
  확인해, 없으면 텔레그램(signal)으로 알린다. 매매와는 완전히 별도 프로세스라
  원격제어가 죽어도 **봇 매매에는 영향이 없다**(알림 목적은 "오늘 폰으로 못 본다"를
  그날 안에 아는 것).

\## 작업 방식 (반드시 지킬 것)

1\. \*\*surgical 패치 우선\*\*: 파일 전체 재작성보다 "찾기 → 교체" 블록 단위로 제시. 

&#x20;  불가피하게 큰 함수 전체를 재구성해야 할 때만 함수 단위 통째 교체.

2\. \*\*파일 삭제 금지\*\*: 기존 파일은 지우지 않고 `\_legacy` 접미사로 백업 후 교체.

3\. \*\*검증 필수\*\*: 코드 변경 직후 반드시 `python -c "import \[모듈]; print('OK')"`로 

&#x20;  import 성공 여부 확인. 여러 파일을 고쳤으면 각각 검증.

4\. \*\*큰 변경 전 드라이런 권고\*\*: 특히 매매 로직(진입/청산/점수 계산) 변경은 

&#x20;  장 시작 전이나 소규모 테스트 권장.

5\. \*\*비동기 코드 주의\*\*: 메인 루프를 막는 `sleep` 절대 금지 (WS 연결 끊김 원인이었던 적 있음).

&#x20;  `asyncio.sleep`만 사용.

6\. \*\*인덴트 안전\*\*: Python 들여쓰기가 걸린 수정은 특히 신중하게 — 

&#x20;  부분 문자열 교체보다 함수/블록 전체를 정확히 교체하는 방식을 우선 고려.

7\. \*\*수정 후 백업 병행\*\* (2026-07-28): 그날 코드 수정이 어느 정도 쌓이면(세션

&#x20;  마무리 시점 등) git 커밋 + 날짜별 파일 스냅샷 백업을 같이 남긴다.

&#x20;  - git: 의미 단위로 커밋(민감정보 커밋 전 확인 — config.ini/settings.py/.env는

&#x20;    이미 .gitignore 처리됨, 그 외 파일에 하드코딩된 키/비밀번호 없는지 훑을 것).

&#x20;  - 파일 스냅샷: 프로젝트 폴더 밖 `AutoTrader\_Project\_backups\YYYY-MM-DD\`에

&#x20;    전체 복사(.git, \_\_pycache\_\_ 제외). Windows 작업 스케줄러에도 매일 자동

&#x20;    실행 등록(대화 세션과 무관하게 돌아감) — 스크립트는

&#x20;    `C:\Users\rober\OneDrive\문서\vscode\daily\_backup.ps1`.



\## 히스토리 (참고용, 과거 세션 요약)

\- 2026-07-24 ~ 07-31: **[아카이브] `docs/HISTORY_2026-07.md` 참고.**
  Phase3 재설계 / VWAP 5종 게이트 / 전략 1A+Pullback+1B+1L 체제 개편 /
  스케줄러 한글경로 junction 해결 / 09:20 게이트 회귀 / 동적 익절캡 도입 /
  틱 아카이빙 인프라 / 조건검색식 3개 체제 전환 등.
  ⚠️ 그 시기 전략(1S/Phase2/Phase3/1N/1B/1L)은 **전부 삭제됐다** —
  지금 살아있는 건 1A뿐이다(눌림목은 08-06 [E]로 슬롯 0).

- 2026-08-01 ~ 08-02: **[아카이브] `docs/HISTORY_2026-08-01_02.md` 참고.**
  치명 버그 9건 수정 / 틱 구동 전면 전환 / 1B·1L 코드 완전 삭제 /
  통합 드라이런 테스트 신설 / 프로젝트 파일 정리.
  ⚠️ 그 시기 결정 중 **무장 3초**는 08-03에 1.5초로 완화됐다가
  08-07에 실측으로 3초 복원됐다. 지금 사양은 CLAUDE.md 본문을 볼 것.

- 2026-08-03 ~ 08-04: **[아카이브] `docs/HISTORY_2026-08-03_04.md` 참고.**
  익절 구조 재설계(조기확정 폐지 -> 본전스톱 + 캡 상향) / 강도 기준선 리앵커 /
  손절을 워밍업 위로 / 시간대 계수 전 구간 1.00 / 지수 하락 가드 신설 /
  되돌림 대기 분할매수 + 분할매도 / 버스트 문턱 4천만 / 테스트 로그 분리.
  ⚠️ 그 시기 값 중 **무장 3초는 08-03에 1.5초로 완화됐다가 08-07에 복원**,
  **상승이탈은 08-06 OFF**, **눌림목은 08-06 [E]로 슬롯 0**이다.
  지금 사양은 반드시 본문 "현재 전략 요약"을 볼 것.

- 2026-08-05 (실전 전환일): **[아카이브] `docs/HISTORY_2026-08-05.md` 참고.**
  실전투자 전환(토큰 URL 하드코딩·예수금 필드·매수금액 3곳 결함) / 조건검색
  거래소구분 / 장중 수술 7건(손절 대신 추가매수·되돌림 ±0.3%·시장가 문턱) /
  매도시점 3건(손실반등 분할·시가필터 부활·조건부 재매수) / 버스트 주가 스케일 /
  VI 상단 확정매도 / 감사 스위트 신설과 결함 3건.
  · **실전 첫날 결산: 청산 20건 / 실현 +9,796원 / 평균 +0.41%**
  · 🔴 **지금도 유효한 결론 3가지**(캡을 만질 때 반드시 읽을 것):
    ① **캡 도달 종목의 77%가 본전까지 되돌아온다** — 캡 상향과 본전스톱은
       서로를 무력화한다. 정책 비교는 **본전스톱을 켠 상태로** 할 것.
    ② **재버스트는 예측력이 0**이다(+4% 도달률 52% vs 50%).
    ③ **예수금은 `100stk_ord_alow_amt`** — 20/30/40/50/60stk 계열은 미수다.
  · ⚠️ 그 시기 값 중 **매수금액 100만원 / 주가계수 상한 3.00 / 조건검색 "K" /
    무장 3.0초 / 상승이탈 OFF / VI 매수차단 OFF**로 바뀌었다.

- 2026-08-06 (09:40, **이관 문서 작성 — 수정은 다음 세션으로 이월**) —
  사용자 지시로 코드 수정을 멈추고 이관 문서부터 정리했다. 이 시점의 상태:

  [상태] 봇 정지(09:19:01) / 미청산 3종목 무방비 / 결함 2건 미수정.
  08-06 결산: 매수 19 · 청산 16 · **실현 -138,152원** · 평균 -0.68% · 승 4/16 · 손절 7.

  [오늘 적용된 것]
  · 버스트 주가 스케일 상한 **2.0 -> 3.0**(클램프 시작 35,264 -> 73,704원).
    10만원대 계수 2.00 -> 3.00, 절대2건 1.6억 -> 2.4억, 단일 2.0억 -> 3.0억.
    08-06 소급 시 **영향받는 4건이 전부 손실/0%**(-6.13%)이고 **승자 4건은 전부
    클램프 밖이라 불변**이다. ⚠️ 3만원대(GS건설 32,600)는 클램프 밖이라 안 바뀐다.
  · (오전) VI 매수차단 OFF / 시가대비 5% -> 8% / 6% 이상 버스트 1.5배 강화.

  [미수정으로 이월한 결함 2건 — "다음 할 일" [A][B]에 재현·수정·검증 절차 상세]
  · **[A] `_today_open`이 장중 재편입에서 틀린 시가를 반환한다.** 금호타이어
    09:18 매수가 시가대비 +14.03%인데 8% 컷을 통과했다(진짜 시가 7,200 vs
    반환값 7,650 = 09:03봉). 09:18에 분봉 15개면 창이 09:03~이라 09:00봉이 없다.
    게다가 호출부가 `open_price or 캐시`라 **틀린 새 값이 정확한 캐시를 이긴다.**
  · **[B] 버스트가 매도 체결을 구분하지 않는다.** `count_large_trades`/
    `max_single_trade_value`가 side를 버린다(`for ts, price, _, volume in d`).
    무장(FID 228)은 당일 누적이라 오전 강세 종목은 급락 중에도 100 위에 머문다.
    -> **오전에 강했던 종목이 투매로 무너지는 순간이 매수 신호**가 된다.
    이게 "-%에서 매수"의 진짜 원인이다.

  [실측 — 급속 손절과 버스트 문턱의 관계]
  | 보유 | 종목 | 주가 | 계수 | 문턱(2건) | 손실 |
  |---|---|---|---|---|---|
  | **16초** | KBI메탈 | 5,360 | 0.71 | 0.57억 | -3.17% |
  | **32초** | 빛과전자 | 3,113 | 0.53 | 0.42억 | -3.15% |
  | 66초 | 대원전선 | 15,580 | 1.28 | 1.02억 | -3.02% |
  | 73초 | 에스피지 | 117,800 | 2.00 | 1.60억 | -3.06% |
  | 229초 | 심텍 | 113,900 | 2.00 | 1.60억 | -3.07% |
  **1만원 미만 손절 2건 평균 24초 / 1만원 이상 5건 평균 116초** — 문턱이 낮을수록
  5배 빨리 죽는다. 다만 고가주도 손절 5건이라(3만원 이상 6건 평균 **-1.46%**,
  승률 1/6) 문턱만의 문제는 아니다.

  [⚠️ 정지 시 배운 것] **보유가 있을 때 `STOP_SIGNAL`을 넣으면 그 포지션은
  무방비가 된다** — 손절·익절·15:10 강제청산이 전부 멈춘다. CLAUDE.md에 예전부터
  "보유가 있으면 절대 하지 말 것"이라고 적혀 있었지만, 이번엔 손실이 진행 중이라
  사용자가 정지를 우선했다. **정지 후 즉시 보유 목록을 알려 수동 처리하게 하는
  것**이 올바른 대응이다(이번에 그렇게 했다).

  [검증] 9개 스위트 **1,088건 전원 통과**. 코드 변경은 버스트 상한 1건뿐이다.

- 2026-08-06 (09:18, **장중 긴급 정지 + 원인 분석 2건**) — 개장 18분에 20건 매수·
  손절 7건이 나와 사용자 지시로 `STOP_SIGNAL` 정지(09:19:01 종료). ⚠️ 정지 시점에
  **보유 3종목이 남아 무방비**가 됐다(주성/다스코/금호타이어, 340만원) — 보유가
  있을 때 정지하면 손절·강제청산이 전부 멈춘다는 걸 다시 확인.

  [🔴 결함 ① `_today_open`이 장중 재편입에서 틀린 시가를 반환한다]
  금호타이어가 09:18:21에 **시가대비 +14.03%**로 매수됐다(8% 컷을 통과).
  · 진짜 당일 시가(09:00봉) = **7,200원** -> +14.03% -> 차단됐어야 함
  · `_today_open()`이 반환한 값 = **7,650원**(09:03봉 시가) -> +7.32% -> 통과
  원인: 09:18에 분봉 15개를 받으면 창이 **09:03~09:18**이라 09:00봉이 없다.
  `_today_open`은 "조회한 봉 중 오늘 것 중 가장 이른 봉"을 시가로 쓰므로
  09:03봉을 당일 시가로 착각한다. **개장 직후만 맞고 장중 재편입에선 틀린다.**
  🔴 **두 번째 겹**: `on_condition_hit`이
  `open_price = open_price **or** self._opening_prices.get(...)`로 쓴다 —
  09:00:55에 정확히 캐시된 7,200이 있는데도 **갓 계산한 틀린 값이 우선**한다.
  -> 고칠 것: (A) 창에 09:00봉이 없으면 `0.0`(판단포기) 반환
             (B) 호출부를 `or`가 아니라 **캐시 우선**으로
  ※ 이 필터는 08-05까지 캐시 미충전으로 죽어 있다가 08-06에 처음 살아났다.
    08-01에 함수를 만들 때 "개장 직후"만 검증하고 장중 재편입을 놓친 것이다.

  [🔴 결함 ② 버스트가 매도 체결을 구분하지 않는다]
  `count_large_trades`/`max_single_trade_value`가 `for ts, price, _, volume in d`로
  **side를 버린다.** 대량 투매도 버스트로 센다. 무장(FID 228)은 **당일 누적**이라
  오전에 강했던 종목은 급락 중에도 100 위에 머문다.
  -> **오전에 강했던 종목이 대량 투매로 무너지는 순간이 매수 신호**가 된다.
     이게 "-%에서 매수"의 구조적 원인이다. (미수정 — 매수 로직이라 신중히)

  [급속 손절 실측 — 문턱이 낮을수록 빨리 죽는다]
  | 보유 | 종목 | 주가 | 계수 | 문턱(2건) | 손실 |
  |---|---|---|---|---|---|
  | **16초** | KBI메탈 | 5,360 | 0.71 | 0.57억 | -3.17% |
  | **32초** | 빛과전자 | 3,113 | 0.53 | 0.42억 | -3.15% |
  | 66초 | 대원전선 | 15,580 | 1.28 | 1.02억 | -3.02% |
  | 73초 | 에스피지 | 117,800 | 2.00 | 1.60억 | -3.06% |
  | 229초 | 심텍 | 113,900 | 2.00 | 1.60억 | -3.07% |
  **1만원 미만 손절 2건 평균 24초 / 1만원 이상 5건 평균 116초** — 문턱이 낮을수록
  5배 빨리 죽는다. 다만 **고가주도 손절 5건**이라 문턱만의 문제는 아니다
  (3만원 이상 6건 평균 **-1.46%**, 승률 1/6).

  [적용 — 버스트 주가 스케일 상한 2.0 -> **3.0**]
  클램프 시작가 35,264 -> **73,704원**. 10만원대 계수 2.00 -> 3.00
  (절대2건 1.6억 -> **2.4억**, 단일 2.0억 -> **3.0억**).
  · 심텍 113,900 단일 **2.93억** -> 새 단일문턱 3.00억에 **탈락**(그 건 -3.07%)
  · 주성 141,100 단일 2.04억 -> 탈락
  · 08-06 소급: **영향받는 4건이 전부 손실/0%**(합계 -6.13%)이고
    **승자 4건(코스모 +3.17 / 금호타이어 +2.85 / 현대약품 +4.25 / GS건설 +0.46)은
    전부 클램프 밖이라 그대로 통과**한다.
  ⚠️ **한계**: 3만원대는 클램프 밖이라 안 바뀐다(GS건설 32,600원 계수 1.92,
    그날 -3.07%). 그 구간까지 올리려면 ALPHA를 올려야 하는데 그러면 2만원대까지
    조여져 그날 승자 코스모(버스트가 문턱에 딱 걸쳐 있었다)가 탈락한다.
    그래서 **의도적으로 MAX만** 올렸다.

  [검증] 9개 스위트 **1,088건 전원 통과**.

- 2026-08-06 (08:25, **VI 매수차단 OFF + 시가대비 8% 완화 + 6% 이상 버스트 강화**,
  사용자 지정) — 08-05 소급 검증 결과를 보고 진입 필터를 재조정했다.

  [🔴 검증이 뒤집은 것] 08-05 실매수 20건에 두 필터를 소급하니:
  | 구분 | 건수 | 합계% | 실현금액 |
  |---|---|---|---|
  | 전체(실제) | 20 | +8.16% | **+9,796원** |
  | 차단분 | 2 | +3.58% | **+12,140원** |
  | 남는 것 | 18 | +4.58% | **-2,344원** |
  **두 필터가 정확히 같은 2종목을 막고, 그 2건이 하루 실현금액의 124%**였다
  (아진엑스텍 +12,980원 / PS일렉 -840원). 적용하면 08-05가 **+9,796 -> -2,344원**
  으로 뒤집힌다. -> VI 매수차단은 **끄고**, 시가대비는 **5% -> 8%**로 완화.

  [🔬 VI 모델은 실증됐다 — 끄는 건 성과 판단이지 모델 오류가 아니다]
  차단 대상 2종목의 1분봉에서 **실제 VI 정지 흔적**을 찾았다:
  · 059120 아진엑스텍(시가 6,310 -> VI상단 6,941): 14:34·14:35 고가가 **6,940**
    (VI선 1원 아래)에서 멈추고 **14:36 분봉이 통째로 결손** -> 14:37 6,960 재개.
    **우리 봇은 14:34:13에 6,870에 사서 그 정지 구간에 실제로 갇혔다.**
  · 332570 PS일렉(시가 8,550 -> VI상단 9,405): 09:40 고가 **9,400** -> **09:41·09:42
    2분 결손**(한국 VI 규칙과 정확히 일치) -> 09:43 9,690 재개.
  즉 `정적VI 상단 = 시가 x 1.10` 모델은 맞다. 되살릴 땐 플래그만 True.

  [⚠️ 끈 또 하나의 이유 — 과잉차단] 332570은 09:41~42에 **이미 VI가 발동·해제**돼
  거래소 기준가가 갱신된 상태였다. 그런데 우리는 여전히 시가 기준으로 계산해
  10:24 매수를 막았다 — 위험이 지나간 종목을 막은 것이다. 근본 해결은 당일 고가를
  캐시해(ka10001 `high_pric`, REST 추가 0콜) "이미 시가x1.10을 넘었으면 기준가가
  갱신된 것으로 보고 차단 해제"하는 것. **되살릴 때 반드시 같이 할 것.**

  [신설 — 시가대비 6% 이상 버스트 강화] `PHASE1A_OPEN_SURGE_STRICT_FROM=6.0` /
  `PHASE1A_OPEN_SURGE_BURST_MULT=1.5`. 매수를 막지 않고 **대금만 더 요구**한다.
  시간계수·주가계수와 같은 방식으로 `check_burst` 한 곳에서 합성된다
  (`surge_mult`로 detail에 기록 — 사후 추적용).
  · 배수 1.5의 근거: 08-03~04 32건의 버스트 규모별 성과가
    `0.8억 미만 승률 37% / 0.8~1.5억 62% / 1.5~3.0억 50%`다. 기본 문턱
    (4천만x2건=0.8억)에 1.5배면 **1.2억**이 되어 37% 구간을 확실히 벗어난다.
    2.0배(1.6억)도 가능하나 그 위는 표본 4건뿐이라 더 올릴 근거가 없다.
  · 시가를 모르면 **강화하지 않는다**(1.0으로 수렴) — 모름이 문턱을 흔들지 않게.

  [⚠️ STRICT_FROM=6.0은 데이터로 고른 값이 아니라 사용자 지정이다]
  08-03~05 청산 36건의 시가대비 구간별 성과:
      ~3%   21건 +0.48% 승률 52% / 3~6% 12건 **-0.75%** 승률 33% / 6%~ 3건 +1.24%
  처음엔 이걸 보고 "정작 나쁜 구간은 3~6%"라고 판단했으나 **그 판단은 틀렸다.**
  🔴 **날짜로 쪼개면 부호가 뒤집힌다 — 구간 효과가 아니라 날짜 효과다:**
      3~6% 구간:  08-04 6건 **-2.22%**  /  08-05 6건 **+0.72%**
      ~3% 구간 :  08-04 3건 **+1.83%**  <- 같은 날 ~3%는 오히려 좋았다
  즉 "3~6%가 나쁘다"가 아니라 "08-04에 그 6종목이 나빴다"이다. 08-04 손절 4건이
  09:11~09:28에 몰려 있는 것도 같은 이야기다.
  ⚠️ **구간별 평균을 볼 때는 반드시 날짜로 쪼개 볼 것.** 3일 36건에서 하루가
  10~20건이라 하루의 색깔이 구간 평균을 그대로 지배한다.
  부수 확인: 상대 경로를 빼면 3~6% 열위가 -0.75% -> **-0.26%**로 줄어든다
  (상대 경로는 08-05 주가 스케일 도입으로 이미 사실상 무력화됐다).
  더 강해 보이는 축은 오히려 **매수 시각**이지만(~09:10 +0.38% / 09:10~09:30
  **-0.81%** / 09:30~ +1.67%) 이것도 08-04 편중과 겹쳐 아직 판단 불가다.

  [검증] 9개 스위트 **1,085건 전원 통과**. 옛 5% 기준을 검증하던 3건(20260801)은
  8% 기준으로 교체 — 테스트가 사양 변경을 잡아낸 정상 동작이다.

- 2026-08-06 (08:15, **VI 상단 근접 매수차단 신설**, 사용자 지정) —
  "무장·버스트를 만족해도 VI 상단 3% 근접에서는 매수차단하고 이하에서만 매수".

  [왜] 무장·버스트가 다 맞아도 VI 상단 코앞에서 사면 세 가지가 겹친다:
  ① VI가 걸리면 2분간 단일가매매라 **산 직후 손도 못 대는 구간**에 갇힌다
  ② 위쪽 여유가 VI까지밖에 없어 **익절 캡(4.0%/2.5%)에 도달할 공간이 없다**
  ③ 매수하자마자 `VI_UPPER_MARGIN_PCT`(0.5%) 확정매도에 걸려 **수수료만 내고
     되파는 회전**이 된다 — 매수·매도 규칙이 서로 싸우는 상태였다.

  [구현] `VI_UPPER_ENTRY_BLOCK_ENABLED / VI_UPPER_ENTRY_BLOCK_PCT(0.03)`.
  판정은 **`vi_entry_block_reason()` 단일 함수**로 모으고, 호출은 두 곳:
  `_open_entry_plan`(계획 생성 전 — 살 수 없는 종목이 슬롯을 120초 묶는 것 방지)과
  `_execute_buy`(주문 직전 하드가드 — 호출부가 3곳이고 상승이탈로 VI에 붙는
  경우가 있다). **호출부가 상수를 직접 보지 못하게** 테스트로 못박았다.

  [실효 지점] VI 상단 = 시가 x1.10이므로 차단 시작가는
  `시가 x 1.10 / 1.03 = 시가 x 1.0680` — 즉 **시가 대비 +6.80%**부터다.
  ⚠️ 1A엔 이미 '시가대비 +5% 매수보류'가 있어 **1A에선 그쪽이 먼저 걸린다**
     (5% < 6.8%). 이 규칙이 실질적으로 새로 막는 건 **Pullback 경로**다
     (눌림목자동엔 시가대비 필터가 없다).

  [🔴 규칙 충돌을 테스트가 잡았다] `test_patch_20260802`의 "눌림목엔 시가대비
  보류를 적용하지 않음"이 실패했다. 그 테스트가 시가 9,000 / 현재가 10,000을
  쓰는데, 10,000은 **정적VI 상단(9,900)을 이미 넘은** 가격이라 새 규칙에도
  걸린 것이다. 두 규칙이 겹쳐 어느 쪽이 막았는지 구분이 안 되는 상태였다.
  -> 시가를 **9,450**으로 바꿔 분리했다(시가대비 +5.82%로 1A 보류는 걸리고,
     VI 상단 10,395까지 3.95% 여유라 VI 차단은 안 걸린다). 그리고
     **"눌림목이라도 VI 3% 이내면 차단"**을 별도 케이스로 추가했다 —
     시가대비 보류는 전략별이지만 **VI 차단은 전략 무관**이라는 게 핵심 차이다.

  ⚠️ **시가를 모르면 차단하지 않는다.** 막아버리면 시가 캐시가 비는 순간
  하루 종일 매수 0건이 되는데 로그를 뒤지기 전엔 안 보인다(08-05에 시가대비
  필터가 정확히 그 이유로 하루 종일 0건이었다). '모름'이 매수를 막지도
  열어주지도 않게 두고, 대신 진단 분류 `VI 상단 근접 매수차단`으로 관측한다.

  [검증] 9개 스위트 **1,070건 전원 통과**(294/89/126/66/126/163/24/63/119).
  감사에 불변식 추가 — **매수차단 폭(3%) > 확정매도 폭(0.5%)**이어야 한다
  (뒤집히면 사자마자 되파는 구조가 된다).

- 2026-08-06 (07:30, **장 시작 전 최종 점검**) — 새 코드 변경 없음. 검증만 강화했다.
  - **`audit_20260805.py`에 섹션 [9][10][11] 신설** (59 -> **115건**):
    · **[9] 문서 ↔ 코드 정합성** — CLAUDE.md의 '기대값' 블록에 라이브 상수값이
      **문자열 그대로** 있는지 기계적으로 대조한다(시간창/슬롯/무장/버스트/주가계수/
      재매수배수/되돌림/분할매도/지수가드/캡/본전스톱/매수금액/VI/등락률상한 등 28항목).
      **"매일 문서와 코드가 어긋난다"는 문제의 구조적 해결책** — 상수만 고치고
      문서를 안 고치면 여기서 실패한다.
    · **[10] 청산 규칙 상호 충돌 매트릭스** — 규칙을 하나씩 단독 성립시켜 **엉뚱한
      사유로 나가지 않는지** 14케이스(손절/익절캡/VI/가드/워밍업 조합 + 경계 1원).
    · **[11] 매수 게이트 순서** — 하드컷오프·등락률·라우팅·가드·pending 회수가
      `_execute_buy`에 살아있는지 + `_entry_block_reason` 4상태 반환값 검증.
  - ⚠️ **소스 문자열 단언은 오탐을 낸다** — 처음에 `check_burst`/`resolve_strategy`/
    `isolat`를 소스에서 찾다가 **3건이 가짜 실패**로 잡혔다(버스트는
    `evaluate_tick_entry`를 거쳐 호출되고, 격리 판정은 `quarantine_until`을 쓴다).
    -> 전부 **실제 호출·반환값 검증으로 교체**했다. 감사는 문자열이 아니라 동작을 봐야 한다.
  - [확인] 9개 스위트 **1,041건 전원 통과**(294/87/126/66/103/163/24/63/115,
    실패 1건은 문서화된 이관 플래그).
  - [확인] `daily_backtest` 08-05 기준 강제 실행 -> 4건 재현·예외 0·새 헤더 렌더링 정상.
    ※ 08-06 아침에 그냥 돌리면 "오늘 신호 종목 없음"이 정상이다(당일 데이터가 아직 없음).
  - [확인] 기동 인프라: STOP_SIGNAL 없음 / python 프로세스 0개 /
    스케줄러 `State=Ready NextRun=08/06 08:59:00` / `.bat` 비ASCII 0바이트 /
    `.ps1` UTF-8 BOM / junction 정상 / `IS_MOCK=False`.
  - [확인] 테스트 로그 분리 — 전수 실행 후 `autotrader.log` 크기 불변(5,981,307바이트).

  [검증] 9개 스위트 **985건 전원 통과**
  (294/87/126/66/103/163/24/63/59, 실패 1건은 문서화된 이관 플래그).
  `daily_backtest` 실제 실행(4건 재현, 예외 0) / `import main` OK /
  전체 .py 구문오류 0 / 태스크 정의 21 = gather 등록 21 / 지연 import 심볼 전수 실존.
  핫패스: `check_burst` 0.22ms, `on_price_update` **3.8us**(VI 추가 후에도 무변화).
  기동 인프라: STOP_SIGNAL 없음 / python 프로세스 0개 / `.bat` 비ASCII 0바이트 /
  `.ps1` UTF-8 BOM / junction 정상 / 스케줄러 08-06 08:59 준비 / `IS_MOCK=false`.
  테스트 로그 분리 확인 — 전수 실행 후에도 `autotrader.log` 크기 불변.

- 2026-08-06 (10:00~10:30, **정지 상태에서 [A][B][C][D] 전부 수정**) —
  아침 이관 문서가 미수정으로 남겨둔 결함 2건과 보류 항목 2건을 순서대로 처리했다.
  상세 근거·검증 절차는 "다음 할 일"의 [A]~[D] 항목에 있다. 여기엔 **분석 과정에서
  드러난 것**만 남긴다.

  [🔴 [D] 판단이 아침 문서와 달랐다 — 로그를 정답으로 삼아 뒤집었다]
  - 아침 문서는 대원전선/KBI메탈/빛과전자를 상승이탈로 지목했는데, 처음에
    `entry_reason`의 `(현재가 N)`을 트리거가로 보고 역산하니 **셋 다 되돌림으로**
    나왔다(매수가가 그 값보다 낮았다). 아침 문서가 틀린 것처럼 보였다.
  - 실제로는 **내 역산이 틀렸다.** `(현재가 N)`은 트리거 시점이 아니라 그 로그를
    찍는 순간의 값이라 계획의 `trigger_price`와 다르다. 로그의
    `되돌림 포기 — 상승 이탈 (트리거 A원 -> B원)` 줄을 정답으로 삼으니 아침
    문서가 맞았다(15건 발동).
  - **교훈: 진입 경로를 사후 판정할 땐 가격 역산이 아니라 그 경로가 직접 남긴
    로그 줄을 쓸 것.** DB의 `entry_reason`만으로는 경로를 복원할 수 없다.

  [🔴 상승이탈은 08-06 하루만 존재한다 — 이틀 대조가 불가능했다]
  - 08-05 13:54에 도입됐지만 그날 발동 **0건**이라, 실거래 표본이 있는 날이
    08-06 하루뿐이다(n=9). 이 프로젝트의 "양일 각각 개선" 기준을 못 채운다.
  - 그래도 끈 이유: (a) 되돌림은 이틀 모두 플러스(n=27, +0.23%) (b) 추격매수라는
    구조적 설명이 결과와 일치 (c) 지금은 손실을 줄이는 게 우선.

  [🔴 08-06 틱 아카이브가 없다 — [B]의 소급 검증이 원리상 불가능했다]
  ⚠️ **(08-08 정정) 08-06 틱이 아주 없지는 않다** — `cache/tick_history/`에
     18종목 27,142틱이 있다(날짜 폴더가 아니라 **다른 날 폴더의 파일 안에**
     섞여 있다. 아카이브 파일은 종목별 누적이라 ka10079가 며칠치를 같이 준다).
     다만 **08-06 매수 종목은 하나도 포함돼 있지 않아** 아래 결론은 그대로다.
     기준선 분석용 5번째 날로는 쓸 수 있다(08-08 발사 게이트 분석에 실제로 썼다).
  - `task_tick_archive`는 09:20에 도는데 09:19:01에 정지해서 **20초 차이로**
    실행되지 못했다. 그래서 "[B]를 적용하면 승자 4건이 살아남는가"를 확인할
    방법이 없었다(그게 더 중요한 판정 기준이라고 아침 문서가 적어둔 바로 그것).
    -> 대신 [C]에서 승자 4건이 전부 통과하는 것으로 간접 확인했다.
  - **이월 항목 I(FID228 틱 아카이브 저장)의 우선순위를 올렸다** — 정확히 이런
    상황에서 필요한 데이터다.

  [실측 — 전일종가 대비 등락률은 장중에 읽기 전용 조회로 복원할 수 있다]
  - 08-06 매수 19건의 진입 시점 등락률이 로그·DB 어디에도 없었다.
    `prev_close = 현재가 / (1 + 등락률/100)`은 하루 내내 불변이므로,
    **장중에 ka10001을 종목당 1콜씩 읽으면 사후 복원된다**(주문 없음, 19콜).
    이 방법으로 "[C]가 승자를 죽이지 않는가"를 확인했다.

  [🟡 감사가 스스로 경고하던 오탐을 또 냈다]
  - `_entry_change_reject()`로 규칙을 모으자 `_execute_buy` 소스에서
    `_entry_change_cap` 문자열이 사라져, `audit_20260805.py`가 **멀쩡한 코드를
    실패로 보고**했다. 그 파일은 08-06 아침에 같은 이유로 오탐 3건을 내고
    "소스가 아니라 동작을 봐야 한다"는 주석까지 달아둔 상태였다.
    -> 스파이를 걸어 **실제 호출 횟수**를 세는 방식으로 교체.
  - 새 테스트를 쓰다가 존재하지 않는 `check_open_surge_block()`을 지어내
    AttributeError로 죽는 일도 있었다(그 검사는 `evaluate_tick_entry` 안에 있다).
    **메서드명은 grep으로 확인하고 쓸 것.**

  [검증] 10개 스위트 **1,157건 전원 통과**
  (294/89/126/66/164/24/142/63/63/127, 실패 1건은 문서화된 이관 플래그).
  `test_patch_20260806.py` **63건 신설**. 옛 사양(상승이탈 ON)을 검증하던
  `test_patch_20260805.py` 3건은 새 사양으로 교체하고, 플래그를 되살리면
  옛 동작이 그대로 나오는지까지 확인하는 케이스를 추가했다(142건으로 증가).
  `import main` OK / 감사 섹션 [9]에 신규 상수 3개(등락률하한/상승이탈/버스트방향)
  추가 — 문서와 코드가 어긋나면 이제 자동으로 잡힌다.

- 2026-08-06 (11:00~, **🔴 근본 진단: 진입 신호에 우위가 없다**) —
  "어제와 비교해 매매가 너무 이상했다, 급하게 사고 빨리 손절했다"는 관찰에서
  출발해 4일치(08-03~06) 실거래 75건 + 조건검색 편입 158건을 전수 재생했다.
  **결론이 파라미터 조정으로 끝나지 않는다 — 반드시 읽을 것.**

  [① 되돌림이 사실상 없었다 — 상승이탈이 79%를 가져갔다]
  | | 08-05(13:54~) | 08-06 |
  |---|---|---|
  | 되돌림 계획 생성 | 6 | 19 |
  | **상승이탈로 즉시 체결** | **0 (0%)** | **15 (79%)** |
  | 순수 되돌림 체결 | 2 | 4 |
  | 시간초과(미도달) | 3 | 0 |
  - 원인은 **시간대**다. 08-05 계획은 오후(13:54~), 08-06은 전부 09:00~09:18이다.
    개장 직후엔 버스트 직후 +0.3%가 -0.3%보다 압도적으로 먼저 온다 —
    ±0.3% 밴드는 대칭인데 **시장이 비대칭**이라 상승이탈이 사실상
    "즉시 매수"로 작동했다. 되돌림 대기가 통째로 무력화된 것이 "급하게 샀다"의 정체.
  - [D] OFF 소급(1분봉 재생): 합계 **-17.50% -> -9.18%**, 손절 4건이 본전스톱으로
    바뀐다(진입가가 0.3~0.6% 낮아져 -3% 손절선까지 거리가 벌어짐). 4건은 되돌림
    미도달로 **거래 자체가 사라진다**. -> [D] OFF 판단이 재확인됐다.

  [② 🔴 청산 정책으로는 못 고친다 — 20개 조합 전부 4일 마이너스]
  진입을 실제 체결가로 고정하고 청산만 바꿔 격자를 돌렸다(75건, 4일):
  | 정책 | 4일 합계 | 플러스일 |
  |---|---|---|
  | 현행(손절-3 / 캡2.5·4.0) | -39.0% | 1/4 |
  | 손절-2.5 캡6/8 (최선 고정) | -20.6% | 1/4 |
  | 손절 2.0xATR / 익절 4.0xATR (최선 전체) | **-13.7%** | 2/4 |
  | 손절-1.5 캡4/6 | -39.3% | 0/4 |
  - ⚠️ **08-06 하루로 최적이던 `손절-1.5 캡4/6`(-0.97%)이 4일로는 -39.3%다.**
    정확히 이 프로젝트가 반복 경고해 온 **날짜 효과 함정**이다.
    청산 파라미터를 하루 데이터로 고르면 안 된다는 사례가 하나 더 늘었다.
  - 진입 필터도 마찬가지 — 단일경로 제외/고가주 제외/시간대 금지 9종과 조합을
    돌렸지만 **어느 것도 "플러스일 1/4"을 못 바꿨다.** 4개를 겹친 최선 조합도
    -13.8%이고 그건 명백한 과최적화다.

  [③ 🔴 근본 원인 — 진입 신호에 방향성 우위가 없다]
  청산 규칙을 전부 제거하고 '매수 후 N분 평균 수익률'만 봤다(75건):
  ```
        +1분    +3분    +5분   +10분   +30분   +60분   | MFE    MAE
      -0.23  +0.19  +0.10  +0.20  -0.17  +0.09   | +5.60  -4.35
  ```
  - **+5분 평균 +0.099% < 왕복수수료 0.23%**. 모든 지평이 사실상 0이다.
    즉 지금의 진입 신호는 **무작위 진입과 통계적으로 구분되지 않는다.**
  - **모든 부분집합에서 +1분이 음수**다 — "트리거 시점은 국소 고점"이 n=75에서
    재확인됐다(되돌림 대기를 넣은 뒤에도 그렇다).
  - MFE +5.60 / MAE -4.35 — **움직임은 충분한데 우리가 못 잡는다.**
    익절캡 2.5%는 MFE의 절반도 못 먹고, 손절 -3%는 MAE(-4.35%) 안에 들어가 있어
    **정상 흔들림에 잘린다.** 이게 "빨리 손절"의 구조적 정체다.

  [④ 🟢 그런데 조건검색식에는 우위가 있다 — 우리 진입 로직이 죽이고 있었다]
  진입 로직 없이 **편입 시점에 그냥 샀다면**(158건, 4일):
  | | +5분 | +30분 | 종가 |
  |---|---|---|---|
  | **조건검색 편입 즉시** | **+0.506%** | **+0.723%** | **+1.10%** |
  | 우리 진입 로직 통과 후 | **+0.099%** | — | — |
  **우위를 1/5로 깎고 있다.** 무장(1.5초)+버스트+되돌림 대기가 기다리는 동안
  가격이 이미 올라, 결국 "이미 오른 자리"에서 산다. 지연이 우위를 파괴한다.

  [⑤ 🟢 조건식별 성과 — 4일 강건, 가장 실행 가능한 발견]
  | 조건식 | n | +30분 | 플러스일 | 종가 보유(수수료 차감) |
  |---|---|---|---|---|
  | **돌파자동매매용** | 38 | **+2.27%** | **3/4** | 평균 **+3.01%** / 승 61% / 합계 +114.2% |
  | **주도주상위** | 74 | +0.55% | **4/4** | 평균 +0.86% / 승 50% / 합계 +63.9% |
  | **눌림목자동** | 46 | **-0.27%** | **1/4** | 평균 **-0.77%** / 승 41% / 합계 **-35.3%** |
  - **눌림목자동은 4일 46건에서 우위가 없다**(08-03 -0.41 / 08-04 -0.16 /
    08-05 +1.18 / 08-06 -3.88). Pullback 전략의 **유일한 유니버스**가 이것이다.
  - 주도주상위는 수익률은 작아도 **4일 전부 플러스**로 가장 안정적이다.
  - 돌파자동매매용이 가장 크다(종가 +3.01%). 08-05/08-06에 편입이 몰렸다(19/15건).
  - 시간대: **09:00~09:30이 최고**(+30분 +1.33%, 종가 +2.06%, MFE +8.35).
    09:30~10:30이 최저(-0.14%).

  [⑥ 조치] 매수금액 **200만 -> 100만원**(3곳 동시, 사용자 지정).
  전략 변경은 하지 않았다 — ③④⑤는 파라미터가 아니라 **설계 수준의 결정**이라
  사용자 판단이 필요하다. 후보는 "다음 할 일"의 [E][F][G] 참고.

  [⑦ 방법론 교훈 — 다음 세션이 반드시 지킬 것]
  - **조건검색식의 우위와 진입 로직의 우위를 분리해서 측정할 것.**
    이번에 그 둘을 나눠 보고서야 "신호가 아니라 타이밍이 문제"임이 드러났다.
    측정법: 편입 시점 1분봉 시가로 산 가정 수익률 vs 실제 진입 수익률.
  - **정책을 하루 데이터로 고르지 말 것.** 08-06 최적이 4일 최악이었다(②).
  - **진입 경로는 가격 역산이 아니라 그 경로가 남긴 로그로 판정할 것.**
    `entry_reason`의 `(현재가 N)`은 트리거가가 아니라 로그를 찍는 순간의 값이라
    역산하면 경로가 뒤바뀐다(이번에 실제로 한 번 틀렸다).
  - **전일종가는 장중에 사후 복원 가능하다**: `prev_close = 현재가/(1+등락률/100)`.
    하루 불변이라 ka10001 1콜/종목이면 과거 진입의 등락률을 되살릴 수 있다.

- 2026-08-06 (14:00~15:00, **[E][F] 적용 + CLAUDE.md 분량 문제 해결**) —
  사용자 지정으로 설계 결정 [E](눌림목 슬롯 0)와 [F](진입 숙성)를 적용했다.
  상세 근거와 반대 근거는 "다음 할 일"의 [E][F] 항목에 있다. 여기엔 **적용
  과정에서 실제로 걸린 것**만 남긴다.

  [🔴 [E]가 슬롯 2칸을 죽일 뻔했다 — 적용 전에 발견]
  · `PHASE1A_MAX_SLOTS(4) + PULLBACK_MAX_SLOTS(4) = 8 > MAX_HOLDINGS(6)`이라는
    기존 설계에서 눌림만 0으로 내리면 **1A 캡 4가 실질 상한**이 되어
    MAX_HOLDINGS(6) 중 2칸이 영구히 논다. 1A를 8로 같이 올려 해결했다.
  · 감사에 `죽은 슬롯 없음 — 캡 합이 공유 상한 이상` 불변식을 신설했다.
    **어느 한쪽 캡을 0으로 내리는 변경은 앞으로 여기서 자동으로 잡힌다.**

  [🔴 [F]의 방향이 문서에 잘못 적혀 있었다 — 데이터가 반박했다]
  · 08-06 오전 분석에서 "편입 즉시 +0.506% vs 진입로직 통과 +0.099%"를 보고
    **'지연이 우위를 파괴한다'**로 읽어 신선도 컷오프를 후보로 적었는데,
    실측은 정반대였다. 0~1분 지연 진입이 평균 **-1.15%로 최악**이고
    신선도 컷오프(1/2/3/5분)는 전부 플러스일 0/4로 더 나빴다.
  · 진짜 기전은 지연이 아니라 **역선택**이었다 — 편입 158건 중 우리가 고른
    73건이 전체보다 나쁘다. 그래서 반대 방향(최소 대기 60초)을 적용했다.
  · **교훈: 두 모집단의 평균 차이를 특정 축(지연)의 탓으로 돌리기 전에
    그 축으로 직접 분해해 볼 것.** 이번엔 분해하니 부호가 뒤집혔다.

  [테스트 — 사양 변경이 정확히 무엇을 깨는지 확인하고 전부 갱신]
  · [F] 때문에 깨진 것(8건): 격리 테스트가 편입 없이 곧바로 틱을 넣어
    '방금 처음 본 종목'이 되는 케이스. 각 setup 헬퍼에서 `_first_seen`을
    과거로 두어 '이미 편입돼 지켜보던 종목'으로 만들었다(숙성 게이트 자체의
    검증은 test_patch_20260806 [9]에서 따로 한다).
  · [E] 때문에 깨진 것(9건): 눌림목 매수를 요구하던 테스트.
    **`PULLBACK_MAX_SLOTS`를 그 블록에서만 잠시 4로 복구**해 파이프라인
    검증을 살려 뒀다 — 슬롯만 되돌리면 다시 돌아야 하는 코드이므로
    "안 산다"는 정책 때문에 배선 회귀 테스트를 잃으면 안 된다.
  · 슬롯 불변식 3건은 "1A가 공유 상한을 독식 못 함"을 검사하고 있었는데,
    눌림이 0이 된 이상 그 불변식은 **폐기가 맞다**(1A가 6칸을 못 채우면
    슬롯이 죽는다). 새 불변식으로 교체했다.
  · 5개 스위트 **1,183건 전원 통과**(실패 1건은 문서화된 이관 플래그).

  [📄 CLAUDE.md 150k 한도 초과 해결 — 지침이 잘려 로드되고 있었다]
  · Claude Code가 `CLAUDE.md is over the 150.0k-char limit (154.8k)`를 경고.
    **한도를 넘으면 지침이 잘린다** — 이 문서의 정확도가 곧 이관 품질인데
    그게 통째로 무력화되는 상태였다(그리고 이날 작업으로 169k까지 늘었다).
  · **아무것도 지우지 않고** 두 덩어리를 분리했다:
    - `docs/HISTORY_2026-07.md` (22,001자) — 7월 히스토리. 그때 전략
      (1S/Phase2/Phase3/1N/1B/1L)은 전부 삭제됐으므로 경위 확인용.
    - `docs/OLD_TODO_2026-08-02_03.md` (13,464자) — 지나간 (구) 다음 할 일.
  · **169,215자 -> 135,076자.** 아직 유효한 이월 항목은 "🐛 알려진 결함"과
    "아직 판단 보류 중인 것"에 이미 들어 있어 손실이 없다.
  · ⚠️ **앞으로 히스토리를 추가할 때 분량을 의식할 것.** 한도에 다시 닿으면
    같은 방식으로 오래된 달을 `docs/`로 내리면 된다.

  [🖥 바탕화면 원격제어 바로가기 신설 (사용자 요청)]
  · `바탕화면\AutoTrader 원격제어.lnk` — 외출 전에 눌러 두면 폰에서 이어받는다.
  · powershell.exe -> `C:\AutoTrader_Bot\ProjectRoot\start_remote_control.ps1`
    (**junction 경로**를 쓴다 — 실제 경로의 "문서"(한글)가 cmd/스케줄러
    컨텍스트를 깨뜨리는 기존 함정 회피). 런처는 `--continue`가 기본이라
    전날 대화를 이어받고, 세션이 끊겨도 자동 재시작한다.
  · ⚠️ **창을 닫으면 연결이 끊긴다** — 코드 실행은 전부 이 PC에서 일어난다.
    PC와 이 창을 켜 둔 채로 나갈 것.

- 2026-08-06 (15:00~16:00, **[H] 버스트 파동 상한 신설 — 이번 세션 최대 발견**) —
  사용자가 "거래대금이 터질 때마다 올라갔다"며 5종목(뉴엔AI 10:09 / JW신약 10:10 /
  현대약품 09:11 / 져스텍 09:22 / KBI메탈 09:45)을 지목하며 정밀 분석을 요청했다.

  [🔎 지목 시각을 분봉으로 재구성하니 전제가 뒤집혔다]
  · 지목한 시각 중 **둘은 거래대금이 터진 게 아니라 터지기 직전**이었다:
    뉴엔AI 10:09 = 중앙값의 **1.1배**(+5분 +5.96%), JW신약 10:10 = 2.2배.
  · 반대로 **가장 크게 터진 분이 고점**이었다:
    뉴엔AI 10:20(17.8배) +5분 **-3.62%** / 현대약품 09:19(76.5배) **-4.42%** /
    져스텍 09:31(61.9배) **-3.74%**.
  · 즉 "터질 때 사라"가 아니라 **"터지기 시작할 때 사고, 반복되면 빠져라"**다.

  [📊 진짜 축은 '몇 번째 폭발인가' — 4일 3,370건, 완전 단조]
  편입 종목 167 종목·일의 분봉에서 '분 거래대금 >= 그날 중앙값 x10'을 폭발로 정의:
  | 순번 | n | +5분 | 상승확률 | 플러스일 |
  |---|---|---|---|---|
  | 1번째 | 163 | **+1.09%** | 64% | **4/4** |
  | 2번째 | 158 | +0.27% | 48% | 3/4 |
  | 3번째 | 156 | +0.16% | 43% | 2/4 |
  | 6번째~ | 2,595 | -0.23% | 38% | 1/4 |
  | **[1~3]** | **477** | **+0.51%** | 52% | **4/4** |
  | **[4~]** | **2,893** | **-0.18%** | 39% | **1/4** |
  · ⚠️ 반면 거래대금 **'배수'는 4일로 보면 판별력이 사라진다**(10~15x +0.07% 3/4 /
    25~40x -0.19% 0/4 / 70x~ +0.01% 2/4). 08-06 단독에서 보였던 "클수록 나쁨"
    단조성은 **날짜 효과**였다. 이 프로젝트가 반복해서 밟는 함정이라 명시한다.
  · 절대 금액(억 단위)도 판별력이 약하다(10~20억만 소폭 플러스).

  [✅ 우리 실거래 75건으로 교차 검증 — 두 분석이 같은 컷을 지목]
  | 컷오프 | n | 4일 합계 | 플러스일 |
  |---|---|---|---|
  | 제한 없음(현행) | 73 | **-35.9%** | 1/4 |
  | [F] 숙성 60초만 | 54 | -16.1% | 1/4 |
  | **파동3까지 + 숙성** | **16** | **+7.8%** | **4/4** |
  | 파동2까지 + 숙성 | 14 | +8.5% | 4/4 |
  | 파동1까지 + 숙성 | 11 | +9.0% | 4/4 |
  · **8번째 이후 매수 30건이 -27.4%로 4일 손실의 70%**를 만들었다.
  · [F]와 **충돌이 아니라 시너지**다 — 숙성 없이 파동2/3만 걸면 1/4인데
    숙성과 함께면 4/4가 된다(숙성이 '급한 첫 진입'을, 파동이 '늦은 반복 진입'을
    각각 막아 서로 다른 실패 모드를 덮는다).
  · 3을 고른 이유: 1/2/3 모두 4/4인데 3이 표본이 가장 많고(16), 독립적인
    대규모 분석(3,370건)도 정확히 "1~3번째"를 지목한다.

  [구현] `BURST_WAVE_MAX=3` / `BURST_WAVE_COOLDOWN_SEC=60`.
  `_note_burst_wave()`가 발화를 세고(쿨다운 60초로 같은 파동은 접는다),
  `_burst_wave_reject()`가 상한을 본다. `evaluate_tick_entry`에서 버스트 성립
  직후에 판정하므로 두 진입 경로(틱/폴링)가 자동으로 같은 규칙을 쓴다.
  · **상한을 넘어도 카운트는 계속 돈다** — 안 그러면 이후 파동의 순번이 틀어진다.
  · ⚠️ **재시작하면 카운트가 리셋된다**(메모리 전용, 버스트는 DB에 안 남아
    복원 불가). 장중 재시작 시 후기 파동이 통과할 수 있다 —
    08-02에 겪은 "재시작 시 당일 리스크 상태 소실"과 같은 클래스의 한계다.
  · 진단 분류 `버스트 파동 상한(후기 파동)` 신설(정상 필터링임을 구분).

  [검증] `test_patch_20260806.py` 85 -> **104건**(섹션 10). 쿨다운 접힘/경계/
  종목 독립성/상한 초과 후 카운트 지속/실제 진입 경로 차단/대조군/롤백.
  10개 스위트 **1,206건 전원 통과**. 감사에 `파동 쿨다운 <= 진입 숙성` 불변식 추가.


- 2026-08-07 (심야, **무장 3초 복원 + VWAP 인프라 준비**) — 08-03 저녁에 근거 없이
  완화한 무장 시간을 실측으로 되돌렸다. **이번 세션의 핵심은 방법론 교훈 2가지다.**

  [🔴 "재현 불가"가 사실이 아니었다 — 로그에 다 있었다]
  - 08-03에 무장을 3.0 -> 2.0 -> 1.5초로 완화하면서 이렇게 적어뒀다:
    "⚠️ 백테스트로 검증한 값이 아니다 — **FID228 이력이 남지 않아 과거 재현이
    원리적으로 불가능**하다(틱 아카이브엔 시각·가격·거래량만 있다)."
    그리고 이월과제 [I](FID228 틱 아카이브 저장)를 최우선으로 올려뒀다.
  - **틀렸다.** 틱 아카이브엔 없지만 **로그에는 매 건 남아 있다**:
      `🔫 무장 — 체결강도 100 이상 N초 연속`
      `⚡ 틱 진입 트리거 (강도 100+ N초 연속, 4,000만원+ 체결 2건(x1.00))`
    DB 실체결과 (날짜, 종목코드)로 역매칭하면 **매수 시점의 무장 지속시간과
    버스트 경로/규모가 그대로 복원된다.** 08-03~08-06 실거래 75건 중 72건 매칭.
  - **교훈: '데이터가 없다'고 적기 전에 로그를 먼저 뒤질 것.** 이 프로젝트는
    진단 로그를 많이 남기는데 그게 사실상 관측 데이터베이스다.
    (이월과제 [I]는 여전히 유효하다 — 로그는 '매수된 건'만 남고 탈락한 건의
     강도는 안 남는다. 다만 긴급도는 내려간다.)

  [🔴 사용자 지적이 분석을 구했다 — 08-05 이전은 섞으면 안 된다]
  - 처음엔 4일(08-03~06) 69건으로 "무장3초 + 대금0.8억" 조합을 권고했다
    (손익비 1.03 -> 1.43, 4일 합계 -13.2% -> +5.7%, 플러스일 3/4).
  - 사용자가 **"5일 이전은 조건식도 달라서 값이 안 맞을 수 있다"**고 지적.
    맞다 — 08-05에 두 가지가 동시에 바뀌었다:
      ① 모의투자 -> **실전투자** (수수료 편도 0.3% -> 0.015%)
      ② 조건검색 대상 KRX -> **통합** (`stex_tp` K -> A, 유니버스 자체가 다름)
  - 08-05 이후 33건만으로 재검증하니 **대금 컷이 뒤집혔다**:
      무장3초만         -0.3%
      무장3초+대금0.8억  **-3.7%**   <- 4일 기준으로는 +5.7%였다
    즉 대금 컷의 이득은 **08-03/04(모의·KRX) 효과**였다. 지적이 없었으면
    유니버스가 다른 날의 결과로 실전 파라미터를 고칠 뻔했다.
  - **교훈: 실전 전환(08-05)·조건식 변경(08-05) 이전 데이터는 진입 파라미터
    판단에 쓰지 말 것.** 청산 정책 비교에는 여전히 쓸 수 있다.

  [적용 — 무장 1.5 -> 3.0초 (사용자 지정)]
  - 08-05~06 실거래 33건 실측:
        컷오프      거래  승률  손익비   합계      08-05    08-06
        1.5초(구)    33   36%  1.41   -6.2%    +4.6%   -10.8%
        **3.0초**    29   38%  **1.61**  **-0.3%**  +4.4%    -4.7%
    제거되는 4건 = 대원전선 0.00% / 포톤 +0.19% / 대원전선 -3.02% /
    에스피지 -3.06% -> 합계 **-5.9%**로 3건이 손실 또는 0%다.
  - ⚠️ **4.0/5.0초로 더 올려도 결과가 완전히 동일하다**(3~5초 구간에 거래가
    없다). 3.0은 '가장 느슨하면서 같은 효과를 내는' 지점이라 이 값을 골랐다.
  - 구간별로는 **1.5~2초가 압도적 최악**이다(6건 승률 17% 손익비 0.34
    기대값 -1.40%). 5~10초가 손익비 2.34로 최고.
  - ⚠️ 반사실 한계: 이 표본은 '현행 조건을 통과한 거래'만 담는다. 3초로 조였을
    때 다른 종목이 그 슬롯을 채웠을 가능성은 재현되지 않는다.

  [준비만 — 세션 VWAP 진입 필터 (`VWAP_ENTRY_ENABLED = False`)]
  - 기전: VWAP **아래**에서 사면 위에 물린 매물이 상단을 막는다. 08-05 실측 —
    VWAP 위 평균이익 +3.38%/평균손실 -2.58%(손익비 1.31) vs
    VWAP 아래 +2.46%/-3.07%(손익비 **0.80**).
    **VWAP 아래는 승률이 오히려 높은데(67% vs 63%) 손익비가 낮다** —
    승률만 보면 안 잡히고 손익비를 봐야 잡히는 축이다.
  - ⚠️ **아직 켜지 않았다.** 4일 69건으로 재검증하니 효과가 08-05 단일 분석의
    1/4로 줄었다(손익비 1.03 -> 1.09). +1.0% 이상은 오히려 0.80으로 악화돼
    단조성도 깨진다. 무장 3초 복원 결과를 먼저 보고 판단하기로 했다.
  - **측정 가능 시각 = 09:05**(사용자 질문에 대한 답). VWAP은 첫 체결부터 값이
    나오지만 초반엔 **VWAP 자체가 요동쳐** 판정이 매분 뒤집힌다. 08-05 실측
    분당 변동률: 1~3분 **0.509%** / 3~5분 0.299% / 5~10분 0.181% /
    15~30분 0.058%. 판정폭(0.5%)보다 변동이 큰 09:00~09:03은 **물리적으로
    판정이 무의미**하다. 성과도 정확히 일치 — 09:00~09:10은 VWAP 위/아래
    손익비가 **0.77로 완전히 동일**(판별력 0)하고, 09:10~09:30에서 비로소
    갈린다(승률 91% vs 0%). 그래서 `VWAP_ENTRY_FROM = 09:05`.
  - **전 구간 적용은 오히려 손해**다. 09:00~09:05까지 거르면 그 구간에서
    제외되는 25건이 평균 **+1.58%**로, VWAP이 안 잡힌 시점에 좋은 거래를
    랜덤하게 버리는 셈이 된다(08-05 합계 +119.1% -> +79.5%).
  - **원천은 0B의 누적 필드** — FID 14(누적거래대금) ÷ FID 13(누적거래량).
    거래소가 장 시작부터 누적한 값이라 **진짜 세션 VWAP**이고 **REST 0콜·O(1)**.
    파서가 `parsed["raw"]`로 원본을 통째로 넘겨 파서 수정도 불필요했다.
    (감시 시작 시점부터 자체 누적하면 손익비 1.31 -> 1.19로 반토막 난다 —
     편입이 09:05 이후인 종목이 절반이라 짧은 이동평균이 되어 의미를 잃는다.)
  - ~~⚠️ FID 14의 단위가 미확인이다(원 vs 백만원)~~
    -> ✅ **08-07 09:00:06 실거래로 백만원 확정. `VWAP_UNIT_SCALE = 1_000_000.0`**
    (같은 날 오전에 수정 완료 — 아래 "2026-08-07 (09:10~) VWAP 단위 확정" 참고).

  [채택하지 않은 것]
  - **1파 고점 돌파**: 파동 2~3의 **82%가 1파 고점 아래**에서 터진다(되돌림
    국면의 재폭발). 돌파를 기다리면 표본의 82%를 버리면서 손익비는
    1.16 -> 1.09로 **오히려 떨어진다**.
  - **거래대금 추가 상향**: 1.5억 이상 구간은 손익비 **0.96**으로 1 아래다.
    거래대금과 VWAP의 상관계수는 **+0.036**(무상관)이라 서로 다른 축이고,
    교차표에서 대금 높/낮 양쪽 모두 VWAP 위가 우세하다(1.32 vs 0.77 / 1.78 vs 0.82).
    즉 대금을 올려도 VWAP이 잡는 것을 대신 잡아주지 못한다.

  [검증] 10개 스위트 **1,228건 전원 통과**. 옛 사양(1.5초)을 검증하던 5건이
  정확히 실패했고 전부 새 사양으로 교체했다 — 그중 3건은 하드코딩된 초 수를
  **상수 참조로 바꿔** 앞으로 무장 시간을 바꿔도 테스트가 따라오게 했다.
  감사 섹션 [9]에 VWAP 상수 3종을 추가해 문서와 어긋나면 자동으로 잡힌다.

- 2026-08-07 (심야, **조건검색 통합 -> KRX 되돌림**, 사용자 지정) — 08-05에
  통합("A")으로 바꾼 것을 다시 KRX("K")로 되돌렸다. `CONDITION_STEX_TP` 한 줄.

  [🔴 계층마다 거래소 기준이 달랐다 — 이게 되돌린 진짜 이유]
      조건검색(편입)      = **통합(KRX+NXT)**   <- 08-05에 여기만 바뀌었다
      실시간 체결틱 0B     = **KRX만**   <- 무장·버스트·VWAP의 근간
      REST 분봉/현재가     = **KRX만**   <- 시가·전일종가·등락률
      주문/잔고            = **KRX만**   (`dmst_stex_tp="KRX"`)
    즉 **'통합 기준으로 고른 종목'을 'KRX 데이터로만' 판정**하고 있었다.
    조건검색이 "거래대금 100억"으로 통과시킨 종목을 봇은 46억으로 보고
    버스트 문턱(4천만x2건)을 쟀다. 파라미터로 메울 수 없는 종류의 어긋남이다.

  [실측] 키움은 거래소를 **종목코드 접미사**로 나눈다(`005930`/`_NX`/`_AL`):
      금호타이어 073240  KRX 19,927,188 + NXT 20,347,524 = 통합 40,274,712
      삼성전자   005930  KRX 26,101,823 + NXT 15,229,570 = 통합 41,331,393
      대원전선   006340  NXT 미상장 -> KRX = 통합 (영향 없음)
    NXT 상장 종목에서 **우리가 못 보는 거래량이 평균 54%**(에스피지 68% /
    336260 57% / 금호타이어·심텍 51% / 042700 42%). 시가도 다르다
    (금호타이어 KRX 7,200 vs 통합 7,150).
  - 실시간 0B가 KRX 전용임은 틱 아카이브로 **확정**했다 — 073240의
    09:00~09:15 0B 틱 합 1,919,958 = KRX 분봉 1,919,958 (정확히 일치).

  [실측] 2026-08-07 02:33 조건식별 A vs K (장 마감 상태):
      주도주상위      A=6   K=7    <- K에만 073240(NXT 상장 종목인데!)
      돌파자동매매용   A=0   K=1    <- K에만 033160
      눌림목자동      A=3   K=3    <- 동일
      종가베팅        A=19  K=14
    **통합이 유니버스를 넓혀주는 게 아니라 '다른 유니버스'다.** 매매용 3개
    기준으로는 오히려 K가 2종목 많았다(9 vs 11). 조건식에 순위·등락률 조건이
    있으면 모집단과 기준가가 달라져 경계에서 갈리기 때문이다.

  [⚠️ 08-05 기록 정정 — "K는 0종목"은 HTS 불일치가 아니었을 가능성이 크다]
  - 08-05 08:33에 `K -> 0종목 / A -> 1종목`이던 것을 "HTS 대상이 통합이라
    K로 요청하면 0종목"으로 결론냈는데, **02:33 실측에서 K가 정상 동작(7종목)**
    했다. HTS 설정은 그대로다.
  - 더 그럴듯한 설명은 **NXT 거래시간**이다. NXT는 프리마켓 08:00~08:50 /
    애프터마켓 15:40~20:00으로 KRX(09:00~15:30)보다 길다. 08:33은 **KRX가
    안 열린 시각**이라 K는 당일 데이터가 없어 0종목, A는 NXT 프리마켓
    데이터로 1종목이 나온 것이다.
  - ⚠️ 이건 아직 **가설**이다(08:33에 재측정해야 확정). 다만 실무상 결론은
    같다 — **08:59 기동 스냅샷이 0종목일 수 있고 그건 정상**이다.
    09:00부터 실시간 편입(`type='02'`)이 주 경로로 받는다.
    09:10까지 실시간 편입도 0이면 그때 HTS 대상 설정을 의심할 것.

  [🔴 "08-06 손실이 통합 때문"이라는 진단은 데이터가 지지하지 않는다]
    사용자가 그렇게 판단해 되돌리기를 지시했으나, 실거래를 갈라보니 반대였다:
        구분                     청산  승률  손절   합계
        NXT 상장(통합 영향 O)      4   25%   2   -3.3%
        KRX 전용(통합 영향 **X**)  12   25%   5   **-7.5%**
    KRX 전용 종목은 NXT 거래가 없어 **통합 = KRX로 데이터가 완전히 동일**하다.
    즉 16건 중 12건은 통합 설정이 원리적으로 아무 영향도 줄 수 없는데
    **손실의 69%가 거기서** 나왔다. 손절 7건 중 5건(대원전선/KBI메탈/GS건설/
    코스모로보틱스/빛과전자)도 전부 KRX 전용이다. 승률도 25%로 같다.
  - 08-06 손실의 실제 원인은 이미 진단·수정했다 — 그날은 **옛 코드**로 돌았다
    (무장 1.5초 / 상승이탈 추격매수 ON / 파동 상한 없음 / 등락률 하한 없음 /
    `_today_open` 시가 버그). [A]~[H] 8건과 무장 3초 복원이 전부 그 뒤 적용분이다.
  - **그래도 되돌린 이유는 성과가 아니라 위 계층 정합성**이다. 근거를 바꿔
    적어 두지 않으면 다음 세션이 "통합이 손실 원인"이라는 틀린 전제로 작업한다.

  [🔎 백테스팅 결과 — 진입 판정은 **하나도 바뀌지 않는다**]
  - `CONDITION_STEX_TP`는 `core/` 어디에도 등장하지 않는다(전략 로직이 이
    상수를 모른다). `on_condition_hit`도 거래소 구분 인자를 받지 않는다.
    **봇은 이미 모든 판정을 KRX 데이터로 하고 있었다.**
  - 08-05 틱 재생을 다시 돌려도 결과가 **완전히 동일**하다(파동 14건 / 7종목 /
    경로 분포 동일). 즉 이 변경이 바꾸는 것은 **유니버스뿐**이다.
  - ⚠️ **유니버스 변화는 백테스트가 원리적으로 불가능하다.** 조건검색은 시점
    조회만 되고 "08-06 09:05에 KRX로 물었으면?"을 되물을 방법이 없다.
    실측: 08-06 편입 55종목 중 지금 KRX 스냅샷에 남은 건 3종목뿐(장중 3시간
    누적 vs 새벽 단일 스냅샷이라 애초에 비교 대상이 아니다).

  [검증] 10개 스위트 **1,228건 전원 통과**. `test_live_20260805.py`의
  `CONDITION_STEX_TP == "A"` 단언을 `"K"`로 교체(사양 변경을 테스트가 잡아낸
  정상 동작). 되돌리려면 `CONDITION_STEX_TP = "A"` + HTS 대상 통합.

  ⚠️ **오늘은 무장 3초 복원과 KRX 전환 두 가지가 동시에 도는 날이다.**
  결과가 나빠져도 어느 쪽 탓인지 구분되지 않는다 — 되돌릴 땐 하나씩.

- 2026-08-07 (07:35~07:55, **기동 전 전면 재검증 — 코드 변경 0건**) —
  "어제 잘못된 매매가 다시 일어나지 않게" 재검증했다. **상수는 한 줄도
  안 바꿨다.** 결함 0건이고, 새로 만든 것은 회귀 스위트 하나뿐이다.

  [🆕 `test_replay_20260806.py` 49건 신설 — 기존 스위트가 못 보던 것]
  - 기존 10개 스위트는 전부 **사양**을 본다("이 게이트가 이 입력을 막는가").
    그런데 게이트를 조이는 변경은 손실만이 아니라 **이익도 같이 막을 수 있고**,
    그건 사양 테스트로는 원리적으로 드러나지 않는다.
  - 그래서 **08-06 그날의 실제 19건(DB `trades` 원본)을 실제 코드에 다시
    태웠다.** `entry_reason`에 무장 지속시간과 버스트 경로가 그대로 남아 있어
    로그 역매칭 없이 재생이 된다.
  - 결과: **차단 5건 / 통과 14건**, 가격기준 합계 **-10.82% -> -4.74%**.
    · 무장 3초가 대원전선(2.1초, -3.02%) · 에스피지(1.6초, -3.06%)를 차단
    · [C] 하한이 주성 2건(전일대비 -1.26% / -1.96%)을 차단
    · [A] 시가 가드가 금호타이어 09:18(시가대비 **+14.03%**)을 차단
  - 🔴 **가장 중요한 단언: 승자 4건이 한 건도 차단되지 않는다**
    (GS건설 19.8초 / 코스모 9.9초 / 금호타이어 17.2초 / 현대약품 81.1초,
    등락률 하한도 전부 통과 — 금호타이어가 **+0.25%**로 아슬아슬하다).
    "손실을 막았다"보다 이쪽이 판단하기 어렵고 더 중요하다.
  - ⚠️ **정량화할 수 없는 것을 정량화하지 않았다.** [D][F][H]는 그날의 편입
    시각과 틱 흐름이 있어야 판정되는데 **둘 다 없다**(틱 아카이브는 09:20
    태스크가 09:19:01 정지로 20초 차이로 못 돌아 존재하지 않고, 편입 시각은
    DB에 없다). 실제 차단은 이보다 많지만(문서 기준 19건 중 15건이 상승이탈
    경로였고 [D]로 그 경로가 꺼졌다) **증명되는 2건만 못박았다.**

  [🔴 새로 찾은 상호작용 — [C] x 손절대신추가매수 (아무 테스트도 안 덮던 것)]
  - rescue-add는 진입가 **-3%**에서 발동하는데 [C]는 전일종가 대비 **0% 초과**를
    요구한다. 즉 **진입 시점 전일대비가 +3.09% 미만이었으면** -3% 지점 가격이
    전일종가를 밑돌아 `_execute_buy`가 막는다. 진입 상한이 13%이므로
    **0~3.09% 구간(실전에서 흔하다)에서 rescue가 사실상 무력화**된다.
  - **안전하다** — `_do_rescue_add`가 `after_qty <= before_qty`로 미체결을
    감지해 False를 돌려주고, `_rescue_gate`가 그걸 `"sell"`로 바꿔
    정상 손절로 수렴한다(실코드로 확인: 매수 0건 / 매도 1건 / 보유 0).
    추가 리스크 없음(최대 -4.5%는 그대로), 관찰 15초만 소모된다.
  - 다만 **기능 발동률이 문서보다 낮다**는 점은 알고 있어야 한다.
    대조군(전일대비 +3.09% 이상)에서는 정상 집행되는 것도 같이 못박았다.

  [검증 — 전부 실행해서 맞춘 값이다]
  - **11개 스위트 1,276건 통과 / 실패 1건**(문서화된 이관 플래그). 총 1,277건.
  - `daily_backtest` **실제 실행**: 08-06 유니버스 34종목 -> **2건 재현, 예외 0**.
    ⚠️ 이때 `KiwoomREST(token)`을 `is_mock` 없이 만들면 **기본값이 True**라
    실전 토큰으로 모의 서버를 때려 `8031` 에러가 난다(내가 처음에 그렇게 짰다).
    **프로덕션은 안전하다** — `main.py:159`가 `is_mock=settings.IS_MOCK`을
    명시하고, 전 구성 지점을 훑어 누락이 없음을 확인했다.
    그래도 이 기본값은 지뢰다(08-05 `auth.py` 하드코딩과 같은 계열).
  - **조건검색 실계정 실측**(07:47): 매매용 4개가 이름·seq 그대로 존재
    (주도주상위=1 / 돌파자동매매용=2 / 눌림목자동=3 / 종가베팅=4).
    스냅샷은 **K=0, A=0 둘 다 0**이었다 — 07:47은 KRX도 NXT(프리마켓 08:00~)도
    안 열린 시각이라 정상이다. 08-05 "K는 0종목" 정정 가설과 모순되지 않는다.
  - 🟢 **08:59 스냅샷 0종목이 안전함을 코드로 확인했다.**
    `_subscribe_conditions()`(main.py:187)가 스냅샷(라인 311)보다 **먼저,
    독립적으로** 구독을 등록한다. 스냅샷이 0이면 "📸 초기 스냅샷: 진입 종목
    없음"으로 early-return 할 뿐 구독은 이미 살아 있다 — 09:00부터 실시간
    편입(`type='02'`)이 정상 수신된다.
  - 상수 전수 대조: CLAUDE.md '기대값' 블록과 **전 항목 일치**(무장 3.0 /
    슬롯 8·0·6·8 / 파동 3 / 숙성 60 / 하한 0.0 / 상승이탈 False /
    주가계수 0.55·0.3·3.0 / 캡 0.04·0.025·0.025·0.06 / VWAP OFF /
    시가대비 8.0 / 급등강화 6.0·1.5 / 재매수 2.5 / 매수금액 100만 3곳 일치 /
    `IS_MOCK False` / `CONDITION_STEX_TP 'K'`).
  - 배선·인프라: .py 98개 구문오류 **0**, `import main` OK,
    태스크 정의 **21 = gather 등록 21**, 스케줄러 `Ready NextRun=08/07 08:59`,
    junction 정상, `.bat` 비ASCII **0바이트**, `.ps1` UTF-8 BOM,
    `STOP_SIGNAL` 없음, python 프로세스 0개.
  - DB: 미청산 `holding` **0건**(수동관리 `manual` 3건은 설계대로 제외).
  - 로그 분리 확인 — 전수 실행 후에도 `autotrader.log`가 08-06 09:19 이후
    **크기·시각 불변**(6,513,529바이트).

- 2026-08-07 (09:10~09:25, **장중 — VWAP 단위 확정 + 수정**) —
  봇이 08:59:04에 정상 기동한 뒤, 첫 실거래 로그로 미확인 상태였던 FID 14
  단위를 확정했다. **필터는 여전히 OFF**이고 매매 동작은 아무것도 안 바뀌었다.

  [✅ 단위 확정 — 로그 한 줄이 결정적이었다]
  ```
  [09:00:06] 🔎 세션VWAP 단위확인 [460940]
             FID13(누적량)=7,649  FID14(누적대금)=43  현재가=5,640원
  ```
  실제 거래대금 = 7,649주 x 5,640원 = **43.14 백만원** vs FID14 = **43**(오차 0.33%).
  비율(VWAP/현재가)로 가설을 태우면:
  | 가설 | VWAP | 비율 | |
  |---|---|---|---|
  | 원 (구 1.0) | 0.01원 | 0.0000 | ❌ |
  | 천원 | 5.62원 | 0.0010 | ❌ |
  | **백만원 (1e6)** | **5,621.6원** | **0.9967** | ✅ |
  | 억원 | 562,165원 | 99.67 | ❌ |
  -> `VWAP_UNIT_SCALE = 1.0` **-> `1_000_000.0`** 수정.

  [🟢 잘못된 단위가 위험하지 않았던 이유 — 같이 기억할 것]
  scale=1.0이면 VWAP이 0.0056원이 되어 `gap = (현재가-VWAP)/VWAP*100`이
  **1억%**가 나온다. 판정이 `gap <= 0.5`이므로 항상 False = **아무것도 안 막는다.**
  즉 이 필터는 고장 나면 '조용히 통과'시키는 방향이라 매매를 죽이지 않는다.
  대신 **고장을 눈치채기가 어렵다** — 그래서 진단 로그를 '확인용'에서
  **'상시 감시용'으로 바꿨다**(비율이 0.9~1.1을 벗어나면 경고 문구가 같이 찍힌다.
  키움이 필드 단위를 바꾸는 날 이게 유일한 신호다).

  [⚠️ 새로 확인한 제약 — FID 14는 백만원 단위 '정수'다]
  값이 작으면 양자화 오차(±0.5/값)가 판정 문턱(0.5%)을 넘는다.
  실제로 09:00:06 시점 FID14=43에서는 오차가 **±1.16%**로 문턱보다 크다.
  그런데 `VWAP_ENTRY_FROM=09:05`가 이 문제까지 막고 있었다 — 08-06 실측:
  | 종목 | 09:05 누적 | FID14 | 오차 |
  |---|---|---|---|
  | 코스모 | 455억 | 45,495 | 0.001% |
  | 대원전선 | 273억 | 27,321 | 0.002% |
  | 빛과전자 | 188억 | 18,758 | 0.003% |
  | KBI메탈 | 119억 | 11,915 | 0.004% |
  | **현대약품(최소)** | **5.78억** | **578** | **0.086%** |
  가장 얇은 종목도 문턱의 **1/6**이다. -> **09:05를 앞당기려면 이 오차를 다시
  계산할 것.** 09:05는 VWAP 안정성(분당 변동률)뿐 아니라 이 정밀도 관점에서도
  타당한 지점이었다(설계 당시엔 몰랐던 두 번째 근거).

  [적용 범위 — 단위만, 활성화는 안 했다]
  - 08-07은 이미 **무장 3초 + KRX 전환 2건이 미검증**으로 도는 날이다.
    3번째를 얹으면 결과가 갈려도 원인 분리가 불가능하다.
  - 효과 근거도 여전히 약하다(08-05 단일 손익비 1.14->1.42지만 4일 69건으로는
    1.03->1.09, +1.0% 이상은 0.80으로 악화). 항목 K로 이월.
  - 단위를 고쳐둔 실익은 **내일부터 `_session_vwap` 관측값이 유효해진다**는 것이다.
    켤지 말지를 실측으로 판단할 수 있게 됐다.

  [⚠️ 장중 수정이지만 오늘 매매에는 영향이 없다]
  파이썬은 상수를 프로세스 시작 시 메모리에 로드하므로, 08:59:04에 기동한
  프로세스는 **옛 값(1.0)을 그대로 쓴다.** 이 수정은 **내일 08:59 재기동부터**
  반영된다. (기능이 OFF라 어느 쪽이든 매매 결과는 동일하다.)

  [검증] 11개 스위트 **1,278건 전원 통과 / 실패 0건**.
  ✅ 08:59 기동이 `NEW_SESSION_REQUESTED`를 소비해 이관 플래그 검사도
  통과한다(그래서 처음으로 '전원' 통과다).
  `test_patch_20260806.py` [12]에 **실측값 회귀**를 추가했다 —
  `raw={"13":"7649","14":"43"}`을 그대로 태워 비율이 0.9~1.1인지 확인한다.
  옛 사양(scale 1.0)을 검증하던 단언 2건은 새 사양으로 교체했다.

- 2026-08-07 (15:20~, **장마감 후 3건 적용**, 사용자 지정) —
  ① VWAP 진입필터 ON ② 숙성을 조건식별로 분리(돌파만 30초)
  ③ 스냅샷 폴링 재활성화(+ WS 안전화). **전부 다음 거래일(08-10 월) 08:59부터 반영된다.**

  [① VWAP 진입필터 ON]
  단위(백만원)를 오전에 확정했고, 08-07 실거래 10건 소급에서 손실 2건을
  정확히 걸렀다 — 심텍 09:12(이격 **-2.01%**, 실현 -1.12%) / 샘씨엔에스
  09:59(**-1.18%**, -3.03%). 가격기준 **-4.35% -> -0.20%**,
  실현금액 **-42,120원 -> -17,110원**(개선 +25,010원).
  ⚠️ **근거는 얇다** — 4일 69건으로는 손익비 1.03 -> 1.09에 그치고,
  08-07 이랜시스는 이격 +2.36%로 통과했는데 -3.11% 손절이었다(손실 3건 중
  1건은 못 잡는다). n=10 하루치이고, 차단된 2건 대신 다른 종목을 샀을
  반사실은 재현되지 않는다. 나빠지면 `VWAP_ENTRY_ENABLED = False` 한 줄.

  [② 숙성 조건식별 분리 — 돌파자동매매용만 30초]
  · **왜**: 돌파는 편입의 **78%(14/18)가 09:00~09:02**에 몰리는데(주도주상위는
    32종목 중 9개=28%) 60초 숙성이 그 구간을 통째로 덮었다.
  · **증거**: 08-07에 숙성으로 막힌 4종목이 **전부 돌파 최초편입**이다 —
    원익홀딩스 09:00:16(**55초 < 60초, 5초 차이**로 끝내 미매수) /
    후성 09:00:26(31초, 미매수) / 금호타이어(76건 차단) / 이랜시스(15건 차단).
  · **층위별로 결론이 뒤집힌다** — 이게 이 예외의 근거다:
    - 조건식 자체(편입 시점 매수 가정, **2일 대조**):
      돌파 **-0.46%** > 주도 -1.13% (08-06 돌파 +0.89 vs 주도 -0.21 /
      08-07 돌파 -1.81 vs 주도 -1.78) — **양일 모두 돌파 우세**
    - 우리 실매수(실전 3일): 단독 주도 **+0.76%**(승60%) > 단독 돌파 -0.17%(승33%)
    -> **조건식은 돌파가 나은데 우리가 산 결과는 주도가 낫다.** 그 간극을
       만드는 게 숙성이라고 보고 돌파만 완화했다.
  · 복합 편입은 **가장 짧은 값**을 쓴다(돌파가 섞이면 30초).
    조건식을 **모르면 기본값 60초**로 수렴한다 — 여기서만은 '모름'이
    문턱을 느슨하게 만들지 않는다(다른 게이트의 '모름은 통과'와 방향이 반대).
  · ⚠️ **실거래 미검증이고 오히려 손해일 수 있다** — 08-07 실측으로 숙성이
    막은 2건은 원익홀딩스 **-3.23%** / 후성 +2.27%로 **합계 -0.96%라 숙성이
    도움**이었다. [F] 60초는 4일 실측 "4/4 전부 개선"으로 근거가 가장 강했던
    항목이라, 이 완화는 그 근거가 없는 구간을 여는 것이다.
    롤백: `MIN_ENTRY_DELAY_SEC_BY_COND = {}`.

  [③ 스냅샷 폴링 재활성화 — 그냥 켜면 안 되는 것이었다]
  · 🔴 **주석 해제만으로는 위험했다.** `_wait_for`가 `self.ws.recv()`를
    **직접** 돌면서 기다리는 응답이 아닌 메시지를 **전부 버린다** —
    장중에 부르면 체결틱(0B)·호가·조건편입(02)이 조건식당 최대 10초,
    3개면 **최대 30초 유실**된다. 게다가 `listen()`과 동시에 `recv()`를
    부르면 websockets가 concurrent recv 에러를 던진다.
    **이게 이 태스크가 주석 처리돼 있던 진짜 이유다**(코드에 경고가 있었다).
  · -> `listen()`이 도는 중에는 소켓을 직접 읽지 않고 **future로 받는다**.
    `_handle_message`가 응답을 잡아 future로 넘기므로 그 사이 도착한 틱은
    **정상 디스패치된다(유실 0)**.
  · ⚠️ **CNSRREQ는 두 용도로 쓰인다** — (a)요청 응답 (b)실시간 편입 push.
    (b)를 삼키면 조건검색 편입이 조용히 사라진다. 판별자는 **`return_code`
    키의 유무**다(응답에만 있다). 그리고 **대기 중인 future가 있을 때만**
    가로챈다 — 없으면 예전과 완전히 동일하게 흘려보낸다.
  · ⚠️ `_listening`은 **recv 루프 직전에만** 켠다. 재연결 블록의
    `fetch_condition_list()`도 `_wait_for`를 쓰는데 그 시점엔 넘겨줄 주체가
    없어서, 거기서 켜져 있으면 **영구 대기**한다(테스트로 순서를 못박았다).
  · 연결이 끊기면 `_abort_pending()`이 대기자를 즉시 깨운다(타임아웃까지
    매달리다 엉뚱한 시점에 깨어나는 것 방지). 취소는 `RuntimeError`로 통일해
    호출부가 기존 '응답 없음' 경로(빈 리스트)로 수렴한다.
  · **주기 20초 -> 60초.** 백업 경로인데 20초면 조건식 3개 x 3회/분 =
    시간당 540건이 라이브 소켓에 실린다. 놓친 종목은 [F] 숙성(30~60초)
    때문에 어차피 그 안에 못 사므로 60초로도 실질 손실이 없다.

  [🔴 분석 과정에서 제가 틀렸던 것 — 다음 세션이 같은 함정을 밟지 않도록]
  오늘 실매수 10건을 조건식 출처로 갈랐더니 **복합 편입 4건이 4승 +5.89%**,
  단독 6건이 1승 -10.24%로 나와 "복합이 좋다"고 결론 낼 뻔했다.
  **날짜로 쪼개니 부호가 뒤집혔다**: 08-05 복합 -0.93%(1/5) vs 단독 +0.85%(8/15) /
  08-06 복합 -2.05%(0/6) vs 단독 +0.15%(4/10) / 08-07 복합 +1.47%(4/4) vs
  단독 -1.71%(1/6). **하루 효과였다.**
  이 프로젝트가 반복 경고해 온 함정을 그대로 밟은 사례가 하나 더 늘었다 —
  **구간·그룹별 평균은 반드시 날짜로 쪼개 볼 것.**

  [부수 관측 — 종목 수집 경로 점검]
  · 08:59 스냅샷은 KRX 기준이라 **구조적으로 0종목**(`data: None`). 오늘은
    09:00:02에 첫 실시간 편입이 와서 정상 동작했다.
  · 08-06(통합) vs 08-07(KRX) **같은 09:00~09:19 창** 수집량:
    주도 22->18 / 돌파 18->14 / 눌림 15->7, 고유 **34 -> 31종목(-9%)**.
    ⚠️ 하루 대 하루라 시장 상황과 구분되지 않는다 — KRX 탓이라 단정 못 한다.
  · 편입 5,061 / 이탈 3,096으로 churn이 크다. 이탈해도 후보 유지가 설계라
    무해하지만 **로그가 10MB 로테이션을 유발**했다(오늘 14:30). 분석할 땐
    `autotrader.log.1`까지 합쳐야 한다 — 이번에 실제로 겪었다.
  · `ConditionManager.update_snapshot()`은 여전히 죽은 코드(`jmcode` vs 실제
    키 `'9001'`). 오늘은 스냅샷이 0이라 에러조차 안 났다.

  [검증] 11개 스위트 **1,315건 전원 통과 / 실패 0건**
  (295+89+126+66+164+24+142+162+63+134+50, 08-07 15:30 실행).
  `test_patch_20260806.py`에 **섹션 [13] 조건식별 숙성 17건 + [14] WS future
  디스派치 20건**을 신설했다(126 -> 162건). 옛 사양(VWAP OFF)을 검증하던
  단언 3건은 새 사양으로 교체하고 **롤백 경로**(OFF로 되돌리면 통과)까지
  같이 못박았다. 감사 섹션 [9]의 문서↔코드 대조도 새 값으로 갱신했다.

- 2026-08-07 (16:00~, **일봉 눌림 태그 신설 — 관측 전용**) —
  "일봉 눌림을 코드로 판정할 수 있는가"라는 질문에서 출발해 검증까지 마치고,
  **매매는 하나도 안 바꾸는 태그**만 넣었다. 다음 단계(캡 상향)의 근거를
  쌓기 위한 것이다.

  [질문] 기준일에 거래량이 폭발하고 D+1·D+2 거래량이 40%+ 줄었다면,
  그 뒤 거래량이 다시 늘고 주가가 오를 때 **일봉 눌림 후 재상승**으로 볼 수
  있지 않나. 장중에 매수한 종목이 그 상태인지 코드로 알 수 있나.

  [답 — 가능하다] `StrategyManager.daily_pullback_state(code)` 신설.
  **완결된 일봉만** 보므로(오늘 봉 제외) 장중 아무 시각에나 값이 안 흔들린다.
  실측 검증(스피어 347700, 08-07 10:00 시점):
  ```
  08-04 거래량 3,538,155  <- 폭발(20일 중앙값 x3 이상)
  08-05 1,449,031 (-59%)  08-06 1,538,429 (-57%)   <- 눌림
  -> 판정 {'gap':3, 'pullback_high':20,700, 'spike_dt':'20260804'}
  실제 08-07: 돌파선 20,700을 뚫고 종가 23,050(+15%). 우리 봇은 22,000에
  사서 +4.32%(VI 상단 확정매도). 패턴이 그대로 맞았다.
  ```

  [🔴 검증 — 패턴만으로는 우위가 없다. 트리거가 있어야 한다]
  일봉 90종목 x 2025-01~ (33,924봉):
  | | n | +5일 | +10일 |
  |---|---|---|---|
  | 눌림 패턴 **자체** | 1,894 | +2.00% | +3.20% |
  | [기준선] 전체 봉 | 31,854 | +1.55% | +2.88% |
  -> **거의 차이 없다.** 그런데 여기에 **재상승 트리거**(눌림 구간 고가 돌파
     + 거래량 x1.5 회복)를 붙이면:
  | | n | +1일 | +3일 | +5일 | +10일 |
  |---|---|---|---|---|---|
  | **눌림 -> 고가돌파+거래량** | 549 | +1.25% | +2.85% | **+4.66%** | +6.86% |
  | [대조] 눌림 없이 같은 트리거 | 4,686 | +0.73% | +1.67% | +2.18% | +4.13% |
  | [기준선] 전체 봉 | 31,854 | +0.29% | +0.93% | +1.55% | +2.88% |
  **눌림 선행조건이 대조군 대비 2배 이상 기여한다.** 파라미터 민감도도
  안정적이다(폭발 2~4배 x 회복 1.2~2.0배 전 조합에서 +4% 내외 = 과최적화 아님).

  [우리 실거래에서도 유효했다 — 08-03~07, 76건]
  | | n | 평균 | 승률 | 실현 |
  |---|---|---|---|---|
  | 🟢 일봉 눌림 상태에서 매수 | 17 | **+0.68%** | **59%** | **+57,600원** |
  | 그 외 | 59 | -0.36% | 41% | -26,711원 |
  날짜로 쪼개도 **4일 중 3일 우세**(08-03만 열등, 08-04는 표본 0).
  그 주 최고 매매가 전부 여기 있다 — 스피어 +4.32 / 현대약품 +4.25 /
  아진엑스텍 +4.01 / 코스모 +3.17 / 금호타이어 +2.85.

  [🔴 가장 중요한 관측 — 다음 단계의 근거]
  청산 사유를 갈라보면:
  | | 익절 캡류 도달 | 손절·손실반등 |
  |---|---|---|
  | 눌림 17건 | **7건 (41%)** | 6건 |
  | 그 외 59건 | **2건 (3%)** | 23건 |
  **캡 도달률이 13배**인데, 일봉은 이 종목들이 +5일 평균 +4.66% 간다고 한다.
  => **눌림 종목은 더 갈 힘이 있는데 우리 익절캡(4.0%/개장초반 2.5%)이 자른다.**
     현대약품 +4.25%(기본캡 4.0%) / 아진엑스텍 +4.01% / 코스모 +3.17%
     (개장초반캡 2.5%)가 전부 캡에서 끊겼다.

  [⚠️ 그래도 캡 상향을 지금 하지 않은 이유 — 반드시 읽을 것]
  · 549건 +5일 평균은 +4.66%인데 **중앙값이 +0.19%**, 승률 50%다.
    소수의 대박이 평균을 만드는 **꼬리가 두꺼운** 분포다.
  · 기간 5구간 중 **1개가 마이너스**(25년 5~8월 **-0.24%**, 승 38%)이고,
    26년 1~4월(**+12.72%**) 한 구간이 전체 평균을 지배한다.
  · 실거래 표본이 **17건 / 5일**이다.
  · 08-05 심야에 "캡 도달 종목의 **77%가 본전까지 되돌아온다**"는 정반대
    실측이 있다(그래서 캡 상향과 본전스톱이 서로 무력화한다고 결론냈다).
    캡을 올릴 땐 **본전스톱을 켠 상태로** 다시 비교할 것.
  · **구조적 불일치** — 일봉 눌림은 '+5일 보유'가 전제인데 우리는 15:10
    당일청산이다. '며칠 보유'가 아니라 **'당일 안에서 캡만 늘리기'**가
    현실적인 적용 방향이다.
  · 그리고 08-07은 이미 미검증 3건(VWAP/숙성/폴링)이 도는 날이라 4번째를
    얹으면 원인 분리가 불가능해진다.

  [구현 — 매매에 절대 영향이 없도록]
  · `api/kiwoom_rest.get_daily_candles()` 신설(ka10081). **캐시는 호출부 책임**
    이라고 못박았다 — 분봉 캐시(8초 TTL)와 정책이 섞이면 안 된다.
  · `_warm_daily_bars()`가 **pre-arm에서만** REST를 탄다(종목당 하루 1콜).
    실패해도 `asof`를 찍어 **하루 안에 재시도로 REST를 낭비하지 않는다**.
  · `daily_pullback_state()`는 **캐시만 읽는다(REST 0콜)** — 테스트로 못박음.
  · 태그는 `_execute_buy`의 entry_reason에만 붙는다:
    `| [일봉눌림 D-3 돌파선 20,700 눌림 38/40%]`
  · 🆕 **(08-08 보강) 반환 dict에 `drops`를 추가하고 태그에도 실었다** —
    D+1, D+2, … 각 봉의 D0 대비 거래량 비율이다. 통과 조건은 '전부 40%+ 감소'
    라는 **불리언**이라, 그것만 남기면 **눌림이 얼마나 깊었는지를 사후에
    되살릴 수 없다**(태그에 `gap`만 남기 때문). 다음 단계인 항목 N(눌림 종목
    익절캡 상향)에서 깊이가 강도 지표가 될 수 있어 지금부터 쌓는다.
    ⚠️ **판정에는 쓰지 않는다** — `drops`가 달라도 통과/탈락은 안 바뀐다
    (테스트로 못박음). 이미 `mid`를 순회하고 있어 **REST 0콜 추가**다.
  · 🔴 **관측 전용임을 테스트로 강제**했다 — `evaluate_tick_entry` /
    `_open_entry_plan` / `_entry_change_reject` / `check_burst` /
    `on_price_update` 어디에도 `daily_pullback_state`가 없어야 통과한다.
    (쓰기 시작하면 이 단언부터 고쳐야 하고, 그때 위 한계를 다시 읽게 된다.)
  · 롤백: `DAILY_PULLBACK_TAG_ENABLED = False` -> 판정도 REST도 완전히 꺼진다.

  [검증] 11개 스위트 **1,335건 전원 통과 / 실패 0건**
  (295+89+126+66+164+24+142+182+63+134+50).
  `test_patch_20260806.py` [15] **일봉 눌림 태그 20건** 신설(162 -> 182건).

- 2026-08-08 (기동 전, **이관 문서 감사 — 결함 16건 수정 + `drops` 관측값 신설**) —
  사용자가 "이관 MD가 이상 없는지 전체 훑어달라"고 요청. **코드는 정상이었고
  결함은 전부 문서에 있었다.** 스위트 1,335건이 전원 통과하는 상태에서 나왔다.

  [🔴 가장 위험했던 것 — VWAP 상태가 문서 안에서 정면 충돌]
  · 코드는 `VWAP_ENTRY_ENABLED = True`(08-07 저녁 ON)인데 **문서 4곳이 OFF**라고
    말하고 있었다: 08-07 변경표("준비 … OFF") / `#### 3` 소제목("코드만 준비,
    OFF") / "아직 켜지 않는다" 문단 / 판단보류 항목 K("여전히 OFF다").
    반대로 30초 요약·진입 절차·되돌리기 표·전략 요약표는 ON이었다.
  · **매수 게이트 하나의 on/off**라, 장중에 "왜 매수가 적지?"를 판단할 때
    정반대 결론으로 간다. 4곳 전부 ON으로 정정하고 근거·한계·롤백을 실제 값
    기준으로 다시 썼다.

  [🔴 왜 감사(134건)를 통과했나 — 구조적 한계를 발견했다]
  · `audit_20260805.py` [9]는 `기대값 블록에 그 문자열이 있는가`만 본다.
    08-07에 VWAP을 켜면서 **기대값 블록만 고쳤더니**, 다른 문단이 정반대를
    말해도 **134건 전원 통과**로 나왔다. 감사가 원리적으로 못 잡는 틈이다.
  · -> 감사 설명 옆에 이 한계를 명시하고 **"플래그를 뒤집었으면 `grep`으로
    그 이름이 나오는 모든 문단을 훑을 것"**을 규칙으로 박았다.
    (이 프로젝트의 '소스 문자열로 단언하지 말 것'과 같은 계열의 교훈이다 —
     기계적 검사는 자기가 보는 범위 밖을 못 본다.)

  [수정한 나머지 — 전부 '현재 사양을 잘못 알려주는' 문장]
  | 결함 | 내용 |
  |---|---|
  | 감사 파일 번호 | "**10번** 감사를 돌릴 것"(3곳) -> 실제 감사는 **11번**. 그대로 따르면 마지막 관문을 건너뛴다 |
  | 「다음 할 일」 도입부 | 한 세션 뒤처짐 — 1,277건/"08-07에 2건"/"총 10건 미검증" -> 1,340건/미검증 4건 열거/총 14건. 08-07 결산(-42,120원) 추가 |
  | 폴링 태스크 | "`task_condition_snapshot_poll` 비활성 유지" -> 08-07에 재활성화됨(`main.py:1542`) |
  | 무장 시간 | 「현재 전략 요약」이 "3.0초"라 해놓고 괄호는 "1.5로 완화"로 끝남 / "느린 조건(강도 **1.5초**)" — **한 문단 안에서 모순**. ⚠️ 나머지 11곳의 "1.5초"는 **정상**(히스토리·롤백값) |
  | 주가계수 상한 | "clamp(…, 0.30, **2.00**)" -> 실제 **3.00**(08-06 상향) |
  | 봇 상태 | "08-06 09:19 STOP_SIGNAL 정지" -> 08-07 정상 기동 후 **15:33 정상 자동종료** |
  | 슬롯 표기 | "슬롯(4/4/6/8)" -> **8/0/6/8**([E] 눌림 0) |
  | 노출 계산 | "만석 1,080만원 = 예수금과 거의 같다" -> 매수금액 100만원 기준 **540만원**(200만원 시절 계산이 남아 있었다) |
  | 모바일 확인 3번 | 이관 성공 기준이 "**1A/Pullback 2전략 체제**"였다 — [E] 이후 정답은 **1A 단독**. **틀린 답을 요구하는 판정 기준**이라 감사 번호와 같은 부류 |
  | 잔여 | #5->#6 / "PC 검증 4항목"->14항목 / git 기준커밋 `02f9e01`->`9fa56e3` / 목록 번호 4 누락 / 표 열 수 불일치 / 「현재 전략 요약」 헤더 날짜 |

  [🆕 `daily_pullback_state`에 `drops` 추가 — 유일한 코드 변경]
  · 사용자 질문("일봉 눌림에서 D+1, D+2를 확실히 구분할 수 있나")을 검증하다
    나왔다. **구분은 완벽하게 된다** — 일봉이 명시적 오름차순이고
    `rows[i+1]`=D+1, `rows[i+2]`=D+2로 인덱스가 1:1이다. `gap`은 달력일이
    아니라 **거래일**이라 주말·공휴일이 자동으로 건너뛰어진다.
  · 경계 전수 확인: D+2만 어겨도 탈락 / 1봉이면 탈락(MIN_BARS=2) / 3·4봉은
    gap 4·5 / 5봉은 MAX_GAP 초과 탈락 / 폭발 x2.9 탈락·x3.0 통과 /
    감소 정확히 40% 통과·39.9% 탈락. **전부 문서와 일치했다.**
  · **다만 반환값에 깊이가 없었다.** 통과 조건이 '전부 40%+ 감소'라는
    불리언이라, 태그에 `gap`만 남으면 **"D+1이 얼마나 깊게 줄었는지"를 사후에
    되살릴 수 없다.** 항목 N(눌림 종목 익절캡 상향)에서 깊이가 강도 지표가 될
    수 있는데, 지금 안 쌓으면 나중에 원하는 순간 과거를 못 만든다.
  · -> `drops: [0.375, 0.40]`(D0 대비 비율) 추가 + 태그에 `눌림 38/40%`로 표기.
    이미 `mid`를 순회하고 있어 **REST 0콜 추가**이고, **판정에는 쓰지 않는다**
    (깊이가 달라도 통과/탈락 불변임을 테스트로 못박음).
  · [15] 섹션에 5건 추가(182 -> **187건**) — D+1/D+2 개별 구분(비대칭 값으로
    순서까지 검증) / 길이 = gap-1 / 판정 불변 / 태그에 깊이 기록.

  [검증] 11개 스위트 **1,340건 전원 통과**(295/89/126/66/164/24/142/187/63/134/50).
  `import main` OK / 감사 134건 통과 / 코드 변경은 `strategy_manager.py` 2곳뿐
  (반환 dict + 태그 문자열, 둘 다 관측 전용).

- 2026-08-08 (**장전 일봉 눌림 스캐너 신설 — Phase 0, 관측 전용**) —
  "조건검색으로 눌림목을 장전에 찾아 순위를 매겨두고 장 시작 때 사면 승률이
  오르지 않나"는 사용자 아이디어에서 출발. **방향은 데이터가 지지하지만 실행
  형태를 바꿔야 했다.**

  [🔴 조건검색으로는 장전 스캔이 원리적으로 불가능하다]
  조건검색식은 **당일 데이터**로 평가된다(3분봉 30이평·20봉내 급등·유동성 100억).
  장 시작 전엔 그 데이터가 없어서 **08:59 스냅샷이 구조적으로 0종목**이다.
  -> 대신 **일봉**은 전날 종가로 확정돼 있어 장전 계산이 된다. 그리고 인프라가
     이미 8할 있었다(`get_daily_candles` / `daily_pullback_state` / `drops`).

  [근거 — 왜 이 방향인가]
  · 조건검색 편입 즉시 매수 +5분 **+0.506%** vs 우리 진입 로직 통과 후 **+0.099%**
    (4일 158건 vs 75건) — **선별에는 우위가 있는데 타이밍 판정이 1/5로 깎는다.**
  · 일봉 눌림 **패턴 단독은 우위 없음**(1,894건 +2.00% vs 기준선 +1.55%).
    **트리거(눌림 고가 돌파 + 거래량 회복)를 붙여야** 549건 +5일 **+4.66%**
    (대조 +2.18%)가 된다. -> 스캐너의 산출물은 후보가 아니라 **`돌파선`**이다.

  [구현 — `premarket_scan.py` + 스케줄러 `AutoTrader_PremarketScan` 08:30]
  · **독립 프로세스**다. 08:30에 돌아 **08:59 봇 기동 전에 끝난다** — 두 프로세스가
    겹치지 않아 토큰·REST 충돌이 원천적으로 없다(봇 안에 태스크로 넣으면 08:59
    기동 시점이라 개장 1분 전 REST 100여 콜이 개장과 겹친다).
  · 유니버스 = **최근 5거래일 조건검색 편입 종목**(로그에서 추출, 실측 117개).
    전 종목이면 수천 콜인데 이러면 종목당 1콜 = **약 110초**로 끝난다.
  · 판정은 **라이브와 같은 함수**(`StrategyManager.daily_pullback_state`)를
    `__new__`로 빌려 쓴다. 복제하면 언젠가 갈라진다(이 코드베이스 반복 사고 1위).
  · 산출: `observations/premarket/YYYYMMDD.json` + 텔레그램(signal).
    종목 / 등급 / **돌파선** / 전일종가 대비 거리 / 눌림 깊이(`drops`) / 유동성.
  · **복기가 내장돼 있다** — 매일 아침 직전 스캔을 채점한다(어제 일봉이 확정돼
    있고, 대부분 오늘 유니버스에 남아 있어 **추가 REST 0콜**). 이게 없으면
    후보 리스트는 쌓이기만 하고 아무것도 알려주지 않는다.

  [🟢 첫 복기 결과 — 등급이 단조로웠다 (08-07 후보 24개)]
  | 등급 | n | 돌파 | 평균 고가(시가대비) | 봇 매수 |
  |---|---|---|---|---|
  | **A** | 2 | **2/2** | **+9.23%** | **0** |
  | B | 12 | 5/12 | +7.22% | 1 |
  | C | 10 | 1/10 | +4.28% | 0 |
  **A등급 2개를 봇이 하나도 안 샀다** — "우위를 깎는다"가 눈에 보이는 형태로
  나왔다. ⚠️ 단 **n=1일**이고 `평균 고가`는 도달 가능한 최대치이지 실현
  수익이 아니다(우리는 캡 4.0%/2.5%에서 자른다).

  [⚠️ 설계상 의도적으로 하지 않은 것]
  · **연속 점수 랭킹을 쓰지 않았다.** 이 프로젝트는 점수 랭킹에서 이미 크게
    실패했다 — 슬롯교체가 `_watch_scores`(저장된 과거 점수)로 후보를 골랐는데
    진입 방식이 바뀌자 "점수 1위"가 "지금 살 수 있는 종목"을 뜻하지 않게 됐고,
    950160은 점수 1위인데 하루 종일 매수 0건이었다(-235,860원). 그리고 549건의
    **중앙값은 +0.19%**(승률 50%)라 꼬리가 두꺼워 기대값 정렬이 무의미하다.
    -> 굵은 등급(돌파됨/A/B/C)만 매기고 **순서는 시장이 정하게** 둔다.
  · **좁게 거르지 않는다.** 필터를 세게 걸면 나중에 "그 밖은 어땠나"를 알 수
    없다. 눌림이면 등급 무관 전부 기록하고, 거르는 것은 분석 시점에 한다.
  · 등급 경계(A=돌파선 3% 이내·유동성 20억+)는 **데이터로 고른 값이 아니라
    가독성용 가설**이다. Phase 0의 목적이 이 경계의 검증이다.

  [🔴 시간 지평 불일치 — 반드시 같이 기억할 것]
  +4.66%는 **+5일 보유** 전제인데 우리는 15:10 당일청산이다. 당일에 가장 가까운
  숫자는 **+1일 +1.25%**(대조 +0.73%, 수수료 0.23% 차감 시 +1.02% vs +0.50%).
  그리고 태그 17건의 **캡 도달률이 41% vs 그 외 3%(13배)**다 —
  **선별만 개선하고 캡을 그대로 두면 이득을 청산에서 반납한다.**
  즉 이 아이디어는 **항목 N(눌림 종목 익절캡 상향)과 세트**여야 한다.
  ⚠️ 다만 08-05에 "캡 도달 종목의 **77%가 본전까지 되돌아온다**"는 정반대
  실측이 있다 — 캡 상향은 **본전스톱을 켠 상태로** 다시 비교해야 한다.

  [단계 — Phase 2는 이 코드베이스에서 가장 위험한 부류다]
  | 단계 | 내용 | 리스크 | 판단 시점 |
  |---|---|---|---|
  | **0 (지금)** | 스캔 + 복기, **관측 전용** | **0** | 2~3주 |
  | 1 | 후보 종목만 **익절캡 상향** (진입 무변경) | 낮음(상수 1개) | 태그 30건 + 3일 우세 |
  | 2 | 돌파선을 실제 진입 트리거로 | **높음 — 새 진입 경로** | 0·1 결과 후 |

  [검증] 스캐너는 **매매 표면이 0**이다 — `_execute_buy`/`order_manager`/
  `holdings` 참조 0건, DB는 복기용 `SELECT` 1회뿐. 11개 스위트 **1,340건 통과**
  (변동 없음). 실행 검증: 08-07·08-08 기준 2회 실제 실행(117종목 -> 눌림 24/39개,
  복기 24건 채점, 추가 REST 0콜). `.bat` 비ASCII **0바이트** / junction 경로 사용.

- 2026-08-08 (토, **분석 전용 세션 — 진입 지표 4축 백테스트 + VWAP 상호작용 2건 발견**) —
  코드 변경은 스캐너(관측 전용)뿐이고 **매매 로직은 한 줄도 안 바꿨다.**
  ⚠️ **08-08은 토요일이다**(문서가 "08-08(월)"로 잘못 적고 있었다).
  **다음 거래일은 08-10(월)**이고, 미검증 4건은 그날 처음 돈다.

  [🔴 발견 ① VWAP × 되돌림 분할 — 체결 사각지대 (미수정, 관찰 대상)]
  VWAP 게이트는 `_open_entry_plan`에서 **트리거가**로 사전 검사하는데, 실제
  체결은 **-0.3% / -0.7% 낮은 가격**에서 일어난다. 이 간극이 죽은 구간을 만든다:
  | 트리거 이격 | 결과 |
  |---|---|
  | <= 0.50% | 계획이 안 열림 (정상) |
  | **0.50~0.80%** | **계획은 열려 슬롯을 120초 잡는데 1차도 체결 안 됨** |
  | **0.80~1.21%** | **1차만 체결 = 반쪽 포지션** |
  | > 1.21% | 정상 2/2 |
  · 실제 코드로 재현했다(이격 +0.8% -> 체결 0건 / +1.0% -> 1건만).
  · 코드베이스가 스스로 적어둔 원칙과 어긋난다 — *"살 수 없는 종목에 계획을
    걸면 슬롯이 묶인다. 값싼 게이트는 계획 생성 전에 본다."* VWAP은 계획 전에
    보긴 하는데 **틀린 가격(트리거가)으로** 본다.
  · 08-05 실측 영향: **삼기(+6.25%, +7,020원)가 이격 +0.60%로 정확히 죽은 구간**
    이라 두 트랜치 모두 체결 불가 = 그날 최고 수익 거래가 통째로 사라진다.
  · **안 고쳤다.** "안 사는" 방향이라 나쁜 매매를 만들지는 않고, 08-10엔 이미
    미검증 4건이 돈다. **08-10에 트랜치가 1/2로만 체결되는 건이 보이면 이 증상**이다.
  · 고칠 때 선택지: (a) 계획 생성 시 **가장 깊은 트랜치가(-0.7%)로 검사**
    (b) 문턱을 1.21% 이상으로 (c) 되돌림 폭 축소. (a)가 구조적으로 옳다.

  [🔴 발견 ② VWAP × 손절대신추가매수 — 사실상 무력화 (미수정)]
  `_do_rescue_add`도 `_execute_buy`를 타므로 VWAP 게이트를 지난다. rescue는
  정의상 **-3% 지점**에서 발동하므로, 통과하려면 **진입 시점 VWAP 이격이
  +3.7% 이상**이어야 한다. 실측 21건의 진입 이격은 대부분 **+0.6~3.0%**라
  3.7%를 넘는 건 2건(그마저 불확정)뿐이다. -> **VWAP ON이면 rescue-add는
  거의 발동하지 않는다.** 08-07에 문서화한 `[C] 등락률 하한 x rescue`와
  **똑같은 계열**인데, VWAP은 그 분석 뒤에 켜져서 검증되지 않았다.
  안전 측면은 동일하다(손절로 수렴, 추가 리스크 없음).

  [🔴 방법론 교훈 — 기준선 표집이 결론을 4배 부풀렸다. 이번 세션 최대 수확]
  틱 기준 백테스트에서 기준선을 **틱 인덱스 균등**으로 뽑았는데, **틱 밀도는
  시간에 균일하지 않다**(활발할수록 촘촘). 활발한 순간을 과대표집했고 그
  순간들은 평균회귀로 수익이 나빠, 기준선이 인위적으로 낮아졌다:
  ```
  기준선 [틱 인덱스 균등]  -0.357%   <- 편향
  기준선 [시간 균등 5초]   +0.119%   <- 정직
  가속도 1.5x            +0.281%
  -> 우위가 +0.638%p 가 아니라 +0.162%p (약 4배 과대평가)
  ```
  **앞으로 틱 데이터로 신호를 평가할 땐 기준선을 반드시 '시간 균등'으로 뽑을 것.**
  이 교훈으로 세션 중간 권고(1.5배)를 2.0배로 정정했다.
  · 부수 교훈: 고속 구현은 **실물 함수와 등가 검증 후에** 쓸 것. 누적합
    구현이 두 번 틀렸다 — (a) `ts >= cutoff` 경계를 `bisect_right`로 처리
    (b) 초 단위 타임스탬프라 **같은 초의 아직 도착 안 한 틱**까지 포함.
    둘 다 검증 없이 썼으면 분석 전체가 거짓말이 됐다.

  [📊 진입 지표 4축 백테스트 — 틱 아카이브 4일(08-03/04/05/07), 09:00~09:15]
  신호 후 +5분, 60초 쿨다운, 지평이 창 밖이면 폐기, **시간 균등 기준선**:
  | 축 | 건수 | 기준선 대비 | 수수료(0.23%) 차감후 | 일별우세 |
  |---|---|---|---|---|
  | **버스트(현행)** | 30 | **-0.257%p** | -0.368% | 1/4 |
  | 강도 절대 >=100 | 470 | **+0.008%p** | -0.103% | 2/4 |
  | 강도가속 1.5x | 356 | -0.042%p | -0.152% | 2/4 |
  | 강도가속 2.0x | 243 | +0.116%p | +0.006% | 3/4 |
  | **대금가속 2.0x** | 105 | **+0.430%p** | **+0.319%** | 3/4 |
  | 강도가속1.5 AND 대금2.0 | 31 | +0.096%p | -0.015% | 2/4 |
  | 강도가속1.5 OR 대금2.0 | 394 | -0.021%p | -0.131% | 1/4 |
  · 🔴 **버스트는 무작위보다 나쁘고, 강도는 무작위와 같다.** 현행 두 게이트
    모두 방향성 우위가 없다. 문서의 근본 진단("진입 신호에 우위가 없다")을
    **게이트별로 분해**한 것이다.
  · **유일한 생존자는 거래대금 가속도 2.0x**(수수료 후 +0.319%). 단 08-05엔
    기준선에 졌다(3/4).
  · **조합은 한 번도 단독보다 낫지 않았다** — AND도 OR도 전부 열위.
  · ⚠️ **강도는 FID228이 아니다.** 아카이브에 공식 강도가 없어 틱룰
    `compute_strength(30초)`로 대체했다. 라이브 무장은 **FID228(당일 누적)**이라
    성격이 상당히 다르다 — **"현행 무장에 우위가 없다"고 단정할 수 없다.**
    정확히는 "30초 창 틱룰 강도에 우위가 없다"이다. 이월과제 [I] 필요성 재확인.

  [🔎 가속도 창 설계 — 30초/2분이 최적]
  문턱 1.5 고정, 창을 훑은 결과(+5분):
  `30/60 4/4 +0.237%` / **`30/120 4/4 +0.281%`(최적)** / `30/180 3/4 +0.142%` /
  `30/300 2/4 +0.114%` / `60/120 3/4 +0.315%` / `15/60 2/4 +0.186%`
  · **3분·5분으로 늘리면 나빠진다.** 현행 `value_acceleration(30,120)` 그대로가 맞다.
  · 🔴 **`TradeFlowTracker(max_window_sec=120)`이 기본값이라 long=120이 버퍼와
    정확히 일치한다.** 180/300으로 늘리면 **버퍼가 조용히 잘려 경고 없이 틀린
    값**이 나온다. 2분을 쓰면 이 함정이 없다.
  · ⚠️ **가속도 5배는 수학적으로 불가능하다** — 상한 = long/short = 4.0
    (short 구간은 long 구간에 포함되므로 short_val <= long_val).
    1.5배=집중도 37.5% / 2.0배=50% / 3.0배=75% / 4.0배=100%(상한).
    5배를 재려면 창을 바꿔야 한다(10s/120s면 상한 12배).
  · ⚠️ **가속도엔 절대 규모 하한이 없다.** 거래가 없던 종목에 소액만 들어와도
    비율이 튄다. 도입한다면 `get_trade_value(120) >= 일정액` 하한을 같이 둘 것
    (버스트엔 4천만원 하한이 있어 이 문제가 없었다).

  [🔴 시간대별 검증은 현재 데이터로 불가능하다]
  · 틱 아카이브가 **09:00~09:15뿐**이라 분봉(1분/4분)으로 하루 전체를 봤더니
    전 구간 기준선 대비 **±0.02%p = 사실상 0**이었다.
  · 같은 틱 데이터로 창만 굵게 하면 우위가 단조 감소한다
    (`30/120` > `60/240`(분봉 상당) > `120/480`). 즉 **가속도의 우위는 30초
    스케일 현상이라 분봉으로는 원리적으로 안 잡힌다.**
  · -> 시간대별 검증은 **틱 아카이브를 전 시간대로 확대**해야만 가능하다.
    `ka10079` 소급 수집으로 가능하며 10종목x4일에 약 40분.

  [교훈 재확인 — 하루 데이터의 위험이 데이터 안에서 증명됐다]
  사용자 지정으로 강도가속을 08-05만 먼저 돌렸을 때 1.5x가 **+0.076%p**로
  유망해 보였는데, 4일로 넓히니 **-0.042%p로 부호가 뒤집혔다.**
  그리고 `대금가속 2.0x`는 08-05만 보면 **-0.148%p(나쁨)**인데 4일로는
  **+0.430%p(최선)**다 — **하필 08-05가 그 신호가 안 먹힌 날**이었다.
  08-05만 보고 판단했으면 4일 최선을 버렸을 것이다.

  [적용] 매매 로직 변경 **0건**. 스캐너(관측 전용)만 신설·확장했다.

- 2026-08-09 (일, **기동 전 전면 검증 — 🔴 결함 1건 + 방어 1건, 독립 리플레이 신설**) —
  "수정된 부분이 많으니 매수/매도가 오류로 나가지 않게 촘촘히 검증해달라"는
  요청. **1,499건이 전원 통과하는 상태에서 결함이 나왔다.** 상수는 전 항목이
  문서 기대값과 일치했고, 인프라도 정상이었다.

  [🔴 결함 — 파동 카운터가 '거래대금'이 아니라 '시간'으로 소진되고 있었다]
  · 08-08 발사 면제 구간(09:00~09:05)의 `_fire_gate`는 **무조건 True**를
    돌려준다. 그런데 파동 카운트가 발사 성립마다 돌기 때문에,
    **거래대금이 하나도 안 터져도 쿨다운 주기(60초)마다 파동이 1개씩 쌓였다.**
  · **직접 재현**: 틱 버퍼가 완전히 빈 종목(거래대금 0원)을 무장만 유지한 채
    5분 흘리면 파동 **6개**. 09:03부터 상한(3) 초과 -> 카운터는 리셋되지
    않으므로 **하루 종일 매수 불가**.
  · **틱 아카이브 5일 실측**(08-03/04/05/06/07, 158 종목·일):
    09:05 시점에 이미 **80/158 종목이 파동 5개 소진** /
    09:15까지 **106/158(67%)**가 상한 초과.
    탈락 사유 합계 — 개장초반 슬롯 캡 11,271 / 파동 상한 5,502.
  · 🔴 **두 신규 기능이 서로를 무력화하고 있었다.** 초반 슬롯 캡(4칸)에 막혀
    대기하는 종목이 그 사이 카운터를 태우므로, **정작 우위가 측정된 09:05
    이후 가속 구간(+0.688%, 플러스일 4/5)에 들어갈 종목이 남지 않는다.**
  · -> `BURST_WAVE_COUNT_REQUIRES_BURST = True`. 면제 구간에서는 `check_burst`로
    **진짜 폭발이었는지만** 확인해 센다(발사 판정은 여전히 면제). 새 규칙이
    아니라 **08-08 이전 카운터 의미의 복원**이다. 읽기전용 `_burst_wave_count()`
    를 같이 신설 — 폭발이 아닐 때도 상한 판정은 해야 하지만 값을 올리면 안 된다.
  · 효과(독립 리플레이 15일): 매수 **92 -> 117건(+27%)**, 캡 위반 0 유지.
    ⚠️ 이 리플레이는 strength 130 고정 = **항상 무장 = 최악 케이스**라 실전
    건수는 이보다 훨씬 적다. 방향만 읽을 것.
  · ⚠️ **08-10에 매수 건수가 늘어난다.** 없던 브레이크가 사라진 것이므로
    의도된 방향이지만, 08-06(18분 19건) 수준으로 튀면 `EARLY_SLOT_CAP = 3`을 볼 것.

  [🟡 방어 — "모든 매도가 on_price_update를 지난다"는 08-05 기록이 사실이 아니었다]
  · `_execute_sell`을 **직접** 부르는 경로가 따로 있다: `check_timeouts` /
    `try_slot_replacement` / `_try_1a_priority_upgrade` /
    `main.task_force_close_watcher`(15:10). 이들은 08-05에 넣은 가격 위생검사를
    통과하지 않는다.
  · **잘못된 매도가 나가지는 않는다**(가격 조건 없이 청산하고, 주문은 항상
    시장가라 `current_price`를 쓰지 않는다 — 코드로 확인). 하지만 그 값이
    기록 손익 -> `_daily_realized` -> **MDD 일손실 차단(-3%)**을 구동하므로,
    한 건의 쓰레기 값이 그날 매수를 전면 차단할 수 있다.
  · -> `_execute_sell` 진입부에 **비차단** 위생검사. 🔴 **절대 매도를 막지
    않는다** — 여기서 return하면 보유분이 무방비가 되는데 그건 잘못된 기록보다
    훨씬 나쁘다. 값만 대체하고(신선틱 -> 매수가) 경고를 남긴다.
  · 이상 입력 9종(0/None/-1/''/'abc'/-9,800/1/1천만/**NaN**) 전수 확인 —
    전부 매도 1건 정상 집행 / 보유 잔존 0 / 손익 오염 0 / 예외 0.

  [🔴 발견한 두 가지 '검증이 거짓말하는' 사례 — 둘 다 문서가 경고하던 것]
  · **스텁이 실물과 달라 감사가 거짓말했다.** 리플레이 하네스의 `_OrderMgr.buy`가
    시장가(price=0)에 `price or 1`로 **1원**을 돌려줘 `buy_price=1`이 됐다.
    그러자 08-05 가격 위생검사가 전 종목의 청산 판정을 스킵해 **매도 0건**이
    나왔다 — 겉보기엔 "청산이 안 도는 심각한 버그"였다. 실물은 시장가면
    `ref_price`(매도1호가)를 돌려준다. 고치니 매도 39건이 정상 발생했다.
    -> 리플레이에 **체결단가 위생 단언**을 넣었다(buy_price가 틱가의 ±10% 밖이면 실패).
  · **소스 문자열 단언이 또 오탐을 냈다.** `audit_deep`의
    `"self.check_burst" not in src_eval`가, 카운트 목적으로 check_burst를
    부르자 **멀쩡한 코드를 실패**로 잡았다. 그 파일이 스스로 경고하던 패턴이다.
    -> 스파이로 `_fire_gate` 호출을 세고, "거래대금 0원인데도 발사된다"는
       **동작**으로 교체했다.

  [부수 정리]
  · `_execute_buy` 주석이 아직 "호출부가 3곳"이었다(문서는 08-08에 4곳으로
    정정됐는데 코드 주석이 안 따라왔다) -> 4곳으로 정정, 각 함수명 명시.
  · '하루 단위' 카운터가 `_risk_daily_reset`에 일부만 모여 있었다 —
    `_priority_upgrades_today` / `_rescue_count_today` / `_burst_waves`를 같이
    리셋한다. 정상 운용(08:59~15:33)에선 프로세스가 하루를 안 넘겨 차이가
    없지만, 하루 이상 띄워두면 이 셋만 영원히 소진된 채로 남았다
    (매수를 덜 하는 보수적 방향이라 사고는 아니었다).

  [🆕 감사 추가]
  · `audit_deep_20260808.py` 63 -> **85건**. 초반 캡 중 **우선순위 교체가
    매수까지 완결되는가**(캡 도입으로 새로 열린 경로 — `_execute_sell`이
    holdings를 동기 삭제해 점유 4->3이 되므로 반쪽이 아니다) +
    `_execute_sell` 직접 호출 경로의 기록가 위생 20건.
  · `test_patch_20260808.py` 85 -> **91건**. 파동 회귀방지 —
    폭발 없으면 파동 0 유지 / **진짜 폭발은 여전히 4번째 차단**(대조군) /
    09:05 이후 가속은 그대로 카운트 / 롤백 경로.
  · `audit_20260805.py` [9]에 `파동카운트` 문서↔코드 대조 추가(147 -> 148건).

  [검증] 13개 스위트 **1,530건 전원 통과 / 실패 0**
  (295/89/126/66/164/24/142/187/63/148/50/91/85).
  `import main` OK / .py 101개 구문오류 0 / 태스크 정의 21 == gather 21 /
  핫패스 `_fire_gate` **0.051ms** / 진입 핫패스 REST **0콜**.
  `daily_backtest` **실계정 실행**(텔레그램 미전송): 08-07 유니버스 47종목 ->
  1건 재현, 예외 0. 상수 전 항목이 문서 기대값과 일치.
  DB 미청산 `holding` **0건**(`manual` 3건은 설계대로 제외).

  [🆕 독립 통합 리플레이 — 이전 세션 스크립트가 커밋 안 돼 처음부터 다시 만들었다]
  아카이브 **15일**(7월분 포함) 561,439틱을 실제 `StrategyManager`에 관통.
  전 일자에서 **초반 점유가 정확히 4/4에서 멈추고**(캡 위반 0), 전체 최대 6/8,
  예외 0, REST 0콜, 매수 117건 전부 게이트(등락률·시가대비·파동·수량·체결단가)
  만족, 매도 47건 전부 정당한 사유(익절캡 17 / 본전스톱 17 / VI 2 / 손절 11).
  ⚠️ 스크립트는 스크래치패드에 있다 — 다음 세션이 또 만들 바에는
  `test_replay_*` 형태로 커밋하는 것을 검토할 것(이월과제).

- 2026-08-08 (저녁, **🔴 발사 게이트 시간대 분리 — 버스트를 가속도로 대체**, 사용자 지정) —
  같은 날 낮 분석의 후속. **매수를 '만드는' 조건 자체를 갈아끼운 변경**이라
  08-06 이후 적용분 중 가장 크다.

  [적용]
  · **09:00~09:05: 발사 게이트 면제**(무장만으로 발사). 버스트를 끈다.
  · **09:05~: 버스트 대신 거래대금 가속도 2.5배**(30초/120초) + **최소 20틱**.
  · 무장·숙성·등락률·VWAP·파동 상한·되돌림 대기는 **전 구간 그대로**다.
  · `check_burst` 자체는 **재매수 판정·슬롯 교체용으로 그대로 살려 뒀다** —
    갈아끼운 것은 `evaluate_tick_entry`의 발사 한 칸뿐이다(`_fire_gate` 단일 창구).

  [🔴 근거 — 버스트는 무해한 게 아니라 유해했다]
  틱 아카이브 **5일**(08-03/04/05/**06**/07) 146 종목·일, 09:02~09:10, +5분,
  **시간 균등** 기준선, **종목·일당 첫 신호 1건**(독립표본):
  | 규칙 | n | 평균 | 수수료후 | 플러스일 |
  |---|---|---|---|---|
  | [기준선] 그냥 매수 | 146 | +0.395% | +0.165% | 4/5 |
  | **현행 버스트** | 28 | **-0.349%** | -0.579% | **1/5** |
  | 버스트 AND VWAP AND 강도 | 9 | -1.394% | — | 1/4 |
  | 09:02~09:05 버스트만 | 12 | **-0.895%** | -1.125% | 1/3 |
  | **09:05~ 가속 2.5** | 54 | **+0.688%** | +0.458% | **4/5** |
  **AND로 얹을수록 나빠진다**(버스트 AND 가속2.0 = -1.071%p, 0승 4패). 그래서
  '가속도를 더한다'가 아니라 **'버스트를 대체한다'**가 유일하게 말이 되는 형태였다.

  [🔴 방법론 — 격자 평균과 독립표본이 정반대였다. 이번 세션 최대 수확]
  · `VWAP AND 가속2.0`: **격자 +0.531%p**(좋아 보임) vs **독립표본 -0.046%**(패배).
  · 이유는 '조건 성립 구간의 평균'과 '조건 성립 **첫 순간**'이 다른 숫자이기
    때문이다 — 봇은 첫 순간에 발화하므로 후자가 동형이다. 이 프로젝트가 반복
    확인한 **"트리거 시점은 국소 고점"**이 지표 평가 방식에서도 그대로 나타났다.
  · **앞으로 진입 규칙 평가는 반드시 독립표본(종목·일당 첫 신호)으로 할 것.**
  · 같은 날 낮 세션의 "기준선은 시간 균등으로" 교훈과 세트다 — 둘 다 안 지키면
    없는 우위가 보인다.

  [🔴 항목 Q의 옛 권고 2개가 실측으로 뒤집혔다]
  · ~~"절대 규모 하한 필수"~~ -> **정반대**. 하한 없음 +0.218% / 1억 -0.049% /
    3억 -0.217% / 5억 -0.572%로 **단조 악화**. 큰돈이 이미 들어온 자리는 늦은
    자리다(버스트가 음수인 것과 같은 기전).
  · ~~"2.0x가 4축 중 유일한 생존자"~~ -> 격자 기준이었다. 독립표본으로는
    2.0이 기준선에 진다(+0.218% vs +0.395%). **2.5부터 이긴다.**

  [🔴 구현 중 기존 테스트가 진짜 결함을 잡았다 — 최소 틱수 가드]
  · `test_patch_20260802`의 무장 TTL 2건이 실패했다. 원인을 보니 **120초 창에
    틱이 2개뿐이고 둘 다 최근 30초에 있으면 가속도가 수학적 최대(4.0)**가 되어
    무조건 발화한다. 즉 **데이터가 없을수록 쉽게 뚫리는 구조**로,
    `compute_strength`가 틱 부족 시 중립값 100.0을 돌려주는데 임계값도 100이라
    그냥 통과하던 것과 **같은 실패 모드**다(07-31 사고의 원인).
  · -> `FIRE_ACCEL_MIN_TICKS = 20`. **금액 하한이 아니라 개수 하한**이라는 게
    핵심이다 — 실측상 개수 가드는 거의 공짜인데(54건 중 1건만 제외, 성과는
    +0.688% -> +0.754%로 오히려 개선) 금액 가드는 해롭다.
    20은 `avg_trade_value(min_ticks=20)`과 같은 값·같은 논리다.
  · ⚠️ **가속도의 수학적 상한은 LONG/SHORT = 4.0**이다(short가 long에 포함되므로).
    5.0 같은 문턱은 도달 불가라 영원히 발화하지 않는다. 감사에 불변식을 넣었다.

  [부수 — `_MISS_TIERS`가 조용히 비는 것을 막았다]
  '놓친 기회' 알림의 🥈 계층이 `대량체결 부족` 라벨만 보고 있어서, 발사 조건이
  바뀌면 **09:05 이후의 '무장 완료·발사만 미달'이 통째로 사라질** 상태였다.
  `거래대금 가속 미달`을 같은 계층에 등록했다. 진단 분류(`_REJECT_RULES`)도
  '대량체결 부족'과 **분리**했다 — 뭉개면 어느 게이트가 막았는지 알 수 없다.

  [⚠️ 알고 있어야 할 위험 — 초반 구간 발화량]
  09:00~09:05에 발사 게이트가 없으므로 **무장만 되면 되돌림 계획이 열린다**.
  틱룰 강도로 근사하면 그 구간에 하루 **28.6종목**이 무장한다(공유 슬롯은 6).
  즉 초반에는 사실상 **먼저 무장한 순서로** 슬롯이 찬다. 실질 브레이크는
  ①숙성(30/60초) ②되돌림 -0.3% 도달 요구 ③파동 상한 3 ④등락률 하한 0%다.
  ⚠️ **무장은 FID228이라 아카이브로 재현할 수 없다** — 이 28.6은 근사치이고,
  실제 발화량은 08-10 장중에 처음 관측된다. **매수 건수가 08-06(18분에 19건)
  수준으로 튀면 즉시 `FIRE_GATE_ACCEL_FROM = time(9, 0)`으로 전 구간 가속도**를
  걸 것(롤백보다 이쪽이 초반만 정확히 조인다).

  [테스트 — 옛 사양 픽스처를 5개 스위트에서 갱신했다]
  · `test_patch_20260808.py` **54건 신설**(경계·대체증명·최소틱수·게이트 생존·롤백).
  · 🔴 **5개 스위트의 `trigger()`/`burst()` 헬퍼가 전부 옛 버스트 사양(대량체결
    2건)을 흉내내고 있었다.** 건당 금액은 그대로 두고 **틱 수만 `_FIRE_N`으로
    올려 신·구 사양을 모두 만족**하게 했다 — 롤백해도 그대로 돈다.
  · 감사의 `진입 평가가 check_burst를 호출한다` 단언은 **옛 사양**이라
    `_fire_gate` 호출 검증으로 교체하고, 시간대 분기·수학적 상한·버퍼창 일치·
    재매수 경로 불변 4건을 추가했다(134 -> 140건).

  [🆕 개장초반 슬롯 캡 4 — 발사 면제의 짝 (사용자 지정)]
  발사 게이트를 끄면 **무장만으로 계획이 열리므로**, 09:00~09:05에 하루 28.6종목이
  무장하는데 슬롯 6이 **먼저 무장한 순서로** 차버린다(08-06에 개장 18분 19건과 같은 형태).

  🔴 **가속도로는 못 막는다 — 실측이 두 번 반대였다:**
  · **09:00~09:02는 가속도가 구조적으로 부풀려진다.** 120초 창인데 09:00 이전
    데이터가 없어 실제로는 [0, t]만 본다. 균일 유입이어도
    `accel = 4*min(30,t)/t`라 **t <= 48초면 무조건 2.5를 넘는다**
    (09:01 2.00 / 09:02 1.00). 실측도 그대로 — 09:00부터 가속 2.5를 걸어도
    발화가 하루 **28.8 -> 28.2종목**으로 거의 안 줄었다(3.0도 27.6).
  · **워밍업을 피해 09:01부터 걸면 발화는 줄지만 성과가 무너진다:**
    | 규칙 | 하루 | 평균 | 플러스일 |
    |---|---|---|---|
    | 게이트 없음 | 28.8 | **+0.896%** | **5/5** |
    | 가속2.5 (09:01~) | 11.8 | +0.295% | 3/5 |
    | 가속3.0 (09:01~) | 7.2 | -0.104% | 2/5 |
    | 가속2.5 (09:02~) | 8.2 | -0.434% | 2/5 |
    **문턱 = 기다림**인데 초반 우위는 지연에 극도로 민감하다
    (09:00:30 +0.896% -> 09:02 +0.372%). 나쁜 종목을 거르는 게 아니라
    **좋은 시각을 통째로 버린다.**

  [그래서 개수로 막았다] 초반 28.8종목의 기대값이 전부 양수(5/5)라 걸러낼 나쁜
  종목이 없다 — **신호 문제가 아니라 슬롯 배분 문제**다.
  · 되돌림 -0.3%가 이미 **27%를 거른다**(122 -> 89). 실체결분 성과는
    **+1.074% / 수수료후 +0.844% / 플러스일 5/5**.
  · 🟢 **가속도는 문턱으론 쓸모없지만 순위로는 강하다**(체결분 89건):
    | 배분 | 하루 | 평균 | 대조(종목코드순) |
    |---|---|---|---|
    | 가속도 상위 3 | 3.0 | **+2.425%**(4/5) | +0.337%(3/5) |
    | **가속도 상위 4** | 4.0 | **+1.738%**(4/5) | +0.754%(3/5) |
    | 가속도 상위 6 | 6.0 | +1.980%(4/5) | +1.876%(4/5) <- 6에선 차이 소멸 |
    순위가 지연 0으로 고르기 때문이다. 캡을 낮출수록 `_try_1a_priority_upgrade`
    (candidate_tier = 가속도 x 강도 x 단일체결)가 자주 돌아 순위 배분에 가까워진다.
  · ⚠️ **위 순위는 09:00~09:05 전체를 놓고 사후에 매긴 것**이다. 실전은 먼저
    무장한 종목이 먼저 슬롯을 잡으므로 그대로 재현되지 않는다.
  · 구현: `early_slot_cap_reject()` -> **`can_buy_more` 한 곳**에서만 호출한다
    (두 전략·두 진입경로가 전부 그 함수를 지난다). **확장 슬롯(7~8)보다 먼저**
    보므로 초반엔 확장도 열리지 않는다. 진단 분류 `개장초반 슬롯 캡`을
    '슬롯 부족'과 **분리**했다 — 전자는 '자리는 있는데 일부러 안 쓴다'이다.
  · 롤백: `EARLY_SLOT_CAP_ENABLED = False`. 더 조이려면 `EARLY_SLOT_CAP = 3`.

  [🔴 전면 심층 검증에서 결함 3건이 추가로 나왔다 — 전부 수정]
  사용자 요구("매수/매도가 오류로 나가는 일이 절대 없어야 한다")로 전체 로직을
  다시 훑었다. **1,421건이 전원 통과하는 상태에서 나온 것들**이다.

  · 🔴 **`_execute_buy` 호출부가 문서엔 3곳인데 실제 4곳**이었다
    (`_maybe_tick_entry`의 즉시매수 폴백이 빠져 있었다 —
     `ENTRY_PULLBACK_ENABLED=False`일 때만 도달하는 경로라 지금은 죽어 있지만
     **되돌림을 끄는 순간 살아난다**). 이 목록은 "새 규칙을 모든 매수 경로에
     반영했는가"를 점검하는 기준표라, 틀리면 점검이 통째로 샌다.
    -> 문서 정정 + `audit_deep_20260808.py`가 호출부 수를 **기계적으로 센다**.

  · 🔴 **우선순위 교체에 일일 한도가 없었다.** 이 경로는 `can_buy_*`가 False일
    때만 도는데 예전엔 '슬롯 6칸 만석'이라 4일에 **1회**였다. 그런데 초반 캡을
    넣으면 09:00~09:05에 **4칸만 차도** 열린다 — 후보가 가장 많이 쏟아지는
    5분이다. 교체는 매도+매수라 **건당 왕복 0.23%**를 확정 지출한다.
    -> `PHASE1A_PRIORITY_MAX_PER_DAY = 6`(슬롯당 평균 1회). 08-03에 슬롯교체가
       통제 없이 3종목을 팔아 -235,860원을 확정한 전례와 같은 계열이다.

  · 🔴 **슬롯 교체가 '팔았는데 못 사는' 반쪽 동작을 할 수 있었다.**
    `try_slot_replacement`는 지수 가드는 확인하지만 초반 캡은 안 봤다.
    장중 재시작으로 DB에서 6종목이 복원된 채 09:00~09:05를 지나면, 정체 종목을
    팔아도 캡에 막혀 매수가 안 돼 **손실만 확정되고 자리는 빈다.**
    -> `early_slot_cap_reject()` 확인 추가(지수 가드와 완전히 같은 이유).

  [🔴 VWAP 사각지대도 같이 정리했다 — 08-08 낮에 '미수정'으로 남겼던 것]
  계획 생성은 **트리거가**로 VWAP을 검사하는데 실제 체결은 -0.3%/-0.7% 낮은
  가격이고, `_execute_buy` 하드가드가 그 낮은 가격으로 다시 잰다. 그래서
  **계획만 열려 슬롯을 120초 묶고 정작 체결은 안 되는 죽은 구간**이 있었다.
  초반 캡(4칸)으로 슬롯이 귀해져 낭비 비용이 커졌으므로 지금 고쳤다.
  -> `VWAP_ENTRY_CHECK_DEEPEST = True` — 계획 생성 시 **가장 깊은 트랜치가**로
     검사한다. 체결가는 항상 검사가 이상이라 하드가드가 자동으로 통과한다.
  · 실측(아카이브 5일 통합 리플레이): 매수 **68 -> 69건**(감소 없음),
    되돌림 미체결 중 VWAP 사유 **5 -> 2건**. 남은 2건은 120초 대기 중 VWAP이
    움직인 정상 케이스다.
  · 원칙으로 승격: **'값싼 게이트는 계획 생성 전에'는 검사 가격과 체결 가격이
    같을 때만 성립한다.** 감사에 불변식으로 박았다.

  [🆕 통합 리플레이 — 사양이 아니라 '실제로 무슨 일이 일어나는가']
  아카이브 틱을 **실제 StrategyManager**에 그대로 흘려 09:00~09:15를 관통시켰다.
  세션 VWAP은 누적을 FID13/14로 주입해 실물 경로(`_note_session_vwap`)를 타고,
  전일종가·시가도 캐시에 넣어 등락률·시가 게이트가 진짜로 돈다.
  무장(FID228)만 재현 불가라 **strength=130 고정 = 항상 무장 = 최악 케이스**다.
  | 날짜 | 종목 | 매수 | 매도 | 초반 최대점유 | 예외 | REST |
  |---|---|---|---|---|---|---|
  | 08-03 | 25 | 11 | 6 | **4/4** | 0 | 0 |
  | 08-04 | 32 | 14 | 7 | **4/4** | 0 | 0 |
  | 08-05 | 42 | 15 | 8 | **4/4** | 0 | 0 |
  | 08-06 | 18 | 10 | 6 | **4/4** | 0 | 0 |
  | 08-07 | 31 | 11 | 5 | **4/4** | 0 | 0 |
  · **초반 캡 위반 0일** — 동시 점유가 항상 정확히 4에서 멈춘다.
  · 우선순위 교체 **0회**(폭주 없음), 예외 0, 진입 핫패스 REST **0콜**.
  · ⚠️ **캡은 '동시 점유'를 막지 '거래 횟수'를 막지 않는다** — 빠른 청산으로
    자리가 비면 다시 산다(초반 5분 주문 8~15건 = 대략 4~8포지션 회전).
    최악 케이스 기준이며 실제 FID228 무장은 훨씬 선별적이다(08-07 실거래 10건).
  · ⚠️ 하네스 함정: 실물 일부가 `now` 인자 없이 `time.time()`을 쓴다. 가상
    epoch를 쓰면 **'숙성 미달'로 전부 막혀 매수 0건**이 나온다(실제로 한 번
    속았다). 리플레이는 반드시 **실시간 기준 과거 시각**으로 돌릴 것.

  [검증] 13개 스위트 **1,499건 전원 통과** (08-08 시점. 08-09에 1,530건으로 늘었다)
  (295/89/126/66/164/24/142/187/63/147/50/85/61, 실패 1건은 이관 플래그).
  `daily_backtest` **실계정 실행**: 08-07 유니버스 47종목 -> 1건 재현, 예외 0.
  .py 101개 구문오류 0 / 태스크 정의 21 == gather 등록 21 /
  스케줄러 `AutoTrader_Start`·`AutoTrader_PremarketScan` 둘 다 Ready /
  STOP_SIGNAL 없음 / python 프로세스 0개 / `IS_MOCK False` / 조건검색 `'K'` /
  매수금액 3곳 100만원 일치.

  [검증] 12개 스위트 **1,421건 전원 통과**
  (295/89/126/66/164/24/142/187/63/144/50/71, 실패 1건은 문서화된 이관 플래그).
  `import main` OK / 핫패스 `_fire_gate` **0.06ms**.
  감사에 불변식 3건 추가(캡 < 공유상한 / 캡 >= 1 / 캡 종료 == 발사 전환시각).


\## 세션 이관 체크리스트 (2026-08-01 신설)

새 세션(PC 터미널 / 모바일 원격 / 새 대화창 어디서든)이 이 프로젝트를
이어받을 때 쓰는 절차. **읽는 순서가 중요하다.**

\### 이관받는 쪽이 읽을 순서
1. `\## 기본 정보` + `\## 개발/운영 환경` — 무엇을 어디서 어떻게 돌리는지
2. `\## 작업 방식` — 지켜야 할 규칙 7가지 (surgical 패치, 파일 삭제 금지 등)
3. `\## 현재 전략 요약` — **지금 코드가 실제로 하는 일**(과거 히스토리보다 이걸 먼저)
4. `\## 다음 할 일` — **🔴 이번 이관의 본체.** 08-07에 적용한 2건(무장 3초 /
   KRX 전환)과 08-06의 [A]~[H] 8건, 그리고 장중에 무엇을 봐야 하는지가 전부 여기 있다.
5. `\## 히스토리`는 필요할 때만 역순으로. **분량이 많으니 통독하지 말 것** —
   "왜 이렇게 됐는지"가 궁금할 때 해당 날짜만 찾아본다.
   7월분은 `docs/HISTORY_2026-07.md`로 분리돼 있다(CLAUDE.md 150k 한도).

\### ⚡ 30초 요약 — 이것만 알면 대화를 시작할 수 있다
- 봇은 **정지 상태**(08-07 15:33 정상 자동종료. 08-08·08-09는 주말이라
  기동 안 함). 스케줄러가 **매일 08:59 자동 기동**한다.
  스위트 **1,530건 통과 / 실패 0**(13개, 08-09 실행).
- 🔴 **08-09(일)에 결함 1건을 찾아 고쳤다 — 파동 카운터가 '시간'으로 소진되던 것.**
  08-08 발사 면제 구간(09:00~09:05)의 `_fire_gate`가 무조건 True를 돌려주는데
  파동을 발사 성립마다 세고 있어서, **거래대금 0원인 종목도 60초마다 파동이
  1개씩 쌓였다**(직접 재현: 5분 흘리면 파동 6개 -> 09:03부터 매수 불가).
  틱 아카이브 5일 실측 **158종목 중 106개(67%)가 09:15까지 상한 초과 =
  하루 종일 매수 불가**. 초반 슬롯 캡(4칸)에 막혀 대기하는 종목이 그 사이
  카운터를 태워서, **정작 우위가 측정된 09:05 이후 가속 구간에 들어갈 종목이
  남지 않는 구조**였다 — 두 신규 기능이 서로를 무력화하고 있었다.
  -> `BURST_WAVE_COUNT_REQUIRES_BURST = True`(면제 구간에선 진짜 폭발일 때만
  카운트. 발사 판정은 여전히 면제). 롤백은 `False` 한 줄.
- 🔴 **08-10(월)에 미검증 변경 4건이 한꺼번에 돈다.** 결과가 갈려도 원인 분리가
  안 되므로, 되돌릴 땐 **반드시 하나씩**(전부 상수 한 줄):
  | # | 변경 | 롤백 |
  |---|---|---|
  | 1 | **VWAP 진입필터 ON** | `VWAP_ENTRY_ENABLED = False` |
  | 2 | **숙성 조건식별 분리**(돌파만 30초) | `MIN_ENTRY_DELAY_SEC_BY_COND = {}` |
  | 3 | **스냅샷 폴링 재활성화**(+WS future 디스패치) | main.py gather에서 주석 |
  | 4 | **일봉 눌림 태그**(관측 전용, 매매 무영향) | `DAILY_PULLBACK_TAG_ENABLED = False` |
  | **5** | 🆕 **발사 게이트 시간대 분리** — 09:00~09:05 버스트 OFF / 09:05~ 거래대금 가속 2.5 | `FIRE_GATE_SPLIT_ENABLED = False` |
  | **6** | 🆕 **개장초반 슬롯 캡 4** — 09:05 이전엔 6칸 중 4칸만 쓴다(5번의 짝) | `EARLY_SLOT_CAP_ENABLED = False` |
  | **7** | 🆕 **VWAP 깊은 검사** — 계획 생성을 **가장 깊은 트랜치가**로 검사(죽은 구간 제거) | `VWAP_ENTRY_CHECK_DEEPEST = False` |
  | **8** | 🆕 **파동 카운트 기준 정정**(08-09) — 면제 구간에선 진짜 폭발일 때만 센다 | `BURST_WAVE_COUNT_REQUIRES_BURST = False` |
  ※ 4번은 매매를 안 바꾸므로 **원인 분리 대상이 아니다**(안심하고 둬도 된다).
  ※ 8번은 5·6번이 만든 결함을 되돌린 것이라 **5·6번과 세트**다. 5번을 끄면
    면제 구간 자체가 없어져 8번은 자동으로 무의미해진다(끌 필요 없다).
  ※ 🔴 **5번이 이 중 가장 큰 변경이다** — 매수를 '만드는' 조건 자체를 갈아끼웠다.
    다른 게이트는 매수를 거르지만 발사는 매수를 **발생시킨다**.
- 그 전(08-06~07)의 [A]~[H] 8건 + 무장 3초 + KRX 전환도 여전히 표본이 얇다.
- 살아있는 전략은 **1A 하나**뿐 — 눌림목은 [E]로 슬롯 0(매매 중단).
- 매수금액 **100만원**, 슬롯 8/0(공유 6).
- 진입은 `편입 → 숙성(돌파 30초/그외 60초) → 무장 3.0초 → **발사** →
  파동 1~3번째 → 등락률 0~13% → VWAP +0.5% 초과 → 되돌림 -0.3%/-0.7% 분할`.
  🆕 **발사가 시간대로 갈린다**(08-08): **09:00~09:05는 게이트 면제**(무장만으로 발사),
  **09:05~는 버스트 대신 거래대금 가속 2.5배**. 버스트는 신규 진입에서 빠졌다.
- 🆕 **08-08(토)에 장전 스캐너를 신설했다**(관측 전용, 매매 무영향).
  매일 **08:30** 별도 프로세스로 일봉 눌림 후보와 **돌파선**을 뽑아
  텔레그램 + `observations/premarket/`에 남기고, 직전 스캔을 자동 채점한다.
  다윤님이 손댈 곳은 `watchlist_premarket.txt` 하나뿐이다.
- 🔴 **근본 문제는 여전하고, 08-08 백테스트로 원인이 더 좁혀졌다**:
  현행 두 게이트가 **각각** 우위가 없다(버스트 **-0.257%p** / 강도 +0.008%p,
  4일 기준선 대비). 유일한 생존자는 **거래대금 가속도 2.0x**(+0.430%p)인데
  그것도 15분 창 n=105다. 지금까지의 수정은 전부 손실을 줄이는 조치다.
- 최근 결산: 08-05 **+9,796원** / 08-06 **-138,152원** / 08-07 **-42,120원**.

\### ⛔ 이관받는 즉시 확인할 것 (검증보다 먼저)

```bash
python -c "from db.connection import get_connection
with get_connection() as c:
    cur=c.cursor(); cur.execute(\"SELECT stock_code,stock_name,buy_price,buy_quantity FROM trades WHERE status!='closed'\")
    r=cur.fetchall(); print('미청산', len(r), '건'); [print(' ', x) for x in r]"
```

- **`status='holding'`이 0건이어야 정상이다.** 08-06 정지 때 남은 3종목
  (주성036930 7주 @140,100 / 다스코058730 552주 @3,305 / 금호타이어073240 73주 @8,210)은
  08-06 저녁에 **`status='manual'`로 격리**해 봇 관리에서 뺐다 — `find_holdings()`가
  `'holding'`만 조회하므로 재가동해도 복원되지 않는다(손절·강제청산 대상 아님).
  현황은 `python manual_position_tool.py list`로 본다.
  · 사용자가 HTS에서 판 뒤에는 `python manual_position_tool.py close <코드> <체결가>`로
    실제 손익을 기록해 통계에 반영할 것.
  · 서버 잔고엔 남아 있으므로 기동 후 **'⚠️ 미관리 잔고 감지' 텔레그램이
    종목당 1회** 온다(우리기술/엑스게이트와 같은 취급). **정상이다.**
- **봇 실행 여부**: `Get-Process python` 이 0이어야 정상(정지 상태).
- **`STOP_SIGNAL` 잔존 여부**: 수동 정지한 날은 남을 수 있다. 있으면 지울 것.
- 🆕 **장전 스캔 결과**: `observations/premarket/YYYYMMDD.json` (08:30 자동).
  **관측 전용이라 매매와 무관**하다 — 실패해도 봇 기동에 영향이 없다.
  수동 실행: `python premarket_scan.py --no-send` (텔레그램 없이).
  스케줄러 작업명은 `AutoTrader_PremarketScan`(봇은 `AutoTrader_Start`).

\### PC 검증 항목 (코드가 성한지 — 이관 직후 반드시 실행)
프로젝트 폴더에서 순서대로 실행하고, 하나라도 실패하면 그 지점부터 원인 추적.

| # | 명령 | 기대 결과 |
|---|---|---|
| 1 | `python -c "import main; print('OK')"` | `OK` (임포트 체인 전체 정상) |
| 2 | `python test_patch_20260801.py` | **295건 전원 통과 / 실패 0건** |
| 3 | `python test_patch_20260802.py` | **89건 전원 통과 / 실패 0건** (틱 구동 + 상대경로 뒷문차단 + VI 매수차단 교차) |
| 4 | `python test_patch_20260803.py` | **126건 전원 통과** (익절/손절/시간계수/자동종료/종목명/지수가드/시간정리/슬롯교체/본전스톱ON) |
| 5 | `python test_patch_20260804.py` | **66건 전원 통과** (되돌림 대기 분할매수 + 분할매도 + 폴링 우회 회귀방지) |
| 6 | `python test_live_dryrun_20260803.py` | **164건 전원 통과** (종일 통합 + 기동 인프라 + 상수 불변식 + 설정조합 + 전면차단 교차감사) |
| 7 | `python test_closing_bet_routing.py` | **24건 전원 통과** (종가베팅 분리 라우팅) |
| 8 | `python test_patch_20260805.py` | **142건 전원 통과** (장중수술 + 매도시점 + 버스트 주가 스케일 + VI 확정매도 + 시가대비 8%/급등강화 + **상승이탈 OFF 전환**) |
| 9 | `python test_patch_20260806.py` | **187건 전원 통과** (🔴 **[A] 시가 절단 / [B] 버스트 side / [C] 등락률 하한 / [D] 상승이탈 OFF / [E] 눌림 슬롯0 / [F] 진입 숙성 / [H] 파동 상한 / 무장 3초 / VWAP ON·단위 1e6 / [13] 조건식별 숙성 / [14] WS future 디스패치 / [15] 일봉 눌림 태그·`drops` 깊이**) |
| 10 | `python test_live_20260805.py` | **63건 전원 통과** (⚠️ 실전 전환 감사 — 모의 하드코딩/예수금 필드/**기존 보유 불가침**/매수금액 3곳 정합/노출한도/**조건검색 거래소 `"K"`**) |
| 11 | `python audit_20260805.py` | **148건 전원 통과** (⚠️ **경로별 오작동 감사** — 잘못된 매수/매도 + **문서↔코드 정합성** + 청산 충돌 매트릭스 + **죽은 슬롯 불변식**) |
| 12 | `python test_replay_20260806.py` | **50건 전원 통과** (🔴 **08-06 실거래 19건 재생** — 손실이 막히는가 **+ 승자 4건이 살아남는가** + [C]x추가매수 교차 + VWAP ON에서도 '모름은 통과') |
| 13 | `python test_patch_20260808.py` | **91건 전원 통과** (🆕 **발사 게이트 시간대 분리 + 개장초반 슬롯 캡** — 09:05 경계 / 버스트를 **더한 게 아니라 대체**했는가 / 최소 틱수 가드 / 캡이 확장슬롯보다 먼저 / 파동·무장·시가 게이트 생존 / check_burst 잔존 / 롤백) |
| 14 | `python audit_deep_20260808.py` | **85건 전원 통과** (🆕 **심층 감사** — 진입 경로 2개 동일 게이트 / `_execute_buy` 호출부 4곳 / **매도 무영향 실증** / '팔았는데 못 사는' 반쪽 동작 / 교체 폭주 한도 / 수치 3중 대조 / 종일 시나리오 / 이상 입력 내성) |
| 15 | `git log --oneline -5` | 최신 커밋이 `9fa56e3`(일봉 눌림 태그) 이후인지 |
| 16 | `git status --short` | 비어 있어야 정상(작업 중이면 예외) |

**합계 1,530건 전원 통과**(295 + 89 + 126 + 66 + 164 + 24 + 142 + 187 + 63 + 148 + 50 + 91 + 85).
※ `NEW_SESSION_REQUESTED`가 있는 동안은 **1,498 통과 / 실패 1**이 정상이다(#5).
하나라도 실패하면 그 지점부터 원인 추적.
**2026-08-08 실행으로 검증한 값이다**(발사 게이트 시간대 분리 반영).
⚠️ 08-08에 **5개 스위트의 픽스처를 같이 고쳤다** — `trigger()`/`burst()` 헬퍼가
전부 옛 버스트 사양(대량체결 2건)을 흉내내고 있어서, **건당 금액은 그대로 두고
틱 수만 `_FIRE_N`으로 올려 신·구 사양을 모두 만족**하게 했다. 롤백해도 그대로 돈다.

- 🔴 **12번은 다른 스위트와 목적이 다르다.** 나머지는 "이 게이트가 이 입력을
  막는가"(사양)를 보지만, 12번은 **08-06 그날의 실제 19건을 실제 코드에 다시
  태운다.** 게이트를 조이는 변경은 손실만이 아니라 **이익도 같이 막을 수 있는데**
  그건 사양 테스트로는 절대 드러나지 않는다 — 그래서 "승자 4건이 한 건도
  차단되지 않는다"가 이 파일의 가장 중요한 단언이다.
  **진입 게이트를 건드렸으면 반드시 이것까지 돌릴 것.**

- 📄 **11번 감사(`audit_20260805.py`)의 섹션 [9]는 이 CLAUDE.md 자체를 검사한다.**
  상수를 바꾸고 위 '기대값' 블록을 안 고치면 여기서 실패한다 — "매일 문서와 코드가
  어긋난다"는 문제의 **구조적 해결책**이다. 상수를 바꿨으면 문서도 같이 고치고
  11번을 돌릴 것.
  ⚠️ **다만 이 검사는 `기대값 블록에 그 문자열이 있는가`만 본다.** 문서의 **다른
  문단이 정반대를 말해도 통과한다** — 실제로 08-07에 VWAP을 켜고 '기대값' 블록만
  고쳤더니, "아직 OFF"라고 적힌 문단 4곳이 134건 전원 통과 상태로 남아 있었다
  (08-08 이관 시 발견·수정). **플래그를 뒤집었으면 `grep`으로 그 이름이 나오는
  모든 문단을 훑을 것.**
- ⚠️ **11번(감사)이 실전 안전의 마지막 관문이다.** 기능별 스위트 902건이 전부
  통과하는 상태에서도 08-05 심야에 결함 3건(비정상 가격 즉시 매도 / 지수조회
  None에 손절 정지 / 가격폴백 루프 중단)이 여기서 나왔다. **매수·매도 로직을
  건드렸으면 반드시 이것까지 돌릴 것.**
※ **이 표는 세 번 연속 옛 값이 남아 있었다.** 08-05 이관 때 #2(287) / #5(63)가,
  08-05 심야 이관 때 #6(164 전원통과)과 합계식(986이 985로 적힘)이,
  08-06 이관 때 #8(140 -> 실제 142)이 어긋났다.
  **표 숫자를 고칠 땐 반드시 11개를 다 실행해서 맞출 것** — 여기가 틀리면 다음
  세션이 "회귀가 났다"고 오판하고 멀쩡한 코드를 뒤진다.
  (08-07 07:50 검증 시점엔 11개 전부 실행해 맞췄다 — 네 번째 어긋남은 없었다.)

- ⚠️ 6번의 `이관 플래그가 남아있지 않음`은 **코드 결함이 아니라 환경 상태** 검사다.
  프로젝트 루트에 `NEW_SESSION_REQUESTED`가 있으면 **실패로 뜨는 게 정상**이다
  (이관작업 직후 일부러 만들어 둔 것 — 다음 원격 세션 1회만 새 대화로 시작하고
  플래그는 즉시 삭제된다). 그 경우 합계는 **1,339건 통과 / 실패 1건**이 된다.
  ⚠️ **2026-08-07 이관 시점에 실제로 이 상태다** — 플래그를 만들어 뒀으므로
  다음 08:59 기동은 새 대화로 시작하고, 그 뒤로는 다시 자동으로 이어간다.
  즉 **#6이 163 통과 / 1 실패로 나오는 것이 정상**이고, 그 외의 실패만
  실제 결함이다.

- ⚠️ **4번(통합)이 가장 중요하다.** 2·3번이 전부 통과하는 상태에서도 08-03에
  매매를 반쪽 낼 버그 6건이 살아있었다(2026-08-02 히스토리 참고) — 유닛은
  부품을 보고 통합은 배선을 본다. **전략을 바꾸면 반드시 4번까지 돌릴 것.**

- ⚠️ 2번은 Windows 콘솔 기본 코드페이지(cp949)에서 유니코드 출력이 깨져
  `UnicodeEncodeError`가 난다. **`PYTHONIOENCODING=utf-8`을 앞에 붙여 실행**할 것
  (Git Bash: `PYTHONIOENCODING=utf-8 python test_patch_20260801.py`).
- 기존 `test_phase1b_controller.py` / `test_microstructure.py`는 **실패하는 게
  정상**이다(5월 작성 스테일 테스트). 회귀로 오해하지 말 것 —
  확인하려면 `git stash` 후 재실행해서 HEAD에서도 같은 실패가 나는지 보면 된다.

설정값이 의도대로인지 한 번에 보는 명령:
```
python -c "import core.strategy_manager as s; from core.order_manager import FORCE_CLOSE_TIME; print('1A', s.GROUP_A_START, '~', s.PHASE1A_END); print('PB', s.PULLBACK_START, '~', s.PULLBACK_END); print('중복전환', s.DUAL_SOURCE_PULLBACK_FROM); print('청산', FORCE_CLOSE_TIME); print('슬롯', s.PHASE1A_MAX_SLOTS, s.PULLBACK_MAX_SLOTS, s.MAX_HOLDINGS, s.MAX_HOLDINGS_HARD); print('무장', s.TICK_STRENGTH_MIN, s.TICK_STRENGTH_SUSTAIN_SEC); print('버스트', s.PHASE1A_BURST_TRADE_VALUE, s.PHASE1A_BURST_TRADE_COUNT, s.PHASE1A_SINGLE_TRADE_VALUE); print('버스트방향', s.BURST_REQUIRE_BUY_SIDE); print('VWAP깊은검사', s.VWAP_ENTRY_CHECK_DEEPEST, s.PHASE1A_PRIORITY_MAX_PER_DAY); print('초반캡', s.EARLY_SLOT_CAP_ENABLED, s.EARLY_SLOT_CAP_UNTIL, s.EARLY_SLOT_CAP); print('발사분리', s.FIRE_GATE_SPLIT_ENABLED, s.FIRE_GATE_ACCEL_FROM, s.FIRE_ACCEL_MIN, s.FIRE_ACCEL_SHORT_SEC, s.FIRE_ACCEL_LONG_SEC, s.FIRE_ACCEL_MIN_TICKS); print('시간계수', sorted(set(v for _,v in s.TICK_BURST_TIME_MULT))); print('되돌림', s.ENTRY_PULLBACK_ENABLED, s.ENTRY_PULLBACK_TRANCHES, s.ENTRY_PULLBACK_TIMEOUT_SEC); print('상승이탈', s.ENTRY_BREAKOUT_ENABLED, s.ENTRY_BREAKOUT_PCT); print('등락률하한', s.MIN_ENTRY_CHANGE_PCT); print('진입숙성', s.MIN_ENTRY_DELAY_SEC); print('파동상한', s.BURST_WAVE_MAX); print('분할매도', s.PARTIAL_EXIT_ENABLED, s.PARTIAL_EXIT_FRACTION, s.PARTIAL_EXIT_TRAIL); print('지수가드', s.INDEX_GUARD_THRESHOLD, s.INDEX_GUARD_FROM, s.INDEX_GUARD_BREAKEVEN_UNTIL, s.INDEX_GUARD_FORCE_CLOSE); print('캡', s.TAKE_PROFIT_CAP, s.TAKE_PROFIT_CAP_PULLBACK, s.TAKE_PROFIT_CAP_EARLY, s.TP_CAP_UPGRADED_MAX); print('본전스톱', s.BREAKEVEN_STOP_ENABLED); print('1B잔재', hasattr(s, 'PHASE1B_ENABLED')); print('틱구동', s.TICK_ENTRY_ENABLED)"
```
기대값(**2026-08-06 [A][B][C][D] 수정 기준**):
```
1A 09:00:00 ~ 14:50:00        PB 09:00:00 ~ 14:50:00        중복전환 10:30:00
청산 15:10                    슬롯 8 0 6 8   <- [E] 눌림 0(매매중단)/1A 8   무장 100.0 3.0
버스트 40000000 2 100000000   시간계수 [1.0]   <- 값이 1.0 하나뿐이어야 정상
버스트방향 True   <- [B] 매수 체결만 버스트로 센다. False면 투매도 세던 구버전
VWAP깊은검사 True 6   <- 🆕 08-08. 계획 생성 시 **가장 깊은 트랜치가(-0.7%)**로
                      VWAP 검사(검사가 != 체결가라 '계획만 열리고 체결 안 되는'
                      죽은 구간이 있었다). 뒤의 6은 **우선순위 교체 일일 한도**
초반캡 True 09:05:00 4   <- 🆕 08-08. 09:05 이전엔 슬롯을 4칸까지만 쓴다
                      (발사 면제의 짝. 남는 2칸은 09:05 이후 가속 구간용)
발사분리 True 09:05:00 2.5 30.0 120.0 20   <- 🆕 08-08. **발사 조건이 시간대로 갈린다**
                      09:00~09:05 게이트 면제 / 09:05~ 버스트 대신 거래대금 가속 2.5배.
                      마지막 20은 **최소 틱수**(금액 하한이 아니다 — 틱이 적으면
                      가속도가 수학적 최대 4.0으로 튀는 것을 막는다)
주가계수 10000.0 0.55 0.3 3.0  <- 2,000원 x0.41 · 10,000원 x1.00 · 50,000원 x2.42
되돌림 True ((0.003, 0.5), (0.007, 0.5)) 120.0   <- -0.3% / -0.7%
상승이탈 False 0.003   <- [D] 08-06에 OFF. 폭 상수는 보존(되살릴 때 재사용)
등락률하한 0.0        <- [C] 전일종가 대비 이 값 '초과'여야 매수 (= +일 때만)
진입숙성 60.0 {'돌파자동매매용': 30.0}   <- [F] 편입 후 이 초가 지나야 매수.
                      08-07부터 **돌파만 30초**(편입 78%가 09:00~09:02에 몰려
                      60초 숙성이 그 검색식을 통째로 덮고 있었다)
파동상한 3            <- [H] 그날 그 종목의 4번째 이후 거래대금 폭발은 매수 안 함
파동카운트 True        <- 🆕 08-09 결함수정. **파동은 '폭발 횟수'지 '게이트 통과
                      횟수'가 아니다.** 08-08 발사 면제(09:00~09:05)가 무조건
                      True를 돌려주는 바람에 거래대금 0원인 종목도 60초마다
                      파동이 1개씩 쌓였다 — 실측 **158종목 중 106개(67%)가
                      09:15까지 상한을 넘겨 하루 종일 매수 불가**.
                      True면 면제 구간에서 check_burst로 **진짜 폭발일 때만**
                      센다(발사 판정은 여전히 면제). 롤백: False = 08-08 사양
분할매도 True 0.5 0.03
지수가드 -5.0 11:00:00 11:30:00 14:50:00
캡 0.04 0.025 0.025 0.06      본전스톱 True   1B잔재 False   틱구동 True
VI True 0.1 0.005 2           VI매수차단 False 0.03   <- 08-06 사용자 지정으로 OFF
VWAP진입 True 0.5 09:05:00 1000000.0   <- 08-07 장마감 후 **ON**. 단위는
                      백만원 확정. 현재가가 VWAP보다 +0.5% 초과 위여야 매수
일봉눌림태그 True 3.0 0.6 20 5 2   <- 08-07 신설. **관측 전용**(매매 무영향).
                      entry_reason에 `[일봉눌림 D-N 돌파선 P]`만 남긴다
시가대비 8.0   급등강화 6.0 1.5   <- 6~8%는 버스트 1.5배, 8%~는 매수보류
```
**거래소 구분 확인 (2026-08-07 신설)** — 조건검색만 다른 기준이면 조용히 어긋난다:
```bash
python -c "from api.kiwoom_ws import CONDITION_STEX_TP as C; print('조건검색', repr(C), '(K=KRX / A=통합)'); print('※ 틱·분봉·주문은 항상 KRX 전용이다 — C가 A면 유니버스만 통합이라 어긋난다')"
```
기대값: `조건검색 'K' (K=KRX / A=통합)` — **HTS [0156] '대상'도 KRX여야 한다.**

**실전 전환 전용 확인 (2026-08-05 신설)** — 위 명령과 별도로 반드시 같이 볼 것:
```bash
python -c "from config import settings as s; import core.strategy_manager as SM; from core.strategy.portfolio_optimizer import DEFAULT_BASE_AMOUNT as D; from core.order_manager import BUY_AMOUNT_PER_STOCK as B; print('IS_MOCK', s.IS_MOCK); print('매수금액 3곳', SM.POSITION_AMOUNT, D, B, '일치' if SM.POSITION_AMOUNT==D==B else '!!!불일치!!!')"
```
버스트 주가 스케일까지 한 번에 보는 명령 (2026-08-05 신설):
```bash
python -c "import core.strategy_manager as s; print('주가계수', s.BURST_PRICE_REF, s.BURST_PRICE_ALPHA, s.BURST_PRICE_MIN, s.BURST_PRICE_MAX); print('재매수배수', s.REBUY_BURST_VALUE_MULT); [print(f'{p:>7,}원 x{s.burst_price_scale(p):.2f} -> 절대 {s.PHASE1A_BURST_TRADE_VALUE*s.burst_price_scale(p)/10000:>6,.0f}만 / 단일 {s.PHASE1A_SINGLE_TRADE_VALUE*s.burst_price_scale(p)/1e8:.2f}억') for p in (1000,2000,5000,10000,20000,50000,150000)]"
```
기대값: `주가계수 10000.0 0.55 0.3 3.0` / `재매수배수 2.5` /
`1,000원 x0.30` · `2,000원 x0.41` · `10,000원 x1.00` · `50,000원 x2.42` · `150,000원 x3.00`.
- 상한(3.00)은 **73,704원 이상**에서만 걸린다(`10,000 x MAX^(1/ALPHA)`). 즉 08-05에
  실제로 발화가 지연된 마키나락스(25,900) · GS건설(29,750)은 **상한 영향 밖**이다.
- `BURST_PRICE_ALPHA = 0.0`으로 두면 전 종목 계수 1.0 = **현행(스케일 없음)으로 완전 복귀**.

기대값: `IS_MOCK False` / `매수금액 3곳 1000000 1000000 1000000 일치`
- `IS_MOCK True`면 모의로 돌아간 것.
- **매수금액이 3곳에 흩어져 있다**. 실제로 지배하는 값은 `DEFAULT_BASE_AMOUNT`
  (main.py가 `PortfolioOptimizer(rest_api=...)`로 base_amount를 안 넘김).
  `POSITION_AMOUNT`는 optimizer 실패 시 fallback, `BUY_AMOUNT_PER_STOCK`은 레거시.
  **하나만 고치면 조용히 어긋난다.**
- `시간계수 [1.0]`에 1.0 외의 값이 섞여 있으면 **오후 완화가 되살아난 것**
  (2026-08-04에 0.85/0.80/0.95를 전부 제거했다).
- `버스트`가 30000000이면 08-04 상향 전으로 돌아간 것.
- `1B잔재 True`가 나오면 1B 코드가 되살아난 것 — 2026-08-02에 전부 삭제했다.

\### 모바일 확인 항목 (원격 세션이 제대로 붙었는지)
폰에서 세션에 들어간 뒤 아래를 확인한다. **PC 검증과 목적이 다르다** —
여긴 "연결과 맥락"을, PC 검증은 "코드 건강"을 본다.

1. **세션 카드에 `🖥 연결됨 · jokings76/AutoTrader_Project`가 보이는지**
   (안 보이면 PC 터미널 창이 닫혔거나 네트워크가 끊긴 것)
2. **작업 폴더가 맞는지** — 세션에 이렇게 물어본다:
   ```
   지금 작업 폴더가 어디야? 파일 목록 보여줘
   ```
   `main.py`, `core/`, `CLAUDE.md`가 나오면 정상.
3. **맥락을 이어받았는지** — 이렇게 물어본다:
   ```
   CLAUDE.md 읽고 지금 살아있는 전략이랑 진입 게이트 순서 요약해줘
   ```
   **살아있는 전략은 1A 하나**(눌림목은 [E]로 슬롯 0 = 매매 중단),
   진입은 `편입 -> 숙성(돌파 30초/그외 60초) -> 등락률 0~13% -> VWAP +0.5% ->
   무장 3.0초 -> **발사(09:00~09:05 면제 / 09:05~ 거래대금 가속 2.5배)** ->
   파동 1~3번째 -> 되돌림 -0.3%/-0.7% 분할`,
   15:10 강제청산이 나오면 이관 성공.
   ⚠️ **"1A/Pullback 2전략 체제"라고 답하면 옛 문서를 읽은 것**이다
   (08-06 [E] 이전 사양). 그 경우 이 문서를 다시 손봐야 한다.
4. **모델 확인** — 원격 세션은 Sonnet 5로 시작한다. 전략/로직 작업이면
   `/model`로 **Opus 5**로 올리고 시작할 것.
5. **장중 주의** — 봇이 돌고 있는 09:00~15:10에는 매매 로직 파일 수정 금지.
   로그 조회·상태 확인·진단 알림 해석 정도만 하고, 코드 수정은 장 마감 후.

\### 이관이 끝났다고 판단하는 기준
- PC 검증 표 **16항목 전부 통과**(#6은 이관 플래그로 1건 실패가 정상)
  \+ 설정값 출력이 기대값과 일치
- 모바일 확인 1~3번 통과(특히 3번 — 맥락 복원이 이관의 본질)
- 위 둘 중 하나라도 실패하면 **아직 이관 안 된 것**이다.

\## 현재 전략 요약 (**2026-08-07 장마감 후 기준 — [A]~[H] 8건 + 무장 3초 + KRX + VWAP ON 반영**)

> 🔴 **눌림목(Pullback)은 [E]로 슬롯 0 = 매매 중단 상태다.** 조건검색 구독과
> 라우팅·평가는 그대로 돌아 데이터는 계속 쌓인다. 아래 표의 Pullback 열은
> '되살렸을 때의 사양'으로 읽을 것.

> ⛔ **봇은 지금 정지 상태다.** 08-07은 08:59:01 정상 기동 -> **15:33 정상
> 자동종료**했다(수동 정지 아님, `STOP_SIGNAL` 없음). 스케줄러가 매일 08:59
> 자동 기동하므로 **다음 거래일 기동은 08-10(월) 08:59**다. 스위트 1,530건 통과.
> ⚠️ 스케줄러는 '매일'이라 **토·일에도 08:59에 봇이 뜬다**(봇엔 주말 가드가
> 없다). 조건검색 0종목으로 돌다 15:30쯤 종료된다 — 무해하지만 알림은 온다.
> (장전 스캐너에는 주말 가드를 넣어 토·일엔 스캔하지 않는다.)
> ✅ **미청산 3종목은 08-06 저녁에 `status='manual'`로 전환해 봇 관리에서
> 뺐다**(주성/다스코/금호타이어). `find_holdings()`가 `'holding'`만 조회하므로
> 재가동해도 **복원되지 않고, 손절·익절·15:10 강제청산 어디에도 안 걸린다.**
> ⚠️ **08-08 이관 시점에도 3건 그대로 남아 있다**(HTS 매도 미확인) —
> 사용자가 HTS에서 판 뒤 `python manual_position_tool.py close <코드> <체결가>`로
> 기록해야 통계에 반영된다. 현황은 `python manual_position_tool.py list`.

> ⚠️ **실전투자다** (2026-08-05 전환). 종목당 매수 **100만원**이고
> (50만 -> 200만(08-05 장마감) -> **100만(08-06)**, 전부 사용자 지정)
> optimizer 비중 0.90이 곱해져 실제로는 **종목당 약 90만원**,
> 되돌림 1차 트랜치 **약 45만원**이다.
> 슬롯은 **1A 8 / 눌림 0 / 공유 6 / 확장 8**([E]로 눌림 매매 중단).
> MDD(-3%)·시간창은 모의와 동일하다.
> ⚠️ **노출** — 슬롯 6 만석이면 약 **540만원**으로 예수금(약 1,080만)의 절반이다.
> 200만원이던 08-05~06에는 만석 = 1,080만원이라 **예수금이 실질 슬롯 상한**으로
> 작동했는데, 100만원으로 내리면서 그 제약이 풀렸다(초과분은 키움이 거부할 뿐
> 오주문은 아니다). 실측 동시보유 최대는 2/6이라 어느 쪽이든 도달한 적은 없다.
>
> **08-05는 실전 첫날이자 수술이 10건 몰린 날이다.** 진입은 ±0.3% 밴드로,
> 청산은 손절 대체(추가매수)와 손실반등 분할로 바뀌었다. 아래 "08-05에 바뀐 것"
> 참고. ⚠️ 그중 **손실반등 분할 / 시가필터 / 재매수 완화 3건은 08-06 08:59
> 재기동부터 처음 실거래에 반영**된다(08-05엔 코드만 들어갔다).

> **⚡ 08-05에 바뀐 것 (실전 전환 + 수술 10건 + 장마감 후 4건)**
> 0. **[장마감 후]** **버스트 주가 스케일**(계수 = clamp((주가/1만)^0.55, 0.30, 2.00)
>    — ⚠️ 상한 2.00은 **그날의 값**이고 08-06에 **3.00**으로 올랐다)
>    / **매수금액 200만 환원** / **VI 상단 확정매도** / **감사 결함 3건 수정**
> 1. **실전투자 전환** — 매수금액은 위 0번대로 **200만원**, 예수금은 D+2 반영
> 2. ~~조건검색 `stex_tp="A"`(통합)~~ -> **08-07에 `"K"`(KRX)로 되돌림**
>    (계층 정합성 — 아래 히스토리 2026-08-07 참고). HTS '대상'도 KRX로 맞출 것
> 3. **시장가 문턱 5,000만 -> 1,000만** (미체결 3건이 +11%/+15%/+5.7%를 놓쳤다)
> 4. **되돌림 -0.5/-1.0% -> -0.3/-0.7%** + **상승 이탈 +0.3%**(±0.3% 대칭 밴드)
> 5. **손절 대신 추가매수**(관찰 15초 -> 반등확증) / **본전바닥 +0.2%** / **1A 상한 13%**
> 6. **손실반등 매도 전량 -> 50% 분할** (08-05 최대 손실 요인이었다)
> 7. **시가대비 필터 부활**(그동안 캐시가 비어 한 번도 발동 안 했다)
> 8. **손실청산 종목 조건부 재진입**(60분+1.6억+상대경로 금지, 종목당 1회)
> ⚠️ 6·7·8은 **08-06 08:59부터 처음 실거래에 반영**된다.
>
> **⚡ 08-04에 바뀐 것 5줄 요약** (상세는 히스토리 "2026-08-04" 항목들)
> 1. **본전스톱 ON** (순+1% 찍고 본전 이탈 시 청산) — 08-03 저녁 OFF에서 되돌림
> 2. **되돌림 대기 분할매수** — 트리거 즉시매수 폐지. -0.5%에 50% / -1.0%에 50%,
>    120초 미도달 시 포기. **1종목 = 1슬롯**(분할해도 슬롯·금액 불변)
> 3. **분할매도** — 동적캡(강도·거래량 동시하락) 시 전량이 아니라 **50%**만,
>    잔량은 고점 대비 **3% 트레일**
> 4. **버스트 문턱 3천만 -> 4천만** (절대 경로 2건 = 0.8억)
> 5. **시간대 계수 전부 1.00** — 오후 완화(0.85/0.80/0.95) 제거

**두 전략이 같은 트리거를 쓴다.** 조건검색식이 '어떤 종목인가'를 전부 판정하고
(3분봉 30이평 근접·눌림폭·급등이력·유동성 100억+), 코드는 '지금이 그 순간인가'만
본다 — 즉 코드가 보는 건 **체결강도와 거래대금 둘뿐**이다. 전략 차이는 유니버스와
슬롯·상한·익절캡·시간창에만 있다.

| 항목 | 1A | Pullback |
|---|---|---|
| 조건검색식 | 주도주상위, 돌파자동매매용 | **눌림목자동 전용** |
| 시간창 | 09:00 ~ 14:50 | **09:00 ~ 14:50** (2026-08-03: 1A와 완전 동일) |
| 진입 조건 | **무장**(체결강도 FID228 100+ **3.0초 연속**) → **발사**(🆕 **09:00~09:05 면제 / 09:05~ 거래대금 가속 2.5배**, 08-08) → **되돌림 대기**(-0.3%/-0.7% 분할매수, 120초) | **1A와 동일** |
| **발사 게이트** | 09:05 이전 면제 · 09:05부터 `value_acceleration(30초/120초) >= 2.5` + **최소 20틱**. ~~대량체결 버스트~~는 신규 진입에서 빠졌다(재매수·슬롯교체엔 그대로 남음) | 동일 |
| **등락률 하한(전일종가)** | **+0% 초과** — 매수는 무조건 + 일 때만 (2026-08-06 [C]) | 동일 |
| 등락률 상한(전일종가) | **13%** (08-05에 16%→13%) | **10%** |
| **버스트 방향** | **매수 체결만** 센다 (2026-08-06 [B]. 투매를 매수 신호로 읽던 결함) | 동일 |
| ~~상승 이탈 즉시매수~~ | **현재 OFF** (2026-08-06 [D]. 실측 평균 -1.00%/승률 22%) | 동일 |
| ~~VI 상단 근접 매수차단~~ | **현재 OFF**(2026-08-06 사용자 지정). 로직은 보존 | 동일 |
| **시가대비 급등 보류** | **+8% 이상 매수보류** (08-06에 5%→8% 완화) | 미적용(되돌림을 사는 전략) |
| **시가대비 6~8% 버스트 강화** | 버스트 문턱 **x1.5** (매수를 막지 않고 대금만 더 요구) | 동일 |
| 슬롯 | **8** | **0** — [E] 매매 중단(공유 상한 6, 확장 8) |
| **진입 숙성** | 편입 후 **60초**(단 **돌파자동매매용은 30초**, 08-07) | 동일 |
| **VWAP 이격** | 09:05부터 현재가가 세션VWAP보다 **+0.5% 초과** 위여야 매수 (08-07 ON) | 동일 |
| **일봉 눌림 태그** | 관측 전용 — entry_reason에 `[일봉눌림 D-N]`만. **매매 무영향** | 동일 |
| **버스트 파동 상한** | 그날 그 종목의 **1~3번째 폭발**까지만 매수 (2026-08-06 [H]) | 동일 |
| 중복 편입 시 | ~10:30 | 10:30~ |
| 익절 캡 | **4.0%** (개장초반 2.5%) | **2.5%** |
| 상향 캡(강도상승/주도테마) | **6.0%** | 6.0% |
| 손절 | -3% (**워밍업 중에도 작동**) | 동일 |
| 본전스톱 | **ON**(2026-08-04 재활성화, 순+1% 후 본전 이탈 시 청산) | 동일 |

\### 진입 절차 (2026-08-04 전면 변경 — 이게 지금 가장 중요한 흐름)

```
편입(조건검색) -> pre-arm(감시 시작 + 전일종가/시가/**일봉** 캐시 예열)
   |          ** 이 순간부터 [F] 숙성 시계가 돈다 **
   |          ** 일봉 예열은 종목당 하루 1콜(ka10081) — 태그 관측용 **
   v
[무장/발사가 성립해도] 편입 후 숙성시간 안이면 **그 신호는 버려진다** ([F])
   |   요구시간: **돌파자동매매용 30초 / 그 외 60초** (2026-08-07)
   |   복합 편입이면 가장 짧은 값. 조건식을 모르면 60초(보수적).
   |   ⚠️ 'N초 기다렸다 산다'가 아니라 '그 트리거를 포기한다'.
   |      다시 터지면 그때 진입한다.
   v
[등락률 게이트]  전일종가 대비 **0% 초과 ~ 상한(1A 13% / 눌림 10%)**
   |             <- 2026-08-06 [C]. 하한 미달(하락중)이면 여기서 끝.
   |                `_entry_change_reject()` 한 함수가 세 경로 전부를 담당한다.
   v
[VWAP 이격]  09:05부터, 현재가가 **세션VWAP +0.5% 초과** 위여야 통과
   |          <- 2026-08-07 ON. 원천은 0B의 FID14(백만원)/FID13. REST 0콜.
   |             09:05 이전 / VWAP 미수신이면 **통과**(모름이 매수를 막지 않는다)
   v
[무장]  FID228 체결강도 100 이상 3.0초 연속        <- 느린 조건을 먼저
   |
   v
[발사]  🆕 **시간대에 따라 규칙이 다르다** (2026-08-08, `_fire_gate` 단일 창구)
   |
   |   ┌ 09:00 ~ 09:05 ─ **게이트 면제**. 무장만으로 발사한다.
   |   |    ⚠️ 대신 **슬롯을 4칸까지만** 쓴다(EARLY_SLOT_CAP) — 발사가
   |   |       열려 있으므로 개수로 막는다. 남는 2칸은 09:05 이후용이다.
   |   |    '거를 게 없어서'가 아니라 **걸수록 나빠져서**다 —
   |   |    그 구간 버스트는 독립표본 -0.895%(1/3)로 기준선(+0.391%)보다 나쁘다.
   |   |    리스크는 숙성·등락률·파동상한·되돌림 대기가 받는다.
   |   |
   |   └ 09:05 ~ ────── **거래대금 가속도 >= 2.5배**
   |        `value_acceleration(최근 30초 / 120초 균일기대치)`
   |        + **최소 20틱**(틱이 적으면 가속도가 수학적 최대 4.0으로 튄다)
   |        ⚠️ **절대 금액 하한은 두지 않는다** — 실측이 정반대다
   |           (하한 1억 -0.049% / 3억 -0.217% / 5억 -0.572%).
   |
   |   ⚠️ ~~대량체결 버스트~~는 **신규 진입에서 빠졌다**(4일 독립표본 -0.349%,
   |      플러스일 1/5로 기준선보다 나빴다). `check_burst` 자체는 **재매수
   |      판정·슬롯 교체용으로 그대로 살아있다** — 거기까지 바꾸지 말 것.
   |   롤백: `FIRE_GATE_SPLIT_ENABLED = False` -> 전 구간 옛 버스트 3경로.
   v
[파동 순번]  그날 그 종목의 **1~3번째 폭발**까지만 ([H])
   |      쿨다운 60초로 같은 파동은 하나로 접는다. 4번째부터 매수 금지.
   |      (상한을 넘어도 카운트는 계속 돌아 순번이 정확하게 유지된다)
   v
[되돌림 대기]  120초
   |      · -0.3% 도달 -> 50% 매수, -0.7% 도달 -> 나머지 50%
   |      · 120초 안에 안 오면 포기(`되돌림 미도달`)
   |      · ~~+0.3% 돌파 시 즉시 전량~~ -> **2026-08-06 [D]로 OFF**
   |        (추격매수라 실측 평균 -1.00%. `ENTRY_BREAKOUT_ENABLED=True`로 복귀)
   |      (대기 중에도 슬롯 1칸 점유 — 자리를 뺏기지 않기 위해)
   v
매수 체결 -> entry_reason에 `[일봉눌림 D-N 돌파선 P]` 태그(있으면, 관측 전용)
   |          ** 태그는 매수 여부/수량/캡 어디에도 영향이 없다 **
   v
워밍업 60초 -> 청산 규칙 적용
```

- **1종목 = 1슬롯**이다. 분할해도 슬롯 개수·매수금액 총액은 변하지 않는다
  (`occupied_slots()`가 집합 합집합이라 중복 제거, 트랜치 비중 합 = 1.0).
- **시간대 계수는 전 구간 1.00**이라 (1)(2) 경로의 문턱이 시각과 무관하다.
  대신 **주가계수는 종목마다 다르다** — 같은 시각에도 2,000원짜리는 1,650만원,
  3만원짜리는 7,320만원이 절대 문턱이다. 로그의 `경로=` / 계수값을 같이 볼 것.
- **진입 경로가 둘**이다: 틱 경로(`_maybe_tick_entry`)와 폴링 경로
  (`_evaluate_1a_pullback_entry` <- `on_condition_hit`/`watchlist_reentry`).
  **둘 다 되돌림 대기를 거친다** — 08-04에 폴링만 즉시매수하던 버그를 고쳤다.
  진입 로직을 고칠 땐 **반드시 두 경로 모두** 확인할 것.

\### 청산 규칙 한눈에 (2026-08-04 본전스톱 재활성화 반영)

우선순위 순서다. 위에서 걸리면 아래는 안 본다.

| 순위 | 규칙 | 조건 | 비고 |
|---|---|---|---|
| 1 | **손절** | 가격 -3% | **워밍업 중에도 작동**(08-03 수정). (08-05) 거래대금 3배+강도상승이면 **15초 관찰 후 반등 확증 시 추가매수**로 대체 — 미구조분은 최대 -4.5%, 구조분은 **원가 -6%**가 최종선 |
| 2 | **지수 가드** | -5% & 11:00~ | 11:30까지 본전 이상 청산 / 14:50 강제청산 |
| 2-b | **VI 상단 매수차단** | **현재 OFF**. 되살리려면 `VI_UPPER_ENTRY_BLOCK_ENABLED = True`. 폭은 `VI_UPPER_ENTRY_BLOCK_PCT`(0.03) |
| **시가대비 상한** | `PHASE1A_LEADING_OPEN_SURGE_CAP`(8.0, 구값 5.0) |
| **급등 버스트 강화** | `PHASE1A_OPEN_SURGE_STRICT_FROM`(6.0) / `PHASE1A_OPEN_SURGE_BURST_MULT`(1.5). 배수를 1.0으로 두면 무효화 |
| **VI 상단 확정매도** | 정적VI 상단(시가 x1.10)까지 **0.5% 이내 또는 2호가 이내** + **순이익>0** | 2026-08-05 신규. VI 발동 시 2분 단일가라 시장가 매도가 막히는 것을 미리 피한다. **시가 캐시가 없으면 발동 안 함**(전일종가 폴백 금지), **이미 VI선을 넘었으면 발동 안 함**. 워밍업보다 앞(순수 가격 판정). 하단 VI 로직은 없다(손절이 항상 먼저) |
| 3 | (워밍업 60초 — 아래는 여기서 대기) | | 강도·거래량 기반 판정만 유예 |
| 4 | 본전스톱 | 순 +1% 후 본전 이탈 | **현재 ON**(08-04 재활성화) |
| 5 | 익절 캡 | 1A 4.0% / 눌림·개장초반 2.5% / 상향 6.0% | 지수 -5%면 1.5%로 축소 |
| 5-b | **분할 잔량 트레일** | 분할 1차가 나간 포지션이 고점 대비 **3%** 하락 | 2026-08-04 신규 |
| 6 | 동적캡 → **분할매도 50%** | **상향된** 포지션 + 강도·거래량 동시하락 + **순이익>0** | 08-03 결함 3건 수정 / **08-04부터 전량이 아니라 절반**, 잔량은 5-b로 |
| 7 | 손실반등 매도 | 저점 대비 +1% 반등이 강도·거래량으로 미확인 | **50% 분할**(2026-08-05 변경, 잔량은 5-b 트레일). 08-05에 6건 중 5건이 이후 상승해 전량청산이 최대 손실 요인이었다 |
| 8 | 정체 정리 15분 / 시간정리 30분 | **슬롯 만석일 때만** + 가드 중 억제 | 08-03 변경 |
| 9 | 슬롯 교체 | **만석 + 후보 무장 + 후보 버스트** | 08-03 변경, 사실상 거의 안 돎 |
| 10 | 강제청산 | 15:10 (가드 시 14:50) | 진입 종료 14:50 < 청산 |

**강도 하락 판정은 전부 `_strength_baseline`(워밍업 후 리앵커)을 쓴다.**
`entry_strength`(진입 순간값)는 버스트 피크라 기준선으로 쓰면 하락이 필연이다 —
08-03에 이 결함으로 동적캡 6건·슬롯교체 3건이 잘못 발동했다.

- **틱 구동 (2026-08-02 전면 전환)** — 진입 평가가 `on_trade`(체결 틱 콜백)에서
  일어난다. 조건이 차는 **그 틱에서** 발화하며 REST 0콜이다.
  - **무장** = 키움 공식 체결강도(FID 228)가 100 이상으로 **3.0초 연속** 유지
    (`TICK_STRENGTH_SUSTAIN_SEC = 3.0`). 08-03 저녁에 3.0 -> 2.0 -> 1.5로
    완화했다가 **08-07에 실측으로 3.0 복원**했다(1.5~2초 구간이 손익비 0.34로
    압도적 최악이었다). **지금 값은 3.0이다.**
    자체 `compute_strength`를 안 쓰는 이유: 틱이 부족하면 정확히 100.0을
    반환하는데 임계값도 100이라 **데이터가 없을수록 쉽게 뚫린다**. 228은
    다윤님이 HTS에서 보는 값과 동일하고 매 틱에 이미 실려 온다.
  - **발사** = 대량체결 버스트 3경로 OR (창 5초):
    ① **4,000만원 × 주가계수** 이상이 **2건** ② **1억원 × 주가계수** 이상이 1건
    ③ max(**①의 문턱**, **평균 1틱 × 20배**) 이상이 2건 ← 자기 정규화
  - **순서가 핵심**: 느린 조건(강도 **3.0초**)을 무장으로, 순간 이벤트(버스트)를
    발사로 둔다. 반대로 하면 버스트를 본 뒤 3.0초를 기다려 그만큼 늦는다.
  - **주가계수**(①②에, ③은 하한을 통해 간접 적용) = `clamp((주가/10,000)^0.55,
    0.30, 3.00)` — **상한은 08-06에 2.00 -> 3.00**(클램프 시작 73,704원). 2026-08-05 마감 후 신설 — 그 전엔 1,000원짜리와 15만원짜리에
    같은 절대 문턱을 적용해 **저가주에서 절대 경로가 사실상 죽어 있었다**
    (발생빈도 124배 차이 실측). ⚠️ **성과 우위는 입증되지 않았다** — 3일
    백테스트에서 08-05만 개선/08-04 악화/08-03 동일이라 이 프로젝트의 강건성
    기준("양일 각각 개선")을 충족하지 못한다. **근거는 성과가 아니라 구조**다.
    나빠지면 `BURST_PRICE_ALPHA = 0.0` 한 줄로 완전 복귀.
  - **시간대 계수는 전 구간 1.00**이다(2026-08-04에 오후 완화 0.85/0.80/0.95를
    전부 제거). 상수(`TICK_BURST_TIME_MULT`)는 남아 있으나 값이 전부 1.0이라
    실질적으로 무효다 — 1.0 외의 값이 보이면 오후 완화가 되살아난 것.
  - **상대 경로의 분모는 최근 5초를 제외**하고 잰다. 안 그러면 지금 터지는
    대량체결이 평균에 섞여 **버스트가 클수록 문턱도 올라가는 자기모순**이
    생긴다(격리 테스트에서 실제로 재현돼 수정함).
- **Pullback의 분봉·점수·VWAP 경로 폐지** — 눌림 판정은 HTS가 이미 했는데
  코드가 1분봉으로 재검증하느라 (a) 시간축이 안 맞아 26종목 전부 탈락하고
  (b) 1분봉 종가를 기다리느라 구조적으로 최대 60초 늦었다. 함수
  (`evaluate_pullback`/`_apply_vwap_filter`/`evaluate_1a_leading_strength`)는
  삭제하지 않고 호출부만 교체했다.

- **라우팅 (2026-08-01 최종)** — 종목당 항상 한 전략만. `resolve_strategy()`
  한 곳에서 결정하고 라우팅·주문가드·백테스트가 모두 이 함수를 호출한다.
  - 눌림목자동 **단독** -> 항상 Pullback
  - 주도주상위/돌파자동매매용 **단독** -> 항상 1A
  - **중복(둘 다 편입)** -> 10:30 이전 1A / 10:30 이후 Pullback
    (`DUAL_SOURCE_PULLBACK_FROM` — 오전 모멘텀, 오후 되돌림)
  - 주문 직전 가드는 **단독 소스만** 막는다. 중복 종목이 시간대에 따라 두
    전략을 오가는 것은 정상이므로 여기서 막으면 오전 1A 매수가 전부 죽는다.
- **진입 종료 14:50 / 보유분 15:10 전량 강제청산**(FORCE_CLOSE_TIME).
  진입 종료 < 청산이라 청산 중 신규매수가 겹치지 않는다.
- 등락률 상한은 **실제 매수한 전략**(sub_strategy)을 따른다 — 중복 종목이
  오전에 1A로 매수되면 **13%**, 오후에 Pullback으로 매수되면 10%.
- **1A/Pullback 외 전략은 코드째 없다** (2026-08-02 삭제, 그 전엔 비활성 상태로
  남아 있었음). 1B/1L은 이날 제거, 1S/Phase2/Phase3/1N은 `_legacy` 백업 후 제거.
  트레일링 익절도 1L 전용이었어서 같이 사라졌다 — 이제 익절은 예외 없이 flat 캡.
  - **⚠️ `phase1b` 객체 자체는 반드시 유지** — 전략이 아니라 1A/Pullback의
    **체결틱·호가 데이터 파이프라인**(TradeFlowTracker/OrderbookTracker)이라
    없애면 두 전략이 눈이 먼다. 이름만 1B다.
  - 이름이 실제로 사고를 두 번 냈다(08-01 1A 버퍼 삭제 / 07-31 1L 조기청산).
    `TickDataPipeline` 같은 리네임이 근본 해결인데 호출부가 ~40곳이라
    **08-03 실거래 검증 후**로 미뤘다.
  - 복구가 필요하면 커밋 `900757c`에 1B/1L 코드와 파라미터 근거 주석이 전부 있다.
- 주문: 매수는 호가 두께로 시장가/지정가 자동 선택, **매도는 항상 시장가**
  (둘 다 실패 시 반대 방식으로 1회 폴백).
- **주문 우선 레인 (2026-08-02)** — 주문(kt10000/kt10001)은 조회 대기열을
  건너뛴다(`_throttle(priority=True)`, 0.2초 간격). 예전엔 진입 트리거가 걸린
  가장 급한 순간에 방금 나간 조회 때문에 최대 0.6초, 429가 겹치면 +2초를
  기다렸다.
- **pre-arm (2026-08-02)** — 편입 즉시 감시 시작 + 전일종가/시가 캐시를 예열해
  둔다. 트리거 시점에 REST를 부르지 않기 위함(`prearm_candidate`).

\## 다음 할 일 (**2026-08-08(토) 기준 — 다음 거래일 08-10(월)에 미검증 4건이 돈다**)

> ⛔ **읽기 전에 알아야 할 상태 5가지**
> 1. **봇은 정지 상태이고, 스케줄러가 매일 08:59 자동 기동한다.**
>    (08-07은 08:59:01 정상 기동 -> 15:33 정상 자동종료. `STOP_SIGNAL` 없음.)
>    코드 수정은 끝났고 스위트 **1,340건 통과**. `import main` OK.
>    ※ `NEW_SESSION_REQUESTED`가 있는 동안은 **1,339 통과 / 실패 1**이 정상이다.
> 2. 🔴 **08-10(월)에 미검증 변경 4건이 한꺼번에 돈다**(전부 08-07 장마감 후 적용).
>    **결과가 갈려도 어느 쪽 기여인지 구분되지 않는다** — 되돌릴 땐 하나씩:
>    ① **VWAP 진입필터 ON** ② **숙성 조건식별 분리**(돌파만 30초)
>    ③ **스냅샷 폴링 재활성화**(+WS future 디스패치) ④ 일봉 눌림 태그(관측 전용)
>    ※ ④는 매매를 안 바꾸므로 **원인 분리 대상이 아니다.**
> 3. ✅ **미청산 3종목은 봇 관리에서 뺐다**(`status='manual'`) —
>    주성엔지니어링 036930 7주 @140,100 / 다스코 058730 552주 @3,305 /
>    금호타이어 073240 73주 @8,210. 재가동해도 복원되지 않으므로
>    **기동 직후 시장가 손절이 나갈 위험이 없다.**
>    ⚠️ **08-08 이관 시점에도 3건 그대로 남아 있다**(HTS 매도 미확인).
>    판 뒤 `manual_position_tool.py close <코드> <체결가>`로 기록해야 통계에 잡힌다.
> 4. **그 전(08-06~07)의 [A]~[H] 8건 + 무장 3초 + KRX 전환도 표본이 얇다.**
>    즉 지금 **총 14건의 미검증 변경**이 겹쳐 도는 상태다.
> 5. 🔴 **근본 문제는 아직 안 풀렸다.** 진입 신호 자체에 방향성 우위가 없다
>    (4일 75건 +5분 평균 **+0.099%** < 왕복수수료 0.23%). 지금까지의 수정은
>    전부 그 위에서 **손실을 줄이는** 조치이지 우위를 만드는 조치가 아니다.
>    상세는 히스토리 "2026-08-06 (11:00~)" 항목.
>
> 최근 결산: 08-05 **+9,796원**(20건) / 08-06 **-138,152원**(16건, 승 4/16, 손절 7)
> / 08-07 **-42,120원**(10건).

\### 📋 08-07에 바뀐 것 (오늘의 본체 — 오전 2건 + 장마감 후 4건)

| # | 변경 | 근거 | 되돌리기 |
|---|---|---|---|
| **1** | **무장 1.5 -> 3.0초** | 08-05~06 실거래 33건: 손익비 1.41 -> **1.61**, 합계 -6.2% -> **-0.3%**. 제거되는 4건 중 3건이 손실/0% | `TICK_STRENGTH_SUSTAIN_SEC = 1.5` |
| **2** | **조건검색 통합 -> KRX** | **계층 정합성** — 틱·분봉·주문이 전부 KRX인데 조건검색만 통합이었다 | `CONDITION_STEX_TP = "A"` + HTS 대상 통합 |
| **3** | **세션 VWAP 필터 ON** (08-07 저녁) | 단위(백만원) 확정 + 08-07 10건 소급 -4.35% -> -0.20% | `VWAP_ENTRY_ENABLED = False` |
| **4** | **숙성 조건식별 분리**(돌파만 30초, 08-07 저녁) | 돌파 편입 78%가 09:00~09:02라 60초가 그 검색식을 통째로 덮었다 | `MIN_ENTRY_DELAY_SEC_BY_COND = {}` |
| **5** | **스냅샷 폴링 재활성화**(+WS future, 08-07 저녁) | 수집 경로 이중화 | `main.py` gather에서 주석 |
| 관측 | **일봉 눌림 태그**(08-07 저녁) | 관측 전용 — **매매 무영향** | `DAILY_PULLBACK_TAG_ENABLED = False` |

\#### 1. 무장 3초 복원 — 근거가 가장 강하다

08-03 저녁에 3.0 -> 2.0 -> 1.5로 완화하면서 "**FID228 이력이 없어 재현이
원리적으로 불가능**"하다고 적고 근거 없이 내렸는데, **그 이력은 로그에 있었다**:
```
🔫 무장 — 체결강도 100 이상 N초 연속
⚡ 틱 진입 트리거 (강도 100+ N초 연속, 4,000만원+ 체결 2건(x1.00))
```
DB 실체결과 (날짜, 종목코드)로 역매칭하면 매수 시점의 무장 지속시간과 버스트
경로/규모가 복원된다(08-03~06 실거래 75건 중 **72건 매칭**).

**08-05 이후 33건 실측** (08-03/04는 모의투자 + KRX 조건식이라 유니버스·수수료가
달라 제외 — 사용자 지적으로 걸러냈다):

| 컷오프 | 거래 | 승률 | 손익비 | 합계 | 08-05 | 08-06 |
|---|---|---|---|---|---|---|
| 1.5초(구) | 33 | 36% | 1.41 | -6.2% | +4.6 | -10.8 |
| **3.0초** | 29 | 38% | **1.61** | **-0.3%** | +4.4 | -4.7 |

제거되는 4건 = 대원전선 0.00% / 포톤 +0.19% / 대원전선 -3.02% / 에스피지 -3.06%.
⚠️ **4.0/5.0초로 더 올려도 결과가 완전히 동일하다**(3~5초 구간에 거래가 없다) —
3.0은 '가장 느슨하면서 같은 효과를 내는' 지점이라 골랐다.
구간별로는 **1.5~2초가 압도적 최악**(6건 승률 17% 손익비 0.34 기대값 -1.40%).

\#### 2. 조건검색 KRX 전환 — 근거는 성과가 아니라 정합성

계층마다 거래소 기준이 달랐다:
```
조건검색(편입)    = 통합(KRX+NXT)   <- 08-05에 여기만 바뀌었다
실시간 체결틱 0B   = KRX만           <- 무장·버스트·VWAP의 근간
REST 분봉/현재가   = KRX만           <- 시가·전일종가·등락률
주문/잔고          = KRX만
```
즉 **'통합 기준으로 고른 종목'을 'KRX 데이터로만' 판정**하고 있었다.
NXT 상장 종목에서 **못 보는 거래량이 평균 54%**(금호타이어 KRX 19.9M + NXT 20.3M).

⚠️ **"08-06 손실이 통합 때문"이라는 진단은 데이터가 지지하지 않는다**:

| 구분 | 청산 | 승률 | 손절 | 합계 |
|---|---|---|---|---|
| NXT 상장(통합 영향 O) | 4 | 25% | 2 | -3.3% |
| **KRX 전용(통합 영향 X)** | 12 | 25% | 5 | **-7.5%** |

KRX 전용은 NXT 거래가 없어 통합=KRX로 데이터가 동일하다 — 16건 중 12건은
통합 설정이 **원리적으로 영향을 줄 수 없는데** 손실의 69%가 거기서 나왔다.
08-06은 옛 코드로 돌았다(무장 1.5초 / 상승이탈 ON / 파동 무제한 / 시가 버그).

**진입 판정은 이 변경으로 하나도 바뀌지 않는다** — `CONDITION_STEX_TP`는 `core/`
어디에도 없고 `on_condition_hit`도 거래소 인자를 받지 않는다. 08-05 틱 재생
결과가 완전히 동일했다. **바뀌는 것은 유니버스뿐이고, 유니버스 변화는
조건검색이 시점 조회만 되므로 원리적으로 백테스트가 불가능하다.**

\#### 3. 세션 VWAP — **현재 ON** (08-07 저녁 적용)

`VWAP_ENTRY_ENABLED=True` / `VWAP_ENTRY_MIN_GAP_PCT=0.5` / `VWAP_ENTRY_FROM=09:05`
/ `VWAP_UNIT_SCALE=1_000_000.0`.
원천은 **0B의 FID 14(누적거래대금) ÷ FID 13(누적거래량)** — 거래소 누적이라
진짜 세션 VWAP이고 **REST 0콜·O(1)**. 파서가 `raw`를 통째로 넘겨 파서 수정 불필요.

✅ **단위 확정 (08-07 09:00:06 실거래)** — FID 14는 **백만원 단위**다.
`VWAP_UNIT_SCALE = 1_000_000.0`으로 **수정 완료**(08-07 오전).
실측: `[460940] FID13=7,649주 / FID14=43 / 현재가=5,640원` ->
실제 거래대금 7,649x5,640 = **43.14 백만원** vs FID14=43(오차 0.33%).
비율(VWAP/현재가)이 원 0.0000 / 천원 0.0010 / **백만원 0.9967** / 억원 99.67이라
1e6 외엔 자릿수가 맞지 않는다.

⚠️ **FID 14는 백만원 단위 '정수'**라 값이 작으면 양자화 오차(±0.5/값)가 판정
문턱(0.5%)을 넘을 수 있다. `VWAP_ENTRY_FROM=09:05`가 그 문제까지 막아준다 —
08-06 실측 09:05 누적: 코스모 455억(FID14=45,495, 오차 0.001%) / 대원전선
273억(0.002%) / **현대약품 5.78억(FID14=578, 오차 0.086%)** <- 가장 얇은 종목도
문턱의 1/6이다. **09:05를 앞당기려면 이 오차를 다시 계산할 것.**

🟢 **잘못된 단위는 위험하지 않았다** — scale=1.0이면 VWAP이 0.0056원이 되어
gap이 1억%가 나오고, `gap <= 0.5`가 항상 False라 **아무것도 막지 않는다.**
이 필터는 고장 나면 '조용히 통과'시키는 방향이라 매매를 죽이지 않는다.
대신 고장을 눈치채기 어려우므로 `🔎 세션VWAP 단위확인` 로그를 **감시용으로
유지**한다(비율이 0.9~1.1을 벗어나면 경고 문구가 같이 찍힌다).

✅ **08-07 장마감 후 ON으로 전환했다**(사용자 지정). 08-07 실거래 10건 소급에서
손실 2건을 정확히 걸렀다 — 심텍 09:12(이격 **-2.01%**, 실현 -1.12%) / 샘씨엔에스
09:59(**-1.18%**, -3.03%). 가격기준 **-4.35% -> -0.20%**, 실현금액
**-42,120원 -> -17,110원**(개선 +25,010원).

⚠️ **근거는 여전히 얇다.** 4일 69건으로는 손익비 1.03 -> **1.09**에 그치고
(+1.0% 이상 구간은 오히려 0.80으로 악화), 08-07 이랜시스는 이격 +2.36%로
통과했는데 -3.11% 손절이었다(손실 3건 중 1건은 못 잡는다). n=10 하루치이고
**차단된 2건 대신 다른 종목을 샀을 반사실은 재현되지 않았다.**
나빠지면 `VWAP_ENTRY_ENABLED = False` 한 줄.

\### 🔬 08-10(월) 장중에 반드시 볼 것 — 우선순위 순

| 순위 | 볼 것 | 정상 | 이상하면 |
|---|---|---|---|
| **0** | 🆕🔴 **09:00~09:05 매수 건수** | **최대 4건**(초반 슬롯 캡) | 5건 이상이면 캡이 안 도는 것 — `EARLY_SLOT_CAP_ENABLED` 확인. 여전히 많다고 느끼면 `EARLY_SLOT_CAP`을 3으로 |
| **0-a** | 🆕 **`개장초반 슬롯 캡` 진단** | 09:05 이전에 뜨는 게 정상(캡이 도는 증거) | 0건이면 캡에 닿은 적이 없다는 뜻(무해) |
| **0-b** | 🆕 **`거래대금 가속 미달` 진단** | 09:05 이후 뜨는 게 정상(새 발사 조건이 도는 증거) | 하루 종일 **0건**이면 발사 게이트가 안 도는 것 / 그것뿐이면 2.5가 과하다 |
| **0-c** | 🆕 **`체결틱 부족` 진단** | 소수 | 급증하면 `FIRE_ACCEL_MIN_TICKS`(20)가 과하거나 구독이 부실한 것 |
| **1** | **09:10까지 편입 신호가 오는가** | 09:00 이후 실시간 편입 정상 | 09:10에도 0건이면 **HTS [0156] '대상'이 KRX인지 확인** |
| **2** | 🆕 **`VWAP 이격 미달` 진단 분류** | 뜨는 게 정상(필터가 도는 증거) | 하루 종일 **0건**이면 VWAP 미수신을 의심 — `🔎 세션VWAP 단위확인` 로그의 비율이 0.9~1.1인지 볼 것 |
| **3** | 🆕 **폴링이 새로 잡는 종목** | `task_condition_snapshot_poll` 로그가 60초마다 | **체결틱이 끊기거나 `concurrent recv` 에러**가 보이면 즉시 폴링을 다시 끌 것(main.py gather 주석) |
| **4** | 🆕 **돌파자동매매용 매수 비중** | 늘어야 정상(숙성 30초) | 그대로면 숙성이 병목이 아니었던 것 |
| 5 | 매수 건수 | 5~15건 수준 | 08-06처럼 18분에 19건이면 게이트가 안 먹는 것 |
| 6 | `진입 숙성 대기` / `버스트 파동 상한` 진단 | 뜨는 게 정상 | 하루 종일 이것뿐이면 게이트 과다 |
| 7 | 눌림목(`1A_눌림`) 매수 | **0건이 정상**([E] 슬롯 0) | 1건이라도 있으면 `PULLBACK_MAX_SLOTS` 확인 |
| 8 | `⚡ 되돌림 포기 — 상승 이탈` | **안 뜨는 게 정상**([D] OFF) | 뜨면 플래그가 되살아난 것 |
| 9 | 🆕 `[일봉눌림 D-N]` 태그 | entry_reason에 일부만 붙음(08-07 기준 22%) | 전부 붙거나 0건이면 판정 파라미터 확인 |

⚠️ **08:59 기동 스냅샷이 0종목인 것은 정상이다.** KRX 개장 1분 전이라 당일
데이터가 없다. `_subscribe_conditions()`가 스냅샷보다 **먼저·독립적으로**
구독을 등록하므로 09:00부터 실시간 편입은 정상 수신된다(코드로 확인함).

⚠️ **매수 0건인 날이 나와도 그 자체는 실패가 아니다.** 진단 알림의 사유 분포를
보고 '정상 필터링'인지 '코드가 죽은 것'인지 구분할 것 —
`_REJECT_INFRA`(체결틱 부족 / 데이터소스 없음 / 버스트 계산 실패)가 뜨면 코드 이상이다.

\### 🔴 다음에 할 만한 것 (우선순위 순)

0-a. 🆕🔴 **[VWAP 사각지대] 08-10 장중에 트랜치 체결 수를 볼 것.**
   VWAP 게이트가 **트리거가**로 사전 검사하는데 체결은 -0.3/-0.7% 낮은 가격에서
   난다. 트리거 이격 **0.50~0.80%면 슬롯만 잡고 한 건도 체결 안 되고**,
   **0.80~1.21%면 1차만 체결 = 반쪽 포지션**이다(실제 코드로 재현함).
   · 08-05 소급: 삼기(+6.25%, +7,020원)가 정확히 죽은 구간이라 통째로 사라진다.
   · **증상**: 로그에 `되돌림 대기 종료` 시 `1/2 트랜치`가 잦거나, 계획을
     걸고 120초 뒤 아무 체결 없이 만료되는 건이 늘어난다.
   · 고칠 때 선택지 (a) **계획 생성 시 가장 깊은 트랜치가(-0.7%)로 검사** ←구조적
     (b) 문턱을 1.21% 이상으로 (c) 되돌림 폭 축소.
   · 상세는 히스토리 "2026-08-08" [발견 ①].
0-b. 🆕 **[VWAP x rescue] 손절대신추가매수가 거의 안 돈다는 것을 알고 있을 것.**
   rescue는 -3% 지점에서 발동하는데 VWAP을 통과하려면 **진입 시점 이격이
   +3.7% 이상**이어야 한다(실측 21건 중 2건뿐). 안전 측면은 동일하다
   (손절로 수렴). 08-07의 `[C] x rescue`와 같은 계열이다.
0. 🆕 **08-10 결과로 미검증 4건 판단** — 되돌린다면 **하나씩**. 근거 강도가
   `숙성 분리 < VWAP < 폴링` 순이므로 **숙성 분리를 먼저 되돌리는 것**이
   순서상 맞다(그 항목은 08-07 실측으로 **오히려 손해였을 수도** 있다 —
   숙성이 막은 2건이 합계 -0.96%였다).
1. 🆕 **일봉 눌림 태그 표본이 쌓이면 → 눌림 종목만 익절캡 상향**
   (현재 4.0%/2.5% → 6.0%). **이번 세션에서 가장 근거가 강한 다음 수순**이다.
   · 판단 기준: 태그 매수가 **30건 이상** 쌓이고, **날짜별로 3일 이상**
     대조군 우세면 적용. (08-07까지는 17건 / 4일 중 3일 우세.)
   · 확인 명령:
     ```bash
     python -c "from db.connection import get_connection
     with get_connection() as c:
         cur=c.cursor(); cur.execute(\"SELECT trade_date,COUNT(*),AVG(profit_rate),SUM(profit_amount) FROM trades WHERE entry_reason LIKE '%일봉눌림%' AND status='closed' GROUP BY trade_date ORDER BY trade_date\")
         [print(r) for r in cur.fetchall()]"
     ```
   · ⚠️ **적용 전 반드시** 상수 블록의 [한계] 5줄을 읽을 것 —
     중앙값 +0.19% / 기간 5구간 중 1개 마이너스 / 08-05의 "캡 도달 종목
     77%가 본전 회귀" 실측과 충돌 가능.
2. **[I] FID228을 틱 아카이브에 저장** — 긴급도는 내려갔다(로그로 대체 가능함이
   밝혀졌다). 다만 로그는 '매수된 건'만 남고 **탈락한 건의 강도는 안 남으므로**
   무장 임계값(100)이나 요구시간을 더 손대려면 여전히 필요하다.
3. **조건검색식 재편 검토** — 층위별로 결론이 갈린다는 걸 08-07에 확인했다:
   **조건식 자체**는 돌파가 낫고(2일 대조 -0.46% vs 주도 -1.13%, 양일 모두),
   **우리 실매수**는 주도가 낫다(실전 3일 +0.76% vs -0.17%). 그 간극이
   숙성 때문이라고 보고 08-07에 돌파만 30초로 풀었다 — **그 결과를 먼저 볼 것**.
   `눌림목자동`은 4일 46건에서 우위 없음(-0.27%, 1/4)이라 슬롯 0 유지가 맞다.
4. **`phase1b` -> `TickDataPipeline` 리네임** — 이름이 실거래 사고를 두 번 냈다.
   호출부 ~40곳이라 계속 이월 중.
5. **`strategy_manager.py` 분리** — 6,000줄 단일 파일. **1단계(상수 1,182줄을
   `core/strategy_config.py`로)는 역의존이 0으로 확인돼 기계적으로 가능**하다.
   `from core.strategy_config import *` 재수출이면 외부 4개 모듈의 import를
   한 줄도 안 고쳐도 된다. ⚠️ 테스트 4곳이 `strategy_manager.py`를 통째로 읽어
   문자열을 단언하므로 같이 손봐야 한다(1곳은 상수 분리만으로도 깨진다).
   **미검증 변경이 정리된 뒤에 할 것.**

\### ⚠️ 실거래 검증이 안 된 것 (= 지금 도는 것 전부)

| 항목 | 적용일 | 실거래 근거 | 강도 |
|---|---|---|---|
| 🆕 **VWAP 깊은 검사** | 08-08 | 아카이브 5일 통합 리플레이: 매수 68 -> **69건**(감소 없음), 되돌림 미체결 중 VWAP 사유 **5 -> 2건**. 구조적 정합성이 주 근거 | 구조 |
| 🆕 **우선순위 교체 한도 6** | 08-08 | 실측 빈도(4일 1회)보다 넉넉한 **안전 상한**. 초반 캡이 이 경로를 자주 열게 만든 부작용 차단 | 구조 |
| 🆕 **개장초반 슬롯 캡 4** | 08-08 | 실측 가속도 순위 상위 4개 +1.738%(4/5) vs 대조군 +0.754%(3/5). **사후 순위라 실전 재현은 근사**(candidate_tier 교체가 대신한다) | 중 |
| 🆕🔴 **발사 게이트 분리**(09:05 이전 버스트 OFF / 이후 가속 2.5) | 08-08 | 틱 아카이브 5일 146 종목·일 **독립표본**: 버스트 -0.349%(플러스일 1/5) vs 가속2.5 +0.688%(4/5). **반사실 미재현 / 09:00~09:15 창뿐 / 무장은 FID228이 없어 검증 못 함** | 중 |
| 🆕 **VWAP 필터 ON** | 08-07 저녁 | 08-07 10건 소급 -4.35%->-0.20%. 4일 69건으로는 손익비 1.03->1.09에 그침 | 중 |
| 🆕 **숙성 조건식별 분리**(돌파 30초) | 08-07 저녁 | 08-07 숙성 차단 4건이 전부 돌파 최초편입. **단, 그 2건은 합계 -0.96%라 숙성이 도움이었다** | **약** |
| 🆕 **스냅샷 폴링 재활성화** | 08-07 저녁 | 성과 근거 없음 — **수집 경로 이중화**가 근거. WS future 디스패치로 틱 유실 0 | 구조 |
| 🆕 **일봉 눌림 태그** | 08-07 저녁 | **관측 전용 — 매매 무영향**(검증 대상 아님) | — |
| **무장 3.0초** | 08-07 | 08-05~06 33건 역매칭(손익비 1.41->1.61). 반사실 미검증 | 중 |
| **조건검색 KRX** | 08-07 | 성과 근거 없음 — **정합성**이 근거 | 구조 |
| [A] `_today_open` 절단 가드 | 08-06 | 확정 결함(재현됨) | 강 |
| [B] 버스트 매수방향 한정 | 08-06 | 확정 결함이나 소급 검증 불가 | 중 |
| [C] 등락률 하한 0% | 08-06 | 08-06 승자 4건 전부 생존 | 중 |
| [D] 상승 이탈 OFF | 08-06 | 하루치 n=9 | **약** |
| [E] 눌림목 슬롯 0 | 08-06 | 4일 편입 46건(플러스일 1/4) | 중 |
| [F] 진입 숙성 60초 | 08-06 | **4일 전부 개선** | **강** |
| [H] 파동 상한 3 | 08-06 | 4일 3,370건 + 실거래 교차검증 | **강** |
| 매수금액 100만원 | 08-06 | 사용자 지정 | — |


\### 📚 08-06 [A]~[D] 상세 근거 (보관 — 필요할 때만 읽을 것)

아래는 08-06에 고친 개별 항목의 재현·수정·검증 절차다. 지금 도는 사양은
위 요약표와 "현재 전략 요약"에 전부 반영돼 있으니 **통독하지 말 것.**

\### ✅ [A] `_today_open` 장중 재편입 버그 — **2026-08-06 수정 완료**

**증상**: 08-06 09:18:21에 금호타이어를 **시가대비 +14.03%**에 매수했다(상한 8%).

**원인 (두 겹)**

| # | 위치 | 문제 |
|---|---|---|
| A-1 | `_today_open()` | 조회 창에 09:00봉이 없으면 **가장 이른 봉(09:03)의 시가**를 당일 시가로 반환. 개장 직후만 맞고 장중 재편입에선 틀린다 |
| A-2 | `on_condition_hit` | `open_price **or** 캐시`라, 09:00:55에 정확히 캐시된 7,200을 두고 **갓 계산한 틀린 7,650이 이겼다** |

```
진짜 당일 시가(09:00봉) = 7,200원 -> 매수가 8,210 대비 +14.03% -> 차단됐어야 함
_today_open() 반환값     = 7,650원(09:03봉)      -> +7.32%  -> 8% 미만이라 통과
```

**수정**
- **A-1 창 절단 가드**: 리스트에서 **가장 오래된 봉**이 전일 것이면 창이 하루
  전체를 덮은 것이므로 그 안의 '오늘 첫 봉'을 신뢰한다(거래가 뜸해 09:00봉이
  없는 종목도 이 경우엔 정상 처리). 가장 오래된 봉까지 오늘 것이면 창이 잘렸을
  수 있으므로, 첫 봉이 실제 개장봉(`GROUP_A_START`)이 아닌 한 **0.0(판단 포기)**.
  → 모르는 값으로 막지도, 틀린 값으로 통과시키지도 않는다.
- **A-2 캐시 우선**: `self._opening_prices.get(...) or open_price`.
  캐시는 ka10001 `open_pric`(거래소 확정 시가)이라 분봉 추정보다 정확하다.

**검증** (`test_patch_20260806.py` [1][2], 11건)
① 절단 창(09:03~09:18) -> `0.0` ② 캐시 7,200이 이김 ③ 그 상태에서 8,210 매수가
`시가대비 +14.0% >= 8%`로 차단 ④ 대조군 — 09:00봉이 있으면 7,200 정상 반환,
전일봉이 섞이면 09:03봉도 정상 신뢰, 09:00봉 단독도 통과.

\### ✅ [B] 버스트가 매도 체결을 구분하지 않는다 — **2026-08-06 수정 완료**

**원인**

```python
# core/strategy/trade_flow.py — count_large_trades / max_single_trade_value
for ts, price, _, volume in d          # <- 이 '_'가 side다. 버려졌다.
```

**대량 투매도 버스트로 셌다.** 무장에 쓰는 FID 228은 **당일 누적**이라 오전에
강했던 종목은 급락 중에도 100 위에 머문다 → "오전에 강했던 종목이 대량 투매로
무너지는 순간"이 그대로 매수 신호가 됐다. 08-06 손절 7건의 구조적 원인.

**수정** (MD 1안 채택 — 기존 호출부가 조용히 안 바뀌게)
- `count_large_trades(..., side=None)` / `max_single_trade_value(..., side=None)`
  파라미터 추가. **기본값 None = 방향 무관 = 기존 동작.**
- `check_burst`만 `side="buy"`를 넘긴다(`BURST_REQUIRE_BUY_SIDE`).
  `candidate_tier` 등 다른 호출부는 **의도적으로 그대로** 뒀다.
- 상대 경로는 **분자만** 매수로 좁힌다 — 분모(`avg_trade_value`)는 '평상시 1틱
  크기'라 방향 무관이 맞고, 그 조합은 판정을 더 엄격하게 만든다.

**⚠️ `neutral` 걱정은 근거가 없었다** — WS 파서가 `volume = abs(volume_signed)`로
넣으므로 `neutral`(FID 15 == 0)이면 volume이 0이고 `price * volume == 0`이라
**어떤 문턱도 원리상 못 넘는다.** 테스트로 못박아 뒀다.

**검증** (`test_patch_20260806.py` [3], 13건)
① 매도 대량체결만으로는 `check_burst` 미발화 ② 같은 규모가 매수면 정상 발화
(대조군) ③ neutral은 side 무관하게 영향 없음 ④ `side` 기본값이 None인지 시그니처로
확인 + 혼재(매수1+매도2)면 2건 조건 미달.
⚠️ **08-06 승자 4건 소급 확인은 못 했다** — 그날 09:19 정지로 틱 아카이브
(09:20 태스크)가 생성되지 않아 재생이 원리상 불가능하다. 대신 [C]에서 승자
4건이 전부 통과하는 것을 확인했다.

\### ✅ [C] 등락률 **하한** 신설 — 매수는 무조건 + 일 때만 (사용자 지정)

`MIN_ENTRY_CHANGE_PCT = 0.0` — 전일종가 대비 이 값을 **초과**해야 매수한다.

두 전략 모두 **상승을 사는 전략**이다(1A=매수세 폭발 / Pullback=상승 중 되돌림).
전일 대비 하락 중인 종목은 어느 쪽 전제에도 안 맞는다. [B]의 가격 축 방어선이다.

**08-06 소급 검증 (매수 19건)**

| | 결과 |
|---|---|
| 하한에 걸리는 건 | **주성엔지니어링 2건뿐** (-1.26% / -1.96%) |
| **승자 4건** | **전부 통과** — 코스모 +7.90 / 금호타이어 **+0.25** / 현대약품 +4.61 / GS건설 +3.69 |

⚠️ 금호타이어가 **+0.25%**로 아슬아슬하다. 하한을 0보다 올리면(예: +1%) 그
승자(+2.85%)를 잃는다. **그래서 정확히 0.0이다.**

⚠️ **시가 대비 하한은 일부러 두지 않았다** — Pullback은 정의상 당일 고가에서
되돌린 자리를 사는 전략이라 시가 아래 매수가 정상 동작일 수 있다.

**구현 — 규칙을 한 곳에 모았다**: `_entry_change_reject()` 신설.
상·하한이 **세 경로**(`_open_entry_plan` / `_execute_buy` / 상승이탈 재확인)에
복제돼 있었다. 이 코드베이스의 반복 사고 패턴이 정확히 이것이라, 하한을 넣으면서
셋을 하나로 접었다. **새 조건은 호출부를 늘리지 말고 이 함수에만 넣을 것.**
전일종가를 모르면 통과(기존 규약 — 모름이 매수를 막지 않는다).
진단 분류 `등락률 하한 미달(하락중)` 신설(상한과 반대 상황이라 반드시 분리).

\### ✅ [D] 상승 이탈 **OFF** 전환

`ENTRY_BREAKOUT_ENABLED = False`. 08-05 도입 근거는 **가정 시뮬레이션**이었고,
08-06에 처음 나온 **실거래 결과**가 반대였다:

| 경로 | n(청산) | 평균 | 승률 | 손절 | 합계 |
|---|---|---|---|---|---|
| **상승이탈** | 9 | **-1.00%** | 22% | 4건 | -8.96% |
| 되돌림(08-05+06) | 27 | **+0.23%** | 41% | 6건 | +6.30% |

구조적으로도 설명된다 — 트리거 시점은 이미 국소 고점인데(되돌림 대기를 넣은
이유가 그것이다), 상승 이탈은 **거기서 더 높은 가격에 사는 추격매수**라 되돌림
대기의 전제를 정면으로 거스른다. 08-06 손절 7건 중 4건이 이 경로였다.

⚠️ **문턱 상향은 대안이 아니다.** 08-06 발동 15건이 전부 **+0.30~+0.52%**
(중앙값 0.36%)에 몰려 있어 0.6%로 올리면 15건 전부 탈락 = 끄는 것과 동일하다.

⚠️ **근거는 하루치(n=9)다.** 이 경로는 08-05 13:54에 도입됐지만 그날은 발동
0건이라 실거래 표본이 있는 날이 08-06 하루뿐이다. 이 프로젝트가 요구해 온
"양일 각각 개선" 기준을 만족하지 못한다. 로직은 보존했고 `True` 한 줄로 복귀된다.

\### ⚠️ 아직 판단 보류 중인 것 (표본 부족)

| # | 항목 | 상태 |
|---|---|---|
| A | 재매수 문턱 배수 2.5 | 08-06 발동 0건. 계속 관측 |
| B | 시가대비 상한 8% 적정값 | **[A] 버그로 08-06 데이터가 오염됐다.** 이제 필터가 진짜 시가로 도니 **재가동 후 처음으로 유효한 측정**이 된다 |
| C | 3~6% 구간 대응 | 🔴 **구간 효과가 아니라 날짜 효과였다**(같은 3~6% 구간이 08-04 **-2.22%** / 08-05 **+0.72%**, 같은 날 ~3% 구간은 08-04 +1.83%). **구간별 평균은 반드시 날짜로 쪼개 볼 것** — 3일 36건에서 하루가 10~20건이라 하루의 색깔이 구간 평균을 그대로 지배한다. **별도 대응 불필요** |
| D | 3만원대 버스트 문턱 | MAX 3.0은 73,704원 이상에만 적용. 3만원대(GS건설 32,600, 08-06 -3.07%)는 불변. ALPHA를 올리면 2만원대 승자까지 잡혀 보류 |
| E | 상대 경로 폐지 | 주가 스케일로 사실상 무력화(상대 하한 = burst_min). [B]로 매수 방향까지 요구하게 돼 더 좁아졌다. 코드 삭제는 미착수 |
| F | 오후장 접기 | 08-06은 오전에 멈춰 데이터 없음 |
| G | 슬롯 교체를 틱 경로로 | 이월 |
| H | `phase1b` -> `TickDataPipeline` 리네임 | 이월(호출부 ~40곳) |
| I | **FID228 틱 아카이브 저장** | 미착수. 🔻 **긴급도 하향**(08-07) — 무장 이력이 **로그에 남아 있음**이 밝혀져 DB 역매칭으로 대체 가능하다. 다만 로그는 '매수된 건'만 남고 **탈락한 건의 강도는 안 남으므로**, 무장 임계값(100)이나 요구시간을 더 손대려면 여전히 필요하다 |
| J | **[D] 상승이탈 재평가** | OFF 상태로 며칠 관찰 후, 되돌림 미도달이 급증하면(=진짜 폭등을 놓치고 있으면) 재검토 |
| **K** | **VWAP 필터 유지 여부** | ✅ **08-07 저녁에 ON으로 전환했다**(단위는 백만원 확정). 이제 판단할 것은 '켤지'가 아니라 **'유지할지'**다. 08-07 10건 소급은 -4.35% -> -0.20%로 좋았으나 4일 69건으로는 손익비 1.03 -> 1.09에 그친다. **08-08 장중에 `VWAP 이격 미달` 진단이 몇 건 뜨는지**로 판단할 것 — 0건이면 필터가 안 도는 것이고, 하루 종일 그것뿐이면 과다다. 끄려면 `VWAP_ENTRY_ENABLED = False` |
| **L** | **KRX 전환 유지 여부** | 08-07 적용. 성과가 아니라 **정합성**이 근거다. 08:59 스냅샷 0종목은 정상일 수 있으니 **09:10 실시간 편입 여부**로 판단할 것 |
| **M** | **`strategy_manager.py` 분리** | 6,029줄. 1단계(상수 1,182줄 -> `core/strategy_config.py`)는 **역의존 0으로 확인돼 기계적 분리 가능**. 재수출로 외부 import 불변. ⚠️ 테스트 4곳이 파일을 통째로 읽어 문자열 단언하므로 같이 손볼 것 |
| **N** | 🆕 **일봉 눌림 종목 익절캡 상향** | 08-07 태그 신설. **표본 30건 + 날짜 3일 이상 우세**면 캡 4.0/2.5% -> 6.0% 적용 검토. 근거: 캡 도달률 41% vs 3%, 일봉 +5일 +4.66%. ⚠️ 중앙값 +0.19% / 기간 1구간 마이너스 / 08-05 "캡 도달분 77% 본전 회귀"와 충돌 가능 |
| **O** | 🆕 **눌림 고가 돌파를 인트라데이 트리거로** | `daily_pullback_state()`가 `pullback_high`를 돌려주므로 장중 돌파 감지가 가능하다. 다만 **새 진입 경로**라 이 코드베이스에서 가장 위험한 부류(호출부 3곳 규칙). N을 먼저 하고 그 결과를 보고 판단할 것 |
| **P** | 🆕 **조건검색 수집 이중화 잔여** | 폴링은 08-07에 살렸다. 남은 것: `ConditionManager.update_snapshot()` 죽은 코드(`jmcode` vs 실제 키 `'9001'`) / 스냅샷 타임아웃 시 재시도 없음(08-06 seq=2 실측) |
| **Q** | ✅ **거래대금 가속도를 진입에 쓸 것인가** | **08-08에 적용했다**(09:05~ 버스트 대체, 2.5배). ⚠️ **이 항목의 옛 권고 2개가 실측으로 뒤집혔다**: ① "절대 규모 하한 필수" -> **정반대**(하한 1억 -0.049% / 3억 -0.217% / 5억 -0.572%, 단조 악화). 필요한 건 금액이 아니라 **최소 틱수**(20)다. ② "2.0x가 생존자" -> 격자 기준이었고 **독립표본(첫 신호)으로 보면 2.0은 기준선에 진다**(+0.218% vs +0.395%). 이제 판단할 것은 '쓸 것인가'가 아니라 **'2.5가 맞는가'** — 3.0은 +1.968%(하루 5.6종목), 2.5는 +0.688%(하루 10.8종목) |
| **R** | 🆕 **틱 아카이브 전 시간대 확대** | 지금은 09:00~09:15뿐이라 **시간대별 검증이 원리적으로 불가능**하다(분봉으론 해상도가 달라 30초 스케일 우위가 안 잡힌다). `ka10079` 소급 수집으로 가능, 10종목x4일 약 40분. 이게 되면 시간대별 + 무장 반사실 둘 다 열린다 |
| **S** | 🆕 **장전 스캐너 Phase 1/2** | Phase 0(관측)은 08-08에 가동. **Phase 1** = 후보 종목만 익절캡 상향(진입 무변경, 상수 1개). **Phase 2** = 돌파선을 진입 트리거로(새 경로 = 최고 위험). 판단 기준은 **태그 30건 + 날짜 3일 이상 우세** |

\### 🐛 알려진 결함 (무해하지만 기록)

- `kiwoom_ws.py:229`가 `item["jmcode"]`를 쓰는데 실제 키는 `'9001'` —
  `ConditionManager.update_snapshot()`이 한 번도 호출된 적 없다(죽은 코드).
- `theme_manager` DB 저장 실패("more than one '%s' placeholder"). 매매 무관.
- `0g` 실시간이 프로그램매매가 아니라 '주식종목정보'다. 단 **VI 계산에 쓰는
  305/306/307(상한가/하한가/기준가)이 여기 실려 온다** — 구독을 빼지 말 것.
- `CandleCache.log_stats()`가 어디서도 호출되지 않는다.
- ~~`task_condition_snapshot_poll` 비활성 유지~~ -> **08-07 저녁에 재활성화됐다**
  (`main.py:1542` gather에 등록, 주기 60초). WS future 디스패치로 틱 유실 0.
  끄려면 그 줄을 주석 처리. 상세는 히스토리 "2026-08-07 (15:20~)" ③ 참고.

\### ⚠️ 이 코드베이스의 반복 사고 패턴 (매번 확인할 것)

**같은 규칙이 여러 경로에 흩어져 있어, 한 곳만 고치고 끝냈다가 조용히 어긋난다.**

- **매수금액 3곳**: `portfolio_optimizer.DEFAULT_BASE_AMOUNT`(**실지배값**) /
  `phase_settings.position_amount` / `order_manager.BUY_AMOUNT_PER_STOCK`
- **`_execute_buy` 호출부 4곳**: `_evaluate_1a_pullback_entry`(폴링) /
  `_try_fill_entry_plan`(되돌림 트랜치 2곳) / `_do_rescue_add`(추가매수) /
  🆕 **`_maybe_tick_entry`(즉시매수 폴백)** — `ENTRY_PULLBACK_ENABLED=False`일
  때만 도달하므로 지금은 사실상 죽은 경로지만 **되돌림을 끄는 순간 살아난다.**
  ⚠️ 이 목록이 오래 "3곳"으로 적혀 있었다(08-08 심층 감사에서 발견). 목록이
  틀리면 "새 규칙을 모든 매수 경로에 반영했는가"라는 점검이 통째로 샌다 —
  `audit_deep_20260808.py` [2]가 이제 호출부 수를 기계적으로 센다.
- **청산 경로 2곳**: `on_price_update`(틱) / `check_timeouts`(폴링)
- **강도 하락 판정 3곳**: `_update_dynamic_caps` / `_is_strength_rising_vs_entry` /
  `find_stagnant_holding`(slot_replacement.py)
- **전면차단은 `_entry_block_reason()` 하나만** 볼 것
- **등락률 상·하한은 `_entry_change_reject()` 하나만** 볼 것
  (2026-08-06에 세 경로 — `_open_entry_plan` / `_execute_buy` / 상승이탈
  재확인 — 에 복제돼 있던 것을 접었다. **호출부를 늘리지 말 것**)
- **시가 판정 2곳**: `_today_open`(분봉) / `_get_prev_close`(ka10001 캐시)
  <- **[A]가 정확히 이 패턴이다.** 두 값이 어긋나면 **캐시가 이긴다**
  (거래소 확정 시가 `open_pric`이라 분봉 추정보다 정확)
- **진입 핫패스에 REST를 넣지 말 것** — 캐시는 `prearm_candidate`가 채운다
- **테스트 픽스처에 수치를 하드코딩하지 말 것** — 전부 상수 참조로
- **진단 라벨에 수치를 박지 말 것** — 상수를 바꿔도 안 따라와 거짓말을 한다
- **소스 문자열로 단언하지 말 것** — 08-06 아침 감사에서 오탐 3건이 났고,
  같은 날 오후에 **또 한 번** 났다: `_entry_change_reject()`로 규칙을 모으자
  `_execute_buy` 소스에서 `_entry_change_cap` 문자열이 사라져 감사가 **멀쩡한
  코드를 실패로 보고**했다(그 감사 파일이 스스로 경고하던 패턴을 그대로 밟았다).
  **실제 호출·반환값으로** 검증할 것 — 지금은 스파이를 걸어 호출 횟수를 센다.
- **추측한 메서드명으로 테스트를 쓰지 말 것** — 08-06에 없는
  `check_open_surge_block()`을 지어내 테스트가 AttributeError로 죽었다.
  먼저 `grep`으로 실제 이름을 확인할 것.
- 🆕 **틱 데이터의 기준선은 반드시 '시간 균등'으로 뽑을 것** (2026-08-08).
  틱 인덱스 간격으로 뽑으면 **틱이 촘촘한 = 활발한 순간을 과대표집**하고,
  그 순간들은 평균회귀로 수익이 나빠 기준선이 인위적으로 낮아진다.
  실측: 인덱스 균등 **-0.357%** vs 시간 균등 **+0.119%** (같은 데이터).
  이 하나로 신호 우위가 **+0.638%p -> +0.162%p, 4배 과대평가**됐다.
- 🆕 **고속 구현은 실물 함수와 등가 검증 후에 쓸 것** (2026-08-08).
  누적합 가속도 구현이 두 번 틀렸다 — ① `ts >= cutoff` 경계를 `bisect_right`로
  처리(같은 초 틱이 통째로 빠짐) ② 초 단위 타임스탬프라 **아직 도착하지 않은
  같은 초의 틱**까지 포함. 검증 없이 썼으면 분석 전체가 거짓말이 됐다.
- 🆕 **진입 규칙은 '독립표본(종목·일당 첫 신호)'으로 평가할 것** (2026-08-08).
  격자(모든 시점) 평균으로 재면 **없는 우위가 보인다** — `VWAP AND 가속2.0`이
  격자로는 +0.531%p인데 독립표본으로는 **-0.046%로 기준선에 진다**.
  봇은 조건이 성립하는 **첫 순간**에 발화하고 그 순간은 국소 고점이기 때문이다.
  '시간 균등 기준선'과 **세트로** 지켜야 한다 — 둘 중 하나만 어겨도 결론이 뒤집힌다.
- 🆕 **비율 지표에는 '표본 수' 하한을 같이 둘 것** (2026-08-08).
  거래대금 가속도는 틱이 2개뿐이면 **수학적 최대(4.0)**가 나온다 —
  `compute_strength`가 틱 부족 시 100.0을 돌려주는데 임계값도 100이라
  그냥 통과하던 것과 같은 실패 모드다. **데이터가 없을수록 쉽게 뚫린다.**
  ⚠️ 단 하한은 **개수**여야 한다 — **금액** 하한은 실측상 해롭다(단조 악화).
- 🆕 **게이트를 '사전 검사'할 땐 검사 가격과 체결 가격이 같은지 볼 것**
  (2026-08-08). VWAP은 계획 생성 시 **트리거가**로 검사하는데 체결은
  **-0.3~-0.7% 낮은 가격**에서 난다 -> 통과했는데 못 사는 죽은 구간이 생긴다.
  '값싼 게이트는 계획 생성 전에' 원칙은 **가격이 같을 때만** 성립한다.

\### 즉시 되돌리는 법 (한 번에 하나씩!)

| 대상 | 방법 |
|---|---|
| **실전 -> 모의** | `config.ini`의 `IS_MOCK = true` + **모의 APP_KEY/SECRET_KEY로 교체** |
| **조건검색 0종목** | HTS [0156] '대상'과 `kiwoom_ws.CONDITION_STEX_TP` 일치. **현재 `"K"`(KRX)** / 통합=`"A"`. ⚠️ 08:59 스냅샷이 0종목인 것은 KRX 개장 전이라 **정상**일 수 있다 — 09:10까지 실시간 편입도 0이면 그때 의심할 것 |
| **매수금액** | **3곳 동시**(위 반복 패턴 참고). 현재 **100만원**(08-06 사용자 지정, 200만 -> 100만) |
| **버스트 주가 스케일** | `BURST_PRICE_ALPHA = 0.0` -> 전 종목 계수 1.0. 상한은 `BURST_PRICE_MAX`(**3.0**, 73,704원 이상에만 적용) |
| 🆕 **VWAP 깊은 검사** | `VWAP_ENTRY_CHECK_DEEPEST = False` -> 계획 생성을 트리거가로 검사(08-07까지의 동작, **죽은 구간 부활**) |
| 🆕 **우선순위 교체 한도** | `PHASE1A_PRIORITY_MAX_PER_DAY`(6). 0으로 두면 교체 기능이 완전히 꺼진다 |
| 🆕 **개장초반 슬롯 캡** | `EARLY_SLOT_CAP_ENABLED = False` -> 09:05 이전에도 공유 상한(6)까지 쓴다. 칸수는 `EARLY_SLOT_CAP`(4), 적용 종료는 `EARLY_SLOT_CAP_UNTIL`(09:05 — 보통 `FIRE_GATE_ACCEL_FROM`과 같게 둔다) |
| 🆕 **발사 게이트 분리** | `FIRE_GATE_SPLIT_ENABLED = False` -> 전 구간 옛 버스트로 완전 복귀. 문턱만 조이려면 `FIRE_ACCEL_MIN`(2.5 -> 3.0이면 하루 10.8 -> 5.6종목). 전환시각은 `FIRE_GATE_ACCEL_FROM`(09:05, 09:00으로 두면 전 구간 가속도). ⚠️ `FIRE_ACCEL_MIN_TICKS`(20)는 **금액이 아니라 개수** 하한이다 |
| **[B] 버스트 매수 방향** | `BURST_REQUIRE_BUY_SIDE = False` -> 08-05까지의 동작(투매도 버스트로 셈). ⚠️ 이걸 끄면 "-%에서 매수"가 되살아난다 |
| **[C] 등락률 하한** | `MIN_ENTRY_CHANGE_PCT = -100.0` -> 사실상 무제한(하한 없음). 현재 **0.0** = 전일종가 대비 + 일 때만 매수 |
| **[D] 상승 이탈** | 현재 **OFF**. 되살리려면 `ENTRY_BREAKOUT_ENABLED = True` (폭 `ENTRY_BREAKOUT_PCT` 0.003은 보존돼 있다) |
| **[E] 눌림목 매매** | `PULLBACK_MAX_SLOTS = 4` **+ `PHASE1A_MAX_SLOTS = 4`** — ⚠️ **반드시 둘 다**. 눌림만 되살리고 1A를 8로 두면 1A가 6칸을 독식한다 |
| **[F] 진입 숙성** | `MIN_ENTRY_DELAY_SEC = 0.0` -> 편입 즉시 매수 가능(08-05까지의 동작) |
| **[H] 파동 상한** | `BURST_WAVE_MAX = 999` -> 파동 제한 없음. 쿨다운은 `BURST_WAVE_COOLDOWN_SEC`(60초) |
| 🆕 **파동 카운트 기준** | `BURST_WAVE_COUNT_REQUIRES_BURST = False` -> 08-08 사양(면제 구간에서 게이트 통과마다 카운트). ⚠️ **되돌리면 거래대금 0원 종목도 60초마다 파동이 쌓여 09:15까지 67%가 하루 종일 매수 불가가 되는 결함이 부활한다** |
| **되돌림 끄기(즉시매수)** | `ENTRY_PULLBACK_ENABLED = False` / 폭은 `ENTRY_PULLBACK_TRANCHES` |
| **시가대비 상한** | `PHASE1A_LEADING_OPEN_SURGE_CAP`(8.0, 구값 5.0) |
| **급등 버스트 강화** | `PHASE1A_OPEN_SURGE_STRICT_FROM`(6.0) / `PHASE1A_OPEN_SURGE_BURST_MULT`(1.5). 배수 1.0이면 무효 |
| **VI 상단 매수차단** | 현재 **OFF**. 되살리려면 `VI_UPPER_ENTRY_BLOCK_ENABLED = True`. ⚠️ 그때 **당일 고가 캐시로 과잉차단도 같이 고칠 것**(08-05 PS일렉 사례) |
| **VI 상단 확정매도** | `VI_UPPER_EXIT_ENABLED = False` |
| **재매수 대금** | `REBUY_BURST_VALUE_MULT`(2.5) |
| **추가매수 끄기** | `RESCUE_ADD_ENABLED = False` |
| **본전스톱** | `BREAKEVEN_STOP_ENABLED` / 바닥 `BREAKEVEN_FLOOR`(0.002) |
| **무장 시간** | `TICK_STRENGTH_SUSTAIN_SEC`(**3.0**, 08-07 복원. 1.5로 두면 08-03 저녁 사양) |
| **조건검색 거래소** | `kiwoom_ws.CONDITION_STEX_TP`(**"K"**=KRX, 08-07 전환). 통합은 `"A"` — ⚠️ **HTS [0156] '대상'도 같이** 바꿀 것 |
| **VWAP 진입필터** | 현재 **ON**. 끄려면 `VWAP_ENTRY_ENABLED = False`. 폭 `VWAP_ENTRY_MIN_GAP_PCT`(0.5) / 시작 `VWAP_ENTRY_FROM`(09:05). 단위 `VWAP_UNIT_SCALE`(**1_000_000.0** = 백만원, 08-07 실측 확정 — 되돌릴 일 없음). ⚠️ `VWAP_ENTRY_FROM`을 09:05보다 앞당기면 FID14 양자화 오차가 판정 문턱을 넘을 수 있다 |
| **숙성 조건식별 예외** | `MIN_ENTRY_DELAY_SEC_BY_COND = {}` -> 전 종목 60초로 복귀. 현재 `{"돌파자동매매용": 30.0}`. 기본값은 `MIN_ENTRY_DELAY_SEC`(60.0) |
| **스냅샷 폴링** | `main.py` gather에서 `self.task_condition_snapshot_poll(),`를 주석 처리. 주기는 `POLL_INTERVAL_SEC`(60). ⚠️ 장중에 **체결틱 끊김이나 `concurrent recv` 에러**가 보이면 즉시 끌 것 |
| **일봉 눌림 태그** | `DAILY_PULLBACK_TAG_ENABLED = False` -> 판정도 REST도 완전히 꺼진다. **관측 전용이라 매매엔 영향이 없다**(끌 이유는 REST 절약뿐) |
| **버스트 문턱** | `PHASE1A_BURST_TRADE_VALUE`(4천만) |
| **지수 가드** | `INDEX_GUARD_ENABLED` (임계는 SEVERE_CRASH와 같은 값 유지) |
| **오후장 접기** | `PHASE1A_END`/`PULLBACK_END`를 `time(13, 0)`으로 |

