\# AutoTrader\_Project 작업 규칙



\## 기본 정보

\- 환경: 모의투자(IS\_MOCK), 왕복수수료 0.23%

\- 구조: asyncio + PostgreSQL (Docker `analytics\_pg` 컨테이너, 자동재시작 설정됨)

\- 사용자: 다윤, 한국어로 소통

\- 텔레그램 채널: 오토트레이더(order, 매수/매도 체결·실패), 주식 따릉이(signal, 시스템 로그/정기보고),

&#x20; 종가베팅 포착방(closing\_bet, 종가베팅 TOP10)



\## 개발/운영 환경 (2026-08-01 구축)

\### 실행 환경
- 프로젝트 경로: `C:\Users\rober\OneDrive\문서\vscode\AutoTrader_Project`
- 스케줄러용 junction 경로: `C:\AutoTrader_Bot\ProjectRoot`
  (**비대화형/S4U 컨텍스트에서 실행되는 배치파일은 반드시 이 경로를 쓸 것** —
   실제 경로의 "문서"(한글) 폴더명이 cmd.exe 파싱을 깨뜨린다. 2026-07-28 참고)
- 자동 기동: Windows 작업 스케줄러 `AutoTrader_Start`, 매일 **08:59**
- DB: Docker `analytics_pg` 컨테이너 (PostgreSQL, 자동재시작)

\### Claude Code CLI (2026-08-01 신규 설치)
| 항목 | 버전/경로 |
|---|---|
| Node.js | v24.18.1 (winget `OpenJS.NodeJS.LTS`, `--scope user`) |
| npm | 11.16.0 |
| claude CLI | 2.1.220 (`npm install -g @anthropic-ai/claude-code`) |
| claude 실행파일 | `...\WinGet\Packages\OpenJS.NodeJS.LTS_...\node-v24.18.1-win-x64\claude.ps1` |
| 로그인 방식 | Claude account with subscription (Claude Pro) |

설치 중 실제로 걸렸던 것 (다시 설치할 일 있으면 참고):
- `winget search`가 msstore 원본 약관 동의 프롬프트에서 멈춘다 →
  `--accept-source-agreements` 플래그 필수.
- npm이 `allow-scripts` 정책으로 postinstall(`node install.cjs`)을 건너뛴다는
  경고를 낸다. **core 기능에는 영향 없음**(`claude --version`/`--help` 정상 확인).
  `npm approve-scripts`는 전역 설치에 안 먹히므로(EGLOBAL) 무시하면 된다.
- 설치 직후 PATH는 **새 터미널을 열어야** 반영된다.

\### 08:59 자동 기동 시 원격제어 세션 동시 실행 (2026-08-02 신규)
작업 스케줄러가 봇을 띄울 때 **별도 창으로 `claude --remote-control` 세션도
같이 시작**한다. 장중에 PC 앞에 없어도 폰에서 상태를 물어볼 수 있게 하는 용도.

- `start_trader.bat`이 `main.py`보다 **먼저** `start`로
  `start_remote_control.ps1`을 띄운다. `start`는 즉시 반환하므로 봇 기동을
  막지 않고, 봇이 죽어도 원격제어는 살아있어 원인 진단이 가능하다.
- 세션 이름을 `autotrader`로 고정했다 — 자동 생성 이름(`remote-control-keen-thimble`
  같은 것)은 매번 바뀌어 폰 앱 목록에서 찾기 번거롭다.

**이 조합에서 실제로 밟은 함정 4가지 (다시 건드릴 때 반드시 확인):**
1. `.ps1`을 **BOM 없는 UTF-8로 저장하면 안 된다.** PowerShell 5.1이 cp949로
   읽어 한글이 깨지고 `string is missing the terminator` 구문 오류가 난다.
   UTF-8 **BOM 필수**. (편집 도구가 BOM을 떼는 경우가 있으니 저장 후 재확인)
2. `.bat`에는 **주석이라도 한글을 넣지 말 것** — 2026-07-28에 겪은 것과 같은
   원인으로 깨진다. 비ASCII 0바이트인지 확인하고 커밋할 것.
   한글 경로 처리는 전부 `.ps1` 안으로 몰아넣었다.
3. claude의 **폴더 신뢰 승인은 '실제 경로' 기준**으로 저장돼 있다. junction
   경로(`C:\AutoTrader_Bot\ProjectRoot`)로 시작하면 다른 폴더로 보고 신뢰
   프롬프트를 다시 띄울 수 있는데, 무인 기동에서 프롬프트가 뜨면 거기서 멈춘다.
   -> `.bat`은 ASCII junction만 참조하고, `.ps1`이 **실제 경로로 Set-Location**.
4. `scheduler_debug.log`는 cmd.exe가 ASCII로 쓰는 파일이라 한글(UTF-8)을 섞으면
   한 파일에 두 인코딩이 공존해 전부 깨져 보인다.
   -> 파일에 남기는 로그는 ASCII, 화면 출력만 한글로 분리했다.

수동으로 원격제어만 띄우고 싶을 때:
```
powershell -NoProfile -ExecutionPolicy Bypass -File "C:\AutoTrader_Bot\ProjectRoot\start_remote_control.ps1"
```

\### 모바일 원격 제어 (PC 세션을 폰에서 조작)
```
cd "C:\Users\rober\OneDrive\문서\vscode\AutoTrader_Project"
claude --remote-control
```
- ⚠️ **`claude remote-control`(하위명령)이 아니라 `--remote-control`(플래그)이다.**
  공식 문서 표기와 다르니 주의. 세션 중이면 `/rc`로도 가능.
- 실행하면 `https://claude.ai/code/session_XXXXXXXX` 형태의 URL이 뜬다.
  폰 Claude 앱 또는 폰 브라우저로 그 URL을 열면 연결된다.
- 폰 앱에서는 **[코드] 탭 → 세션 카드(예: `remote-control-keen-thimble`,
  `🖥 연결됨 · jokings76/AutoTrader_Project`)를 탭**해야 채팅창으로 들어간다.
  목록 화면만 보고 "연결이 안 됐다"고 오해하기 쉬움.
- **코드 실행·파일 접근은 전부 PC에서** 일어난다(클라우드 실행 아님).
  따라서 PC가 켜져 있고 **그 터미널 창이 열려 있어야** 하며, 창을 닫으면 끊긴다.
- 모바일에서는 **새 MCP 서버를 추가할 수 없다** — 필요한 건 PC에서 미리 세팅.
- 원격 세션은 기본 Sonnet 5로 시작한다. 전략 로직처럼 복잡한 작업은
  세션에서 `/model` 입력해 **Opus 5로 바꾸고 진행할 것**.
- ⚠️ **실거래 봇이 돌고 있는 장중에 모바일에서 매매 로직 파일을 고치지 말 것.**
  고칠 거면 이 문서 "작업 방식"의 surgical 패치 + import 검증 규칙을 그대로 지킨다.

\#### 세션 연속성 — 매일 전날 대화를 이어간다 (2026-08-02 변경)
`start_remote_control.ps1`이 기본으로 **`--continue`**를 붙인다. 08:59 자동기동도,
장중 자동재시작도 전부 직전 대화를 그대로 이어받는다. 폰 앱에 뜨는 URL은 매번
새로 생기지만 **대화 내용은 이어진다**(세션 이름 `autotrader` 고정).

- **대화가 길어져 한도가 걱정될 때**: 세션 안에서 "이관작업 해줘"라고 요청해
  CLAUDE.md에 맥락을 정리시킨 뒤, 프로젝트 루트에 빈 파일
  **`NEW_SESSION_REQUESTED`**를 만들어 둔다. 다음 기동 **딱 1회만** 새 대화로
  시작하고 플래그는 즉시 삭제되어, 그 다음부터는 다시 자동으로 이어간다.
  (`STOP_SIGNAL`과 완전히 같은 방식. 둘 다 `.gitignore` 대상 — 커밋되면
  체크아웃마다 되살아나 매번 대화가 끊긴다.)
  ```
  New-Item "NEW_SESSION_REQUESTED" -ItemType File
  ```
- **대화 내용은 PC를 꺼도 사라지지 않는다.** 메시지마다
  `~/.claude/projects/<프로젝트>/<세션ID>.jsonl`에 즉시 기록된다(OneDrive 폴더
  **밖**이라 동기화 충돌과도 무관). PC를 끄면 "일시 중단"이지 "소실"이 아니다.
- **자동 재시작**: 모바일에서 세션 '종료'를 눌러도(실수로 두 번 눌러도) 런처가
  5초 뒤 새 세션을 자동으로 띄운다(최대 30회). 예전엔 여기서 창이 프롬프트에
  멈춰, PC 앞에 사람이 없으면 그날 내내 폰 접속이 끊긴 채 방치됐다.
  **완전히 끄려면 PowerShell 창 자체를 닫을 것** — 모바일 '종료' 버튼은 세션만
  끝낼 뿐 창을 못 닫는다. 5초 미만 즉시종료가 3회 연속이면 클릭 실수가 아니라
  설정/인증 문제로 보고 자동재시작을 멈춘다.
- **감시**: `main.task_remote_control_watchdog`가 09:06에 1회 프로세스 존재를
  확인해, 없으면 텔레그램(signal)으로 알린다. 매매와는 완전히 별도 프로세스라
  원격제어가 죽어도 **봇 매매에는 영향이 없다**(알림 목적은 "오늘 폰으로 못 본다"를
  그날 안에 아는 것).

\## 작업 방식 (반드시 지킬 것)

1\. \*\*surgical 패치 우선\*\*: 파일 전체 재작성보다 "찾기 → 교체" 블록 단위로 제시. 

&#x20;  불가피하게 큰 함수 전체를 재구성해야 할 때만 함수 단위 통째 교체.

2\. \*\*파일 삭제 금지\*\*: 기존 파일은 지우지 않고 `\_legacy` 접미사로 백업 후 교체.

3\. \*\*검증 필수\*\*: 코드 변경 직후 반드시 `python -c "import \[모듈]; print('OK')"`로 

&#x20;  import 성공 여부 확인. 여러 파일을 고쳤으면 각각 검증.

4\. \*\*큰 변경 전 드라이런 권고\*\*: 특히 매매 로직(진입/청산/점수 계산) 변경은 

&#x20;  장 시작 전이나 소규모 테스트 권장.

5\. \*\*비동기 코드 주의\*\*: 메인 루프를 막는 `sleep` 절대 금지 (WS 연결 끊김 원인이었던 적 있음).

&#x20;  `asyncio.sleep`만 사용.

6\. \*\*인덴트 안전\*\*: Python 들여쓰기가 걸린 수정은 특히 신중하게 — 

&#x20;  부분 문자열 교체보다 함수/블록 전체를 정확히 교체하는 방식을 우선 고려.

7\. \*\*수정 후 백업 병행\*\* (2026-07-28): 그날 코드 수정이 어느 정도 쌓이면(세션

&#x20;  마무리 시점 등) git 커밋 + 날짜별 파일 스냅샷 백업을 같이 남긴다.

&#x20;  - git: 의미 단위로 커밋(민감정보 커밋 전 확인 — config.ini/settings.py/.env는

&#x20;    이미 .gitignore 처리됨, 그 외 파일에 하드코딩된 키/비밀번호 없는지 훑을 것).

&#x20;  - 파일 스냅샷: 프로젝트 폴더 밖 `AutoTrader\_Project\_backups\YYYY-MM-DD\`에

&#x20;    전체 복사(.git, \_\_pycache\_\_ 제외). Windows 작업 스케줄러에도 매일 자동

&#x20;    실행 등록(대화 세션과 무관하게 돌아감) — 스크립트는

&#x20;    `C:\Users\rober\OneDrive\문서\vscode\daily\_backup.ps1`.



\## 히스토리 (참고용, 과거 세션 요약)

\- 2026-07-24: Phase3 A/B 트리거 재설계, 손절/익절 정책 분리, 눌림목 VWAP 필터,

&#x20; 자동실행 인프라 버그 3종 해결, 안전종료 기능

\- 2026-07-26: VWAP 필터 5종 게이트(ATR 적응형/기울기/reclaim/밴드/연속스코어) 확장,

&#x20; 네이버 테마 종목코드 크롤러(fetch\_themes.py), 거래대금 폭발 히스토리 파이프라인

&#x20; (core/history\_fetcher.py, core/explosion\_scorer.py), 종가베팅 독립 스케줄러

&#x20; (main.py task\_closing\_bet\_scanner, 매일 14:50), 텔레그램 채널 3분리(order/signal/closing\_bet),

&#x20; 수동매도 즉시 감지+슬롯 해제(\_reconcile\_manual\_sells, SYNC\_INTERVAL 60→15초),

&#x20; 슬롯 교체 로직(core/slot\_replacement.py, 체결강도 하락+거래량 정체 종목을

&#x20; 감시종목 고득점 후보로 교체, 손절과 동일 취급, 하루 40회 상한)

\- 2026-07-27: 치명적 라이브 버그 다수 수정 — on\_price\_update()에 \_execute\_buy 알림

&#x20; 코드가 잘못 붙어있어 손절/트레일링/익절 판정이 전혀 실행 안 되던 문제,

&#x20; \_net\_profit()이 존재하지 않는 전역변수 참조로 매도할 때마다 크래시 나던 문제

&#x20; (→ "매도됐는데 계속 보유중" 증상의 원인), entry\_reason DB 컬럼 길이초과로

&#x20; 매수기록 실패 시 포지션 추적 끊기던 문제. 테마 매핑 REST 호출 800회→2회 축소

&#x20; (ka10027 랭킹 API 활용). 지수 방어 로직을 코스피 전용에서 코스피+코스닥

&#x20; worst-of-two(NORMAL/CAUTION -3%/HALT -5%)로 통합, 모든 진입 전략에 일관 적용.

&#x20; 일일 백테스트 시스템 신규(core/daily\_backtest.py, 매일 15:30 자동실행 →

&#x20; 텔레그램 오토트레이더 전송, task\_auto\_shutdown 15:20 자동종료는 이와 충돌해

&#x20; 임시 비활성화). 네트워크 단절/복구 3단계 방어 완성(on_disconnect/on_reconnect

&#x20; 콜백, 단절시간대별 격리기간 10초/60초/10분 차등, 미체결 주문 재확인 후 pending

&#x20; 정리·무조건취소 안 함, WS backoff에 jitter 추가).

- 2026-07-27 (계속) — 전략 구조 전면 재설계: 기존 1A/1S/1B/2/3/1N 체제에서
  **1A + Pullback + 1B(FSM) + 1L(주도주) 4개 전략 체제**로 개편.
  - 1A: 30봉신고가 필터 → 거래량증가지속(`is_volume_increasing_streak`)으로 교체
    (이 교체 과정에서 `calc_ma.is_30_candle_new_high` 호출이 함수를 모듈처럼 잘못
    참조해 매번 AttributeError로 조용히 실패하던 버그 발견·해결 — 1A는 사실상
    한 번도 작동한 적 없었음). 시간대 09:01~10:30(기본 6.5점)/10:30~14:50(8.5점
    상향, CAUTION 시 +1.0 추가). 슬롯 3개.
  - Pullback: 09:01~10:30로 시간대 변경(기존 09:30~10:40), 전용 슬롯 3개
    분리("1A_눌림" 라벨, 기존엔 1A와 슬롯 공유). 매수 미체결 시 1B 감시 시작
    트리거로 지정(기존엔 Surge가 담당).
  - 1B(5단계 FSM): Pullback 쪽으로 트리거 이관, 슬롯 4→3개.
  - 1L(주도주): 테마+체결강도100 판정을 순간체크에서 **2분 연속 유지**로 변경
    (`_leading_since` 타이머), 시간대 09:01~10:50 추가, 슬롯 2→3개.
  - 슬롯 구조: 1A/Pullback/1B/1L 각각 자체 상한 3개 + `MAX_HOLDINGS=6`으로 전체
    합산 상한 공유 — 1L/1B는 실시간 틱 콜백이라 사실상 우선 처리, 1A/Pullback은
    조건검색 경로라 자연스럽게 차순위.
  - **삭제(`_legacy` 백업)**: 1S(Surge), "2"(Phase2 — `evaluate_phase2` 정의만
    있고 호출부가 아예 없어 원래도 죽어있었음), 3(Phase3 A/B, `Phase3Controller`
    포함), 1N(10MA 눌림목 — `watch_candidates` 큐에 아무도 값을 넣지 않아 원래도
    죽어있었음). `entries/` 패키지(registry/base/surge/pullback/phase3) 전체
    미사용 처리, `on_condition_hit` 3중 루프를 단순 흐름으로 재작성.
  - 청산: 트레일링은 1L 전용, 나머지(1A/Pullback/1B)는 항상 flat 익절 2.5%
    (기존 09:30 매수시각컷/Phase3 A·B 트리거 기반 분기 전부 제거).
  - `config/phase_settings.py`: MAX_HOLDINGS 5→6, PHASE_1B 4→3, PHASE_2/PHASE_3/
    SCORING 삭제. `core/order_manager.py`의 MAX_POSITIONS도 5→6(실매수 경로엔
    안 걸리지만 일관성 위해). `core/daily_backtest.py`도 새 전략셋(1A/Pullback만
    재현)에 맞게 갱신.
- 2026-07-27 (계속) — 실거래 크래시 진단 및 수정:
  - **의도치 않은 차단 알림**: 지수방어 모드 전환(NORMAL↔CAUTION↔HALT) 시에만
    텔레그램 알림 추가(반복 호출 스팸 방지, 상태 바뀔 때만 발송).
  - **일부 수동매도 반영**: `_reconcile_manual_sells`가 전량매도(서버 잔고에서
    종목 자체가 사라짐)만 감지하던 걸, 일부 수량만 줄어든 경우도 감지해서
    `holdings[code]["qty"]`를 서버 값으로 갱신하도록 확장(기존엔 죽은 코드였던
    `pos.get("qty", ...)` 폴백이 이제 실제로 쓰임).
  - **작업 스케줄러 진단**: `AutoTrader_Start` 작업이 매일 09:01 정상 실행되는
    것 확인했으나, 그날은 테마 800회 호출 버그(이미 해결됨)로 멈춰서 실매매
    전 단계에서 죽고 있었던 것으로 확인. `C:\AutoTrader_Bot\start_trader.bat`
    (실제 스케줄러가 가리키는 파일, 관리자 권한 없어 스케줄러 자체는 못 고침)를
    프로젝트 폴더의 `start_trader.bat`를 호출하도록 위임 처리해서 경로 이원화
    해소.
  - **치명적 크래시 원인 발견·수정**: `task_closing_bet_scanner`가 코루틴
    시작 즉시(시간대 체크보다 먼저, try/except 밖에서) `from core.explosion_scorer
    import evaluate_closing_bet_candidate`를 실행하는데 이 함수가 애초에
    구현된 적이 없어서 **봇 기동 시마다 100% ImportError로 `asyncio.gather()`
    전체가 죽는 상태**였음(실제 야간 테스트에서 재현·확인). import를 기존
    try/except 안으로 이동시켜 이 기능만 조용히 스킵되도록 수정, 같은 위험
    패턴이던 `task_daily_backtest`/`task_slot_replacement`의 import도 예방
    차원에서 같이 옮김. `evaluate_closing_bet_candidate` 자체는 여전히
    미구현 — 설계만 합의됨(아래 "다음 할 일" 참고), 구현은 다음 세션으로 이월.
  - **start_trader.bat/stop_trader.bat 한글 인코딩 문제**: cmd.exe가 UTF-8로
    저장된 .bat 파일을 기본 cp949로 잘못 해석해 콘솔에 깨진 글자 출력 + 일부
    환경(바로가기 등)에서 명령 파싱 자체가 깨짐. `chcp 65001`로는 완전히
    해결 안 돼서, **.bat 파일 안의 한글 텍스트를 전부 제거**하고 텔레그램
    알림 문구는 별도 파일 `notify_scheduler_start.py`(파이썬이 UTF-8 소스를
    안정적으로 읽음)로 분리하는 방식으로 최종 해결. `stop_trader.bat` 신규
    생성(STOP_SIGNAL 파일 생성 → main.py의 기존 감시 로직이 5초 내 안전 종료).
  - ~~남은 원인불명 잔여 이슈: 배치파일 실행 시 "지정된 경로를 찾을 수 없습니다"~~
    → **2026-07-28에 근본원인 확정·해결** (아래 히스토리 참고).
- 2026-07-28 — **스케줄러 09:01 자동실행 근본 수정**: 아침에 python 전체경로
  하드코딩으로 1차 수정했다고 봤으나, 실제 `schtasks /Run`으로 재검증하니 여전히
  100% 실패(LastTaskResult=1, 로그 완전 무출력). 여러 단계로 원인 추적:
  (1) `AutoTrader_Start` 작업 자체가 2026-07-22에 Task Scheduler GUI로 수동
  등록된 것으로 추정, `schtasks /Create`로 새로 만든 진단용 작업들은 전부 정상
  작동 → 기존 작업 등록 상태 자체가 문제로 판명, `schtasks /Delete` + `/Create`로
  재등록(PowerShell `Register-ScheduledTask` cmdlet은 이 세션 권한으로 "Access is
  denied" — `schtasks.exe` CLI는 됨, 앞으로 스케줄 작업 등록/변경은 이 방법 사용).
  (2) 재등록 후에도 "'o'은(는) 내부 또는 외부 명령이 아닙니다" 에러 지속 →
  순수 ASCII 배치파일은 스케줄러에서 완벽히 작동하는 것 확인 후, 문제를
  `C:\Users\...\OneDrive\문서\...` 경로의 **"문서"(한글) 폴더명 자체**로 좁힘 —
  cmd.exe가 이 UTF-8 경로 문자열을 스케줄러의 S4U/비대화형 실행 컨텍스트에서
  잘못된 코드페이지로 읽어 파싱이 깨짐(2026-07-27에 "찾은" 것과 같은 뿌리의
  문제였음, 그때는 배치파일 자체 텍스트만 정리하고 "문서" 경로 문자열 자체는
  못 건드려서 미해결로 남았던 것). 8.3 short path는 이 볼륨/폴더에서 비활성화돼
  있어 우회 불가 확인. **최종 해결**: `C:\AutoTrader_Bot\ProjectRoot`에 디렉토리
  junction 생성(`New-Item -ItemType Junction`, 관리자 권한 불필요)해서 실제
  프로젝트 폴더를 한글 없는 경로로 매핑, `C:\AutoTrader_Bot\start_trader.bat`와
  프로젝트의 `start_trader.bat` 둘 다 이 junction 경로를 참조하도록 수정.
  `schtasks /Run`으로 실제 스케줄러 컨텍스트에서 재현 테스트 → 성공(봇 정상
  기동, LastTaskResult=0) 확인. **앞으로 스케줄러(비대화형/S4U 컨텍스트)에서
  실행되는 모든 배치파일/스크립트는 "문서" 등 한글이 섞인 실제 경로 대신
  `C:\AutoTrader_Bot\ProjectRoot` junction 경로를 참조할 것** — 인터랙티브
  세션에서는 문제없이 작동해서 이 클래스의 버그는 수동 테스트로는 절대
  못 잡음, 항상 `schtasks /Run`으로 실제 스케줄러 컨텍스트에서 검증 필요.
- 2026-07-29 — 실거래 버그 다수 수정 + Pullback 점수 재설계:
  - kiwoom_ws.py 조건검색 스냅샷 매핑 NoneType 크래시 수정(하루 31회 발생).
  - 1B(FSM)만 매수 시간윈도우가 없어 장마감 후에도 매수시도 계속되던 버그 →
    `_execute_buy`에 15:10 하드컷오프 추가.
  - 손실 청산은 사유(손절/슬롯교체/시간정리) 무관 당일 재매수 차단, 손실차단
    종목은 슬롯교체 대체후보에서도 제외. `watchlist_reentry` 경로에 재매수차단
    체크가 누락돼 손실차단/쿨다운 중에도 재매수될 수 있던 구멍도 수정.
  - 1A 거래량증가지속 필터가 개장초반(09:01~09:04) 전일데이터 병합 때문에
    매번 오탈락하던 문제 수정 — 오늘 조건검색 21종목 전부 이 이유로 탈락했던
    것 확인. 이어서 **1A 이중 게이트 버그**도 발견: `evaluate_phase1` 자체
    컷라인(12점 만점 9.0)이 바로 아래 시간대별 커트라인(오전 6.5/10:30이후
    8.5)보다 항상 높아서 "10:30부터 빡빡하게" 설계가 하루 종일 죽은 코드였음
    — 이중 게이트 제거하고 시간대별 커트라인 하나로 통일(07-30 계속 항목).
  - Pullback 점수구조 재설계: MA/양봉은 게이트에서 이미 확정돼 항상 만점이라
    점수에서 제외, 거래량/강도/OBV(신규)로 9점 재분배 + 전용 컷라인(0.5) 분리.
    `phase1b.start_watching()`을 눌림성립 시점으로 앞당겨 강도가 항상 0점이던
    구조적 문제 완화(단, setup_ok 조건 자체는 07-30에 다시 제거됨 — 아래 참고).
  - `trade_flow.compute_strength()`의 9999류 극단값(짧은 윈도우+분모 0 근접
    노이즈) 방지 — 최소틱수 미달시 중립값(100), 계산돼도 상한(300) 적용.
    실측: 극단값일수록 손실이었던 패턴 확인 후 수정.
  - 조건검색식 이름 추적 버그 2건 수정 — 실제 조건명이 "초기스냅샷"/"알수없음"
    으로 뭉개지던 문제(`main.py` `_process_initial_snapshot`/
    `task_condition_snapshot_poll`).
  - 주도주상위 외 조건검색식(체결강도100/돌파자동매매용/관심종목감시)은
    09:20부터 감시 시작하도록 지연평가 게이트 추가(`OTHER_COND_START`).
  - 일일 백테스트가 매일 "재현된 매매 없음"이었던 근본원인 수정 — 전일 데이터가
    섞여도 날짜 안 보고 시:분만 비교해 시뮬레이션이 시작하자마자 멈추던 버그.
    체결강도 placeholder(100)가 "중립"이 아니라 0점으로 계산되던 문제도 같이
    수정(해당 요소 가중치 0으로 배제).
  - 종료 경로(`shutdown`/비정상종료 핸들러)의 텔레그램 알림 호출에 예외처리
    추가 — 스케줄러 `Last Result: 255` 원인으로 추정.
- 2026-07-30 — 09:01~09:20 매매정지 회귀 진단·수정 + 청산 로직 전면 재설계:
  - **회귀 2건 발견**(둘 다 07-29 세션에서 만든 부작용): ① 키움 실시간 편입
    payload엔 조건 seq가 없어(`jmcode` 키 하나뿐, 여러 날 확인) cond_name이
    항상 "기타"로 들어와 09:01 스냅샷이 정확히 기록한 "주도주상위+..."를
    덮어씀 → OTHER_COND_START(09:20) 게이트가 주도주상위 종목까지 지연시킴.
    `_known_hits`(seq→종목 set)로 역추적해 복원하도록 수정. ② `start_watching`에
    걸어둔 `setup_ok`(눌림성립) 조건 제거 — 1B는 눌림목이 아니라 매도벽 소멸을
    보는 독립 전략이라 이 조건 때문에 07-30 주도주 9종목 전부 감시조차
    시작 안 돼 1B 매매가 27건(07-29)→7건(07-30)으로 급감했음.
  - 부분체결 시 DB `profit_rate`/`profit_amount` 왜곡 버그 수정 —
    `update_sell`이 매도금액(실체결 수량)에서 매수원가(주문 수량)를 그대로
    빼서 미보유 수량까지 손실로 계산(실측: 실제 +0.49%인데 DB -39.15%).
    실거래 판단 로직(`_net_profit`)은 원래 정상이었음 — DB 통계만 오염.
  - **1B 진입에 우위 없음 확인**(실거래 34~37건, 추세필터/손익비 전수검증) →
    추세필터는 전부 역효과(승리 거래가 오히려 더 과매도 상태), 필터 추가 대신
    **반등확증 게이트** 도입: 신호봉 고가를 1분봉 종가로 돌파해야 매수
    (기존 즉시매수 → 확증 대기). 백테스트: 승률 29.7%→50.0%, 건당
    -1.33%→-0.33%(07-29/07-30 양일 모두 개선, 과최적화 아님 강건성 확인).
  - 익절 캡 전략/시간대별 분리(손절 -3%는 유지): 1B/Pullback/개장초반
    (09:01~09:10)은 1.5%, 1A(그 외 시간대)는 2.5%, 1L도 개장초반은 flat 1.5%.
  - 익절 후 재매수 2회 제한(종목당 당일 최초1+재매수2=3회).
  - **동적 익절캡** 도입 — 순 +1.0% 도달 시점 갈림길: 체결강도 상승(진입×1.2
    이상)이면 캡을 2.5%로 상향, 미상승이면 그 자리서 익절 확정, 판단불가면
    기본 캡 유지. 2.5%캡 종목은 강도 하락(진입×0.8미만) **AND** 거래량 하락
    (ratio<1.0) 동시 충족 시 즉시매도(OR로 하면 과민해서 오히려 악화됨을
    백테스트로 확인). 상향기준 1.0%가 1.2/1.5%보다 일관되게 우수, 유일한
    플러스 구간(건당 +0.040%, 승률 64.3%). 강도 임계값 자체는 과거 틱 데이터가
    없어 분봉 대용지표로만 검증 — 실전 관찰 필요.
  - 1L(주도주) 진단 로깅 추가 — 연일 0건인 원인을 알 수 없어 상태전이(지속
    감시시작/리셋/충족했으나 매수실패) + 10분 주기 요약(테마소속·강도충족
    비율, 최장유지시간) 로그 신설.
  - 조건검색 돌파자동매매용/관심종목감시 07-30 0건 원인 분석 — HTS 조건식
    자체가 그 시각(09:01) 매칭 없었을 가능성 + `task_condition_snapshot_poll`
    (주기 재확인 태스크)이 현재 비활성화 상태라 하루 한 번만 체크되는 구조적
    공백 발견. 사용자가 HTS 쪽 조건식 직접 확인 중, 코드 조치는 보류.
- 2026-07-31 — 조건검색식 3개 체제 전환 + 슬롯 로직 전면 확장:
  - **조건검색식 재편**: 체결강도100 -> `눌림목자동`으로 개명(Pullback 전용
    검색식), `관심종목감시` 삭제 -> 주도주상위/눌림목자동/돌파자동매매용
    3개 체제. `config.ini`의 CONDITION_NAMES도 같이 갱신 — **이걸 안 하면
    `name_to_seq` 조회 실패로 눌림목자동이 아예 구독조차 안 된다**(코드가
    조건식을 이름으로 찾음, main.py `_subscribe_conditions`). 앞으로 HTS에서
    조건식 이름을 바꾸면 config.ini도 반드시 같이 바꿀 것.
  - `IMMEDIATE_COND_NAMES` 신설 — 눌림목자동도 09:01 즉시평가 대상에 추가
    (Pullback 시간창이 09:01~10:30 89분뿐이라 09:20 지연은 21% 손실).
    돌파자동매매용은 대형주 위주라 기존 09:20 지연 유지.
  - 눌림목자동 단독 편입 종목은 1A 평가를 건너뛰고 Pullback으로 직행 —
    "당일고가 -3% 눌림"을 전제하는 검색식이라 1A(급등·거래량증가지속형)와
    전제가 반대.
  - cond_name first-write-wins 보정: 다른 조건식이 먼저 걸린 종목에 나중에
    주도주상위가 와도 갱신 안 돼 09:20까지 잘못 지연되던 문제(콜라보 사용
    시작하며 발견) — 주도주상위가 새로 포함되면 병합 승격.
  - **슬롯교체 우선순위**: 자격 종목(강도하락+거래량정체, AND 유지)을 전부
    모아 **손실이 얕은 순**으로 교체. 기존엔 dict 순서상 처음 걸린 종목을
    잘라서 -2.5%짜리를 realize하는 일이 있었음. 깊게 밀린 종목은 손절/
    손실반등 매도가 따로 담당.
  - **손실반등 하이브리드 매도** 신규: 손실 종목이 저점 대비 +1% 이상
    반등했는데 그 반등이 체결강도 하락 AND 거래량 하락으로 확인되면 손절
    (-3%)까지 안 기다리고 즉시 청산("수익 안 나도 손실 최소화"). 저점 추적
    `pos["lowest_price"]`을 on_price_update에 추가. 손절은 최후 방어선 유지.
  - **확장 슬롯 6 -> 8**: 평소 6개만 쓰고, ①점수 >= 자기 컷라인 x1.5
    ②슬롯 만석 90초 이상 지속(=교체 태스크가 60초 주기로 돌았는데 교체
    대상이 없었다) ③점수 정보가 있는 경로(1A/Pullback) 세 조건 동시 충족
    시에만 7~8번째 개방. 컷라인 **대비 비율**로 판정하는 이유는 전략마다
    만점 스케일이 달라서(1A 12점 / Pullback 7점).
  - **장중 전략 성과 기반 우선순위 자동조정**(`core/strategy_performance.py`
    신규): 오늘 잘 되는 전략의 점수 컷라인을 낮추고(HOT x0.85) 안 되는
    전략은 높인다(COLD x1.15 + 공유슬롯 마지막 1칸 양보). 핵심은 **축소추정**
    `adj = n*observed/(n+5)` — 장중 표본이 2~5건뿐이라 관측 평균을 그대로
    믿으면 '방금 운 좋았던 전략'을 추종하게 됨. 최소표본 3건/조정폭 ±15%/
    09:20 이후 활성/극단값 ±10% 클램프로 다중 방어. `ENABLED=False` 한 줄로
    즉시 무력화 가능. 슬롯 개수가 아니라 문턱을 조정하는 이유: 개수를 늘리면
    그 전략이 꺾일 때 손실도 같이 집중되지만, 컷라인은 품질 축이라 우대해도
    아무 종목이나 사지 않음.
  - 실데이터 재생 검증(07-28~07-30 실거래 56건, 부분체결 오염 2건 제외):
    07-28 09:38 1A->COLD(그날 최악 전략), 07-29 1B가 HOT 갔다가 스스로
    NEUTRAL 복귀(과열 추종 안 함), 07-30 10:25 1B->HOT(유일한 플러스 전략
    포착). **부수 발견**: 07-29 1B 평균은 -3.26%가 아니라 **-0.08%** —
    앞서 본 -3.26%는 부분체결 오염행 2건이 만든 착시였음.
  - 1A `evaluate_new_intensity_strategy`가 info에 `score_threshold`를 안
    담아서 1A만 확장슬롯 판정을 통과할 수 없던 문제도 같이 해결.
- 2026-07-31 (계속) — **REST 호출예산 실측: 이미 포화 상태**:
  - 429(호출빈도 초과) 추이: 07-27 77건 -> 07-28 385 -> 07-29 3,221 ->
    07-30 2,469. 07-30 시간대별 09시 439/10시 421/11시 378/12시 379/13시 392/
    14시 328 — **장중 사실상 매 분 발생**(429 발생 분: 474분).
    api_id별: `ka10080`(분봉) 2,223 / `ka10027` 231 / `ka20001` 15.
  - `KiwoomREST.MIN_INTERVAL=0.6초` = 분당 100콜이 자체 상한인데 거기 계속
    붙어 있다는 뜻. 429마다 `_request`가 **2초 blocking sleep** -> 07-30
    기준 약 82분이 대기로 소모됨. **종목별 REST 폴링을 추가할 여유가 없다**
    (20종목 1분주기 = 분당 +20콜 = +20%). 새 데이터가 필요하면
    ① WS에 이미 실려오는지 확인 ② 시장 전체 랭킹형 1콜/주기 순으로 검토할 것.
  - `CandleCache.log_stats()`는 구현돼 있지만 **어디서도 호출되지 않아**
    캐시 적중률을 아무도 모르는 상태 — 다음 세션에서 가시화할 것.
  - **종가베팅 스캔 호출 실측·최적화**: 종목당 5콜이었음(3분봉 20일치 3콜 +
    60분봉 1콜 + 당일분 1콜). 11종목 x 5 = 55콜이 14:50~14:52 2분에 몰려
    전역 스로틀을 33초 이상 독점. `core/history_cache.py` 신규로 과거 분봉을
    `cache/history/{interval}m/{code}.json`에 영속 저장하고 다음날엔 최근
    구간만 1콜로 이어붙이도록 변경 -> **종목당 5콜 -> 2콜(60% 절감)**.
    겹침 검증(신규 최古 <= 캐시 최신)으로 공백 생기면 전체 재수집 폴백,
    원자적 저장(tmp -> os.replace), API 실패 시 기존 캐시 반환.
    **첫 실행일은 캐시가 비어 기존과 동일한 콜 수** — 절감은 다음날부터.
  - **종가베팅 today_bins 정렬 버그 발견·수정**: `_raw_get_minute_candles`는
    내림차순(최신->과거)인데 `evaluate_closing_bet_candidate`는
    `today_bins[-5:]`로 '최근 5봉'을 채점한다고 가정 -> 실제로는 **가장
    오래된 5봉(전일 오후)** 을 채점하고 있었음(count=150이 전일까지 걸침,
    07-30 로그 `[0]=20260730144800 ~ [-1]=20260729134800`로 확인).
    `history_cache.slice_today()`(오름차순 보장)로 대체해 중복 호출 제거와
    동시에 해결. **이 수정으로 종가베팅 후보가 어제까지와 달라지는 게 정상.**
  - **정렬 규약**: 오름차순 = `history_fetcher.fetch_n_days_candles`,
    `history_cache.*` / 내림차순 = `KiwoomREST._raw_get_minute_candles`.
    이 둘을 섞어 써서 실제 버그가 두 번 났다(daily_backtest 날짜경계,
    종가베팅 today_bins). 새 코드는 항상 오름차순 전제로 쓸 것.
  - **프로그램 매매 유입 관측 인프라 신규**(매매 판단에는 미연결, 기록 전용):
    `core/program_flow.py`(종목·분별 유입 누적 + '꾸준함' 판정 —
    positive_minutes/max_streak/cum_net 세 축을 따로 기록해 백테스트에서
    어느 축이 예측력 있는지 비교), `analyze_program_flow.py`(대조군 비교 +
    5/10/20/30분 다중 홀딩구간 검증 하니스), `probe_program_api.py`(프로그램
    매매 api_id 탐색기 — 장중 실행 금지), `main.py task_program_flow`
    (60초 flush / 10분 요약, **REST 호출 안 함**).
    `api/kiwoom_ws.py`에 0B 체결 raw FID 키 1회 로깅 추가 — 프로그램 데이터가
    WS로 공짜로 오는지 판별용(0D는 원래 있었는데 0B만 없었음).
- 2026-07-31 (계속) — **종목프로그램매매(0g) WS 실시간 배선 완료**:
  - 사용자가 문서로 확인: 0B(주식체결)엔 프로그램 FID 없음, 별도 실시간
    타입 "종목프로그램매매"에 118(매도수량)/119(매수수량)/120(순매수수량)/
    122(매도금액)/123(매수금액)/124(순매수금액) 존재. REST 폴링 대신 WS로
    **추가 호출 0**에 연결 완료.
  - **타입 코드 "0g"는 검증되지 않은 추정치** — 문서엔 FID만 있고 실시간
    등록용 타입 코드값은 없어서 Kiwoom 관례(0B/0D 계열)로 추정. 진단 로그
    `🔑 0g 프로그램매매 raw 키`가 09:01 이후 찍히면 맞는 것, 0B/0D는 찍히는데
    이것만 안 찍히면 타입 코드 재확인 필요.
  - `api/kiwoom_ws.py`: `TYPE_PROGRAM="0g"` + `on_program` 콜백 +
    `_dispatch_program_item`(FID 파싱, raw 키 최초 1회 로깅, 미지정 타입이
    조건검색으로 잘못 흘러가지 않도록 분기 추가).
  - `main.py`: `_on_program()`이 REST 없이 바로 `program_flow.record_cumulative()`
    호출. `subscribe_realtime` 두 호출부(초기 스냅샷/배치 구독) types에 "0g"
    추가 — 재연결 재구독은 `_subscribed_realtime` 기반이라 이 두 곳만 고치면
    충분.
  - `core/program_flow.py`: `record_cumulative()` 신규 — FID들이 장시작부터
    **누적값**(FID13 누적거래량과 동일 관례)이라고 보고 종목별 마지막
    누적값을 기억해 델타로 변환. 당일 첫 관측은 델타를 모르므로 기준점만
    잡고 기록 안 함(0으로 넣으면 장시작~지금 전부가 한 틱에 몰려 왜곡).
    누적값 감소(자정 넘김 등) 시에도 같은 이유로 기준점만 재설정.
  - **여전히 매매 판단에는 미연결** — 기록 전용. 며칠 쌓이면
    `analyze_program_flow.py`로 대조군 대비 우위 확인 후 연결 여부 결정.
  - 격리 테스트 15건 통과(record_cumulative 6/WS파서 7/디스패치라우팅 2).
- 2026-07-31 (계속) — **09:01 장중 라이브 디버깅: 실거래 버그 6건 발견·수정**.
  아침 자동실행 직후 실시간으로 로그를 지켜보며 발견한 문제들 — 전부 그날
  실제 매매 데이터로 재현 확인 후 수정, 격리 테스트 46건 추가(누적 152건).
  이 세션에서 봇을 5차례 재시작하며 순차 적용(재시작 사유는 각 항목 참고).
  - **CNSRREQ 응답 교차매칭 버그** (`api/kiwoom_ws.py`): `_wait_for()`가
    trnm만 보고 seq를 확인 안 해서, 주도주상위(seq=1) 스냅샷을 요청했는데
    눌림목자동(seq=3)·돌파자동매매용(seq=2)의 응답을 대신 받아오는 사고가
    있었음 — 09:01 기동 시 주도주상위가 계속 0종목으로 나오던 근본원인
    (다윤님이 HTS 화면엔 강한 종목이 계속 올라온다고 알려줘서 발견). seq
    파라미터 추가로 불일치 응답은 버리지 않고 계속 대기하도록 수정. 재시작
    직후 즉시 11종목으로 정상화 확인.
    부수: `fetch_condition_snapshot`에 재시도 로직도 추가 — return_code=0인데
    return_msg에 에러가 실려오는 응답(돌파자동매매용에서 실제 관측, "에러입니다")을
    조용히 "0종목"으로 삼키던 것을 1.5초 후 1회 재시도.
  - **1L 포지션이 동적캡에 잘못 걸려 조기청산**: `_update_dynamic_caps()`가
    sub_strategy 구분 없이 순회해서, 트레일링 전용인 1L도 "상한캡 조기이탈"
    체크 대상에 포함됨(`_take_profit_cap`의 fallback 기본캡이 우연히
    TP_CAP_UPGRADED와 같은 값). 010120/067310이 매수 66/69초 만에 "동적캡
    즉시매도"로 날아간 실거래로 발견 — 가격은 거의 안 움직였는데 체결강도
    중립값(STRENGTH_NEUTRAL=100, 틱부족으로 판단불가)을 "하락"으로 오판한
    것과 겹쳐서 터짐. cap_exit에서 1L 제외 + 강도 중립값 가드 추가(판단불가는
    하락 아님) 둘 다 수정.
  - **재시작 시 당일 리스크 상태 전체 소실** (가장 심각) — `_stoploss_blocked`/
    `sold_at`/`_buy_count_today`가 전부 메모리 전용이라 재시작하면 통째로
    비워짐. 413630(씨피시스템)이 09:53 손실청산 후 정상 차단됐는데, 봇 재시작
    (이 세션에서 제가 직접 실행)으로 차단이 풀려 10:09 재매수되는 사고로 발견
    — **오늘 재시작을 여러 번 한 게 직접 원인**이었음. `_restore_daily_risk_state()`
    신규: 재시작 시 당일 trades 전체를 조회해 손절차단/쿨다운/재매수횟수를
    재구성. holdings처럼 DB 기반으로 복원해야 재시작이 안전. 이후 재시작부터
    "DB 복원: ... 손절차단 N종목" 로그로 유지 확인됨.
  - **눌림목자동 26종목 전부 Pullback 진입 불가**: 눌림목자동은 "당일 고가
    대비 -3% 눌림"을 HTS가 이미 검증해서 넘겨주는데, `_pullback_setup`은
    최근 10봉(10분) 로컬 구간으로 되돌림을 다시 잼 — 시간축이 완전히 달라서
    (하루 스케일 vs 10분 스케일) 그날 26종목 전부 재검증 통과 못 함(로컬
    되돌림이 3% 넘거나, 눌림이 끝나 옆걸음 중이라 반등 캔들 자체가 안 나옴).
    `score_pullback`에 `skip_setup_check` 파라미터 추가 — 눌림목자동 단독
    태그 종목은 로컬 재검증을 건너뛰고 반등확인+점수로 바로 감. 재시작 직후
    079650이 "pullback 조건 OK but VWAP 필터 탈락"까지 도달해 효과 확인
    (이전엔 전부 눌림/반등 단계에서 무한 정체).
  - **매수 등락률 상한(12%)이 기준을 잘못 잡아 실제로 뚫림**: `MAX_ENTRY_
    CHANGE_PCT` 체크가 "당일 시가" 대비였는데, 갭상승 출발 종목은 시가
    자체가 이미 높아서 과소평가됨. 후성(093370): 시가대비 +4.60%(통과)
    였지만 전일종가대비(HTS 표시 기준, 다윤님이 실제로 지적) +18.18%.
    씨피시스템(413630)도 시가대비 +10.14% vs 전일종가대비 +13.24%. 기준을
    전일종가로 변경 — `_get_prev_close()` 신규가 기존 `get_stock_change_rate`
    (ka10001, 1콜)로 등락률을 받아 전일종가를 역산해 캐시하므로 REST 부담
    증가 없음.
  - **[확인, 코드변경 아님] 매수 주문 방식**: `trde_tp="0"`(지정가) + 현재가
    +1틱 가격으로 즉시체결 노림. 함수명이 `buy_market_order`라 오해하기
    쉬우나 순수 시장가(`trde_tp="3"`)는 아님.
  - 커밋: `d1cacb4`.
- 2026-07-31 (계속) — **대신증권(CYBOS Plus) 백테스트 데이터 확보 방향 논의**
  (코드 변경 없음, 다음 세션 참고용). 현재 백테스트가 실제로 검증 못 하는
  것의 근본원인은 **틱 단위 체결 데이터 부재** — `daily_backtest.py`가 이미
  스스로 "1B/1L은 틱/호가 데이터 없으면 재현 불가"라고 명시하고 체결강도를
  중립값 100 고정으로 때우고 있음(1A/Pullback도 강도·전략에 따라 가중치를
  0으로 죽여서 백테스트함). 대신증권에서 틱 데이터(체결시각·가격·수량·매수/
  매도구분)를 받으면 이 문제 대부분 해결 가능 — `TradeFlowTracker.add_tick()`
  형식으로 변환하는 로더만 만들면 라이브와 동일한 `compute_strength()`
  계산식을 과거 데이터에 그대로 적용 가능. 단, **호가 잔량 이력은 대신증권을
  포함해 업계 어디서도 못 구함**(거래소가 소매용으로 아카이브 안 함) —
  1B의 매도벽 감지(`WallDetector`)는 어떤 데이터를 구해도 완전 재현 불가,
  실전 관찰로만 검증 가능하다는 기대치를 사용자에게 명확히 전달함. OBV는
  이미 있는 분봉 데이터만으로 계산 가능한데 daily_backtest.py가 단순화
  목적으로 가중치를 0으로 죽여둔 것뿐이라, 이건 새 데이터 없이도 코드만
  고치면 되는 별개 항목으로 구분해서 안내함.
- 2026-07-31 (계속, 야간 세션) — **전략 구조 전면 재설계: 1A 체결강도
  단독화 + 1B WallDetector 제거 + 1L 주석처리**.
  발단은 사용자가 씨피시스템(413630) HTS 차트를 보여주며 "체결강도/거래량/
  거래대금 다 맞는데 매수가 한참 위에서 된다"고 지적한 것. 413630 실제
  타임라인 재구성 — 09:22 조건검색 편입(주도주상위) → 09:23 처리(34초,
  빠름) → **09:23~09:42 20분간 1A/Pullback/1L 3개 전략의 자체 게이트에
  막혀 있다가** 09:42 1B로 겨우 매수(09:43). **조건검색이 너무 많이
  걸려서 순위 매기느라 늦는 게 아니라, 우리 쪽 진입 로직 자체가 병목**
  이라는 게 핵심 진단.

  - **cond_name 추적 인프라**: `watch_list_log`에 `cond_name` 컬럼 추가
    (`add_cond_name_column.py`, 비파괴적 ALTER). 기존엔 매수 안 된 후보의
    조건검색 출처가 `trades.entry_reason`에만(매수된 것만) 남아서 백테스트가
    조건검색식별 로직 차이를 재현할 방법이 없었음.
  - **백테스트 재진입 규칙 정합화**: `daily_backtest._simulate_stock`이
    "재진입 없음"이라 주석돼 있었지만 실제론 무제한 재진입을 허용해 손실
    종목 반복매수로 승률을 과대평가하고 있었음. 라이브의 `_is_rebuy_blocked`와
    동일 3규칙(손실청산 시 당일 영구차단/종목당 최대 3회/매도 후 3분
    쿨다운) 재현 — 재현매매 75건→28건, 평균 -0.64%→-1.58%로 더 정직해짐.
  - **1B 확증게이트 슬리피지 축소**: 확증 성립 5건 실측 결과 실제 매수가가
    기준선 대비 +0.15~+1.72% 더 비쌌음. 원인은 확증 '규칙'이 아니라 REST가
    55초 간격으로만 도는 감지 지연 — 무료 실시간 체결가로 먼저 돌파여부를
    보고 넘었을 때만 12초 간격으로 REST 확인하도록 변경(429 예산도 절약).
  - **1A 전면 단순화**: `evaluate_new_intensity_strategy`(거래량증가지속+
    체결강도지속+점수)가 급등형 종목과 구조적으로 안 맞고, 최초 평가 시
    phase1b 감시가 아직 안 켜져 있어 체결강도 게이트가 항상 실패하는 공백도
    발견. `evaluate_1a_leading_strength`(체결강도 100 이상 1분 유지 단독,
    감시를 직접 즉시 개시) 신설, 주도주상위 전용→돌파자동매매용까지 확장해
    1A 전체를 이 방식으로 통일. 옛 로직은 삭제 없이 보존(호출부만 교체).
  - **1L 주석처리**: 새 1A(강도100·1분)가 1L(테마리더+강도100·2분)과 설계
    중복(1분<2분이라 항상 1A가 먼저 사감) — `on_trade`/`on_orderbook`의 1L
    로직을 통째로 주석 처리(사용자 지정).
  - **1B 전면 재설계**: `WallDetector`(매도벽 감지) 파라미터가 도입 시점부터
    "TBD: 실데이터로 튜닝" placeholder였고 한 번도 튜닝된 적 없었음 + 호가
    잔량 이력은 원리상 영구 검증 불가 + FSM 자체가 지연의 큰 축(413630
    사례: 확증게이트 전 FSM만 19분). WallDetector 완전 제거, "60초 내
    PHASE1B_PULLBACK_PCT 이상 하락"만으로 확증게이트 직행. 하락폭도
    재조정 — 오늘 실데이터로 -1.0/-1.5(기존)/-2.0/-2.5/-3.0% 비교, 기존
    -1.5%가 테스트한 것 중 최악(19건, 평균 -0.220%)이었고 깊을수록 단조
    개선 확인, 표본크기 고려해 **-2.0%**(9건, 평균 +1.261%) 채택.
  - **Pullback skip_setup_check 전면 적용**: 기존엔 눌림목자동 단독 소스만
    로컬 되돌림 재검증을 건너뛰었는데, 413630처럼 다른 소스도 같은 이유로
    갇히는 걸 확인해 소스 구분 없이 전부 생략(반등확인+점수+VWAP은 유지).
  - **시간창 09:01→09:00 통일**(`GROUP_A_START`/`LEADING_START`) + **스케줄러
    08:59로 변경**(`schtasks`, 기존 09:01) — 새 1A/1B가 틱 기반이라 09:00부터
    평가해도 데이터 공백 없음, 봇 셋업(인증/WS연결/조건구독) 버퍼 확보 목적.
  - **1A 부가 3종**: ①개장초반(09:00~09:10) 슬롯 우선순위 교체 — 슬롯 꽉 찼을
    때 새 후보가 가장 약한 보유종목보다 1.2배 이상 강하면 즉시 교체(첫 3자리는
    여전히 즉시매수) ②주도주상위 시가대비 +5% 이상 매수보류(눌림 가능성)
    ③동적 강도 임계값 — 지수 CAUTION(x1.2)과 `strategy_performance.py`의
    기존 HOT/COLD 축소추정을 max()로 결합해 필요할 때만 100 위로 상향(100을
    바닥으로 고정, HOT이어도 안 내려감).
  - **틱 아카이빙 인프라 신규**(`core/tick_archive.py`): 1A/1B의 체결강도
    로직을 백테스트하려면 틱 데이터가 필요한데, 키움 REST `ka10079`(주식틱
    차트조회요청)로 대신증권 없이 개별 체결(시각+가격+거래량) 확보 가능함을
    실제 호출로 확인(`probe_tick_chart_api.py`). 매수/매도 구분 필드는 없어
    틱룰(직전가 대비 상승/하락)로 방향 추정. 종목당 하루 전체는 22~185회
    호출 필요(실측, 429 예산상 무리)해서 개장초반(09:00~09:15)만 수집하도록
    스코프 축소. `ka10079`는 '현재 시각'에서 과거로 페이징하는 구조라 이
    태스크(`main.py task_tick_archive`, 09:20 트리거)는 그 직후 실행해야
    저렴함 — 소급 수집은 하루 대부분을 거쳐야 해서 비쌈. 07-31 소급분은
    무의미하다 판단해 스킵, **08-03(월요일)부터 자동 축적 시작**.
  - **주도테마 매수후 가산점(동적캡)**: 1L 주석처리로 `theme_mgr`(테마 판별,
    백그라운드는 계속 작동 중)를 참조하는 코드가 전부 죽어있던 걸 발견.
    "매수 전 게이트가 아니라 매수 후 부합 시 가산점"으로 전환 — 기존 동적
    익절캡(순+1.0%서 강도상승 여부로 캡 1.5→2.5% 상향/조기확정 갈림길)에
    주도테마 부합을 강도상승과 동등한 자격으로 추가. 강도가 안 올랐어도
    주도테마면 조기확정을 면제받고 캡이 상향됨.
  - **거래대금 최소 유동성 필터 신규**: "체결강도 100만으로 불안하면 지연
    없이 뭘 보완할 수 있나"는 질문에 1순위로 채택. `TradeFlowTracker.
    get_trade_value()` 신규 — 같은 틱 버퍼 재사용, REST/대기 추가 없음.
    임계값(3천만원/60초)은 오늘 실제 유니버스 35종목 09:00~09:15 분당
    거래대금 528개 샘플 실측(중앙값 4.8억원, 5퍼센타일 5,350만원)에서
    폴라리스AI 등 일부가 나머지와 뚜렷이 분리된 저유동성 구간(분당 486만~
    2,500만원)만 걸러내는 보수적인 값으로 잡음.
  - **검증**: 이 세션에서만 격리 테스트 약 50건 신규(재진입규칙/1B tick
    프리필터/1A leading-strength/1A 시가급등필터/1A 우선순위교체/1B
    가격트리거/1A 동적강도임계값/get_trade_value/유동성필터/주도테마
    동적캡 등) — 전부 종료코드 확인 후 통과. 검증 도중 오래된 스크래치
    테스트 3개가 새 mock 누락으로 예외를 던지며 조용히 실패(스테일 결과
    파일을 계속 읽고 있었음)하던 것을 발견·수정 — 결과 파일만 보고 통과
    판단하면 안 된다는 교훈.
  - **커밋**: `26b6a29`(전략 재설계 본체) + `edd549e`(원격 테마봇 병합,
    `theme_data.json` 충돌은 최신 원격판 채택) + `8e3fc4b`(주도테마 가산점 +
    유동성 필터). 원격 푸시 완료.

- 2026-08-01 (토, 장 없음 — 정적 분석 + 전면 수정 세션) — **"이관 후 실행에
  문제없는지 꼼꼼히 확인"** 요청에서 출발. 발단은 "어제도 문제없다고 했는데
  실제 장중엔 전략이 뭔가 빼먹거나 그냥 통과하거나 조건검색과 연동이 안 됐다".
  정적 분석 + 실거래 로그 대조 + 격리 재현으로 **9건 발견, 그중 3건은
  08-03에 하루 종일 매매 0건이 될 수준**이었다.

  [치명 3건]
  - **실시간 조건검색 편입(type='02')이 100% 유실되고 있었음** — 키움은 조건검색
    실시간을 두 형태로 보낸다: (1) 조건식 등록(CNSRREQ search_type=1) 응답은
    플랫 구조(`{'9001','302','10',...}`) (2) 실시간 편입/이탈 push는 `type='02'`
    + **values 중첩**(`{'841','9001','843','20'}`). `_dispatch_condition_item`이
    item 최상위에서만 9001/843/841을 찾아서 (2)가 전부 `stock_code=''` -> 조용히
    return 됐다. 07-31 로그 실측: WS 레벨 편입/이탈 로그 **0건**, 그날의
    "편입 신호" 21건은 전부 기동 스냅샷이었다. `task_condition_snapshot_poll`도
    비활성이라 **기동 1회분 외에는 하루 종일 신규 종목이 안 들어오는 상태**였다.
  - **08:59 기동 시 초기 스냅샷 전량 폐기** — `on_condition_hit`이
    `get_current_phase()`가 None(09:00 이전)이면 이름/조건명 기록 **전에**
    return 했다. 07-31 실측 setup 소요 38초라 08:59 기동이면 스냅샷 처리가
    09:00 전에 끝나 통째로 사라진다. 위 항목과 겹치면 조건검색 종목 0건.
  - **10:30 이후 1A 사실상 사망** — `tick()`이 PULLBACK_END 이후 미보유
    감시종목을 10초마다 `stop_watching`(-> `trade_flow.reset`)했다. 원래
    1B(10:30 종료)용 정리인데 새 1A가 같은 버퍼를 쓰게 되면서 자기 데이터를
    계속 지우는 구조가 됐다. 재현: 동일 틱 흐름에서 60초 거래대금 6천만 ->
    1천만(1/6)으로 떨어져 유동성 필터 탈락.
    **1A 창(09:00~14:50)의 70%가 죽어 있었다.**

  [높음 2건]
  - **당일 시가 계산 오류** — `candles[-1]["open"]`은 내림차순상 '가장 오래된 봉'
    이고, 개장 직후엔 키움이 요청 개수를 채우려 전일 봉을 붙여주므로 사실상
    **전일 시가**였다(07-30 09:01 로그: `candles[-1]` = 20260729 15:09봉).
    그 값으로 "주도주상위 시가대비 +5% 매수보류"를 재던 탓에 실제로는
    '전일종가 대비 +5%'가 되어 개장 초반 주도주 대부분이 잘렸다.
    `_today_open()` 신규(time_str로 당일 봉만 걸러 가장 이른 봉의 시가).
  - **보유 종목에 체결틱이 안 쌓임** — `on_trade`가 보유 종목이면
    `on_price_update` 후 즉시 return 해서 `trade_flow.add_tick`에 도달 못 했다.
    매수 10초 후부터 `compute_strength`가 중립값만 반환 -> 동적 익절캡 상향/
    조기확정, 동적캡 즉시매도, 손실반등 하이브리드 매도가 전부 '판단 불가'로
    **영구 미발동**. (07-31에 매수 66초 만에 발동했던 건 매수 전에 쌓아둔 틱이
    남아있던 구간뿐이었다.)

  [중간 4건]
  - `slot_replacement`에 중립값 가드 누락 — 07-31 조기청산 사고와 같은 원인인데
    `_update_dynamic_caps`에만 넣고 여기는 빠져 있었다.
  - 1A가 `score`/`score_threshold`를 안 실어 확장슬롯·슬롯교체 대상에서
    제외됐다. 겸사겸사 전략 간 점수 스케일 불일치(1A 강도 0~300 / Pullback 0~9)도
    해결 — 이제 **컷라인 대비 비율**로 정규화해 비교한다.
  - `_record_watch_list`가 `watch_list_today` 하나로 "재평가 후보"와 "DB 기록"을
    겸해서, 지연평가 경로가 먼저 후보에 넣으면 DB 행이 영영 안 남았다(일일
    백테스트/틱아카이브 유니버스에서 누락). `_watch_db_written`으로 분리.
  - **pending 오버부킹** — `can_buy_more`가 `len(holdings)`만 세는데, 주문 REST가
    도는 동안(429 재시도 2초 blocking 포함) 종목은 pending에만 있다.
    `on_condition_hit`과 `watchlist_reentry`는 별도 `to_thread`라 급등이
    동시다발하면 같은 마지막 칸을 둘이 통과할 수 있었다. `occupied_slots()` 신규.

  [전략 변경 — 사용자 지정]
  - **1B 전면 비활성화** (`PHASE1B_ENABLED=False`). 1A와 트리거 방향이 정반대
    (1A=매수세 폭발 시 진입 / 1B=60초 -2% 급락 시 진입)라 같은 후보 풀에 동시
    적용하면 진입 타이밍이 오락가락하고 어느 로직이 그 자리를 만들었는지 사후
    분석도 불가능하다. 코드는 플래그로 보존. **phase1b 객체 자체는 유지** —
    이제 1B 전략이 아니라 **1A의 체결틱·호가 데이터 파이프라인**
    (TradeFlowTracker/OrderbookTracker)이라 없애면 1A가 눈이 먼다.
  - **1A 진입조건 재설계**: 체결강도 100 이상 60초 -> **3초** + 대량체결 버스트를
    AND로 (3천만원+ 단일체결 **3건 이상** OR 단일 **1억원+ 1건**). 틱 부족은
    명시적 탈락 — 기존 `is_intensity_sustained`는 틱이 없는 구간에 중립값 100을
    채워 임계값 100을 **그냥 통과**시켰다(데이터가 없을수록 쉽게 뚫리는 구조).
  - **호가 기반 하이브리드 주문**: 매도 1~3호가 잔량 '금액'이 5천만원 이상이면
    시장가(`trde_tp="3"`), 미만(빈 호가창)이면 지정가. 호가 스냅샷 없음/길이
    불일치/예외는 **전부 지정가로 수렴**하고, **시장가 거부 시 지정가 1회 자동
    폴백**. 이 계정은 지금까지 지정가만 내봐서 모의서버의 시장가 지원이 미검증.
    `on_orderbook`도 재활성화(07-31 WallDetector 제거 때 통째로 `return`이 되어
    OrderbookTracker가 한 번도 갱신되지 않고 있었음 — 적재만 하고 FSM은 안 함).
  - **전략 라우팅 배타화**: cond_name에 "눌림목자동" 포함 -> Pullback만,
    그 외 -> 1A만. 주문 직전(`_execute_buy`)에도 하드가드를 둬서 다른 경로가
    생겨도 눌림목 종목이 1A로 새지 않는다.
  - **매수 등락률 상한 전략별 분리**: 1A 12% -> **16%**, 눌림목 -> **10%**.
    1A는 추세추종이라 이미 오른 종목이 더 가는 경우를 12%가 잘라내고 있었고,
    Pullback은 되돌림을 사는 전략이라 많이 오른 종목일수록 되돌림 폭도 커서
    위험하다. `_entry_change_cap`이 sub_strategy와 cond_name 둘 다 보고
    보수적인 쪽으로 수렴시킨다.
  - 1L은 07-31부터 계속 주석 처리 상태(변경 없음).

  [슬롯 효율 — 실거래 73건 실측]
  - 07-28~31 청산 73건 분석: 슬롯 용량 8,400 슬롯·분 중 실사용 **834분 = 9.9%**.
    **슬롯은 부족하지 않고 후보/신호가 부족한 것**이 병목이다(슬롯 개수를
    늘리는 건 의미 없음).
  - 슬롯·분 배분: 손절 31% / 시간정리 25% / 슬롯교체 24% / 익절 14% /
    트레일링 5% -> **돈을 번 청산은 슬롯·분의 19%만 사용**. 분당 손익은
    익절 +0.385%, 손절 -0.326%, 시간정리 -0.026%.
  - 조치 (1) 10:30 이후 1A 캡 3 -> 5 (Pullback 창은 하루의 26%뿐인데 3칸을 상시
    예약해 이후 영구히 놀았음) (2) 이로써 **확장 슬롯(7~8) 구조적 도달 불가
    해소** — 1B/1L을 끄면서 1A(3)+눌림(3)=6=MAX_HOLDINGS가 되어 7번째를
    요청할 주체가 아무도 없는 데드코드였다 (3) **정체 포지션 15분 조기 정리**
    (순손익 ±0.5% 밴드, 30분 컷은 최후 방어선으로 유지).

  [종목 우선순위 — 조건검색식 우선순위 대체]
  - 사용자 요구: "조건검색식 우선순위보다 종목 우선순위가 낫다. 다만 surge는
    빠른 포착이 생명인데 순위를 매기면 딜레이가 생길 것 같다."
  - 설계 원칙 = **비교는 미리 해두고 트리거는 즉시 실행**:
    (1) 슬롯에 여유가 있으면 tier를 **조회조차 안 한다**(딜레이 0. 실측 슬롯
    사용률 10%라 대부분의 트리거가 이 경로) (2) 만석일 때만 tier를 보되 후보를
    모아 정렬하지 않고 이미 틱 버퍼에 있는 값을 조회만 한다(REST 0콜)
    (3) 지표를 **자기 대비 비율**로 만들어 조건검색식별 우선순위 개념 자체를
    제거 (4) tier를 모르면(틱 부족) 남의 슬롯만 못 뺏고 빈 슬롯 매수는 정상 진행.
  - `candidate_tier = 거래대금 가속도(최근30초 / 120초평균) x 체결강도배수 x
    신호배수`. 검증: 절대 거래대금이 5배 큰 BIG(균일·중립) tier 0.83 vs 규모
    1/10인 SMALL(최근 집중·매수우세) tier 10.0 — **소형 급등주가 대형주에
    안 밀린다.**
  - 교체 대상은 "아직 결과가 안 난 자리"(순손익 ±0.5%)로 제한 — 오르고 있는
    포지션을 순간 tier 스파이크로 빼앗지 않는다. 기존 09:00~09:10 시간제한은
    해제(슬롯 만석 자체가 이미 충분한 게이트).

  [제안 A/B/C 적용]
  - **A 신호 계층화**: tier에 단일 대량체결 가중(1억+ x1.5 / 3천만+ x1.2).
    진입 트리거는 두 조건을 동등 취급하되 **슬롯 경합 시에만** 차등을 둔다.
    이산·유한 배수라 tier 폭주 없음.
  - **B 조건검색식별 성과 보정**: `strategy_performance`의 축소추정을
    `cond:주도주상위` 등 **조건식 축**으로도 적용. 병합 조건명은 대표 키 하나로
    접어 표본이 쪼개지지 않게 한다. **100을 바닥 고정**(성과가 좋아도 진입이
    원래보다 헐거워지지 않음). 우선순위가 아니라 문턱 조정이라 **지연 0**.
  - **C tier 기반 금액 가중**: tier 1.0 -> 1.0배, 1.5 이상 -> 1.5배(상한).
    **위쪽으로만** 움직인다 — 축소 방향까지 열면 평범한 후보의 매수 수량이
    0주가 되어 조용히 매매를 잃는다. 최대 노출 8슬롯 x 300만 = 2,400만원.

  [진입 진단 알림 — 신규]
  - "조건검색엔 계속 포착되는데 매수가 안 된다"를 장중에 원인별로 판단하기 위한
    관측 인프라 — `StrategyManager.build_entry_diagnostics()` +
    `main.task_entry_diagnostics`. 텔레그램 '주식 따릉이'(signal) 채널.
  - **정상 필터링과 코드/인프라 이상을 분리해서 보여주는 것**이 핵심:
    사유별 종목 수 집계 + 경고(감시중인데 체결틱 0 -> 0B 구독 이상 / 후보인데
    감시 미시작 -> 라우팅 이상 / 최근 10분 평가 기록 없음 -> 재평가 루프 정지 /
    실시간 편입 수신 0건 -> WS 조건검색 구독 이상) + 전면차단 표시
    (MDD/HALT/WS격리/15:10 컷오프).
  - 알림 피로 방지: 평상시 30분 주기, **경고가 잡히면 10분** 주기.
  - 사유 분류는 `_REJECT_RULES`(문자열 -> 카테고리 매핑)로 하므로, 새 탈락
    사유 문자열을 만들면 여기 규칙도 같이 추가할 것(안 하면 "기타"로 뭉개짐).

  [REST 호출 예산]
  - 실시간 편입이 살아나면 후보 수가 급증해서 `watchlist_reentry`(15초 주기)가
    후보마다 분봉을 긁어 429가 폭증할 위험이 생겼다(이미 하루 2,469건 포화).
    새 1A는 분봉이 전혀 필요 없으므로 **REST 0콜 고속경로** 추가 — 실시간
    체결가 + 캐시된 당일 시가가 있으면 REST를 한 번도 안 부른다. 시가 캐시가
    없으면 1회만 조회하고 캐싱(자가 치유). 후보 40종목 기준 한 사이클
    40콜 -> 0콜. **Pullback은 분봉이 필요하므로 기존 경로 유지.**

  [세션 후반 추가 지시 반영 — 시간창/라우팅/주문]
  - (1차 결정, 아래 "최종 정정" 항목으로 대체됨) **Pullback 시간창
    09:00~10:30 -> 09:20~15:10**으로 처음 늘렸다. 시작을 늦춘 이유는 개장
    직후엔 당일 고가가 아직 형성되지 않아 "눌림(고가 대비 되돌림)" 판정 자체가
    성립하기 어렵기 때문이고, 종료를 늘린 이유는 눌림목자동 조건검색이 하루
    종일 도는데 오후 눌림을 통째로 버릴 이유가 없어서였다.
  - **`ENTRY_WINDOW_END`(두 전략의 진입 평가가 살아있는 마지막 시각) 신설**.
    on_condition_hit / _cleanup_watched / watchlist_reentry가 전부
    PHASE1A_END(14:50)를 직접 박아 쓰고 있어서, Pullback 시간창만 늘려도
    재평가·감시가 14:50에 먼저 끊겨 그 뒤 구간이 조용히 죽는 함정이 있었다.
    세 곳을 `max(PHASE1A_END, PULLBACK_END)`로 자동 산출되는 이 상수 하나로
    통일 — 이후 어느 쪽 시간창을 고쳐도 세 곳이 항상 같이 따라온다.
  - 1A 분기에도 시간창 확인 추가 — 예전엔 `can_buy_phase1a`에만 있어서 자기
    창이 끝난 뒤에도 평가·워치리스트 기록이 계속 돌고 매수만 막혔다(Pullback
    분기와 대칭이 안 맞았음).
  - **조건검색식별 09:20 지연 게이트 제거**(OTHER_COND_START / IMMEDIATE_COND_NAMES
    상수째 삭제, 이 항목은 최종 상태에서도 유지됨). 07-29에 돌파자동매매용을
    지연시킨 근거는 "그때의 1A는 대형주에 덜 급하다"였는데, 지금 1A는 3초 틱
    기반이라 09:00부터 판정 가능하고 급등이 가장 많은 개장 20분을 버리는
    손실이 더 컸다. 1B 폐지로 돌파자동매매용은 1A 외에 갈 곳도 없어졌다.
  - **슬롯 캡 1A/눌림 각 3 -> 4**(이 값은 최종 상태에서도 유지), 시간대별
    상향(`PHASE1A_MAX_SLOTS_LATE`)은 폐지. 캡 합(8)이 공유 상한(6)을 넘게
    잡아 (a) 한쪽이 후보를 못 찾으면 다른 쪽이 그 자리를 흡수하고
    (b) 확장 슬롯(7~8) 도달 가능 상태를 유지한다. 한 전략이 6칸을 독식하는
    일은 여전히 불가(캡 4).
  - **시가대비 +5% 매수보류를 1A 전 경로로 확대**(주도주상위 전용 -> 주도주상위
    +돌파자동매매용, 사용자 지정, 최종 상태에서도 유지). 조건명으로 분기하지
    않는 이유: cond_name이 "기타"로 뭉개진 경우에도 필터가 조용히 꺼지지
    않게 하기 위함.

  [세션 최종 정정 — Pullback 09:25~14:50 + 중복종목 10:30 전략전환 + 강제청산 15:10]
  - 위 1차 결정(Pullback 09:20~15:10)을 사용자가 같은 세션에서 다시 조정.
    **Pullback 시작 09:20 -> 09:25**(기존 눌림목 로직 그대로 유지),
    **종료 15:10 -> 14:50**(=PHASE1A_END와 정확히 동일). 두 전략의 진입
    중단 시각을 한 점으로 묶어 "한쪽만 살아있는 구간"을 아예 없앴다.
  - **강제청산 15:15 -> 15:10** — 진입 종료(14:50) < 청산(15:10)이라 청산
    처리 중 신규매수가 겹치지 않고, 신규매수 하드컷오프(ENTRY_HARD_CUTOFF)와도
    같은 시각이 됐다.
  - **중복 편입 종목의 시간대별 전략 전환** 신규(`DUAL_SOURCE_PULLBACK_FROM
    =10:30`) — 단독 소스는 시각과 무관하게 완전 분리(눌림목자동 단독은 항상
    Pullback, 주도주상위/돌파 단독은 항상 1A)를 유지하되, **중복 편입** 종목만
    10:30 이전엔 1A(오전 모멘텀), 10:30 이후엔 Pullback(오후 되돌림)으로
    갈아탄다. `resolve_strategy()` 한 함수로 규칙을 모아 라우팅·주문직전
    가드·daily_backtest 세 곳이 전부 이 함수를 호출하게 했다 — 그렇지 않으면
    규칙이 갈라져 어긋난다.
  - 이 정정으로 주문 직전 가드도 다시 손봤다: 예전 가드("눌림목자동이 cond_name에
    있으면 1A 매수 전부 차단")를 그대로 두면 중복 종목의 정상적인 오전 1A
    매수까지 막히므로, **단독 소스만** 막도록(반대 방향인 "1A 단독을 Pullback
    으로 사려는 시도"도 함께) 변경했다. 등락률 상한도 cond_name 병용 판정에서
    **실제 매수 전략(sub_strategy) 단독 판정**으로 바꿨다 — 중복 종목이 오전에
    1A로 사졌는데 눌림목 상한(10%)이 걸리는 모순을 없앴다.

  [제안 1 — 매도 시장가 (구멍 메우기, 이번 세션 최대 위험 해소)]
  - 기존엔 **모든 매도**가 `현재가 -1틱` 지정가였다. 그런데 키움 응답의
    `return_code == 0`은 "주문 접수"이지 "체결"이 아닌데 `_execute_sell`은
    접수를 체결로 간주해 holdings에서 지우고 DB에 매도가를 기록한다.
    미체결이면 그 포지션은 **봇의 관리 대상에서 영구 이탈**한다 —
    `_reconcile_manual_sells`는 holdings->서버 방향만 보고, 서버엔 있는데
    holdings엔 없는 종목을 되살리는 로직이 없었기 때문이다. 손절도 익절도
    안 되고 15:15 강제청산(같은 지정가 경로)마저 미체결이면 오버나이트로 넘어간다.
  - 미체결 확률 자체는 낮지만 **위험 구간이 하필 급락 손절**이다:
    get_current_price(REST) 조회와 주문 도달 사이에 가격이 더 떨어지면 지정가가
    시장보다 위에 남는데, 429 스로틀 시 그 사이에 2초 blocking sleep이 낀다.
    1틱을 아끼려다 -3% 손절이 -6%가 되는 거래였다.
  - `order_manager.sell` 기본값을 **시장가**로 바꾸고, 거부 시 지정가 1회 폴백.
    (사용자가 모의투자 시장가 지원을 직접 확인함 — 폴백은 보험으로만 유지)
  - 안전망 `_detect_orphan_positions` 신규: 서버 잔고엔 있는데 holdings엔 없는
    종목을 감지해 텔레그램 알림(종목당 1회). **자동 복구는 하지 않는다** —
    사용자가 직접 산 종목까지 봇이 관리·청산하면 더 큰 사고이므로 판단은 사람에게.

  [제안 2 — 빈 호가창 지정가를 매도N호가로]
  - 호가창이 비었다는 건 매도1호가가 현재가보다 여러 틱 위일 가능성이 크다는
    뜻이라 '현재가+1틱' 지정가는 **아예 안 걸릴 수** 있었다. 미체결이면 봇은
    산 줄 알고 슬롯을 점유한 채 잘못된 매수가로 손익을 계산하다가 45초 뒤
    reconcile이 정리한다(매수 쪽은 자가 치유되지만 그동안 낭비).
  - 이제 매도3호가로 걸어 **슬리피지 상한이 정해진 즉시체결**로 만든다.
    호가 정보가 없으면 기존 현재가+1틱 폴백 유지.
    `OrderbookTracker.get_ask_price()` 신규(요청 레벨이 없으면 가장 깊은 호가로 대체).

  [daily_backtest 동기화 — 조용히 죽을 뻔한 것]
  - 삭제된 상수(IMMEDIATE_COND_NAMES / OTHER_COND_START)를 그대로 임포트하고
    있어서 **매일 15:30 백테스트가 ImportError로 조용히 실패**할 상태였다
    (task_daily_backtest가 lazy import + try/except라 봇은 안 죽고 리포트만 사라짐).
    제거하고, Pullback 시간창을 문자열 하드코딩("1030") 대신 라이브 상수에서
    직접 가져오도록 바꿔 앞으로 라이브를 바꾸면 백테스트가 자동으로 따라온다.

  [검증]
  - `test_patch_20260801.py` **245건 전원 통과**(32개 섹션). 07-31 실제 로그의
    편입 이벤트 원본을 그대로 투입해 재현했고, 구버전 버그도 함께 테스트로
    못박았다(예: "구버전은 여기서 리셋", "구버전은 0건").
  - 기존 `test_phase1b_controller.py`/`test_microstructure.py`는 실패하지만
    **HEAD에서도 동일하게 실패** — 5월 작성 스테일 테스트(WallDetector FSM +
    STRENGTH_MIN_TICKS 도입 이전 가정)라 회귀가 아님. 확인 방법은
    `git stash` 후 재실행.
  - 세션 후반 변경(시간창/라우팅/매도 시장가)으로 옛 사양을 검증하던 테스트
    9건이 정확히 실패했고, 전부 새 사양 단언으로 교체했다 — 테스트가
    사양 변경을 잡아낸 정상 동작이다.
  - 커밋: `96abe57`(치명 3건+2군+1B 비활성화+1A 재설계+하이브리드 주문),
    `423e6a2`(슬롯 효율+종목 우선순위), `a052c8a`(제안 A/B/C+진단 알림),
    `b845d26`(등락률 상한 분리+CLAUDE.md), `242ab76`(시간창/라우팅/매도 시장가),
    `6e54404`(Pullback 09:25~14:50 + 중복종목 10:30 전환 + 강제청산 15:10).



  [모바일 원격 제어 구축 — 2026-08-01 세션 마지막]
  - 이 PC에 Claude Code CLI가 아예 없던 상태에서 처음부터 구축했다:
    Node.js LTS(winget) -> claude CLI(npm 전역) -> Claude Pro 계정 로그인 ->
    프로젝트 폴더 신뢰 승인 -> `claude --remote-control`로 원격 세션 개설.
    상세 버전/경로/함정은 위 "개발/운영 환경" 섹션 참고.
  - 목적: PC에서 작업하던 것을 **폰에서 이어서** 확인·조작하기 위함.
    특히 장중(09:00~15:10)에 PC 앞에 없을 때 로그/진단 알림을 보고
    바로 상태를 물어볼 수 있게 하는 용도.
  - 원격 세션은 **이 대화의 맥락을 그대로 갖고 있지 않다.** 대신 프로젝트
    폴더에서 시작하므로 이 CLAUDE.md를 읽어 맥락을 복원한다 —
    **그래서 이 문서의 정확도가 곧 이관 품질**이다.

- 2026-08-02 (일, 장 없음) — **진입 로직 틱 구동 전면 전환**. 사용자가 눌림목자동
  조건검색식(3분봉 30이평 근접 + 고점대비 -3% + 20봉내 25% 급등 + 5일 평균
  거래대금 100억+)을 공유하며 "지연 없이 확실한 종목만 매수"를 요구한 데서 출발.

  [핵심 발견 — 3초 조건이 원리상 작동 불가였음]
  - `on_trade`(체결 틱 콜백)가 **틱을 버퍼에 쌓기만 하고 진입 평가를 전혀
    하지 않고 있었다.** 1L 블록은 `if False:`로 죽어 있었고, 평가가 실행되는
    경로는 `on_condition_hit`(편입 순간 1회 — 이때는 틱이 0개라 "체결틱 부족"
    으로 무조건 탈락) + `task_watchlist_reentry`(**15초 주기 폴링**)뿐이었다.
  - 진입 조건은 "체결강도 100 이상 3초 유지"라는 **3초짜리 신호**다. 그걸
    15초 간격으로 샘플링하고 있었으니 평균 7.5초/최대 15초가 늦고, 강도가
    3~5초만 100을 넘었다 꺼지는 종목은 **관측조차 되지 않았다.**
  - 원인: 1B/1L을 비활성화할 때 그 둘이 갖고 있던 "실시간 틱 콜백 진입"
    경로가 통째로 사라졌는데 새 1A가 그걸 물려받지 못했다.
  - -> `on_trade`에 `_maybe_tick_entry()` 배선. 판정은 전부 메모리 연산이라
    REST 0콜이고, 조건이 차는 그 틱에서 발화한다.

  [설계 — 무장 -> 발사 2단]
  - **무장**: 키움 공식 체결강도(**FID 228**)가 100 이상 **3초 연속**.
    자체 `compute_strength`를 안 쓴 이유: 틱 부족 시 정확히 100.0을 반환하는데
    임계값도 100이라 **데이터가 없을수록 쉽게 뚫린다**. 228은 이미 매 틱에
    실려오는데(`parsed["strength"]`) 1L 주석처리 이후 아무도 안 쓰고 있었다.
    윈도우 평균이 아니라 `_strength_since` 타이머로 진짜 '연속'을 잰다.
  - **발사**: 대량체결 버스트 3경로 OR (창 5초).
    사용자 원안의 "1초 이내 2회"는 **판정이 물리적으로 불가능**해서 5초로 바꿨다
    — 키움 FID 20이 `HHMMSS` 초 단위라 같은 초의 두 체결은 타임스탬프가 동일하고,
    WS는 여러 틱을 배치로 몰아 보낸다.
  - **순서가 핵심**: 느린 조건(강도 3초)을 무장으로, 순간 이벤트(버스트)를
    발사로 뒀다. 반대로 하면 버스트를 본 뒤 3초를 기다려 그만큼 늦는다.

  [사용자 질문 — "주가대비 가중평균거래대금"]
  - **주가로 나누는 건 틀린 축**이라고 답했다. 3천만원은 1,500원 주식이면
    2만주, 15만원 주식이면 200주인데 결국 *주식 수*가 나오고, 주식 수는
    유동성과 무관하다(같은 7만원짜리도 유동성이 100배 다르다).
  - 옳은 축은 **그 종목의 평균 1틱 체결금액 대비 배수**다. 조건식 C가 보장하는
    건 일 거래대금 100억+뿐이라 그 안에서도 평균 1틱은 30만~300만원으로 10배
    벌어진다. `avg_trade_value()` 신규 + 경로 ③(평균 × 20배)로 반영.
  - **부수 효과가 더 크다**: 자기 정규화라 **시간대 보정이 자동**이다. 점심에
    시장이 마르면 분모도 같이 줄어 '평균의 20배'라는 난이도가 시각과 무관하게
    유지된다.

  [시간대 계수 — 사용자 지정 "시간이 지날수록 느슨해지니 반영"]
  - 절대 경로(①②)에만 적용: 09:00~ 1.00 / 10:30~ 0.85 / 11:30~ 0.65(점심 최저)
    / 13:00~ 0.80 / 14:00~ 0.95. 안 낮추면 오후가 통째로 죽는다.
  - 경로 ③에는 적용하지 않는다(자기 정규화라 중복 완화가 된다).

  [격리 테스트가 잡은 실제 설계 결함]
  - 상대 경로의 분모(평균 1틱)에 **지금 터지는 버스트가 같이 들어가** 있었다.
    분자와 분모가 함께 커져서 **버스트가 클수록 문턱도 올라가는 자기모순** —
    "평균의 40배짜리 체결 2건"이 실제로 탈락했다. `exclude_recent_sec`(최근
    5초 제외)로 '버스트 직전까지의 평상시 체결 크기'만 재도록 수정.
  - `_maybe_tick_entry`가 pending을 미리 넣는데 `_execute_buy`에는 **pending
    등록 전에 early-return 하는 경로가 여럿**(하드컷오프/등락률 상한/전략
    불일치/수량 0) 있어서, 그 경우 pending이 영구 잔류해 `occupied_slots()`가
    그 자리를 계속 점유로 세고 그 종목은 다시는 매수되지 않았다. finally로 회수.

  [함께 바뀐 것]
  - **Pullback의 분봉·점수·VWAP 경로 폐지** — 눌림 판정은 HTS가 이미 하는데
    코드가 1분봉으로 재검증하느라 (a) 시간축이 안 맞아 26종목 전부 탈락하고
    (b) 1분봉 종가를 기다리느라 구조적으로 최대 60초 늦었다. 이제 1A와 완전히
    같은 트리거를 쓴다(차이는 유니버스/슬롯/상한/익절캡/시간창에만).
  - **주문 우선 레인**: 주문(kt10000/kt10001)이 조회와 같은 큐에 서서, 가장
    급한 순간에 방금 나간 조회 때문에 최대 0.6초(+429면 2초)를 기다렸다.
    `_throttle(priority=True)` + `ORDER_MIN_INTERVAL=0.2`로 분리(초당 5회 제한
    안이라 안전). 매도도 같이 적용 — 손절이 늦으면 그만큼 더 밀린 가격에 나간다.
  - **pre-arm**(`prearm_candidate`): 편입 즉시 감시 시작 + 전일종가/시가 캐시
    예열. 트리거 시점에 REST를 부르지 않는다.
  - `watchlist_reentry`는 **主 경로에서 안전망으로 강등** — 두 전략 모두
    REST 0콜이 됐다(구버전은 Pullback 후보마다 분봉 2콜).
  - `entry_reason`에 틱 경로 분기를 **최우선**으로 추가. 안 그러면 sub_strategy
    분기에 걸려 실제로는 쓰지 않는 옛 설명(MA5 눌림목/거래량증가지속)이 DB에
    기록돼 사후 분석이 통째로 어긋난다.
  - 진단 알림 `_REJECT_RULES`에 `강도 미무장(3초 미달)` 분류 추가.
  - `daily_backtest`는 **이제 라이브 진입을 재현하지 못한다**(분봉만으로 재현
    가능한 진입 로직이 하나도 안 남음). 숫자는 그럴듯하게 나오므로 리포트
    상단에 경고를 넣었다 — 청산 정책 비교용으로만 볼 것.

  [검증] `test_patch_20260802.py` 신규 83건 전원 통과(10개 섹션) +
  기존 `test_patch_20260801.py` 250건 전원 통과(구 사양을 검증하던 9건은
  새 사양 단언으로 교체 — 테스트가 사양 변경을 잡아낸 정상 동작).

- 2026-08-02 (계속) — **실전 드라이런 정밀 검증: 08-03에 터졌을 버그 6건 발견·수정**.
  "장중에 오류 나서 고치는 상황을 없애달라"는 요구로, 08:59 기동부터 15:10
  강제청산까지 하루 전 구간을 재현하는 통합 테스트(`test_live_dryrun_20260803.py`,
  71건/16섹션)를 새로 만들어 전수 점검했다. **단위 테스트 333건이 전부 통과하는
  상태에서도 아래가 전부 살아있었다** — 유닛은 부품을, 이건 배선을 본다.

  [치명 — 그대로 뒀으면 08-03에 매매가 반쪽 났을 것]
  - **청산 후 강도 타이머 잔류 -> 무장 단계 우회**: `_strength_since`/`_armed_at`을
    어디서도 안 지웠다. 매도 후 그 종목이 재매수 가능해지면 첫 틱에서
    `now - since`가 수 분~수십 분으로 계산돼 **"3초 연속"이 이미 충족된 것으로
    처리**된다. 그 상태에서 버스트 하나만 오면 진입 조건의 절반이 없는 채로
    매수가 나간다. 손실청산·쿨다운은 재매수차단이 막아주지만 **익절 후 3분이
    지난 종목은 그 그물을 정상 통과**하므로 실제로 뚫린다.
    -> `reset_tick_entry_state()` 신설, 매도(`_execute_sell`)와 감시해제
    (`_cleanup_watched`) 두 지점에 연결.
  - **08:59 스냅샷 종목이 체결틱을 못 쌓음**: 장 시작 전이면 `watch_list_today`에만
    넣고 `return` 해서 `start_watching`이 안 됐다. 무장은 틱이 쌓여야 시작되므로
    09:00 이후 첫 `watchlist_reentry`(최대 15초)까지 대기 — **하루 중 거래가 가장
    폭발하는 개장 직후 15초를 통째로 날리는** 구조였다. 장 시작 전에도 pre-arm은
    하도록 수정(매수는 여전히 09:00부터).
  - **cond_name 병합이 한 방향뿐**: "주도주상위가 **나중에** 온 경우"에만 병합해서,
    주도주상위가 먼저 걸리고 눌림목자동이 나중에 온 종목은 영영 단독으로 남았다.
    -> 중복 편입 종목의 10:30 전략 전환이 **절반만 작동**하고 있었다.
    방향 무관 병합으로 수정.

  [높음]
  - **분봉 부족 종목은 감시조차 시작 안 됨**: `on_condition_hit`이 분봉 15개 미만이면
    `return` 했는데, 그 앞에 pre-arm이 없어서 신규 상장주·거래 뜸한 종목은
    **무장이 원천적으로 불가능**했다. 틱 구동은 분봉이 전혀 필요 없으므로
    pre-arm을 분봉 조회보다 앞으로 옮기고, 분봉 실패 시엔 실시간 체결가로 진행.
  - **15:10 강제청산이 REST 실패 시 오버나이트**: `get_minute_candles` 하나만 보고
    `if candles:`에 걸리면 `_execute_sell`이 아예 안 불렸다. 이 시각은 429가 가장
    심한 구간이고 장 마감 후엔 재시도할 태스크도 없다. 매도는 시장가라 기준가는
    기록용이므로 **실시간 체결가 -> 분봉 -> 매수가** 순으로 폴백하도록 수정.
  - **감시 누락 시 무장만 되고 발사는 영원히 안 되는 상태**: 무장은 FID 228만 보는데
    버스트 판정은 `trade_flow` 버퍼를 읽는다. pre-arm이 어떤 이유로든 누락되면
    그 종목은 조용히 매매에서 빠진다. `_maybe_tick_entry`에 자가 치유 가드 추가.

  [중간 — 붕괴 패턴 예방]
  - `task_subscribe_flush`(1초 주기, WS 전송)와 `task_auto_shutdown`(텔레그램)에
    try/except가 없었다. 여기서 예외가 새면 `asyncio.gather()`가 무너져 봇이
    통째로 종료된다(2026-07-27 실제 장애와 같은 경로). 18개 태스크 전수 점검 후
    이 둘만 보강.

  [함께 확인한 것 — 정상]
  - 프로젝트 242개 .py 전수 구문검사: 오류 8건 전부 미사용 덤프/worktree 사본
    (`db_all.py`, `backtest_simulation.py` 등), 라이브 경로는 이상 없음.
  - 루트 `phase_settings.py`는 어디서도 import되지 않는 스테일 덤프
    (라이브는 `config/phase_settings.py`).
  - `_get_market_defense_mode`는 `MARKET_DEFENSE_ENABLED=False`라 즉시 반환 —
    핫패스에서 REST가 나가지 않는다(켜더라도 60초 캐시).
  - 핫패스 성능 실측: 30종목 x 3,000틱에 **틱당 1us**. 실시간 유입을 못 따라갈
    위험 없음. 무장 후에도 쿨다운(0.25초)이 200틱당 8회로 게이트 폭주를 막는다.
  - 슬롯 회계: 상한 초과 매수 없음, pending 누수 없음, `occupied_slots()`가
    실제 보유수와 일치.
  - 이상 입력 내성: 강도 None/음수/무한대, 종목코드 빈값, 호가 없음,
    phase1b 없음, DB 다운 — 전부 예외 없이 통과하고 매매는 계속된다.

  [검증] 3개 스위트 **404건 전원 통과** (단위 250 + 83, 통합 71).
  통합 테스트는 앞으로도 전략을 바꿀 때마다 돌릴 것 —
  **유닛이 다 통과해도 배선이 끊어져 있을 수 있다는 게 이번의 교훈**이다.

- 2026-08-02 (계속) — **프로젝트 파일 정리**: 추적 파일 139개 -> 99개,
  저장소 27.6MB -> 1.1MB. 용량이 목적이 아니라 **옛 덤프에 삭제된 전략 코드가
  통째로 남아 grep·검색·AI 컨텍스트를 오염시키는 것**이 문제였다(이 세션에서만
  두 번 걸렸다 — 스테일 `phase_settings.py`의 `evaluate_new_intensity_strategy`
  호출을 라이브로 착각할 뻔했다).
  - 판단 근거: `main.py`에서 시작한 **import 도달성 분석**(상대 임포트 포함).
    라이브 모듈은 34개뿐이다. 초안에서 상대 임포트를 빼먹어 `core/theme_manager.py`가
    고아로 잘못 잡혔던 걸 발견·수정했다 — **이 분석을 다시 할 일이 있으면
    반드시 상대 임포트를 처리할 것.**
  - **삭제**: 루트 `phase_settings.py`(진짜는 `config/phase_settings.py`, 이름이
    같아 가장 위험했음) / 코드 덤프 txt 9개(`combined_strategy_code.txt` 25.4MB
    포함, `merge_code.py`로 재생성 가능) / 일회성 패치 `fix_*.py` 3개 /
    0바이트 파일 15개 / **스테일 테스트 8개**(삭제된 Phase2·Phase3·WallDetector
    FSM을 검증하고 있었음) / 구문오류로 실행 불가한 `backtest_simulation.py`·
    `db_all.py` / `utilslogger.py`(ThemeManager 중복 정의 + `utils/logger.py`와
    이름 혼동).
  - **보존 + `[삭제 예정 / DEPRECATED]` 배너**(15개): `*_legacy.py` 12개 +
    `phase3_controller.py`/`order_queue.py`/`api/kiwoom_api.py`.
    CLAUDE.md 규칙 2("파일 삭제 금지")와 충돌해서 지우지 않되, 읽는 사람이
    현재 코드로 착각하지 않도록 최상단에 명시했다. 정리하려면
    `git rm $(git ls-files "*_legacy.py")`.
  - **절대 삭제 금지로 확인된 것**: `notify_scheduler_start.py`(start_trader.bat이
    매일 08:59 호출), `__init__.py` 3개(패키지 마커), 수동 도구 8개
    (`analyze_program_flow.py`/`probe_tick_chart_api.py`/`fetch_themes.py`/
    `create_tables.py` 등).
  - 이전 세션이 남긴 `.claude/worktrees/` 2개도 정리(3.3MB, git 미추적인데
    grep에는 계속 걸렸음). `git worktree remove`가 OneDrive 잠금으로 실패해
    등록 해제 후 PowerShell로 폴더를 지웠다.
  - **유효한 테스트는 이제 3개뿐**: `test_patch_20260801.py`(250 -> 08-02에 281) /
    `test_patch_20260802.py`(83) / `test_live_dryrun_20260803.py`(71).
    정리 후 재검증 404건 전원 통과 + `import main` 정상.

- 2026-08-02 (계속) — **중복/충돌 재감사 + 관측 보강**. 정리 후 라이브 34개
  모듈을 다시 훑었다. 클래스/함수 이름 충돌은 사실상 없었고(`_cache_path`/
  `_save`는 서로 다른 파일의 private 헬퍼), **정합성이 깨진 5건**을 정리했다.

  [🔴 관측 보강 — 08-03 최대 리스크 대응]
  - **FID 228 수신 감시 신설**(`task_fid228_watchdog`, 09:05 1회).
    이번 개편에서 **유일하게 실거래로 검증되지 않은 가정**이 "체결 틱에 228이
    실려 온다"는 것이다(파싱 코드는 예전부터 있었지만 1L 주석처리 후 아무도
    안 씀). 228이 안 오면 무장이 영원히 성립하지 않아 **하루 종일 매수 0건**이
    되는데, 겉으로는 아무 에러도 안 나서 로그를 뒤지기 전엔 알 수 없다.
    - `_trade_tick_total`(0B 자체가 오는가) / `_fid228_seen`(228이 실려온 종목)을
      `on_trade`에서 집계 — set.add라 핫패스 비용 없음(실측 틱당 2us 유지).
    - **"0B가 안 옴"과 "0B는 오는데 228만 빔"을 구분해서 경고**한다. 전자는
      WS 구독 문제, 후자는 FID 매핑 문제로 대응이 완전히 다르기 때문이다.
    - 진단 알림(30분 주기)에도 같은 경고가 들어가지만, 워치독은 09:05에
      **즉시 한 번** 알려 장 초반에 손쓸 수 있게 한다.
  - **진단 알림에 틱 단계별 통과 수 노출**: `⚡ 틱 12,340 → 무장 8 → 버스트 3
    → 매수 2`. `_tick_entry_stats`를 수집만 하고 안 보여주고 있었다 — 08-03에
    가장 알고 싶은 숫자인데 어느 단계가 병목인지 알 방법이 없었다.

  [🟡 정합성 정리]
  - **주석이 코드와 어긋난 3곳 수정** — `strategy_manager.py`의 Pullback 시간창
    주석과 `can_buy_pullback` docstring이 전부 "09:20~15:10"이라고 하는데
    **실제 상수는 09:25~14:50**이었다(세션 중간안이 주석에만 남음). 다음 세션이
    잘못된 시간창을 근거로 작업할 위험이 있어 고쳤고, 테스트로 못박았다.
  - `_REJECT_RULES`에서 `"조건식 지연(09:20)"` 분류 제거 — 그 게이트는 08-01에
    상수째 폐지돼 해당 사유 문자열이 더 이상 생성되지 않는다.
  - 미사용 상수 3개(`OBV_LOOKBACK`/`MAX_WATCH_SLOTS`/`WATCH_TIMEOUT`)에
    "[미사용]" 표시 — 지우지 않은 이유는 전자는 Pullback 분봉 경로를 되살릴 때
    필요하고, 후자 둘은 삭제된 1N 전략의 잔재라 맥락 기록이 유용해서다.
  - **`Phase1BController` docstring 전면 교체** — 이름은 '1B 컨트롤러'인데
    실제로는 **1A/Pullback의 데이터 파이프라인**이다. 라이브에서 쓰는 것
    (`trade_flow`/`orderbook`/`watched`)과 **한 번도 안 불리는 것**
    (`on_trade`/`on_orderbook`/`get_state`, `wall_detector`, `evaluator`)을
    명시했다. StrategyManager가 이 래퍼를 거치지 않고 트래커를 직접 갱신하기
    때문인데, 모르면 "1B가 아직 도나?"로 오해하기 딱 좋다. 리네임은 위험이
    커서 문서화로 대신했다.

  [확인만 하고 둔 것]
  - **수수료 상수 이원화**: `config/settings.py`의 `COMMISSION_RATE`+`TAX_RATE`는
    **아무도 안 읽고** 실제로는 `ROUND_TRIP_COST=0.23%`만 쓴다. settings.py가
    .gitignore 대상(로컬 전용)이라 손대지 않았지만, **거기서 수수료를 고쳐도
    반영되지 않는다**는 점은 기억할 것.
  - 핫패스는 깨끗하다 — `WallDetector.on_orderbook`은 주석 처리돼 있어 호가
    틱마다 낭비되지 않는다(확인함).

  [검증] `test_live_dryrun_20260803.py` 71 -> **84건**(섹션 17 FID228 감시,
  18 정합성 신설) 전원 통과 + 기존 333건 유지 = **총 417건**.

- 2026-08-02 (계속) — **1B/1L 전략 코드 완전 삭제** (순 -419줄).
  발단은 "1B/1L 때문에 충돌이나 헷갈릴 수 있으면 삭제해도 된다"는 지시.
  먼저 조사한 결론은 **실제 충돌은 없다**였다 — 1L은 `if False:` + 문자열
  리터럴이라 파이썬이 파싱조차 안 했고, 1B는 `PHASE1B_ENABLED=False`로 전부
  가드돼 테스트로 못박혀 있었다. 비용은 순수하게 "헷갈림"이었는데, **그
  헷갈림으로 이미 실거래 사고가 두 번 났다**(08-01 `_cleanup_watched`가 1A
  틱 버퍼를 지워 1A 창의 70%가 죽음 / 07-31 1L 특별케이스 누락으로 매수
  66초 만에 조기청산). 그래서 "지금 안 터진다"가 아니라 "다음에 터진다"를
  근거로 삭제를 택했다.
  - **삭제한 것**: 1L 진입 블록·진단 함수·상수 5개·인스턴스 상태 6개 /
    1B 매수 함수 2개(`_try_phase1b_buy`/`_check_1b_confirmations`)·
    `can_buy_phase1b`/`can_buy_leading`·`_1b_confirm`·상수 8개·
    `TAKE_PROFIT_CAP_1B`·on_trade 하락 트리거·on_orderbook FSM 주석 /
    `Phase1BController`의 WallDetector·ChemulEvaluator 배선과
    `on_trade`/`on_orderbook`/`get_state` 래퍼(전부 라이브 호출 0회).
  - **트레일링 익절도 함께 제거**(사용자 지정) — `sub_strategy == "1L"`이
    유일한 진입점이라 1L 삭제 시점에 이미 도달 불가였다. 이제 익절은 예외
    없이 flat 캡이라 **라이브와 daily_backtest의 청산 규칙이 정확히 일치**한다
    (예전엔 백테스트가 "트레일링은 재현 안 함"이라고 적어두고 넘어갔음).
  - **유지한 것**: `phase1b` 컨트롤러 객체(trade_flow/orderbook/watched) —
    전략이 아니라 1A/Pullback의 데이터 파이프라인이라 없애면 두 전략이 눈이
    먼다. `theme_mgr`도 유지 — 1L과 함께 죽은 게 아니라 **매수 후 동적 익절캡
    가산점**으로 계속 쓰인다.
  - `config/phase_settings.py`의 `PHASE_1B`와 `EXIT_POLICY`의 `trail_*`는
    이제 **아무도 안 읽는다** — 삭제 대신 "[미사용]" 배너를 달았다(CLAUDE.md
    규칙 2). 거기서 값을 고쳐도 반영되지 않는다는 점 주의.
  - **리네임은 미룸**: `phase1b` -> `TickDataPipeline`이 근본 해결이지만
    호출부가 ~40곳이고 **08-03이 틱 구동 신규 구조 첫 실거래일**이라,
    지금 얹으면 내일 문제가 났을 때 "새 전략 탓인지 리팩터링 탓인지"를
    구분할 수 없게 된다. 실거래 검증 후로 이월.
  - [검증] `test_patch_20260801.py` 250 -> **281건**(섹션 15를 "비활성화 확인"
    에서 "코드 완전 제거 확인"으로 교체 — 상수 16개·메서드 5개·인스턴스 상태
    7개가 **존재하지 않는지**를 단언하고, 반대로 데이터 파이프라인과 theme_mgr은
    **살아있는지**를 단언). 3개 스위트 **448건 전원 통과**.

- 2026-08-02 (계속) — **`.history/` 삭제 + 신규 고아 모듈 배너**.
  - **`.history/` 삭제** (1,050 파일 / 28.4MB). VSCode 로컬 히스토리 확장이
    05-18~07-26에 쌓아둔 타임스탬프 사본으로, `.gitignore:15`에 등재돼 저장소엔
    영향이 없었지만 **grep·검색·AI 컨텍스트를 심하게 오염**시켰다(이 세션의
    1B/1L 조사에서 grep 결과 225KB가 통째로 이 쓰레기였다). 삭제 전 확인한 것:
    전 파일이 `이름_YYYYMMDDHHMMSS.확장자` 자동 사본(수동 생성 0개), git 추적
    0건, 코드 참조 0건(`core.history_fetcher`/`self.history` 히트는 전부 무관).
    프로젝트가 OneDrive 안이라 휴지통에서 30일 복구 가능.
    효과: 삭제된 옛 전략 심볼 검색 히트가 수백 건 -> `WallDetector` 17 /
    `Phase3Controller` 3 / `evaluate_phase2` 1건으로 줄어 눈으로 훑을 수 있게 됨.
  - **1B 삭제로 새로 고아가 된 2개에 DEPRECATED 배너**:
    `core/strategy/wall_detector.py`, `core/strategy/chemul_evaluator.py`.
    `phase1b_controller.py`의 배선이 사라지면서 이제 서로만 참조한다
    (chemul_evaluator -> wall_detector). 08-02 앞선 정리에서 쓴 것과 같은
    `[삭제 예정 / DEPRECATED]` 배너 형식을 그대로 따랐다(파일 삭제는 규칙 2로 보류).
  - **도달성 분석 재실행** (main.py 기준, 상대 임포트 포함):
    라이브 모듈 **34 -> 32개**. 정확히 위 2개만 줄어 앞선 세션의 분석과 일치.
    나머지 고아는 전부 `_legacy` / 배너 기부착(`api/kiwoom_api.py`,
    `order_queue.py`, `phase3_controller.py`) / 수동 도구 / `__init__.py`다.
    **미조치로 남긴 것 3개**(전부 오늘 변경과 무관한 기존 고아):
    `api/merge.py`(일회성 코드 병합 스크립트), `core/migrate_themes.py`
    (일회성 DB 마이그레이션), `models/account_info.py`(9줄짜리 미사용 클래스 —
    이름 때문에 '계좌 모델'로 오해할 여지는 있음).

- 2026-08-02 (계속) — **08-03 기동 전 동기화 감사** (1B/1L 삭제 후 '꼬임' 점검).
  기존 3개 스위트가 안 보는 축만 따로 감사했다 — 결과 **구멍 1건 발견·수정**.
  - **[발견·수정] `버스트 계산 실패`가 진단에서 "기타"로 뭉개지고 있었다.**
    `check_burst`의 예외 핸들러가 내는 사유인데 `_REJECT_RULES`에 규칙이 없어
    미분류였다. 이건 '시장에 신호가 없다'가 아니라 **우리 코드가 터졌다**는
    뜻이고, 발사 조건이 죽으면 매수가 0건이 된다. 진단 인프라의 목적(정상
    필터링 ↔ 코드 이상 분리)과 정면으로 어긋나는 지점이라 전용 분류
    `버스트 계산 실패(코드 이상)` 신설 + `_REJECT_INFRA`에 추가(인프라 경고로 승격).
    회귀 테스트로 못박음(dryrun 84 -> **86건**).
  - **[확인] 옛 전략 포지션 내성**: sub_strategy가 `1B`/`1L`/`알수없음`/`None`인
    포지션이 DB 복원·수동매수로 들어와도 익절캡 산출과 손절 청산이 정상 동작.
    1L도 이제 트레일링이 아니라 flat 캡(기본 2.5%)으로 청산된다.
  - **[확인] 지연 임포트 10개 심볼 전수 실존** — 봇은 안 죽고 기능만 조용히
    사라지는 부류(07-27 실제 장애 클래스). `daily_backtest`가 strategy_manager에서
    가져오는 15개 심볼도 전부 살아있다.
  - **[확인] 라이브 ↔ daily_backtest 상수 5종 동기화**, 삭제 심볼의 실행코드
    잔존 0건, 라이브 모듈 31개 개별 임포트 전부 성공, 전체 89개 .py 구문오류 0.
  - **[확인] 진입점**: `.bat` 비ASCII 0바이트 / `.ps1` UTF-8 BOM 있음 /
    junction 경로 정상 / 스케줄러 `AutoTrader_Start` 내일 08:59 Enabled /
    **배치가 쓰는 python(3.14.3)과 테스트에 쓴 python이 동일 실행파일**.
  - **[주의] 중복 실행 방지 장치가 없다** — `main.py`에 싱글톤 가드가 없어서
    이전 인스턴스가 살아있는 채로 08:59가 되면 두 개가 뜬다(토큰 충돌).
    지금은 `task_auto_shutdown`(15:40, 요일 무관)이 매일 스스로 꺼주므로
    실무상 문제가 없지만, **수동으로 봇을 띄웠다면 STOP_SIGNAL로 먼저 끌 것**.
    실제로 08-02 08:59에 뜬 인스턴스가 오후까지 살아있는 것을 이 감사에서 확인했다.

- 2026-08-02 (야간, 08-03 기동 전 최종 점검) — **원격제어 연속성 + 기동 인프라
  회귀 테스트 신설**. "중간중간 확인해도 막상 장 시작하면 오류로 재수정하는 일이
  대부분이었다"는 문제의식에서, 전략이 아니라 **기동 계층**을 처음으로 테스트로
  못박았다. 이 계층은 무인(비대화형) 실행이라 실패해도 화면에 아무도 없다 —
  대화형 수동 확인으로는 원리상 못 잡는 부류다(2026-07-28 교훈과 같은 뿌리).
  - **원격제어 세션이 매일 리셋되던 문제** — 08:59마다 새 대화로 시작해 전날
    맥락이 사라졌다. `start_remote_control.ps1`이 기본으로 `--continue`를 붙이도록
    변경(상세는 위 "세션 연속성" 항목). 한도가 걱정될 때만
    `NEW_SESSION_REQUESTED` 플래그로 1회 새로 시작한다.
  - **모바일 '종료' 클릭 후 방치되던 문제** — 세션이 끝나면 런처 창이 프롬프트에
    멈춰, PC 앞에 사람이 없으면 그날 내내 폰 접속이 끊겼다. 자동 재시작 루프
    추가(최대 30회, 5초 미만 즉시종료 3연속이면 설정 문제로 보고 중단).
    **매매 봇과는 완전히 별도 프로세스라 이 창에서 무슨 일이 나든 매매엔 영향 없음**
    (`start_trader.bat`이 `start`로 띄우고 `main.py`는 그 뒤 독립 실행).
  - `main.task_remote_control_watchdog` 신규(09:06 1회) — 프로세스 부재 시 텔레그램
    알림. 블로킹 확인은 `asyncio.to_thread`로 분리(실측 0.67초), 확인 자체가
    실패하면 오탐 방지를 위해 '정상'으로 간주한다.
  - **[발견·조치] 고아 `STOP_SIGNAL`이 실제로 남아 있었다**(당일 20:57 생성).
    이중 방어(배치파일 `del` + `setup()`의 낡은 신호 정리)가 이미 있어 기동은
    막히지 않았겠지만, 봇 미실행을 확인하고 삭제해 삼중으로 안전하게 만들었다.
    **수동으로 봇을 끈 날은 이 파일이 남는다는 것을 기억할 것.**
  - **[확인] 내일 중복 실행 위험 없음** — 점검 시점에 python 프로세스 0개
    (08-02와 달리 봇이 살아있지 않음).
  - **[확인] `config.ini` ↔ 코드 정합성** — 조건검색식 4개가 올바르게 분리
    등록돼 있다(`CONDITION_NAMES` 3개 + `CLOSING_BET_CONDITION_NAME` 분리).
    이게 어긋나면 에러 없이 조용히 구독이 빠지는 부류라 매번 확인할 것.
  - **[확인] 배선 감사** — 정의된 태스크 20개 = gather 등록 20개(불일치 0),
    모든 태스크가 루프 안에 try/except 보유, 지연 import 심볼 10개 전부 실존,
    전체 90개 .py 구문오류 0건.
  - **[인지, 미조치] 5분 이상 긴 WS 단절 시 `_process_initial_snapshot`이 장중에
    호출된다**(main.py 재연결 복구 경로). `_wait_for`가 소켓을 직접 recv 하므로
    이론상 체결틱을 흘릴 수 있으나, 이미 끊겼다 붙은 상황에서 유니버스를 복원하는
    트레이드오프가 명확한 기존 동작이라 손대지 않았다.
  - [검증] `test_live_dryrun_20260803.py`에 **섹션 19(기동 인프라)** 신설,
    86 -> **104건**. 원격제어 배선/비차단/폴백, `--continue` 기본값, 플래그
    1회성, `.ps1` BOM, `.bat` 순수 ASCII, junction 참조, 고아 신호 부재까지
    검사한다. 4개 스위트 **492건 전원 통과**.

- 2026-08-03 (월, **틱 구동 첫 실거래일**) — 오전 관측 + 오후 **익절 로직 수술**.

  [기동·인프라 — 전부 정상]
  - 08:59:03 자동 기동, 조건검색식 4개 정상 등록(주도주상위=1 / 돌파자동매매용=2 /
    눌림목자동=3 / **종가베팅=4는 매매 라우팅 제외**로 분리). 어제 만든 분리 구독이
    설계대로 동작했다. 참고로 seq는 **4**였다 — 08-02에 HTS 화면 [005]를 보고
    "seq=6"으로 추정했던 건 틀렸고, 코드가 번호가 아니라 **이름으로 찾기 때문에**
    아무 문제가 없었다(이름 기반 설계가 옳았음이 증명된 셈).
  - **FID 228 수신 정상**(09:05 워치독, 체결틱 5,181건). 이번 개편의 유일한 미검증
    가정이었고 틀렸으면 하루 매수 0건이었다. **매도 시장가도 정상 체결**(폴백 로그 없음).
  - **실시간 편입이 장중 내내 살아있었다** — 08-01에 고친 `type='02'` 파싱 효과.
    seq별 편입: 주도주상위 42 / 눌림목자동 92 / 종가베팅 254 / **돌파자동매매용 0**.
    돌파는 구독은 정상인데 HTS 조건식이 하루 종일 한 종목도 못 잡았다(코드 무관,
    사용자가 검색식이 과하게 빡빡하다고 판단 — 추가 조치 없음).
  - 14:50 종가베팅 스캔 정상(유니버스 4종목 -> 적격 0종목 -> "후보 없음").
    분봉 캐시 48파일/5.9MB가 실제로 쓰였다.
  - 429는 5건뿐(07-30 하루 2,469건 대비). 틱 구동이 REST를 거의 안 쓰는 효과.

  [눌림목이 안 사지는 이유 — 대금이 아니라 무장이 문제]
  - 눌림목 단독 3종목(039980/365660/059120)은 **대금 판정까지 가지도 못했다**.
    게이트가 `무장 -> 시간창 -> 대금` 순인데 첫 관문(강도 100·3초)에서 멈췄다.
    039980은 10회 무장했지만 **전부 09:01~09:03**이라 Pullback 창(09:25) 밖이었고,
    창이 열린 뒤엔 무장 0회. 365660/059120은 하루 종일 무장 0회.
  - **`PULLBACK_START=09:25`의 근거가 이미 소멸했다.** 09:25로 늦춘 이유는
    "개장 직후엔 당일 고가가 없어 눌림 판정이 성립 안 함"이었는데, 08-02에
    그 판정(분봉 재검증)을 코드에서 **통째로 없앴다**. 지금 눌림 판정은 전부
    HTS(3분봉 30이평·20봉내 25% 급등)가 하므로 09:00에도 성립한다.
    -> 시간창을 앞당기는 것을 검토할 것(이번엔 미적용, 익절 수술과 섞지 않기 위해).
  - **강도 100 자체는 내리지 말 것.** 100은 "매수체결 = 매도체결"이라 그 아래는
    매도 우위에서 사는 것이다. 07-30에 1B(하락 시 진입)가 실데이터 37건에서
    우위 없음으로 판명된 것과 같은 함정.

  [실적 — 이기는 매매를 하고도 수수료에 먹혔다]
  - 청산 16건, 가격기준 합계 +2.58%(평균 +0.16%). **왕복수수료 0.23%×16=3.68%를
    빼면 순 -1.10%.** 회전이 수익을 다 먹은 하루였다.
  - 유형별: 동적캡 즉시매도 **6건**(평균 +0.35%, 보유 **2.4분**) / 슬롯교체 3건
    (-1.76%) / 조기확정 2건(+1.56%) / 정체정리 2건(+0.60%) / 지수급락 1(+1.76%) /
    익절캡 1(+2.76%) / 손절 1(-3.11%).

  [🔴 발견한 로직 결함 3가지 — 파라미터가 아니라 버그]
  - **① `cap_exit`가 모든 1A에서 항상 참이었다.** 조건이
    `cap >= TP_CAP_UPGRADED`인데 1A 기본캡(0.025)과 TP_CAP_UPGRADED(0.025)가
    **같은 값**이라, 원래 의도("상향된 종목만")와 달리 1A는 **매수 직후
    (워밍업 60초 종료 시점)부터** 동적캡 즉시매도 대상이었다. 실제로 6건이
    75~250초 만에 잘렸다. -> `pos["tp_cap_upgraded"]` 플래그 기반으로 교체.
  - **② `entry_strength`가 진입 순간의 스파이크였다.** 진입은 대량체결 버스트가
    터지는 그 순간이라, 그때 캡처한 `compute_strength`(최근 10초 창)에는 버스트가
    통째로 들어가 거의 항상 국소 최고점이다. 오늘 **042700/090710/010170 세 건이
    정확히 300**(compute_strength 상한)으로 포화돼 있었다. 포화된 최고점을 기준으로
    `현재 < 기준×0.8`을 재면 **정상으로 돌아오기만 해도 '하락'**이 된다 —
    하락 판정이 구조적으로 필연이었다. **08-02에 고친 `avg_trade_value`의
    자기모순과 정확히 같은 종류**(그때도 지금 터지는 버스트가 분모에 섞였다).
    -> `_strength_baseline` / `_maybe_anchor_strength_baseline` 신규: 워밍업 종료
    후 처음 관측되는 안정화된 강도로 **리앵커**. 리앵커 실패 시 옛 스파이크로
    폴백하지 않고 **판단 보류**(폴백하면 수정이 무의미해진다).
  - **③ '익절' 로직이 손실 구간에서 발동했다** — -0.99% / -0.48% / -0.43%를
    "동적캡 즉시매도"라는 이름으로 실현시켰다. -> cap_exit 경로에 순이익>0 가드.
    손실 구간은 손절(-3%)과 손실반등(경로 B)이 담당한다.

  [🔧 익절 구조 변경 — '조기확정' -> '본전스톱' + 캡 상향]
  - **조기확정 폐지.** 순 +1.0% 도달 시 강도 미상승이면 즉시 매도하던 분기를
    제거했다. 오늘 2건이 **전부 매도 후 더 올랐다**(037070은 매도가 대비 **+12.0%**,
    439960은 **+3.4%** — 후자는 우리 봇이 17,040에 되사서 17,350에 판 실제 체결가로 확인).
  - **본전스톱 신설**(`BREAKEVEN_STOP_ENABLED/TRIGGER/FLOOR`): 같은 지점에서 팔지 않고
    **손절선을 본전으로 끌어올린다**. 하방을 먼저 닫으므로 캡을 올려도 리스크가
    늘지 않는다(최악 = 왕복수수료 0.23%).
  - **캡 상향**: 1A 2.5% -> **4.0%** / 눌림·개장초반 1.5% -> **2.5%** /
    상향목표 2.5% -> **6.0%**(`TP_CAP_UPGRADED_MAX` 신규). 기존 `TP_CAP_UPGRADED`는
    **[미사용]** 배너 — 이 값이 기본캡과 같았던 것이 결함 ①의 뿌리다.
  - **왜 트레일링이 아닌가**: 037070 실제 틱 재생 결과 고점 대비 **-0.5~1.0%
    트레일링은 현행보다 나빴다**(+0.72% vs +0.86%). 매수 직후 본격 상승 전에
    **-2.72%까지 밀리는 흔들림**이 있어 좁은 트레일은 전부 털린다. 의미 있는
    개선은 -3.0% 트레일(+9.44%)부터인데 하루치 1종목으로 정하기엔 폭이 커서,
    결과가 예측 가능한 **캡 + 본전스톱** 조합을 먼저 쓴다.
  - 037070 틱 재생 요약(매수 7,340 / 09:15까지 고점 8,310 = +13.22%):
    현행 조기확정 순 **+0.86%** / 본전스톱+캡4% **+3.86%** / 캡6% **+5.90%** /
    본전스톱+4%부터 트레일-3% **+9.44%**.
  - `daily_backtest._exit_signal`에도 본전스톱을 같이 넣어 라이브와 동기화.

  [정정 — 과잉 일반화했던 것]
  - "조기확정이 추세를 세 조각으로 잘라 손해"라고 했으나 **전수로는 사실이 아니었다**.
    회전 손익: 439960 **-1.13%** / 222800 **-1.64%** / 010170 **+5.19%**(더 싸게
    되사서 이득) -> 합계 **+2.42%로 오늘은 회전이 도움**이 됐다. 문제는 회전 자체가
    아니라 **큰 추세를 조기에 놓치는 것**이다.

  [한계 — 반드시 같이 기억할 것]
  - 틱 재생은 **037070 한 종목의 09:00~09:15**가 전부다(다른 종목은 아카이브에 없거나
    매매 시각이 아카이브 범위 밖). **숫자를 기대값으로 보면 안 되고**, "좁은 트레일은
    역효과" / "본전스톱이 하방을 닫는다"는 **구조적 결론만** 유효하다.
  - 틱 아카이브 유니버스가 09:20 시점 감시 종목 기준이라 **정작 눌림목 단독 종목이
    빠졌다**. 검증하고 싶은 대상이 수집되지 않는 구조라 선정 기준 손볼 것.
  - [검증] `test_patch_20260803.py` **32건 신설**(캡 상향/본전스톱/결함 ①②③/청산
    우선순위/백테스트 동기화). 5개 스위트 **524건 전원 통과**.
  - ⚠️ **이 변경은 08-04 08:59 재기동부터 적용된다** — 오늘 돌고 있는 프로세스는
    이미 메모리에 로드된 옛 코드를 계속 쓴다.

  [🔴 손절 구멍 — 워밍업 60초 동안 무방비였다]
  - `on_price_update`의 **워밍업 return이 손절 판정보다 위**에 있어서, 매수 직후
    60초는 얼마가 빠지든 아무 대응이 없었다. 가격 폴백 태스크(30초 주기)도 결국
    같은 함수를 부르므로 **우회 경로도 없었다**. 워밍업의 목적은 '체결강도·거래량
    처럼 매수 직후 흔들리는 지표로 성급히 판단하지 않는 것'이지 **가격 기반 하드
    손절까지 멈추는 것이 아니다** — 급락은 매수 직후일수록 위험한데 정확히 그
    구간이 비어 있었다. -> 손절 판정을 워밍업 게이트 **위**로 올렸다.
    본전스톱·익절캡·정체정리 등 나머지는 워밍업을 그대로 존중한다.
  - 손절 경로 전수 점검 결과 나머지는 정상: 주 경로는 `on_trade`가 보유 종목마다
    매 틱 `on_price_update`를 부르고, 틱이 끊겨도 30초 폴백이 받는다.
    `sell_blocked`는 매도 **연속 실패** 시에만 설정되며 그때는 포지션을 holdings
    에서 빼고 알림까지 보내는 종착 상태라 손절 누락이 아니다.

  [🔴 시간대 계수 — 완화 방향이 거꾸로였다 (우선순위 1 적용)]
  - 08-03 청산 16건을 계수별로 갈라보니 **완화 폭이 클수록 성과가 나빴다**:
    계수 1.00(7건) **+6.56%** / 0.85(3건) -0.60% / **0.65(3건) -5.29%** / 0.80(3건) +1.91%.
    그날 손실 청산 4건이 전부 11:23~12:56에 몰렸고, 오전에 번 +6.56%를 점심
    -5.29%가 거의 다 지웠다.
  - 원래 의도("안 낮추면 오후가 통째로 죽는다")는 거래를 유지하려던 것인데, 실제로는
    **유동성이 가장 마른 시간에 가장 낮은 문턱으로 들어가는** 구조였다. 점심에
    신호가 없는 건 큰 손이 없다는 뜻이지 문턱을 깎을 이유가 아니다.
    -> **점심 계수 0.65 -> 1.00**(완화 제거). 나머지 0.85/0.80/0.95는 오늘 표본으로
    판단이 갈려(0.85 손실 / 0.80 이익) 그대로 뒀다. 며칠 더 쌓아 재검증할 것.
  - 손실 4건 진입 사유 원문(참고): 059120 상대경로·강도 144.7초·비중x3.00(당일 최대
    금액 603만원)인데 -3.11% 손절 / 319400 절대경로 **1,950만원**(=3,000만x0.65) /
    090710 상대경로·평균1틱 41만원(저유동) / 336260 단일 1.00억.
    이 중 **319400은 계수 완화가 없었다면 진입 자체가 안 됐다.**

  [자동종료 — 고정 시각 -> 할 일 끝나면 즉시]
  - 마무리 3종(14:50 종가베팅 / 15:10 강제청산 / 15:30 백테스트) 완료 플래그를
    두고 **셋 다 끝나면 즉시 종료**한다. 08-03엔 15:10 청산 후에도 테마 재계산과
    조건검색 수신이 계속 돌았다.
  - ⚠️ **종가베팅(14:50)만으로 내리면 안 된다** — 뒤에 강제청산(15:10)이 있어서
    먼저 종료하면 보유가 오버나이트로 넘어간다. 그래서 조건이 AND다.
    보유가 남아 있으면 완료로 치지 않고 15:40 하드 폴백까지 살려 둔다.
    강제청산 완료 판정은 트리거 블록 **밖**에 둬야 한다 — 안에 두면 하루 1회만
    평가돼, 그 순간 매도가 pending이면 플래그를 영영 못 세운다.
  - 종가베팅·백테스트는 **실패해도 완료 처리**한다(`finally`). 부가 기능 하나가
    실패했다고 봇이 밤새 도는 옛 문제로 되돌아가면 안 된다.

  [🟡 테스트가 실거래 로그를 오염시켰다 — 재발 방지 완료]
  - 장중에 테스트를 돌렸더니 `logs/autotrader.log`에 가짜 매매가 섞였다
    (`BUY [S03]`, `SELL [000006] 손절 -4.00%` 등 56줄+). 봇 동작과 DB에는 영향이
    없지만(테스트는 Repository를 스텁 처리) **그날을 로그로 분석할 때 실거래와
    구분이 안 된다** — 실제로 "마지막 매수는?"에 테스트 종목이 나왔다.
    오염량이 커서 로그 로테이션까지 유발됐다(autotrader.log.1 생성).
  - -> `utils/logger.py`가 `AUTOTRADER_TEST_LOG=1`이면 **`logs/test_run.log`**에
    쓰도록 분리하고, 5개 테스트 스위트 최상단(core/main 임포트보다 **먼저**)에서
    이 환경변수를 세팅한다. 검증: 테스트 전수 실행 후 `autotrader.log` 크기 불변.
  - **교훈: 장중에 테스트를 돌리려면 로그부터 분리할 것.** DB는 스텁이라 안전했지만
    로그는 실제 파일 핸들을 공유하고 있었다.

  [08-03 종료 경위 — 백테스트는 실행되지 않았다]
  - 15:29:40에 `STOP_SIGNAL` 감지로 정상 종료(15:29:42 "안녕히"). 15:30 일일
    백테스트는 **20초 차이로 실행되지 못했다.** 수동 종료는 모든 것에 우선하는
    설계라 오작동이 아니다. 다만 그날 리포트가 없다는 점은 기억할 것.

  [🟡 종목명이 전부 "조건검색"으로 찍히던 문제 — 표시 전용, 매매 무관]
  - 08-03 텔레그램 '매수 체결' 3건 모두 `종목: 조건검색 (037070)`처럼 나왔다.
    원인은 `main._extract_stock_name`의 후보 키에 `'name'`이 들어 있던 것.
    키움 **실시간 편입 push(type='02')의 최상위 'name'은 종목명이 아니라
    실시간 타입 라벨("조건검색")**이고, 그 페이로드엔 종목명이 아예 없다:
    `{'type':'02','name':'조건검색','item':'079650','values':{...}}`
    그런데 'name'을 후보로 두면 "찾았다"고 판단해버려, 호출부의
    `if stock_name == stock_code:` **REST 폴백이 영영 실행되지 않았다.**
    기동 스냅샷(CNSRREQ)은 '302'에 진짜 이름이 있어 정상이었고, 08-01에 실시간
    편입 파싱을 되살리면서 이 경로가 처음 살아나 드러난 부작용이다.
  - -> 후보 키에서 `'name'`만 제거. 그러면 stock_code가 반환되고 기존 REST
    폴백이 살아난다. **REST 부담 없음** — `order_manager.get_stock_name()`이
    `_name_cache`로 캐싱해 종목당 평생 1콜이다(039980이 42회 편입돼도 1콜).
  - 매매 판정·주문·손익에는 전혀 영향이 없던 **표시 전용** 문제다.

  - [검증] `test_patch_20260803.py` 32 -> **57건**(손절 워밍업/시간계수/자동종료/
    종목명 추출 섹션 추가). 시간계수 변경으로 옛 사양을 검증하던 5건이 정확히 실패했고 전부
    새 사양으로 교체했다 — 테스트가 사양 변경을 잡아낸 정상 동작이다.
    5개 스위트 **552건 전원 통과**.

- 2026-08-03 (계속, 장마감 후) — **2차 수술: 진입 완화 + 청산 실험 + 전면 재검증**.
  오전 수술(익절/손절/시간계수)에 이어 사용자 지정으로 4건을 더 바꿨다.
  **모의투자이므로 리스크를 실험해본다**는 방침이 배경이다.

  [변경 4건]
  - **Pullback 시간창 09:25 -> 09:00** (1A와 완전 동일). 09:25의 근거였던
    "개장 직후엔 당일 고가가 없어 눌림 판정 불가"는 **08-02에 분봉 재검증을
    폐지하면서 이미 소멸**했다 — 지금 눌림 판정은 전부 HTS(3분봉 30이평 +
    20봉내 25% 급등)가 하고 여러 날 봉을 보므로 09:00에도 성립한다.
    ※ 사용자는 09:05를 제안했으나 **실측으로 09:00을 권고**했다: 눌림목 단독
      3종목 중 유일하게 무장한 039980의 무장 10회가 전부 **09:01:47~09:03:16**
      이라 09:05로는 잡히는 게 0건이었다. 나머지 둘은 무장 0회(365660) /
      10:34부터라 기존 창 안(059120)이었다.
    ※ 슬롯 경합 없음 확인 — 08-03 동시보유 최대 2종목(상한 6).
  - **무장 3.0초 -> 2.0초**. ⚠️ **백테스트로 검증한 값이 아니다** — FID228
    이력이 남지 않아 과거 재현이 원리적으로 불가능하다(틱 아카이브엔 시각·
    가격·거래량만 있음). 간접 근거는 08-03 16건의 '매수 시점 지속시간 vs 성과'
    (3~5초 1건 +1.76% / 5~20초 4건 +0.13% / 20초+ 11건 +0.03%)뿐인데 가장 짧은
    구간이 1건이라 약하다. **실험적 완화**임을 명확히 인지할 것.
  - **본전스톱 OFF**(`BREAKEVEN_STOP_ENABLED=False`). 도입 당일 저녁에 08-03
    실제 틱으로 재생해보니 개장 구간(09:00~09:10, 진입 16건)에서 오히려
    손해였다: **끔 -0.177% / +1.0% -0.325% / +1.5% -0.436% / +2.0% -0.253%**.
    변동이 커서 +1% 찍고 무장한 뒤 되돌려 본전에 털리는 일이 잦고, 캡까지
    갈 수 있었던 포지션을 0%로 만든다. 단 이 시뮬레이션의 진입은 **버스트만으로
    생성**한 것이라 무장 게이트가 빠져 실제보다 품질이 낮다 — 수치를 그대로
    믿을 수는 없다. **로직은 보존**했고 플래그만 False다.
  - **개장초반 익절캡 2.5%는 유지**(백테스트가 지지). 같은 재생에서
    1.5% -0.339 / **2.5% -0.325** / 4.0% -0.638 / 6.0% -0.447 / 캡없음 -0.973
    으로 **캡을 풀수록 나빴다**. 개장 구간은 16건 중 손절이 6건일 만큼 험하다.

  [슬롯 — 유지 결정, 근거 정정 포함]
  - ⚠️ **자본 오인 정정**: 분석 도중 "자본 1,000만원"으로 봤던 값은 **테스트
    스텁 값**이었다. 실제는 09:00:06 기록 기준 **497,017,161원**. 6슬롯을 꽉
    채워도 자본의 5.4%라 **자본은 병목이 아니고 슬롯이 진짜 상한**이다.
  - 그럼에도 **유지**를 권고했다: ① 08-03 동시보유 최대 2/6, "슬롯 없음" 탈락
    0건 ② 내일 진입 증가 요인 4개(창 확대·무장 완화·돌파검색식 가동·본전스톱
    OFF로 보유 연장)를 다 합쳐도 3배가 돼야 6 ③ **전략이 아직 순 -1.10%**라
    노출 확대는 손실 확대 ④ 이미 변경이 6개 겹쳐 슬롯까지 바꾸면 원인 분리 불가.
  - 늘릴 근거는 **진단 알림의 `1A 슬롯 없음`이 뜨기 시작할 때** 생긴다.

  [전면 재검증 — "작은 오류도 없게"]
  - **daily_backtest 실제 실행 검증**(가장 위험했던 미검증 경로). 08-03 15:30
    리포트는 STOP_SIGNAL 종료로 **한 번도 안 돌았다** — 새 상수(BREAKEVEN_*,
    캡 4.0/2.5/6.0)를 임포트하는데 검증된 적이 없었다. 봇이 꺼진 상태에서
    수동 실행 -> **11건 재현, 예외 0**.
  - **상수 정합성 불변식 30개를 테스트로 고정**(dryrun 섹션 20). 08-03 결함 ①
    (1A 기본캡 == TP_CAP_UPGRADED라 cap_exit이 항상 참)이 정확히 "값 하나를
    고치고 짝을 안 고친" 부류라, 시간창/캡/틱/슬롯/백테스트동기 5축의 모순을
    상수 수준에서 잡는다. 예: `기본캡 != 상향캡`, `무장시간 < 무장TTL`,
    `시간계수 전부 <= 1.0`, `모든 캡 > 왕복수수료`.
  - **설정 조합 통합 시뮬레이션 신설**(dryrun 섹션 21) — 바뀐 값들을 개별이
    아니라 **동시에** 태운다: 09:02 눌림목이 2.0초 무장+버스트로 매수 ->
    개장초반 캡 2.5% 적용 -> 본전스톱 OFF라 +1% 후 되돌려도 보유 유지 ->
    워밍업 중 손절 작동 -> 점심 문턱 3천만원 -> 11:00 매수는 기본캡 4.0%.
  - 배선 감사: 태스크 정의 20 = gather 등록 20, 전 태스크 루프 내 try/except,
    지연 import 심볼 10개 실존, 91개 .py 구문오류 0.
  - 낡은 주석 정리: 라이브·테스트의 "3초" 하드코딩 표기를 상수 참조 표현으로
    교체(`_legacy`/DEPRECATED 파일은 당시 기록이므로 보존).

  [🛡 지수 하락 가드 신규 (-3%, 11:00~) — 사용자 지정]
  - 기존 SEVERE_CRASH(-5%)보다 **한 단계 얕은** 방어선. 둘은 공존한다.
    판정은 **코스피/코스닥 중 나쁜 쪽**(둘 중 하나라도 -3% 이하면 발동).
  - 발동 조건: **11:00 이후** + 지수 -3% 이하. 개장 직후의 일시적 급락에
    과민 반응하지 않기 위해 11시부터 본다(11시까지 회복 못 한 하락은 그날
    추세일 가능성이 높다는 판단).
  - 효과 3단:
    ① **신규매수 즉시 중단** (`can_buy_more` + `_execute_buy` 이중 가드)
    ② **11:30까지** 순 0%(본전) 이상인 보유분은 청산. 손실 중인 건 안 판다 —
       지수 급락 중 저점 매도가 되므로 회복 기회를 준다.
    ③ **14:50** 남은 보유분(본전 미만)은 손익 무관 강제청산. 정규 강제청산
       (15:10)보다 앞당겨 약세장 오버나이트 리스크를 줄인다.
  - 청산 우선순위: **손절(-3%) > 지수 가드 > 익절캡/정체정리**. 워밍업보다도
    앞이다(가드는 가격 기반이라 강도·거래량처럼 흔들리는 지표를 안 쓴다).
  - REST 추가 부담 **0** — `_refresh_market_rates()`의 60초 캐시를 재사용한다
    (이미 `_is_severe_crash`가 매 가격갱신마다 부르고 있다). 지수 조회 실패 시
    캐시 직전값이 쓰이고 초기값이 0.0이라 **데이터가 없으면 발동하지 않는다**
    (보수적 = 매매를 막지 않음).
  - 진단 분류 `지수 하락 가드(-3%)` 신설 — 없으면 "기타"로 뭉개져 장중에
    "매수 0건"의 원인을 판단할 수 없다(08-02 '버스트 계산 실패'와 같은 교훈).
  - ⚠️ **해석 하나 확인 필요**: "11:30까지 본전 이상이면 청산"을 문자 그대로
    구현해서, **11:30 이후에 본전을 회복한 종목은 팔지 않고 14:50까지 들고
    간다**. 회복 즉시 파는 게 나으면 `INDEX_GUARD_BREAKEVEN_UNTIL`을
    14:50으로 두면 된다(상수 한 줄).

  [죽은 코드 배너 — 이름 때문에 생기는 오해 차단]
  - `PHASE1A_LEADING_SUSTAIN_SEC = 3`과 `evaluate_1a_leading_strength()`에
    **[미사용] / [DEPRECATED] 배너**를 달았다. "1A 매수도 2초로 바뀌었나"를
    확인하다 발견 — 이름이 `PHASE1A_...`라 1A만 3초로 묶는 것처럼 보이지만,
    그 함수는 08-02 틱 구동 전환 때 호출부가 교체되어 **라이브 호출 0건**이다
    (테스트만 호출). 실제 무장은 `TICK_STRENGTH_SUSTAIN_SEC` 하나가 담당하고,
    그 게이트는 **전략 라우팅보다 앞**이라 1A/Pullback에 공통 적용된다.
  - 실측 확인: 주도주상위/돌파자동매매용/눌림목자동 **세 경로 모두** 2.1초에
    무장 -> 매수 성립. 상수를 3.0으로 되돌리면 같은 입력에서 매수가 안 되는
    **반증**까지 확인했다.

  [검증] 5개 스위트 **622건 전원 통과**
  (283 / 86 / 59->84 / 145 / 24). 테스트 로그 분리가 작동해
  전수 실행 후에도 `autotrader.log` 크기 불변(799,833바이트) 확인.

\## 세션 이관 체크리스트 (2026-08-01 신설)

새 세션(PC 터미널 / 모바일 원격 / 새 대화창 어디서든)이 이 프로젝트를
이어받을 때 쓰는 절차. **읽는 순서가 중요하다.**

\### 이관받는 쪽이 읽을 순서
1. `\## 기본 정보` + `\## 개발/운영 환경` — 무엇을 어디서 어떻게 돌리는지
2. `\## 작업 방식` — 지켜야 할 규칙 7가지 (surgical 패치, 파일 삭제 금지 등)
3. `\## 현재 전략 요약` — **지금 코드가 실제로 하는 일**(과거 히스토리보다 이걸 먼저)
4. `\## 다음 할 일` — 08-03 확인 항목과 이월 과제
5. `\## 히스토리`는 필요할 때만 역순으로. **분량이 많으니 통독하지 말 것** —
   "왜 이렇게 됐는지"가 궁금할 때 해당 날짜만 찾아본다.

\### PC 검증 항목 (코드가 성한지 — 이관 직후 반드시 실행)
프로젝트 폴더에서 순서대로 실행하고, 하나라도 실패하면 그 지점부터 원인 추적.

| # | 명령 | 기대 결과 |
|---|---|---|
| 1 | `python -c "import main; print('OK')"` | `OK` (임포트 체인 전체 정상) |
| 2 | `python test_patch_20260801.py` | **283건 전원 통과 / 실패 0건** |
| 3 | `python test_patch_20260802.py` | **86건 전원 통과 / 실패 0건** (틱 구동) |
| 4 | `python test_patch_20260803.py` | **84건 전원 통과** (익절/손절/시간계수/자동종료/종목명/지수가드) |
| 5 | `python test_live_dryrun_20260803.py` | **145건 전원 통과** (종일 통합 + 기동 인프라 + 상수 불변식 + 설정조합) |
| 6 | `python test_closing_bet_routing.py` | **24건 전원 통과** (종가베팅 분리 라우팅) |
| 7 | `git log --oneline -5` | 최신 커밋이 `2fa4a2d` 이후인지 |
| 8 | `git status --short` | 비어 있어야 정상(작업 중이면 예외) |

**합계 622건**(283 + 86 + 84 + 145 + 24). 하나라도 실패하면 그 지점부터 원인 추적.

- ⚠️ **4번(통합)이 가장 중요하다.** 2·3번이 전부 통과하는 상태에서도 08-03에
  매매를 반쪽 낼 버그 6건이 살아있었다(2026-08-02 히스토리 참고) — 유닛은
  부품을 보고 통합은 배선을 본다. **전략을 바꾸면 반드시 4번까지 돌릴 것.**

- ⚠️ 2번은 Windows 콘솔 기본 코드페이지(cp949)에서 유니코드 출력이 깨져
  `UnicodeEncodeError`가 난다. **`PYTHONIOENCODING=utf-8`을 앞에 붙여 실행**할 것
  (Git Bash: `PYTHONIOENCODING=utf-8 python test_patch_20260801.py`).
- 기존 `test_phase1b_controller.py` / `test_microstructure.py`는 **실패하는 게
  정상**이다(5월 작성 스테일 테스트). 회귀로 오해하지 말 것 —
  확인하려면 `git stash` 후 재실행해서 HEAD에서도 같은 실패가 나는지 보면 된다.

설정값이 의도대로인지 한 번에 보는 명령:
```
python -c "import core.strategy_manager as s; from core.order_manager import FORCE_CLOSE_TIME; print('1A', s.GROUP_A_START, '~', s.PHASE1A_END); print('PB', s.PULLBACK_START, '~', s.PULLBACK_END); print('중복전환', s.DUAL_SOURCE_PULLBACK_FROM); print('청산', FORCE_CLOSE_TIME); print('슬롯', s.PHASE1A_MAX_SLOTS, s.PULLBACK_MAX_SLOTS, s.MAX_HOLDINGS, s.MAX_HOLDINGS_HARD); print('무장', s.TICK_STRENGTH_MIN, s.TICK_STRENGTH_SUSTAIN_SEC); print('캡', s.TAKE_PROFIT_CAP, s.TAKE_PROFIT_CAP_PULLBACK, s.TAKE_PROFIT_CAP_EARLY, s.TP_CAP_UPGRADED_MAX); print('본전스톱', s.BREAKEVEN_STOP_ENABLED); print('점심계수', dict(s.TICK_BURST_TIME_MULT)[(11,30)]); print('1B잔재', hasattr(s, 'PHASE1B_ENABLED')); print('틱구동', s.TICK_ENTRY_ENABLED)"
```
기대값(2026-08-03 마감 기준):
`1A 09:00~14:50` / `PB 09:00~14:50` / `중복전환 10:30` / `청산 15:10` /
`슬롯 4 4 6 8` / `무장 100.0 2.0` / `캡 0.04 0.025 0.025 0.06` /
`본전스톱 False` / `점심계수 1.0` / `1B잔재 False` / `틱구동 True`
(`1B잔재 True`가 나오면 1B 코드가 되살아난 것 — 2026-08-02에 전부 삭제했다.)

\### 모바일 확인 항목 (원격 세션이 제대로 붙었는지)
폰에서 세션에 들어간 뒤 아래를 확인한다. **PC 검증과 목적이 다르다** —
여긴 "연결과 맥락"을, PC 검증은 "코드 건강"을 본다.

1. **세션 카드에 `🖥 연결됨 · jokings76/AutoTrader_Project`가 보이는지**
   (안 보이면 PC 터미널 창이 닫혔거나 네트워크가 끊긴 것)
2. **작업 폴더가 맞는지** — 세션에 이렇게 물어본다:
   ```
   지금 작업 폴더가 어디야? 파일 목록 보여줘
   ```
   `main.py`, `core/`, `CLAUDE.md`가 나오면 정상.
3. **맥락을 이어받았는지** — 이렇게 물어본다:
   ```
   CLAUDE.md 읽고 현재 전략 구성이랑 08-03 확인 항목 요약해줘
   ```
   1A/Pullback 2전략 체제, 1B/1L 삭제됨, 09:25 Pullback 시작, 10:30 중복 전환,
   15:10 청산이 나오면 이관 성공. 안 나오면 이 문서를 다시 손봐야 한다.
4. **모델 확인** — 원격 세션은 Sonnet 5로 시작한다. 전략/로직 작업이면
   `/model`로 **Opus 5**로 올리고 시작할 것.
5. **장중 주의** — 봇이 돌고 있는 09:00~15:10에는 매매 로직 파일 수정 금지.
   로그 조회·상태 확인·진단 알림 해석 정도만 하고, 코드 수정은 장 마감 후.

\### 이관이 끝났다고 판단하는 기준
- PC 검증 4항목 전부 통과 + 설정값 출력이 기대값과 일치
- 모바일 확인 1~3번 통과(특히 3번 — 맥락 복원이 이관의 본질)
- 위 둘 중 하나라도 실패하면 **아직 이관 안 된 것**이다.

\## 현재 전략 요약 (2026-08-02 기준, 08-03 실거래 반영 예정)

**두 전략이 같은 트리거를 쓴다.** 조건검색식이 '어떤 종목인가'를 전부 판정하고
(3분봉 30이평 근접·눌림폭·급등이력·유동성 100억+), 코드는 '지금이 그 순간인가'만
본다 — 즉 코드가 보는 건 **체결강도와 거래대금 둘뿐**이다. 전략 차이는 유니버스와
슬롯·상한·익절캡·시간창에만 있다.

| 항목 | 1A | Pullback |
|---|---|---|
| 조건검색식 | 주도주상위, 돌파자동매매용 | **눌림목자동 전용** |
| 시간창 | 09:00 ~ 14:50 | **09:00 ~ 14:50** (2026-08-03: 1A와 완전 동일) |
| 진입 조건 | **무장**(체결강도 FID228 100+ **2초 연속**) → **발사**(대량체결 버스트) | **1A와 동일** |
| 등락률 상한(전일종가) | **16%** | **10%** |
| 시가대비 급등 보류 | +5% | 미적용(되돌림을 사는 전략) |
| 슬롯 | 4 | 4 (공유 상한 6, 확장 8) |
| 중복 편입 시 | ~10:30 | 10:30~ |
| 익절 캡 | **4.0%** (개장초반 2.5%) | **2.5%** |
| 상향 캡(강도상승/주도테마) | **6.0%** | 6.0% |
| 손절 | -3% (**워밍업 중에도 작동**) | 동일 |
| 본전스톱 | **OFF** (로직 보존, 플래그만 False) | 동일 |

- **틱 구동 (2026-08-02 전면 전환)** — 진입 평가가 `on_trade`(체결 틱 콜백)에서
  일어난다. 조건이 차는 **그 틱에서** 발화하며 REST 0콜이다.
  - **무장** = 키움 공식 체결강도(FID 228)가 100 이상으로 **3초 연속** 유지.
    자체 `compute_strength`를 안 쓰는 이유: 틱이 부족하면 정확히 100.0을
    반환하는데 임계값도 100이라 **데이터가 없을수록 쉽게 뚫린다**. 228은
    다윤님이 HTS에서 보는 값과 동일하고 매 틱에 이미 실려 온다.
  - **발사** = 대량체결 버스트 3경로 OR (창 5초):
    ① 3,000만원 × 시간계수 이상이 **2건** ② 1억원 × 시간계수 이상이 1건
    ③ max(1,000만, **평균 1틱 × 20배**) 이상이 2건 ← 자기 정규화
  - **순서가 핵심**: 느린 조건(강도 3초)을 무장으로, 순간 이벤트(버스트)를
    발사로 둔다. 반대로 하면 버스트를 본 뒤 3초를 기다려 그만큼 늦는다.
  - **시간대 계수**(①②에만): 09:00~ 1.00 / 10:30~ 0.85 / 11:30~ 0.65(점심 최저)
    / 13:00~ 0.80 / 14:00~ 0.95. 경로 ③은 분모가 같이 줄어 자동 보정되므로
    계수를 적용하지 않는다(중복 완화 방지).
  - **상대 경로의 분모는 최근 5초를 제외**하고 잰다. 안 그러면 지금 터지는
    대량체결이 평균에 섞여 **버스트가 클수록 문턱도 올라가는 자기모순**이
    생긴다(격리 테스트에서 실제로 재현돼 수정함).
- **Pullback의 분봉·점수·VWAP 경로 폐지** — 눌림 판정은 HTS가 이미 했는데
  코드가 1분봉으로 재검증하느라 (a) 시간축이 안 맞아 26종목 전부 탈락하고
  (b) 1분봉 종가를 기다리느라 구조적으로 최대 60초 늦었다. 함수
  (`evaluate_pullback`/`_apply_vwap_filter`/`evaluate_1a_leading_strength`)는
  삭제하지 않고 호출부만 교체했다.

- **라우팅 (2026-08-01 최종)** — 종목당 항상 한 전략만. `resolve_strategy()`
  한 곳에서 결정하고 라우팅·주문가드·백테스트가 모두 이 함수를 호출한다.
  - 눌림목자동 **단독** -> 항상 Pullback
  - 주도주상위/돌파자동매매용 **단독** -> 항상 1A
  - **중복(둘 다 편입)** -> 10:30 이전 1A / 10:30 이후 Pullback
    (`DUAL_SOURCE_PULLBACK_FROM` — 오전 모멘텀, 오후 되돌림)
  - 주문 직전 가드는 **단독 소스만** 막는다. 중복 종목이 시간대에 따라 두
    전략을 오가는 것은 정상이므로 여기서 막으면 오전 1A 매수가 전부 죽는다.
- **진입 종료 14:50 / 보유분 15:10 전량 강제청산**(FORCE_CLOSE_TIME).
  진입 종료 < 청산이라 청산 중 신규매수가 겹치지 않는다.
- 등락률 상한은 **실제 매수한 전략**(sub_strategy)을 따른다 — 중복 종목이
  오전에 1A로 매수되면 16%, 오후에 Pullback으로 매수되면 10%.
- **1A/Pullback 외 전략은 코드째 없다** (2026-08-02 삭제, 그 전엔 비활성 상태로
  남아 있었음). 1B/1L은 이날 제거, 1S/Phase2/Phase3/1N은 `_legacy` 백업 후 제거.
  트레일링 익절도 1L 전용이었어서 같이 사라졌다 — 이제 익절은 예외 없이 flat 캡.
  - **⚠️ `phase1b` 객체 자체는 반드시 유지** — 전략이 아니라 1A/Pullback의
    **체결틱·호가 데이터 파이프라인**(TradeFlowTracker/OrderbookTracker)이라
    없애면 두 전략이 눈이 먼다. 이름만 1B다.
  - 이름이 실제로 사고를 두 번 냈다(08-01 1A 버퍼 삭제 / 07-31 1L 조기청산).
    `TickDataPipeline` 같은 리네임이 근본 해결인데 호출부가 ~40곳이라
    **08-03 실거래 검증 후**로 미뤘다.
  - 복구가 필요하면 커밋 `900757c`에 1B/1L 코드와 파라미터 근거 주석이 전부 있다.
- 주문: 매수는 호가 두께로 시장가/지정가 자동 선택, **매도는 항상 시장가**
  (둘 다 실패 시 반대 방식으로 1회 폴백).
- **주문 우선 레인 (2026-08-02)** — 주문(kt10000/kt10001)은 조회 대기열을
  건너뛴다(`_throttle(priority=True)`, 0.2초 간격). 예전엔 진입 트리거가 걸린
  가장 급한 순간에 방금 나간 조회 때문에 최대 0.6초, 429가 겹치면 +2초를
  기다렸다.
- **pre-arm (2026-08-02)** — 편입 즉시 감시 시작 + 전일종가/시가 캐시를 예열해
  둔다. 트리거 시점에 REST를 부르지 않기 위함(`prearm_candidate`).

\## 다음 할 일 (2026-08-03 마감 기준 — **08-04에 변경 6건이 한꺼번에 처음 적용**)

\### ⚠️ 08-04는 "변경이 겹친 날"이다 — 해석 원칙부터
오늘 하루에 **6가지**가 동시에 바뀐다. 결과가 좋든 나쁘든 **무엇 때문인지
분리하기 어렵다**는 점을 먼저 인지할 것. 그래서 슬롯·비중은 일부러 건드리지
않았다(변수를 더 늘리지 않기 위해).

| # | 변경 | 기대 방향 | 검증된 근거 |
|---|---|---|---|
| 1 | 조기확정 -> 본전스톱(단 **OFF**) + 캡 4.0/2.5/6.0 | 익절 확대 | 037070 틱 재생 1건 |
| 2 | 손절이 워밍업 중에도 작동 | 손실 축소 | 로직 결함 수정(표본 무관) |
| 3 | 점심 시간계수 0.65 -> 1.00 | 손실 축소 | 08-03 계수별 실측 |
| 4 | Pullback 창 09:25 -> 09:00 | 진입 증가 | 근거 소멸 + 039980 실측 |
| 5 | 무장 3.0 -> 2.0초 | 진입 증가 | **미검증(실험)** |
| 6 | 종목명 표시 수정 | 표시만 | 재현 테스트 |

\### 09:00 최우선 확인
1. **종목명이 실제 이름으로 찍히는지** — `종목: 삼성전자 (005930)` 형태.
   코드만 나오면 REST 폴백 실패이므로 `raw에 종목명 없음 ... REST 조회 ->` 로그 확인.
2. **09:00~09:25에 눌림목(1A_눌림) 매수가 나오는지** — 이 구간은 어제까지
   구조적으로 0건이었다. 나오면 창 확대가 작동한 것.
3. **`🛡 본전스톱 무장` 로그가 안 뜨는 게 정상**(OFF 상태). 뜨면 플래그가
   되살아난 것이므로 즉시 확인.
4. **매수 건수가 급증하는지** — 4·5번이 겹쳐 어제(16건)보다 크게 늘 수 있다.
   늘면서 손실이 커지면 5번(무장 2초)을 먼저 3초로 되돌릴 것.
5. **점심(11:30~13:00) 매수가 줄어드는지** — 3번의 직접 효과.
   어제 이 구간 3건이 전부 손실(합계 -5.29%)이었다.
6. **15:30 백테스트 리포트가 도착하는지** — 어제는 STOP_SIGNAL로 실행되지
   못했다. 오늘 수동 실행으로 정상 확인(11건 재현)했지만 스케줄 경로는 미검증.
7. **지수 하락 가드가 발동하면** 텔레그램 `🛡 지수 하락 가드 발동`이 온다.
   그 뒤로 신규매수가 멈추고, 11:30까지 본전 이상 보유분이 정리되며,
   남은 건 14:50에 강제청산된다. **발동 안 하는 게 정상**(지수 -3%는 드물다).
8. **마무리 3종 완료 후 즉시 자동종료되는지** — 15:40 고정 대기가 아니라
   백테스트 끝나면 바로 내려가야 한다. 텔레그램 문구로 구분 가능:
   "장마감 마무리 완료(...)" = 정상 / "설정된 시간(15:40) 도달 ... (미완료: ...)"
   = 폴백이 걸린 것이므로 괄호 안의 미완료 항목을 확인할 것.

\### 즉시 되돌리는 법 (실전에서 이상 시)
- 무장 2초 -> 3초: `TICK_STRENGTH_SUSTAIN_SEC = 3.0`
- 본전스톱 켜기: `BREAKEVEN_STOP_ENABLED = True` (로직은 그대로 살아있음)
- Pullback 창 원복: `PULLBACK_START = time(9, 25)`
- 점심 완화 원복: `TICK_BURST_TIME_MULT`의 `((11,30), 1.00)` -> `0.65`
- 지수 하락 가드 끄기: `INDEX_GUARD_ENABLED = False`
- 가드 임계 조정: `INDEX_GUARD_THRESHOLD`(-3.0) / `INDEX_GUARD_FROM`(11:00) /
  `INDEX_GUARD_BREAKEVEN_UNTIL`(11:30) / `INDEX_GUARD_FORCE_CLOSE`(14:50)
- 캡 원복: `config/phase_settings.py`의 `take_profit_cap` 0.040 -> 0.025,
  `TAKE_PROFIT_CAP_PULLBACK/EARLY` 0.025 -> 0.015
- ⚠️ 어느 것을 되돌리든 **한 번에 하나씩** — 그래야 원인이 분리된다.

\### 며칠 모아서 판단할 것
- **본전스톱 ON/OFF 비교** — 오늘 OFF로 돌리므로, 켰을 때 데이터와 대조하려면
  최소 며칠이 필요하다. 시뮬레이션(개장 구간 OFF 우세)이 실거래에서도 맞는지.
- **무장 2초의 실제 효과** — 진입 건수 증가분이 수익으로 이어지는지, 아니면
  약한 신호만 늘어 손실이 커지는지. FID228 이력이 없어 사후 백테스트가 불가능
  하므로 **실거래 관찰이 유일한 검증 수단**이다.
- **비중(tier) 예측력** — 08-03엔 최대 비중(x3.00)이 최대 손실(-3.11%)이었다.
  `entry_tier`가 포지션에 기록되므로 며칠 쌓아 상관을 볼 것. 슬롯보다 이쪽이
  먼저 검증돼야 할 축이다.
- **슬롯 확대 시점** — 진단 알림에 `1A 슬롯 없음`이 뜨기 시작하면 그때가
  근거가 생기는 시점(08-03엔 0건, 동시보유 최대 2/6).

\### 이월 (기존)

\## (구) 다음 할 일 (2026-08-02 세션 마무리 기준 — 08-03이 신규 구조 첫 실거래일)

\### 08-03 아침에 반드시 인지할 것 (두 가지)

**(1) 조건검색 실시간 편입 유실** — 08-01 세션에서 이게 그동안 100%
유실되고 있었다는 사실이 확인됐다(07-31 이전 로그도 마찬가지). 즉 지금까지의
모든 실거래 성과는 "기동 시점 스냅샷 종목만으로 낸 결과"다. 08-03부터는 장중
내내 신규 종목이 들어오므로 **매수 건수와 후보 수가 크게 늘어날 수 있다.**

**(2) 진입이 15초 폴링 -> 틱 구동으로 바뀜 (08-02)** — 이게 이번 개편의 본체다.
`on_trade`가 체결틱을 쌓기만 하고 **진입 평가를 전혀 하지 않고 있었다.**
평가 경로는 편입 순간 1회(이때는 틱이 0개라 무조건 탈락) + `watchlist_reentry`
15초 폴링뿐이었다. 진입 조건이 "강도 100 이상 3초 유지"라는 3초짜리 신호인데
15초 간격으로 들여다보고 있었으니 **평균 7.5초가 늦고, 짧게 스치는 신호는
아예 관측조차 안 됐다.** 1B/1L을 끄면서 그 둘이 갖고 있던 실시간 틱 진입
경로가 사라졌는데 새 1A가 물려받지 못한 것이 원인이었다.
-> **08-03에는 매수 건수가 크게 늘 수 있다.** (1)과 겹치면 슬롯 경합이
처음 겪는 수준이 될 수 있으니 슬롯 부족 로그를 특히 볼 것.

\### 09:00 최우선 확인 (중요도순)
1. **08:59 기동 -> `장 시작 전 편입 — 09:00부터 평가 대기` 로그**. 스냅샷
   종목이 폐기되지 않고 후보로 남는지.
2. **09:00 이후 `편입 신호`가 장중 내내 계속 뜨는지** — 이번 수정의 핵심
   지표. 07-31엔 기동 시점 21건 이후 0건이었다. 안 뜨면 `type='02'` 파싱을
   다시 확인할 것(`신규 item_type='02' 최초 관측` 로그로 원본 구조 확인).
3. **첫 `[진입 진단]` 알림 도착**(09:05~09:35). 이후 경고 유무에 따라
   10분/30분 주기. 판단표는 아래 "진단 알림 읽는 법" 참고.
4. **`주문방식 판정: 매도1~3호가 잔량 ...` 로그** — 시장가/지정가가 실제로
   갈리는지. **`시장가 주문 실패 -> 지정가로 1회 폴백`이 뜨면 모의서버가
   시장가 미지원**이라는 뜻이므로 하이브리드를 지정가 전용으로 되돌릴지 판단.
5. **틱 구동이 실제로 도는지 — 이번 개편의 핵심 지표.**
   - **09:05에 FID 228 워치독이 자동 판정한다**(`task_fid228_watchdog`).
     정상이면 로그에 `✅ FID 228 수신 정상`만 남고 조용하다.
     **텔레그램으로 `🚨 [긴급]`이 오면 그게 오늘의 최우선 문제**다:
       · `체결강도(FID 228)가 한 번도 안 옴` -> `🔑 0B 체결 raw 키` 로그에
         `'228'`이 있는지 확인. 없으면 `kiwoom_ws.py` FID 매핑 수정 필요.
       · `체결틱(0B) 수신 0건` -> WS 실시간 구독 문제(`실시간 등록:` 로그 확인).
       둘은 원인이 완전히 달라서 메시지가 구분해서 나온다.
   - 진단 알림의 `⚡ 틱 N → 무장 N → 버스트 N → 매수 N` 한 줄로 **어느 단계가
     병목인지** 바로 판단할 것. 무장이 0이면 228 문제, 무장은 많은데 버스트가
     0이면 대량체결 임계값이 높은 것이다.
   - `🔫 무장 — 체결강도 100 이상 N초 연속` 로그가 뜨는지.
   - `⚡ 틱 진입 트리거 [1A] — ...` 로그로 매수가 발생하는지.
   - 매수 사유 문자열: `1A 틱즉시진입 (강도 100+ N초 연속, ...) | 경로=절대
     | 시간계수 x1.00` 형태. `경로=` 값이 절대/단일/상대 중 무엇인지 세어두면
     임계값 조정 근거가 된다.
   - **진단 알림에 `강도 미무장(3초 미달)`이 대부분이면 정상**(신호 대기 중).
     반대로 무장은 많은데 매수가 0이면 버스트 임계값이 너무 높은 것.
6. **10:30 이후, 그리고 11시~14:50 사이에도 1A 매수가 계속 나오는지** —
   1A 시간창은 09:00~14:50 전체이지 10:30에 끊기지 않는다(이번 개편의
   성공 판정 기준. 표로 볼 때 10:30/11:00 열이 비어있으면 회귀, 14:55 이후
   열이 비어있는 건 정상 — 1A/Pullback 둘 다 14:50에 진입을 멈추기 때문).
6-1. **09:05~09:20 사이 돌파자동매매용 종목이 즉시 1A 평가되는지**
   (09:20 지연 게이트 제거 효과). 예전엔 이 구간이 통째로 비어 있었다.
6-2. **눌림목자동은 09:25 전엔 평가 안 되는 게 정상**, 14:50까지는
   계속 Pullback 매수가 나오는지.
6-3. **중복 편입 종목(주도주상위+눌림목자동 등)이 10:30을 경계로 전략을
   갈아타는지** — 같은 종목이 오전엔 `[1A]`, 오후엔 `[1A_눌림]`으로
   기록되면 정상(`resolve_strategy()` 규칙 실거래 첫 검증).
6-4. **14:50 이후 신규 진입 로그가 완전히 멈추는지, 15:10에 보유 종목이
   전량 강제청산되는지**(기존 15:15에서 변경). 15:10 이후 보유 0종목이어야
   한다.
6-5. **매도 로그에 시장가가 찍히는지** — `시장가 매도 실패 -> 지정가로
   1회 폴백`이 뜨면 매도 시장가가 거부된 것이므로 즉시 확인.
6-6. **`⚠️ 미관리 잔고 감지` 알림이 뜨는지** — 뜨면 매도 미체결이 실제로
   발생했다는 뜻이므로 해당 종목을 수동 확인할 것.
7. **1B/1L 관련 로그가 전혀 안 뜨는 게 정상**(2026-08-02에 코드째 삭제).
   뜨면 회귀 — 옛 커밋이 섞여 들어왔는지 확인할 것.

\### 진단 알림 읽는 법 (원인 판단표)
- `대량체결 부족`이 대부분 -> **코드 정상**, 필터가 제대로 거르는 중(시장에
  큰 손이 안 들어옴). 며칠 관찰 후 3천만/1억 임계값 조정 검토.
- `체결 데이터 없음` / `체결틱 부족` 다수 -> **0B 실시간 미수신**.
  WS 구독 로그(`실시간 등록:`) 확인.
- `감시중인데 체결틱 0` + 종목코드 -> 그 종목만 0B 미수신.
- `후보인데 감시 미시작` -> **라우팅 이상**. 1A 평가에 도달조차 못 함.
- `최근 10분간 평가 기록 없음` -> **`task_watchlist_reentry` 정지**.
- `실시간 편입 수신 0건` -> **조건검색 구독 이상**(오늘 고친 문제의 재발).
- `1A 슬롯 없음` -> 정상. 슬롯 회전 문제로 접근.
- `MDD/HALT/격리/컷오프` -> 정상(리스크 장치 작동 중).

\### 표본 쌓이는 대로 검증 (며칠~2주)
8. **`candidate_tier` 공식의 예측력** — 매수 시 `entry_tier`를 포지션에
   기록해 두었으므로, tier가 높았던 종목이 실제로 더 벌었는지 확인.
   상관이 없으면 공식을 바꾸거나 `PHASE1A_PRIORITY_MARGIN`을 크게 올려
   교체 로직을 사실상 끌 것.
9. **제안 A/B/C 파라미터 검증** — A: 1.5/1.2 배수, B: 조건식별 표본이
   최소 3건에 도달하는지(하루 검색식당 2~5건이라 대부분 중립일 것으로 예상),
   C: 1.5배 확대가 손실도 1.5배로 키운 건 아닌지.
10. **정체 정리 15분 / ±0.5% 밴드** — 너무 일찍 자르는지, 그 자리에 실제로
    더 나은 종목이 들어갔는지.
11. **1A 대량체결 임계값(3천만x3 / 단일 1억)** — 하루 매수 건수가 극단적으로
    적거나 많으면 조정. 진단 알림의 `대량체결 부족` 종목 수가 1차 지표.
12. **등락률 상한 16%/10%** — 16%로 완화한 뒤 고가 추격매수가 늘지 않았는지,
    눌림목 10%가 너무 타이트하지 않은지.
13. **틱 아카이브 데이터로 1A 백테스트 착수** — 08-03부터 `cache/tick_history/`에
    쌓이기 시작. 1~2주 후 `TradeFlowTracker.add_tick()`에 먹여 3초 강도 +
    대량체결 버스트 조건을 과거 데이터로 처음 검증.

\### 이월 (여전히 유효)
14. **0g 프로그램매매 FID 불일치** — 로그로 확정됨: `type='0g'`는 프로그램매매가
    아니라 **'주식종목정보'**(VI 관련, FID 297/305/306/307/370/382/592~594/689).
    기대한 118~124와 전혀 안 겹친다. `program_flow`가 하루 종일 0을 기록 중.
    매매 판단에 미연결이라 무해하지만, 전 종목 `0g` 구독은 낭비 -> 구독 목록에서
    빼거나 진짜 타입 코드를 찾을 것.
15. **429 포화 재측정** — 07-30 실측(하루 2,469건) 이후 재측정 안 함. 이번에
    실시간 편입이 살아나고 틱 아카이빙이 추가된 게 새 변수(반대로 reentry
    고속경로는 크게 줄인다). 08-03 실측 필요.
16. **`CandleCache.log_stats()` 미호출** — 여전히 어디서도 안 부름.
17. **`task_condition_snapshot_poll` 비활성 유지** — `_wait_for`가 소켓에서
    직접 recv 하는 구조라 `listen()`과 경합한다. Future/큐 방식으로 재설계해야
    안전하게 켤 수 있음. **다만 실시간 편입이 정상화됐으므로 우선순위 하락.**
18. **부분 매도 시 DB `buy_quantity` 미반영**(심각도 낮음, 미착수).
19. **`theme_manager.save_themes_to_db()` DB 저장 실패**("more than one '%s'
    placeholder"). 매매엔 영향 없음.
20. **WS 재연결 핸들러 진단** — 계속 관찰.
21. **지수 방어 임시 비활성화**(`MARKET_DEFENSE_ENABLED=False`, 07-28부터).
    새 전략 검증이 끝나면 재활성화 여부 판단할 것.

\### 즉시 끄는 법 (실전에서 이상 징후 발견 시)
- **틱 구동 진입 끄기(폴링으로 복귀)**: `TICK_ENTRY_ENABLED = False`
  — 되돌리면 15초 폴링만 남으므로 매수가 급감한다는 점 인지할 것.
- 무장 시간 조절: `TICK_STRENGTH_SUSTAIN_SEC` (3.0초)
- 버스트 완화/강화: `PHASE1A_BURST_TRADE_COUNT`(2건),
  `TICK_BURST_WINDOW_SEC`(5초), `TICK_BURST_REL_MULT`(20배)
- 시간대 계수 끄기: `TICK_BURST_TIME_MULT`의 계수를 전부 1.00으로
- 상대 버스트 경로만 끄기: `TICK_BURST_REL_MULT = 10**9`
- 주문 우선 레인 끄기: `KiwoomREST.ORDER_MIN_INTERVAL = 0.6` (조회와 동일)
- ~~1B 되살리기: `PHASE1B_ENABLED = True`~~ — **2026-08-02에 코드째 삭제됨.**
  되살리려면 커밋 `900757c`에서 되가져와야 한다(플래그만으로는 안 됨).
- 제안 A 끄기: `PHASE1A_TIER_SINGLE_MULT = 1.0`, `PHASE1A_TIER_BURST_MULT = 1.0`
- 제안 B 끄기: `core/strategy_performance.py`의 `ENABLED = False`
- 제안 C 끄기: `PHASE1A_SIZE_MAX_MULT = 1.0`
- 종목 우선순위 교체 끄기: `PHASE1A_PRIORITY_MARGIN = 999`
- 하이브리드 주문 -> 지정가 전용: `PHASE1A_ASK_DEPTH_MIN = 10**12`
- 정체 정리 끄기: `DEAD_POSITION_MIN = 999`
