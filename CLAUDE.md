\# AutoTrader\_Project 작업 규칙



\## 기본 정보

\- 환경: ⚠️ **실전투자 (2026-08-05부터, `IS_MOCK=false`)**. 왕복수수료 0.23%
&#x20; — 실전 위탁계좌(번호는 `config.ini` 참조, git 미추적), 종목당 매수 **100만원**
&#x20; (08-05 전환 시 50만원 -> 같은 날 장마감 후 200만원 환원. 실지배값은
&#x20; `portfolio_optimizer.DEFAULT_BASE_AMOUNT` — **3곳이 같이 움직여야 한다**).
&#x20; **이 계좌에는 봇과 무관한 사용자 보유 종목이 있다**(우리기술/엑스게이트) —
&#x20; 봇은 DB 기반으로만 청산하므로 건드리지 않지만, 잔고를 볼 때 헷갈리지 말 것.
&#x20; 상세는 히스토리 "2026-08-05 실전투자 전환" 참고.

\- 구조: asyncio + PostgreSQL (Docker `analytics\_pg` 컨테이너, 자동재시작 설정됨)

\- 사용자: 다윤, 한국어로 소통

\- 텔레그램 채널: 오토트레이더(order, 매수/매도 체결·실패), 주식 따릉이(signal, 시스템 로그/정기보고),

&#x20; 종가베팅 포착방(closing\_bet, 종가베팅 TOP10)



\## 개발/운영 환경 (2026-08-01 구축)

\### 실행 환경
- 프로젝트 경로: `C:\Users\rober\OneDrive\문서\vscode\AutoTrader_Project`
- 스케줄러용 junction 경로: `C:\AutoTrader_Bot\ProjectRoot`
  (**비대화형/S4U 컨텍스트에서 실행되는 배치파일은 반드시 이 경로를 쓸 것** —
   실제 경로의 "문서"(한글) 폴더명이 cmd.exe 파싱을 깨뜨린다. 2026-07-28 참고)
- 자동 기동: Windows 작업 스케줄러 `AutoTrader_Start`, **평일(월~금) 08:59**
  (2026-08-15에 '매일' -> **주간 트리거 월~금**으로 좁혔다. 요일마스크 62)
- DB: Docker `analytics_pg` 컨테이너 (PostgreSQL, 자동재시작)

\### Claude Code CLI (2026-08-01 신규 설치)
| 항목 | 버전/경로 |
|---|---|
| Node.js | v24.18.1 (winget `OpenJS.NodeJS.LTS`, `--scope user`) |
| npm | 11.16.0 |
| claude CLI | 2.1.220 (`npm install -g @anthropic-ai/claude-code`) |
| claude 실행파일 | `...\WinGet\Packages\OpenJS.NodeJS.LTS_...\node-v24.18.1-win-x64\claude.ps1` |
| 로그인 방식 | Claude account with subscription (Claude Pro) |

설치 중 실제로 걸렸던 것 (다시 설치할 일 있으면 참고):
- `winget search`가 msstore 원본 약관 동의 프롬프트에서 멈춘다 →
  `--accept-source-agreements` 플래그 필수.
- npm이 `allow-scripts` 정책으로 postinstall(`node install.cjs`)을 건너뛴다는
  경고를 낸다. **core 기능에는 영향 없음**(`claude --version`/`--help` 정상 확인).
  `npm approve-scripts`는 전역 설치에 안 먹히므로(EGLOBAL) 무시하면 된다.
- 설치 직후 PATH는 **새 터미널을 열어야** 반영된다.

\### 원격제어 · 모바일 세션 -> **[아카이브] `docs/OPERATIONS.md`**

08:59 자동기동 시 `claude --remote-control` 세션 동시 실행(세션명 `autotrader`,
`--continue`로 전날 대화 연속), 폰 접속 절차, 자동재시작·워치독, 그리고
**실제로 밟은 함정 4가지**(`.ps1` UTF-8 BOM 필수 / `.bat`에 한글 금지 /
claude 폴더 신뢰는 실제 경로 기준 / 로그 인코딩 분리)가 전부 거기 있다.

- 수동 기동: `powershell -NoProfile -ExecutionPolicy Bypass -File "C:\AutoTrader_Bot\ProjectRoot\start_remote_control.ps1"`
- 대화가 길어지면 프로젝트 루트에 빈 파일 **`NEW_SESSION_REQUESTED`** 를 만든다
  (다음 기동 **1회만** 새 대화, 플래그는 즉시 삭제. `.gitignore` 대상).
- ⚠️ **실거래 봇이 도는 장중에 모바일에서 매매 로직 파일을 고치지 말 것.**

\### 🔔 알림 시스템은 **별도 문서**다 (2026-08-13 분리)

텔레그램·이메일 알림(유튜브 자막 요약 / 장마감 수급·특징주 / 장중 주도주 /
주식 뉴스)은 **이 프로젝트와 별개 시스템**이고 문서가 따로 있다:

```
../alert_systems/ALERTS.md          ← 이관 문서
../alert_systems/workflows/         ← GitHub Actions YML 사본
../youtube_transcript_bot/          ← 유튜브 봇 (이 PC에서 1시간마다)
```

- 🔴 **봇 토큰을 공유한다.** `@bell_stock_bot` 하나가 이 프로젝트의 signal 채널과
  알림 시스템 채널 6개를 전부 담당한다. **토큰을 재발급하면 `config.ini`만
  고쳐선 안 되고** GitHub Secrets·유튜브 봇 config 까지 3곳을 같이 갱신해야 한다.
- 매매 로직과는 무관하므로 여기서는 이 사실만 알면 된다. 상세는 `ALERTS.md`.

\### 🆕 대시보드 (2026-08-11 신설, Phase 0 — 읽기 전용)

`run_dashboard.bat` -> http://127.0.0.1:8787 . **관측 전용, 주문·설정변경 0.**
실행법·매매일지(`daily_journal.py`)·Phase 1 설계는 **`docs/OPERATIONS.md`**,
패널 상세는 `ui/README.md`. 아래 둘만 봇을 만질 때 항상 필요하다.

- 🔴 **`ui/`는 `core/`·`main.py`를 절대 import하지 않는다.** 그래서 UI를
  아무리 고쳐도 스위트에 영향이 없고 **장중에도 UI만은 안전하게 수정할 수
  있다.** 허용 의존은 `db.connection` + 표준 라이브러리뿐. 이 규칙이 깨지면
  Phase 0의 안전성이 통째로 사라진다.
- 🔴 **데이터 함정 3가지**(실측 확인, 분석할 때도 그대로 적용된다):
  ① `system_events.timestamp`는 **UTC**인데 `trades.buy_time`은 **KST**다
     (실측: 뉴엔AI 손절이 13:51:44 vs 04:51:44). **섞으면 9시간 어긋난다.**
  ② **추가매수는 `entry_reason`을 덮지 않는다** — 즉 **물타기 발동은
     `trades`로 셀 수 없고** 로그(`💧 물타기`)나 `system_events.BUY_ADD`를 봐야 한다.
  ③ `watch_list_log`의 `surge_rate`/`open_price`/`today_open`이 **전부 NULL**이다.
     그래서 체크리스트 0번이 '편입 등락률 8%' 대신 **조건식 이름 확인**으로 갔다.

\## 작업 방식 (반드시 지킬 것)

\### 🆕 전용 에이전트 · 스킬 · 커맨드 (2026-08-17 신설, `.claude/`)

이 프로젝트의 실패는 대부분 **"검증하는 장치 자체가 틀려서 통과했는데 무효"**였다
(문서화된 사고 10건). 그래서 **자기기만이 기록된 지점에만** 신규 컨텍스트를 둔다.

| 종류 | 이름 | 언제 부르나 |
|---|---|---|
| 에이전트 | **`edge-auditor`** | 매매 규칙·상수를 바꾼 뒤 — 그 규칙이 사는 **모든 경로** 전수 탐색(실패 1위: 산재 규칙) |
| 에이전트 | 🔴 **`null-hypothesis`** | 변경 직후 — **"이 변경은 무효다"를 증명하려 시도**. ON/OFF A/B가 실제로 갈리는지. **반드시 구현한 대화와 분리해서 호출** |
| 에이전트 | **`stats-referee`** | 백테스트로 정책을 정하기 전 — 숫자가 아니라 **방법론**을 심판(시간균등·독립표본·날짜분해·양가정 등 10종) |
| 에이전트 | **`market-watch`** | 장중 — 로그·DB로 체크리스트 판정. **읽기 전용**(Edit/Write 도구 없음) |
| 스킬 | `strategy-change-protocol` | 매매 상수를 바꿀 때의 절차 8단계 |
| 스킬 | `verify` | 스위트 19개 실행 + 기대 건수 대조 |
| 커맨드 | `/premarket` `/watch` `/postmarket` `/ship` | 장전 / 장중 / 마감후 / 변경 마무리 |

- 🔴 **`/ship`이 기본 경로다** — 산재감사 → 무효반증 → 검증19 → 문서3곳 → 커밋을 순서대로 강제한다.
- always-on 비용은 전부 합쳐 **약 975토큰**(설명만 상주, 본문은 호출 시 로드).
- ⚠️ **훅은 아직 없다.** 즉 "장중 매매파일 수정 금지"는 여전히 **규칙일 뿐 강제되지 않는다.**
  넣으려면 `PreToolUse(Edit|Write)`로 09:00~15:30 && `core/**`|`main.py` 차단.

1\. \*\*surgical 패치 우선\*\*: 파일 전체 재작성보다 "찾기 → 교체" 블록 단위로 제시. 

&#x20;  불가피하게 큰 함수 전체를 재구성해야 할 때만 함수 단위 통째 교체.

2\. \*\*파일 삭제 금지\*\*: 기존 파일은 지우지 않고 `\_legacy` 접미사로 백업 후 교체.

3\. \*\*검증 필수\*\*: 코드 변경 직후 반드시 `python -c "import \[모듈]; print('OK')"`로 

&#x20;  import 성공 여부 확인. 여러 파일을 고쳤으면 각각 검증.

4\. \*\*큰 변경 전 드라이런 권고\*\*: 특히 매매 로직(진입/청산/점수 계산) 변경은 

&#x20;  장 시작 전이나 소규모 테스트 권장.

5\. \*\*비동기 코드 주의\*\*: 메인 루프를 막는 `sleep` 절대 금지 (WS 연결 끊김 원인이었던 적 있음).

&#x20;  `asyncio.sleep`만 사용.

6\. \*\*인덴트 안전\*\*: Python 들여쓰기가 걸린 수정은 특히 신중하게 — 

&#x20;  부분 문자열 교체보다 함수/블록 전체를 정확히 교체하는 방식을 우선 고려.

7\. \*\*수정 후 백업 병행\*\* (2026-07-28): 그날 코드 수정이 어느 정도 쌓이면(세션

&#x20;  마무리 시점 등) git 커밋 + 날짜별 파일 스냅샷 백업을 같이 남긴다.

&#x20;  - git: 의미 단위로 커밋(민감정보 커밋 전 확인 — config.ini/settings.py/.env는

&#x20;    이미 .gitignore 처리됨, 그 외 파일에 하드코딩된 키/비밀번호 없는지 훑을 것).

&#x20;  - 파일 스냅샷: 프로젝트 폴더 밖 `AutoTrader\_Project\_backups\YYYY-MM-DD\`에

&#x20;    전체 복사(.git, \_\_pycache\_\_ 제외). Windows 작업 스케줄러에도 매일 자동

&#x20;    실행 등록(대화 세션과 무관하게 돌아감) — 스크립트는

&#x20;    `C:\Users\rober\OneDrive\문서\vscode\daily\_backup.ps1`.



\## 히스토리 (참고용, 과거 세션 요약)

\- 2026-07-24 ~ 07-31: **[아카이브] `docs/HISTORY_2026-07.md` 참고.**
  Phase3 재설계 / VWAP 5종 게이트 / 전략 1A+Pullback+1B+1L 체제 개편 /
  스케줄러 한글경로 junction 해결 / 09:20 게이트 회귀 / 동적 익절캡 도입 /
  틱 아카이빙 인프라 / 조건검색식 3개 체제 전환 등.
  ⚠️ 그 시기 전략(1S/Phase2/Phase3/1N/1B/1L)은 **전부 삭제됐다** —
  지금 살아있는 건 1A뿐이다(눌림목은 08-06 [E]로 슬롯 0).

- 2026-08-01 ~ 08-02: **[아카이브] `docs/HISTORY_2026-08-01_02.md` 참고.**
  치명 버그 9건 수정 / 틱 구동 전면 전환 / 1B·1L 코드 완전 삭제 /
  통합 드라이런 테스트 신설 / 프로젝트 파일 정리.
  ⚠️ 그 시기 결정 중 **무장 3초**는 08-03에 1.5초로 완화됐다가
  08-07에 실측으로 3초 복원됐다. 지금 사양은 CLAUDE.md 본문을 볼 것.

- 2026-08-03 ~ 08-04: **[아카이브] `docs/HISTORY_2026-08-03_04.md` 참고.**
  익절 구조 재설계(조기확정 폐지 -> 본전스톱 + 캡 상향) / 강도 기준선 리앵커 /
  손절을 워밍업 위로 / 시간대 계수 전 구간 1.00 / 지수 하락 가드 신설 /
  되돌림 대기 분할매수 + 분할매도 / 버스트 문턱 4천만 / 테스트 로그 분리.
  ⚠️ 그 시기 값 중 **무장 3초는 08-03에 1.5초로 완화됐다가 08-07에 복원**,
  **상승이탈은 08-06 OFF**, **눌림목은 08-06 [E]로 슬롯 0**이다.
  지금 사양은 반드시 본문 "현재 전략 요약"을 볼 것.

- 2026-08-05 (실전 전환일): **[아카이브] `docs/HISTORY_2026-08-05.md` 참고.**
  실전투자 전환(토큰 URL 하드코딩·예수금 필드·매수금액 3곳 결함) / 조건검색
  거래소구분 / 장중 수술 7건(손절 대신 추가매수·되돌림 ±0.3%·시장가 문턱) /
  매도시점 3건(손실반등 분할·시가필터 부활·조건부 재매수) / 버스트 주가 스케일 /
  VI 상단 확정매도 / 감사 스위트 신설과 결함 3건.
  · **실전 첫날 결산: 청산 20건 / 실현 +9,796원 / 평균 +0.41%**
  · 🔴 **지금도 유효한 결론 3가지**(캡을 만질 때 반드시 읽을 것):
    ① **캡 도달 종목의 77%가 본전까지 되돌아온다** — 캡 상향과 본전스톱은
       서로를 무력화한다. 정책 비교는 **본전스톱을 켠 상태로** 할 것.
    ② **재버스트는 예측력이 0**이다(+4% 도달률 52% vs 50%).
    ③ **예수금은 `100stk_ord_alow_amt`** — 20/30/40/50/60stk 계열은 미수다.
  · ⚠️ 그 시기 값 중 **매수금액 100만원 / 주가계수 상한 3.00 / 조건검색 "K" /
    무장 3.0초 / 상승이탈 OFF / VI 매수차단 OFF**로 바뀌었다.

- 2026-08-06 (결함 [A]~[H] 8건을 만들고 고친 날):
  **[아카이브] `docs/HISTORY_2026-08-06.md` 참고.**
  장중 긴급정지(개장 18분에 20건 매수·손절 7건, -138,152원) -> 원인 분석 ->
  [A] 시가 절단 / [B] 버스트 side / [C] 등락률 하한 / [D] 상승이탈 OFF /
  [E] 눌림 슬롯 0 / [F] 진입 숙성 60초 / [H] 파동 상한 3 / VI 매수차단 OFF /
  시가대비 8% 완화 / 매수금액 200만 -> 100만.
  · 🔴 **지금도 유효한 근본 진단**: 진입 신호에 방향성 우위가 없다
    (4일 75건 +5분 평균 **+0.099%** < 왕복수수료 0.23%). 반면 조건검색
    **편입 즉시** 매수는 +0.506% — **우위를 진입 로직이 1/5로 깎는다.**
  · 🔴 **방법론 3가지**(이후 세션들이 계속 인용한다):
    ① 구간별 평균은 **반드시 날짜로 쪼개 볼 것**(3~6% 구간 열위는 날짜 효과였다)
    ② 정책을 **하루 데이터로 고르지 말 것**(08-06 최적이 4일 최악)
    ③ 진입 경로는 가격 역산이 아니라 **그 경로가 남긴 로그**로 판정할 것
  · ⚠️ 그 시기 값 중 **익절캡 6.0 / 본전스톱 +2.5% / 손절 변동성비례 /
    정체정리 OFF**(전부 08-10)로 바뀌었다.

- 2026-08-07: **[아카이브] `docs/HISTORY_2026-08-07.md` 참고.**
  무장 1.5 -> **3.0초 복원**(실측 33건, 1.5~2초 구간이 손익비 0.34로 최악) /
  조건검색 통합 -> **KRX 되돌림**(성과가 아니라 **계층 정합성** — 틱·분봉·주문이
  전부 KRX인데 조건식만 통합이었다) / 기동 전 전면 재검증(결함 0, 회귀 스위트
  신설) / **VWAP 단위 백만원 확정**(`VWAP_UNIT_SCALE=1e6`) / 장마감 후 3건
  (**VWAP 진입필터 ON** / 숙성 조건식별 분리 / 스냅샷 폴링 재활성) /
  **일봉 눌림 태그** 신설(관측 전용).
  · 🔴 **지금도 유효한 방법론 2가지:**
    ① **"데이터가 없다"고 적기 전에 로그를 먼저 뒤질 것** — FID228 이력이
       "재현 불가"라고 적어뒀는데 실제로는 로그에 매 건 남아 있었다.
       이 프로젝트의 진단 로그는 사실상 관측 데이터베이스다.
    ② **08-05(실전전환·조건식변경) 이전 데이터는 진입 파라미터 판단에 쓰지 말 것.**
       4일로 보면 대금 컷이 +5.7%인데 08-05 이후 33건만 보면 **-3.7%로 뒤집혔다.**
       (청산 정책 비교에는 여전히 쓸 수 있다.)
  · ⚠️ 그때 값 중 **숙성 예외 키는 `돌파전`**(08-11), **손절 -4.5%**(08-11),
    **익절캡 6%**(08-10), **15:10 강제청산 폐지**(08-12)로 바뀌었다.
- 2026-08-08 ~ 08-09: **[아카이브] `docs/HISTORY_2026-08-08_09.md` 참고.**
  이관 문서 감사(결함 16건) / 장전 일봉 눌림 스캐너 신설(⛔ **08-09에 OFF**) /
  진입 지표 4축 백테스트 / **발사 게이트 시간대 분리**(버스트 -> 거래대금 가속도) /
  개장초반 슬롯 캡 4 / VWAP 깊은 검사 / 파동 카운터 결함수정.
  · 🔴 **지금 도는 진입 사양이 여기서 만들어졌다** — 09:00~09:05 발사 면제 +
    09:05~ 가속 2.5배는 전부 이 이틀의 결과다. 근거를 다시 볼 일이 있으면
    아카이브를 열 것.
  · 🔴 **지금도 유효한 방법론 4가지**(이후 세션들이 계속 인용한다):
    ① **기준선은 '시간 균등'으로 뽑을 것** — 틱 인덱스 균등으로 뽑으면 활발한
       순간을 과대표집해 우위가 **4배 과대평가**된다(-0.357% vs +0.119%).
    ② **진입 규칙은 '독립표본(종목·일당 첫 신호)'으로 평가할 것** — 격자 평균은
       없는 우위를 보여준다(`VWAP AND 가속2.0`이 격자 +0.531%p vs 독립 -0.046%).
       ①과 **세트**다. 둘 중 하나만 어겨도 결론이 뒤집힌다.
    ③ **비율 지표엔 '표본 수' 하한을 둘 것** — 가속도는 틱이 2개면 수학적
       최대(4.0)가 나온다. 단 하한은 **개수**여야 한다(금액 하한은 실측상 해롭다).
    ④ **게이트를 사전 검사할 땐 검사 가격 == 체결 가격인지 볼 것.**
  · 🔴 **버스트는 무해한 게 아니라 유해했다** — 독립표본 -0.349%(기준선 +0.395%),
    플러스일 1/5. `check_burst` 자체는 **재매수·슬롯교체용으로 살아있다** —
    거기까지 바꾸지 말 것.
  · ⚠️ 그 시기 값 중 **우선순위 교체는 08-10에 0(완전 OFF)**, 파동 카운트는
    08-09에 `BURST_WAVE_COUNT_REQUIRES_BURST=True`로 바뀌었다.
    지금 사양은 반드시 본문 「현재 전략 요약」을 볼 것.

- 2026-08-10 ~ 08-15: **[아카이브] `docs/HISTORY_2026-08-10_15.md` 참고.**
  🔴 **지금 도는 청산 사양이 전부 이 6일에 만들어졌다.** 익절캡 6% 전면 상향 /
  변동성 손절(-4.5~-5.5%) / -3% 물타기 / **당일청산 폐지(오버나이트)** /
  진입 유효창 30분 / VI·본전스톱 50% 분할 / **확정익절 2%(본전스톱 대체)** /
  매수 주가 상한 10만원 / 대시보드 Phase 0 신설 / 결함 20여 건 수정.
  · 🔴 **이 6일의 한 줄** — 08-10 전수 분석: **매도한 자리에서 당일 고가까지
    평균 +5.45% 더 갔다**(41건). 전부 종가보유였으면 -44,016 -> +338,087원.
    **종목 선정은 맞았고 청산이 일렀다** — 이후 모든 청산 정책 변경의 출발점이다.
  · 🔴 **지금도 유효한 방법론 5가지**(이후 세션들이 계속 인용한다):
    ① 새 규칙을 넣으면 반드시 **ON/OFF A/B로 '실제로 달라지는가'**를 볼 것 —
       08-10 변동성 손절이 조용히 무효였던 것을 이 방법으로만 찾았다.
       **스위트가 전원 통과해도 규칙이 무효일 수 있다.**
    ② **스텁 시그니처가 실물과 다르면 감사가 거짓말한다**(하루에 세 번 밟았다).
    ③ **아카이브 폴더명 != 실제 틱 날짜** — 분석할 땐 `time_str`로 다시 묶을 것.
    ④ **경계값 검증에 `int(가격*(1+비율))`을 쓰지 말 것** — 부동소수 오차로
       멀쩡한 코드가 실패로 보인다. 실물 `_net_rate`로 역산할 것.
    ⑤ **'최근 N일 발동 0건'은 '앞으로도 안 돈다'가 아니다** — 08-13에 "당장
       효과 거의 0"이라 적은 규칙이 **바로 다음 거래일에 5건** 발동했다.
  · ⚠️ 그 시기 값 중 **본전스톱(무장 2.5% / 바닥 1.0%)은 08-15에 확정익절
    2%로 대체**됐고, **15:10 강제청산은 08-12에 폐지**됐다.
    지금 사양은 반드시 본문 「현재 전략 요약」과 「기대값」 블록을 볼 것.

\## 세션 이관 체크리스트 (2026-08-01 신설)

새 세션(PC 터미널 / 모바일 원격 / 새 대화창 어디서든)이 이 프로젝트를
이어받을 때 쓰는 절차. **읽는 순서가 중요하다.**

\### 이관받는 쪽이 읽을 순서
1. `\## 기본 정보` + `\## 개발/운영 환경` — 무엇을 어디서 어떻게 돌리는지
2. `\## 작업 방식` — 지켜야 할 규칙 7가지 (surgical 패치, 파일 삭제 금지 등)
3. `\## 현재 전략 요약` — **지금 코드가 실제로 하는 일**(과거 히스토리보다 이걸 먼저)
4. `\## 다음 할 일` — **🔴 이번 이관의 본체.** **08-17(월)에 처음 도는 3건**
   (🆕 **확정익절 2%(본전스톱 대체)** / 50% 분할 기계 / 분할매도 x 잔고지연 가드)과
   장중에 무엇을 봐야 하는지(「🔬 08-17 장중에 반드시 볼 것」 Q0~Q4)가 전부 여기 있다.
5. `\## 히스토리`는 필요할 때만 역순으로. **분량이 많으니 통독하지 말 것** —
   "왜 이렇게 됐는지"가 궁금할 때 해당 날짜만 찾아본다.
   🔴 **본문에 남은 히스토리는 전부 포인터뿐이고 실물은 `docs/`에 있다**
   (`HISTORY_2026-07.md` / `_2026-08-01_02` / `_03_04` / `_05` / `_06` / `_07` /
   `_08_09` / **`_2026-08-10_15`**). 그 외 참고 자료도 같이 분리돼 있다 —
   `DEFECTS_2026-08-06_AD.md`(결함 [A]~[D] 상세) /
   `OBSERVATION_CHECKLISTS_PAST.md`(08-12·08-13 장중 관찰표) /
   `OPERATIONS.md`(원격제어·대시보드 설치·운영) /
   🔴 **`EXPECTED_VALUES_RATIONALE.md`(기대값 44개 각각의 근거·실측·롤백)** /
   `SUITE_HISTORY.md`(스위트 건수 변천 이력).
   · 🔴 **왜 나눴나** — CLAUDE.md는 **매 요청마다 통째로 다시 청구된다.**
     2026-08-17 실측: 요청 1건당 고정비용 140,235토큰 중 CLAUDE.md가 **약 62%**,
     세션당 도구 호출이 수백 번이라 이것만으로 억 단위 토큰이 나갔다.
     분리 후 87,263 -> **약 46,500토큰(-47%)**. **감사 #11/#14는 그대로 통과한다**
     (문서 대조 문자열 62개가 전부 본문에 남아 있음을 기계 검증했다).
   · ⚠️ **새 히스토리를 쓸 땐 본문에 길게 쓰지 말고 처음부터 `docs/`에 쓸 것.**
   · 🔴 **값을 바꾸기 전엔 `EXPECTED_VALUES_RATIONALE.md`를 먼저 읽을 것.**
     기대값 블록에는 이제 **값 줄만** 있다(감사 [9]가 대조하는 문자열). 왜 그 값인지는
     전부 그 파일에 있고, 이 프로젝트가 반복해서 밟은 사고가 **근거를 모른 채 값을
     되돌리는 것**이다(예: 본전스톱 무장 문턱을 낮추면 4개 바닥 열 전부 악화 — 08-13 격자).

\### ⚡ 30초 요약 — 이것만 알면 대화를 시작할 수 있다
- 🔴 **08-13(목)은 스케줄러가 08:59에 안 돌아 개장 25분을 놓쳤다** — 자동업데이트
  후 로그인 잠김 -> 절전. 09:14 수동 기동해서 정상 매매했다.
  **그날 작업 설정 4개를 고쳤다**(`StartWhenAvailable`/`WakeToRun` True,
  배터리 제한 2개 해제) — 이제 절전이면 깨우고 놓친 실행을 따라잡는다.
  ⚠️ 다만 **로그인 화면(로그아웃 상태)이면 여전히 안 뜬다**(`LogonType=Interactive`).
  그 경우 로그인하는 즉시 기동한다. 상세는 히스토리 08-13.
- 스위트 **1,942건 통과 / 실패 0**(**20개**, 2026-08-18 장마감 후 실행).
- 🔴🔴 **다음 거래일은 08-17(월). 그날 처음 도는 것이 3가지다.**
  | # | 변경 | 왜 위험한가 | 롤백 |
  |---|---|---|---|
  | **Ⅰ** | 🆕 **확정익절 2% — 본전스톱을 대체**(08-15) | **매도 규칙 자체가 바뀌었다.** 순 +2.0% 도달 **즉시** 50% 분할하고 잔량은 3% 트레일. 본전스톱(무장 2.5% -> 바닥 1.0%)은 상호 배타로 죽는다 | `FLAT_TP_ENABLED = False` |
  | Ⅱ | **50% 분할 기계**(08-14 19:38 적용) | 08-14에 본전스톱이 **하루 5건** 발동했지만 그날은 전량 사양으로 돌았다 — 분할은 **한 번도 실거래를 안 거쳤다**. 이제 확정익절이 같은 기계를 쓴다 | `BREAKEVEN_EXIT_PARTIAL = False` |
  | Ⅲ | **분할매도 x 잔고대조 결함수정**(08-15) | 봇이 자기 분할매도를 '수동 추가매수'로 오인해 **물타기가 2배를 사던** 것을 막았다 | `RECONCILE_SELL_GRACE_SECONDS = 0` |
  · 🔴 **셋은 한 덩어리다** — Ⅰ이 분할을 **훨씬 자주** 발생시키고(150건/8일 기준
    발동률 41~51%), 그 잔량이 오래 살아남으면서 Ⅲ의 위험 구간(잔고 반영 지연
    실측 99초)에 처음으로 들어간다. **되돌릴 땐 Ⅰ부터.**
  · 🔴 **08-17 장중 최우선 확인**: `확정익절 2.0%` 매도에 `— 분할 1차 50%`가 붙고
    **잔량이 다음 틱에 안 팔리는가**. 그리고 `💰 수동 추가매수 감지`가
    **봇 분할 직후에 뜨지 않는가**(뜨면 Ⅲ의 회귀다).
  · ⚠️ **매도 건수가 크게 늘어난다.** 본전스톱은 08-11~13에 0건이었는데
    확정익절은 **하루 절반 가까운 포지션**에서 발동한다. 이건 정상이다.
- 🔬 **확정익절을 고른 근거**(실매수 150건 / 8일 종일분봉, 비관·낙관 + 슬리피지 0.2%)
  | 정책 | 비관 | 낙관 | 승률 | 날짜별(비관/낙관) |
  |---|---|---|---|---|
  | 현행이던 본전스톱 2.5/1.0 | 0.326 | **-0.240** | 55% | — |
  | **채택: +2% 50%분할** | **0.360** | **-0.046** | **71%** | 5승3패 / 6승2패 |
  | (참고) +2% 전량 | 0.657 | 0.719 | 73% | 6승2패 / 8승0패 |
  | (참고) +2.5% 50%분할 | 0.486 | 0.148 | 69% | 5승3패 / 8승0패 |
  · **두 가정 모두에서 본전스톱을 이긴다**(08-11 물타기 백테스트는 여기서 부호가
    뒤집혀 판정 불가였다 — 이번엔 안 뒤집혔다).
  · ⚠️ **같은 구조에서 문턱만 올리거나(2.5%) 전량으로 하면 더 낫다.**
    2.0% / 50% 분할은 **승률과 상승 참여를 사는 사용자 지정 선택**이다.
- ✅ **08-14(금)은 정상 매매했다** — 청산 8건 / DB +48,947원.
  08-13 적용분 ⓐⓑ 중 **ⓑ(본전스톱 바닥 +1.0%)가 5건 발동해 전부 +0.85~+1.00%**에서
  나갔다(예측은 "당장 효과 0"이었는데 **하루 만에 빗나갔다**).
  ⓐ(주가 상한)는 그날 10만원+ 후보가 없어 **판정 보류**.
- 🔴 (참고, 이미 지나간 것) **08-14(금)에 처음 돌았던 변경 2건 (08-13 적용)**
  | # | 변경 | 근거 | 롤백 |
  |---|---|---|---|
  | ⓐ | **매수 주가 상한 10만원** — 이 가격 이상 신규매수 금지 | 10만+ 8건 평균 **-1.87%**·중앙 **-2.08%**·손절률 62%, **4/4일 열위**. 기전은 '비쌀수록 게이트가 헐거워진다'(무효율 26%→50%) | `MAX_ENTRY_PRICE = 0` |
  | ⓑ | **본전스톱 바닥 +0.2% → +1.0%** | 발동 12건이 고점 +1.85% → 실현 **+0.07%**로 **1.78%p 반납**(12/12건 +1% 미만) | `BREAKEVEN_FLOOR = 0.002` |
  · 🔴 **무장 문턱(2.5%)은 절대 낮추지 말 것** — 08-13 격자에서 낮추면 4개 바닥
    열 **전부** 나빠졌다(승자를 자른다: 조건검색 +6.05→+1.00 / 서산 +4.39→+0.77).
  · ⚠️ ⓑ는 **당장 효과가 거의 0**이다(08-11~13 본전스톱 발동 **0건**). 보험이다.
  · ⚠️ ⓐ는 **표본 8건**이고 경계 10만원은 데이터로 고른 값이 아니다(5~10만 표본 0건).
- 🔴 **08-12 심야에 결함 5건을 더 잡았다**(전부 08-13에 처음 도는 변경분에서
  나왔다). 스위트 1,762건이 전원 통과하는 상태에서 나온 것들이다 —
  상세는 히스토리 「2026-08-12 (심야, 적용분 재검증)」.
- 🔴 **08-13(목)에 처음 도는 변경 5건 (08-12 장마감 후 적용)**
  | # | 변경 | 근거 | 롤백 |
  |---|---|---|---|
  | ⑪ | 🔴 **수동 추가매수 `elif` 결함수정** — 08-11에 만든 기능이 **한 번도 안 돌았다** | 실측: JW신약 봇 515주 vs 실계좌 1,032주(517주 방치) | — (결함) |
  | ⑫ | **진입 유효창 30분** — 편입 후 30분 지나면 매수 포기 | 8일 104건, 30분~ MFE +1.79%(절반이 못 먹음), **5/5일 우세** | `MAX_ENTRY_AGE_SEC = 0` |
  | ⑬ | **VI 상단 50% 분할** (잔량은 트레일) | 8일 9건, **78%가 판 뒤 +3%↑** 더 감 | `VI_UPPER_EXIT_PARTIAL = False` |
  | ⑭ | **분할 잔량 보호** — 동적캡 재발동으로 잔량 안 판다 | 실측 44~55초 만에 잔량이 털렸다(설계 무력화) | `PARTIAL_EXIT_REMAINDER_HOLD = False` |
  | ~~⑮~~ | ~~편입가 앵커~~ → 🔴 **적용 당일 저녁에 OFF로 되돌림** | 시뮬레이션 결함 + 구간마다 방향 반대 | (이미 False) |
  · ⑫가 이번 묶음에서 근거가 가장 강하다(5/5일).
  · 🔴 **⑮는 넣었다가 같은 날 껐다.** "체결 90/90 · downside 0"이 **시뮬레이션
    결함**이었다 — 2차 미체결 시 1차만 체결돼 **평단이 오르는 효과**를 빠뜨렸다.
    틱 실측에서 09:00~09:10은 현행 우위(+8.55 vs +8.53%p), 09:10 이후는 C안
    우위(+0.18%p)로 **방향이 뒤집혀 판정 불가**.
    **교훈: 트랜치 가격을 깊게 바꾸면 '체결률 하락 -> 평단 상승'을 반드시 같이
    모델링할 것.** 가격만 비교하면 개선처럼 보인다.
  · => 실제로 08-13에 도는 것은 **⑪~⑭ 4건**이다.
  · 🔴 **⑪은 결함수정이라 성격이 다르다** — 나머지는 정책 변경이다.
  · ⚠️ 08-13은 **미검증 5건이 도는 날**이다. 되돌릴 땐 하나씩.
- 🔴🔴 **08-12 새벽에 이 봇의 전제가 바뀌었다 — 당일청산 폐지.**
  | # | 변경 | 롤백 |
  |---|---|---|
  | ⑥ | 🔴 **15:10 전량 강제청산 폐지** — 가격 규칙에 안 걸리면 **오버나이트** | `order_manager.FORCE_CLOSE_ENABLED = True` |
  | ⑦ | **지수가드 14:50 강제청산도 폐지** (1단계 본전청산은 생존) | `INDEX_GUARD_FORCE_CLOSE_ENABLED = True` |
  | ⑧ | **오버나이트분은 다음 기동 때 `manual` 격리** (봇 관리 밖) | `OVERNIGHT_RESTORE_AS_MANUAL = False` |
  | ⑨ | **물타기 11:00 컷오프** (없어서 15:09에도 발동했다 — 실측) | `AVG_DOWN_CUTOFF = time(15, 10)` |
  | ⑩ | 🔴 **물타기 주문거부 재시도 상한 3회** — 결함수정 | `AVG_DOWN_MAX_RETRY` |
  · 사용자 지정: **"15:10 강제청산은 없는걸로 — 내가 수동매도하던지 오버나이트 할 수 있게"**.
  · => **시간 기반 자동청산이 하나도 없다.** 남은 건 전부 가격 기반이다.
  · ⚠️ **갭 하락은 손절로 못 막는다.** 그리고 격리분을 정리하지 않으면
    **매일 쌓여 예수금이 잠긴다**(첫 증상 = "신규 매수가 안 된다").
    매일 `python manual_position_tool.py list`로 확인할 것.
  · ⑩은 실측 결함이었다 — 예수금 부족 같은 지속 거부에서 **50틱에 주문 50회**를
    내보내 REST가 429로 막히면 **매도 주문까지 밀린다**.
- 🔴 **08-12(수)에 처음 도는 변경 5개 (08-11 적용분)**
  | # | 변경 | 롤백 |
  |---|---|---|
  | ① | **손절 -3% -> -4.5%** (+ 관찰바닥 -5.5%) | `stop_loss_rate` / `STOP_LOSS_VOL_MIN` / `RESCUE_ADD_OBSERVE_FLOOR` **세트** |
  | ② | **-3% 물타기** (동일수량 1회, 종목당 200만 캡) | `AVG_DOWN_ENABLED = False` |
  | ③ | **조건식 개편** 돌파자동매매용 -> 돌파전 + **돌파후(확인전용)** | config 1줄 + 코드 6곳 |
  | ④ | **수동 추가매수 합산** (평단·수량 반영) | `_reconcile_manual_sells` 증가분기 |
  | ⑤ | **수동매도 vs 미체결 구분** (기록 정확도) | — |
  ⚠️ **여기에 HTS 조건식 내용 변경(등락률 8% / 강도 300% / [1분]5봉전 200%)이
  같이 돈다.** 즉 결과가 갈려도 원인 분리가 안 된다 — 되돌릴 땐 하나씩.
  🔴 ②는 **백테스트가 판정 불가**로 나왔는데 사용자 지시로 넣었다(아래 히스토리).
  ⚠️ **08-12은 미검증 변경이 10건 도는 날이다.** 이 프로젝트 기준으로 가장 많다.
- 🔴 **08-10 결산: 청산 41건 / -44,016원 / 승률 49%.** 그런데 **장은 아주 좋았다**
  (대한광통신 +22.5% / 저스템 +18.1% / 광전자 +14.1% ...). 전수 분석 결과:

  > **매도한 자리에서 그 종목들이 당일 고가까지 평균 +5.45% 더 갔다.**
  > 전부 종가까지 들고만 있었으면 **-44,016 -> +338,087원**.

  **종목 선정은 맞았고 청산이 전부 일렀다**(41건 평균 보유 **25분**).
  이게 지금 이 봇의 핵심 문제이고, 08-10 변경은 전부 여기에 대한 대응이다.
- 🔴 **08-11(화)에 미검증 변경이 두 묶음 돈다. 되돌릴 땐 반드시 하나씩.**

  **[A묶음] 08-10 청산 정책 개편 — 이번이 가장 큰 변경이다**
  | # | 변경 | 롤백 |
  |---|---|---|
  | A1 | **익절캡 4.0/2.5/2.5/6.0 -> 6.0/6.0/6.0/8.0** | 각 상수 원복 |
  | A2 | **본전스톱 무장 +1.0% -> +2.5%** | `BREAKEVEN_TRIGGER = 0.010` |
  | A3 | **정체정리 15분·시간정리 30분 OFF** | `STAGNANT_EXIT_ENABLED = True` |
  | A4 | **손절 고정 -3% -> 장중 120초 고저폭 x1.5** (08-11에 클램프 -4.5~-5.5%로 확대) | `STOP_LOSS_VOL_ENABLED = False` |
  | A5 | **반등소진 매도는 본전 이상에서만** | `LOSS_REBOUND_REQUIRE_BREAKEVEN = False` |
  | A6 | **우선순위 교체 완전 OFF** | `PHASE1A_PRIORITY_MAX_PER_DAY = 6` |
  ⚠️ **A1과 A2는 세트다.** 캡만 올리면 08-05의 "캡 도달 종목 77%가 본전까지
  되돌아온다"가 재현된다. 되돌릴 때도 같이 되돌릴 것.

  **[B묶음] 08-08~09 진입 변경 — 08-10 하루만 돌았다**
  | # | 변경 | 롤백 |
  |---|---|---|
  | B1 | **발사 게이트 시간대 분리**(09:00~09:05 면제 / 09:05~ 가속 2.5) | `FIRE_GATE_SPLIT_ENABLED = False` |
  | B2 | **개장초반 슬롯 캡 4**(B1의 짝) | `EARLY_SLOT_CAP_ENABLED = False` |
  | B3 | **VWAP 깊은 검사** | `VWAP_ENTRY_CHECK_DEEPEST = False` |
  | B4 | **파동 카운트 기준 정정** | `BURST_WAVE_COUNT_REQUIRES_BURST = False` |
  | B5 | VWAP 진입필터 ON / 숙성 조건식별(돌파 30초) / 스냅샷 폴링 | 각 상수 |
  ※ B4는 B1·B2가 만든 결함을 되돌린 것이라 **세트**다(B1을 끄면 자동 무의미).
- ✅ **08-10에 근본결함 2건을 고쳤다** — 유령 포지션이 DB에 남아 재시작 때
  부활하던 것(매도 3회 실패 시 메모리에서만 뺐다), 그리고 '수량 부족' 매도
  실패를 3회 재시도하며 우선순위 교체 한도까지 태우던 것.
- 살아있는 전략은 **1A 하나**뿐 — 눌림목은 [E]로 슬롯 0(매매 중단).
- 매수금액 **100만원**, 슬롯 8/0(공유 6).
- 🆕 **진입 유효창 [30초 ~ 30분]** — 숙성(하한)에 08-12 저녁 **상한**이 붙었다.
  편입 후 30분이 지나면 그 종목은 **그날 더 이상 사지 않는다**(`MAX_ENTRY_AGE_SEC`).
- 진입은 `편입 → 숙성(돌파전 30초/그외 60초) → 무장 3.0초 → **발사** →
  파동 1~3번째 → 등락률 0~13% → VWAP +0.5% 초과 → 되돌림 -0.3%/-0.7% 분할`.
  **발사가 시간대로 갈린다**: **09:00~09:05는 게이트 면제**(무장만으로 발사),
  **09:05~는 버스트 대신 거래대금 가속 2.5배**. 버스트는 신규 진입에서 빠졌다.
- 청산은 `**물타기(-3%)** → **구조후 최종선(원가 -6%)** → 손절(-4.5~-5.5%) →
  지수가드 → VI상단 → 워밍업 → 🆕 **확정익절 +2%(50% 분할)** → 분할트레일 → 익절캡 6% →
  ~~정체·시간정리(OFF)~~ → ~~15:10 강제청산(**08-12 폐지**)~~`.
  ⚠️ **본전스톱은 08-15에 확정익절로 대체됐다**(상호 배타, `FLAT_TP_ENABLED`).
- 🔴 **08-12부터 시간 기반 자동청산이 하나도 없다**(사용자 지정). 가격 규칙에
  안 걸린 종목은 **오버나이트**로 넘어가고, 다음 기동 때 `manual`로 격리된다.
  물타기도 **11:00까지만** 발동한다.
- ⛔ **장전 스캐너는 2026-08-09에 껐다**(사용자 지정, **임시 보류**).
  사유: **일봉 눌림 후보가 조건검색식에 거의 잡히지 않는다** — 스캐너가 뽑은
  후보와 실제로 매매 가능한 유니버스가 겹치지 않으면 Phase 1/2로 갈 근거가
  쌓이지 않는다. 판단이 설 때까지 보류.
  · 끄는 방법은 **스케줄러 작업 하나**다(코드·상수 변경 없음):
    `Disable-ScheduledTask -TaskName "AutoTrader_PremarketScan"` -> 지금 `Disabled`.
  · 되살리려면 `Enable-ScheduledTask -TaskName "AutoTrader_PremarketScan"`.
    수동 1회 실행은 `python premarket_scan.py --no-send`(텔레그램 없이).
  · ✅ **봇에는 아무 영향이 없다** — 별도 프로세스이고, `main.py`/`core/`/
    `start_trader.bat` 어디에도 참조가 0건임을 확인했다(트리거는 그 작업뿐).
  · ⚠️ 봇 안의 **일봉 눌림 태그**(`DAILY_PULLBACK_TAG_ENABLED`)는 **별개이고
    아직 켜져 있다.** 그건 스캐너가 아니라 실제 매수 건에 `[일봉눌림 D-N]`을
    붙이는 관측 기능이다(pre-arm에서 종목당 하루 1콜). 항목 N(눌림 종목 익절캡
    상향)의 근거가 여기서 쌓이므로 같이 끌 이유는 없다 — 끄려면 그 상수를 False로.
  · [원래 사양] 매일 08:30 별도 프로세스로 일봉 눌림 후보와 **돌파선**을 뽑아
    텔레그램 + `observations/premarket/`에 남기고 직전 스캔을 자동 채점했다.
- 🔴 **근본 문제는 여전하고, 08-08 백테스트로 원인이 더 좁혀졌다**:
  현행 두 게이트가 **각각** 우위가 없다(버스트 **-0.257%p** / 강도 +0.008%p,
  4일 기준선 대비). 유일한 생존자는 **거래대금 가속도 2.0x**(+0.430%p)인데
  그것도 15분 창 n=105다. 지금까지의 수정은 전부 손실을 줄이는 조치다.
- 최근 결산: 08-05 **+9,796원** / 08-06 **-138,152원** / 08-07 **-42,120원**.
- 🆕 **대시보드가 생겼다**(08-11, Phase 0 읽기 전용): `run_dashboard.bat` ->
  http://127.0.0.1:8787 . 위 「장중에 반드시 볼 것」 0~10을 **자동 판정**한다.
  08-12 장중엔 로그 grep 대신 이걸 볼 것. **봇 코드 변경 0줄**이라 미검증 5건에
  6번째를 얹지 않는다. 상세는 `ui/README.md`.

\### ⛔ 이관받는 즉시 확인할 것 (검증보다 먼저)

```bash
python -c "from db.connection import get_connection
with get_connection() as c:
    cur=c.cursor(); cur.execute(\"SELECT stock_code,stock_name,buy_price,buy_quantity FROM trades WHERE status!='closed'\")
    r=cur.fetchall(); print('미청산', len(r), '건'); [print(' ', x) for x in r]"
```

- 🔴 **08-13 아침엔 `holding` 2건이 정상이다**(자이에스앤디 80주 @8,968 /
  JW신약 515주 @2,413). 당일청산을 폐지해 08-12 보유분이 그대로 넘어온 것이고,
  **기동하면 `OVERNIGHT_RESTORE_AS_MANUAL`이 자동으로 `manual`로 격리**한다
  (`🌙 오버나이트 보유 2종목 격리` 알림). 기동 후 다시 조회하면 `manual` 5건이 된다.
- 🔴 **JW신약은 실계좌 1,032주인데 DB엔 515주뿐이다.** 517주는 사용자가 직접
  산 물량인데 `elif` 결함(08-12 수정)으로 반영되지 않았다. **DB에 없으므로
  격리 대상도 아니고 봇이 영영 모른다** — 직접 처리해야 한다.
- **그 외 `status='manual'` 3종목**(주성036930 7주 @140,100 / 다스코058730 552주
  @3,305 / 금호타이어073240 73주 @8,210)은 08-06 저녁에 격리한 것이다 —
  `find_holdings()`가 `'holding'`만 조회하므로 재가동해도 복원되지 않는다.
  현황은 `python manual_position_tool.py list`로 본다.
  · 사용자가 HTS에서 판 뒤에는 `python manual_position_tool.py close <코드> <체결가>`로
    실제 손익을 기록해 통계에 반영할 것.
  · 서버 잔고엔 남아 있으므로 기동 후 **'⚠️ 미관리 잔고 감지' 텔레그램이
    종목당 1회** 온다(우리기술/엑스게이트와 같은 취급). **정상이다.**
- **봇 실행 여부**: `Get-Process python` 이 0이어야 정상(정지 상태).
- **`STOP_SIGNAL` 잔존 여부**: 수동 정지한 날은 남을 수 있다. 있으면 지울 것.
- ⛔ **장전 스캔은 08-09부터 꺼져 있다**(`AutoTrader_PremarketScan` = `Disabled`).
  즉 `observations/premarket/`에 **새 파일이 안 생기고 텔레그램도 안 오는 것이
  정상**이다 — 고장으로 오해하지 말 것. 사유·복구법은 위 30초 요약 참고.
  · 상태 확인: `Get-ScheduledTask -TaskName "AutoTrader_PremarketScan"`
    (⚠️ `Disabled`여도 `NextRunTime`은 값이 남아 있다 — `State`를 볼 것)
  · **관측 전용이라 매매와 무관**하다 — 꺼져 있어도 봇 기동에 영향이 없다.
  · 수동 1회 실행: `python premarket_scan.py --no-send` (텔레그램 없이).

\### PC 검증 항목 (코드가 성한지 — 이관 직후 반드시 실행)
프로젝트 폴더에서 순서대로 실행하고, 하나라도 실패하면 그 지점부터 원인 추적.

| # | 명령 | 기대 결과 |
|---|---|---|
| 1 | `python -c "import main; print('OK')"` | `OK` (임포트 체인 전체 정상) |
| 2 | `python test_patch_20260801.py` | **297건 전원 통과 / 실패 0건** |
| 3 | `python test_patch_20260802.py` | **89건 전원 통과 / 실패 0건** (틱 구동 + 상대경로 뒷문차단 + VI 매수차단 교차) |
| 4 | `python test_patch_20260803.py` | **137건 전원 통과** (익절/손절/시간계수/자동종료/종목명/지수가드/시간정리/슬롯교체/본전스톱ON) |
| 5 | `python test_patch_20260804.py` | **72건 전원 통과** (되돌림 대기 분할매수 + 분할매도 + 폴링 우회 회귀방지 + 🆕 **확정익절이 다른 규칙의 잔량을 안 건드린다**) |
| 6 | `python test_live_dryrun_20260803.py` | **165건 전원 통과** (종일 통합 + 기동 인프라 + 상수 불변식 + 설정조합 + 전면차단 교차감사) |
| 7 | `python test_closing_bet_routing.py` | **24건 전원 통과** (종가베팅 분리 라우팅) |
| 8 | `python test_patch_20260805.py` | **145건 전원 통과** (장중수술 + 매도시점 + 버스트 주가 스케일 + VI 확정매도 + 시가대비 8%/급등강화 + **상승이탈 OFF 전환**) |
| 9 | `python test_patch_20260806.py` | **187건 전원 통과** (🔴 **[A] 시가 절단 / [B] 버스트 side / [C] 등락률 하한 / [D] 상승이탈 OFF / [E] 눌림 슬롯0 / [F] 진입 숙성 / [H] 파동 상한 / 무장 3초 / VWAP ON·단위 1e6 / [13] 조건식별 숙성 / [14] WS future 디스패치 / [15] 일봉 눌림 태그·`drops` 깊이**) |
| 10 | `python test_live_20260805.py` | **68건 전원 통과** (⚠️ 실전 전환 감사 — 모의 하드코딩/예수금 필드/**기존 보유 불가침**/매수금액 3곳 정합/노출한도/**조건검색 거래소 `"K"`**) |
| 11 | `python audit_20260805.py` | **182건 전원 통과** (⚠️ **경로별 오작동 감사** — 잘못된 매수/매도 + **문서↔코드 정합성** + 청산 충돌 매트릭스 + **죽은 슬롯 불변식** + 🆕 **확정익절 문서대조·상호배타·잔량보호**) |
| 12 | `python test_replay_20260806.py` | **51건 전원 통과** (🔴 **08-06 실거래 19건 재생** — 손실이 막히는가 **+ 승자 4건이 살아남는가** + [C]x추가매수 교차 + VWAP ON에서도 '모름은 통과') |
| 13 | `python test_patch_20260808.py` | **91건 전원 통과** (🆕 **발사 게이트 시간대 분리 + 개장초반 슬롯 캡** — 09:05 경계 / 버스트를 **더한 게 아니라 대체**했는가 / 최소 틱수 가드 / 캡이 확장슬롯보다 먼저 / 파동·무장·시가 게이트 생존 / check_burst 잔존 / 롤백) |
| 14 | `python audit_deep_20260808.py` | **103건 전원 통과** (🆕 **심층 감사** — 진입 경로 2개 동일 게이트 / `_execute_buy` 호출부 **5곳**(08-11 물타기 추가) / **매도 무영향 실증** / '팔았는데 못 사는' 반쪽 동작 / 교체 폭주 한도 / 수치 3중 대조 / 종일 시나리오 / 이상 입력 내성) |
| 15 | `python test_replay_archive.py` | **22건 전원 통과** (🆕 **아카이브 통합 리플레이** — 실물 `StrategyManager`에 아카이브 전 틱을 관통. ⏱ **콜드 약 3~4분 / 웜 약 1분**(08-15 실측 3회: **204초 / 51초 / 220초**. 100MB 아카이브라 디스크 캐시 상태가 4배를 가른다 — **몇 분 걸려도 멈춘 게 아니니 기다릴 것**. 진행 여부는 `Get-Process python`의 CPU 시간이 늘어나는지로 확인한다). 08-11에 로더 결함 수정 — 파일의 첫 틱 날짜만 채택하던 것을 **모든 날짜 추출**로 고쳐 매수 표본이 95 -> 169건이 됐다) |
| 16 | `python test_patch_20260811.py` | **62건 전원 통과** (🆕 **08-11 변경 전용** — 손절 계층 -4.5/-5.5/-6.0 / 돌파전·돌파후(확인전용) / **-3% 물타기**(경계·1회·캡·MDD·지수가드·복원분 제외·롤백) / **구조 후 최종선이 손절 분기 밖에 있는가** / 수동 추가매수 합산 / 수동매도·미체결 구분) |
| 17 | `python test_patch_20260812.py` | **47건 전원 통과** (🆕 **08-12 변경 전용** — 물타기 **11:00 컷오프**(경계 7종·15:09 회귀방지·롤백) / 🔴 **물타기 주문거부 재시도 상한**(50틱 50회 -> 3회, 쿨다운, 포기 후에도 손절 정상) / **15:10 강제청산 폐지**(자동종료 비차단·가격청산 생존) / **지수가드 14:50 폐지**(1단계 본전청산은 생존·롤백) / **오버나이트 manual 격리**(당일분은 복원·슬롯 미점유·롤백) / 시간청산 전멸 불변식) |
| 18 | `python test_patch_20260813.py` | **61건 전원 통과** (🆕 **08-12 장마감 후** — 🔴 **수동 추가매수 `elif` 결함**(JW신약 515→1032주 재현) / **진입 유효창 30분**(경계 6종·재편입 불변·롤백) / **VI 50% 분할**(잔량 생존·롤백) / **분할 잔량 보호**(동적캡 재발동 무시·손절은 생존) / ~~편입가 앵커~~(**OFF로 되돌림** — 배선은 ON으로 켜서 검증, 캐시 보존 확인) / 경로 수 불변) |
| 19 | `python test_patch_20260814.py` | **54건 전원 통과** (🆕 **매수 주가 상한 + 본전스톱 바닥** — 경계 10만원 / 두 진입경로 차단 / **전일종가 몰라도 상한 생존**(결합 회귀) / 진단 분류 분리 / 물타기 우회 기록 / **바닥 1.0% 경계 3종 + A/B(신·구가 실제로 갈리는가) + 무장 문턱 불변 + 손절 생존** / 롤백 2종) |
| 20 | `python test_patch_20260815.py` | **55건 전원 통과** (🆕 **08-15 검증분** — 🔴 **분할매도 x 잔고 반영 지연**(봇이 자기 분할을 '수동 추가매수'로 오인 / **A/B로 물타기 50주 vs 100주가 갈리는지** / 08-11 기능 보존 / 유예 만료 복귀 / 롤백) / **본전스톱 50% 분할 잔량 전 경로 — 🔄 롤백 경로 검증**(같은 가격 10틱 생존 · 트레일 · 익절캡 · 손절 · 1주/3주 경계 · 실물 `_net_rate` 역산 경계) / 🆕🆕 **확정익절 2% x 50% 분할**(경계 · 즉시 분할 · 10틱 생존 · 잔량 전 경로 · **본전스톱과 상호 배타** · 분할 스위치 공용 · 롤백 2종)) |
| 21 | `python test_patch_20260818.py` | **28건 전원 통과** (🆕🆕 **분할 잔량 하한** — 1차 매도가 기록(단일 창구·**첫 분할만**) / 경계 3종(+1원·동일가 5틱 생존·-1원) / 🔴 **동일가 연속틱에 잔량이 살아남는가**(`<=`로 쓰면 즉시 털린다 — 실제로 첫 판이 그랬고 기존 스위트 3개가 잡았다) / LB형(하한 승) x STX형(트레일 승) / **A/B 무효반증** / 상방 무영향 / 손절 생존 / 하위호환(구 포지션) / 롤백) |
| 21 | `git log --oneline -5` | 최신 커밋이 `457cf47`(08-15 확정익절 전환) 이후인지 |
| 22 | `git status --short` | 비어 있어야 정상(작업 중이면 예외) |

**합계 1,942건 전원 통과**(297+89+137+72+165+24+145+187+68+**184**+51+91+103+22+62+47+61+54+55+**28**).
**2026-08-15 저녁 실행으로 맞춘 값이다**(확정익절 2% 전환 반영).
⚠️ **어떤 날 어떤 스위트가 몇 건에서 몇 건이 됐는지**(08-12~08-15 변천 이력)는
`docs/SUITE_HISTORY.md`. 표 숫자 오기를 추적할 때만 열 것.
🔴 **끈 기능의 배선 테스트는 지우지 말고 롤백 컨텍스트로 감쌀 것** — 지우면
되살릴 때 검증이 없다(08-10 교훈). 확정익절 전환 때 옛 본전스톱 사양을 단언하던
**6개 스위트가 한꺼번에 실패**했는데, `FLAT_TP_ENABLED = False` 컨텍스트로 감싸
전부 살렸다.
🔴 **`test_replay_archive`(#15)가 이번에 결정적이었다** — 사양 스위트 16개가 전부
통과하는 상태에서 **매수 0건**을 잡아냈다(하네스가 `_first_seen`을 -999초로
밀어둬 새 유효창 상한에 전부 걸렸다). 시간 게이트를 건드리면 반드시 #15까지 돌릴 것.

- 🔴 **15번은 15개 스위트 중 유일하게 '실제로 무슨 일이 일어나는가'를 본다.**
  나머지는 전부 **사양**을 본다("이 게이트가 이 입력을 막는가"). 15번은
  `cache/tick_history/`의 **그날 진짜 체결틱**을 실물 StrategyManager에 흘린다.
  사양 테스트가 전원 통과해도 배선이 어긋나 규칙이 무효가 되는 일이 반복해서
  일어났다(08-09 `buy_price=1`로 매도 0건 / 08-10 `compute_stop_rate` 창 비움).
  · **A/B가 내장돼 있다** — 변동성 손절을 ON/OFF로 두 번 돌려 **결과가 실제로
    갈리는지** 확인한다. 08-10에 규칙이 무효인 걸 정확히 이 방법으로 찾았다.
  · ⚠️ **무장(FID228)은 아카이브에 없어 strength=130 고정 = 항상 무장 =
    최악 케이스**다. 건수의 절대값이 아니라 **불변식 위반 여부**만 읽을 것.
  · 🔴 **아카이브 폴더명 != 실제 틱 날짜.** 아카이버가 ka10079로 소급 수집하는데
    당일 거래가 없던 종목은 **직전 거래일 틱**이 그대로 오늘 폴더에 저장된다
    (실측: `20260810/` 37개 중 실제 08-10 틱은 12개). 폴더 6개 -> 실제 **11일**.
    **아카이브로 분석할 땐 반드시 `time_str`로 날짜를 다시 묶을 것** —
    폴더명으로 묶으면 "4일치 분석"이 실제로는 7개 날짜가 섞인 것이 된다.
※ `NEW_SESSION_REQUESTED`가 있는 동안은 **1,659 통과 / 실패 1**이 정상이다(#6).
하나라도 실패하면 그 지점부터 원인 추적.
⚠️ 08-08에 **5개 스위트의 픽스처를 같이 고쳤다** — `trigger()`/`burst()` 헬퍼가
전부 옛 버스트 사양(대량체결 2건)을 흉내내고 있어서, **건당 금액은 그대로 두고
틱 수만 `_FIRE_N`으로 올려 신·구 사양을 모두 만족**하게 했다. 롤백해도 그대로 돈다.

- 🔴 **12번은 다른 스위트와 목적이 다르다.** 나머지는 "이 게이트가 이 입력을
  막는가"(사양)를 보지만, 12번은 **08-06 그날의 실제 19건을 실제 코드에 다시
  태운다.** 게이트를 조이는 변경은 손실만이 아니라 **이익도 같이 막을 수 있는데**
  그건 사양 테스트로는 절대 드러나지 않는다 — 그래서 "승자 4건이 한 건도
  차단되지 않는다"가 이 파일의 가장 중요한 단언이다.
  **진입 게이트를 건드렸으면 반드시 이것까지 돌릴 것.**

- 📄 **11번 감사(`audit_20260805.py`)의 섹션 [9]는 이 CLAUDE.md 자체를 검사한다.**
  상수를 바꾸고 위 '기대값' 블록을 안 고치면 여기서 실패한다 — "매일 문서와 코드가
  어긋난다"는 문제의 **구조적 해결책**이다. 상수를 바꿨으면 문서도 같이 고치고
  11번을 돌릴 것.
  ⚠️ **다만 이 검사는 `기대값 블록에 그 문자열이 있는가`만 본다.** 문서의 **다른
  문단이 정반대를 말해도 통과한다** — 실제로 08-07에 VWAP을 켜고 '기대값' 블록만
  고쳤더니, "아직 OFF"라고 적힌 문단 4곳이 134건 전원 통과 상태로 남아 있었다
  (08-08 이관 시 발견·수정). **플래그를 뒤집었으면 `grep`으로 그 이름이 나오는
  모든 문단을 훑을 것.**
- ⚠️ **11번(감사)이 실전 안전의 마지막 관문이다.** 기능별 스위트 902건이 전부
  통과하는 상태에서도 08-05 심야에 결함 3건(비정상 가격 즉시 매도 / 지수조회
  None에 손절 정지 / 가격폴백 루프 중단)이 여기서 나왔다. **매수·매도 로직을
  건드렸으면 반드시 이것까지 돌릴 것.**
※ **이 표는 세 번 연속 옛 값이 남아 있었다.** 08-05 이관 때 #2(287) / #5(63)가,
  08-05 심야 이관 때 #6(164 전원통과)과 합계식(986이 985로 적힘)이,
  08-06 이관 때 #8(140 -> 실제 142)이 어긋났다.
  **표 숫자를 고칠 땐 반드시 15개를 다 실행해서 맞출 것** — 여기가 틀리면 다음
  세션이 "회귀가 났다"고 오판하고 멀쩡한 코드를 뒤진다.
  (08-11 이관 검증 시점에 15개 전부 실행해 맞췄다 — 표의 개별 숫자와 합계
   1,660이 실측과 정확히 일치한다.)

- ⚠️ 6번의 `이관 플래그가 남아있지 않음`은 **코드 결함이 아니라 환경 상태** 검사다.
  프로젝트 루트에 `NEW_SESSION_REQUESTED`가 있으면 **실패로 뜨는 게 정상**이다
  (이관작업 직후 일부러 만들어 둔 것 — 다음 원격 세션 1회만 새 대화로 시작하고
  플래그는 즉시 삭제된다). 그 경우 **#6이 164 통과 / 1 실패**로 나오고 합계는
  **1,659건 통과 / 실패 1건**이 된다.
  ✅ **2026-08-11 이관 검증 시점엔 플래그가 없다** — 즉 지금은 **#6도 165건
  전원 통과가 정상**이고, 어떤 실패든 실제 결함이다.

- ⚠️ **4번(통합)이 가장 중요하다.** 2·3번이 전부 통과하는 상태에서도 08-03에
  매매를 반쪽 낼 버그 6건이 살아있었다(2026-08-02 히스토리 참고) — 유닛은
  부품을 보고 통합은 배선을 본다. **전략을 바꾸면 반드시 4번까지 돌릴 것.**

- ⚠️ 2번은 Windows 콘솔 기본 코드페이지(cp949)에서 유니코드 출력이 깨져
  `UnicodeEncodeError`가 난다. **`PYTHONIOENCODING=utf-8`을 앞에 붙여 실행**할 것
  (Git Bash: `PYTHONIOENCODING=utf-8 python test_patch_20260801.py`).
- 기존 `test_phase1b_controller.py` / `test_microstructure.py`는 **실패하는 게
  정상**이다(5월 작성 스테일 테스트). 회귀로 오해하지 말 것 —
  확인하려면 `git stash` 후 재실행해서 HEAD에서도 같은 실패가 나는지 보면 된다.

설정값이 의도대로인지 한 번에 보는 명령:
```
python -c "import core.strategy_manager as s; from core.order_manager import FORCE_CLOSE_TIME; print('1A', s.GROUP_A_START, '~', s.PHASE1A_END); print('PB', s.PULLBACK_START, '~', s.PULLBACK_END); print('중복전환', s.DUAL_SOURCE_PULLBACK_FROM); print('청산', FORCE_CLOSE_TIME); print('슬롯', s.PHASE1A_MAX_SLOTS, s.PULLBACK_MAX_SLOTS, s.MAX_HOLDINGS, s.MAX_HOLDINGS_HARD); print('무장', s.TICK_STRENGTH_MIN, s.TICK_STRENGTH_SUSTAIN_SEC); print('버스트', s.PHASE1A_BURST_TRADE_VALUE, s.PHASE1A_BURST_TRADE_COUNT, s.PHASE1A_SINGLE_TRADE_VALUE); print('버스트방향', s.BURST_REQUIRE_BUY_SIDE); print('VWAP깊은검사', s.VWAP_ENTRY_CHECK_DEEPEST, s.PHASE1A_PRIORITY_MAX_PER_DAY); print('초반캡', s.EARLY_SLOT_CAP_ENABLED, s.EARLY_SLOT_CAP_UNTIL, s.EARLY_SLOT_CAP); print('발사분리', s.FIRE_GATE_SPLIT_ENABLED, s.FIRE_GATE_ACCEL_FROM, s.FIRE_ACCEL_MIN, s.FIRE_ACCEL_SHORT_SEC, s.FIRE_ACCEL_LONG_SEC, s.FIRE_ACCEL_MIN_TICKS); print('시간계수', sorted(set(v for _,v in s.TICK_BURST_TIME_MULT))); print('되돌림', s.ENTRY_PULLBACK_ENABLED, s.ENTRY_PULLBACK_TRANCHES, s.ENTRY_PULLBACK_TIMEOUT_SEC); print('상승이탈', s.ENTRY_BREAKOUT_ENABLED, s.ENTRY_BREAKOUT_PCT); print('등락률하한', s.MIN_ENTRY_CHANGE_PCT); print('주가상한', s.MAX_ENTRY_PRICE); print('진입숙성', s.MIN_ENTRY_DELAY_SEC); print('파동상한', s.BURST_WAVE_MAX); print('분할매도', s.PARTIAL_EXIT_ENABLED, s.PARTIAL_EXIT_FRACTION, s.PARTIAL_EXIT_TRAIL); print('지수가드', s.INDEX_GUARD_THRESHOLD, s.INDEX_GUARD_FROM, s.INDEX_GUARD_BREAKEVEN_UNTIL, s.INDEX_GUARD_FORCE_CLOSE); print('캡', s.TAKE_PROFIT_CAP, s.TAKE_PROFIT_CAP_PULLBACK, s.TAKE_PROFIT_CAP_EARLY, s.TP_CAP_UPGRADED_MAX); print('본전스톱', s.BREAKEVEN_STOP_ENABLED); print('본전스톱무장', s.BREAKEVEN_TRIGGER); print('본전스톱바닥', s.BREAKEVEN_FLOOR); print('본전스톱분할', s.BREAKEVEN_EXIT_PARTIAL); print('1B잔재', hasattr(s, 'PHASE1B_ENABLED')); print('틱구동', s.TICK_ENTRY_ENABLED)"
```
기대값(**2026-08-06 [A][B][C][D] 수정 기준**):
```
1A 09:00:00 ~ 14:50:00        PB 09:00:00 ~ 14:50:00        중복전환 10:30:00
청산 15:10                    슬롯 8 0 6 8   <- [E] 눌림 0(매매중단)/1A 8   무장 100.0 3.0
버스트 40000000 2 100000000   시간계수 [1.0]   <- 값이 1.0 하나뿐이어야 정상
버스트방향 True   <- [B] 매수 체결만 버스트로 센다. False면 투매도 세던 구버전
VWAP깊은검사 True 0   <- 🆕 08-08. 계획 생성 시 **가장 깊은 트랜치가(-0.7%)**로
초반캡 True 09:05:00 4   <- 🆕 08-08. 09:05 이전엔 슬롯을 4칸까지만 쓴다
발사분리 True 09:05:00 2.5 30.0 120.0 20   <- 🆕 08-08. **발사 조건이 시간대로 갈린다**
주가계수 10000.0 0.55 0.3 3.0  <- 2,000원 x0.41 · 10,000원 x1.00 · 50,000원 x2.42
되돌림 True ((0.003, 0.5), (0.007, 0.5)) 120.0   <- -0.3% / -0.7%
상승이탈 False 0.003   <- [D] 08-06에 OFF. 폭 상수는 보존(되살릴 때 재사용)
등락률하한 0.0        <- [C] 전일종가 대비 이 값 '초과'여야 매수 (= +일 때만)
주가상한 100000       <- 🆕 08-13 사용자 지정. 이 가격 **이상**은 신규 매수 안 함.
진입숙성 60.0        <- [F] 편입 후 이 초가 지나야 매수(기본값)
숙성조건식별 {'돌파전': 30.0}   <- 🆕 08-11 조건식 개편으로 키 변경(돌파자동매매용
확인전용 ('돌파후',)   <- 🆕 08-11. `돌파후` 단독 편입은 **매수하지 않는다**
파동상한 3            <- [H] 그날 그 종목의 4번째 이후 거래대금 폭발은 매수 안 함
파동카운트 True        <- 🆕 08-09 결함수정. **파동은 '폭발 횟수'지 '게이트 통과
분할매도 True 0.5 0.03
지수가드 -5.0 11:00:00 11:30:00 14:50:00
캡 0.06 0.06 0.06 0.08        본전스톱 True   1B잔재 False   틱구동 True
본전스톱무장 0.025      <- ⚠️ **08-15부터 비활성**(확정익절이 대체). 값은 롤백용으로
확정익절 True 0.02      <- 🆕🆕 08-15 사용자 지정. **본전스톱을 대체한다**(상호 배타).
본전스톱분할 True       <- 🆕 08-14 사용자 지정. ⚠️ **08-15부터 확정익절이 이 스위치를
본전스톱바닥 0.01       <- 🆕 08-13 사용자 지정. 0.002 -> **0.010** ('본전'이 아니라 +1% 확보).
정체시간정리 False     <- 🆕 08-10 **OFF**. 8건 -20,560원 vs 종가보유 +25,715원.
변동성손절 True 120.0 20 1.5 -0.045 -0.055
고정손절 -0.045       <- 🔴 08-11 **손절 -3% -> -4.5%** (사용자 지정).
추가매수관찰 15.0 0.055 0.06   <- 🔴 관찰바닥을 0.045 -> **0.055**로 같이 내렸다.
물타기 True 0.03 2000000 True   <- 🆕 08-11 **-3% 물타기**(사용자 지정).
물타기컷오프 11:00:00 3 5.0   <- 🆕 08-12 사용자 지정 + 결함수정.
강제청산 False 15:10   <- 🔴🔴 08-12 사용자 지정 **시간 기반 자동청산 전면 폐지**.
지수가드강제청산 False   <- 🆕 08-12. 지수 -5% 날의 14:50 강제청산도 같이 껐다.
진입유효창 1800.0      <- 🆕 08-12 장마감 후. **편입 후 30분 지나면 매수 포기**.
VI분할 True            <- 🆕 08-12 장마감 후. VI 상단 확정매도를 **50% 분할**로.
분할잔량보호 True       <- 🆕 08-12 **결함수정**. 분할 1차 후 동적캡이 재발동하면
분할잔량하한 False      <- 🔴 08-18 신설했으나 **반증에서 전제가 깨져 OFF 보류**(잔량 91.8%가 같은 초에 즉시 청산).
편입가앵커 False        <- 🔴 08-12 저녁 **OFF로 되돌림(검증이 틀렸다)**.
오버나이트격리 True     <- 🆕 08-12. 장을 넘긴 보유분은 **다음 기동 때
잔고지연가드 45 300 120  <- `main.py`의 세 유예. 앞의 둘은 08-07·08-12에 만든
반등소진매도 True 0.0   <- 🆕 08-10. **본전 이상에서만** 발동(구 사양은 손실 구간 전용).
우선순위교체 0         <- 🆕 08-10 **완전 OFF**. 115초에 일일한도 6회를 소진하고
VI True 0.1 0.005 2           VI매수차단 False 0.03   <- 08-06 사용자 지정으로 OFF
VWAP진입 True 0.5 09:05:00 1000000.0   <- 08-07 장마감 후 **ON**. 단위는
일봉눌림태그 True 3.0 0.6 20 5 2   <- 08-07 신설. **관측 전용**(매매 무영향).
시가대비 8.0   급등강화 6.0 1.5   <- 6~8%는 버스트 1.5배, 8%~는 매수보류
  ※ 각 값의 **근거·실측·롤백 상세**는 `docs/EXPECTED_VALUES_RATIONALE.md`.
     (값 줄은 여기 그대로 두고 설명만 뺐다 — 감사 [9]는 이 값 문자열을 대조한다)
```
**거래소 구분 확인 (2026-08-07 신설)** — 조건검색만 다른 기준이면 조용히 어긋난다:
```bash
python -c "from api.kiwoom_ws import CONDITION_STEX_TP as C; print('조건검색', repr(C), '(K=KRX / A=통합)'); print('※ 틱·분봉·주문은 항상 KRX 전용이다 — C가 A면 유니버스만 통합이라 어긋난다')"
```
기대값: `조건검색 'K' (K=KRX / A=통합)` — **HTS [0156] '대상'도 KRX여야 한다.**

**실전 전환 전용 확인 (2026-08-05 신설)** — 위 명령과 별도로 반드시 같이 볼 것:
```bash
python -c "from config import settings as s; import core.strategy_manager as SM; from core.strategy.portfolio_optimizer import DEFAULT_BASE_AMOUNT as D; from core.order_manager import BUY_AMOUNT_PER_STOCK as B; print('IS_MOCK', s.IS_MOCK); print('매수금액 3곳', SM.POSITION_AMOUNT, D, B, '일치' if SM.POSITION_AMOUNT==D==B else '!!!불일치!!!')"
```
버스트 주가 스케일까지 한 번에 보는 명령 (2026-08-05 신설):
```bash
python -c "import core.strategy_manager as s; print('주가계수', s.BURST_PRICE_REF, s.BURST_PRICE_ALPHA, s.BURST_PRICE_MIN, s.BURST_PRICE_MAX); print('재매수배수', s.REBUY_BURST_VALUE_MULT); [print(f'{p:>7,}원 x{s.burst_price_scale(p):.2f} -> 절대 {s.PHASE1A_BURST_TRADE_VALUE*s.burst_price_scale(p)/10000:>6,.0f}만 / 단일 {s.PHASE1A_SINGLE_TRADE_VALUE*s.burst_price_scale(p)/1e8:.2f}억') for p in (1000,2000,5000,10000,20000,50000,150000)]"
```
기대값: `주가계수 10000.0 0.55 0.3 3.0` / `재매수배수 2.5` /
`1,000원 x0.30` · `2,000원 x0.41` · `10,000원 x1.00` · `50,000원 x2.42` · `150,000원 x3.00`.
- 상한(3.00)은 **73,704원 이상**에서만 걸린다(`10,000 x MAX^(1/ALPHA)`). 즉 08-05에
  실제로 발화가 지연된 마키나락스(25,900) · GS건설(29,750)은 **상한 영향 밖**이다.
- `BURST_PRICE_ALPHA = 0.0`으로 두면 전 종목 계수 1.0 = **현행(스케일 없음)으로 완전 복귀**.

기대값: `IS_MOCK False` / `매수금액 3곳 1000000 1000000 1000000 일치`
- `IS_MOCK True`면 모의로 돌아간 것.
- **매수금액이 3곳에 흩어져 있다**. 실제로 지배하는 값은 `DEFAULT_BASE_AMOUNT`
  (main.py가 `PortfolioOptimizer(rest_api=...)`로 base_amount를 안 넘김).
  `POSITION_AMOUNT`는 optimizer 실패 시 fallback, `BUY_AMOUNT_PER_STOCK`은 레거시.
  **하나만 고치면 조용히 어긋난다.**
- `시간계수 [1.0]`에 1.0 외의 값이 섞여 있으면 **오후 완화가 되살아난 것**
  (2026-08-04에 0.85/0.80/0.95를 전부 제거했다).
- `버스트`가 30000000이면 08-04 상향 전으로 돌아간 것.
- `1B잔재 True`가 나오면 1B 코드가 되살아난 것 — 2026-08-02에 전부 삭제했다.

\### 모바일 확인 항목 (원격 세션이 제대로 붙었는지)
폰에서 세션에 들어간 뒤 아래를 확인한다. **PC 검증과 목적이 다르다** —
여긴 "연결과 맥락"을, PC 검증은 "코드 건강"을 본다.

1. **세션 카드에 `🖥 연결됨 · jokings76/AutoTrader_Project`가 보이는지**
   (안 보이면 PC 터미널 창이 닫혔거나 네트워크가 끊긴 것)
2. **작업 폴더가 맞는지** — 세션에 이렇게 물어본다:
   ```
   지금 작업 폴더가 어디야? 파일 목록 보여줘
   ```
   `main.py`, `core/`, `CLAUDE.md`가 나오면 정상.
3. **맥락을 이어받았는지** — 이렇게 물어본다:
   ```
   CLAUDE.md 읽고 지금 살아있는 전략이랑 진입 게이트 순서 요약해줘
   ```
   **살아있는 전략은 1A 하나**(눌림목은 [E]로 슬롯 0 = 매매 중단),
   진입은 `편입(돌파후 단독은 매수 X) -> 숙성(돌파전 30초/그외 60초) -> 등락률 0~13% -> VWAP +0.5% ->
   무장 3.0초 -> **발사(09:00~09:05 면제 / 09:05~ 거래대금 가속 2.5배)** ->
   파동 1~3번째 -> 되돌림 -0.3%/-0.7% 분할`,
   15:10 강제청산이 나오면 이관 성공.
   ⚠️ **"1A/Pullback 2전략 체제"라고 답하면 옛 문서를 읽은 것**이다
   (08-06 [E] 이전 사양). 그 경우 이 문서를 다시 손봐야 한다.
3-b. **청산 사양도 물어볼 것** (08-10에 6개가 한꺼번에 바뀌었다):
   ```
   손절선이랑 익절캡, 익절 확정 지점이 지금 얼마야?
   ```
   **손절 = 장중 120초 고저폭 x1.5(-4.5~-5.5%, 틱 부족 시 -4.5%) /
   익절캡 6% / 🆕 **확정익절 순 +2.0%에서 50% 분할**(잔량 3% 트레일) /
   정체·시간정리 OFF / 원가 -3%면 동일수량 물타기 1회(**11:00까지**) /
   구조분 최종선 원가 -6% / 🔴 **시간 기반 자동청산 없음(15:10 강제청산 폐지)** /
   **VI 상단도 50%만 분할매도**하고 잔량은 3% 트레일**이 나와야 한다.
   ⚠️ **"본전스톱 무장 +2.5%"라고 답하면 08-15 이전 문서를 읽은 것**이다
   (08-15에 확정익절 2%가 본전스톱을 대체했다).
   ⚠️ **"손절 -3% 고정"이라고 답하면 08-11 이전 문서**,
   **"15:10에 전량 강제청산"이라고 답하면 08-12 이전 문서**를 읽은 것이다.
3-c. **진입 사양도 확인** (08-12 저녁에 상한이 생겼다):
   ```
   편입되고 한참 지난 종목도 사? 진입 유효창이 어떻게 돼?
   ```
   **편입 후 30초~30분 안에만 매수**(숙성 하한 + `MAX_ENTRY_AGE_SEC` 상한)라고
   나와야 한다. "상한은 없다"고 답하면 08-12 이전 문서다.
4. **모델 확인** — 원격 세션은 Sonnet 5로 시작한다. 전략/로직 작업이면
   `/model`로 **Opus 5**로 올리고 시작할 것.
5. **장중 주의** — 봇이 돌고 있는 09:00~15:10에는 매매 로직 파일 수정 금지.
   로그 조회·상태 확인·진단 알림 해석 정도만 하고, 코드 수정은 장 마감 후.

\### 이관이 끝났다고 판단하는 기준
- PC 검증 표 **22항목 전부 통과**(= 스위트 **19개** + `import main` + git 2건)
  (⚠️ `NEW_SESSION_REQUESTED`를 만들어 둔 상태에서만 #6이 1건 실패가 정상.
   **2026-08-15 검증 시점엔 플래그가 없으므로 #6도 전원 통과여야 한다**)
  \+ 설정값 출력이 기대값과 일치
- 모바일 확인 1~3번 통과(특히 3번 — 맥락 복원이 이관의 본질)
- 위 둘 중 하나라도 실패하면 **아직 이관 안 된 것**이다.

\## 현재 전략 요약 (**2026-08-15 기준 — 🆕 확정익절 2% x 50% 분할이 본전스톱을 대체**)

> 🔴 **눌림목(Pullback)은 [E]로 슬롯 0 = 매매 중단 상태다.** 조건검색 구독과
> 라우팅·평가는 그대로 돌아 데이터는 계속 쌓인다. 아래 표의 Pullback 열은
> '되살렸을 때의 사양'으로 읽을 것.

> ⛔ **봇은 지금 정지 상태다.** 마지막 정상 거래일은 **08-14(금)**로
> 청산 8건 / DB +48,947원(그중 **본전스톱 5건**). 08-15(토) 19:38에 주말
> 오기동이 있었으나 **9초 만에 정상 종료**했고, 같은 날 트리거를 월~금으로
> 좁혔다. **다음 기동은 08-17(월) 08:59**(요일마스크 62, NextRun 실측 확인).
> 스위트 **1,912건 통과 / 실패 0**(19개, 08-15 저녁).
> ⚠️ ~~스케줄러는 '매일'이라 **토·일에도 08:59에 봇이 뜬다**~~ ->
> **2026-08-15에 트리거를 월~금으로 좁혔다.** 이제 주말엔 안 뜬다.
> (봇 자체에는 여전히 **주말 가드가 없다** — 트리거로만 막는 것이라, 주말에
>  수동 기동하거나 트리거를 되돌리면 그대로 돈다. 다만 체결틱이 없어 매수는
>  원리적으로 불가능하다.) 장전 스캐너에는 코드 레벨 주말 가드가 있다.
> ✅ **미청산 3종목은 08-06 저녁에 `status='manual'`로 전환해 봇 관리에서
> 뺐다**(주성/다스코/금호타이어). `find_holdings()`가 `'holding'`만 조회하므로
> 재가동해도 **복원되지 않고, 손절·익절·15:10 강제청산 어디에도 안 걸린다.**
> ⚠️ **08-10 이관 시점에도 3건 그대로 남아 있다**(HTS 매도 미확인). 실계좌엔
> 그 외에 다윤님 개인 보유 **우리기술 71주**가 있다(봇과 무관, 08-10 확인) —
> 사용자가 HTS에서 판 뒤 `python manual_position_tool.py close <코드> <체결가>`로
> 기록해야 통계에 반영된다. 현황은 `python manual_position_tool.py list`.

> ⚠️ **실전투자다** (2026-08-05 전환). 종목당 매수 **100만원**이고
> (50만 -> 200만(08-05 장마감) -> **100만(08-06)**, 전부 사용자 지정)
> optimizer 비중 0.90이 곱해져 실제로는 **종목당 약 90만원**,
> 되돌림 1차 트랜치 **약 45만원**이다.
> 슬롯은 **1A 8 / 눌림 0 / 공유 6 / 확장 8**([E]로 눌림 매매 중단).
> MDD(-3%)·시간창은 모의와 동일하다.
> ⚠️ **노출** — 슬롯 6 만석이면 약 **540만원**으로 예수금(약 1,080만)의 절반이다.
> 200만원이던 08-05~06에는 만석 = 1,080만원이라 **예수금이 실질 슬롯 상한**으로
> 작동했는데, 100만원으로 내리면서 그 제약이 풀렸다(초과분은 키움이 거부할 뿐
> 오주문은 아니다). 실측 동시보유 최대는 2/6이라 어느 쪽이든 도달한 적은 없다.
>
> **08-05는 실전 첫날이자 수술이 10건 몰린 날이다.** 진입은 ±0.3% 밴드로,
> 청산은 손절 대체(추가매수)와 손실반등 분할로 바뀌었다. 아래 "08-05에 바뀐 것"
> 참고. ⚠️ 그중 **손실반등 분할 / 시가필터 / 재매수 완화 3건은 08-06 08:59
> 재기동부터 처음 실거래에 반영**된다(08-05엔 코드만 들어갔다).

> **⚡ 08-05에 바뀐 것 (실전 전환 + 수술 10건 + 장마감 후 4건)**
> 0. **[장마감 후]** **버스트 주가 스케일**(계수 = clamp((주가/1만)^0.55, 0.30, 2.00)
>    — ⚠️ 상한 2.00은 **그날의 값**이고 08-06에 **3.00**으로 올랐다)
>    / **매수금액 200만 환원** / **VI 상단 확정매도** / **감사 결함 3건 수정**
> 1. **실전투자 전환** — 매수금액은 위 0번대로 **200만원**, 예수금은 D+2 반영
> 2. ~~조건검색 `stex_tp="A"`(통합)~~ -> **08-07에 `"K"`(KRX)로 되돌림**
>    (계층 정합성 — 아래 히스토리 2026-08-07 참고). HTS '대상'도 KRX로 맞출 것
> 3. **시장가 문턱 5,000만 -> 1,000만** (미체결 3건이 +11%/+15%/+5.7%를 놓쳤다)
> 4. **되돌림 -0.5/-1.0% -> -0.3/-0.7%** + **상승 이탈 +0.3%**(±0.3% 대칭 밴드)
> 5. **손절 대신 추가매수**(관찰 15초 -> 반등확증) / **본전바닥 +0.2%** / **1A 상한 13%**
> 6. **손실반등 매도 전량 -> 50% 분할** (08-05 최대 손실 요인이었다)
> 7. **시가대비 필터 부활**(그동안 캐시가 비어 한 번도 발동 안 했다)
> 8. **손실청산 종목 조건부 재진입**(60분+1.6억+상대경로 금지, 종목당 1회)
> ⚠️ 6·7·8은 **08-06 08:59부터 처음 실거래에 반영**된다.
>
> **⚡ 08-04에 바뀐 것 5줄 요약** (상세는 히스토리 "2026-08-04" 항목들)
> 1. **본전스톱 ON** (순+1% 찍고 본전 이탈 시 청산) — 08-03 저녁 OFF에서 되돌림
> 2. **되돌림 대기 분할매수** — 트리거 즉시매수 폐지. -0.5%에 50% / -1.0%에 50%,
>    120초 미도달 시 포기. **1종목 = 1슬롯**(분할해도 슬롯·금액 불변)
> 3. **분할매도** — 동적캡(강도·거래량 동시하락) 시 전량이 아니라 **50%**만,
>    잔량은 고점 대비 **3% 트레일**
> 4. **버스트 문턱 3천만 -> 4천만** (절대 경로 2건 = 0.8억)
> 5. **시간대 계수 전부 1.00** — 오후 완화(0.85/0.80/0.95) 제거

**두 전략이 같은 트리거를 쓴다.** 조건검색식이 '어떤 종목인가'를 전부 판정하고
(3분봉 30이평 근접·눌림폭·급등이력·유동성 100억+), 코드는 '지금이 그 순간인가'만
본다 — 즉 코드가 보는 건 **체결강도와 거래대금 둘뿐**이다. 전략 차이는 유니버스와
슬롯·상한·익절캡·시간창에만 있다.

| 항목 | 1A | Pullback |
|---|---|---|
| 조건검색식 | 주도주상위, **돌파전** (+ `돌파후`는 **확인 전용** — 단독 편입으론 매수 안 함) | **눌림목자동 전용** |
| 시간창 | 09:00 ~ 14:50 | **09:00 ~ 14:50** (2026-08-03: 1A와 완전 동일) |
| 진입 조건 | **무장**(체결강도 FID228 100+ **3.0초 연속**) → **발사**(🆕 **09:00~09:05 면제 / 09:05~ 거래대금 가속 2.5배**, 08-08) → **되돌림 대기**(-0.3%/-0.7% 분할매수, 120초) | **1A와 동일** |
| **발사 게이트** | 09:05 이전 면제 · 09:05부터 `value_acceleration(30초/120초) >= 2.5` + **최소 20틱**. ~~대량체결 버스트~~는 신규 진입에서 빠졌다(재매수·슬롯교체엔 그대로 남음) | 동일 |
| **등락률 하한(전일종가)** | **+0% 초과** — 매수는 무조건 + 일 때만 (2026-08-06 [C]) | 동일 |
| 🆕 **매수 주가 상한** | **100,000원 이상은 신규 매수 안 함** (2026-08-13 사용자 지정) | 동일 |
| 등락률 상한(전일종가) | **13%** (08-05에 16%→13%) | **10%** |
| **버스트 방향** | **매수 체결만** 센다 (2026-08-06 [B]. 투매를 매수 신호로 읽던 결함) | 동일 |
| ~~상승 이탈 즉시매수~~ | **현재 OFF** (2026-08-06 [D]. 실측 평균 -1.00%/승률 22%) | 동일 |
| ~~VI 상단 근접 매수차단~~ | **현재 OFF**(2026-08-06 사용자 지정). 로직은 보존 | 동일 |
| **시가대비 급등 보류** | **+8% 이상 매수보류** (08-06에 5%→8% 완화) | 미적용(되돌림을 사는 전략) |
| **시가대비 6~8% 버스트 강화** | 버스트 문턱 **x1.5** (매수를 막지 않고 대금만 더 요구) | 동일 |
| 슬롯 | **8** | **0** — [E] 매매 중단(공유 상한 6, 확장 8) |
| **진입 숙성(하한)** | 편입 후 **60초**(단 **돌파전은 30초**) | 동일 |
| 🆕 **진입 유효창(상한)** | 편입 후 **30분** 초과면 매수 포기(`MAX_ENTRY_AGE_SEC`) | 동일 |
| **VWAP 이격** | 09:05부터 현재가가 세션VWAP보다 **+0.5% 초과** 위여야 매수 (08-07 ON) | 동일 |
| **일봉 눌림 태그** | 관측 전용 — entry_reason에 `[일봉눌림 D-N]`만. **매매 무영향** | 동일 |
| **버스트 파동 상한** | 그날 그 종목의 **1~3번째 폭발**까지만 매수 (2026-08-06 [H]) | 동일 |
| 중복 편입 시 | ~10:30 | 10:30~ |
| 익절 캡 | **6.0%** (개장초반도 6.0%) | **6.0%** | ← 08-10 전면 상향
| 상향 캡(강도상승/주도테마) | **8.0%** | 8.0% | ← 기본캡과 같아지면 무의미해져 같이 올림
| 손절 | **장중 120초 고저폭 x1.5** (클램프 **-4.5~-5.5%**, 08-11 확대. 틱 20개 미만이면 고정 **-4.5%**) (**워밍업 중에도 작동**) | 동일 |
| 🆕 **확정익절** | **순 +2.0% 도달 즉시 50% 분할매도**, 잔량은 고점대비 3% 트레일 (08-15 신설, **본전스톱 대체**) | 동일 |
| ~~본전스톱~~ | **OFF** — 08-15에 확정익절로 대체됐다(상호 배타). 상수·로직은 보존돼 있고 `FLAT_TP_ENABLED = False` 한 줄로 복귀(무장 +2.5% / 바닥 +1.0%) | 동일 |
| 정체·시간정리 | ~~15분 / 30분~~ **OFF** (08-10) | 동일 |

\### 진입 절차 (2026-08-04 전면 변경 — 이게 지금 가장 중요한 흐름)

```
편입(조건검색) -> pre-arm(감시 시작 + 전일종가/시가/**일봉** 캐시 예열)
   |          ** 이 순간부터 [F] 숙성 시계가 돈다 **
   |          ** 일봉 예열은 종목당 하루 1콜(ka10081) — 태그 관측용 **
   v
[무장/발사가 성립해도] 편입 후 숙성시간 안이면 **그 신호는 버려진다** ([F])
   |   요구시간: **돌파전 30초 / 그 외 60초**
   |   복합 편입이면 가장 짧은 값. 조건식을 모르면 60초(보수적).
   |   ⚠️ 'N초 기다렸다 산다'가 아니라 '그 트리거를 포기한다'.
   |      다시 터지면 그때 진입한다.
   v
[진입 유효창 상한]  편입 후 **30분**이 지났으면 그 종목은 그날 매수 포기 (2026-08-12)
   |   `_entry_delay_reject`가 숙성과 **같은 함수**에서 판정한다(호출부 2곳 자동 적용).
   |   사유 문구가 `진입 유효창 만료`라 숙성과 진단에서 구분된다.
   |   근거: 8일 104건 — 1~3분 매수 MFE +7.00%(최고) vs 30분~ +1.79%(절반이 못 먹음).
   v
[주가 상한]  현재가가 **100,000원 이상**이면 매수하지 않는다 (2026-08-13)
   |          `_entry_change_reject()` **맨 앞**에서 본다 — 전일종가 판정보다
   |          앞이라, 전일종가를 몰라도 상한은 살아 있다(결합 방지).
   |          근거: 10만+ 8건 평균 -1.87%·중앙 -2.08%·손절률 62%, 4/4일 열위.
   |          기전은 '비싼 게 나쁘다'가 아니라 **비쌀수록 발사 게이트가 헐거워진다**.
   v
[등락률 게이트]  전일종가 대비 **0% 초과 ~ 상한(1A 13% / 눌림 10%)**
   |             <- 2026-08-06 [C]. 하한 미달(하락중)이면 여기서 끝.
   |                `_entry_change_reject()` 한 함수가 세 경로 전부를 담당한다.
   v
[VWAP 이격]  09:05부터, 현재가가 **세션VWAP +0.5% 초과** 위여야 통과
   |          <- 2026-08-07 ON. 원천은 0B의 FID14(백만원)/FID13. REST 0콜.
   |             09:05 이전 / VWAP 미수신이면 **통과**(모름이 매수를 막지 않는다)
   v
[무장]  FID228 체결강도 100 이상 3.0초 연속        <- 느린 조건을 먼저
   |
   v
[발사]  🆕 **시간대에 따라 규칙이 다르다** (2026-08-08, `_fire_gate` 단일 창구)
   |
   |   ┌ 09:00 ~ 09:05 ─ **게이트 면제**. 무장만으로 발사한다.
   |   |    ⚠️ 대신 **슬롯을 4칸까지만** 쓴다(EARLY_SLOT_CAP) — 발사가
   |   |       열려 있으므로 개수로 막는다. 남는 2칸은 09:05 이후용이다.
   |   |    '거를 게 없어서'가 아니라 **걸수록 나빠져서**다 —
   |   |    그 구간 버스트는 독립표본 -0.895%(1/3)로 기준선(+0.391%)보다 나쁘다.
   |   |    리스크는 숙성·등락률·파동상한·되돌림 대기가 받는다.
   |   |
   |   └ 09:05 ~ ────── **거래대금 가속도 >= 2.5배**
   |        `value_acceleration(최근 30초 / 120초 균일기대치)`
   |        + **최소 20틱**(틱이 적으면 가속도가 수학적 최대 4.0으로 튄다)
   |        ⚠️ **절대 금액 하한은 두지 않는다** — 실측이 정반대다
   |           (하한 1억 -0.049% / 3억 -0.217% / 5억 -0.572%).
   |
   |   ⚠️ ~~대량체결 버스트~~는 **신규 진입에서 빠졌다**(4일 독립표본 -0.349%,
   |      플러스일 1/5로 기준선보다 나빴다). `check_burst` 자체는 **재매수
   |      판정·슬롯 교체용으로 그대로 살아있다** — 거기까지 바꾸지 말 것.
   |   롤백: `FIRE_GATE_SPLIT_ENABLED = False` -> 전 구간 옛 버스트 3경로.
   v
[파동 순번]  그날 그 종목의 **1~3번째 폭발**까지만 ([H])
   |      쿨다운 60초로 같은 파동은 하나로 접는다. 4번째부터 매수 금지.
   |      (상한을 넘어도 카운트는 계속 돌아 순번이 정확하게 유지된다)
   v
[되돌림 대기]  120초
   |      · -0.3% 도달 -> 50% 매수, -0.7% 도달 -> 나머지 50%
   |      · 120초 안에 안 오면 포기(`되돌림 미도달`)
   |      · ~~+0.3% 돌파 시 즉시 전량~~ -> **2026-08-06 [D]로 OFF**
   |        (추격매수라 실측 평균 -1.00%. `ENTRY_BREAKOUT_ENABLED=True`로 복귀)
   |      (대기 중에도 슬롯 1칸 점유 — 자리를 뺏기지 않기 위해)
   v
매수 체결 -> entry_reason에 `[일봉눌림 D-N 돌파선 P]` 태그(있으면, 관측 전용)
   |          ** 태그는 매수 여부/수량/캡 어디에도 영향이 없다 **
   v
워밍업 60초 -> 청산 규칙 적용
```

- **1종목 = 1슬롯**이다. 분할해도 슬롯 개수·매수금액 총액은 변하지 않는다
  (`occupied_slots()`가 집합 합집합이라 중복 제거, 트랜치 비중 합 = 1.0).
- **시간대 계수는 전 구간 1.00**이라 (1)(2) 경로의 문턱이 시각과 무관하다.
  대신 **주가계수는 종목마다 다르다** — 같은 시각에도 2,000원짜리는 1,650만원,
  3만원짜리는 7,320만원이 절대 문턱이다. 로그의 `경로=` / 계수값을 같이 볼 것.
- **진입 경로가 둘**이다: 틱 경로(`_maybe_tick_entry`)와 폴링 경로
  (`_evaluate_1a_pullback_entry` <- `on_condition_hit`/`watchlist_reentry`).
  **둘 다 되돌림 대기를 거친다** — 08-04에 폴링만 즉시매수하던 버그를 고쳤다.
  진입 로직을 고칠 땐 **반드시 두 경로 모두** 확인할 것.

\### 청산 규칙 한눈에 (2026-08-04 본전스톱 재활성화 반영)

우선순위 순서다. 위에서 걸리면 아래는 안 본다.

| 순위 | 규칙 | 조건 | 비고 |
|---|---|---|---|
| **0** | 🆕 **-3% 물타기** | 원가 대비 **-3%**면 조건 없이 **동일 주식수 1회** 추가매수 | 손절보다 **먼저** 본다. 종목당 총 200만원 캡 / 당일 매수분만 / MDD·지수가드는 존중. ⚠️ 백테스트 판정 불가 — 실거래 채점 중 |
| **0-b** | 🆕 **구조 후 최종선** | 물타기·추가매수한 포지션이 **원가 -6%** 도달 | 🔴 손절 분기 **밖**에 있다(08-11). 안에 두면 평단 손절선(원가 -5.93%)이 더 깊어 영영 도달하지 않는다 |
| 1 | **손절** | 가격 **최근 120초 고저폭 x1.5** (클램프 **-4.5~-5.5%**, 08-11 확대. 틱 부족 시 고정 **-4.5%**) | **워밍업 중에도 작동**(08-03). (08-05) 거래대금 3배+강도상승이면 **15초 관찰 후 반등 확증 시 추가매수**로 대체 — 미구조분은 최대 **-5.5%**, 구조분은 **원가 -6%**가 최종선 |
| 2 | **지수 가드** | -5% & 11:00~ | 11:30까지 본전 이상 청산 / 14:50 강제청산 |
| 2-b | **VI 상단 매수차단** | **현재 OFF**. 되살리려면 `VI_UPPER_ENTRY_BLOCK_ENABLED = True`. 폭은 `VI_UPPER_ENTRY_BLOCK_PCT`(0.03) |
| **시가대비 상한** | `PHASE1A_LEADING_OPEN_SURGE_CAP`(8.0, 구값 5.0) |
| **급등 버스트 강화** | `PHASE1A_OPEN_SURGE_STRICT_FROM`(6.0) / `PHASE1A_OPEN_SURGE_BURST_MULT`(1.5). 배수를 1.0으로 두면 무효화 |
| **VI 상단 확정매도** | 정적VI 상단(시가 x1.10)까지 **0.5% 이내 또는 2호가 이내** + **순이익>0** -> 🆕 **50%만 분할매도**(08-12), 잔량은 3% 트레일 | 2026-08-05 신규. VI 발동 시 2분 단일가라 시장가 매도가 막히는 것을 미리 피한다. **시가 캐시가 없으면 발동 안 함**(전일종가 폴백 금지), **이미 VI선을 넘었으면 발동 안 함**. 워밍업보다 앞(순수 가격 판정). 하단 VI 로직은 없다(손절이 항상 먼저).<br>🆕 **08-12에 50% 분할로 바꿨다** — 8일 9건에서 실현 +2.37%인데 판 뒤 30분 **+5.67%**, **78%가 +3% 이상** 더 갔다(전 청산 사유 중 가장 일찍 판다). 목적이 익절이 아니라 **VI 발동 회피**이므로 절반만 빼고 절반은 추세에 남긴다. 이미 분할이 나간 포지션이면 잔량 전량(리스크 회피 우선). 롤백 `VI_UPPER_EXIT_PARTIAL=False` |
| 3 | (워밍업 60초 — 아래는 여기서 대기) | | 강도·거래량 기반 판정만 유예 |
| 4 | 🆕 **확정익절** | **순 +2.0% 도달 즉시 50% 분할매도**, 잔량은 3% 트레일 | 🆕 **08-15 사용자 지정 — 본전스톱을 대체했다(상호 배타).** 되돌아오길 기다리지 않는다. 근거: 150건/8일 종일분봉에서 현행 본전스톱은 비관 0.326 / 낙관 **-0.240**인데 이 안은 0.360 / -0.046(승률 55%->71%, 날짜별 비관 5승3패·낙관 6승2패). 🔴 **다른 규칙(동적캡·VI·반등소진)이 남긴 잔량은 건드리지 않는다** — 그 잔량은 트레일이 받는다. 롤백 `FLAT_TP_ENABLED=False` |
| ~~4-b~~ | ~~본전스톱~~ | ~~순 +2.5% 도달 후 순 +1.0% 이탈 시 50% 분할~~ | **08-15에 확정익절로 대체됨.** 로직·상수(`BREAKEVEN_*`)는 보존돼 있고 `FLAT_TP_ENABLED = False` 한 줄로 즉시 복귀한다 |
| 5 | 익절 캡 | **1A 6.0% / 눌림·개장초반 6.0% / 상향 8.0%** (08-10 상향) | 지수 -5%면 1.5%로 축소. 실질 천장은 **VI 상단** |
| 5-b | **분할 잔량 트레일** | 분할 1차가 나간 포지션이 고점 대비 **3%** 하락 **또는 1차 매도가 아래로 하락** | 2026-08-04 신규. 🆕 **08-12부터 잔량은 동적캡으로 안 판다**(`PARTIAL_EXIT_REMAINDER_HOLD`) — 예전엔 분할 1차 후 **44~55초 만에** 동적캡이 재발동해 잔량이 전량 털렸다(일성건설 45초/마녀공장 44초/한켐 54초). 이제 잔량은 **이 트레일 + 손절/최종선/지수가드**만 받는다 🆕🆕 **08-18 잔량 하한 신설** — 발동가 = `max(고점x(1-3%), 1차 매도가)`. 3% 트레일이 1차가보다 높게 팔리려면 고점이 1차가 대비 **+3.09% 이상**이어야 하는데, 확정익절은 +2%에서 나가므로 그러지 못하면 트레일이 **반드시 1차보다 싸게 판다**(실측 5건 중 3건). 상방은 안 깎는다. 롤백 `PARTIAL_EXIT_FLOOR_AT_FIRST=False` |
| 6 | 동적캡 → **분할매도 50%** | **상향된** 포지션 + 강도·거래량 동시하락 + **순이익>0** | 08-03 결함 3건 수정 / **08-04부터 전량이 아니라 절반**, 잔량은 5-b로 |
| 7 | **반등소진 매도** | 저점 대비 +1% 반등이 강도·거래량으로 미확인 **+ 순손익 0% 이상**(08-10 신설 — 손실 구간은 손절이 전담) | **50% 분할**(2026-08-05 변경, 잔량은 5-b 트레일). 08-05에 6건 중 5건이 이후 상승해 전량청산이 최대 손실 요인이었다 |
| 8 | ~~정체 정리 15분 / 시간정리 30분~~ | **현재 OFF**(08-10). 되살리려면 `STAGNANT_EXIT_ENABLED = True` | 8건 -20,560 vs 종가보유 +25,715 |
| 9 | 슬롯 교체 | **만석 + 후보 무장 + 후보 버스트** | 08-03 변경, 사실상 거의 안 돎 |
| 10 | ~~강제청산 15:10~~ | 🔴 **폐지 (2026-08-12 사용자 지정)** | 아래 참고 |

🔴 **시간 기반 자동청산이 전부 없다 (2026-08-12).** `FORCE_CLOSE_ENABLED=False` /
`INDEX_GUARD_FORCE_CLOSE_ENABLED=False` / `STAGNANT_EXIT_ENABLED=False`.
즉 위 표의 **가격 기반 규칙(0~7) 중 아무것도 안 걸린 종목은 장을 넘긴다.**
- 사용자 지정: "15:10 강제청산은 없는걸로 — 내가 수동매도하던지 오버나이트 할 수 있게".
- 넘어간 보유분은 **다음 기동 때 `status='manual'`로 격리**되어 봇 관리에서 빠진다
  (`OVERNIGHT_RESTORE_AS_MANUAL=True`). 그 뒤론 손절·익절 대상이 아니다.
- ⚠️ **오버나이트 리스크는 손절로 못 막는다** — 갭 하락은 손절선(-4.5%)을
  건너뛰고 시가에 체결된다. 그리고 정리하지 않으면 **매일 쌓여 예수금이 잠긴다**
  (첫 증상은 "신규 매수가 안 된다"). `python manual_position_tool.py list`로 볼 것.
- 롤백: `order_manager.FORCE_CLOSE_ENABLED = True` (+ 지수가드까지 되살리려면
  `INDEX_GUARD_FORCE_CLOSE_ENABLED = True`).

**강도 하락 판정은 전부 `_strength_baseline`(워밍업 후 리앵커)을 쓴다.**
`entry_strength`(진입 순간값)는 버스트 피크라 기준선으로 쓰면 하락이 필연이다 —
08-03에 이 결함으로 동적캡 6건·슬롯교체 3건이 잘못 발동했다.

- **틱 구동 (2026-08-02 전면 전환)** — 진입 평가가 `on_trade`(체결 틱 콜백)에서
  일어난다. 조건이 차는 **그 틱에서** 발화하며 REST 0콜이다.
  - **무장** = 키움 공식 체결강도(FID 228)가 100 이상으로 **3.0초 연속** 유지
    (`TICK_STRENGTH_SUSTAIN_SEC = 3.0`). 08-03 저녁에 3.0 -> 2.0 -> 1.5로
    완화했다가 **08-07에 실측으로 3.0 복원**했다(1.5~2초 구간이 손익비 0.34로
    압도적 최악이었다). **지금 값은 3.0이다.**
    자체 `compute_strength`를 안 쓰는 이유: 틱이 부족하면 정확히 100.0을
    반환하는데 임계값도 100이라 **데이터가 없을수록 쉽게 뚫린다**. 228은
    다윤님이 HTS에서 보는 값과 동일하고 매 틱에 이미 실려 온다.
  - **발사** = 대량체결 버스트 3경로 OR (창 5초):
    ① **4,000만원 × 주가계수** 이상이 **2건** ② **1억원 × 주가계수** 이상이 1건
    ③ max(**①의 문턱**, **평균 1틱 × 20배**) 이상이 2건 ← 자기 정규화
  - **순서가 핵심**: 느린 조건(강도 **3.0초**)을 무장으로, 순간 이벤트(버스트)를
    발사로 둔다. 반대로 하면 버스트를 본 뒤 3.0초를 기다려 그만큼 늦는다.
  - **주가계수**(①②에, ③은 하한을 통해 간접 적용) = `clamp((주가/10,000)^0.55,
    0.30, 3.00)` — **상한은 08-06에 2.00 -> 3.00**(클램프 시작 73,704원). 2026-08-05 마감 후 신설 — 그 전엔 1,000원짜리와 15만원짜리에
    같은 절대 문턱을 적용해 **저가주에서 절대 경로가 사실상 죽어 있었다**
    (발생빈도 124배 차이 실측). ⚠️ **성과 우위는 입증되지 않았다** — 3일
    백테스트에서 08-05만 개선/08-04 악화/08-03 동일이라 이 프로젝트의 강건성
    기준("양일 각각 개선")을 충족하지 못한다. **근거는 성과가 아니라 구조**다.
    나빠지면 `BURST_PRICE_ALPHA = 0.0` 한 줄로 완전 복귀.
  - **시간대 계수는 전 구간 1.00**이다(2026-08-04에 오후 완화 0.85/0.80/0.95를
    전부 제거). 상수(`TICK_BURST_TIME_MULT`)는 남아 있으나 값이 전부 1.0이라
    실질적으로 무효다 — 1.0 외의 값이 보이면 오후 완화가 되살아난 것.
  - **상대 경로의 분모는 최근 5초를 제외**하고 잰다. 안 그러면 지금 터지는
    대량체결이 평균에 섞여 **버스트가 클수록 문턱도 올라가는 자기모순**이
    생긴다(격리 테스트에서 실제로 재현돼 수정함).
- **Pullback의 분봉·점수·VWAP 경로 폐지** — 눌림 판정은 HTS가 이미 했는데
  코드가 1분봉으로 재검증하느라 (a) 시간축이 안 맞아 26종목 전부 탈락하고
  (b) 1분봉 종가를 기다리느라 구조적으로 최대 60초 늦었다. 함수
  (`evaluate_pullback`/`_apply_vwap_filter`/`evaluate_1a_leading_strength`)는
  삭제하지 않고 호출부만 교체했다.

- **라우팅 (2026-08-01 최종)** — 종목당 항상 한 전략만. `resolve_strategy()`
  한 곳에서 결정하고 라우팅·주문가드·백테스트가 모두 이 함수를 호출한다.
  - 눌림목자동 **단독** -> 항상 Pullback
  - 주도주상위/**돌파전** **단독** -> 항상 1A
  - 🔴 **`돌파후` 단독 -> 매수하지 않는다**(확인 전용). `resolve_strategy`는
    여전히 "1A"를 돌려주므로 `_confirm_only_reject`가 따로 막는다
  - **중복(둘 다 편입)** -> 10:30 이전 1A / 10:30 이후 Pullback
    (`DUAL_SOURCE_PULLBACK_FROM` — 오전 모멘텀, 오후 되돌림)
  - 주문 직전 가드는 **단독 소스만** 막는다. 중복 종목이 시간대에 따라 두
    전략을 오가는 것은 정상이므로 여기서 막으면 오전 1A 매수가 전부 죽는다.
- **진입 종료 14:50 / 신규매수 하드컷오프 15:10**(`ENTRY_HARD_CUTOFF`) — 둘 다 **유지**된다.
  ~~보유분 15:10 전량 강제청산~~ -> 🔴 **08-12 폐지**(`FORCE_CLOSE_ENABLED=False`).
  즉 15:10은 이제 '더 안 산다'는 뜻이지 '판다'는 뜻이 아니다.
- 등락률 상한은 **실제 매수한 전략**(sub_strategy)을 따른다 — 중복 종목이
  오전에 1A로 매수되면 **13%**, 오후에 Pullback으로 매수되면 10%.
- **1A/Pullback 외 전략은 코드째 없다** (2026-08-02 삭제, 그 전엔 비활성 상태로
  남아 있었음). 1B/1L은 이날 제거, 1S/Phase2/Phase3/1N은 `_legacy` 백업 후 제거.
  트레일링 익절도 1L 전용이었어서 같이 사라졌다 — 이제 익절은 예외 없이 flat 캡.
  - **⚠️ `phase1b` 객체 자체는 반드시 유지** — 전략이 아니라 1A/Pullback의
    **체결틱·호가 데이터 파이프라인**(TradeFlowTracker/OrderbookTracker)이라
    없애면 두 전략이 눈이 먼다. 이름만 1B다.
  - 이름이 실제로 사고를 두 번 냈다(08-01 1A 버퍼 삭제 / 07-31 1L 조기청산).
    `TickDataPipeline` 같은 리네임이 근본 해결인데 호출부가 ~40곳이라
    **08-03 실거래 검증 후**로 미뤘다.
  - 복구가 필요하면 커밋 `900757c`에 1B/1L 코드와 파라미터 근거 주석이 전부 있다.
- 주문: 매수는 호가 두께로 시장가/지정가 자동 선택, **매도는 항상 시장가**
  (둘 다 실패 시 반대 방식으로 1회 폴백).
- **주문 우선 레인 (2026-08-02)** — 주문(kt10000/kt10001)은 조회 대기열을
  건너뛴다(`_throttle(priority=True)`, 0.2초 간격). 예전엔 진입 트리거가 걸린
  가장 급한 순간에 방금 나간 조회 때문에 최대 0.6초, 429가 겹치면 +2초를
  기다렸다.
- **pre-arm (2026-08-02)** — 편입 즉시 감시 시작 + 전일종가/시가 캐시를 예열해
  둔다. 트리거 시점에 REST를 부르지 않기 위함(`prearm_candidate`).

\## 다음 할 일 (**2026-08-15(토) 검증 기준 — 다음 거래일 08-17(월)**)

> ⛔ **읽기 전에 알아야 할 상태 8가지**
> 1. **봇은 정지 상태다**(08-14 정상 매매 후 자동종료. 08-15 19:38에 주말
>    오기동이 있었으나 **9초 만에 정상 종료**했고, 그날 트리거를 월~금으로
>    좁혀 재발이 막혔다 — 요일마스크 62, **NextRun 08-17 08:59** 확인).
>    스위트 **1,912건 전원 통과 / 실패 0**(**19개**, 2026-08-15 저녁 실행).
>    `import main` OK. 최신 커밋 **`457cf47`**(08-15 확정익절 전환)까지
>    **push 완료**(원격 테마 자동커밋 `283ce84` 위로 rebase, 충돌 0).
>    🆕 **08-15 검증에서 매수 결함 1건을 고쳤다**(아래 3번).
> 2. 🔴 **08-13은 스케줄러가 08:59에 안 돌아 개장 25분을 놓쳤다** —
>    자동업데이트 후 로그인 잠김 -> 절전. **09:14 수동 기동**해서 정상 매매했다.
>    그날 작업 설정 4개를 고쳤다(`StartWhenAvailable`/`WakeToRun` True,
>    배터리 제한 2개 해제). ⚠️ **로그아웃/로그인 화면이면 여전히 안 뜬다**
>    (`LogonType=Interactive`) — 그 경우 로그인 즉시 기동한다.
>    **08-14 아침에 08:59 자동기동이 실제로 됐는지가 첫 확인 사항이다.**
> 3. 🔴🔴 **08-17(월)에 처음 도는 것 3가지** — 아래 「⚠️ 실거래 검증이 안 된 것」 참고.
>    · **Ⅰ 확정익절 2% — 본전스톱 대체**(08-15). **매도 규칙 자체가 바뀌었다.**
>      순 +2.0% 도달 **즉시** 50% 분할하고 잔량은 3% 트레일. 본전스톱은
>      상호 배타로 죽는다(로직·상수는 롤백용으로 보존).
>    · **Ⅱ 50% 분할 기계**(08-14 19:38 적용). 08-14에 본전스톱이 하루 **5건**
>      발동했지만 그날은 **전량 사양**으로 돌았다(적용이 장마감 후라서) —
>      분할은 한 번도 실거래를 안 거쳤다. 이제 확정익절이 같은 기계를 쓴다.
>    · **Ⅲ 분할매도 x 잔고대조 결함수정**(08-15). 봇이 자기 분할매도를
>      '수동 추가매수'로 오인해 수량을 되살리고, 그 결과 **물타기가 2배를 사던**
>      것을 막았다. 실물 A/B로 재현·검증했다(가드 OFF 100주 / ON 50주).
>    · 🔴 **셋은 한 덩어리다** — Ⅰ이 분할을 **훨씬 자주** 만들고(발동률 41~51%),
>      그 잔량이 오래 살아남으면서 Ⅲ의 위험 구간(잔고 반영 지연 실측 99초)에
>      **처음으로** 들어간다. **되돌릴 땐 Ⅰ부터.**
> 4. 🟢 **08-13에 08-12 수정분이 실거래로 확인됐다** —
>    · ④ 분할 잔량 보호: 덕산하이메탈 동적캡 분할(26주 @10,990, 순 +0.97%) 후
>      **잔량이 살아남아** VI 상단 11,140(순 +2.35%)에 청산. 예전이면 45초에 털렸다.
>    · ⑪ 수동 추가매수 `elif` 결함수정: 삼화전자 **2회 발동**(364->514->614주).
>      08-11에 만든 뒤 한 번도 안 돌던 기능이 처음 동작했다.
>    · ⑬ **VI가 '첫 분할'을 하는 케이스는 아직 안 나왔다** — 미검증 유지.
> 5. **최근 결산** — 08-13 청산 9건 / DB +547원, **08-14 청산 8건 / DB +48,947원**.
>    ⚠️ 두 날 모두 '수동 매도 감지'(손익 0 가짜 기록)가 섞였다(08-13 4건 / 08-14 2건).
>    **결산은 DB가 아니라 실계좌 체결내역으로 볼 것.**
>    · 08-14 청산 8건 중 **5건이 본전스톱**(전부 순 +0.85~+1.00%) —
>      08-13에 적은 "당장 효과 거의 0" 예측이 **하루 만에 빗나갔다.**
>      📌 '최근 N일 발동 0건'은 '앞으로도 안 돈다'가 아니다.
>    · 08-13 한국콜마 139,700 -> **-4.51% 손절**(246분) — ⓐ가 겨냥한 바로 그 패턴.
> 6. 🔴 **DB 미청산 = `manual` 5건뿐, `holding` 0건.** 08-13 보유분은 전부 청산됐다.
>    (`manual` 5 = 08-06 격리 3종목 + 08-12 오버나이트 격리 2종목)
> 7. ⛔ 장전 스캐너는 꺼져 있다(`AutoTrader_PremarketScan` = `Disabled`).
> 8. 🔴 **근본 문제는 두 층이다**(진입층 = 신호에 방향성 우위 없음 /
>    청산층 = 판 자리에서 더 간다). 상세는 이전 「다음 할 일」 내용 참고.

\### 🔬 08-17(월) 장중에 반드시 볼 것 — 08-14·08-15 적용분

| 순위 | 볼 것 | 정상 | 이상하면 |
|---|---|---|---|
| **Q0** | 🔴🔴 **`확정익절 2.0%` 매도에 `— 분할 1차 50%`가 붙는가** | 순 +2% 도달 즉시 절반만 나가고 **잔량이 계속 유지**된다 | 전량이면 `BREAKEVEN_EXIT_PARTIAL` 확인. 🔴 **분할 직후 1~2초 안에 잔량이 또 나가면** `be_partial_done` 가드가 죽은 것(08-12 VI에서 똑같이 겪었다) |
| **Q0-b** | 🔴 **`본전스톱` 사유가 하나도 안 나오는가** | **0건이 정상** (확정익절이 대체했다) | 나오면 `FLAT_TP_ENABLED`가 False로 돌아간 것 |
| **Q0-c** | 🔴 **동적캡·VI·반등소진 잔량이 확정익절에 안 털리는가** | 그 잔량은 **트레일/캡/손절**로만 나간다 | 분할 직후 잔량이 `확정익절` 사유로 나가면 `partial_exited` 가드가 죽은 것 |
| **Q1** | 🔴🔴 **`💰 수동 추가매수 감지`가 봇 분할 직후에 뜨는가** | **안 떠야 정상** (직접 HTS 매수를 안 했다면) | 뜨면 08-15 결함수정이 회귀한 것. **그때 물타기가 2배를 산다** — 즉시 `RECONCILE_SELL_GRACE_SECONDS` 확인 |
| **Q2** | 🔴 **`💧 물타기` 주문 수량** | 그 시점 **보유 잔량과 같아야** 한다 | 잔량의 2배면 Q1과 같은 원인 |
| **Q3** | ⚠️ **물타기 직후 같은 초에 `분할 잔량 트레일` 매도가 붙는가** | 조합 자체가 처음이라 **관측이 목적** | 반복되면 왕복 수수료만 나간다 — 히스토리 08-15 「사용자 판단 필요」 참고 |
| **Q4** | **잔량 청산 사유 분포** | 트레일 / 익절캡 / 손절만 | 동적캡으로 잔량이 털리면 `PARTIAL_EXIT_REMAINDER_HOLD` 확인 |
| **P0** | 🔴 **08:59 자동 기동** | 09:00 전에 로그 시작 | 안 뜨면 로그인 상태 확인 후 `schtasks /Run /TN "AutoTrader_Start"` |
| **P1** | 🔴 **`주가 상한 초과` 진단** | 10만원+ 후보가 있으면 뜬다 | 10만원+를 **샀으면** `MAX_ENTRY_PRICE` 배선 확인 |
| **P2** | **10만원+ 매수 0건인가** | **0건이 정상** | 1건이라도 있으면 상한이 안 먹은 것 |
| **P3** | 🔴 **`본전스톱` 발동 시 실현 순이익** | **순 +1.0% 부근** | +0.2% 부근이면 `BREAKEVEN_FLOOR`가 안 먹은 것 |
| **P4** | **본전스톱 발동 건수** | **0~1건이 정상**(08-11~13 0건) | 갑자기 5건+면 무장 문턱이 바뀐 것 |
| **P5** | 🆕 **VI 상단 매도에 `— 분할 1차 50%`** | 절반만 나가고 잔량 유지 | **분할 직후 1~2초에 잔량이 또 나가면** `vi_partial_done` 회귀 |
| **P6** | **`진입 유효창 만료` 종목 수** | 하루 **2~9종목**(08-13 실측 9종목) | **0**이면 게이트 미작동 / **15종목+**면 과도하게 조인 것 |
| **P7** | **`수동 매도 감지`** | 실제 수동매도 때만 | 봇 매수 직후 뜨면 `RECONCILE_UNSEEN_GRACE_SECONDS` 확인 |

⚠️ **로그는 반드시 로테이션까지 훑을 것** — 08-13에 `autotrader.log`가 11:49에
로테이션돼서 `logs/autotrader.log`만 grep하면 **오전 기록이 통째로 안 보인다**
(실제로 "VI 분할 0건"이라는 잘못된 결론을 냈다가 정정했다).
`grep ... logs/autotrader.log*` 로 전부 볼 것.

⚠️ **결산은 DB가 아니라 실계좌 체결내역(kt00007)으로 볼 것.**

\### 🔬 (참고) 지난 체크리스트 -> **[아카이브] `docs/OBSERVATION_CHECKLISTS_PAST.md`**

08-13(08-12 적용분 4건: 진입 유효창 / VI 50% 분할 / 잔량 보호 / 수동 추가매수
`elif` 결함) 과 08-12(조건식 개편·물타기·당일청산 폐지) 의 장중 관찰표.
**오늘 볼 것은 바로 위 「🔬 08-17 장중에 반드시 볼 것」이다.**

\### 🔴 다음에 할 만한 것 (우선순위 순)

**0. 08-13 결과로 08-12 적용분 4건 판단 — 되돌린다면 하나씩.**
   근거 강도: `①(수동추가매수, **결함수정**) > ②(진입 유효창, 5/5일) >
   ③(VI 분할, n=9·78%) > ④(잔량 보호, 설계 복원)`.
   ①은 되돌릴 이유가 없다(되돌리면 기능이 죽는다). **되돌릴 땐 ③부터** —
   표본이 9건으로 가장 얇다.

**0-b. ✅ VI 상단 재검토 — 08-12에 '50% 분할'로 처리했다(이월 종료).**
   08-11에 7건, 08-12에 **9건으로 재확인**했다: 실현 +2.37%인데 판 뒤 30분
   **+5.67%**, 종가까지 +8.85%, **78%가 +3% 이상** 더 갔다.
   "끄는 게 아니라 절반만 팔고 잔량은 트레일"이라는 당시 권고 그대로 넣었다.
   -> 08-13부터 실거래로 채점할 것(체크리스트 N2).

**0-c. 🆕 조건식별 성과 격차 — 관측만 하고 손대지 않았다.**
   08-12 실측: **주도주상위 단독 8건 MFE +13.54%** vs **돌파전 단독 3건 +2.24%**
   (6배). 그런데 **고점 매수 경향은 차이가 없다**(swing 0.74 vs 0.75,
   신고가권 25% vs 33%) — 즉 "돌파전이 고점에서 산다"가 아니라
   **데려오는 종목의 질이 다르다**는 뜻이다.
   ⚠️ 옛 조건식(08-05~11, n=119)에서는 돌파 신고가권 **52%** vs 주도 32%로
   확연했는데, `돌파전` 전환 후 그 격차가 사라졌다 — **전환이 고점 매수 쪽은
   개선했을 가능성**이 있다(n=3이라 단정 불가).
   · CLAUDE.md 08-07에 이미 같은 패턴이 기록돼 있다("조건식은 돌파가 나은데
     우리가 산 결과는 주도가 낫다").
   · **돌파전 매수가 10건쯤 쌓이면**(08-13~15) 판단할 것. 후보:
     ⓐ 돌파 계열에만 swing 상한  ⓑ 돌파전도 확인 전용으로  ⓒ 그대로 둔다.
   · 🔴 **하루 데이터로 조건식 정책을 고르지 말 것**(08-06 최적이 4일 최악).

**0-d. 🔴 수동 매도가 봇 자동매도를 크게 이겼다 (08-12, 원인 미규명).**
   실계좌 체결 기준: **수동 10건 +241,052원(건당 +2.84%, 승률 10/10)** vs
   **자동 10건 +21,093원(건당 +0.84%, 승 7/3)**. 건당 **+2.00%p** 차이.
   · 봇 매도를 보면 삼화전자 +0.73% / 마녀공장 +0.74%·+0.50% / 한켐 **-0.99%·
     -1.46%**처럼 1% 미만에서 자잘하게, 심지어 손실로 판다.
   · ⚠️ **편향이 크다** — 수동 7건이 09:02~09:06 급등 구간에 몰려 있고,
     오르는 것만 골라 판 **선택 편향**이 있다. 봇은 규칙대로 전량 처리한다.
   · 그래도 방향은 8일치 청산 분석과 일치한다(동적캡 12건 중 50%가 판 뒤
     +3%↑). ③④가 이에 대한 첫 대응이고, **08-13부터 채점**할 것.

**1. 🔴 진입층 근본 문제 — 아직 아무것도 안 했다.**
   조건검색 **편입 즉시** 매수는 +5분 +0.506%인데 우리 진입 로직 통과 후는
   **+0.099%**다(4일, 158건 vs 75건). **우위를 진입 로직이 1/5로 깎는다.**
   08-06에 진단하고 08-08에 발사 게이트를 갈아끼웠지만 아직 해결되지 않았다.
   · 다음 수순 후보: 편입 시점 매수 가정 대비 **각 게이트가 얼마를 깎는지**
     하나씩 분해 측정(숙성 / 무장 / 발사 / 파동 / VWAP / 되돌림).
     지금까지는 게이트를 **개별로** 평가했지 **누적 손실**을 재보지 않았다.

**2. 틱 아카이브를 전 시간대로 확대 (이월, 우선순위 상승).**
   지금은 **09:00~09:15뿐**이라 08-10 실거래 최고 구간(10:30 이후 5건 +50,819원)을
   **원리적으로 검증할 수 없다.** 08-10 문턱 스윕도 사실상 09:05~09:10 결과다.
   `ka10079` 소급 수집으로 가능(10종목x4일 약 40분). **이게 되면 시간대별 정책과
   무장 반사실 둘 다 열린다.**

**3. ✅ 독립 통합 리플레이 커밋 — 08-10 저녁에 완료(이월 종료).**
   08-08·08-09·08-10 세 세션이 매번 스크래치패드에 새로 만들었다가 잃어버렸다.
   -> **`test_replay_archive.py`로 커밋했다**(PC 검증 표 #15, 22건).
   아카이브 **11일 558,479틱**을 실물 `StrategyManager`에 관통시켜 캡 위반·예외·
   REST·매수매도 위생을 전수 검증하고, **변동성 손절 ON/OFF A/B가 내장**돼 있다.
   ⚠️ 이전 세션들이 "15일 561k틱"이라고 적은 것은 **폴더명으로 센 값**이다 —
   실제 틱 날짜로 다시 묶으면 11일이다(아래 히스토리 참고).

**4. 지정가 미체결 대책.**
   08-10에 21건 중 **6건(29%)**이 지정가로 나갔고 그중 1건이 유령이 됐다.
   유령 자체는 이제 자동 정리되지만(08-10 수정), **미체결 자체를 줄이는** 쪽도
   봐야 한다 — 시장가 문턱(현재 1,000만원) 상향 또는 지정가에 틱 여유 추가.

**5. `phase1b` -> `TickDataPipeline` 리네임** — 이름이 실거래 사고를 두 번 냈다.
   호출부 ~40곳이라 계속 이월 중.

**6. `strategy_manager.py` 분리** — 6,000줄+. 1단계(상수를 `core/strategy_config.py`로)는
   역의존 0으로 확인돼 기계적으로 가능. **미검증 변경이 정리된 뒤에 할 것.**

\### ⚠️ 실거래 검증이 안 된 것 (= 지금 도는 것 전부)

**[⏸ 08-18 장마감 후 만들었으나 OFF로 보류 — 08-19에 도는 것은 없다]**

🔴 **`PARTIAL_EXIT_FLOOR_AT_FIRST`는 만들어서 검증까지 마쳤지만 껐다.**
무효 반증에서 **전제가 깨졌다** — 분할 잔량이 1차 매도가 아래로 내려가기까지
**같은 초가 91.8% / 5초 이내 97.1%**(실틱 1,801건). 즉 켜면 잔량이 사실상
**즉시 전량 청산**되어, 이 규칙은 `BREAKEVEN_EXIT_PARTIAL = False`(전량)와
실질적으로 같아진다 — 그것도 **문서화된 스위치가 아니라 옆문으로.**
· 채택 근거였던 반사실(트레일 4건 -0.47% -> +0.47%)은 **'잔량이 트레일까지
  살아남는다'는 전제**로 계산한 것인데 그 전제가 틀렸다. 하한은 트레일로 나갈
  건뿐 아니라 **익절캡·VI로 크게 벌던 건까지** 1차가에서 자른다.
· 실제 반례: 08-18 LB세미콘 2번은 VI 분할(5,110) 후 잔량이 익절캡 5,450까지
  살아남아 **+10,899원**을 냈다. 하한이 걸렸으면 +2,496원 = **-7,820원**.
  1번 포지션 +2,945원과 합치면 **하루만 봐도 부호가 뒤집힌다.**
· 🔴 **그래도 원래 문제는 그대로 남아 있다** — 3% 트레일이 1차 매도가보다
  싸게 파는 것은 산술적 필연이다(고점이 1차가 +3.09% 미만이면 예외 없음).
  실측 5건 중 3건이 그랬다. **해결책을 다시 골라야 한다**(아래 선택지).
· 선택지 ⓐ 현행 유지(분할 + 3% 트레일, 손실 감수)
  ⓑ **`BREAKEVEN_EXIT_PARTIAL = False`** — 확정익절 +2%에서 **전량** 매도.
     08-15 백테스트상 **전량이 최고**(비관 0.657 / 낙관 0.719 vs 분할 0.360/-0.046).
     문서화된 스위치라 하한 문제가 **원천적으로 사라진다.**
     ⚠️ 08-15에 사용자가 '승률과 상승 참여를 사려고' 분할을 고른 것을 되돌리는 것.
  ⓒ 하한 + 유예(N초)나 버퍼(1차가 -X%) — **데이터 없는 새 파라미터라 비추천.**
· 로직·상수·스위트(`test_patch_20260818` 29건)는 전부 보존. 켜려면 True 한 줄.

**[🔴 08-17(월)에 처음 돈 것] — 되돌릴 땐 반드시 하나씩**
| 항목 | 근거 | 강도 | 롤백 |
|---|---|---|---|
· ⚠️ **비용**: 스톱이 더 타이트해진다. 1차가 근처에서 흔들리면 예전보다 일찍 나가고 그 뒤
  반등분은 못 먹는다(휩쏘). 08-18 익절캡 +6.65%로 간 건이 중간에 1차가 아래로 내려갔다면
  그것도 잘렸을 텐데 **틱이 없어 확인 불가**다. **실거래로 채점할 것.**
· 🔴 **첫 판이 `<=`였고 그게 결함이었다** — 1차 분할이 바로 그 가격에서 나므로 같은 가격
  틱에서 잔량이 즉시 털렸다(08-12 VI 상단 결함의 재현). **기존 스위트 3개(#4/#18/#19)가
  잡아냈다.** 지금은 strict `<`다. 이 경계를 건드리지 말 것.

**[🔴 08-17(월)에 처음 돈 것] — 되돌릴 땐 반드시 하나씩**
| 항목 | 근거 | 강도 | 롤백 |
|---|---|---|---|
| **Ⅰ 확정익절 2% (본전스톱 대체)**(08-15) | 실매수 **150건 / 8일** 종일분봉, 비관·낙관 + 슬리피지 0.2%: 본전스톱 0.326/**-0.240** vs 이 안 **0.360/-0.046**, 승률 55%->**71%**, 중앙값 0.137->**0.864**. 날짜별 비관 5승3패 / 낙관 6승2패 — **두 가정 모두 우세**(08-11 물타기 때와 달리 부호가 안 뒤집혔다) | **중~강** (8일·양가정) | `FLAT_TP_ENABLED = False` |
| **Ⅱ 50% 분할 기계**(08-14 19:38) | 08-14 본전스톱 5건 종일 분봉 반사실에서 **비관·낙관 두 가정 모두 최고**(분할 +58,155/+37,844 vs 전량 +30,968). 구조 논거는 **정합성** — VI상단·동적캡·반등소진이 이미 전부 50%인데 본전스톱만 전량이었다 | 중 (표본 **5건·하루치**) | `BREAKEVEN_EXIT_PARTIAL = False` |
| **Ⅲ 분할매도 x 잔고지연 가드**(08-15) | **결함수정**. 실물 A/B 재현 — 가드 OFF면 물타기가 **100주**(2배), ON이면 50주 | **결함** | `RECONCILE_SELL_GRACE_SECONDS = 0` |
· Ⅲ은 되돌릴 이유가 없다(되돌리면 잘못된 주문이 부활한다). **되돌린다면 Ⅰ부터.**
· 🔴 **Ⅰ이 Ⅱ·Ⅲ의 전제를 만든다** — 본전스톱은 08-11~13에 **0건**이었는데
  확정익절은 **하루 절반 가까운 포지션**에서 발동한다. 분할이 훨씬 잦아지고,
  그 잔량이 3% 트레일로 오래 살아남으면서 잔고 반영 지연(99초) 창에 처음
  들어간다. 08-14까지는 분할 잔량이 대개 30초 안에 정리돼 우연히 비켜갔다.
· ⚠️ **Ⅰ은 같은 구조에서 더 나은 값이 있다는 걸 알고 고른 것이다** —
  문턱 +2.5%는 0.486/0.148, **전량 청산**은 0.657/0.719로 둘 다 위다.
  2.0% / 50% 분할은 **승률과 상승 참여를 사는 사용자 지정**이다.

**[08-13 적용분] 08-14(금)에 돌았다 — 🟢 ⓑ 확인 / ⚠️ ⓐ 판정 보류**
| 항목 | 실거래 근거 | 강도 |
|---|---|---|
| **매수 주가 상한 10만원** | 10만+ 8건 평균 -1.87%·중앙 -2.08%·손절률 62%, **4/4일 열위**. MAE 전수로 근거가 **손익비**로 정정됐다(아래) | 중 (표본 8건) |
| **본전스톱 바닥 +1.0%** | 발동 12건이 고점 +1.85% -> 실현 +0.07%로 **1.78%p 반납**(12/12건 +1% 미만) | 중 (단 **당장 효과 ~0**) |
· 🔴 **주가 상한의 근거가 MAE 측정으로 바뀌었다**(08-13 장마감 후):
  MAE(하락폭)는 **10만+ -5.03% vs 2만미만 -4.87%로 거의 같다**(0.16%p).
  차이는 MFE(상승폭)로 **+2.28% vs +6.74% = 3배**. 즉 "비싸면 더 떨어진다"가
  아니라 **"같이 떨어지는데 안 올라온다"**(손익비 0.45 vs 1.38)가 맞다.
  -> 손절을 넓히거나 보유를 늘리는 방향으로는 해결되지 않는다.
· ⚠️ 본전스톱 바닥은 **08-11~13에 발동 0건**이라 며칠간 아무 일도 안 일어날 수 있다.
  그게 정상이다(무장 문턱 2.5%를 넘는 건이 32건 중 2건뿐).

**[08-12 장마감 후 적용분] 08-13에 하루 돌았다 — 🟢 ④⑪ 실거래 확인**
| 항목 | 실거래 근거 | 강도 |
|---|---|---|
| 🔴 **수동 추가매수 `elif` 결함수정** | 08-12에 JW신약 517주가 방치된 것을 확정 확인. 기능이 아예 안 돌던 것을 살린 것 | **결함** |
| **진입 유효창 30분** | 8일 104건, 30분 초과 14건 MFE +1.79%(절반이 1% 미만), corr -0.291, **5/5일 우세** | **강** |
| **VI 상단 50% 분할** | 8일 9건, 실현 +2.37%인데 판 뒤 +5.67%, **78%가 +3%↑**. 08-11 7건과 방향 일치 | 중 |
| **분할 잔량 보호** | 실측 44~55초 만에 잔량이 털렸다(설계 무력화). 성과 근거는 n=3(+3,233원)로 얇고 **정합성이 주된 근거** | 정합성 |
| ~~편입가 앵커~~ | 🔴 **적용 당일 저녁 OFF** — 시뮬 결함 + 구간마다 방향 반대 | (철회) |

**[08-11 적용분] 08-12에 하루 돌았다 — 결산 +262,145원(실계좌 기준)**
⚠️ 하지만 그중 **수동 매도가 +241,052원**이라 봇 정책의 성과로 읽으면 안 된다.
· 물타기는 09:01~09:03에 **4건 발동**했고 11:00 컷오프도 정상 작동했다
  (`11:07:29 물타기 생략: 마감 시각(11:00) 경과`).
· 손절 -4.5%는 그날 손절이 거의 없어 판정 보류.
· 조건식 `돌파전`: 단독 3건 MFE +2.24% vs 주도 단독 8건 +13.54% — **0-c 참고**.

**[08-11 적용분 원문] 08-12에 처음 돌았다**
| 항목 | 실거래 근거 | 강도 |
|---|---|---|
| **수동매도 vs 미체결 구분** | 08-11에 6건이 오기록된 것을 확정 확인. 기록 정확도만 바꾼다 | **강**(구조) |
| **손절 -4.5%** | 08-11 손절 9건 중 6건이 5분 이내 + **5종목 전부 22~26분 내 재무장**. 그날 -4.5% 초과는 1건뿐 | **중~강** |
| **조건식 돌파전/돌파후** | 08-11 돌파 단독 4건 손절 3(-35,802원) vs 돌파+주도 2건 손절 0(+26,180원). **n이 2~4로 작다** | 약~중 |
| **수동 추가매수 합산** | 구조적 정합(봇이 모르는 물량을 없앤다). 실거래 발동은 아직 0건 | 구조 |
| 🔴 **-3% 물타기** | **백테스트 판정 불가** — 비관 +29,206 / 낙관 -93,569원으로 부호가 뒤집힌다. 상위 3건 빼면 -249,832원. 날짜별 3승 2패 | **없음** |

**[08-10 적용분] 08-11 하루 돌았다 — 결산 -86,912원(29건)**
청산 정책 6건(캡 6%/본전스톱 2.5%/정체정리 OFF/변동성 손절/반등소진 본전 이상/
우선순위 교체 OFF). ⚠️ **하루로는 판단할 수 없다** — 08-11은 물타기 없이
손절 -3%로 돈 날이고, 손절 9건 중 8건이 새 손절선(-4.5%)이면 안 났을 건이다.

**[상시] 표본이 얇은 것**
무장 3.0초(33건) / KRX 전환(정합성만) / [D] 상승이탈 OFF(하루 n=9) /
[E] 눌림 슬롯 0(4일 46건) / 매수금액 100만(사용자 지정) /
발사 게이트 분리·초반 슬롯 캡(틱 5일 독립표본).

\### 📚 08-06 [A]~[D] 상세 근거 -> **[아카이브] `docs/DEFECTS_2026-08-06_AD.md`**

[A] `_today_open` 장중 재편입 버그 / [B] 버스트가 매도 체결을 구분하지 않음 /
[C] 등락률 **하한** 신설 / [D] 상승 이탈 **OFF** — 넷 다 **2026-08-06 수정 완료**이고
지금 사양은 「현재 전략 요약」과 「기대값」 블록에 반영돼 있다.
재현·수정·검증 절차가 필요할 때만 아카이브를 열 것.

\### ⚠️ 아직 판단 보류 중인 것 (표본 부족)

| # | 항목 | 상태 |
|---|---|---|
| A | 재매수 문턱 배수 2.5 | 08-06 발동 0건. 계속 관측 |
| B | 시가대비 상한 8% 적정값 | **[A] 버그로 08-06 데이터가 오염됐다.** 이제 필터가 진짜 시가로 도니 **재가동 후 처음으로 유효한 측정**이 된다 |
| C | 3~6% 구간 대응 | 🔴 **구간 효과가 아니라 날짜 효과였다**(같은 3~6% 구간이 08-04 **-2.22%** / 08-05 **+0.72%**, 같은 날 ~3% 구간은 08-04 +1.83%). **구간별 평균은 반드시 날짜로 쪼개 볼 것** — 3일 36건에서 하루가 10~20건이라 하루의 색깔이 구간 평균을 그대로 지배한다. **별도 대응 불필요** |
| D | 3만원대 버스트 문턱 | MAX 3.0은 73,704원 이상에만 적용. 3만원대(GS건설 32,600, 08-06 -3.07%)는 불변. ALPHA를 올리면 2만원대 승자까지 잡혀 보류 |
| E | 상대 경로 폐지 | 주가 스케일로 사실상 무력화(상대 하한 = burst_min). [B]로 매수 방향까지 요구하게 돼 더 좁아졌다. 코드 삭제는 미착수 |
| F | 오후장 접기 | 08-06은 오전에 멈춰 데이터 없음 |
| G | 슬롯 교체를 틱 경로로 | 이월 |
| H | `phase1b` -> `TickDataPipeline` 리네임 | 이월(호출부 ~40곳) |
| I | **FID228 틱 아카이브 저장** | 미착수. 🔻 **긴급도 하향**(08-07) — 무장 이력이 **로그에 남아 있음**이 밝혀져 DB 역매칭으로 대체 가능하다. 다만 로그는 '매수된 건'만 남고 **탈락한 건의 강도는 안 남으므로**, 무장 임계값(100)이나 요구시간을 더 손대려면 여전히 필요하다 |
| J | **[D] 상승이탈 재평가** | OFF 상태로 며칠 관찰 후, 되돌림 미도달이 급증하면(=진짜 폭등을 놓치고 있으면) 재검토 |
| **K** | **VWAP 필터 유지 여부** | ✅ **08-07 저녁에 ON으로 전환했다**(단위는 백만원 확정). 이제 판단할 것은 '켤지'가 아니라 **'유지할지'**다. 08-07 10건 소급은 -4.35% -> -0.20%로 좋았으나 4일 69건으로는 손익비 1.03 -> 1.09에 그친다. **08-08 장중에 `VWAP 이격 미달` 진단이 몇 건 뜨는지**로 판단할 것 — 0건이면 필터가 안 도는 것이고, 하루 종일 그것뿐이면 과다다. 끄려면 `VWAP_ENTRY_ENABLED = False` |
| **L** | **KRX 전환 유지 여부** | 08-07 적용. 성과가 아니라 **정합성**이 근거다. 08:59 스냅샷 0종목은 정상일 수 있으니 **09:10 실시간 편입 여부**로 판단할 것 |
| **M** | **`strategy_manager.py` 분리** | **7,111줄**(08-10 실측 — 08-08의 5,593줄에서 2일 만에 +1,518). 1단계(상수 -> `core/strategy_config.py`)는 **역의존 0으로 확인돼 기계적 분리 가능**. 재수출로 외부 import 불변. ⚠️ 소스를 통째로 읽어 문자열 단언하는 테스트가 ~~4곳~~ **11곳**이다(08-10 재측정, 계속 늘고 있다) — 같이 손볼 것. 모듈레벨 상수도 123 -> **166개**라 견적이 커졌다 |
| **N** | 🆕 **일봉 눌림 종목 익절캡 상향** | 08-07 태그 신설. **표본 30건 + 날짜 3일 이상 우세**면 캡 4.0/2.5% -> 6.0% 적용 검토. 근거: 캡 도달률 41% vs 3%, 일봉 +5일 +4.66%. ⚠️ 중앙값 +0.19% / 기간 1구간 마이너스 / 08-05 "캡 도달분 77% 본전 회귀"와 충돌 가능 |
| **O** | 🆕 **눌림 고가 돌파를 인트라데이 트리거로** | `daily_pullback_state()`가 `pullback_high`를 돌려주므로 장중 돌파 감지가 가능하다. 다만 **새 진입 경로**라 이 코드베이스에서 가장 위험한 부류(호출부 3곳 규칙). N을 먼저 하고 그 결과를 보고 판단할 것 |
| **P** | 🆕 **조건검색 수집 이중화 잔여** | 폴링은 08-07에 살렸다. 남은 것: `ConditionManager.update_snapshot()` 죽은 코드(`jmcode` vs 실제 키 `'9001'`) / 스냅샷 타임아웃 시 재시도 없음(08-06 seq=2 실측) |
| **Q** | ✅ **거래대금 가속도를 진입에 쓸 것인가** | **08-08에 적용했다**(09:05~ 버스트 대체, 2.5배). ⚠️ **이 항목의 옛 권고 2개가 실측으로 뒤집혔다**: ① "절대 규모 하한 필수" -> **정반대**(하한 1억 -0.049% / 3억 -0.217% / 5억 -0.572%, 단조 악화). 필요한 건 금액이 아니라 **최소 틱수**(20)다. ② "2.0x가 생존자" -> 격자 기준이었고 **독립표본(첫 신호)으로 보면 2.0은 기준선에 진다**(+0.218% vs +0.395%). 이제 판단할 것은 '쓸 것인가'가 아니라 **'2.5가 맞는가'** — 3.0은 +1.968%(하루 5.6종목), 2.5는 +0.688%(하루 10.8종목) |
| **R** | 🆕 **틱 아카이브 전 시간대 확대** | 지금은 09:00~09:15뿐이라 **시간대별 검증이 원리적으로 불가능**하다(분봉으론 해상도가 달라 30초 스케일 우위가 안 잡힌다). `ka10079` 소급 수집으로 가능, 10종목x4일 약 40분. 이게 되면 시간대별 + 무장 반사실 둘 다 열린다 |
| **S** | 🆕 **장전 스캐너 Phase 1/2** | ⛔ **08-09에 보류(스캐너 OFF).** 사유: **일봉 눌림 후보가 조건검색식에 거의 잡히지 않는다** — 스캐너 후보와 실제 매매 유니버스가 겹치지 않으면 Phase 1/2의 근거가 애초에 안 쌓인다. 재개하려면 **먼저 그 겹침부터 측정할 것**(`observations/premarket/*.json`의 후보 ↔ 그날 `watch_list_log` 편입 종목 교집합). 겹침이 낮으면 Phase 1/2가 아니라 **스캐너 유니버스 설계**를 다시 봐야 한다. (Phase 1 = 후보 종목만 익절캡 상향 / Phase 2 = 돌파선을 진입 트리거로 = 최고 위험) |

\### 🔴 새 결함 후보 — 재시작 시 분할 상태 전체 유실 (2026-08-18 발견, 미수정)

`_restore_from_db()`가 만드는 `pos` 딕트에 **분할 관련 키가 하나도 없다**:
`partial_exited` / `partial_exit_price` / `trail_peak` / `be_partial_done` / `vi_partial_done`.
즉 **장중에 봇을 재시작하면 이미 절반 판 포지션이 '한 번도 분할 안 한 새 포지션'이 된다.**

- 결과 ⓐ 순이익이 여전히 +2% 이상이면 **확정익절이 다시 발동해 또 절반을 판다**(과다 분할).
- 결과 ⓑ 가격이 내려가 있으면 `partial_exited=False`라 **08-18 잔량 하한이 아예 적용되지 않는다**
  — 조용히 이전 동작으로 되돌아간다.
- 수량 불일치는 `main._reconcile_manual_sells`의 '일부 수동 매도' 분기가 나중에 바로잡지만
  **분할 플래그는 어디서도 복원되지 않는다.**
- 🔴 **08-18 신설분만의 문제가 아니다** — `trail_peak`·`be_partial_done`·`vi_partial_done`도
  같이 유실된다. 즉 08-04부터 있던 gap인데 지금까지 문서화된 적이 없다.
- ⚠️ **장중 재시작은 실제로 있었다**(08-10, 08-12). 빈도가 낮을 뿐 가정할 수 없는 일이 아니다.
- 고치려면 `trades`에 분할 상태를 저장하거나(스키마 변경), 재시작 시 서버 잔고와 DB
  `buy_quantity` 차이로 **분할 여부를 역산**해야 한다. 별도 변경이라 08-18에는 손대지 않았다.

\### 🐛 알려진 결함 (무해하지만 기록)

- `kiwoom_ws.py:229`가 `item["jmcode"]`를 쓰는데 실제 키는 `'9001'` —
  `ConditionManager.update_snapshot()`이 한 번도 호출된 적 없다(죽은 코드).
- `theme_manager` DB 저장 실패("more than one '%s' placeholder"). 매매 무관.
- `0g` 실시간이 프로그램매매가 아니라 '주식종목정보'다. 단 **VI 계산에 쓰는
  305/306/307(상한가/하한가/기준가)이 여기 실려 온다** — 구독을 빼지 말 것.
- `CandleCache.log_stats()`가 어디서도 호출되지 않는다.
- ~~`task_condition_snapshot_poll` 비활성 유지~~ -> **08-07 저녁에 재활성화됐다**
  (`main.py:1542` gather에 등록, 주기 60초). WS future 디스패치로 틱 유실 0.
  끄려면 그 줄을 주석 처리. 상세는 히스토리 "2026-08-07 (15:20~)" ③ 참고.

\### ⚠️ 이 코드베이스의 반복 사고 패턴 (매번 확인할 것)

**같은 규칙이 여러 경로에 흩어져 있어, 한 곳만 고치고 끝냈다가 조용히 어긋난다.**

- **매수금액 3곳**: `portfolio_optimizer.DEFAULT_BASE_AMOUNT`(**실지배값**) /
  `phase_settings.position_amount` / `order_manager.BUY_AMOUNT_PER_STOCK`
- **`_execute_buy` 호출부 5곳**(함수 기준): `_evaluate_1a_pullback_entry`(폴링) /
  `_try_fill_entry_plan`(되돌림 트랜치 — **한 함수 안에서 2번 부른다**) /
  `_do_rescue_add`(손절 대신 추가매수) / 🆕 **`_maybe_average_down`(-3% 물타기,
  08-11 신설)** / **`_maybe_tick_entry`(즉시매수 폴백)** —
  `ENTRY_PULLBACK_ENABLED=False`일 때만 도달하므로 지금은 사실상 죽은
  경로지만 **되돌림을 끄는 순간 살아난다.**
  ⚠️ **라인으로 세면 6개, 함수로 세면 5개**다(`_try_fill_entry_plan`이 2번).
  `audit_deep_20260808.py` [2]는 **함수 기준**으로 센다.
  ⚠️ 이 목록이 오래 "3곳"으로 적혀 있었고(08-08 발견), 08-11에 물타기가
  늘었는데 이 문단만 "4곳"에 머물러 있었다(08-12 발견 — **두 번째 뒤처짐**).
  목록이 틀리면 "새 규칙을 모든 매수 경로에 반영했는가"라는 점검이 통째로 샌다.
  🔴 **물타기는 `bypass_entry_gates=True`라 품질 게이트를 건너뛴다** — 새 진입
  게이트를 넣을 때 "물타기에도 적용해야 하나"를 반드시 따로 판단할 것.
- 🆕 **분할매도 4경로 -> `_execute_sell` 단일 창구**(2026-08-18 확인): 확정익절 / 동적캡 /
  VI 상단 / 반등소진이 전부 `sell_qty=`로 `_execute_sell`을 부른다(파일 전체에 그 4곳뿐).
  그래서 `is_partial` 블록 한 곳에서 상태를 기록하면 4경로가 자동으로 덮인다.
  🔴 **5번째 분할 경로를 추가할 땐 이 창구를 벗어나지 않았는지 반드시 확인할 것.**
- **청산 경로 2곳**: `on_price_update`(틱) / `check_timeouts`(폴링)
- **강도 하락 판정 3곳**: `_update_dynamic_caps` / `_is_strength_rising_vs_entry` /
  `find_stagnant_holding`(slot_replacement.py)
- **전면차단은 `_entry_block_reason()` 하나만** 볼 것
- **등락률 상·하한은 `_entry_change_reject()` 하나만** 볼 것
  (2026-08-06에 세 경로 — `_open_entry_plan` / `_execute_buy` / 상승이탈
  재확인 — 에 복제돼 있던 것을 접었다. **호출부를 늘리지 말 것**)
- **시가 판정 2곳**: `_today_open`(분봉) / `_get_prev_close`(ka10001 캐시)
  <- **[A]가 정확히 이 패턴이다.** 두 값이 어긋나면 **캐시가 이긴다**
  (거래소 확정 시가 `open_pric`이라 분봉 추정보다 정확)
- **진입 핫패스에 REST를 넣지 말 것** — 캐시는 `prearm_candidate`가 채운다
- **테스트 픽스처에 수치를 하드코딩하지 말 것** — 전부 상수 참조로
- **진단 라벨에 수치를 박지 말 것** — 상수를 바꿔도 안 따라와 거짓말을 한다
- **소스 문자열로 단언하지 말 것** — 08-06 아침 감사에서 오탐 3건이 났고,
  같은 날 오후에 **또 한 번** 났다: `_entry_change_reject()`로 규칙을 모으자
  `_execute_buy` 소스에서 `_entry_change_cap` 문자열이 사라져 감사가 **멀쩡한
  코드를 실패로 보고**했다(그 감사 파일이 스스로 경고하던 패턴을 그대로 밟았다).
  **실제 호출·반환값으로** 검증할 것 — 지금은 스파이를 걸어 호출 횟수를 센다.
- **추측한 메서드명으로 테스트를 쓰지 말 것** — 08-06에 없는
  `check_open_surge_block()`을 지어내 테스트가 AttributeError로 죽었다.
  먼저 `grep`으로 실제 이름을 확인할 것.
- 🆕 **틱 데이터의 기준선은 반드시 '시간 균등'으로 뽑을 것** (2026-08-08).
  틱 인덱스 간격으로 뽑으면 **틱이 촘촘한 = 활발한 순간을 과대표집**하고,
  그 순간들은 평균회귀로 수익이 나빠 기준선이 인위적으로 낮아진다.
  실측: 인덱스 균등 **-0.357%** vs 시간 균등 **+0.119%** (같은 데이터).
  이 하나로 신호 우위가 **+0.638%p -> +0.162%p, 4배 과대평가**됐다.
- 🆕 **고속 구현은 실물 함수와 등가 검증 후에 쓸 것** (2026-08-08).
  누적합 가속도 구현이 두 번 틀렸다 — ① `ts >= cutoff` 경계를 `bisect_right`로
  처리(같은 초 틱이 통째로 빠짐) ② 초 단위 타임스탬프라 **아직 도착하지 않은
  같은 초의 틱**까지 포함. 검증 없이 썼으면 분석 전체가 거짓말이 됐다.
- 🆕 **진입 규칙은 '독립표본(종목·일당 첫 신호)'으로 평가할 것** (2026-08-08).
  격자(모든 시점) 평균으로 재면 **없는 우위가 보인다** — `VWAP AND 가속2.0`이
  격자로는 +0.531%p인데 독립표본으로는 **-0.046%로 기준선에 진다**.
  봇은 조건이 성립하는 **첫 순간**에 발화하고 그 순간은 국소 고점이기 때문이다.
  '시간 균등 기준선'과 **세트로** 지켜야 한다 — 둘 중 하나만 어겨도 결론이 뒤집힌다.
- 🆕 **비율 지표에는 '표본 수' 하한을 같이 둘 것** (2026-08-08).
  거래대금 가속도는 틱이 2개뿐이면 **수학적 최대(4.0)**가 나온다 —
  `compute_strength`가 틱 부족 시 100.0을 돌려주는데 임계값도 100이라
  그냥 통과하던 것과 같은 실패 모드다. **데이터가 없을수록 쉽게 뚫린다.**
  ⚠️ 단 하한은 **개수**여야 한다 — **금액** 하한은 실측상 해롭다(단조 악화).
- 🆕 **게이트를 '사전 검사'할 땐 검사 가격과 체결 가격이 같은지 볼 것**
  (2026-08-08). VWAP은 계획 생성 시 **트리거가**로 검사하는데 체결은
  **-0.3~-0.7% 낮은 가격**에서 난다 -> 통과했는데 못 사는 죽은 구간이 생긴다.
  '값싼 게이트는 계획 생성 전에' 원칙은 **가격이 같을 때만** 성립한다.
- 🆕 **외부 상태와 대조하는 로직은 반드시 '양방향'을 같이 볼 것** (2026-08-15).
  잔고 대조에서 08-12에 고친 것은 `서버 < 봇`(반영 지연을 **수동 매도**로 오판)
  이었는데, **정반대인 `서버 > 봇`**(반영 지연을 **수동 매수**로 오판)은 그대로
  남아 있었다. 같은 지연, 같은 15초 폴링, 같은 함수인데 **한 방향만 고쳤다.**
  🔴 더 나쁜 건 **봇 자신이 만든 변화**였다는 점이다 — 봇이 방금 판 것을
  "누가 샀다"고 읽었다. 대조 로직은 **내가 방금 한 일**을 먼저 빼고 봐야 한다.
- 🆕 **경계값 검증에 `int(가격 * (1 + 목표비율))`을 쓰지 말 것** (2026-08-13, 08-15).
  부동소수 오차로 목표에 **1e-17 모자라** 멀쩡한 코드가 실패로 보인다
  (실측: `int(10000*(1+0.025+0.0023))` -> 순 2.4999999...%).
  **실물 `_net_rate`로 경계 가격을 역산**해서 판정할 것. 두 세션 연속 밟았다.
- 🆕 **'최근 N일 발동 0건'을 '앞으로도 안 돈다'로 읽지 말 것** (2026-08-15).
  08-13에 본전스톱 바닥을 올리며 "08-11~13 발동 0건이라 **당장 효과는 거의 0**"
  이라 적었는데, **바로 다음 거래일에 5건**(그날 청산 8건 중 5건) 발동했다.
  발동 빈도는 장세가 정한다 — 표본 없음은 '안전'이 아니라 **'미검증'**이다.

\### 즉시 되돌리는 법 (한 번에 하나씩!)

| 대상 | 방법 |
|---|---|
| **실전 -> 모의** | `config.ini`의 `IS_MOCK = true` + **모의 APP_KEY/SECRET_KEY로 교체** |
| **조건검색 0종목** | HTS [0156] '대상'과 `kiwoom_ws.CONDITION_STEX_TP` 일치. **현재 `"K"`(KRX)** / 통합=`"A"`. ⚠️ 08:59 스냅샷이 0종목인 것은 KRX 개장 전이라 **정상**일 수 있다 — 09:10까지 실시간 편입도 0이면 그때 의심할 것 |
| **매수금액** | **3곳 동시**(위 반복 패턴 참고). 현재 **100만원**(08-06 사용자 지정, 200만 -> 100만) |
| **버스트 주가 스케일** | `BURST_PRICE_ALPHA = 0.0` -> 전 종목 계수 1.0. 상한은 `BURST_PRICE_MAX`(**3.0**, 73,704원 이상에만 적용) |
| 🆕 **VWAP 깊은 검사** | `VWAP_ENTRY_CHECK_DEEPEST = False` -> 계획 생성을 트리거가로 검사(08-07까지의 동작, **죽은 구간 부활**) |
| 🆕 **우선순위 교체 한도** | `PHASE1A_PRIORITY_MAX_PER_DAY`(6). 0으로 두면 교체 기능이 완전히 꺼진다 |
| 🆕 **개장초반 슬롯 캡** | `EARLY_SLOT_CAP_ENABLED = False` -> 09:05 이전에도 공유 상한(6)까지 쓴다. 칸수는 `EARLY_SLOT_CAP`(4), 적용 종료는 `EARLY_SLOT_CAP_UNTIL`(09:05 — 보통 `FIRE_GATE_ACCEL_FROM`과 같게 둔다) |
| 🆕 **발사 게이트 분리** | `FIRE_GATE_SPLIT_ENABLED = False` -> 전 구간 옛 버스트로 완전 복귀. 문턱만 조이려면 `FIRE_ACCEL_MIN`(2.5 -> 3.0이면 하루 10.8 -> 5.6종목). 전환시각은 `FIRE_GATE_ACCEL_FROM`(09:05, 09:00으로 두면 전 구간 가속도). ⚠️ `FIRE_ACCEL_MIN_TICKS`(20)는 **금액이 아니라 개수** 하한이다 |
| **[B] 버스트 매수 방향** | `BURST_REQUIRE_BUY_SIDE = False` -> 08-05까지의 동작(투매도 버스트로 셈). ⚠️ 이걸 끄면 "-%에서 매수"가 되살아난다 |
| **[C] 등락률 하한** | `MIN_ENTRY_CHANGE_PCT = -100.0` -> 사실상 무제한(하한 없음). 현재 **0.0** = 전일종가 대비 + 일 때만 매수 |
| 🆕 **매수 주가 상한**(08-13) | `MAX_ENTRY_PRICE = 0` -> 상한 없음(08-12까지의 동작). 현재 **100,000**(이 가격 **이상** 신규매수 금지). ⚠️ 물타기는 bypass라 안 받는다 |
| **[D] 상승 이탈** | 현재 **OFF**. 되살리려면 `ENTRY_BREAKOUT_ENABLED = True` (폭 `ENTRY_BREAKOUT_PCT` 0.003은 보존돼 있다) |
| **[E] 눌림목 매매** | `PULLBACK_MAX_SLOTS = 4` **+ `PHASE1A_MAX_SLOTS = 4`** — ⚠️ **반드시 둘 다**. 눌림만 되살리고 1A를 8로 두면 1A가 6칸을 독식한다 |
| **[F] 진입 숙성** | `MIN_ENTRY_DELAY_SEC = 0.0` -> 편입 즉시 매수 가능(08-05까지의 동작) |
| **[H] 파동 상한** | `BURST_WAVE_MAX = 999` -> 파동 제한 없음. 쿨다운은 `BURST_WAVE_COOLDOWN_SEC`(60초) |
| 🆕 **파동 카운트 기준** | `BURST_WAVE_COUNT_REQUIRES_BURST = False` -> 08-08 사양(면제 구간에서 게이트 통과마다 카운트). ⚠️ **되돌리면 거래대금 0원 종목도 60초마다 파동이 쌓여 09:15까지 67%가 하루 종일 매수 불가가 되는 결함이 부활한다** |
| **되돌림 끄기(즉시매수)** | `ENTRY_PULLBACK_ENABLED = False` / 폭은 `ENTRY_PULLBACK_TRANCHES` |
| **시가대비 상한** | `PHASE1A_LEADING_OPEN_SURGE_CAP`(8.0, 구값 5.0) |
| **급등 버스트 강화** | `PHASE1A_OPEN_SURGE_STRICT_FROM`(6.0) / `PHASE1A_OPEN_SURGE_BURST_MULT`(1.5). 배수 1.0이면 무효 |
| **VI 상단 매수차단** | 현재 **OFF**. 되살리려면 `VI_UPPER_ENTRY_BLOCK_ENABLED = True`. ⚠️ 그때 **당일 고가 캐시로 과잉차단도 같이 고칠 것**(08-05 PS일렉 사례) |
| **VI 상단 확정매도** | `VI_UPPER_EXIT_ENABLED = False` |
| **재매수 대금** | `REBUY_BURST_VALUE_MULT`(2.5) |
| **추가매수 끄기** | `RESCUE_ADD_ENABLED = False` |
| 🆕🆕 **확정익절 2%**(08-15) | `FLAT_TP_ENABLED = False` -> **본전스톱(무장 2.5% / 바닥 1.0%)으로 즉시 복귀**. 문턱만 조이려면 `FLAT_TP_RATE`(0.020 -> 0.025면 실측상 더 낫다). 🔴 둘은 **상호 배타**(elif)라 동시에 살지 않는다 |
| 🆕 **본전스톱/확정익절 50% 분할**(08-14) | `BREAKEVEN_EXIT_PARTIAL = False` -> 종전대로 전량 청산. **08-15부터 확정익절도 이 스위치를 공용으로 쓴다**(False면 확정익절도 전량). 잔량 트레일 폭은 `PARTIAL_EXIT_TRAIL`(3%) 공용. ⚠️ 실측상 **전량이 성과는 더 낫다**(+2% 전량 0.657 vs 분할 0.360) — 분할은 상승 참여를 남기는 선택이다 |
| **본전스톱** | `BREAKEVEN_STOP_ENABLED` / 무장 `BREAKEVEN_TRIGGER`(**0.025**, 08-10에 0.010에서 상향 — 🔴 **낮추지 말 것**, 08-13 격자에서 4개 열 전부 악화) / 바닥 `BREAKEVEN_FLOOR`(🆕 **0.010**, 08-13에 0.002에서 상향. 롤백 0.002) |
| 🆕 **익절캡**(08-10 상향) | `EXIT_POLICY['default']['take_profit_cap']`(0.06) / `TAKE_PROFIT_CAP_PULLBACK`(0.06) / `TAKE_PROFIT_CAP_EARLY`(0.06) / `TP_CAP_UPGRADED_MAX`(0.08). ⚠️ **본전스톱과 세트로** 되돌릴 것 |
| 🆕 **정체·시간정리** | `STAGNANT_EXIT_ENABLED = True` -> 08-01~08-10 사양 부활(정체 15분 / 시간 30분) |
| 🆕 **변동성 손절** | `STOP_LOSS_VOL_ENABLED = False` -> 전 종목 고정 `STOP_LOSS_RATE`. 배수 `STOP_LOSS_VOL_MULT`(1.5) / 클램프 `STOP_LOSS_VOL_MIN`(**-0.045**)~`STOP_LOSS_VOL_MAX`(-0.055) |
| 🆕 **손절 -4.5%**(08-11) | `phase_settings.EXIT_POLICY["default"]["stop_loss_rate"]`(-0.045) + `STOP_LOSS_VOL_MIN`(-0.045). ⚠️ **`RESCUE_ADD_OBSERVE_FLOOR`(0.055)와 세트다** — 되돌릴 땐 0.045로 같이 내릴 것(안 그러면 관찰 창이 과하게 넓어진다) |
| 🆕 **-3% 물타기**(08-11) | `AVG_DOWN_ENABLED = False` -> 완전 OFF. 문턱 `AVG_DOWN_TRIGGER`(0.03) / 종목당 상한 `AVG_DOWN_MAX_AMOUNT`(200만) / 지수가드 중 금지 `AVG_DOWN_BLOCK_ON_INDEX_GUARD`(True). ⚠️ **백테스트 판정 불가**(부호 뒤집힘) — 실거래로 채점 중 |
| 🆕 **물타기 11시 컷오프**(08-12) | `AVG_DOWN_CUTOFF = time(15, 10)` -> 08-11까지의 '시간 제한 없음'과 동일. ⚠️ 그러면 **15:09에도 물타기가 발동**한다(실측) |
| 🆕 **물타기 재시도 상한**(08-12) | `AVG_DOWN_MAX_RETRY`(3) / `AVG_DOWN_RETRY_COOLDOWN_SEC`(5.0). ⚠️ **크게 두거나 없애면 주문 폭주가 부활한다** — 예수금 부족 시 매 체결틱마다 주문이 나가 REST 429로 **매도까지 밀린다**(실측 50틱 -> 50회) |
| 🔴🔴 **15:10 강제청산**(08-12 폐지) | `order_manager.FORCE_CLOSE_ENABLED = True` -> 당일청산 복귀. **이게 이 봇의 원래 전제였다.** 되살리면 오버나이트가 사라지고 `OVERNIGHT_RESTORE_AS_MANUAL`도 사실상 무의미해진다 |
| 🆕 **지수가드 14:50 청산**(08-12 폐지) | `INDEX_GUARD_FORCE_CLOSE_ENABLED = True`. ⚠️ 11:30까지의 **1단계 본전청산은 끄지 않았다**(익절 계열이라 손실을 확정하지 않는다) |
| 🆕 **오버나이트 격리**(08-12) | `OVERNIGHT_RESTORE_AS_MANUAL = False` -> 전일 보유분도 봇이 그대로 이어받아 손절·익절 대상이 된다. 🔴 판별은 `buy_time`의 **날짜**라 **장중 재시작(당일분)은 어느 쪽이든 정상 복원**된다 |
| 🆕 **진입 유효창**(08-12 저녁) | `MAX_ENTRY_AGE_SEC = 0` -> 상한 없음(08-12까지의 동작). 현재 **1800**(편입 후 30분). 숙성(하한)과 세트로 [30초~30분] 창을 만든다. ⚠️ 기준은 `_first_seen`(**첫 편입**, 재편입해도 불변) |
| 🆕 **VI 50% 분할**(08-12 저녁) | `VI_UPPER_EXIT_PARTIAL = False` -> 종전대로 전량 확정매도. 잔량 트레일 폭은 `PARTIAL_EXIT_TRAIL`(3%) 공용 |
| 🆕🆕 **분할 잔량 하한**(08-18) | `PARTIAL_EXIT_FLOOR_AT_FIRST = False` -> 08-18 이전 동작(고점 대비 트레일만). 현재 **True** = 잔량 트레일 발동가가 `max(고점x(1-트레일), 1차 매도가)`다. ⚠️ 되돌리면 **트레일이 1차 매도가보다 싸게 파는 구간이 부활한다** — 실측 5건 중 3건이 그랬다(LB세미콘 -1.90% / 다날 -1.86% / 심텍 -0.19%) |
| 🆕 **분할 잔량 보호**(08-12 저녁) | `PARTIAL_EXIT_REMAINDER_HOLD = False` -> 동적캡 재발동 시 잔량을 전량 청산(08-12까지의 동작). ⚠️ 되돌리면 **분할 1차 후 45초 만에 잔량이 털리는** 동작이 부활한다 |
| 🆕 **편입가 앵커**(08-12, **이미 OFF**) | `ENTRY_ANCHOR_SECOND_TRANCHE`(현재 **False**). 켰다가 같은 날 껐다 — 시뮬레이션 결함(2차 미체결 시 평단 상승을 누락) + 구간마다 방향 반대. `_cond_hit_prices` 캐시는 재검증용으로 계속 쌓인다 |
| 🆕 **수동 추가매수 합산**(08-12 결함수정) | `main.py`의 `if server_qty > tracked_qty:`를 `elif`로 되돌리면 **기능이 다시 죽는다**(도달 불가). 되돌릴 이유가 없다 |
| 🆕 **분할매도 x 잔고지연 가드**(08-15 결함수정) | `main.RECONCILE_SELL_GRACE_SECONDS = 0` -> 08-15 이전 동작. ⚠️ **되돌리면 봇이 자기 분할매도를 '수동 추가매수'로 오인해 수량을 되살리고, 그 결과 물타기가 2배를 산다**(실물 A/B 재현: OFF 100주 / ON 50주). 되돌릴 이유가 없다 |
| 🆕 **확인 전용 조건식**(08-11) | `CONFIRM_ONLY_CONDITIONS = ()` -> `돌파후` 단독으로도 매수한다. 🔴 비우면 `resolve_strategy` 폴백 때문에 **확인용 검색식이 그대로 매수 소스가 된다** |
| 🆕 **조건식 이름**(08-11) | `config.ini` `CONDITION_NAMES` + `strategy_manager`의 `source_flags`/`cond_perf_key`/성과등급 튜플/`MIN_ENTRY_DELAY_SEC_BY_COND` + `condition_manager.CONDITION_STRATEGIES`. **6곳이 같이 움직여야 한다** |
| 🆕 **수동 추가매수 합산**(08-11) | `main.py:_reconcile_manual_sells`의 `server_qty > tracked_qty` 분기를 제거. 되돌리면 사용자가 HTS에서 더 산 물량을 봇이 **모르고 방치**한다(손절·15:10 대상 밖) |
| 🆕 **반등소진 매도** | `LOSS_REBOUND_REQUIRE_BREAKEVEN = False` -> 손실 구간 전용(구 사양). ⚠️ **되돌리면 08-10에 8건 -50,636원을 만든 동작이 부활한다** |
| 🆕 **우선순위 교체** | `PHASE1A_PRIORITY_MAX_PER_DAY`(**0** = 완전 OFF). 되살릴 땐 6이 아니라 **2~3** 또는 '09:05 이전 금지'를 먼저 검토할 것 |
| **무장 시간** | `TICK_STRENGTH_SUSTAIN_SEC`(**3.0**, 08-07 복원. 1.5로 두면 08-03 저녁 사양) |
| **조건검색 거래소** | `kiwoom_ws.CONDITION_STEX_TP`(**"K"**=KRX, 08-07 전환). 통합은 `"A"` — ⚠️ **HTS [0156] '대상'도 같이** 바꿀 것 |
| **VWAP 진입필터** | 현재 **ON**. 끄려면 `VWAP_ENTRY_ENABLED = False`. 폭 `VWAP_ENTRY_MIN_GAP_PCT`(0.5) / 시작 `VWAP_ENTRY_FROM`(09:05). 단위 `VWAP_UNIT_SCALE`(**1_000_000.0** = 백만원, 08-07 실측 확정 — 되돌릴 일 없음). ⚠️ `VWAP_ENTRY_FROM`을 09:05보다 앞당기면 FID14 양자화 오차가 판정 문턱을 넘을 수 있다 |
| **숙성 조건식별 예외** | `MIN_ENTRY_DELAY_SEC_BY_COND = {}` -> 전 종목 60초로 복귀. 현재 `{"돌파전": 30.0}`. 기본값은 `MIN_ENTRY_DELAY_SEC`(60.0) |
| **스냅샷 폴링** | `main.py` gather에서 `self.task_condition_snapshot_poll(),`를 주석 처리. 주기는 `POLL_INTERVAL_SEC`(60). ⚠️ 장중에 **체결틱 끊김이나 `concurrent recv` 에러**가 보이면 즉시 끌 것 |
| **일봉 눌림 태그** | `DAILY_PULLBACK_TAG_ENABLED = False` -> 판정도 REST도 완전히 꺼진다. **관측 전용이라 매매엔 영향이 없다**(끌 이유는 REST 절약뿐) |
| **버스트 문턱** | `PHASE1A_BURST_TRADE_VALUE`(4천만) |
| **지수 가드** | `INDEX_GUARD_ENABLED` (임계는 SEVERE_CRASH와 같은 값 유지) |
| **오후장 접기** | `PHASE1A_END`/`PULLBACK_END`를 `time(13, 0)`으로 |

